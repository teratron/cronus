# core (Rust library)

The Cronus engine: orchestration, memory, scheduler, routers, quality gates,
Kanban board state, office projection, storage. Frontends and apps depend on this;
it depends only on libraries (incl. the external workflow-runtime crate), never on a frontend.
