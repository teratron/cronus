[package]
name = "cronus-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "cronus"
path = "src/main.rs"

[dependencies]
clap = { workspace = true }
clap_complete = { workspace = true }
cronus-codegraph = { workspace = true }
cronus-contract = { workspace = true }
cronus-core = { workspace = true }
cronus-tui = { workspace = true }
nodus = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
# The shared conformance corpus (§4.4/§4.5) — a surface registers by driving
# it from its own test target, never from product code, so this stays a
# dev-dependency only.
cronus-conformance = { workspace = true }
