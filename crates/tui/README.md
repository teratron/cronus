# tui (Rust library)

The terminal UI over `core`, launched via `cronus tui` (see `crates/cli`, the single binary and composition root — this crate has no `[[bin]]` target of its own). No domain logic.
