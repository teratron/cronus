# Master Task Index (Registry)

**Version:** 1.27.0
**Generated:** 2026-07-17
**Based on:** .design/main/PLAN.md v2.25.0
**Based on RULES:** .design/RULES.md v1.5.0
**Execution Mode:** Parallel
**Status:** Active

## Overview

Tactical registry of all phases and their statuses, ordered by the growth metaphor (seed → stem → leaf → flower). Atomic checklists live in `tasks/phase-{N}.md`. Build phases 1–17 are Done (16 + 17 archived). **Phase 18 (Service Activation) is now the open buildable phase** — `l2-service-activation` reached Stable via `/magic.spec` (RFC→Stable 1.0.1, three TBDs resolved as design decisions + two bundled reconciliation amendments) and is decomposed into 8 tasks / 6 tracks (A seam+policy · B BA-4 barriers · C state-root lock · D OS adapters · E command surface · T validation), status Todo. Realizes how a host becomes the always-on INV-4 hub under one constraint — the background engine runs **as the user, never a superuser** — with BA-4 (activation is a SEC-10 authority grant the agent has no structural write path to) as the load-bearing security property. A real-integration phase (Phase 17 precedent — genuine platform I/O behind an adapter crate, `SystemCalls`-seam testability, no live-OS dependency in CI). Next step: `/magic.run main` to execute Phase 18. The remaining 7 Backlog items (6 RFC + 1 Draft) each await their own `/magic.spec` review.

## Active Phases

| Phase | Description | Status |
| --- | --- | --- |
| [Phase 1](tasks/phase-1.md) | Seed I — Foundation: workspaces, filesystem, core skeleton, security | `Done` |
| [Phase 2](archives/tasks/phase-2.md) | Seed II — Workflow runtime (`crates/nodus`) port | `Done (Archived)` |
| [Phase 3](archives/tasks/phase-3.md) | Stem — CLI (command framework + grammar + core bindings) | `Done (Archived)` |
| [Phase 4](archives/tasks/phase-4.md) | Core Subsystems: memory store, model/context routers, workspace management | `Done (Archived)` |
| [Phase 5](archives/tasks/phase-5.md) | Office Work Engine: role catalog, kanban board, scheduler, quality pipeline, extension registry | `Done (Archived)` |
| [Phase 6](archives/tasks/phase-6.md) | Orchestration & Autonomy: delegation, /goal+judge+budget, briefings | `Done (Archived)` |
| [Phase 7](archives/tasks/phase-7.md) | Leaf — TUI (interactive terminal) | `Done (Archived)` |
| [Phase 8](archives/tasks/phase-8.md) | Flower — Desktop App: application UI, office view, dashboard | `Done (Archived)` |
| [Phase 9](archives/tasks/phase-9.md) | Operational Hardening: sandbox policy, multi-user auth, doctor, config hot-reload, backup, agent migration, GitHub issue reporting, self-improvement, telemetry | `Done (Archived)` |
| [Phase 10a](archives/tasks/phase-10.md) | Advanced Office Features (L2) — Foundational Wave: office-control, acp, navigation, automation-pipeline + kanban KAN-8 delta → 15 tasks / 5 tracks (A–E) | `Done (Archived)` |
| [Phase 10b](tasks/phase-10b.md) | Advanced Office Features (L2) — Dependent Wave: automation-canvas, voice-input, deliberation, version-control, inner-monologue, lookahead-planning, global-orchestration → 18 tasks / 7 tracks (F–L) | `Done` |
| [Phase 11](archives/tasks/phase-11.md) | Content, Sharing & Dev-Workflow Subsystems: resource-sharing, file-store, notes, development-workflow → 8 tasks / 4 tracks; +26 tests | `Done (Archived)` |
| [Phase 12](archives/tasks/phase-12.md) | Skill System (L2): two-tier stores, canonical package, built-in command surface, conversion pipeline, prompt synthesis, `cronus skill` verbs → 8 tasks / 5 tracks (A–D, T) | `Done (Archived)` |
| [Phase 13](archives/tasks/phase-13.md) | Core Decomposition (Crate Topology): repartition `crates/core` into contract · domain · store-local · auth-local · facade; invert the one domain→infra edge; DN-2 seams as crate boundaries; CI boundary guard → 8 tasks / 5 tracks (A–D, T) | `Done (Archived)` |
| [Phase 14](archives/tasks/phase-14.md) | Memory Intelligence & Consolidation (L2): realize `l2-memory-consolidation` (MC-1…MC-10) + `l2-memory-intelligence` at query-surface scope (MI-1/2/3/4/5/7/8/9/13) over the SQLite substrate behind the UserDataStore seam — fact-vs-derived signal table, multiplicative ranking, corpus-maintenance pass, grounded `answer`, temporal modes, conflict routing, lifecycle states, gated experience reuse → 8 tasks / 4 tracks (A schema · B consolidation · C intelligence · T) | `Done (Archived)` |
| [Phase 15](archives/tasks/phase-15.md) | Memory Capture Policy & Metadata (L2): the MI-6/10/11/12 capture-path delta deferred at Phase 14 — salience-gated capture policy + actor/expiry/subject/cross-ref metadata (MI-6), capture-time date normalization (MI-10), caller capture directives (MI-11), raw/inferred mode (MI-12), all behind the UserDataStore seam → 5 tasks / 3 tracks (A capture-metadata schema · B capture path · T) | `Done (Archived)` |
| [Phase 16](archives/tasks/phase-16.md) | Project Wiki Store (L2): realize `l2-project-wiki` as a per-office SQLite projection **cache** (`wiki/wiki.db`), not loose Markdown — `wiki_page` (parent/ord tree, citations, source_fingerprint, stale) + `wiki_changelog` + `wiki_page_fts`; event-driven incremental transactional regeneration from ground truth (board/decisions/ledger/deliverables) with `rebuild()` as the projection-not-source proof (PW-3/4/5), read-only client access (PW-2), access-grants Read gate (PW-7), internal-detail content filter (PW-8) → 7 tasks / 4 tracks (A schema · B regeneration · C read/access surface · T) | `Done (Archived)` |
| [Phase 17](archives/tasks/phase-17.md) | Model Transport & Provider Connectivity (L2): realize `l2-model-runtime` — a NEW `InferenceBackend` trait in `crates/contract` (streaming call surface the routing-metadata `ModelProvider` lacks) realized by a new `crates/model-local` REST transport (blocking HTTP+TLS, endpoint profiles over router `api_base`, worker-thread pull-iterator + cancellation, embed/describe/pull, failure→error-recovery-taxonomy no-retry, egress-gated remote); facade wiring + host-side nodus bridge collapsing the stream to satisfy `nodus::ModelProvider`; `model-local` registered as peer infra crate + CI boundary guard. Closes the audit keystone (selection implemented, no seam could call). Real-integration phase, no async runtime, hermetic mock-HTTP tests → 7 tasks / 5 tracks (A contract trait · B transport crate · C facade+bridge · D topology+guard · T) | `Done (Archived)` |
| [Phase 18](tasks/phase-18.md) | Service Activation (L2): realize `l2-service-activation` — how a host becomes the always-on INV-4 hub, under one constraint (the engine runs as the user, never a superuser). The `ActivationRegistry` seam (`capabilities`/`observe`/`enable`/`disable`, no persisted mirror — BA-8) + domain transition state machine (mutual exclusion + remove-then-register-verify, converge-on-`Inactive`) + the three structural BA-4 barriers (no agent tool / no sandbox write path / no elevation) + per-OS adapters behind a `SystemCalls` seam (Windows `Run`/S4U task, macOS `SMAppService`/`LaunchDaemon`, Linux systemd user/system/linger + XDG-autostart) + the state-root lock realizing attach-never-duplicate (BA-11) + `cronus activation status\|enable\|disable`. Real-integration, seam-first → 8 tasks / 6 tracks (A seam+policy · B BA-4 barriers · C state-root lock · D OS adapters · E command surface · T) | `Todo` |

## Meta Information

- **Last Updated**: 2026-07-17 (v1.27.0: **opened Phase 18 — Service Activation**. `l2-service-activation` promoted RFC→Stable via `/magic.spec` (1.0.1 — three TBDs resolved as design decisions: non-systemd Linux → XDG-autostart login + system Unsupported; `tauri-plugin-autostart` rejected per std-first policy; Windows S4U design-settled, timing → build-time gate) and decomposed into 8 tasks / 6 tracks (A seam+policy · B BA-4 barriers · C state-root lock · D OS adapters · E command surface · T validation). Realizes how a host becomes the always-on INV-4 hub under one constraint — the engine runs as the user, never a superuser — with BA-4 (activation is a SEC-10 authority grant the agent has no structural write path to) as the load-bearing security property. Real-integration, seam-first (Phase 17 precedent): `ActivationRegistry` seam + no-op adapter first (BA-8/BA-10 testable before registration), sandbox deny entries before the first `enable()` (BA-4 barrier precedes the capability), login-scoped before system-scoped, Windows last (S4U launch-timing gate); raw OS calls behind a `SystemCalls` seam so adapter logic is CI-testable against a fake. Critical path = T-18A01 (the `ActivationRegistry` trait shape). Two bundled `/magic.spec` amendments rode the promotion: `l2-technology-stack` 1.2.1 (Windows-service→S4U reconciliation) + `l2-crate-topology` 1.0.1 (registered the shipped `model-local` peer adapter crate). Pre-flight clean (ok:true, 0 orphans, no header drift). Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent). Backlog 7 (6 RFC + 1 Draft). PLAN v2.25.0, INDEX v1.0.119. Prior: v1.26.0: **milestone sync — Phase 16 complete (7/7) and archived; no new phase, no open buildable phase remains**. The Project Wiki Store shipped end to end: per-office SQLite projection cache, office-owned transactional regeneration with PW-4 citation + PW-8 internal-detail guards + no-generator grounded-stub degrade, PW-5 fingerprint-drift freshness, structurally read-only client surface (PW-2/PW-6), `access-grants` Read gate (PW-7), and `rebuild()` as the projection-not-source proof (PW-3 — a real `wiki.db` file is deleted and an equivalent wiki re-derived from ground truth). `cargo test --workspace` green (exit 0, 64 result-sets) + clippy `-D warnings` + fmt clean. Pre-flight clean (ok:true, 0 orphans, no header drift). Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent `l1-loop-governance`). **Every currently-Stable spec is built; the Backlog stays 8 (7 RFC + 1 Draft), each advancing only via `/magic.spec`.** DA-3 topological next `/magic.spec` target: `l2-service-activation`; deferred mechanical follow-up: amend `l2-crate-topology` (model-local peer-crate row). PLAN v2.24.0. Prior: v1.25.0: **milestone sync — Phase 17 complete (7/7) and archived; no new phase**. The model-transport wave shipped end to end: `contract::InferenceBackend`, the `crates/model-local` REST transport (no async runtime; loopback + egress-gated remote), facade `NodusModelBridge` + `TransportCompactor`, and a self-tested CI `domain → model-local` boundary guard; two pre-existing `cronus-core` test-isolation races (mission_mode env, router BANDIT_COUNTER) were fixed to unblock the workspace-green gate. `cargo test --workspace` green (exit 0 ×3) + clippy `-D warnings` + fmt clean. **Phase 16 (Project Wiki Store) is the sole open buildable phase (DA-3 next).** Pre-flight clean (ok:true, no orphans, no drift). Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent `l1-loop-governance`). No new phase — every Stable spec is built or covered by an open phase. Deferred follow-up: `/magic.spec` amend `l2-crate-topology` (model-local peer-crate row). PLAN v2.23.0. Prior: v1.24.0: **opened Phase 17 — Model Transport & Provider Connectivity**. `l2-model-runtime` promoted RFC→Stable via `/magic.spec` (1.0.0→1.0.1, one Post-Update Review fix — the transport's generate surface was wrongly attributed to the routing-metadata `contract::ModelProvider`; corrected to a NEW `InferenceBackend` trait + two-facet provider + stream-collapsing nodus bridge) and decomposed into 7 tasks / 5 tracks (A contract inference trait · B model-local transport crate · C facade + nodus bridge · D topology + CI guard · T validation). Closes the concept-conformance audit keystone: model selection implemented while no seam could perform the call. Real-integration phase (spec-sanctioned HTTP+TLS client, hermetic mock-HTTP tests, no async runtime), first non-domain-logic-first phase since Phase 13. Foundation-then-parallel: Track A (`InferenceBackend` trait) gates B/C; B serialized on the shared crate + mock-server fixture. Now open alongside Phase 16 (both Todo) — by leverage the transport is higher-priority (unblocks triage classification, compaction, nodus `gen`), but neither depends on the other. Pre-flight clean (ok:true, 0 orphans, no header drift). Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent). PLAN v2.22.0, INDEX v1.0.118. Prior: v1.23.1: **registry-sync — audit wave absorbed; no task changes**. PLAN v2.21.0 (based-on INDEX v1.0.117) absorbed eleven Stable `concept-only` L1s into Phase 0 and parked `l2-model-runtime` (RFC, the audit's transport keystone) in Backlog; Phase 16 (Project Wiki Store) unchanged, still the open buildable target (7 tasks / 4 tracks, Todo, ready for `/magic.run main`). Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent). DA-3 next `/magic.spec` review target: `l2-model-runtime`. Prior: v1.23.0: **opened Phase 16 — Project Wiki Store**. `l2-project-wiki` promoted RFC→Stable via `/magic.spec` (1.0.0→1.0.1, one Post-Update Review fix — a `rebuild` byte-equivalence overclaim corrected to informational equivalence; spec-critic + prompt-engineer PASS) and decomposed into 7 tasks / 4 tracks (A schema · B regeneration · C read/access surface · T). Domain-logic-first, foundation-then-parallel: Track A (wiki_page/wiki_changelog/wiki_page_fts schema) gates B/C; Track B serialized on the shared regeneration entry point (B01 write path → B02 grounding/honesty guards → B03 freshness). PW-3 projection-not-source is the load-bearing acceptance property (`rebuild` reconstructs an equivalent wiki + drop-loses-nothing); PW-2 read-only-to-client is structural (no mutating client method); generator-optional degrade required (no-generator run stores a grounded stub, never fabricates). Pre-flight clean (ok:true, no header drift). Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent). DA-3 next target: Phase 16. INDEX v1.0.107, PLAN v2.20.0. Prior: v1.22.0: **plan-sync — wiki-store wave + MEM-4 amendment; no new phase**. `l1-project-wiki` gained an authored L2 `l2-project-wiki` (`RFC`, parked in Backlog per C6) realizing the client-facing wiki as a per-office SQLite projection cache (`wiki/wiki.db`), not loose Markdown — rebuildable cache of upstream truth, contrasted with the memory learned-tier where the store IS truth. Absorbed MEM-4 v1.1 (`l1-memory-model` 1.1.0 / `l2-memory-store` 1.4.0 / `l2-memory-consolidation` 1.0.2) — spec-only, no code delta (shipped `crates/store-local` already store-authoritative, no notes/*.md dual-write). Pre-flight clean (ok:true, no header drift). Pre-Planning Stabilization: 0 promoted (RFC→Stable is a `/magic.spec` act, not `/magic.task`; sole Draft `l2-loop-runner` stays Draft — RFC parent). No buildable phase opened — a wiki-implementation phase opens only when `l2-project-wiki` reaches Stable via `/magic.spec`. DA-3 buildable next target unchanged: Phase 15. INDEX v1.0.106, PLAN v2.19.0. Prior: v1.21.0: **Phase 14 complete + archived; opened Phase 15 — Memory Capture Policy & Metadata** (post-run replan). Phase 14 delivered 9/9 tasks — `l2-memory-consolidation` whole (MC-1…MC-10) and `l2-memory-intelligence` at query-surface scope (MI-1/2/3/4/5/7/8/9/13); workspace `cargo test` 1,333 passed / clippy `-D warnings` / fmt clean. The four capture-path invariants (MI-6/10/11/12) were deferred at delivered scope — each needs net-new capture-metadata schema (actor/expiry/subject/cross-ref) and forms one coherent write-path unit — and carry forward to Phase 15 as an explicit delta (source-layout 1.1.0/1.2.0 precedent: Phase 14 keeps `[x]` at delivered scope, immutable; the remainder is a new line item, never a reopen). Phase 15 = 5 tasks / 3 tracks (A capture-metadata schema · B capture path · T), domain-logic-first, foundation-then-parallel; no `/magic.spec` needed (spec Stable, Invariant Compliance table specifies each net-new item). Pre-flight clean (ok:true, no header drift). Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent). PLAN v2.18.0. Prior: v1.20.0: **opened Phase 14 — Memory Intelligence & Consolidation**. Both memory L2s (`l2-memory-consolidation` 1.0.1, `l2-memory-intelligence` 1.0.0) promoted RFC→Stable via `/magic.spec` (INDEX v1.0.101, 188 Stable) — one review fix in consolidation (§4.4/MC-8 reworded from "subsumes §4.2" to explicitly compositional, no store edit). Decomposed into 8 tasks / 4 tracks (A schema · B consolidation · C intelligence · T). Domain-logic-first, foundation-then-parallel: Track A (memory_signal table + depth/lifecycle columns) gates B/C; cross-edges MC-8→C-recall and MC-6-archive→MI-9-lifecycle respected; generator-optional degrade paths are build requirements. PLAN v2.17.0. Prior: v1.19.2 plan-sync (memory wave to Phase 0 + Backlog); v1.19.1 milestone (Phase 13 archived) (8/8 tasks Done; `l2-crate-topology` + `l2-source-layout` 1.2.0 realized — five-crate topology, INV-8 compiler-enforced, CI boundary guard live). All thirteen phases Done; every Stable spec implemented; no phase open. Pre-flight clean (ok:true, no drift). Pre-Planning Stabilization: 0 promoted — sole Draft `l2-loop-runner` stays Draft (RFC parent `l1-loop-governance`, layer constraint). Ten Backlog items all RFC/Draft — advance only via `/magic.spec`. DA-3 next target: `l2-service-activation` (direct downstream of Phase 13's crate seam). PLAN v2.15.1. Prior: v2.15.0 opened Phase 13 from the core-decomposition wave; v2.14.1 milestone — all 12 build phases Done)
- **Maintainer**: Core Team
