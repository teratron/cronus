# Master Task Index (Registry)

**Version:** 1.37.0
**Generated:** 2026-07-22
**Based on:** .design/main/PLAN.md v2.35.0
**Based on RULES:** .design/RULES.md v1.5.0
**Execution Mode:** Parallel
**Status:** Active

## Overview

Tactical registry of all phases and their statuses, ordered by the growth metaphor (seed → stem → leaf → flower). Atomic checklists live in `tasks/phase-{N}.md`. **All build phases 1–21 are Done** — Phase 21 (Knowledge Store) completed all 8 tasks / 5 tracks (A schema+types · B ingestion · C retrieval · D authorship+curation+access+CLI · T validation): the agent-queryable RAG subsystem shipped end to end — named access-controlled collections with hybrid semantic (real `sqlite-vec` `vec0` ANN) + keyword (FTS5) retrieval fused by RRF (k=60); an ingestion pipeline over all three KB-5 source types (file/URL/record) with atomic KB-3 re-indexing (`reindex_chunks`); storage-enforced KB-9 authorship zones (a typed `WriteOverride`) and a KB-10 curation lifecycle with a `min_curation` floor; an optional KB-11 `QueryPreparer` seam; a KB-4 `GatedKnowledge` access gate (the `GatedWiki` precedent); and a `KnowledgeService` facade + `cronus knowledge` CLI, proven by a real CLI smoke run. Two genuine design gaps found and fixed mid-phase (not user feedback): a KB-3 chunk-replacement atomicity gap and a KB-9/index-state-bookkeeping conflation gap — plus a real upstream `sqlite-vec` packaging bug root-caused and fixed via `.cargo/config.toml`. 69 tests across 6 new files; `cargo test --workspace` green across 3 consecutive full runs (0 failed) + clippy `-D warnings` + fmt clean. **Phase 22 (Developer Office) is complete (7/7) and archived** — the self-hosting developer office shipped end to end: a conditional `WorkspaceKind::Developer` that materializes only in a genuine canonical-repo checkout for a human-admitted developer, an orchestration layer over already-shipped subsystems with DVO-3 agent-self-escalation structurally unrepresentable, DVO-4 observe-not-remember held by construction, and DVO-8's no-exception-lane proven structurally (the dev-workflow `Pipeline` takes no workspace-identity parameter at all). One real planning-time gap — `l1-tool-receipts` has no implementation anywhere in the project — was found and disclosed during execution rather than hidden; the real, already-shipped SEC-7 audit-log baseline was composed instead. `cargo test --workspace` green across 3 consecutive full runs + clippy + fmt clean. **With Phase 22 done, no buildable phase remains open** — every currently-Stable spec is now built. The remaining 2 Backlog items (both RFC, both concept-only L1) each await their own `/magic.spec` review before advancing.

## Active Phases

| Phase | Description | Status |
| --- | --- | --- |
| [Phase 1](tasks/phase-1.md) | Seed I — Foundation: workspaces, filesystem, core skeleton, security | `Done` |
| [Phase 2](archives/tasks/phase-2.md) | Seed II — Workflow runtime (`crates/nodus`) port | `Done (Archived)` |
| [Phase 3](archives/tasks/phase-3.md) | Stem — CLI (command framework + grammar + core bindings) | `Done (Archived)` |
| [Phase 4](archives/tasks/phase-4.md) | Core Subsystems: memory store, model/context routers, workspace management | `Done (Archived)` |
| [Phase 5](archives/tasks/phase-5.md) | Office Work Engine: role catalog, kanban board, scheduler, quality pipeline, extension registry | `Done (Archived)` |
| [Phase 6](archives/tasks/phase-6.md) | Orchestration & Autonomy: delegation, /goal+judge+budget, briefings | `Done (Archived)` |
| [Phase 7](archives/tasks/phase-7.md) | Leaf — TUI (interactive terminal) | `Done (Archived)` |
| [Phase 8](archives/tasks/phase-8.md) | Flower — Desktop App: application UI, office view, dashboard | `Done (Archived)` |
| [Phase 9](archives/tasks/phase-9.md) | Operational Hardening: sandbox policy, multi-user auth, doctor, config hot-reload, backup, agent migration, GitHub issue reporting, self-improvement, telemetry | `Done (Archived)` |
| [Phase 10a](archives/tasks/phase-10.md) | Advanced Office Features (L2) — Foundational Wave: office-control, acp, navigation, automation-pipeline + kanban KAN-8 delta → 15 tasks / 5 tracks (A–E) | `Done (Archived)` |
| [Phase 10b](tasks/phase-10b.md) | Advanced Office Features (L2) — Dependent Wave: automation-canvas, voice-input, deliberation, version-control, inner-monologue, lookahead-planning, global-orchestration → 18 tasks / 7 tracks (F–L) | `Done` |
| [Phase 11](archives/tasks/phase-11.md) | Content, Sharing & Dev-Workflow Subsystems: resource-sharing, file-store, notes, development-workflow → 8 tasks / 4 tracks; +26 tests | `Done (Archived)` |
| [Phase 12](archives/tasks/phase-12.md) | Skill System (L2): two-tier stores, canonical package, built-in command surface, conversion pipeline, prompt synthesis, `cronus skill` verbs → 8 tasks / 5 tracks (A–D, T) | `Done (Archived)` |
| [Phase 13](archives/tasks/phase-13.md) | Core Decomposition (Crate Topology): repartition `crates/core` into contract · domain · store-local · auth-local · facade; invert the one domain→infra edge; DN-2 seams as crate boundaries; CI boundary guard → 8 tasks / 5 tracks (A–D, T) | `Done (Archived)` |
| [Phase 14](archives/tasks/phase-14.md) | Memory Intelligence & Consolidation (L2): realize `l2-memory-consolidation` (MC-1…MC-10) + `l2-memory-intelligence` at query-surface scope (MI-1/2/3/4/5/7/8/9/13) over the SQLite substrate behind the UserDataStore seam — fact-vs-derived signal table, multiplicative ranking, corpus-maintenance pass, grounded `answer`, temporal modes, conflict routing, lifecycle states, gated experience reuse → 8 tasks / 4 tracks (A schema · B consolidation · C intelligence · T) | `Done (Archived)` |
| [Phase 15](archives/tasks/phase-15.md) | Memory Capture Policy & Metadata (L2): the MI-6/10/11/12 capture-path delta deferred at Phase 14 — salience-gated capture policy + actor/expiry/subject/cross-ref metadata (MI-6), capture-time date normalization (MI-10), caller capture directives (MI-11), raw/inferred mode (MI-12), all behind the UserDataStore seam → 5 tasks / 3 tracks (A capture-metadata schema · B capture path · T) | `Done (Archived)` |
| [Phase 16](archives/tasks/phase-16.md) | Project Wiki Store (L2): realize `l2-project-wiki` as a per-office SQLite projection **cache** (`wiki/wiki.db`), not loose Markdown — `wiki_page` (parent/ord tree, citations, source_fingerprint, stale) + `wiki_changelog` + `wiki_page_fts`; event-driven incremental transactional regeneration from ground truth (board/decisions/ledger/deliverables) with `rebuild()` as the projection-not-source proof (PW-3/4/5), read-only client access (PW-2), access-grants Read gate (PW-7), internal-detail content filter (PW-8) → 7 tasks / 4 tracks (A schema · B regeneration · C read/access surface · T) | `Done (Archived)` |
| [Phase 17](archives/tasks/phase-17.md) | Model Transport & Provider Connectivity (L2): realize `l2-model-runtime` — a NEW `InferenceBackend` trait in `crates/contract` (streaming call surface the routing-metadata `ModelProvider` lacks) realized by a new `crates/model-local` REST transport (blocking HTTP+TLS, endpoint profiles over router `api_base`, worker-thread pull-iterator + cancellation, embed/describe/pull, failure→error-recovery-taxonomy no-retry, egress-gated remote); facade wiring + host-side nodus bridge collapsing the stream to satisfy `nodus::ModelProvider`; `model-local` registered as peer infra crate + CI boundary guard. Closes the audit keystone (selection implemented, no seam could call). Real-integration phase, no async runtime, hermetic mock-HTTP tests → 7 tasks / 5 tracks (A contract trait · B transport crate · C facade+bridge · D topology+guard · T) | `Done (Archived)` |
| [Phase 18](archives/tasks/phase-18.md) | Service Activation (L2): realize `l2-service-activation` — how a host becomes the always-on INV-4 hub, under one constraint (the engine runs as the user, never a superuser). The `ActivationRegistry` seam (`capabilities`/`observe`/`enable`/`disable`, no persisted mirror — BA-8) + domain transition state machine (mutual exclusion + remove-then-register-verify, converge-on-`Inactive`) + the three structural BA-4 barriers (no agent tool / no sandbox write path / no elevation) + per-OS adapters behind a `SystemCalls` seam (Windows `Run`/S4U task, macOS `SMAppService`/`LaunchDaemon`, Linux systemd user/system/linger + XDG-autostart) + the state-root lock realizing attach-never-duplicate (BA-11) + `cronus activation status\|enable\|disable`. Real-integration, seam-first → 8 tasks / 6 tracks (A seam+policy · B BA-4 barriers · C state-root lock · D OS adapters · E command surface · T) | `Done (Archived)` |
| [Phase 19](archives/tasks/phase-19.md) | Loop Runner (L2): realize `l2-loop-runner` — the single governed place every autonomous loop runs. A loop runner + single governor for `crates/domain` (I/O-free logic; adapter-touching composition in the `crates/core` facade), composing execution-workspace/budget-engine/agent-autonomy/model-router/session-checkpoint/version-control + nodus provider seams: `LoopSpec`/`MutationManifest`/`Oracle`/`Ceiling`/`ObjectiveSlot` with `MutableArtifact` lacking a `Criteria` variant (LG-3 by the type system); execution-loop runner (worktree → fresh-context turn → governor write-guard → oracle verdict → finalize/VC-4 rollback); evolution-loop runner wrapping harness EVALUATE→ANALYZE→IMPROVE with nested execution scoring; single-point governor (check_ceiling/guard_writes/judge); oracle wiring (deterministic/model-router-judge/autonomy-human, reduced_confidence on lineage match); escalation gate (separated oracle + external novelty + held-out gain); LG-10 objective persistence; `cronus loop run\|evolve\|log\|show`. Real-integration composition phase; pure logic tested against fakes, real wiring in the facade → 8 tasks / 5 tracks (A types+governor · B execution-runner+oracle · C evolution-runner+escalation · D LG-10+facade+CLI · T) | `Done (Archived)` |
| [Phase 20](archives/tasks/phase-20.md) | Archetype Catalog (L2): realize `l2-archetype-catalog` — the concrete office-archetype system (OA-1…OA-11) for `crates/domain/src/archetype/`. A domain-scoped prior on staffing that is never a roster: the closed four-key `ArchetypeDefinition` schema + validator (a fifth key fails validation → OA-4 unrepresentable; every pool/seed role id resolves against `roles::catalog::PRESET_CATALOG` → OA-10; seed capped at two + mandatory justification → OA-2); the shipped `software-engineering` archetype (18-role pool, empty seed) with `advertising-agency`/`finance-department` declared-blocked; inferred selection + application (OA-7/OA-8/OA-11, archetype-free is complete); the three deviation counters with the pool read by the recorder never the hire gate (OA-3) + explicit `unvalidated` state (OA-9); `cronus archetype list\|info\|set\|create` (no `hire` verb). Domain-logic-first; ships software-engineering alone (role-catalog amendment + imported-archetype vetting are disclosed deferrals) → 6 tasks / 5 tracks (A schema+validation · B catalog+shipped+selection · C deviation recording · D command surface · T) | `Done (Archived)` |
| [Phase 21](archives/tasks/phase-21.md) | Knowledge Store (L2): the agent-queryable RAG subsystem — named access-controlled collections with hybrid semantic (real `sqlite-vec` `vec0`) + keyword (FTS5) retrieval fused by RRF (k=60); ingestion over all three KB-5 source types (file/URL/record) with atomic KB-3 re-indexing; storage-enforced KB-9 authorship zones + KB-10 curation lifecycle; KB-11 `QueryPreparer`; KB-4 `GatedKnowledge` access gate (the `GatedWiki` precedent); `KnowledgeService` facade + `cronus knowledge` CLI, proven by a real CLI smoke run. Two genuine design gaps found and fixed mid-phase (KB-3 atomicity via `reindex_chunks`; KB-9/index-state conflation via `update_document_status`) plus a real upstream `sqlite-vec` packaging bug fixed via `.cargo/config.toml` → 8 tasks / 5 tracks (A schema+types · B ingestion · C retrieval · D authorship+curation+access+CLI · T) | `Done (Archived)` |
| [Phase 22](archives/tasks/phase-22.md) | Developer Office (L2): the self-hosting developer office (DVO-1…DVO-8), an orchestration layer over shipped subsystems — a conditional `WorkspaceKind::Developer` materializing only in a genuine canonical-repo checkout (network-free `repo_authenticity` vs a compiled-in `CANONICAL_UPSTREAM`, fail-closed — DVO-2) for a human-admitted developer (admission on the `crates/auth-local` human-write-only plane read through a read-only `AdmissionReader` port, agent self-escalation unrepresentable — DVO-3, the BA-4/OA-4 pattern); trigger-loaded module gated on `DevOfficeGate::resolve == Elevated` with clean unload (DVO-4); a 3-state `AdmissionTier` with feedback tier default-off (DVO-5); repo-scoped isolation (DVO-6); confinement+audit composing `l2-tool-security` + a real SEC-7 audit log (DVO-7, `l1-tool-receipts` disclosed as project-wide unbuilt — composed the real primitive instead); unchanged `l2-development-workflow` engine, no exception lane (DVO-8); `cronus dev status\|admit\|revoke`, proven by a real CLI smoke round-trip. 7/7 tasks; 9-test DVO-1…DVO-8 sweep; `cargo test --workspace` green ×3 + clippy `-D warnings` + fmt clean | `Done (Archived)` |

## Meta Information

- **Last Updated**: 2026-07-22 (v1.37.0: **milestone sync — Phase 22 (Developer Office) complete (7/7) and archived**. The self-hosting developer office shipped end to end: `DevOfficeGate::resolve` (pure domain gate, `crates/domain/src/dev_office.rs`) resolving `Absent`/`Feedback`/`Elevated` from `RepoAuthenticity` + admission + a feedback-tier deploy flag; `repo_authenticity` (`crates/core/src/dev_office_gate.rs`) — real, network-free, fail-closed on ambiguous multi-remote configs (DVO-2); `DeveloperAdmission` on the `crates/auth-local` human-write-only plane, a `HumanPrincipal` marker constructible only at a human-operated entry point so agent self-escalation is structurally unrepresentable (DVO-3); `DevOfficeModule` composing the shared extension loader with DVO-4 observe-not-remember held by construction, not discipline (no cached-tier field exists to go stale); the reserved-id `WorkspaceKind::Developer` singleton guarded at the real `workspace create`/`delete` CLI flow (DVO-1); confinement + audit composing `l2-tool-security`'s `ToolPolicy::is_permitted` gate with a real SEC-7 append-only `AuditEntry` log, both allowed *and* blocked outcomes logged (DVO-7); `cronus dev status|admit|revoke`, proven end to end by a real CLI smoke round-trip against this machine's own genuine `git@github.com:teratron/cronus.git` checkout, not a fixture. DVO-8 (no exception lane) proven structurally: `development_workflow::Pipeline::new`/`advance` take no workspace-identity parameter at all, so two independently constructed pipelines behave byte-identically under the same inputs, including an identical quality-gate refusal. **One genuine planning-time gap found during execution (T-22D01) and disclosed, not hidden or silently worked around:** `l1-tool-receipts` (TR-1…TR-9, MAC-signed/tamper-evident/model-unforgeable receipts) has zero implementation anywhere in this project despite Phase 22's own opening milestone text listing it among "already-shipped subsystems" — building the full subsystem inline would have blown T-22D01's composition-only scope and routed a security-sensitive, secret-bearing subsystem around the SDD pipeline it deserves its own pass through, so the real, already-shipped SEC-7 `AuditEntry`/`append_audit_entry` baseline was composed instead, with the gap flagged for a future dedicated spec-driven phase. 9-test DVO-1…DVO-8 acceptance sweep (`crates/core/tests/dev_office_invariants.rs`) through the real gate + facade export chain, no fakes for the security-critical paths (real temporary `.git` fixtures for DVO-2, a real `DeveloperAdmissionStore` + `HumanPrincipal` for DVO-3, a real audit-log file for DVO-7). `cargo test --workspace` green across **3 consecutive full runs** (71 result-sets each run, 0 failed) + `cargo clippy --workspace --all-targets -- -D warnings` clean + `cargo fmt --all -- --check` clean, sustained across all 7 tasks. Documentation closed out: `phase-22.md` frontmatter `status: Todo→Done` + `provides`(5 items)/`key_files`(5 created, 7 modified)/`patterns_established`(3 patterns) filled; `TASKS.md` v1.36.0→1.37.0 (row→`Done (Archived)`, Overview rewritten, meta entry); `PLAN.md` v2.34.0→2.35.0 (checklist→`[x]`, 7/7 summary, milestone paragraph, heading `— **Done**`). **With Phase 22 done, no buildable phase remains open — every currently-Stable spec is now built.** The Backlog stays 2 (both RFC concept-only L1: `l1-spec-driven-governance`, `l1-dynamic-harness`); each advances only via its own `/magic.spec` review, and promoting either opens no new build phase (no L2 realizations authored for them yet). Next: `finalize --workflow=run` (archives the phase), then per §5 Post-Task Replan the sole user-visible next step is `/magic.task main` (never `/magic.spec`/`/magic.analyze` proactively). Prior: 2026-07-19 (v1.36.0: **opened Phase 22 — Developer Office**. `l1-dev-office` reached Stable via `/magic.spec` (0.1.0→1.0.0 — the sole open policy question DVO-5 resolved autonomously to *feedback tier off by default* / developer-credential-only; tiebreaker = the security-minimization principle the whole contract rests on, and non-lossy because ordinary-user feedback is already served by the shipped `l1-issue-reporting` pipeline) and its realization `l2-dev-office` was authored Stable (1.0.0) in the same pass, decomposed into 7 tasks / 5 tracks (A gate+tier+kind · B repo-authenticity · C admission+module · D confinement+registration+CLI · T validation). Realizes the self-hosting developer office (DVO-1…DVO-8): a conditional `WorkspaceKind::Developer` materializing only in a genuine canonical-repo checkout for a human-admitted developer, otherwise entirely absent — Cronus maintaining itself (dogfooding, QLY-6) as a first-class *gated* capability. An **orchestration phase**: the elevated capabilities ARE the already-shipped subsystems (workspace-management/navigation/dev-workflow/sandbox-policy/tool-security/extensions/issue-reporting/tool-receipts); the phase builds only the gate + tier resolution + `WorkspaceKind::Developer` + the composing wiring. **Load-bearing security property = DVO-3 structural enforcement:** the domain gate holds only a read-only `&dyn AdmissionReader`, so agent self-escalation is *unrepresentable* on the agent surface (the BA-4/OA-4 lineage) — most likely error is an admission-mint on an agent-reachable API. Crate placement: pure gate/tier/kind logic in `crates/domain/src/dev_office.rs`, git/fs repo-authenticity + module wiring in the `crates/core` facade, admission on `crates/auth-local` (SEC-10 human-write-only plane). No new GUI work (conditional floor is a thin `l2-navigation` binding). Critical path = T-22A01 (gate + tier types). Planner audit + prompt-engineer PQ-6 PASS. `l1-dev-office` repositions to Phase 0 as a realized concept (built-bound L2). Pre-flight ok:true (ORPHANED_SPEC `l2-dev-office` + SYNC_GAP from the `/magic.spec` authoring closed here; header parity clean). Pre-Planning Stabilization 0 promoted (0 Draft). Backlog drops to 2 (both RFC concept-only L1). PLAN v2.34.0, INDEX v1.0.124. Prior: v1.35.0: **milestone sync — Phase 21 (Knowledge Store) complete (8/8) and archived**; the RAG subsystem shipped end to end, 69 tests, two mid-phase design gaps + one upstream sqlite-vec packaging bug found and fixed. Prior: v1.34.0: **opened Phase 21 — Knowledge Store**. `l2-knowledge-store` reached Stable via `/magic.spec` (1.0.0→1.1.0 — the L2 had lagged its already-Stable parent `l1-knowledge-base` v1.2.0; the promotion authored the missing KB-9 authorship-zone / KB-10 curation-lifecycle / KB-11 query-preparation compliance) and is decomposed into 8 tasks / 5 tracks (A schema+types · B ingestion · C retrieval · D authorship+curation+access+CLI · T validation). Realizes the agent-queryable RAG subsystem — named access-controlled collections with hybrid semantic (sqlite-vec ANN) + keyword (FTS5) retrieval fused by RRF, an async ingestion pipeline (file/URL/record), incremental re-indexing, soft-delete GC, storage-enforced authorship zones + a curation lifecycle; distinct from the ephemeral memory model and the client-facing project wiki. The sole mature Stable-but-unbuilt L2 (DA-3), its parent Stable and its embedding seam (`contract::InferenceBackend`) delivered by Phase 17. Foundation-then-parallel: Track A (the `knowledge_*` schema in `crates/store-local/src/knowledge/` + contract types) gates B/C/D. Crate placement reconciled at the plan level — the spec §4.5 standalone-crate layout maps onto the store-local decomposition (store-tier `cronus-store-local`, ingestion/retrieval `cronus-domain`, service `cronus-core` facade), the memory/wiki-store precedent; a mechanical `/magic.spec` §4.5 amendment is a non-blocking follow-up. Principal execution risk (disclosed): the `vec0` ANN half needs the sqlite-vec extension — a genuine external-dependency decision (the `ureq`/`windows-sys` flag-before-proceeding precedent); FTS5 + RRF + ingestion + authorship/curation/access build without it, and if deferred the vector half degrades to a documented seam (generator-optional precedent). Embedding is tested against a deterministic fake `EmbeddingBackend` (no live model in CI; the memory store's own vector is still a zeroed stub, so the real sqlite-vec wiring is net-new). Load-bearing security property: KB-9 authorship zones enforced at the single store write seam via a typed `WriteOverride` (the BA-4 / OA-3 structural-enforcement lineage) — the most likely error is placing the check in the caller instead of the seam. Critical path = T-21A01 (schema + types). Planner audit + prompt-engineer PQ-6 PASS. Pre-flight ok:true (SYNC_GAP from the `/magic.spec` promotion closed; 0 orphans, header parity clean). Pre-Planning Stabilization 0 promoted (0 Draft). Backlog drops to 3 (all RFC, all concept-only L1). PLAN v2.32.0, INDEX v1.0.123. Prior: v1.33.0: **milestone sync — Phase 20 (Archetype Catalog) complete (6/6) and archived; no new phase, no open buildable phase remains**. The office-archetype system shipped end to end in `crates/domain/src/archetype/`: the closed four-key `ArchetypeDefinition` schema making OA-4 unrepresentable (`validate_definition_keys` rejects a fifth key), `ArchetypeCatalog::program()` shipping `software-engineering` (18-role pool, empty seed) + the two declared-blocked entries with their §4.4 missing-role lists (OA-10 honesty), `infer` + `ActiveArchetype::set`/`clear` that touch only the archetype record (OA-7/OA-8, archetype-free complete OA-11), the deviation recorder whose `record_hire` returns `()` so it structurally cannot refuse a hire (OA-3) with an `Unvalidated`-until-observed status (OA-9), `create_from_preset` recording `derived_from` (OA-6), and the `cronus archetype list|info|set|create` CLI (no `hire` verb — clap rejects it). 24 domain tests + 4 real CLI-smoke tests + a 10-test facade-level acceptance sweep mapping every OA-1…OA-11 invariant. `cargo test --workspace` green across 3 consecutive full runs (0 failed) + clippy `-D warnings` + fmt clean. Disclosed deferrals carry forward: the ROL-9-gated role-catalog nine-role amendment, the imported-archetype vetting path (OA-5), and the office-scoped active-archetype record. **With Phase 20 done, no buildable phase remains open — every currently-Stable spec is now built.** The Backlog stays 4 (all RFC); each advances only via its own `/magic.spec` review. PLAN v2.31.0. Prior: v1.32.0: **opened Phase 20 — Archetype Catalog**. `l2-archetype-catalog` reached Stable via `/magic.spec` (1.0.0→1.0.1, reviewed as-authored — OA-1…OA-11 substantively complete; one cosmetic §4.4 TBD→prose edit) and decomposed into 6 tasks / 5 tracks (A schema+validation · B catalog+shipped-archetype+selection · C deviation recording · D command surface · T validation). Realizes the concrete office-archetype system — a prior on staffing that is never a roster, with the two load-bearing structural properties: OA-4 (no authority) made *unrepresentable* by a closed four-key schema (a fifth key fails validation), and the candidate pool read by the deviation recorder *never* by the hire gate (OA-3 — a hire outside the pool succeeds and is recorded, never refused). Domain-logic-first in `crates/domain/src/archetype/`, resolving role ids against the existing `roles::catalog::PRESET_CATALOG`; the shipped `software-engineering` archetype is an embedded program-tier constant (18-role pool, empty seed). Critical path = T-20A01 (the closed-key schema + validator gates every track). Planner audit: the single most likely implementation error is inverting the pool/gate order (reconstructing the fixed org chart) → enforced by a test that a hire outside the pool succeeds; two disclosed deferrals carry forward — the ROL-9-gated `l2-role-catalog` nine-role amendment (unblocks the other two archetypes) and the imported-archetype content-vetting path (OA-5, depends on the admission-vetting gate). prompt-engineer PQ-6 PASS. Pre-flight ok:true (SYNC_GAP from the `/magic.spec` promotion closed; 0 orphans, no header drift). Pre-Planning Stabilization 0 promoted (0 Draft). Backlog 4 (all RFC). PLAN v2.30.0, INDEX v1.0.122. Prior: v1.31.0: **milestone sync — Phase 19 (Loop Runner) complete (8/8) and archived; no new phase, no open buildable phase remains**. The single governed place every autonomous loop runs shipped end to end: `domain::loop_runner` (types with `MutableArtifact` lacking a `Criteria` variant — LG-3 by the type system; the governor's `check_ceiling`/`guard_writes`/`judge` single-enforcement-point; the execution-loop runner nesting a real ceiling-first/rollback-on-illegal-mutation/ledgered control flow; the evolution-loop runner nesting `run_execution` per task with keep/revert/partial scoring; the escalation gate hard-refusing on a shared-lineage oracle or absent novelty source before any metric comparison; LG-10 objective persistence composing the already-shipped CC-9 `trim_cascade`/`ContextEntry::protect()`), `core::loop_bootstrap` (a real `FileExistsBackend`, real clock, ledger/spec persisted as plain text under the real state tier), and `cronus loop run|evolve|log|show` (`evolve` present but marked unavailable per INV-9 — no harness registry exists yet, never a silent success stub). 42 domain tests + 4 core tests + 4 real CLI-smoke tests (spawning the compiled binary) + an 11-test facade-level acceptance sweep mapping every LG-1…LG-10 invariant through the real re-export chain. `cargo test --workspace` green across 3 consecutive full runs (0 failed) + clippy `-D warnings` + fmt clean. **With Phase 19 done, no buildable phase remains open — every currently-Stable spec is now built.** The Backlog stays 5 (all RFC); each advances only via its own `/magic.spec` review. PLAN v2.29.0. Prior: v1.30.0: **opened Phase 19 — Loop Runner**. The loop-governance chain reached Stable via `/magic.spec` (`l1-loop-governance` 0.2.0→1.0.0 RFC→Stable, reviewed as-is; `l2-loop-runner` 0.1.0→1.0.0 Draft→Stable — its Draft status was held only by the layer constraint, LG-10 compliance row + `ObjectiveSlot` added + crate placement reconciled to the post-decomposition topology) and `l2-loop-runner` decomposed into 8 tasks / 5 tracks (A types+governor · B execution-runner+oracle · C evolution-runner+escalation · D LG-10+facade+CLI · T validation). Realizes the single governed place every autonomous loop runs — a runner + single governor enforcing declared class + mutation manifest + separated oracle + independent ceiling (LG-1…LG-10), the criteria-immutability anti-drift spine (LG-3) enforced by the type system (no `Criteria` variant). The direct conceptual downstream of the Phase 18 always-on hub. A **composition** phase (not domain-logic-from-scratch): the runner composes shipped subsystems (execution-workspace/budget-engine/model-router/session-checkpoint/version-control/agent-autonomy + nodus provider traits) behind one governed contract; pure loop logic CI-tested against fakes (the `Oracle`/`ProviderType`/`BudgetHandle` abstractions), real subsystem wiring isolated to the `crates/core` facade (`loop_bootstrap.rs`) + one real end-to-end CLI smoke. Critical path = T-19A01 (the `LoopSpec`/`MutationManifest`/`Oracle` type shape). Planner audit: hidden dependency = the many composition targets (contained to the facade wiring D02); cascade risk if the A01 types are wrong → prove with the governor unit tests before building runners; disclosed scope boundary = the §6 migration of existing orchestration/kanban/heartbeat loops to route through the runner is a deferred behavior-preserving follow-up, not this phase. prompt-engineer PQ-6 PASS (Verify lines name concrete tests incl. the fake-seam testability criterion for the un-CI-runnable heavy integration). `l1-loop-governance` repositioned to PLAN Phase 0 as a realized concept (no longer concept-only). Pre-flight ok:true (SYNC_GAP from the `/magic.spec` promotion closed; 0 orphans, no header drift). Pre-Planning Stabilization 0 promoted (0 Draft remains). Backlog 5 (all RFC). PLAN v2.28.0, INDEX v1.0.121. Prior: v1.29.0: **registry-sync — absorbed one new Stable `concept-only` L1; no new phase, no open buildable phase remains**. `l1-value-settlement` v1.0.0 (agent-initiated outbound payment — the third budget axis beside inbound usage-allowance and per-request generation-budget: principal-set agent-read-only spending envelope on the human-write-only authority plane, bounded-authority custody, top-of-ladder gating with allowlisted recipients, fail-closed, receipted+ledgered, opt-in/local-first/rail-neutral seam, VS-1…VS-8) placed into PLAN Phase 0 (concept-only — no authored `Implements:` L2 yet, so not buildable until an L2 reaches Stable via `/magic.spec`). Added by an adoption pass over an external agent-harness reference; the same pass refined `l1-nodus-portability → LP-17` in the separate **nodus** workspace (out of scope for this main-scoped run). Reconciled the stale PLAN `**Based on:**` header (v1.0.118 → v1.0.120) that had lagged since Phase 18 opened, closing the Pre-flight SYNC_GAP. Pre-flight ok:true (the sole orphan `l1-value-settlement` resolved by this absorption). Pre-Planning Stabilization 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent `l1-loop-governance`). Registry INDEX v1.0.120 (210 specs: 203 Stable, 6 RFC, 1 Draft). Backlog unchanged at 7 (6 RFC + 1 Draft). PLAN v2.27.0. Prior: v1.28.0: **milestone sync — Phase 18 complete (8/8) and archived; no new phase, no open buildable phase remains**. Service Activation shipped end to end: the `ActivationRegistry` seam (capabilities/observe/enable/disable, BA-8 observed-not-remembered) + domain transition state machine (BA-3 mutual exclusion via remove-then-register-verify) + the three BA-4 structural barriers (no agent tool name, no sandbox write path, no elevation path) + the state-root lock (BA-11 attach-never-duplicate) + a new peer adapter crate `cronus-activation-os` behind a `SystemCalls` seam — real Windows S4U scheduled task + `Run` value (raw `windows-sys` FFI, real UAC elevation via `ShellExecuteExW`), Linux systemd user/system unit + `pkexec`/lingering-user fallback + XDG-autostart, macOS `LaunchAgent`/`LaunchDaemon` via `sudo` (disclosed `SMAppService` gap) — plus a genuine per-host BA-10 capability check (a hardcoded-`Supported` gap was found and fixed during the closing validation sweep; non-systemd Linux now reports `Unsupported` with a reason). `cronus activation status|enable|disable` wired end to end, BA-5 disclosure + BA-6 least-privilege refusal proven via a real non-interactive smoke test. `cargo test --workspace` green (exit 0, one disclosed transient flake unrelated to the new code) + clippy `-D warnings` (native Windows + cross-compiled Linux/macOS) + fmt clean. Pre-flight clean (ok:true, 0 orphans, no header drift). Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent). **Every currently-Stable spec is now built; no open buildable phase remains.** Backlog stays 7 (6 RFC + 1 Draft), each advancing only via `/magic.spec`. PLAN v2.26.0. Prior: v1.27.0: **opened Phase 18 — Service Activation**. `l2-service-activation` promoted RFC→Stable via `/magic.spec` (1.0.1 — three TBDs resolved as design decisions: non-systemd Linux → XDG-autostart login + system Unsupported; `tauri-plugin-autostart` rejected per std-first policy; Windows S4U design-settled, timing → build-time gate) and decomposed into 8 tasks / 6 tracks (A seam+policy · B BA-4 barriers · C state-root lock · D OS adapters · E command surface · T validation). Realizes how a host becomes the always-on INV-4 hub under one constraint — the engine runs as the user, never a superuser — with BA-4 (activation is a SEC-10 authority grant the agent has no structural write path to) as the load-bearing security property. Real-integration, seam-first (Phase 17 precedent): `ActivationRegistry` seam + no-op adapter first (BA-8/BA-10 testable before registration), sandbox deny entries before the first `enable()` (BA-4 barrier precedes the capability), login-scoped before system-scoped, Windows last (S4U launch-timing gate); raw OS calls behind a `SystemCalls` seam so adapter logic is CI-testable against a fake. Critical path = T-18A01 (the `ActivationRegistry` trait shape). Two bundled `/magic.spec` amendments rode the promotion: `l2-technology-stack` 1.2.1 (Windows-service→S4U reconciliation) + `l2-crate-topology` 1.0.1 (registered the shipped `model-local` peer adapter crate). Pre-flight clean (ok:true, 0 orphans, no header drift). Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent). Backlog 7 (6 RFC + 1 Draft). PLAN v2.25.0, INDEX v1.0.119. Prior: v1.26.0: **milestone sync — Phase 16 complete (7/7) and archived; no new phase, no open buildable phase remains**. The Project Wiki Store shipped end to end: per-office SQLite projection cache, office-owned transactional regeneration with PW-4 citation + PW-8 internal-detail guards + no-generator grounded-stub degrade, PW-5 fingerprint-drift freshness, structurally read-only client surface (PW-2/PW-6), `access-grants` Read gate (PW-7), and `rebuild()` as the projection-not-source proof (PW-3 — a real `wiki.db` file is deleted and an equivalent wiki re-derived from ground truth). `cargo test --workspace` green (exit 0, 64 result-sets) + clippy `-D warnings` + fmt clean. Pre-flight clean (ok:true, 0 orphans, no header drift). Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent `l1-loop-governance`). **Every currently-Stable spec is built; the Backlog stays 8 (7 RFC + 1 Draft), each advancing only via `/magic.spec`.** DA-3 topological next `/magic.spec` target: `l2-service-activation`; deferred mechanical follow-up: amend `l2-crate-topology` (model-local peer-crate row). PLAN v2.24.0. Prior: v1.25.0: **milestone sync — Phase 17 complete (7/7) and archived; no new phase**. The model-transport wave shipped end to end: `contract::InferenceBackend`, the `crates/model-local` REST transport (no async runtime; loopback + egress-gated remote), facade `NodusModelBridge` + `TransportCompactor`, and a self-tested CI `domain → model-local` boundary guard; two pre-existing `cronus-core` test-isolation races (mission_mode env, router BANDIT_COUNTER) were fixed to unblock the workspace-green gate. `cargo test --workspace` green (exit 0 ×3) + clippy `-D warnings` + fmt clean. **Phase 16 (Project Wiki Store) is the sole open buildable phase (DA-3 next).** Pre-flight clean (ok:true, no orphans, no drift). Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent `l1-loop-governance`). No new phase — every Stable spec is built or covered by an open phase. Deferred follow-up: `/magic.spec` amend `l2-crate-topology` (model-local peer-crate row). PLAN v2.23.0. Prior: v1.24.0: **opened Phase 17 — Model Transport & Provider Connectivity**. `l2-model-runtime` promoted RFC→Stable via `/magic.spec` (1.0.0→1.0.1, one Post-Update Review fix — the transport's generate surface was wrongly attributed to the routing-metadata `contract::ModelProvider`; corrected to a NEW `InferenceBackend` trait + two-facet provider + stream-collapsing nodus bridge) and decomposed into 7 tasks / 5 tracks (A contract inference trait · B model-local transport crate · C facade + nodus bridge · D topology + CI guard · T validation). Closes the concept-conformance audit keystone: model selection implemented while no seam could perform the call. Real-integration phase (spec-sanctioned HTTP+TLS client, hermetic mock-HTTP tests, no async runtime), first non-domain-logic-first phase since Phase 13. Foundation-then-parallel: Track A (`InferenceBackend` trait) gates B/C; B serialized on the shared crate + mock-server fixture. Now open alongside Phase 16 (both Todo) — by leverage the transport is higher-priority (unblocks triage classification, compaction, nodus `gen`), but neither depends on the other. Pre-flight clean (ok:true, 0 orphans, no header drift). Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent). PLAN v2.22.0, INDEX v1.0.118. Prior: v1.23.1: **registry-sync — audit wave absorbed; no task changes**. PLAN v2.21.0 (based-on INDEX v1.0.117) absorbed eleven Stable `concept-only` L1s into Phase 0 and parked `l2-model-runtime` (RFC, the audit's transport keystone) in Backlog; Phase 16 (Project Wiki Store) unchanged, still the open buildable target (7 tasks / 4 tracks, Todo, ready for `/magic.run main`). Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent). DA-3 next `/magic.spec` review target: `l2-model-runtime`. Prior: v1.23.0: **opened Phase 16 — Project Wiki Store**. `l2-project-wiki` promoted RFC→Stable via `/magic.spec` (1.0.0→1.0.1, one Post-Update Review fix — a `rebuild` byte-equivalence overclaim corrected to informational equivalence; spec-critic + prompt-engineer PASS) and decomposed into 7 tasks / 4 tracks (A schema · B regeneration · C read/access surface · T). Domain-logic-first, foundation-then-parallel: Track A (wiki_page/wiki_changelog/wiki_page_fts schema) gates B/C; Track B serialized on the shared regeneration entry point (B01 write path → B02 grounding/honesty guards → B03 freshness). PW-3 projection-not-source is the load-bearing acceptance property (`rebuild` reconstructs an equivalent wiki + drop-loses-nothing); PW-2 read-only-to-client is structural (no mutating client method); generator-optional degrade required (no-generator run stores a grounded stub, never fabricates). Pre-flight clean (ok:true, no header drift). Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent). DA-3 next target: Phase 16. INDEX v1.0.107, PLAN v2.20.0. Prior: v1.22.0: **plan-sync — wiki-store wave + MEM-4 amendment; no new phase**. `l1-project-wiki` gained an authored L2 `l2-project-wiki` (`RFC`, parked in Backlog per C6) realizing the client-facing wiki as a per-office SQLite projection cache (`wiki/wiki.db`), not loose Markdown — rebuildable cache of upstream truth, contrasted with the memory learned-tier where the store IS truth. Absorbed MEM-4 v1.1 (`l1-memory-model` 1.1.0 / `l2-memory-store` 1.4.0 / `l2-memory-consolidation` 1.0.2) — spec-only, no code delta (shipped `crates/store-local` already store-authoritative, no notes/*.md dual-write). Pre-flight clean (ok:true, no header drift). Pre-Planning Stabilization: 0 promoted (RFC→Stable is a `/magic.spec` act, not `/magic.task`; sole Draft `l2-loop-runner` stays Draft — RFC parent). No buildable phase opened — a wiki-implementation phase opens only when `l2-project-wiki` reaches Stable via `/magic.spec`. DA-3 buildable next target unchanged: Phase 15. INDEX v1.0.106, PLAN v2.19.0. Prior: v1.21.0: **Phase 14 complete + archived; opened Phase 15 — Memory Capture Policy & Metadata** (post-run replan). Phase 14 delivered 9/9 tasks — `l2-memory-consolidation` whole (MC-1…MC-10) and `l2-memory-intelligence` at query-surface scope (MI-1/2/3/4/5/7/8/9/13); workspace `cargo test` 1,333 passed / clippy `-D warnings` / fmt clean. The four capture-path invariants (MI-6/10/11/12) were deferred at delivered scope — each needs net-new capture-metadata schema (actor/expiry/subject/cross-ref) and forms one coherent write-path unit — and carry forward to Phase 15 as an explicit delta (source-layout 1.1.0/1.2.0 precedent: Phase 14 keeps `[x]` at delivered scope, immutable; the remainder is a new line item, never a reopen). Phase 15 = 5 tasks / 3 tracks (A capture-metadata schema · B capture path · T), domain-logic-first, foundation-then-parallel; no `/magic.spec` needed (spec Stable, Invariant Compliance table specifies each net-new item). Pre-flight clean (ok:true, no header drift). Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent). PLAN v2.18.0. Prior: v1.20.0: **opened Phase 14 — Memory Intelligence & Consolidation**. Both memory L2s (`l2-memory-consolidation` 1.0.1, `l2-memory-intelligence` 1.0.0) promoted RFC→Stable via `/magic.spec` (INDEX v1.0.101, 188 Stable) — one review fix in consolidation (§4.4/MC-8 reworded from "subsumes §4.2" to explicitly compositional, no store edit). Decomposed into 8 tasks / 4 tracks (A schema · B consolidation · C intelligence · T). Domain-logic-first, foundation-then-parallel: Track A (memory_signal table + depth/lifecycle columns) gates B/C; cross-edges MC-8→C-recall and MC-6-archive→MI-9-lifecycle respected; generator-optional degrade paths are build requirements. PLAN v2.17.0. Prior: v1.19.2 plan-sync (memory wave to Phase 0 + Backlog); v1.19.1 milestone (Phase 13 archived) (8/8 tasks Done; `l2-crate-topology` + `l2-source-layout` 1.2.0 realized — five-crate topology, INV-8 compiler-enforced, CI boundary guard live). All thirteen phases Done; every Stable spec implemented; no phase open. Pre-flight clean (ok:true, no drift). Pre-Planning Stabilization: 0 promoted — sole Draft `l2-loop-runner` stays Draft (RFC parent `l1-loop-governance`, layer constraint). Ten Backlog items all RFC/Draft — advance only via `/magic.spec`. DA-3 next target: `l2-service-activation` (direct downstream of Phase 13's crate seam). PLAN v2.15.1. Prior: v2.15.0 opened Phase 13 from the core-decomposition wave; v2.14.1 milestone — all 12 build phases Done)
- **Maintainer**: Core Team
