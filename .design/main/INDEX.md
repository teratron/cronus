# Workspace Specifications Registry

**Version:** 1.0.184
**Status:** Active

## Overview

Local registry of specifications for this workspace.

## Domain Specifications

| File | Description | Status | Layer | Version |
| --- | --- | --- | --- | --- |
| [l1-architecture.md](specifications/l1-architecture.md) ; v1.4.0 INV-9 amended — **retirement is the same rule read backwards**: a verb the surface no longer binds is by definition unbound, so INV-9 governs departure as strictly as arrival, forbidding both extremes — **silent deletion** (breaks scripts and muscle memory with no diagnosis, failing as an unknown command indistinguishable from a typo) and **permanent aliasing** (freezes the grammar around outgrown names and leaves two verbs for one capability, a parity violation of its own); sanctioned shape is a **declared retirement** — off the shipped default surface and out of help/completion, migration path still discoverable, retired invocations resolving to a message naming the replacement, any retained entry point opt-in + marked retired + stating what supersedes it — under the same identity discipline as a distributed artifact's rename (XM-2/HNR-6), an old name never silently re-pointed at a different capability | Layered core+frontends, inward dependencies, hub-and-spoke topology, command parity (INV-1…7); single-deployable **modular monolith** — not network-distributed microservices, sanctioned process boundaries limited to frontend↔core / security-confinement of untrusted code / hub↔spoke & device-replication of the same whole engine (INV-8); INV-9 shipped-surface honesty — a frontend never ships a verb unbound to the core capability it names: unbound commands are hidden/marked unavailable, never "not implemented" stubs (tech-agnostic) ; v1.3.0 INV-10 — **representation isolation at the inward seam**: the domain/core depends only on **shared contract types**; an adapter's **internal representation** (storage schema/row shape, wire DTO, transport-specific form) is **private to the adapter** and mapped to/from contract types at the seam, the domain never naming or depending on it; the **inward** counterpart to INV-2 (which keeps *logic* from leaking *outward* into a frontend) — dependency inversion applied to **data**, the twin of the behavioral trait-seam inversion (a domain module depends on a port trait, never a concrete backend) and of the error-shape boundary (l1-security SEC-11); forbids the classic leak where a persistence table schema or an HTTP DTO quietly *becomes* a domain type, after which the domain cannot compile or unit-test without that backend and silently re-couples to the technology the seam existed to isolate; a representation crossing the seam is mapped never shared (composing INV-8 + the DN-2 seam discipline); realized in l2-crate-topology | Stable | 1 | 1.4.0 |
| [l1-background-activation.md](specifications/l1-background-activation.md) | How a host becomes the always-on hub INV-4 presumes. Three activation modes (manual default / login-scoped / system-scoped) differing in lifecycle owner, privilege, and supervision — never in behavior (BA-2). Background activation is a **SEC-10 authority grant, not a preference**: the agent has no write path to it (BA-4), so activation state lives in the OS registration and is never mirrored into agent-writable config (§4.2). Consent discloses unattended autonomy + spend ceiling (BA-5); elevation authorizes registration only, never run-time privilege (BA-6); state is observed from the OS, never remembered (BA-8); removal is verified, not assumed (BA-7); one engine per state root — frontends attach, never duplicate (BA-11); spoke hosts refuse visibly (BA-10) | Stable | 1 | 1.0.0 |
| [l1-office-model.md](specifications/l1-office-model.md) | Office/corporation agent model: orchestrator, roles, client interaction, autonomy (tech-agnostic) | Stable | 1 | 1.0.1 |
| [l1-office-fabric.md](specifications/l1-office-fabric.md) | Office integration fabric — one office through coordinated **lenses** (work board / staffing graph / automation canvas / knowledge wiki / change-history timeline), each projecting its own authoritative substrate, no second source (OFA-1/OFA-2); stable **cross-lens object identity** so one work-object is one identity across lenses (OFA-3); **derived, traversable correspondence** (assignment card↔employee, realization card↔commits VC-3, maintenance automation↔cards CONV-6, documentation wiki↔work PW-4, memory↔entity), navigable from any lens, never a hand-kept mapping that drifts (OFA-4); live coherence within one event cycle, no settled contradiction between lenses (OFA-5); memory woven through every lens as a cross-cutting thread, addressed not copied (OFA-6, MEM-4/MEM-7); lens completeness extending no-shadow-work (CONV-1) to the whole set (OFA-7); legible to a non-technical client, subsystem boundaries stay beneath presentation (OFA-8); the representation half of the office-integration pair, steering owned by l1-directability; composes office-visualization/work-convergence/kanban/automation-canvas/project-wiki/version-control/memory-model; main-only host concept (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-directability.md](specifications/l1-directability.md) | Directability — the office is **directable, not co-driven**: full automation is the foundation (OFF-5/OFF-8), steering is an optional overlay. Direct manipulation on any fabric lens (move a card, reassign a worker, rewire an automation, request a wiki correction, cut a branch) is a **first-class control binding** over the one source-of-truth operation the UI/CLI/TUI/chat invoke (DIR-1, CC-2), applied to the substrate not the projection (DIR-2, OVZ-1/GS-6); **per-lens steerability** respects each lens's authority — direct-steer where permitted, **request-steer** where the office reserves authorship (wiki content PW-2/PW-4, pinned re-run AC-7), never exceeding it (DIR-3, reconciles KAN-2/PW-2/OFF-5); identical authority & confirmation gates to any surface, no bypass (DIR-4, SEC-9/SEC-10); an **accepted steer is a standing constraint** reconciled with autonomy — honored going forward, the office overriding only by surfacing the conflict + reason, never silently reverting (DIR-5); steering always optional so the autopilot is never demoted (DIR-6); legible/reversible/traceable (DIR-7); scope-respecting (DIR-8); **honest control — no dead controls, no faked agency**: an unhonorable steer is refused/surfaced at attempt-time, never accepted-then-ignored (DIR-9); the direct-manipulation sibling of conversational-control, rides on l1-office-fabric; the steering half of the office-integration pair; main-only host control concept (tech-agnostic); v1.0.1 links `l1-adjustment-vocabulary` — same steering instinct, different subject: DIR steers substrate operations and re-projects the lens, an adjustment moves a produced artifact along one of its own named bidirectional axes; DIR-5 standing-constraint reconciliation is what the ADJ-10 trail feeds | Stable | 1 | 1.0.1 |
| [l1-office-archetype.md](specifications/l1-office-archetype.md) ; v1.0.1 §5 records the **named scenario roster with staged activation** as considered and rejected — a phase-grouped roster (*always* / *from week three* / *as needed*) plus a fixed size mode, deployed in one selection: rejected on OA-1 (it is a roster), on OFF-4 in its most confident form (staging moves the hire decision from the work as it developed to a calendar the template author guessed at), on PS-1/PS-4 (width fixed in advance rather than following observed volume), and on OA-3 (anything ungrouped reads as *not part of this scenario*); what the shape gets right — that a domain's work arrives in phases with different specialty demands — belongs in the **norms** as knowledge the manager reads, never as a schedule that hires on its behalf | Domain-scoped staffing **priors** (software engineering, advertising agency, finance department). An archetype is a prior on hiring, **never a roster** — it carries a candidate pool, an org shape adopted only if the work grows, a bounded seed, and domain norms; it changes what the manager expects, never how it decides (OA-1/OA-2), preserving OFF-4 and the §5 rejection of the fixed org chart. The pool classifies hires, never refuses them (OA-3). An archetype carries **no authority** — the schema has no vocabulary for permissions/budget/autonomy (OA-4, SEC-10); third-party archetype prose is agent instruction and is vetted before admission (OA-5). Composes the role catalog, never forks it (OA-10); archetypes are falsifiable via recorded deviations (OA-9); the archetype-free office is complete (OA-11) | Stable | 1 | 1.0.1 |
| [l1-storage-model.md](specifications/l1-storage-model.md) | Two-tier (immutable program / mutable state) + multi-level memory model; STO-9 versioned state with forward one-way migration + timestamped pre-write backups (age-rotated) + non-destructive merge-by-identity import (§4.4) — makes STO-2/STO-7 survive upgrades that change state shape (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-memory-model.md](specifications/l1-memory-model.md) | Memory subsystem: 4 scopes, hybrid recall, lifecycle, service+curator ownership; MEM-4 source-of-truth by kind — authored tier text-authoritative, high-volume learned tier store-authoritative (text a derived projection), distilled skills are workflows not memory records (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-memory-intelligence.md](specifications/l1-memory-intelligence.md) | Active query & intelligence surface over the memory substrate: grounded `answer` projection composing knowledge-base attribution + claim-verification honesty gate (MI-1); first-class temporal recall modes as-of/changed-since/recent over the bi-temporal record (MI-2); immediate recall-visibility with strictly-async enrichment (MI-3); conflict surfacing with a closed keep-new/keep-old/merge/drop recommendation set — unambiguous auto-supersede, ambiguous adjudicated, never silent overwrite (MI-4); scheduled read-only intelligence digest — narrative + activity/type/confidence analytics, composing scheduler + dashboard (MI-5); salience-gated confidence-honest capture policy with actor attribution, caller-declared explicit expiry, and capture-time cross-reference (MI-6); end-of-run procedural distillation of a bounded agentic run into one structured procedure memory (MI-7); structured comparison-operator filter predicate — eq/ne/ordering/membership/containment combinable via AND/OR/NOT (MI-8); reversible lifecycle states active/paused/archived distinct from delete with audited transitions, prune-protective over decay (MI-9); capture-time normalization of relative→absolute time against the observation instant (MI-10); caller capture directives include/exclude/custom-instructions steering extraction without lowering the honesty floor (MI-11); raw-verbatim vs inferred-LLM capture modes as a local-first escape hatch (MI-12); MI-13 experience reuse — recall-before-acting gated reuse (active counterpart to MI-7 capture): before a costly/repeatable action, query typed success/failure/insight quality-scored prior experiences (MI-2/MI-8 filters); a high-quality prior success MAY be reused directly (short-circuit) only when a similarity+score+freshness gate passes, a failure surfaces as avoid-signal, an insight injects as guidance; every outcome captured back typed+scored (MI-6/MI-7); four guards — gated reuse, independent read/write, reused-not-re-derived attribution (MI-1), retained authority gate (SEC-9/SEC-10); case-based reasoning over the substrate (§4.12); nodus: reuse = RUN(@macro), gating host-side on StorageProvider (no nodus invariant); subject-of-memory lens about-user vs about-agent-self (MI-6 ext); ideas-to-adopt no-duplication ledger vs l2-memory-store §4.6/§4.7/§4.11–4.16 (cross-agent ACL/access-log, open-vocabulary auto-categories, declarative retention policy dispositioned as refinement candidates to memory-model/security/telemetry) + nodus StorageProvider/policy-config/error-taxonomy/operator-gap/macro-learning relevance mapping; v1.1.0 MI-14 **anchored neighbourhood recall** — a third mode beside similarity and temporal-window: given an anchor record (or a query top hit), return the records around it in time to a declared depth before/after, **interleaving every record kind** in one chronological sequence; explicitly not servable by re-ranking, since similarity returns the one thing a neighbourhood cannot contain (more records like the anchor, from unrelated moments); bounded depth, per-item kind + signed offset, anchor always marked; serves reconstruction (an isolated hit is unreadable without the work around it) and honest attribution (a decision without the discussion that produced it is a fact stripped of its reason) (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-memory-consolidation.md](specifications/l1-memory-consolidation.md) | Memory consolidation & corpus-maintenance layer between substrate (memory-model) and query surface (memory-intelligence): processing-depth tiers raw→working→consolidated orthogonal to the MEM-1 scopes, evidence immutable (MC-1); scheduled incremental idempotent "sleep-time" consolidation off the hot path — changed-input-only, failed-not-checkpointed retry, no-generator = successful no-op (MC-2); write-time additive-only relationship binding — in-text open-typed-predicate + mandatory provenance edges, edge set only grows across prose edits (MC-3); consolidation action algebra create/corroborate/refine/correct as finer MEM-6 supersession, distinct from and upstream of MI-4 adjudication (MC-4); hard fact-layer (authored text+edges) vs derived-signal-layer (centrality/cluster/recency/archived) separation with per-signal versioning + graceful neutral-default degradation + cold-start support (MC-5); corpus-maintenance action set redundancy→merge / overload→split / staleness→archive / abstraction-gap→summarize, confidence-proportional-to-blast-radius (multi-sample vote gates lossy merge, additive actions auto-apply), anti-cycle cooldowns, audited (MC-6); emergent topic-cluster abstraction into grounded size-bounded summary hubs recognized structurally by edge-coverage not by a marker, no privileged node type (MC-7); multiplicative offline-precomputed recall ranking text×centrality×cluster×recency with weak-signal veto and zero model-calls/graph-walk on the hot path (MC-8); co-edit write safety — optimistic concurrency + mechanical edge-conservation check + transactional-or-rollback multi-item writes (MC-9); advisory diversity-windowed interest extraction where memory decides what and inner-monologue decides whether/when/how to surface (MC-10); ideas-to-adopt no-duplication ledger (vs memory-model/memory-intelligence/l2-memory-store; structure-preserving segmentation, node-vs-fragment granularity, recency-cushion+auto-thaw, corpus health-checks dispositioned as refinement candidates) + nodus relevance (maintenance-as-host-job, derived-signals-behind-StorageProvider, summary-as-macro-candidate, degrade-not-throw error alignment) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-workspace-lifecycle.md](specifications/l1-workspace-lifecycle.md) | Home vs project workspaces, creation/edit/delete, default manager, hire/fire (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-dev-office.md](specifications/l1-dev-office.md) | Developer (self-hosting) office — a special system workspace for maintaining Cronus itself, distinct from the permanent home floor and user project floors: a conditional system floor that renders only while its gates hold (DVO-1); repository-authenticity binding — activates only in a genuine checkout of the canonical Cronus repo (local version-control marker + bound upstream identity, network-free), operating on the product's own source only, never user data (DVO-2); identity-gated, human-admitted elevated access (credential/key, external-account verification, or attested membership) never self-granted by an agent — a prompt-injected or ordinary-office agent cannot escalate in, the authority plane is human-principal-write-only (DVO-3, SEC-10); hidden by default, loaded on demand as a module/extension when the trigger fires (repo authenticity + admitted identity), never present in a default install, unloads cleanly (DVO-4); tiered admission — a verified developer gets the full self-maintenance office, a non-developer NEVER gets dev capabilities and at most a constrained consent-gated feedback surface (bug reports / improvement suggestions, no source/engine access), no in-band feedback→elevated promotion (DVO-5, resolved: feedback tier OFF by default — a normal install ships no dev-office surface at all; ordinary-user feedback already met by the shipped l1-issue-reporting pipeline, so the feedback surface is an opt-in ceiling, never a default); purpose-confined to Cronus self-maintenance held to the QLY-6 dogfooding bar, never a backdoor over user projects nor beyond the bound repo (DVO-6); contained & audited elevated authority — human checkpoints for irreversible actions (DW-8), durable audit/receipt of every elevated action, structurally isolated from user offices so a compromised user office cannot reach dev authority (DVO-7, INV-8); runs the standard Design→Plan→Execute→Review→Deliver dev workflow with no exception lane (DVO-8, DW); composes l1-workspace-lifecycle + l1-navigation-model + l1-development-workflow + l1-security + l1-attestation + l1-extensions + l1-issue-reporting (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l2-dev-office.md](specifications/l2-dev-office.md) | Implements l1-dev-office. The concrete developer-office realization as an orchestration layer over shipped subsystems (almost no new domain logic): a conditional `WorkspaceKind::Developer` in the workspace registry surfaced to l2-navigation only while gated (DVO-1); a local network-free `repo_authenticity` check against a compiled-in `CANONICAL_UPSTREAM` build constant — Genuine/NotCanonical/NotARepo, un-retargetable by user config (DVO-2); admission on the human-write-only auth plane (crates/auth-local) read through a read-only `AdmissionReader` port so escalation is unrepresentable on the agent surface — the BA-4/OA-4 structural-enforcement pattern (DVO-3, SEC-10); trigger-loaded module gated on `DevOfficeGate::resolve == Elevated` with clean unload (DVO-4); a three-state `AdmissionTier` Absent/Feedback/Elevated where Elevated needs (Genuine repo AND admitted) and the Feedback tier is a default-off deploy opt-in routing to the shipped l1-issue-reporting pipeline, no in-band promotion (DVO-5, resolved off-by-default); repository-scoped workspace isolation (DVO-6); confinement + audit composing l2-sandbox-policy + l2-tool-security + l1-tool-receipts + DW-8 human checkpoints (DVO-7); the unchanged l2-development-workflow engine, no exception lane (DVO-8). Domain/facade tier split: pure gate + tier logic in crates/domain, git/fs authenticity read + module wiring in the crates/core facade | Stable | 2 | 1.0.0 |
| [l1-kanban-model.md](specifications/l1-kanban-model.md) | Canonical work board: fixed canonical pipeline as mandatory semantic backbone, office-managed, auto-archival, one board of record per office; KAN-8 custom columns/boards as mapped extensions — every custom column anchors to exactly one canonical state, custom boards are saved views over the single card set, cross-office consumers read canonical states only (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-scheduler-model.md](specifications/l1-scheduler-model.md) | Schedules: recurring/one-shot, recurrence, fire actions (wake/routine/reminder), autonomy (SCH-1…7); SCH-8 bounded catch-up window for missed fires (§4.4) — a schedule MAY declare a per-occurrence lateness bound beyond its exact fire instant (e.g. 09:00 valid until 10:30) so a fire missed because the office was off/asleep still runs if the office recovers within the window (a catch-up fire, honestly marked fired-late, carrying its intended time) and is dropped never fired stale once the window closes; no window = zero-width = drop-if-missed default, the window an explicit opt-in; bounds lateness only, orthogonal to WL-4 coalescing (how many missed occurrences collapse) and to routine replay breadth (one vs all), composing with l1-doctor crash-recovery restart sweep; distinct from SCH-2's schedule-wide start/end window; resolves the prior missed-fire (downtime) TBD (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-work-convergence.md](specifications/l1-work-convergence.md) ; v1.2.0 CONV-9 added - a **decision-shaped unit** converges like any other and is not a second queue: work whose product is a decision rather than a deliverable lands on the same board under the same rules, honouring CONV-3's one-kind-of-work-unit **by construction** (a card whose completion criterion is a recorded answer, not a parallel representation with its own lifecycle), declaring its relation from the same closed CONV-2 set, and **may not carry a deliverable as its product** - a unit that produced one has changed kind and the board shows the change; closes the loophole of a planning phase run off-board because it is not-work-yet, which is CONV-1's shadow work wearing a phase name | Convergence contract — the office runs **all** activity through one legible surface, the Kanban board; no shadow work (CONV-1). Every stream declares exactly one of three board relations: **materialize** (client task / task-graph unit / `routine` fire / automation-spawned work → a persistent card, CONV-3/CONV-5/CONV-6), **drive** (pulse/`wake` heartbeat + automation `kanban_event`/board-action advance existing cards without creating one — reconciles "pulse passes through the board" with SCH-4 as *drive* not *materialize*, CONV-4), or **surface** (`reminder` → traceable notification, no pipeline card); relations exhaustive+exclusive per event, no fourth "acts invisibly" (CONV-2); one kind of work unit — the card, no parallel/shadow queue (CONV-3/§4.4); every convergence event traceable + office-managed never client-routed (CONV-7 composing KAN-7/KAN-2/OFF-5); building-level activity view is a read-only projection *of* office boards, board is ground truth (CONV-8 composing GO-4); sibling model-spec to l1-kanban-model/l1-scheduler-model/l1-task-graph-model, composes l1-automation-pipeline + l1-work-liveness WL-3; v1.1.0 adds onboarding import of an existing external backlog as a **materialize** stream (CONV-1 + §4.1/§4.2) — imported work-items land as canonical cards through triage, migration mechanics delegated to l1-work-import (tech-agnostic) | Stable | 1 | 1.2.0 |
| [l1-work-import.md](specifications/l1-work-import.md) | Work import — bounded one-directional onboarding migration of an existing external tracker's backlog into the canonical office model (distinct from the automation pipeline's ongoing event ingestion and from a live two-way sync, both out of scope): source-adapter isolation, one adapter per external tool/format behind a uniform read→normalize interface holding no target knowledge (WI-1); source-agnostic normalized intermediate model — work-items + id-keyed side tables of statuses/actors/labels/attachments, references resolved through the tables (WI-2); reconciling load never blind-insert — labels deduped against existing, actors resolved by email→name→policy-default, entities reused not duplicated (WI-3); external status maps onto the fixed canonical Kanban pipeline (KAN-1), no direct match inferred to a canonical kind from completion/start timestamps, office refinements as KAN-8 anchored custom columns, never a foreign state (WI-4); idempotent + resumable + rate-limit-aware load, re-import skips already-seen origins, partial import resumable not corrupt (WI-5); provenance preserved (source + back-reference = the idempotency key + lineage pointer) with imported content untrusted-by-default neutralized at the prompt/tool boundary (WI-6, composes l1-data-lineage + l1-context-provenance CP-1); fidelity-honest lossy-declared mapping — unmappable fields surfaced not silently dropped, comments folded with author/date (WI-7); asset/reference rewriting — embedded attachment/image URLs re-hosted or authenticated with a skip hatch, no dangling/credential-leaking external link (WI-8); interactive + headless parity, headless flags = the embeddability contract (WI-9, INV-3); converges as materialize cards through triage never a shadow queue, live-tracker pull consent-gated + credential-scoped egress while local-file import needs none, an authorized onboarding action not a silent sync (WI-10, composes l1-work-convergence CONV-3 + l1-security + SUP-3); ideas-to-adopt mapping (source-adapter migration, normalized result, dedup/resolve reconciliation, status-type inference) + nodus disposition Reuse behind host provider seams (LP-2), no new nodus invariant (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-work-liveness.md](specifications/l1-work-liveness.md) ; v1.3.0 WL-11 - **a guard that blocks completion measures semantic progress and carries a bounded release**: WL-8 classified a live-but-silent run, but the inverse mechanism (a guard refusing to let an actor *stop* while obligations remain) has its own failure mode; progress is read from **resolved obligation state, never artifact bytes** (a byte-diff guard counts a reflowed line, an added comment, or the recorder rewriting its own metadata as progress, re-arming forever so its release can only fire for an actor doing literally nothing - the one case least in need of it), the release is **bounded and diagnostic** after a declared number of consecutive no-progress blocks (an actor that is trying and failing is who an unbounded guard traps hardest), the budget is **per-actor** with serialized state and clearance on obligation disappearance, and the guard **observes, never executes** (a stop-time guard that ran its own checks would make stopping produce effects and turn every abandoned attempt into a side effect); each block states an actionable next move | Work liveness & ownership — keeps autonomously-executed work units alive, single-owned, and recoverable: exclusive atomic claim with conflict-not-clobber release (WL-1/WL-2, no double-work); affirmative liveness contract — every owned non-terminal unit always has a next-move from a closed typed set (active run · queued wake · deferred monitor · pending interaction/approval · human owner · live blocker-leaf · open recovery action) or is surfaced stalled (WL-3); single active run + wake coalescing realizing the de-dup the scheduler model deferred (WL-4); owner-preserving stranded-work reconciliation distinct from run-resume via a reap/resume/reconcile sweep (WL-5); bounded auto→explicit→human recovery ladder (WL-6) with a typed durable recovery action — comment-is-evidence-not-path (WL-7); silence-is-suspect liveness watchdog that classifies a wedged-but-alive run without killing it (WL-8); idempotent exact-once fan-out keyed by (source, approved revision) (WL-9); sits beneath orchestration delegation and above a single execution-graph run; composes with l1-kanban-model / l1-doctor / l1-operational-health; §4.3 concrete WL-1 realization — negated compare-and-set (transition to active iff not-active, race-free) — plus cooperative external preemption of a healthy run (STOP/PAUSE/RESUME on a per-unit channel, producer/consumer decoupled) with terminal-state-plus-short-TTL so a crashed executor's claim self-expires into reap (tech-agnostic) | Stable | 1 | 1.3.0 |
| [l1-quality-standards.md](specifications/l1-quality-standards.md) | Mandatory tiered quality gates as definition-of-done, role-enforced, universal+dogfood; QLY-9 gate-scope completeness — always-on gates cover every shipped deliverable unit, a unit outside the default gate runner requires an equivalent recorded lane, an uncovered unit is surfaced QLY-8 debt (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-office-visualization.md](specifications/l1-office-visualization.md) | Office as a live projection — **v2.0.0 scope change**: the core renders **one** representation, the **interaction-graph block-diagram** (agent/task nodes; reporting/collaboration/assignment edges), the single canonical office rendering (OVZ-3, narrowed from the prior two-representation model); **clickable drill-down to the underlying correspondence** — selecting an edge reveals the two agents' deliberation threads/briefings/hand-offs, a node reveals that agent's activity + inner-monologue, a projection of existing records within the viewer's access scope, observational never required (new OVZ-8); **no 3D/spatial in core** — any immersive/spatial representation is excluded from the core and available **only as a user-added extension** contributed via the office-lens extension point (new OVZ-9, composes l1-extension-points EP-2/EP-8), default-absent, still a pure projection; OVZ-7 re-scoped to graph-node layout; per-office + building overview; sole L2 implementer l2-office-view carries the change pending magic.task reconciliation (tech-agnostic) | Stable | 1 | 2.0.0 |
| [l1-orchestration.md](specifications/l1-orchestration.md) ; v1.3.0 ORC-13/14 added - **ending a dispatch's bookkeeping is a declared disposition**: a re-delegation returns the unit to the pool (ORC-4), a **handoff release** ends orchestration tracking and authority over the dispatch WITHOUT signaling or tearing down the delegate's still-running process, which continues as an explicitly **unsupervised** entity outside the orchestration's lifecycle, recorded as such never silently (ORC-13); **dispatch authority is a minted, generation-bound grant** over the (dispatch, delegate, execution-generation) triple - verified on all three so a superseded generation is rejected even given a valid dispatch+delegate identity, composing CB-3's bound-element-change lapses-the-grant rule for inter-agent delegation - revoked **exactly once, first-write-wins**, at whichever terminal transition (completion, failure, or an ORC-13 handoff) the dispatch reaches first, and never reinstated by a stale or delayed signal, closing the zombie-heartbeat-masks-a-hung-retry failure (ORC-14) | Coordination protocol: adaptive topology, delegation, /goal+judge+budget, briefings, error containment (ORC-11) — each delegation boundary classifies failures as retryable/fatal/escalation and contains them before propagation; ORC-12 transparent+intervenable coordination (§4.5) — inter-agent coordination (delegation/briefing/hand-off/cross-role messaging) flows through a medium the human can observe and interject into, no hidden agent-to-agent back-channels: surfaced (observable via office-visualization + event-mesh EM-8) and intervenable (human may redirect any in-flight coordination/turn as a first-class participant, composing l1-acp ACP-10 steering); a capability contract not a babysitting mandate (autonomy stays default OFF-5/ORC-6 but never means opacity); reconciles ORC-5 isolation + ORC-11 containment (they govern what is summarized upward, never license an unobservable/unreachable coordination path) (tech-agnostic) | Stable | 1 | 1.3.0 |
| [l1-intent-resolution.md](specifications/l1-intent-resolution.md) | Intent resolution — turns under-specified client intent into correct action without interviewing and without silently guessing: ground-before-ask from ledger/memory/user-model/KB/defaults (IR-1); ask only when blocking AND costly, minimal/non-technical/batched, the exception not the rule (IR-2, reaffirms OFF-6); assume-and-record never silently guess — explicit recorded reversible assumption with rationale+confidence is the audit unit of autonomy (IR-3); assumption≠fact, reversible, supersede-don't-mutate (IR-4); risk-proportional ask-or-assume scaling with blast radius — payments/finance/destructive bias to ask/approve, low-impact reversible bias to assume (IR-5, composes ORC-9 + autonomy-ladder); surface material assumptions at a natural checkpoint not mid-flight (IR-6); correction re-plans dependents (TG-9), confirmed assumptions become user-model defaults, corrected ones become self-improvement anti-patterns (IR-7); **IR-8 interpretation fidelity** — a distinct axis from IR-1…7 gap-resolution: they resolve what the client left *unsaid*, IR-8 guards against *misreading what they did say* — when confidence in the *interpretation* of a stated idea is genuinely low (materially different readings) or the idea is about to become a durable high-leverage artifact (spec/plan) where a misread propagates, the office confirms understanding **before** building on it by **restating its interpretation back** ("here's what I understood: … — right?") for validate/correct; uncertainty-gated + lower-friction than an IR-2 clarifying question (reflects the office's reading rather than interrogating for missing info), reaffirming IR-2/§4.4 anti-interview; corrected interpretation propagates + feeds learning as a corrected assumption (IR-7); ideation → specification boundary the canonical trigger; operationalizes OFF-5/OFF-6 and resolves the acting-on-misread-intent risk; interview deliberately rejected (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-roles.md](specifications/l1-roles.md) ; v1.2.0 ROL-9 gains a **content half** — the ≥2-axes test judges whether a role is *warranted*, never whether its definition is *new*, and a noun-swapped copy of a preset inherits credible justifications along with its body, so a custom role additionally clears the `l1-corpus-originality` admission gate (measured against catalog + sibling candidates, on neutralized content, against published calibrated thresholds), near-duplicates resolved by substantive distinctness or a **declared** variant relationship, never by loosening either half; the admitted duplicate makes the manager's selection arbitrary (ORI-11), the same fragmentation ROL-9 targets via a route the axes cannot see | Roles as specialties: preset + custom, hire/fire non-destructive, manager-driven (ROL-1…8); ROL-9 anti-sprawl justification gate — a custom role must justify itself on ≥2 independent axes (distinct expertise / parallelism / context isolation / reuse), one weak reason insufficient, reuse-or-extend-a-preset preferred otherwise (resolves the custom-role-sprawl TBD; role-level application of l1-harness-composition HC-5) (tech-agnostic) | Stable | 1 | 1.2.0 |
| [l1-routing.md](specifications/l1-routing.md) | Smart-router pattern: multi-signal selection, fallback, cache, scope resolution; function-scoped model roles — internal LLM chores (titling/triage/decomposition/profiling/summarization/curation/vision/approval) resolve through dedicated economical-by-default auxiliary bindings, never the premium user-facing route by default (RTG-9); credential-lane routing (RTG-10) — when one model is reachable via multiple credential lanes (metered API vs subscription/session), lane is a first-class signal bound by cache-warmth (continuing session prefers its prior lane to keep the provider cached prefix valid) and scope-safety (lane choice never leaks/voids a credential scope, no auto cache-marker injection); composes with l1-cache-stable-context; RTG-11 user-adjustable model effort with office-autonomy default (§4.5) — the reasoning effort a model applies (low↔high, quick↔deep) is a first-class routing signal, the office chooses effort autonomously per task by default while the human MAY set a floor/ceiling/bias envelope (global/per-office/per-request) the office routes within, a real traceable participation lever over how hard models work without seizing the per-task decision, higher effort cost-bounded by generation-budget, no human setting = office autonomous choice stands (tech-agnostic) | Stable | 1 | 1.3.0 |
| [l1-doctor.md](specifications/l1-doctor.md) ; v1.3.0 HEAL-8 — **build-parity skew as a named constitutive check**: two halves deployed independently and loaded at different moments (a frontend and the engine it attaches to under BA-11, a hot-reloaded module and the process still holding the old one) can run **different builds of the same product**, and the symptom is an ordinary-looking unrecognized-verb / rejected-field error indistinguishable from a defect, sending the investigation to the wrong place; identity is **content-derived, never timestamp-derived** (a synchronizing deploy commonly lands files *older* than what they replace, a deletion changes no timestamp, an edit to anything but the newest leaves the maximum untouched — each reporting *nothing to do* while stale code runs, the HNR-6/SG-2 argument), skew is **surfaced with an honest remedy** (commonly a full restart, since a reload that re-runs setup without re-importing leaves the old code in place), and it is **constitutive and not auto-repairable** under HEAL-7 since restarting belongs to the human; the doctor owns the diagnosis because a failing call site cannot tell a bug from a version gap from a capability the peer never had; composes ACP-2 and is the runtime counterpart of INV-9 | Self-healing: continuous checks, safe auto-repair, escalation, crash recovery; §4.3 restore-on-access — heartbeat-touch guard lazily restoring an idle-evicted/cold resource just-in-time on first access, best-effort (touch failures swallowed), complements the scheduled sweep and feeds WL-8; HEAL-7 user-governed healing authority (§4.4) — observe/heal tiers as a setting on the human-write-only authority plane (SEC-10), constitutive checks + crash recovery never disableable, suppressed repairs surface as recommendations and feed the improvement loop (tech-agnostic) | Stable | 1 | 1.3.0 |
| [l1-security.md](specifications/l1-security.md) | Client security: secret isolation, safe defaults, no exfiltration, sandboxing; SEC-8 confinement-mode duality (embedded vs remote sandbox behind one interface) with non-mixing mount scope — client-local paths mount only embedded, remote never mounts a local path + requires a bearer credential, fail-closed, mode+mounts audited (§4.3); SEC-9 learnable/scoped/revocable/governed permission promotion — an explicit human "always-allow" MAY become a durable allow-rule at an explicit minimal scope (this-action/action-class/office/global, default narrowest) keyed to a stable action identity, every promotion + suppressed prompt auditable and independently revocable, a governed escape hatch the managed tier can cap/disable, destructive/irreversible + always-forbidden classes non-promotable and fail-closed to prompting (§4.4); SEC-10 authority self-containment (§4.5) — the plane governing what the agent may do (permission/trust rules, autonomy level, credential grants, sandbox policy) and who may reach it (channels, admitted parties, pairing, ingress) is human-principal-write-only through a boundary the agent's execution cannot write to; agent reads its constraints but its only move toward more authority is a request through the SEC-9 human approval path (a request is data, never a self-grant); a structurally-enforced security confinement (INV-8-sanctioned, not a service decomposition) so a prompt-injected agent still cannot rewrite the plane it runs under; consolidates autonomy-not-model-elevatable + sandbox host-sole-writer + managed-tier + SEC-9 human-only into one L1 (counterpart to SEC-9: human may grant / agent never grants); nodus realization = l1-nodus-portability LP-10 (tech-agnostic) ; v1.4.0 SEC-11 — **error-disclosure boundary**: an error is a **semantic value owned by the core**, transport-ignorant, rendered per external surface (API/CLI/TUI/connector/tool-call reply) at the adapter boundary, and that rendering is a **disclosure boundary** — internal diagnostic detail (cause string, failing host/path, stack, query) is **logged internally, never returned to the external consumer**, which receives only a stable typed-code/status + safe message; generalizes SEC-5 from *secrets* to *internal implementation detail* (a detail like "connection to internal-db-host refused" is not a secret but leaks topology to an attacker — the information-exposure-through-error-messages weakness); three rules — semantic-not-raw (external representation from a closed set of typed error kinds, never a passed-through internal string), logged-is-not-disclosed (full detail on the internal diagnostic plane composing the dual-audience log + l1-fault-lifecycle, external projection redacted), core-stays-transport-ignorant (the semantic-error → surface-representation mapping lives in the adapter not the core, dependency inversion); the error-channel counterpart to confidentiality-flow's per-sink bound (CF-4) | Stable | 1 | 1.4.0 |
| [l1-context-provenance.md](specifications/l1-context-provenance.md) | Context provenance & trusted composition — makes the prompt-injection boundary safe by construction, elevating the scattered l2-tool-security spotlighting (§4.6 delimiter+preamble) + injection classifier (§4.4) into one L1 contract every composition surface obeys: per-fragment provenance with unknown=untrusted (CP-1); untrusted-by-default neutralization at the boundary where a fragment is interpolated into a prompt/tool-argument — encoded/escaped/delimited-as-data, raw insertion never by omission (CP-2); explicit auditable per-fragment trust elevation, never global/implicit (CP-3); sticky monotonic provenance — trust doesn't wash off via copy/concat/summarize/function-passthrough, trusted+untrusted=untrusted (CP-4); structural neutralization primary, detection/classifier complementary — a novel injection evading detection is still inert (CP-5); delimiter integrity — content can't forge/close/escape its quarantine (CP-6); every model-facing surface guarded not only an explicit external-content wrapper, closing the naive-interpolation bypass (CP-7); trust decisions observable in the trace for attribution + regression detection (CP-8); structural floor complementary to l1-claim-verification (output side); nodus realization = l1-nodus-language NL-11 (tech-agnostic); CP-9 control-channel unforgeability at the encoding boundary — where the receiving model distinguishes a control sub-alphabet (frame/turn/role/channel/stop markers), neutralization is enforced at the point a fragment enters that alphabet and not only by escaping its text: an untrusted fragment can never emit a control symbol by construction of the encoder rather than by a sanitizer applied after it, and content that merely resembles one is refused by default and thereafter either encoded inertly or minted per an explicit auditable per-surface disposition (CP-3 kinship), never silently promoted nor silently inerted; the structural floor beneath CP-6 (a delimiter built from ordinary content is a string an adversary must fail to forge, whereas an unreachable sub-alphabet removes the forgery capability), CP-6 remaining the defense-in-depth layer for flat prompt surfaces with no control alphabet; §4.2 realization table extended with the alphabet-level row and reordered by guarantee strength; governed by the new l1-tokenization-boundary TB-4/TB-5 | Stable | 1 | 1.1.1 |
| [l1-attestation.md](specifications/l1-attestation.md) | Verifiable attestation — the supply-chain/integrity trust layer: a produced/distributed artifact carries a signed witness independently verifiable offline with no central authority (AT-1); exact content-set binding — one byte invalidates (AT-2); authorship binding — knows who signed (AT-3); explicit typed checkable claims built-from/passed-gates/produced-by (AT-4); trust by verification not by channel, verify before the first privileged interaction (AT-5); default-deny the unattested/failed, fail-closed (AT-6); append-only revocation-by-supersession (AT-7); mechanism-agnostic but key-hygiene-bound to SEC-1 (AT-8); verification observability for audit + tamper detection (AT-9). Distinct from l1-context-provenance (content-trust) and l1-operational-ledger (data-provenance) — this proves artifact integrity+authorship; composes with l1-security (signing key), l1-extensions (EXT-11 import-verify), l1-acp (witness handshake elevates trust level), l1-multi-device-sync (SY-9 authenticated membership realized by attestation), l1-version-control (release author + passed-gates as claims), l1-quality-standards; nodus realization = l1-nodus-portability LP-9 (tech-agnostic) | Stable | 1 | 1.0.1 |
| [l1-tool-receipts.md](specifications/l1-tool-receipts.md) | Tool-call execution authenticity via model-unforgeable per-action receipts — the execution-side anti-fabrication sibling of l1-claim-verification (output grounding) and l1-attestation (artifact integrity): every effectful action (allowed/auto-approved/blocked) produces a keyed receipt binding action identity+inputs+result+time (TR-1); model-unforgeable — the signing secret never enters the model's context so it can echo but not mint a receipt, a fabricated one fails verification (TR-2); result authenticity — the result is inside the MAC so an altered/invented result mismatches (TR-3); existence authenticity — a narrated action with no verifying receipt is treated as fabricated, absence is signal (TR-4); ephemeral isolated secret — in-memory only, never persisted/logged/in-context/egressed, rotated on session restart (TR-5, reaffirms SEC-1); runtime-verified not third-party — deliberate opposite of artifact attestation, receipts secret-free + safe to surface (TR-6); complement never replacement — proves an action happened + result is real, not whether it should have (SEC-9/SEC-10 authority) nor whether claims are grounded (claim-verification) (TR-7); honest coverage boundary — detached/background actions surface receipts on resumption not before (TR-8, EG-12), never implying coverage it lacks; tamper-evident auditable record strengthening SEC-7 from logged to provable (TR-9); closes the LLM-as-string-generator fabricated-action/fabricated-result gap no trust layer owned; nodus realization = l1-nodus-observability HO-9 (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-interception-model.md](specifications/l1-interception-model.md) | Interception discipline — how cross-cutting behaviour (permission decisions, provenance neutralization, execution receipts, telemetry) attaches to the agent lifecycle, unifying the currently-divergent fail-behaviours of the plugin-hook (fail-forward) and tool-security (fail-open guardrail) realizations under one L1 contract: capability-bounded interceptor taxonomy observe/decide/transform enforced by type — observe may neither veto nor mutate, decide may veto not mutate, transform may rewrite (INT-1); check-before-use TOCTOU-safe ordering decide→effect→observe so a check is never recorded as a use and an observation always reflects the completed effect (INT-2); fail-direction fixed per class — decide/transform guards fail closed (abort, never silently permit), observe fails forward (record, never wedge the agent) (INT-3); nested session⊃turn⊃operation interception state with one-way (broad→narrow) visibility, isolated from the guarded effect's own state (INT-4); transitive guard enforcement across delegation — a capability the guard denies is denied whichever agent in the tree attempts it, a sub-agent cannot escape its parent's guard envelope by performing the effect itself, enforced by the mechanism not by prompt re-injection (INT-5); strip-vs-deny model-visibility restriction axis — strip removes a capability from the affordance set invisibly at zero token cost (all-or-nothing), deny leaves it visible and refuses per-attempt with an argument-aware legible reason the model can adapt to (INT-6); deterministic specificity×safety priority resolution — more-specific over wildcard, safer decision (deny>ask>allow) within a band, first-match short-circuit, fail-closed predicate, construction-time validation (INT-7); honest coverage boundary — an entry path a guard does not sit on (e.g. a trigger/background/scheduled-initiated turn vs the user-send pre-turn guard) is a NAMED gap, never presented as guarded (INT-8); composes l1-security SEC-10 (principal behind INT-5) / l1-context-provenance CP-2 (a transform interceptor) / l1-tool-receipts TR-8 (an observe interceptor + coverage honesty) / l1-policy-governance PG-4 (governs which interceptors load) / l1-orchestration ORC-12; realizations-in-practice l2-plugin-hooks + l2-tool-security dispositioned as reconciliation candidates; nodus realization = l1-nodus-portability LP-11; v1.1.0 INT-9 **prefer a convergence point over per-entrance guards** — where multiple entry paths lead to the same governed effect and share a single boundary each must cross for the effect to become real, the deterministic guard SHOULD sit at that convergence point rather than being replicated per entrance; INT-8 makes an incomplete guard *honest*, INT-9 makes it *unnecessary* where a chokepoint exists, since a guard there is source-independent by construction (unable to be evaded through a different actor or path because there is no other path to the effect) and thus has no coverage gap to name; does not weaken INT-8 (per-entrance guards with named gaps remain honest where no single convergence point exists) and does not drop inline per-path guards (they compose on top for early contextual feedback); full placement discipline — delta-not-state, deferred obligations, attributed bypass — owned by l1-convergence-gate; v1.2.0 INT-10 **declared attachment phase relative to resolution** (pre-resolution / post-resolution-any / post-resolution-matched): only pre-resolution may rewrite the address and a post-resolution rewrite attempt is a defect not a tolerated no-op; observation defaults to post-resolution-any since an invocation resolving to nothing is the misadvertisement signal (ATE-13); a target-bound guard defaults to post-resolution-matched, since deciding against a nonexistent address spends the check on nothing and answers *denied* where the truth is *no such target*, disclosing existence via the error class; phase declared and structurally enforced like the INT-1 class — an implicit phase makes INT-8 coverage unstatable (tech-agnostic) | Stable | 1 | 1.3.0 |
| [l1-error-reporting.md](specifications/l1-error-reporting.md) | Consent-gated, scrubbed, de-duplicated error reporting; ERR-6 submission autonomy off/confirm(default)/automatic — automatic is a standing, scoped, revocable, audited grant on the human-write-only authority plane satisfying ERR-1 by durable consent (SEC-9 kinship), never silent (every auto-filing locally visible); non-error finding reports (improvement, RP-2f) are automatic-grant-only, confirm mode routes them via the invitation path; resolves the default-consent TBD (default = confirm); v1.2.0 ERR-7 — a report addresses a **fault** (the identity defined by the fault-identity contract), not an ad-hoc textual similarity judged at filing time: one report per fault with later occurrences updating it; a **regression reopens the existing report** rather than filing a fresh unrelated one, since filing anew erases the fact that a fix was claimed and did not hold — the very thing a maintainer most needs to see; substatus gates filing volume (*new*/*regressed*/*escalating* warrant reaching outward, merely *ongoing* does not); consent untouched — the fault layer is local and always-on while ERR-1/ERR-6 still govern every crossing of the device boundary (tech-agnostic) ; v1.3.0 ERR-8 a **refusal reports the conditions that caused it**, each named, never a count or score that merely correlates — the sharpest form being a figure computed over a *subset* of the deciding conditions, individually truthful and jointly misleading, so a refusal caused entirely outside the subset arrives beside an accurate zero; binds every decline surface, not only crash-shaped errors | Stable | 1 | 1.3.0 |
| [l1-issue-reporting.md](specifications/l1-issue-reporting.md) | User-initiated "Report Issue" button + function — the human-triggered complement to error-reporting (error-triggered): first-class user action, no error need occur (ISS-1); payload = user's own narrative + closed category set bug/feedback/idea (ISS-2); preview-before-send consent — nothing egresses until the user sees+approves the EXACT payload incl. every attachment, strengthens ERR-1 (ISS-3); opt-in sanitized diagnostics bundle — version/recent logs/process+health snapshot(OH-8)/screenshot, never auto/silent (ISS-4, ERR-2/SEC); local-first never-lost — retained+queued when filing unavailable/declined/offline (ISS-5, ERR-5); shared pipeline & tracker not a parallel egress — reuses error-reporting sanitize→dedup→file/update + l2-github-issue channel (ISS-6); frontend parity — extends existing `report` command group with `report issue` across CLI/TUI/GUI button (ISS-7, INV-3); demarcated from error-reporting by trigger+actor+content, unified by one audited egress path; v1.1.0: the flow MAY be entered from a system-prompted invitation with a prefilled editable draft (l1-report-prompting RP-1/RP-5) — send remains user-initiated via ISS-3 preview on every path (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-report-prompting.md](specifications/l1-report-prompting.md) | Report prompting & diagnostic findings — system-suggested, user-completed reporting that bridges error-reporting (system-sent) and issue-reporting (user-sent): invitation-never-transmission — the prompting layer MUST NOT send/file/egress or open a new egress path, a report leaves only via ISS-3 preview or ERR-1 consent, declining has no egress side effect (RP-1); closed trigger taxonomy — failure (incl. crash-on-next-start from the forensic log DL-2) / degradation (health alerts+anomalies ≥ warning, OH-3/OH-4) / inconsistency (unrepairable self-check escalations, HEAL-3) / component failure (embedded severity-coded taxonomies, e.g. the workflow-language runtime's canonical codes) / friction (repeat retry/cancel patterns), a new kind is a taxonomy amendment (RP-2); setting-governed closed modes off/passive/active + per-category enablement/thresholds, mode changeable in one step from any invitation, findings still recorded when off (RP-3); bounded non-modal deduplicated invitations — fingerprint discipline reused from ERR-3, recurrence updates the existing invitation's count, rate limits + dismissal cooldown, mute honored until unmute/product-version change, a finding already auto-reported under ERR-1/ERR-3 is marked reported and never re-invited (RP-4); prefilled evidence-linked sanitized fully-editable draft — prefill only from secret-free projections (LL-7), attachments only via the receiving flow's ISS-4 opt-in + ISS-3 preview (RP-5); local findings ledger, two consumers — user invitations + developer triage: source/category/severity/fingerprint/first-last-seen/occurrences/evidence/outcome (pending/shown/dismissed/muted/reported/resolved), inspectable + exportable, bounded retention (triage queue not a durable audit), never self-egressing (RP-6); respects reporting config and never pressures consent — honest local-capture offer (ISS-5) when filing unconfigured/declined/offline (RP-7); frontend parity, library-first (RP-8, INV-3); measure/repair separation preserved — consumes the observation planes' outputs, never diagnoses/scores/repairs itself; v1.1.0: RP-2 gains (f) improvement — system-observed evidence-backed improvement/optimization opportunities captured under the improvement loop's discipline (IMP-1/IMP-2), filing directly only under the ERR-6 automatic grant (invitation path otherwise), mapped to issue category `idea` (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-telemetry.md](specifications/l1-telemetry.md) | Opt-in, program-data-only, privacy-first improvement telemetry; v1.0.1 links `l1-artifact-derived-observation` as the uninstrumented-source sibling (telemetry = what the product emits about itself by design; that = what is reconstructed about a foreign tool from its own trail), whose derived output crosses this same consent gate (tech-agnostic) | Stable | 1 | 1.0.1 |
| [l1-improvement-loop.md](specifications/l1-improvement-loop.md) | Product self-improvement loop — the closed fleet-feedback contract (flagship differentiator): in-work non-blocking observation derives product-level findings (defect/friction/inefficiency/optimization/improvement idea) from planes already recorded, budget-bounded, no new instrumentation (IMP-1); findings product-subject-only, generalized at capture — user content never becomes a finding (IMP-2); user-governed submission autonomy off/confirm(default)/automatic — automatic a standing, revocable, audited grant on the human-write-only authority plane, one audited pipeline, no parallel egress (IMP-3, realized by ERR-6 + RP-2f + ISS-3); upstream intake at the canonical tracker managed by an autonomous triage service — classify, spam-separate (quarantine recorded, never silent-delete), dedup vs open+history+rejection ledger, evidence-score, route/aggregate/auto-answer, maintainer attention the protected resource, every decision recorded + maintainer-reversible, queue-management-only authority (IMP-4); evidence-over-eloquence submitter-strength awareness — heterogeneous (weak-model) senders, reproducibility + independent cross-fleet corroboration outweigh prose, low-evidence reports aggregate into corroboration buckets, report content untrusted data at the triage boundary (IMP-5, CP-1 kinship); rejection memory — every rejection recorded with rationale + supersession criteria, equivalent resubmissions auto-answered from the record, supersedable on materially new evidence, append-only never a permanent verdict, recurrence itself corroboration (IMP-6); disposition feedback closes the loop — accepted/shipped/duplicate/aggregated/rejected flows back and marks the local finding (RP-6 outcome), powering prior-resolution surfacing (IMP-7); triage service is itself a Cronus office — dogfooded, spec-governed, no bespoke second system (IMP-8, QLY-6/DVO kinship); optional by construction — zero-server product, upstream is maintainer infra never a runtime dependency, full local value with submission off (IMP-9, deployment-neutrality); doctor demarcated as local-healing sibling (HEAL-7 setting), l2-self-improvement demarcated as agent-practice learning — it changes the agent, this loop changes the product (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-diagnostic-log.md](specifications/l1-diagnostic-log.md) | Diagnostic log — the forensic observation plane of last resort: the raw, local, developer/operator-facing record of what the process actually emitted, captured low enough to survive failures the higher planes cannot witness. Distinct non-conflated channel separate from the semantic run-trace, execution-authenticity audit, aggregate health, live process view, and outward telemetry — each with its own consumer/retention/egress, never folded together (DL-1); survives what the semantic planes structurally cannot — a native crash/fault, output before any structured-observation subsystem is initialized, and unstructured dependency/subprocess output; sink installed at the earliest point of process life so a session that dies before or without ever reaching structured-event emission still leaves a diagnosable record, the witnessed-vs-only-forensic boundary named not blurred (DL-2, the defining invariant); self-describing session header (platform/runtime/argv/cwd/ts) so a captured log is diagnosable offline (DL-3); own-scope fidelity with dependency noise clamped to a higher severity floor, keeping the forensic record legible (DL-4); bounded ephemeral rotating retention — size-capped, backup-count-limited, not a durable ledger, dropping the oldest rotation is correct not data loss (DL-5); local-first, consent-gated, secret-safe egress — never auto/silent, leaves the device only in a user-previewed issue-reporting bundle (ISS-4) or under telemetry opt-in, after the error-reporting scrub (DL-6); live in-product inspection at frontend parity — filter/search/copy/tail without external tools (DL-7, INV-3/PM-5); resilient neutral sink — a write/rotation failure (locked file/full disk/permission) degrades gracefully and never crashes/blocks/perturbs the observed process (DL-8, OH-1/PM-1 posture); complements the semantic trace (nodus l1-nodus-observability HO-10 truncation-honesty side) and the aggregate-health/telemetry/execution-authenticity planes; the raw-forensic sibling that owns the crash no structured plane can witness (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-log-legibility.md](specifications/l1-log-legibility.md) | Log legibility & economy — the dual-audience contract that makes one log stream ideal for a human reader AND an AI consumer without overloading either: every observable fact recorded once as one canonical event, every surface (human console / machine record / live stream / agent-context feed) a faithful projection that reshapes but never invents or contradicts facts, so a human and an AI never read two divergent stories (LL-1); the canonical event carries both a human-legible message and machine-legible typed fields as first-class content, neither a lossy reparse of the other (LL-2); each projection consumer-shaped — human scannable/attributed/folded, machine complete/queryable, agent compact/payload-free — over a canonical event that stays complete (LL-3); overload bounded by construction via three composable controls, severity/verbosity gating × payload policy (descriptor/excerpt/full) × compaction (fold repeats to ×N, window the tail), conservative defaults with full fidelity opt-in, no reader handed a firehose by default (LL-4); honest reduction — every truncation/fold/omission/sample flagged with its magnitude (original size, repeat count, sample rate), a reduced record never masquerades as complete so neither human nor AI mistakes an excerpt for the whole (LL-5); stable attribution (component/severity/category/correlation-id/acting-role) for filterability, the mechanism that makes economy achievable — narrow rather than drink the firehose (LL-6); secret-safety re-asserted at every projection not just the sink, widening visibility never widens secret exposure, sensitive sources denylistable from capture (LL-7); AI-consumer economy — a log fed back into an agent's own context is the compact payload-free budget-bounded projection never the raw firehose, reading its own logs must not evict working context (LL-8, composes context-budget + cache-stable-context); stable self-describing versioned namespaced schema with a migration path so a machine/AI parses without human explanation and a schema change is a migration not a silent break (LL-9); the legibility/economy contract over every observation channel (l1-diagnostic-log / semantic trace / l1-operational-health / l1-dashboard / l1-telemetry), composing l1-security + l1-context-provenance + l1-inner-monologue; nodus realization = l1-nodus-observability HO-11 (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-dashboard.md](specifications/l1-dashboard.md) | Live read-only statistics projection: per-office + building aggregate (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-process-monitor.md](specifications/l1-process-monitor.md) | Process monitor — the product's own task-manager: a live, read-only view of the application's OWN OS process tree (main + spawned children) with per-process CPU/memory, across CLI/TUI/GUI with parity (PM-5/INV-3). Read-only observation, no control/repair — anomalies hand off to operational-health + self-healing, never restarted/killed by the monitor (PM-1, OH-7); named role identity + OS PID + live metric set, unavailable metric reported never fabricated (PM-2); parent/child tree rooted at main by real spawn parentage (PM-3); topology fidelity — every running process classified against sanctioned boundaries (surface-per-frontend / confined agent-plugin execution / platform utility helpers, INV-8), unclassified surfaced never hidden incl. the monitor's own process (PM-4); live refreshable configurable-cadence sampling (PM-6); local-first + secret-safe, no egress except telemetry opt-in, never surfaces secrets/sensitive args (PM-7, INV-7); sibling read-only projection to l1-dashboard (OS-process domain vs office/work), thinnest of the observability family — feeds l1-operational-health (scoring/alerting) and l1-doctor (repair); process control kill/restart deferred to a later authorized doctor+security path (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-workflow-language.md](specifications/l1-workflow-language.md) | Agent workflow DSL: dual-mode, schema contract, constraints, subsystem-bound steps (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-extensions.md](specifications/l1-extensions.md) | Unified extensions: skills/MCP/plugins/**connectors**, default-deny+sandboxed lifecycle, skill generation; service connector as a fourth extension kind for external HTTP/REST services with no MCP server (EXT-10) — closed auth-scheme taxonomy (none/basic/api-key/digest/oauth1/oauth2/session) with uniform test/label/refresh/deauth contract, trigger/create/search operations with optional resource shortcut, trigger delivery duality (polling deduped by stable identity vs subscription REST hooks + mandatory polling fallback) feeding the automation pipeline as external_event/webhook sources, hydration for deferred large-payload dereferencing, idempotent search-or-create, dynamic/dependent input fields; EXT-11 verifiable import attestation — a third-party/imported extension presents an independently-verifiable signed witness (per l1-attestation) verified before activation, missing/failed witness default-denied (composes EXT-3), sharpening EXT-8 from declared to proven provenance (tech-agnostic) | Stable | 1 | 1.2.0 |
| [l1-extension-points.md](specifications/l1-extension-points.md) | Core-wide extension-point (seam) model — the **dual of l1-extensions** (artifact↔seam): the microkernel core is extended **only at named, declared, mediated seams**, contributions never reach subsystem internals or each other (EP-1/EP-3); a **closed, uniform point-kind taxonomy** — *observe* (react, cannot change outcome) / *transform* (value→value pipeline) / *provide* (core selects one implementation) / *contribute* (add an invocable surface) / *decide* (answer/short-circuit a decision) — used by **every aspect** (office, board, automation, memory, wiki, model serving, roles, navigation/fabric, version-control, security, CLI), no per-subsystem bespoke mechanism (EP-2/EP-8); **deterministic composition under declared precedence** — observe fan-out / transform pipeline / provide selection / decide first-non-abstaining / contribute name-collision rule — never load-order-accidental (EP-4); **versioned seam contracts** with pre-activation refusal of incompatible targets (EP-5); **fail-isolated** contributions under declared per-kind failure policies, kernel survives a bad plugin (EP-6); least-privilege, declared, grant-gated point reach (EP-7); **lazy activation** (installed-but-unused costs nothing) + inspectable/auditable live wiring (EP-9); microkernel/contribution-point best-practice synthesis; composes l1-extensions/l2-plugin-hooks(one realization of observe/decide)/l1-architecture/l1-security; main-only host architecture concept (tech-agnostic)  ; v1.1.0 adds the three rules governing a contribution's **presence** rather than its invocation: EP-10 core-drawn attribution marker a contribution cannot compose away + interrupt ceiling (may interrupt work, never a decision *about the session itself*); EP-11 reserved core namespace, contributor identity owns its qualified names so no shadowing *whatever the core adds later*; EP-12 the core registers its own implementations through the published seam — no privileged door, because a contract only third parties exercise is one nobody notices has become insufficient | Stable | 1 | 1.2.0 |
| [l1-attention-steering.md](specifications/l1-attention-steering.md) | Attention steering — the **inverse of directability**: autonomous work moving a co-present human's live view. A live surface is an addressable object with a declared scope (AST-1) steered **out-of-band**, never by impersonating the user's keystrokes (AST-2); targeting auto-resolves only when unique and refuses ambiguity (AST-3); read → move → annotate as a contract (AST-4); structure first, payload by explicit request (AST-5); **focus is metered per act**, never a side effect of annotating (AST-6); **replacement outranks movement and withdraws open decision requests** (AST-7); authorship asymmetry — an agent may enumerate and remove human content, never author it (AST-8); a batch lands whole or not at all in front of a watching human (AST-9); the live view is a projection, never the record (AST-10); no attached surface is a first-class answer (AST-11) | Stable | 1 | 1.0.0 |
| [l1-semantic-addressing.md](specifications/l1-semantic-addressing.md) | Semantic addressing — naming a place inside an artifact so the name survives the artifact, the surface, and the process. One grammar with one serializer/parser (SA-1); **semantic keys only** — never index, rendered row, coordinate, or runtime handle (SA-2); exactly one target, ambiguity refused rather than resolved by precedence (SA-3); the address records what it was minted against (SA-4); resolution is **ternary** — resolved / relocated-with-evidence / orphaned, and an orphan is retained and disclosed (SA-5); **clamp inside a resolved container, refuse an unresolved one** (SA-6); **placement is all-or-nothing per container**, since partial placement is silent data loss wearing the appearance of success (SA-7); presentation-independent (SA-8); opaque to whoever carries it (SA-9); one address, every surface (SA-10) | Stable | 1 | 1.0.0 |
| [l1-surface-parity.md](specifications/l1-surface-parity.md) | Surface parity — what keeps "thin bindings over one core" (INV-8/9/10) true past the first surface, given that boundary gates, type checks, and per-surface tests are all structurally blind to a surface re-deriving what the core computes. One decision point per observable behavior (SP-1); derived facts consumed, never recomputed — defective **while still agreeing** (SP-2); divergence as a tracked finding inventory (SP-3); **repaid = copies deleted + deletion pinned + adversarial fixture + every consumer registered** (SP-4); append-only tombstones, shrink-only debt (SP-5); parity proven by one corpus through each surface's **real** projection (SP-6); a new consumer registers before it ships (SP-7); legitimate difference named with its reason (SP-8); **extract before the second implementation exists** (SP-9); **converge first, correct second** (SP-10); shared action catalog with declared locus, where an unexposed action is declared rather than merely absent (SP-11) | Stable | 1 | 1.0.0 |
| [l1-derived-instructions.md](specifications/l1-derived-instructions.md) | Derived instructions — the agent-facing document is **generated from the surface**, not written beside it, because a stale instruction steers an agent away from the working path and the failure reads as a model mistake. Rendered from the capability's declaration (DIN-1); a generated artifact is not an editing surface (DIN-2); **every quoted message single-sourced with its emitter** through one catalog (DIN-3); the correspondence pinned by contract tests (DIN-4); conditional capability gated on a **runtime probe**, never the document's assumption (DIN-5); the surface locates its own instructions (DIN-6); failures are catalog entries carrying their **correction** (DIN-7); a description-invalidating change fails the build, not the agent (DIN-8); authored guidance lives in the generator (DIN-9); one description of an action across every presenter (DIN-10) | Stable | 1 | 1.0.0 |
| [l1-automation-pipeline.md](specifications/l1-automation-pipeline.md) | Event-driven automation pipeline: Trigger→Filter→Transform→Action directed graph; AP-1…AP-11 invariants; dual-mode architecture (implicit per-worker `@ON:` blocks + explicit canvas-authored pipelines); 8-node taxonomy (trigger/filter/transform/branch/action/delay/aggregate/loop); 7 trigger types (schedule/state_change/kanban_event/message_received/webhook/external_event/manual); event payload contract; pipeline versioning; deduplication window; office-scoped isolation; AuditProvider observability binding; durable opt-in node memory for change/spike/gap/dedup/digest continuity, pipeline-local not office-store (AP-8); control plane separate from data plane — control edges carry enable/disable/trigger verbs distinct from event-flow data edges (AP-9); event lifecycle — per-node retention policy, TTL expiry+GC preserving change-detector continuity, immediate-vs-deferred per-edge propagation, node health predicate (last-evaluated/received/error + working?), dry-run preview (AP-10); portable definitions-only automation bundles (stable id+origin+tags, export/import non-destructive with credential rebinding + staged reversible apply, never carries memory/history/credentials) (AP-11); pipeline composition — `subpipeline` node invokes another pipeline by identity (bounded depth, acyclic, caller-scoped), the reuse mechanism for AP-11 bundles (AP-12); pinned partial re-execution for authoring — pin a node's output, re-run only the downstream subgraph against it, reusing §4.9 dry-run side-effect quarantine, marked development-not-production (AP-13); scoped automation state with declared durability — node-private vs pipeline-shared scope hierarchy generalizing AP-8, named volatile-vs-durable persistence backends with a default, office-scoped + content-excluded, distinct from the office memory store (AP-14); in-graph lifecycle observers — `observer` node subscribing to error/status/completion of a declared scope, scoped-precedes-catch-all routing with outward error propagation across `subpipeline` boundaries, the inbound mirror of the AP-9 control plane (AP-15); external-source change classification — a change-detector over an external uncontrolled source (page/feed/API-poll via `external_event`) assesses change against a declared canonical projection (normalization strips volatile/cosmetic regions so a re-rendered-but-unchanged source is `same`, not `changed`) and emits a typed status {new/same/changed/removed} not a differ-boolean, `removed` (durable disappearance) first-class and distinct from a transient fetch `error`, tag-namespaced comparison lineage, hardening AP-8 raw-equality (correct for internal state, noise-dominated for external content) over AP-10/AP-14 continuity; materiality gating left to `filter`+reference-free-judgment composition (AP-16) | Stable | 1 | 1.4.0 |
| [l1-event-mesh.md](specifications/l1-event-mesh.md) | In-process publish/subscribe event-routing substrate: typed event envelope (id/source/type, EM-1); topic addressing (type, source) not recipient (EM-2); subscription as pure cacheable matcher→mapper with exact + prefix/hierarchy match (EM-3); publish/subscribe decoupling + dynamic subscriptions (EM-4); on-demand handler activation by (type, key) factory addressing (EM-5); request/response as correlated pub/sub projection with timeout, point-to-point as a projection (EM-6); declared delivery semantics + per-subscriber fault isolation (EM-7); observable-by-construction routing carrying envelope+correlation, no raw payload in the clear (EM-8); in-process scope boundary composing with INV-8 modulith — NOT distributed worker/service placement (EM-9); office-scoped isolation with mediated cross-office relay (EM-10); unifies the scattered inbox/board/office-state/scheduler/trigger bus events; ideas-to-adopt no-duplication mapping (team patterns/interception/model-context/memory/workbench/component-config already owned; dual-ledger orchestrator a refinement candidate; distributed placement out-of-scope by INV-8) + nodus-relevance mapping (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-system-readout.md](specifications/l1-system-readout.md) | System readout & refresh — the one unified mechanism every live system readout (remaining token balance, health score, process stats, connection/sync status) flows through, the third convergence beside the one work board and the one event mesh so refresh logic is not reinvented per widget: one source of truth per readout, every surface reads the same source, two surfaces never disagree (SR-1); a closed trigger taxonomy — activation (surface opened) / manual (refresh button) / event (mesh state-change) / scheduled (bounded poll for a persistent panel) / dependency (upstream readout changed), no ad-hoc paths (SR-2); one shared refresh core owning fetch/coalesce/cache/fan-out, surfaces subscribe and declare triggers but carry NO per-surface fetch/poll/debounce logic (SR-3); coalescing & single-flight so N concurrent triggers/surfaces yield one in-flight fetch fanned out, never a fetch storm (SR-4); explicit freshness & honest staleness — value carries last-updated + a fresh/refreshing/stale/error state, never presents stale-as-current nor blanks on failure (SR-5, parallels LL-5); visibility-gated economy — refresh only what a surface is showing or a panel polls, bounded configurable cadence, hidden surfaces suspend, never burn cost/quota polling data nobody is looking at, correctness-critical when a refresh is a metered API call like a token-balance check (SR-6); read-only projection, refresh observes never mutates (SR-7, parallels PM-1/OH-1); cross-surface & cross-frontend consistency, same source/triggers/freshness everywhere across CLI/TUI/GUI, only rendering differs, a new surface for an existing readout needs no new refresh logic (SR-8, INV-3); consumes l1-event-mesh (event trigger) + l1-scheduler-model (scheduled trigger) rather than replacing them, hosts on l1-navigation-model panels + l1-application-shell state; the token-balance readout mapped end-to-end in the taxonomy (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-search.md](specifications/l1-search.md) | Search — application-wide capability to find anything the user can see (office/card/memory/message/file/doc/setting) through one query surface within an explicit selectable scope: scope is a first-class dimension — global (whole app) / office-project (one workspace only) / settings (config only), always visible, a result never silently crosses scope (SRCH-1); one unified query federated over heterogeneous sources into one merged ranked list, user issues one query not one-per-source (SRCH-2); isolation & access-respecting — office content only within its scope or global-with-attribution (WSL-8), settings search never surfaces a secret, elevated dev-office content not searchable without admission (DVO-3), never widens visibility beyond what the user may already reach (SRCH-3); attributed & navigable results carrying source+scope+location, a pointer to a real place to jump to (SRCH-4); read-only projection (SRCH-5); fresh via incremental event-driven indexing with honest reconciliation of stale hits, never a full re-scan per query (SRCH-6); bounded & economical — ranked/paginated/latency-budgeted, index-not-rescan, lexical-default with semantic opt-in, local-first no egress (SRCH-7); cross-frontend parity CLI/TUI/GUI (SRCH-8, INV-3); settings a deliberately separate scope not a global source; federates memory/KB/code-search as sources rather than replacing their per-domain query (tech-agnostic) [v1.1.0] SRCH-9 history-as-a-distinct-scope — the evidence archive is searchable as its own scope, never folded into `global` (what *is* vs what *was*); SRCH-6 freshness inverts (archived content is immutable, a hit is never stale), attribution navigates by expansion (EA-3), coverage beyond the horizon is reported (EA-8). | Stable | 1 | 1.1.0 |
| [l1-usage-allowance.md](specifications/l1-usage-allowance.md) | Usage allowance & quota windows — the external-allowance member of the token economy (distinct from input-context and output-generation budgets): the remaining allowance a provider/subscription grants over rolling windows, the source of truth behind the "remaining daily/weekly token balance" readout. Rolling-window model with a daily+weekly minimum, configurable beyond, per resource/lane (a metered key vs a subscription have different caps, RTG-10), each tracking cap/consumed/remaining and resetting at its boundary (UA-1); provider-authoritative-when-reported else locally-estimated from accounted consumption vs a configured cap, the two never conflated — an estimate labeled and superseded by an authoritative report on arrival (UA-2); remaining as a first-class readout displayed/refreshed through the unified readout mechanism, source-tagged (authoritative/estimated) and never silently stale (UA-3, composes l1-system-readout SR-5); consumed by routing (near-cap biases lane RTG-10 / effort RTG-11 before exhaustion) and office-control (exhaustion → OC-3/OC-4), the model informs it does not itself act (UA-4); honest provider-aligned window reset, no stale-pre-reset nor confident-full-before-confirmed (UA-5); observation does not materially consume the allowance (UA-6); local-first & secret-safe, credential behind a lane never surfaced, egress only telemetry opt-in (UA-7); the leading signal behind office-control exhaustion/recovery; v1.1.0 UA-8 **exhaustion degrades acquisition before access** — capabilities shed in a *declared* order that puts spend-driving acquisition (generate/enrich/summarize/index/capture) ahead of access, so recall/search/read degrade last: making already-paid-for knowledge unreachable to save money destroys value rather than deferring it, and failing whichever request arrives after the cap is an accident, not a policy; plus **a cost guard is not a safety guard** — a metering/quota guard that cannot reach its counter answers a budget question, so its fail direction is an explicit human-set policy, never inherited from INT-3 fail-closed (conflating them turns a broken counter into an outage or a spend cap into a fiction) (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-conversational-control.md](specifications/l1-conversational-control.md) | Conversational control — the natural-language chat/prompt surface as a first-class control plane over the application's own management operations (create/edit/delete workspaces & offices, schedules/crons, roles, automations, settings), not merely a channel to discuss work: an operation reachable via UI or CLI is reachable by asking (CC-1); the same underlying library operation the UI/CLI/TUI invoke — a fourth binding over one source of truth, never a re-implementation, prompt and button produce identical results (CC-2, INV-3 parity); understood via intent resolution (ground-before-ask, assume-and-record IR-1/IR-2/IR-3), not a brittle keyword grammar (CC-3); the identical authority/autonomy/confirmation gates as any surface — a destructive management action needs the same human confirmation whether button or prompt, NL never a bypass around a gate (CC-4, SEC-9/SEC-10); legible+reversible+traceable execution narrated back and audited, never silent (CC-5); scope-respecting per the surface it is issued in — project-office prompt acts on that office, home/building prompt may act cross-office per WSL-2, never crossing an isolation boundary the operation would respect (CC-6); control distinct from work — a control command mutates management state through a management op, a work request enters the board, classified & routed correctly, "create a project" is control vs "build me a website" is work (CC-7, l1-work-convergence); distinct from l1-office-control (lifecycle states, not NL management-op invocation) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-automation-canvas.md](specifications/l1-automation-canvas.md) | Visual automation canvas: three-panel layout (pipeline selector + flow graph + node inspector + execution log); AC-1…AC-7 invariants; node rendering with live execution state (incl. `subpipeline` drill-in + pin badge); explicit pipeline editing (node palette, connection rules, validation, non-destructive versioning); implicit pipeline read-only surfaces with explicit conversion workflow; inspection and debugging (run list, trace, replay, live mode via AuditProvider stream); pin-and-partial-re-run authoring loop — pin a node output, request engine-side partial re-execution from a chosen node (AP-13), canvas requests+renders only (AC-3 preserved), dry-run actions, marked development (AC-7); `observer` node rendering with kind badge (error/status/completion) + dashed scope overlay, observer scope visually explicit — scoped vs catch-all (*unhandled*) distinguished, canvas renders relationship+trace only (AC-3 preserved) (AC-8); graceful degradation when engine offline | Stable | 1 | 1.2.0 |
| [l1-harness-engineering.md](specifications/l1-harness-engineering.md) | Harness engineering pattern: harness≠model (HE-1), nodus workflow as harness spec (HE-2), frozen evaluation pipeline (HE-3), evidence-backed amendment with 4 required fields (HE-4), single-component change per iteration (HE-5), transfer validity on held-out set (HE-6), append-only history (HE-7), artifact extraction mandatory in ANALYZE (HE-8), context freshness per iteration via artifact load (HE-9); six-component harness taxonomy (workflow spec/schema/macro library/memory contract/policy config/trace config) with nodus mapping; three-phase evolution loop (EVALUATE→ANALYZE→IMPROVE) with durable artifact write and fresh-context open; versioning and transfer protocols; budget/termination criteria | Stable | 1 | 1.1.0 |
| [l1-harness-optimization.md](specifications/l1-harness-optimization.md) | Harness optimization — the outer-loop search over a harness-candidate space toward a frontier, the third activity beside harness-engineering (evolve one lineage) and agent-coevaluation (measure a pair): searchable candidate space (HX-1); outer-loop/inner-runtime separation (HX-2); content-addressed archived candidates with provenance (HX-3); trace-rich proposer reading prior candidates' source+scores+traces not just scalars (HX-4); declared bounded wrapper-first mutation space (HX-5); frontier of non-dominated candidates across the objective vector quality/cost/latency/stability/safety — not a scalar best (HX-6); comparability-partitioned frontier + task-selection-hash-guarded baseline reuse or fresh run (HX-7); regression-bounded + held-out-honest + train/eval-disjoint + reward-hacking-guarded acceptance (HX-8, also formalizes the reward-hacking-filter mechanic); budgeted terminating search (HX-9); durable concurrency-safe resumable observable frontier (HX-10); composes l1-harness-engineering (per-candidate evolution) / l1-agent-coevaluation (scoring+comparability) / l1-evaluation-suites (grading) / l1-attestation (hashing) / l1-nodus-environment (graded-run substrate NE-12) — adds the search+frontier layer none owned (tech-agnostic); HX-11 budget-normalized evaluation & mutation-invariant metric — when candidates in one search differ in resource-affecting dimensions their evaluations MUST be budget-comparable: preferred design is budget normalization (the evaluation contract fixes the resource budget — wall-clock/compute/tokens/trials — not the workload, every candidate measured on outcome-within-fixed-budget) so radically heterogeneous candidates compete directly, efficiency is absorbed into the outcome rather than hand-weighed as a second axis, and the optimum is honestly platform-relative (budget + platform recorded with every score, composing MR-14); differing budgets partition the frontier (HX-7, sharpening ACE-5's a-longer-budget-is-a-different-task row into a design principle); and the scoring metric MUST be mutation-invariant — chosen to remain meaningful across the entire mutable candidate space, a mutation never able to change what the metric measures (criteria-drift sibling of loop-governance oracle-ownership); v1.2.0 HX-12 — **offline acceptance is necessary, never sufficient**: HX-8's held-out regression-bounded acceptance admits a candidate to the archive but may not by itself promote it to the default for live work; an accepted candidate reaches real work behind a named, instantly revertible switch at a declared exposure fraction with deterministic assignment and a reserved holdout on the prior default, bounding a regression the held-out set could not see to the exposed fraction and making it reversible without shipping; the reason is structural rather than a matter of gate strength — the held-out set is a *sample of tasks* while live work is a *distribution that moves*, so a candidate promoted straight to 100% on a held-out win has been validated against the past and deployed against the future; exposure fraction, guardrails, and stopping rule declared before exposure and frozen for its lifetime (the live analog of HX-8 held-out honesty and HX-11 criteria-immutability) | Stable | 1 | 1.2.1 |
| [l1-harness-composition.md](specifications/l1-harness-composition.md) | Harness composition — the right-sizing / continuous-justification / anti-bloat *trim* counterpart to harness-engineering's *grow*: minimal justified composition, bloat is a first-class defect not neutral surplus (HC-1); component = named capability-gap compensation, no nameable gap ⇒ unjustified (HC-2); continuous re-justification — prune when the model or host closes the gap, justification is not one-time (HC-3); dedup against host-native + existing components, host-supersedes-custom ⇒ remove naming the native feature (HC-4); anti-sprawl ≥N-axis justification gate for specialists (HC-5, = roles ROL-9); bounded complexity — capped delegation depth + scale-proportional component count (HC-6); right-size to the subject from a trait-selected catalog, not a one-size maximal set (HC-7); deferred-with-trigger not speculative (HC-8); lifecycle disposition keep/update/add/remove with reasons + safe reversible archive-then-remove (HC-9); observable/auditable composition, drift-to-bloat detectable (HC-10); composes harness-engineering/harness-optimization/agent-coevaluation/roles/orchestration/extensions/office-model; two supersession sources (model-improves, host-adds-native) invisible unless actively checked (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-solution-frugality.md](specifications/l1-solution-frugality.md) | Solution frugality — the output-side anti-over-engineering discipline (sibling to harness-composition's tooling-side right-sizing; the subject is the produced deliverable code, not the agent's harness): comprehension precedes frugality — a minimal diff produced without understanding the change is a confident wrong fix, the discipline shortens the solution never the reading (FR-1); the reuse-before-build ladder, first-hold-wins — need-to-exist?/reuse-in-codebase/language-stdlib/native-platform/installed-dependency/one-expression/minimum-new-code (FR-2); the hard negligence floor — frugality never cuts trust-boundary validation / data-loss error handling / security / accessibility / requested scope / physical calibration, below it "less" is a defect (FR-3); root-cause locality — one guard at the shared point, not per-caller (FR-4); tunable non-self-elevated intensity off/advisory/enforced/aggressive, human-set per office/session, never model-elevated (FR-5, composes SEC-10); disclosed simplification with a named ceiling + upgrade trigger, harvestable debt ledger, no-trigger rot flag, undisclosed cut = defect (FR-6, composes operational-ledger + self-improvement); complexity-only scoped non-mutating frugality review/audit with a closed finding vocabulary delete/stdlib-covered/native-covered/speculative-abstraction/shrinkable, produces a delete-list never applies it (FR-7); output economy — unrequested justification prose is complexity, requested reports exempt (FR-8); proportional verification — one runnable check for non-trivial logic, composes quality-standards' mandatory-test gate, never licenses skipping a required test (FR-9); measured-not-asserted with an honest counterfactual — never fabricate a per-instance savings number, the counted debt ledger is the only real per-project figure (FR-10); projects onto nodus as macro-reuse (RUN(@macro)) + a frugality-lint family over authored workflows, no new language primitive (§4.6) (tech-agnostic); v1.0.1 links `l1-change-containment` as the independent edit-footprint axis (FR-7 delete-findings scheduled separately CTN-3; FR-4 root-cause a containment warrant not creep; FR-1 comprehension extends to removal CTN-2) | Stable | 1 | 1.0.1 |
| [l1-change-containment.md](specifications/l1-change-containment.md) ; v1.2.0 CTN-12 added - the **wide mechanical change** exception: a single mechanical transformation whose call sites number in the hundreds cannot be made both correct and contained, and splitting it into contained pieces yields pieces that do not build; sanctioned shape is **expand -> migrate -> contract** (new form beside the old, call sites migrated in blast-radius-sized batches each leaving the system green, old form removed in a final unit gated on every batch), the exception **declared not assumed** because it is the most available excuse for an unwarranted hunk (CTN-1 applies unchanged within each batch), and where a batch cannot stand green alone the batches share an integration line gated by one explicit **verify** unit | Change containment — the **edit-footprint** discipline, the diff-side counterpart to solution frugality (which bounds solution *size* and has no notion of an edit's entitlement; the two axes fail independently — §4.6 four-cell table). Every changed line carries one of exactly three warrants — realization / correctness-necessity (root cause, updated caller, gate-required test, sibling path) / self-repair of an orphan this change created — with no fourth; "it was better this way" is not a warrant (CTN-1). **Comprehension gate on removal**: nothing whose purpose was never established — code, branch, config value, or *comment* — may be deleted, rewritten, or condensed, at any change size; apparent redundancy is evidence of missing comprehension, not of dead code, and "no static references" is a lookup result not a purpose (CTN-2, extends FR-1 to the removal side). Foreign dead code is **reported, never removed in the same change**; orphans the change itself created are cleaned up in it — the line is authorship of the orphaning, not the code's state (CTN-3). Local idiom outranks personal preference, an explicit standard outranks both, and reconciling a divergence is its own change (CTN-4). Mechanical transformation never bundled with semantic change — the direct cause of unreviewable diffs and lost history legibility; lands on the VC-3 card-aligned commit boundary (CTN-5). Expansion **declared and warranted**, with silent widening *and* correctness-narrowing both defects — containment governs disclosure, never timidity (CTN-6). Declining to edit is never declining to report: every deferred observation filed as an actionable finding, since acquiring knowledge and discarding it is worse than the drive-by edit (CTN-7). A distinct **non-mutating** containment review with a closed vocabulary — unwarranted-hunk / style-drift / foreign-cleanup / bundled-mechanical / comprehension-gap / undeclared-expansion — that must not flag warranted root-cause or gate-test hunks (CTN-8). Measured on the diff (unwarranted-hunk share, off-surface files, mechanical-line ratio, blind removals, deferral discipline), never asserted, no fabricated per-change savings figure (CTN-9). Uniform rules, proportional ceremony (CTN-10). **nodus projection, no new primitive**: dual-representation AST-equality makes the mechanical/semantic split *decidable* for authored workflows (bundled-mechanical becomes a check, not a heuristic), plus a containment lint family and reuse of declared effect surfaces as mechanical rather than instructed enforcement (§4.8). Composes solution-frugality / development-workflow (DW-4) / review-checkpoint (RC-7) / version-control (VC-3) / code-intelligence / operational-ledger / intent-resolution (IR-3) / pattern-codification / practice-analytics; v1.1.0 CTN-11 **a declared edit boundary is enforced by mechanism, blocks rather than warns, and is never self-relaxed** — the mechanical floor under a discipline that was entirely self-applied: for high-cost work the principal declares which paths the work may write to, enforced at the write gate rather than by instructing the producer (instruction being the enforcement that already failed); narrowable by the producer, widenable only by the principal (SEC-10 / AG-8), so the constrained actor cannot dissolve its own constraint; scoped and announced; explicitly a floor never a substitute — staying inside the path does not make an unwarranted hunk warranted (tech-agnostic) | Stable | 1 | 1.2.0 |
| [l1-dynamic-harness.md](specifications/l1-dynamic-harness.md) | Dynamic harness pattern (run-time complement to harness engineering): two declared modes — runtime adaptation (in-run execution reshaping) and runtime substitution (one spec over interchangeable runtimes); DH-1…DH-12 invariants; observer/interceptor separation (DH-2), bounded+budgeted interception (DH-3…DH-5), ordered inspectable middleware chain with adaptation-kind taxonomy (context-shaping/provider-failover/output-bounding/loop-control/environment-injection/validation-retry); contract-preserving substitution (DH-6); layered sourced experience observability with provenance back-pointers (DH-7); falsification ledger with machine-checkable predicted flips + keep/revert/partial verdict (DH-8); structural genome + advisory drift detection without re-eval (DH-9); promotion-gated provisional→durable online adaptation (DH-10); verification independence across lineages (DH-11); order-dependence disclosure (DH-12); nodus integration via write-capable interceptor seam, runtime-substitution abstraction above ModelProvider, AST genome function, and consumption of the already-LP-3-graduated PolicyProvider (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-navigation-model.md](specifications/l1-navigation-model.md) | Navigation model as a strictly nested four-layer "building/corporation": Building frame (L0) ⊃ Floor tab bar (L1, one workspace/office per tab) ⊃ Subsystem sidebar (L2) ⊃ Mechanism sub-navigation (L3+), each child scoped to exactly one parent (NV-6). Canonical 12-tab sidebar (Chat/Notifications/Channels/Sessions/Schedule/Pulse/Memory/Office/Kanban/Security/Wiki/Settings) with fixed order and live badges (NV-1); floor tab bar with lazy loading (NV-2) and live OfficeState icons (NV-3); two-tier settings hierarchy Global/Local (NV-4); IDE integration via shell-spawn from floor tab dropdown (NV-5). NV-7 L0 Building frame + application menu (File/Edit/View/Help, Providers/ACP, Process Monitor) acting across all floors; NV-8 floor = disk-bound workspace tab created via File-menu / "+" / drag-and-drop of a project folder; NV-9 pinned non-closable default home floor (organizer/life-planner backed by the state tier, hosting the building-level boss); NV-10 recursive per-subsystem L3+ sub-navigation (Schedule→Cron/Pulse, Inbox→Messages/Poll-Clarify). New subsystem candidates (Discover/Graph/Process Monitor) carried as UX-stage TBD (tech-agnostic) ; v1.3.0 UX-stage resolution from an approved desktop mockup — §4.1 sidebar catalog revised (primary run reordered + Dashboard/Employees/Automation added, Automation split from Office; foot utility group Channels/Security/Providers-ACP/Settings; Pulse folds into Schedule as an L3 facet), §4.5 gains the per-subsystem L3 facet catalog, new §4.6 fixes the File/Edit/View/Help leaf lists, NV-7 extended with the L0 file-tree dock + command palette; NV-1…NV-10 wording unchanged | Stable | 1 | 1.3.0 |
| [l1-context-attachment.md](specifications/l1-context-attachment.md) | Context attachment — attaching context references to an outgoing chat message via the active floor's browsable project file/directory tree (a right-dock companion panel in the Chat subsystem): reference-not-inline-copy resolved at send (CA-1); workspace-rooted live tree, no traversal outside root (CA-2); direct-manipulation drag-to-attach with full non-drag affordance parity — context-menu/keyboard/picker/@-reference (CA-3); explicit reversible removable-chip attachment, nothing attached from browsing (CA-4); bounded folder attachment (max count/size/depth + ignore rules), never a silent unbounded dump (CA-5); identity via the file subsystem's explicit-ingestion/content-addressed path, in-workspace files referenced by path without re-upload while external drops ingest as upload (CA-6); resolution passes the same access/egress gate as any file read, secrets never attached silently, fail-closed (CA-7); provenance-marked untrusted-by-default session entry with auditable message→attachments binding (CA-8); budget-aware accounting, over-budget surfaced not silently truncated (CA-9); composes file-management + navigation-model + application-shell + context-management + context-provenance + security; extended attachable item-kind catalog (memory items/kanban cards/sessions/URLs) carried as TBD (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-voice-input.md](specifications/l1-voice-input.md) | Voice input subsystem: on-device only — no audio or transcript leaves the device (VI-1); user review overlay before injection (VI-2); explicit push-to-talk or toggle activation only (VI-3); prominent active-recording indicator always visible (VI-4); cancellation discards everything atomically (VI-5); optional post-transcription transform — deterministic shaping (custom words/filter/locale-normalization) + opt-in consent-gated language-model assistance via prompt-template+structured-output through the egress gate, off by default, on-device default (VI-6); pluggable transcription engine — interchangeable backends incl. platform-native, accel-when-available with CPU fallback, optional streaming + auto-language (VI-7); on-device speech-model lifecycle acquire/store(content-addressed)/load/idle-unload, bundled default + on-demand catalog (VI-8); optional local transcription history of confirmed entries, re-injectable/searchable/deletable, never egressed (VI-9); non-destructive clipboard-safe injection with save/restore or synthetic input, failures surfaced (VI-10); pipeline ACTIVATE→CAPTURE(cpal 16kHz)→VAD(ONNX,smoothed)→TRANSCRIBE→[TRANSFORM?]→REVIEW→INJECT→[HISTORY?]; per-binding plain-vs-postprocess action mapping | Stable | 1 | 1.1.1 |
| [l1-deliberation.md](specifications/l1-deliberation.md) | Office deliberation protocol: independent parallel reasoning by diverse-specialty workers before synthesis (DL-1); specialty diversity via active workers or on-demand hires (DL-2); orchestrator finality — no voting rule (DL-3); append-only deliberation log in Channels tab (DL-4); token-budget-bounded rounds with truncation marker (DL-5); lifecycle OPEN→ARGUE(parallel)→SYNTHESISE→DECIDE→LOG; devil's advocate designation; log entry format; v1.1.0 DL-6…DL-9 — the **blind cross-critique round** between independent argument and synthesis, the step that makes deliberation more than "ask N times": each participant critiques the full set (strongest / biggest blind spot / what all missed), surfacing cross-argument flaws and panel-wide gaps no single independent argument contains (DL-6); critique on **anonymized, position-randomized** material so it evaluates the argument not who produced it or where it appeared, defeating deference and positional/primacy bias, de-anonymized only at synthesis (DL-7); **full-stance arguments** — independence means leaning fully into the assigned specialty or deliberately-opposed stance not hedging toward a balanced middle that collapses the DL-2 diversity, the tension being the instrument and balancing the orchestrator's job at synthesis not the participant's (DL-8); synthesis MUST name convergence, name clashes without smoothing them, surface the critique-caught blind spots, and MAY side with a **minority argument on merit** — averaging into "it depends", hiding disagreement, and defaulting to the majority all forbidden (DL-9, sharpening DL-3 from not-a-vote to disagreement-preserved-minority-can-win); §4.1 lifecycle gains an optional CRITIQUE step, §4.4a the blind-round mechanics | Stable | 1 | 1.1.0 |
| [l1-office-control.md](specifications/l1-office-control.md) | Office control: safe-checkpoint before freeze (OC-1); exact-state resume from checkpoint (OC-2); model substitution attempted before hibernation on quota exhaustion (OC-3); automatic wake on resource recovery (OC-4); OfficeState always visible via event (OC-5); OfficeState taxonomy (Active/Idle/Paused/Hibernating/Error/Offline); master switch; token exhaustion hibernation protocol with model-router fallback; per-subsystem pause granularity (scheduler/kanban-auto/automation/heartbeat) composing with master switch | Stable | 1 | 1.0.0 |
| [l1-version-control.md](specifications/l1-version-control.md) | Version control: role-based commit authority enforced by orchestrator (VC-1); quality gate required before any commit (VC-2); card-aligned commit boundary (VC-3); virtual staging area (VSA) atomicity via git worktrees — failed task discards all changes (VC-4); no direct push to main/trunk at any autonomy level (VC-5); non-git versioning out of scope (VC-6); trunk-based vs Git Flow strategy selection; role authority table (Worker/Orchestrator/Maintainer/Release Manager/Manager); Conventional Commits convention with card reference footer; remote platform neutrality | Stable | 1 | 1.0.1 |
| [l1-inner-monologue.md](specifications/l1-inner-monologue.md) | Inner monologue: non-interrupting background cognitive process (IM-1); log-before-act discipline (IM-2); bounded token budget per cycle (IM-3); no direct store mutations — dispatched via standard subsystem interfaces (IM-4); individually pauseable via Heartbeat/Pulse subsystem toggle (IM-5); cycle lifecycle SNAPSHOT→REFLECT→DECIDE→LOG→DISPATCH; 5 intention types (ProactiveMessage/AutomationTrigger/MemoryWriteProposal/TaskProposal/NoAction); 5 reflection focus areas; Pulse log format; proactivity threshold with suppression | Stable | 1 | 1.0.0 |
| [l1-lookahead-planning.md](specifications/l1-lookahead-planning.md) | Lookahead planning: pre-execution consequence simulation N steps ahead before committing to high-impact actions; LP-1…LP-6 invariants; trigger-scoped (LP-1 — 7-category catalog: branch merge/schema migration/file deletion/dependency upgrade/security policy change/mass refactor/architecture change); no real execution during simulation (LP-2); budget-bounded depth (LP-3); conclusion before commit (LP-4); graceful fallback to ORC-9 gate on budget exhaustion (LP-5); log persistence append-only (LP-6); simulation process EXTRACT→PROJECT→CONCLUDE→LOG→EXECUTE; conclusion types CONFIRM/MODIFY/ESCALATE/BUDGET_EXHAUSTED; early termination on unambiguously blocking consequences | Stable | 1 | 1.0.0 |
| [l1-simulation.md](specifications/l1-simulation.md) | Simulation — playing out a generated mechanism (workflow / automation pipeline / role interaction / task graph / schedule) to see how it behaves, in a modeled world with real effects suppressed, distinct from testing/evaluation/lookahead and unifying the project's scattered dry-run realizations: play-out not assertion — output is an observable trace of the mechanics, not a pass/fail verdict, observational not a definition-of-done gate (SIM-1); no production side effects — real actions (tool calls/writes/egress/state mutations/real-account spend/schedule fires) suppressed, mocked, or quarantined-and-recorded, a hard boundary generalizing the automation dry-run §4.9 + evaluation ES-11 quarantine (SIM-2); declared legible fidelity structural/modeled/shadow recorded with the result, never presents itself as more faithful than it ran, reusing log-legibility LL-5 honest-reduction (SIM-3); deterministic & repeatable via pinned/seeded nondeterminism so a mechanism is studyable/comparable/shareable (SIM-4); mechanism-addressable — simulates what was generated by identity+version, rehearsing before/without activation as a first-class use (SIM-5); observable play-out trace as the product, a projection over the observation substrate consumable by human and agent (SIM-6, composes l1-log-legibility); distinct from evaluation (grades) & lookahead (predicts one action to gate a commit) — the four-questions taxonomy behave/well/commit/match, composing never conflating (SIM-7); bounded & safe by default — step/time/token budget + loop guard, cannot escape the modeled world nor run unbounded, budget-exhaustion returns a partial honestly-marked play-out never real effects (SIM-8); reconciles automation dry-run + evaluation sandbox as SIM-2 realizations and lookahead as a sibling; nodus realization of the execution-mode-provenance half = l1-nodus-observability HO-12 (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-acp.md](specifications/l1-acp.md) | Agent Client Protocol: transport-agnostic protocol for external callers (other agents, CI, mobile clients) to interact with a running office; ACP-1…ACP-7 invariants; session-oriented stateful (ACP-1); capability declaration before interaction (ACP-2); streaming event protocol — typed events thinking/tool_call/client_tool_request/text_delta/done/error/budget_exhausted/interrupted (ACP-3); tool delegation boundary with client opt-in (ACP-4); idempotent session creation (ACP-5); graceful interrupt preserving session (ACP-6); budget transparency in every terminal event (ACP-7); trust levels trusted/restricted/anonymous; cross-office transparent relay; ACP-8 per-session monotonic event ordering (order/dedup by sequence, transport-reorder-tolerant, gap=dropped) + ACP-9 protocol projections are pure logic-free adapters (N surfaces over one session observe the identical event sequence) + §4.6 concurrent peer-agent/client-completion/UI-event projections with capabilities+trust enforced once beneath all; ACP-10 live steering (§4.7, steering/action_skipped events) — distinct from interrupt (ACP-6 which drains to a partial terminal and ends the turn), a client MAY inject a steering message that redirects an in-progress turn without stopping it, absorbed at the next safe boundary (after the current atomic step, never mid-step ORC-5); not-yet-started planned actions are cancelled and surfaced to the model as skipped-superseded (never silently completed so a "don't send it" redirect prevents the pending side effect, never silently dropped so the model re-plans knowingly); session-scoped so a steer can never leak into another conversation; same-session messages serialize into the one live turn's queue (WL-4 coalescing) while different sessions run concurrently; the fourth client-interaction control verb (redirect) beside interrupt/stop/pause/resume | Stable | 1 | 1.2.0 |
| [l1-global-orchestration.md](specifications/l1-global-orchestration.md) | Global orchestration: building-level coordination above individual offices; GO-1…GO-6 invariants; one global orchestrator (GO-1); non-intrusive coordination — no unilateral cancel/override (GO-2); phase-awareness enforcement — cross-cutting concerns (localization/observability/security/accessibility/error handling/budget-safety) declared as non-optional acceptance criteria per implementation phase (GO-3); unified read-only visibility of all offices' OfficeState+kanban+budget (GO-4); ACP routing mediation — global orchestrator is the routing decision layer (GO-5); escalation authority via cross-office deliberation rounds (GO-6); phase-awareness protocol QUERY→CHECK→ANNOTATE→PROCEED; cross-office delegation via ACP relay; building-level escalation with HITL gate | Stable | 1 | 1.0.0 |
| [l1-development-workflow.md](specifications/l1-development-workflow.md) | Agent-assisted development workflow: five-stage pipeline (Design→Plan→Execute→Review→Deliver); DW-1…DW-10 invariants; mandatory design gate (DW-2); task isolation via file handoffs (DW-3); two-stage quality gate per task (DW-4); durable progress ledger (DW-5); workspace isolation (DW-6); model-tier assignment (DW-7); human checkpoints for irreversible actions (DW-8); review neutrality (DW-9); critical findings block (DW-10); v1.0.1 links `l1-change-containment` as the Review-stage edit-footprint lens giving DW-4's "nothing extra" verdict a closed finding vocabulary; v1.1.0 DW-11 **release notes describe the delivered system, never the path to it** — the note is the base→shipped difference stated as what a consumer can now do; branch-internal version bumps, defects introduced and fixed within the branch, review outcomes, plan approvals, scope negotiations, merge/rebase activity and queued-not-shipped work belong to a different document, since the reader has no context in which they exist; a property of the shipped system is documented as a property never as a fix to something the reader never had; contributor-facing changes segregated; a delivery with nothing consumer-facing gets one honest sentence rather than padding; composes reference containment | Stable | 1 | 1.1.0 |
| [l1-agent-framework-skeleton.md](specifications/l1-agent-framework-skeleton.md) ; v1.0.1 links `l1-host-native-rendering` as the full contract AFS-14 hands off to (render determinism, fidelity classes, slug identity, declared destinations) and `l1-corpus-originality` as the catalog admission gate, demarcated from AFS-13 (lineage novelty, not catalog novelty) | Paradigm-neutral agent-framework meta-spec: primitive triad (Agent/Work-Unit/Coordinator), 5-pattern coordination catalog (sequential/fan-out-in/hierarchical/evaluator-optimizer/mesh) with failure modes + containment rules, 3 concrete engines (graph-superstep/conversation-speaker-selection/handoff-swarm); AFS-1…AFS-14 invariants; typed state channels with reducers + scoped handoff; cross-cutting concerns (HITL gates, observability contract, failure taxonomy, eval-driven dev, least-privilege); autonomous self-improvement loop (heartbeat, voluntary stop, generational lineage, text-only inheritance) with anti-collapse novelty rule (AFS-13); ordered conditional handoff (AFS-7); role catalog + canonical-source→per-host adapter distribution (AFS-14); ideas-to-adopt mapping (tech-agnostic) | Stable | 1 | 1.0.1 |
| [l1-task-graph-model.md](specifications/l1-task-graph-model.md) ; v1.1.0 TG-15 added - absent its source artifact, generation is **refused, never improvised**: TG-1 stated where a graph comes from and was silent on the case that actually occurs, and a planner asked for a plan produces one anyway, filling the absent requirement with plausible scope so the result is **indistinguishable in form** from a real graph while every unit below the invented part carries invented scope; an artifact that exists but is **substantially unsettled** counts as absent, and the refusal **names the upstream phase** (`l1-exploratory-planning`) rather than terminating | Task graph model — decomposition algebra complementing the board/pipeline: requirement-to-graph generation honoring stated constraints + gap-minimal (TG-1); hierarchical addressable units (TG-2) + dependency-DAG integrity with auto-repair (TG-4); deterministic next-selection priority→unblock-impact→id (TG-5); complexity-gated decomposition depth (TG-6); additive reversible expansion (TG-7); append-only implementation journal (TG-8); drift-driven forward re-planning (TG-9); isolated migration-safe planning contexts (TG-10); bounded status lifecycle pending/in-progress/done/review/deferred/blocked/cancelled (TG-11); role-bound generation with research/external-novelty channel (TG-12); coordinator/executor work-unit split (TG-13) with guarded autonomous delivery — isolated branch, green-gated commits, bounded retries, resumable run artifacts, confirm-gated outward actions (TG-14) (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-application-shell.md](specifications/l1-application-shell.md) | Reactive desktop application-shell runtime (frontend substrate complementing the fixed tab navigation): single-authority state with typed entity handles + weak-handle cycle-breaking (AS-1/AS-2); push-based notify/observe reactivity (AS-3); typed events with drop-deregistering subscriptions (AS-4); pure declarative render-from-state + render-only components (AS-5); named-namespaced action command vocabulary (AS-6); context-predicate keymap dispatch over a focus/dispatch tree with multi-keystroke sequences + precedence (AS-7); layered base→platform→user keymaps/settings, fully rebindable + palette (AS-8); workbench composition workspace→center-panes + edge docks/panels + floating modals/pickers under a uniform unit contract (AS-9); delegated reusable selection surfaces — one picker + delegate (AS-10); structured cancellation-on-drop async on dual foreground/background executors (AS-11); persisted restorable per-window layout distinct from content (AS-12); OS platform behind one context for agnosticism + simulated-context testability (AS-13) (tech-agnostic) ; v1.0.1 Related Specifications now links its implementation `l2-application-shell.md` (link-only patch) | Stable | 1 | 1.0.1 |
| [l1-fidelity-variants.md](specifications/l1-fidelity-variants.md) | Fidelity variants — the axis the runtime's degradation discipline **explicitly declines**: MR-15 trades speed for capacity and MUST NOT alter outputs, classifying a result-altering configuration as *a different configuration*, and that class had no owner. **A fidelity variant changes the output; a placement tier does not**, and conflating them lets a quality regression enter through the door reserved for changes promised not to alter anything (FV-1); the variant set is declared, ordered, and profiled with its resource cost and quality effect, since an undeclared "fast mode" is a second product shipped without a name (FV-2); selection is explicit or policy-driven and **never opportunistic**, because silent substitution yields consecutive results whose difference has no recorded cause — indistinguishable to a user from the system becoming unreliable (FV-3); the variant **travels with the artifact** as a recipe field, since an artifact whose fidelity is unrecorded will be compared anyway (FV-4); **measurement is within-variant**, generalizing MB-5 (which already folds quantization into the identity of the model under test) to every variant-bearing capability and every instrument, with mixed-variant comparisons reported invalid rather than averaged (FV-5); a **declared floor** below which the honest answer is a stated refusal with the shortfall, because a chain of individually reasonable reductions otherwise terminates in output that is cheap and useless with nothing objecting (FV-6); **no silent upgrade either** — spending budget the requester did not authorize is the same defect pointed the other way (FV-7); the quality effect **measured and sliced**, never assumed monotone, since lower fidelity typically collapses one slice while barely moving the mean, and `UNKNOWN` is a first-class value rather than an omission (FV-8); a **named reference variant** anchoring the axis, without which "the fast one" and "the good one" drift into unrelated products with no common yardstick (FV-9). CE-10's select-cheap-produce-once is the worked special case, referenced not restated. **nodus projection, no new primitive**: the `§config` surface declares the choice with a stale id resolving to the reference, the run-record annotation carries it, and the environment contract's comparability partition (profile/budget/measure) already expresses the within-variant rule. Composes model-runtime (MR-7/MR-15/MR-16) / competitive-execution (CE-10) / model-benchmarking (MB-5) / agent-coevaluation (ACE-4/ACE-5) / reproduction-recipe (RR-2) / outcome-attributed-cost / usage-allowance (UA-8) / generation-shaping (GS-4) / outcome-confidence (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-model-runtime.md](specifications/l1-model-runtime.md) | Local model runtime — lifecycle substrate routing schedules onto: local-first on-device serving with explicit remote opt-in (MR-1); provider-abstracted backend contract load/generate-stream/embed/describe/unload (MR-2); content-addressed immutable-blob model store with manifest digest pinning + dedup (MR-3, reuses l1-file-management); verifiable resumable named registry acquisition + acquire-on-run + export (MR-4); portable declarative model definition base+params+template+system+adapters built without copying weights (MR-5); explicit load/unload lifecycle with idle/LRU eviction + resident-model introspection (MR-6); fit-gated hardware scheduling — feasibility gate with placement full-accel/partial-offload/CPU + shortfall report, scoring delegated to router (MR-7); streaming inference contract + industry-compatible surface (MR-8); full catalog management list/show/copy/remove with blob GC (MR-9); managed observable background server start/stop/status + logs + health (MR-10); thin CLI/SDK clients over the server, single source of truth, graceful offline degradation (MR-11); loose-or-digest reproducible references with served-digest recording (MR-12); multi-device placement — feasibility gate over an accelerator set, split across devices before CPU fallback, per-device residency/shortfall, mixture-of-expert footprint sizing (resident-total vs active-per-token) (MR-13); calibrated honest estimates — figures labeled estimate-vs-measurement, recalibrated by recorded (model,placement,hardware) residency/throughput, community-measured corpus as opt-in telemetry-gated extension (MR-14); v1.2.0 MR-15 **degrade before refusing** — where a model does not fit the fastest placement the runtime MUST consider a named degradation tier trading throughput for capacity before refusing under the MR-7 fit gate, refusal becoming the last answer rather than the first, because on a local-first system running on hardware the user already owns "your machine cannot run this" is far more often a failure of the runtime than a fact about the machine and a runtime that only refuses is unusable on exactly the modest hardware it exists to serve; three obligations — the tier's cost **stated before the run** as a calibrated basis-carrying estimate (MR-14) so a slow run is chosen rather than discovered, the tier **recorded on the run** so a slow result is attributable to the placement instead of mistaken for a model or prompt regression, and a tier **changing speed never outputs** (a placement that alters results is a different configuration and therefore a reproduction-recipe field, RR-2 ambient layer, not an invisible operational choice); v1.3.0 MR-16 **feasibility is a running property, not a load-time verdict** — MR-7 answers *can this start here*, not *will it still fit an hour in*: the **load transient** can peak near twice the resident footprint (a model that fits once resident may fail while arriving, so the gate reasons about the peak and the loading path honours the approved figure); **resident ≠ active**, so a single size scalar mis-predicts memory and throughput in opposite directions for architectures where only a fraction of parameters participate per step; and **demand grows with the working context**, per-sequence attention state growing with the sequence at an architecture-declared rate, so a run that started comfortably crosses the boundary later. Estimates are driven by declared resource characteristics riding the MR-5 portable definition (absent characteristic = degraded confidence per MR-14, never a zero), and a mid-run crossing is a **managed transition** — reduce the working set → named degradation tier (MR-15) → honest stop with the shortfall — recorded on the run, since an unrecorded mid-run placement change is the misattribution MR-15 exists to prevent (tech-agnostic); v1.3.1 links `l1-fidelity-variants` as the owner of the class MR-15 explicitly hands off — a result-altering configuration is not a degradation tier | Stable | 1 | 1.3.1 |
| [l1-agent-tool-ergonomics.md](specifications/l1-agent-tool-ergonomics.md) | Agent tool ergonomics — design discipline for agent-facing tool surfaces (MCP/extension/in-session) that agents actually use well, won by shaping tool I/O around existing agent behavior not by instructing the agent: sufficiency-to-stop as the design target — output complete enough the agent doesn't fall back to manual discovery (ATE-1); recoverable conditions return success-shaped guidance, hard/isError reserved for stop-trying — early false errors teach session-wide abandonment (ATE-2); absence as the unmissable signal — empty tool list + inactive notice over tools that all fail (ATE-3); adapt-the-tool-to-the-agent over instruct-the-agent — influence channels (descriptions/init instructions) are low-salience and don't move tool choice (ATE-4); output budget monotonic in workload — larger tier never smaller per-item budget (ATE-5); explicit per-item staleness signaling (ATE-6, general form of CI-3); never steer to a manual fallback (ATE-7); lean surface by measured pick-rate folding rare tools inline (ATE-8, reuses TC-7); validate by A/B ablation on the floor model — affordance that lands on the weakest model generalizes up (ATE-9); ideas-to-adopt + nodus error-taxonomy/sufficiency/floor-model relevance; per-call intent declaration on mutating tools, schema-level and ordered before payload (ATE-10); opaque reference passing with host-side hydration, copy-verbatim identifier discipline (ATE-11); host-side overflow guard for uncontrolled tool results — a result exceeding a declared host bound is offloaded whole to durable local storage and replaced by a bounded navigable reference (retrievable/searchable on demand), never blowing the window nor silently truncated, the lossless trace-recorded scoped exception to ATE-7 for third-party tools the host cannot redesign, tool-output dual of RD-1, composing ATE-11 (ATE-12, §4.5); v1.3.0 ATE-13 **unresolved address ≠ wrong invocation form** — opposite corrective actions (find another capability vs correct a call to one already found), so collapsing both into "unknown tool" makes an agent abandon a reachable capability (the ATE-1 manual-fallback failure one step earlier); the wrong-form rejection enumerates the forms the target does offer (one corrected call, not a search), both success-shaped per ATE-2, neither steering to a manual fallback per ATE-7; also an ATE-8 coverage signal — a target never invoked in an offered form is misadvertised, not misunderstood (tech-agnostic) | Stable | 1 | 1.3.0 |
| [l1-tool-call-transport.md](specifications/l1-tool-call-transport.md) | Tool-call transport — the wire seam by which a logical tool invocation crosses the agent↔model boundary and the reply is decoded back: logical/wire separation, a tool call/result defined independent of transport with at least a native structured channel and a text-encoded protocol (TCT-1); deterministic pure bidirectional encode/decode round-trip holding no loop state (TCT-2); capability-detected per-turn selection preferring native, encoded as the universal fallback so a no-native-tool-API backend is never a dead end, explicit recorded override (TCT-3); single tool-schema source (tool-composition TC-2) rendered transport-idiomatically — native tool list vs injected description block, never double-authored (TCT-4); reasoning isolation — a thought span lifted out before call parsing so private reasoning is never mis-parsed as intent (TCT-5); multi-call fidelity — zero/one/many calls per turn decoded in order, never dropped/merged/reordered, dispatch order delegated to the tool DAG (TCT-6); streaming-safe incremental decode — partial protocol tokens buffered not flashed, cancellation leaves no half-parsed call (TCT-7); typed recoverable malformed-call containment re-injected for output-contract repair-retry under the generation budget, never crash/silent no-op (TCT-8); result re-injection symmetry across transports and mid-session transport switches (TCT-9); provenance-gated decode — only the model's own channel decoded, an encoded look-alike in untrusted retrieved/tool/user content is inert (TCT-10, reuses context-provenance CP-1/CP-2); ideas-to-adopt mapping (native-vs-prompted tool-calling, symmetric pre/postprocess, streaming partial-token tolerance) + nodus disposition Reuse behind ModelProvider LP-2, no new nodus invariant (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-retrieval-evaluation.md](specifications/l1-retrieval-evaluation.md) | Retrieval evaluation — measurement subsystem for ranked-recall quality across any surface (memory recall / RAG / code-index / router cache): labeled version-controlled fixture as ground truth, never self-graded (RE-1); the real production pipeline under test, not a stub (RE-2); standard IR metric set P@K/R@K/MRR/nDCG@K reported separately, no opaque single score (RE-3); graded relevance binary-compatible (RE-4); baseline persistence with provenance + signed-delta reporting (RE-5); configurable regression gate with recorded override (RE-6); retrieval-mode A/B comparability lexical/semantic/hybrid/reranked (RE-7); per-query fault isolation to zero-score not abort (RE-8); tag-sliceable + sample-honest aggregates reusing PA-7/PA-8 (RE-9); surface-agnostic ranked-recall contract query→ordered-ids (RE-10); on-device privacy-aware fixtures (RE-11); distinct eval-family member beside customization-behavior suites and session-coaching analytics; ideas-to-adopt + nodus baseline/regression-gate methodology mapping (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-claim-verification.md](specifications/l1-claim-verification.md) | Claim/grounding verification — faithfulness counterpart to retrieval evaluation: (claims, sources) → per-claim ternary verdict (supported/contradicted/unverifiable) with mandatory evidence attribution; claim-level granularity (CV-1); source-grounded never self-graded knowledge (CV-2); ternary verdict, unverifiable ≠ contradicted (CV-3); citation-or-downgrade-to-unverifiable (CV-4); verifier independence from generator, same-lineage = reduced confidence (CV-5); non-authoritative advisory boundary, never silently edits (CV-6); gate composition reusing output-contract retry/escalation (CV-7); bounded + observable + on-device by default (CV-8); one surface-agnostic contract across answer/wiki/report/research (CV-9); the runtime hallucination detector enforcing previously-asserted-only anti-hallucination (PW-4/OL-4/OL-7); ideas-to-adopt no-duplication mapping (guard/redact/scan/test/on-device-guard-models/SDK-CLI-MCP-hook → existing security stack; red-team + unified content-safety classifier as refinement candidates) + nodus-relevance (VERIFY-class ^validator + grounding error code mapping ternary→severity) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-completion-verification.md](specifications/l1-completion-verification.md) ; v1.3.0 CMP-8 **evidence does not travel upward - a roll-up re-executes, it does not inherit**: a parent/branch/orchestrator re-runs the child's own authoritative checks rather than reading its recorded status (which reproduces what was true in the child's environment before every later change, including the parent's own integration), a child whose oracle no longer passes **returns to unmet**, and an explicitly handed-off required outcome is **terminal non-success** that never aggregates into a complete parent at any level (complete siblings + one handoff = a handoff) - extending CMP-3 from the delegate's *word* to the delegate's *record*; CMP-9 **evidence names the environment it was produced in** (interpreter, location, name-resolution context, observed termination), so a material difference on re-execution is *unverified - environment differs*, a mismatch to resolve rather than a pass to accept - the environmental axis of CMP-1 temporal freshness (stale in *place* as well as stale in *time*) ; v1.4.0 CMP-10 added - a completion requiring a **counterparty** is not satisfied by the actor's own contribution: CMP-1...CMP-9 all govern claims about work the actor did, and a distinct class escapes every one of them - an outcome whose definition requires a second party's participation (a human decision, an approval, an interview answer, an external acknowledgement) where the actor supplies that party's side itself; every prior invariant passes because the work was real, the evidence fresh, the check proving and the report honest - what was fabricated is the **participant**, not the report; the counterparty's contribution must be **present and attributable**, absence yields *not complete* rather than complete-with-an-inferred-answer, and the detectable shape is named because it is what occurs: **a record with one participant, and an outcome attributed to nobody** | Completion verification — the **work-state** member of the grounding/honesty family (beside claim-verification for *facts* and tool-receipts for *actions*): a claim that work is complete/passing/fixed/succeeded needs **fresh same-turn evidence** from the authoritative check (test output+exit, build exit, original symptom re-run), else the *actual verified* state is reported — extrapolation/prior-run/"should pass"/confidence/partial-check is not evidence (CMP-1); the claim **names its check** (command+full-output+failure-count, reconstructable/falsifiable), an unrunnable check ⇒ `unverified` not asserted (CMP-2, composes OI-8); **delegated success is verified independently** against an observable artifact the subordinate did not author the report of (actual diff/output/RunResult), never relayed on the subordinate's word — a self-report is a signal to verify not a verdict to relay (CMP-3, the cross-actor case tool-receipts TR-8 leaves open); the **check proves the claim not a weaker proxy** — linter≠build, build≠tests, one-pass≠regression-fixed (red-green), green-suite≠requirements-met (checked line-by-line, gaps reported) (CMP-4); **no unverified completion status is surfaced as done** on any surface (status line/report/hand-off/commit-PR/orchestrator roll-up), unverified is labeled never rendered as done (CMP-5); closes the highest-frequency agent dishonesty — "done!" without running the check, and relaying a sub-agent's unverified "success" upward — sitting in the gap between claim-verification (facts) and tool-receipts (actions); nodus-relevance needs no new NL invariant — RunResult/@err/!!/@test already ground outcomes, CMP is the host-side "don't lie about the RunResult" consumption discipline; v1.1.0 CMP-6 — a probabilistic outcome estimate is **not evidence in either direction**: however well calibrated it may never satisfy/replace/weaken/shorten/waive a required check, and a check's absence is never excused by a high estimate; composition is one-way (evidence decides *done*, an estimate decides *how hard to look and whom to involve*), so a low estimate may demand more checking than the minimum while a high one may never demand less; the converse is closed too — a passing check does not license suppressing a low estimate from the record or hand-off, since the estimate's value is flagging the gaps no check was written for; both travel with the outcome, neither overwrites the other; v1.2.0 CMP-7 **a dismissal is a claim and needs the same evidence** — the symmetric assertion that a failure is *not this work's* ("pre-existing", "unrelated", "flaky", "fails on main too") now requires the check run against the **baseline** with output shown: reproduces there = pre-existing, does not reproduce there = **yours** whatever the reasoning suggests, baseline unrunnable = *unverified, may or may not be related* carried as a risk; demanding proof to say *done* while accepting confidence to say *not mine* leaves the cheapest escape hatch open and hides real regressions when they are cheapest to find (tech-agnostic) | Stable | 1 | 1.4.0 |
| [l1-code-intelligence.md](specifications/l1-code-intelligence.md) | Code intelligence — the source tree as a queryable semantic graph and agent-facing ground truth for code understanding: parser-agnostic graph core with typed node/edge taxonomy (CI-1/CI-2); cache-not-authority best-effort discipline with mandatory location validation (CI-3); persistent + incremental + namespace-isolated multi-project store with content-fingerprint skip (CI-4); model-free structural analysis family — depth-bounded traversal, impact/blast-radius classified by change kind (rename/signature-vs-body), circular deps, hot paths, dead/unused, entry points, complexity, module summary (CI-5); intent-aware budget-bounded context assembly with reported compression accounting entities-in-graph/traversed/kept (CI-6); composite edit + change/PR bundles joining graph+VCS+tests+memory+docs — blast radius/test-gaps/diff-aware signature-vs-body/stale-docs/reviewers/commit-hint (CI-7); hybrid lexical+semantic retrieval with lazy embeddings + model-free graph-only fast path (CI-8); docs↔design verification forward/reverse/gaps + architecture-doc generation, drift-reported never auto-edited (CI-9); memory anchoring to graph entities with change-driven invalidation (CI-10); capability-surface profiling (CI-11); interoperable open-ended export dot/json/csv/triples + property-graph-push/vault-wiki/diagram (CI-12); secret-exclusion at ingestion (CI-13); node summaries for AI navigation (CI-14); vocabulary-grounded query + navigable BFS/DFS/path/explain surface (CI-15); multi-modal + external-system ingestion breadth (docs/papers/images/audio + DB-schema/dependency-manifest/PR/query-log, file_type-tagged, cross-modal edges); audit report with honesty rules; reference resolution + indirect-edge synthesis with provenance + close-flow-end-to-end (CI-16); measured resolution coverage with honest frontier (CI-17); per-item staleness signaling (CI-3→ATE-6); ideas-to-adopt + nodus workflow-graph relevance mapping (tech-agnostic) | Stable | 1 | 1.2.0 |
| [l2-technology-stack.md](specifications/l2-technology-stack.md) | Validated cross-platform technology stack; local LLM provider catalog (Ollama/llama.cpp/MLX/vLLM/LM Studio/Docker, GpuBackend taxonomy, GPU detection cascade, special-platform notes); quantization system (GGUF/MLX/AWQ/GPTQ hierarchy, bpp/speed-multiplier/quality-penalty table, best-quant-for-budget algorithm, KV cache quantization formats); audio processing stack (16 kHz sample rate, VAD/ONNX pipeline, cpal/rubato/rodio, platform mute cascade, stream lifecycle); §4.8 config serialization format policy — JSON default + only boundary format, RON narrow opt-in for Rust-owned hand-edited enum-heavy config (justified `ron` dep), TOML for build manifests, serde-uniform no big-bang migration, nodus exempt/format-neutral (LP-1 zero-dep); §3 Windows always-on mechanism reconciled to the S4U scheduled task (not a Windows Service) per l2-service-activation §4.2  ; v1.2.2 extended §3 Invariant-Compliance to INV-8/9/10 (single-deployable stack — one Rust/Tauri binary, no orchestrator, anti-microservice by choice; the contract as the single binding target; adapter representations mapped behind the seam keeping DN-3 buildable) — completeness fix, gate restored | Stable | 2 | 1.2.2 |
| [l2-core-library.md](specifications/l2-core-library.md) | Layer 1: embeddable core library (foundation); manager pattern (Arc + RAII loading guard + idle watcher thread + backend enum dispatch); typed prefixed ID system (ses/msg/prt/per/que/evt/job/wrk/tool/pty prefixes, ascending/descending factories, monotonic base62 26-char body, branded type enforcement); ThinkingLevel taxonomy (off/minimal/low/medium/high/xhigh, prepareNextTurn-settable); CustomAgentMessages declaration-merging extensibility (augment interface for typed custom message variants)  ; v1.2.1 extended §3 Invariant-Compliance to INV-8/9/10 (the core IS the monolith; the honest binding referent for frontends; owner of the inward domain↔adapter seam — domain on contract types, adapter representations mapped) — completeness fix | Stable | 2 | 1.2.1 |
| [l2-cli.md](specifications/l2-cli.md) | Layer 2: command-line frontend  ; v1.0.1 extended §3 Invariant-Compliance to INV-8/9/10 (sanctioned frontend↔core boundary; honest verb surface — the strong INV-9 case; consumes contract types only, no adapter shape) — completeness fix | Stable | 2 | 1.0.1 |
| [l2-tui.md](specifications/l2-tui.md) | Layer 3: terminal UI frontend  ; v1.0.1 extended §3 Invariant-Compliance to INV-8/9/10 (frontend boundary; honest slash-command surface; binds contract types only) — completeness fix | Stable | 2 | 1.0.1 |
| [l2-app-ui.md](specifications/l2-app-ui.md) | Layer 4: desktop/web/mobile application UI/UX (theming + localization); UI stack: React 19 + Vite + TypeScript + Tailwind CSS v4 + shadcn/ui + Lexical; settings persistence (load_or_create, additive migration, dual deserializer, platform defaults, AtomicU8 hot settings); tray icon state machine (State × Theme = 9 variants, state-dependent menu, copy-last-result fallback); global shortcut binding system (ShortcutBinding struct, dual backend with auto-rollback, suspend/resume, dynamic shortcuts); overlay window system (OverlayPosition, per-OS implementation, escape hatch); single instance + runtime-only CLI flags + shared trigger entry point; per-provider system prompt dispatch (provider-keyed variants for anthropic/gpt/gemini/kimi/etc.); XML structured environment context (`env` + `available_references` XML blocks); MCP client transports (Stdio/SSE/StreamableHTTP, DEFAULT_TIMEOUT=30_000ms), roots capability, connection status states (connected/disabled/failed/needs_auth/needs_client_registration), OAuth flow with pending transport map; §4.5 theming resolves the custom-theme TBD — built-in themes are the built-in layer of the l1-design-identity catalog, user/imported identities its user layer (DI-2/DI-4/DI-8)  ; v1.3.1 extended §3 Invariant-Compliance to INV-8/9/10 (embedded-core/client boundary; honest UI affordances — no dead controls, DIR-9; renders contract types over IPC) — completeness fix ; v1.4.0 §4.5 Theming split into two orthogonal axes — OS-appearance **mode** (system/light/dark) × named **colour scheme** (a design-identity token package; one built-in `default` ships, more added as data); resolver + token contract move to the new l2-design-system; §4.1 Surfaces regrouped and deferred to l2-navigation §4.3 as the ordering authority; Document History section introduced ; v1.4.1 cross-link only — added l2-ui-module-topology.md (source partition of `packages/ui`; §4.1 surfaces populate its surface tier, its single-seam rule keeps the §4.2 bridge the sole site performing core calls) | Stable | 2 | 1.4.1 |
| [l2-filesystem-layout.md](specifications/l2-filesystem-layout.md) | OS-native filesystem layout: tiers, trees, DB placement, memory paths; top-level skill stores in both tiers (program preset + state mutable); per-office wiki.db projection cache under the workspace wiki/ dir | Stable | 2 | 1.2.0 |
| [l2-memory-store.md](specifications/l2-memory-store.md) | Memory store: SQLite + sqlite-vec + FTS5 + tags, archivist curator; trust scoring (asymmetric +0.05/−0.10, TRUST_MIN_SEARCH=0.3), shallow entity links (memory_entity + junction), HRR phase encoding (model-free, SNR-guarded); Bellman propagation (gamma=0.9, alpha=0.1, max_depth=2, threshold=0.5), session chaining (2h window, Continuation links), VerificationState weights (0.30→1.00); lightweight quick memory (MEMORY.md + USER.md per role, §-delimiter, configurable char limits 2200/1375); two-phase consolidation pipeline (Phase 1: bounded-parallel rollout extraction with lease/claim; Phase 2: global-lock git-baseline diff-driven consolidation sub-agent; MAX_ROLLOUTS_PER_STARTUP=2, MAX_ROLLOUT_AGE_DAYS=10, MIN_ROLLOUT_IDLE_HOURS=6, MAX_RAW_MEMORIES=256, MAX_UNUSED_DAYS=30); quick memory recall lifecycle (ExtractCursor processedOffset, skip reasons memory\_tool/already\_running/queued, patch filter rules; dual-path recall model→heuristic fallback, MAX\_RECALL\_DOCS=5, MAX\_DOC\_BODY\_CHARS=1200; Forget stable entry IDs relativePath[:index]; MEMORY.md limits 200 lines/150 chars/25KB); bi-temporal metadata (valid\_at/created\_at separation, superseded\_at, was\_valid\_at/was\_current\_at); typed MemoryKind (ArchitecturalDecision/DebugContext/KnownIssue/Convention/ProjectContext) + MemorySource enum; confidence field (write-time certainty, multiplied with trust\_score in recall); CodeLink (node\_id, node\_type, relevance, line\_range) + graph-proximity recall boost (w\_graph=0.2); CodeChangeType→SuggestedAction auto-invalidation (Deleted→Invalidate@1.0, SignatureChanged→Review@0.9, MajorRefactor→Review@0.8, MinorEdit→None, Renamed→Update@0.7, Moved→Update@0.7); multi-script lexical robustness (FTS5 MATCH→LIKE fallback for CJK/unsegmented scripts, §4.2.1); diversity & recency ranking refinements (config-gated MMR + opt-in recency, §4.2.2); MEM-4 reconcile — learned corpus store-authoritative (content column is truth, no notes/*.md dual-write), authored quick-memory (MEMORY.md/USER.md) text-authoritative; FR-6 disclosure — shipped HRR encoder is a zeroed-vector stub, fallback recall leg inert until a real encoding lands | Stable | 2 | 1.4.1 |
| [l2-memory-consolidation.md](specifications/l2-memory-consolidation.md) | Implements l1-memory-consolidation. Local SQLite realization of MC-1…MC-10 as an off-hot-path maintenance tier composing l2-memory-store (no new engine), reached only through the UserDataStore seam (l2-crate-topology) so the substrate stays backend-swappable: processing-depth column raw/working/consolidated with never-rewrite-raw guard (MC-1); incremental failed-not-checkpointed watermark over the §4.12 two-phase pipeline (MC-2); typed additive-only in-text edges + mandatory provenance edge over §4.7 entity links, additive enforced by the conservation check (MC-3); closed create/corroborate/refine/correct action algebra recorded per write, upstream of MI-4 (MC-4); load-bearing memory_signal fact-vs-derived table (centrality/cluster_id/recency/archived, version+computed_at, absent/stale→neutral multiplier 1, cold-start=base relevance) (MC-5); four-action maintenance pass merge(elevated multi-sample gate, else surface)/split(additive)/archive(centrality-cushioned, MI-9, auto-thaw)/summarize with anti-cycle cooldown + maintenance_audit (MC-6); edge-coverage-structural emergent summary nodes, no privileged type (MC-7); multiplicative offline-precomputed ranking text×centrality×cluster×recency wrapping §4.2 base relevance, weak-signal veto, zero hot-path model/graph-walk (MC-8); optimistic-concurrency + conservation-check + transactional whole-or-rollback co-edit safety (MC-9); read-only advisory interest-topic surface to l1-inner-monologue (MC-10) | Stable | 2 | 1.0.2 |
| [l2-memory-intelligence.md](specifications/l2-memory-intelligence.md) | Implements l1-memory-intelligence. Local realization of MI-1…MI-13 as the caller-facing query surface behind the UserDataStore seam (l2-crate-topology, backend-swappable), composing l2-memory-store (§4.2/4.3/4.4/4.6/4.14/4.15) + l2-memory-consolidation (MC-8 signals) with no new engine: answer grounded projection (KB-6 attribution + CV-3/CV-4 gate, extractive degrade with no generator) (MI-1); three temporal modes as-of/changed-since/recent over the §4.14 bi-temporal record (MI-2); immediate recall-visibility with async enrichment (MI-3); conflict routing over §4.4 reconcile — unambiguous auto-supersede via §4.14, ambiguous → structured report (MI-4); scheduler+dashboard intelligence digest (MI-5); salience-gated capture policy over confidence/MemorySource/MemoryKind + actor/expiry/subject metadata (MI-6); end-of-run procedural distillation (MI-7); closed structured predicate eq/ne/ordering/membership/containment + AND/OR/NOT compiled to SQL WHERE, post-fetch fallback (MI-8); reversible lifecycle states active/paused/archived/deleted with prune-protective guard over MEM-5 decay (MI-9); capture-time relative→absolute normalization (MI-10); include/exclude/custom capture directives (MI-11); raw vs inferred write modes, raw = no-generator escape hatch (MI-12); gated experience reuse — typed success/failure/insight, reuse gate sim+score+freshness, reused-not-re-derived attribution, retained SEC-9/10 authority gate (MI-13). Discharges the two L1-deferred params: MI-4 ambiguity threshold (confidence/trust-gap + recency-dominance) and MI-5 digest cadence (per-session-close + daily floor, opt-in per office) | Stable | 2 | 1.0.0 |
| [l2-workspace-management.md](specifications/l2-workspace-management.md) | Desktop tab UX, creation form, kebab naming, template→state, manager bootstrap | Stable | 2 | 1.0.0 |
| [l2-navigation.md](specifications/l2-navigation.md) | Four-layer building navigation render (Tauri v2 + React 19): Building frame (L0 menu + Providers/ACP + Process Monitor), floor tab bar with lazy load + live OfficeState icons, frozen sidebar catalog (L2), recursive mechanism sub-nav (L3+), two-tier settings, Open-in-IDE launcher; `Implements: l1-navigation-model.md`; maps NV-1…NV-10 ; v1.1.0 tracks l1-navigation-model v1.3.0 — §4.3 splits the catalog into `SIDEBAR_PRIMARY` + a foot `SIDEBAR_UTILITY` run (Pulse → Schedule L3), §4.1 component tree adds the right-edge file-tree dock + command-palette overlay (AS-10) + full-screen global-settings overlay, new §4.6/4.7/4.8, NV-1/4/7/10 compliance rows updated; palette/dock/overlay are thin-runtime view state (no core layout persistence in this slice) | Stable | 2 | 1.1.0 |
| [l2-kanban-board.md](specifications/l2-kanban-board.md) | Board storage, transitions, auto-archival store, execution semantics (atomic checkout, monitor scheduling, delegation depth); extended card fields (assignee, priority, skills[], workspace_kind/path, max_retries); KanbanRun log (per-attempt execution record, KANBAN_IPC_TIMEOUT_MS=20k); KanbanEvent + KanbanComment append-only logs; sprint-status.yaml format (generated/last_updated/project/development_status/action_items; story key N-N-name e.g. 1-2-user-auth; story lifecycle backlog/ready-for-dev/in-progress/review/done; epic lifecycle backlog/in-progress/done; retrospective optional/done; action_items epic+action+owner+status; sequential story creation — next story created only after previous is done); task XML format for plan execution (type=auto/tdd/checkpoint:decision/checkpoint:human-verify; gate=blocking triggers .continue-here.md; files/read_first/action/verify/acceptance_criteria/done fields; gate=advisory non-blocking); priority-ordered story cards (P1 MVP/P2 important/P3 nice-to-have tiers; cross-tier sequencing enforced by orchestrator; TUI grouping with 🎯 P1 marker; P2 gate fires after all P1 done; P3 auto-deferred below capacity threshold); KAN-8 custom columns with mandatory canonical `anchor` in board.json + custom boards as saved views (no second card store) | Stable | 2 | 1.1.0 |
| [l2-scheduler.md](specifications/l2-scheduler.md) | Friendly recurrence + raw cron, per-workspace storage, firing, routine execution policy (concurrency, catch-up, idempotency, webhooks), event-driven triggers (threshold counter, singleflight dedup); cron isolated session execution (session key isolation, model preflight, run log, delivery dispatch — local/announce/session targets, failure notification); enriched CronJob fields (repeat{times,completed}, deliver[], skills[], script) | Stable | 2 | 1.0.4 |
| [l2-quality-pipeline.md](specifications/l2-quality-pipeline.md) | Per-language toolchain map (incl. JS/TS structural analysis), local/pre-commit/CI gate runner, check command surface; 4-phase codebase audit workflow (recon→parallel audit→vet→plans; quick/standard/deep effort levels); 9-category finding format (correctness/security/perf/tests/tech-debt/deps/DX/docs/direction; leverage ordering; direction findings separate); self-contained plan template (plans/NNN-{slug}.md; drift check; verification gates; STOP conditions; plans/README.md index with status machine); live dependency vulnerability lookup (OSV.dev batch API, cross-ecosystem PyPI/npm/crates.io/Go, 1h in-memory cache, offline fallback list, 10s timeout non-blocking); SARIF 2.1.0 output format for CI/CD integration (severity mapping, gate-qualified rule IDs, single upload for scanner + gate findings); 5-field capability contract (Why/CAP-N intent+success/Constraints/Non-goals/Success signal; CAP-N monotonic stable ids; WHAT not HOW intent; testable Given/When/Then success criterion); adversarial review protocol (cynical posture, 10-finding floor, HALT on zero; 3-lane parallel subagent review: Blind Hunter/Edge Case Hunter/Acceptance Auditor; critical+high surfaced first, medium/low rolled up); goal-backward verification protocol (assume goal NOT achieved until codebase evidence proves otherwise; VERIFIED/FAILED BLOCKER/UNCERTAIN WARNING classification; "SUMMARY.md claims not evidence"; VERIFICATION.md format with status: passed/failed/needs-review gate); decision ladder for code generation (6-rung YAGNI waterfall: YAGNI/stdlib/platform-native/existing-dep/1-liner/minimum; stops at first true rung; hard exceptions for validation/security/error-handling); over-engineering audit (deletion-focused tags: delete/stdlib/native/yagni/shrink; single-file `L{line}:{tag}` format; "net: -N lines possible"; whole-repo format ranked by cut size); comment ledger convention (`cronus: {ceiling}, {upgrade trigger}`; rot-risk tracking for markers with no trigger; harvestable by audit scan); 3-dimensional verification (COMPLETENESS/CORRECTNESS/COHERENCE as independent dimensions; dimension check protocols; output format with ✓/✗/⚠; archive blocked on FAILED; UNCERTAIN requires acknowledgment); RFC 2119 enforcement (SHALL/MUST→FAILED on violation; SHOULD→UNCERTAIN; MAY→never a finding; well-formed requirement examples; bad-to-good rewrites); complexity justification record (mandatory table in CONTEXT.md or PLAN.md when violation accepted: Violation/Why Needed/Simpler Alternative Rejected Because columns; absent table = must act; escalated after one milestone unresolved); independent test guarantee (per-story field: Trigger/Expected/State reset; planner verifies non-circular before In Progress; blocked transition if field absent); proposal maturity grading (A-F rubric; 5 dimensions: constraints/success-criteria/verification/context/specificity; draft→ready blocked below C grade; per-dimension issues list; per-phase average grade in retrospective); spec-driven start detection (spec-driven/planning/unstructured classification; SUMMARY.md start_mode+start_confidence; ⚠️ unstructured P1 warning; retrospective start-mode distribution); confidence calibration+mode gates (1-10 scale; daily≥7/comprehensive≥5; independent sub-task verification; filter stats: candidates→exclusion→confidence-gate→reported; sha256 fingerprinting for trend ↑↓→); data sensitivity classification (RESTRICTED/CONFIDENTIAL/INTERNAL/PUBLIC; blast_radius in SUMMARY.md; incident response playbook per category: secret-leak/webhook/CVE/CI-injection); review readiness dashboard (SUMMARY.md ## Readiness tree: Plan/Code/Test/Security ✓/⚠/✗; any ✗ blocks; cronus finalize --force "{reason}" override writes exception to CHANGELOG; auto-runs after finalize --workflow=run/spec); boil-the-lake anti-shortcut (delta_lines × 0.3 min/LOC vs 60-min threshold; [SHORTCUT DETECTED] report; justified shortcuts: linked card / blocking dep / explicit deferral acknowledgment; // TODO without card = unjustified); LLM-as-judge quality tier (post finalize --workflow=spec/task only; Clarity/Completeness/Actionability axes 0–10; block < 6 / warn < 8; config.json quality.judge_model; .planning/judge-scores.jsonl; CRONUS_JUDGE=0 opt-out); analysis-first generation protocol (5-dim pre-generation analysis in CONTEXT.md ## Analysis: domain/objectives/audience/complexity/verbatim_required; self-check: "does output let audience do each objective?"; verbatim_required=true → copy stats/quotes EXACT, no paraphrase/rounding); systematic debugging methodology (IRON LAW: no code change before root cause stated; Phase 1 Investigation: full trace/reproduce/git-diff/trace-upstream; Phase 2 Pattern Analysis: compare working vs failing; Phase 3 Hypothesis: one hypothesis/minimal test/refine-until-confirmed; Phase 4 Fix: fix at source not symptom/full test suite/commit context; random mutation anti-pattern); failure-mode taxonomy for skill reflection (8 categories: sequencing_error/premature_termination/action_loop/object_missed/rule_missing/rule_wrong/rule_ignored/other; failure analyst JSON: batch_size+failure_summary+patch edits; success analyst JSON; support_count ranks edits; generalization rule no task-specific values); step context buffer (per-step entry: action/n_fail/failure_patterns/rejected_edits with score delta; current epoch only, resets at boundary; injected into reflect prompt; failure pattern extraction from fail_reason prefix; anti-repetition guarantee); semantic quality analysis of skill documents (6 diagnostic categories: contradiction/ambiguity/persona_inconsistency/cognitive_overload/coverage_gap/composition_conflict; combined single-call LLM + parallel composition-conflict pass via allSettled; custom_checks extension point; telemetry: counts/duration/outcome only, no content or paths); anti-hallucination guard for dynamic content injection (json_serialize content before embedding; named DATA_PAYLOAD sentinel tag + explicit data-not-instructions instruction; composed prompt sanitization: strip nested tags + 0.8+0.2 head-tail truncation at MAX_COMPOSED_SIZE) ; v1.3.0 resolved the §4.1 `.fallowrc.json` boundary TBD for `packages/ui` — **custom zones**, rejecting the bundled `feature-sliced` preset (its domain-logic layers contradict INV-2 and would give business logic a tool-endorsed home in a presentation-only package); ownership split — this spec keeps the gate (tool/invocation/tier), l2-ui-module-topology §4.4 owns what the rules say | Stable | 2 | 1.3.0 |
| [l2-office-view.md](specifications/l2-office-view.md) | Office projection sources, graph+spatial render, home building overview, office command surface; 3D visualization two-process architecture (renderer subprocess + WS adapter subprocess, PID files, OfficeVisualizationStatus, remote SSH probe) | Stable | 2 | 1.0.1 |
| [l2-office-control.md](specifications/l2-office-control.md) | OfficeState machine (crates/core): cooperative drain-and-checkpoint (DRAIN_TIMEOUT_MS, PARTIAL marker), token-exhaustion hibernation ladder (substitute-before-hibernate via model-router, auto resource-recovery wake, RECOVERY_POLL_MS backstop), per-subsystem pause toggles, state-change event before commit; `Implements: l1-office-control.md`; maps OC-1…OC-5 | Stable | 2 | 1.0.0 |
| [l2-acp.md](specifications/l2-acp.md) | ACP server semantics (crates/core, beneath the agent-session `/acp` transport): SQLite session store (idempotent create), per-session monotonic event bus (seq order/dedup, gap surfacing), capability declaration + trust gate (trusted/restricted/anonymous), pure protocol-projection adapters, cross-office relay, live-steering queue + interrupt fence; `Implements: l1-acp.md`; maps ACP-1…ACP-10 | Stable | 2 | 1.0.0 |
| [l2-orchestration.md](specifications/l2-orchestration.md) | Delegation via board, messaging, context isolation, judge+budget /goal loop, adaptive topology; agent tier hierarchy (Chat/Reasoning/Worker), static loader validation, MAX_SPAWN_DEPTH=3, toolkit action ranking; permission ruleset evaluation (findLast-wins, Deferred async approval, scope-based finalizer cleanup); agent mode enum (primary/subagent/all) + on-demand generation from description; tool terminate batch semantics (all-or-nothing: all tools must return terminate=true to stop the loop); AgentLoopConfig hooks (beforeToolCall block+reason, afterToolCall field-by-field override, shouldStopAfterTurn, prepareNextTurn, transformContext, convertToLlm, getSteeringMessages/getFollowUpMessages); tool execution modes (sequential/parallel at global and per-tool level); file-mutation queue (per-file serialized via chained Promises, realpath dedup, multi-agent safe); architecture spine format (invariants-only consistency contract; AD-n with Binds/Prevents/Rule/[ADOPTED]; altitude hierarchy initiative→feature→epic; Inherited Invariants table; Deferred section; Consistency Conventions table; spine purpose variants build-substrate/discussion/report/deck); wave-based parallel execution (PLAN.md frontmatter: phase/plan/type/wave/depends_on/files_modified/autonomous/requirements/must_haves; per-plan worktree isolation; wave pre-computed from dependency+file-conflict analysis; submodule intersection guard); safe resume gate (git log check before executor spawn: clean/complete/partial/absent states; 3-option partial recovery); checkpoint continuation file (.planning/.continue-here.md; 3 hard auto-advance stops: continuation file/error state/unresolved FAIL); artifact dependency graph (artifact-graph.yaml DAG; requires=enabler not blocker; optional=true for skippable artifacts; artifact-status.json tracking: pending/enabled/in-progress/complete/skipped/failed; standard chain: context-md→plan-md→summary-md→verification-md); change dependency metadata (.change.yaml: dependsOn/provides/requires/touches/parent; DAG validation: no cycles/dependsOn resolvable/archive-order enforced; cronus change graph/next/split/status commands); foundational phase gate (type=foundation blocking wave-1 phase; ⚠️ FOUNDATION GATE emitted on incomplete tasks; P1 MVP/P2 important/P3 nice-to-have story tiers; cross-tier parallelism disallowed; ✓ Foundation phase complete checkpoint before story waves); drift check gate (git diff --stat {sha}..HEAD before executor start; DRIFT DETECTED STOP on excerpt mismatch; planned_at.sha in plan frontmatter; machine-checkable done criteria: exit codes/grep patterns/git status; self-contained context rule: no "as discussed"); continuous checkpoint mode (config.json mission.checkpoint_mode explicit/continuous; cp-{timestamp}.yaml with decisions_made/remaining_steps/failed_approaches/context_sha; cronus mission restore [--from]; WIP: [cronus-cp] git commits opt-in; cronus mission squash collapses WIP before finalize; checkpoint_push: false default); three-tier durability model (Tier 1 delegation: synchronous/non-durable/max-3-parallel/leaf-vs-orchestrator; Tier 2 scheduled: durable/fire-and-forget/hard-interrupt/delivery-target/per-job-overrides; Tier 3 queue: durable+persistent/SQLite/atomic-claim/toolset-gated/audit-trail; selection rule: lowest tier that satisfies durability; Tier 1 max 5 min; Tier 3 only when Tier 1/2 insufficient); resumable skill training pipeline (runtime_state.json with last_completed_step/current_skill_path/best_skill_path/scores/origins; auto-resume from last step on re-run; selection cache via candidate_hash SHA-256 avoids redundant validation rollouts; versioned skill snapshots skill_v{N:04d}.md; best_skill.md separate from current working skill; history.json per-step with timing+tokens; api_key redacted in config.json, no secrets in runtime_state or history); post-eval recommendation engine (every eval run pass or fail; inputs: SKILL.md+eval+up-to-8 task files+results or error context assembled in parallel; change-task-files/skill/both/neither decision; natural-language markdown report at recommendations/{name}-{timestamp}.md; silent skip on LLM failure — never blocks results); analysis result fingerprinting (SHA-256 of text+NUL+serialized custom_checks; cache hit skips LLM; staleness on any edit; in-memory per-session per URI+config pair) | Stable | 2 | 1.1.0 |
| [l2-role-catalog.md](specifications/l2-role-catalog.md) | Preset catalog + hired instances, role definition format, custom roles, hire/fire, adapter protocol (integration levels, fat/thin context, config revisions) | Stable | 2 | 1.0.1 |
| [l2-archetype-catalog.md](specifications/l2-archetype-catalog.md) | Implements l1-office-archetype.md. Two-artifact definition split along the trust boundary: `archetype.json` (pool/shape/seed — structured references, validated mechanically) + `NORMS.md` (prose — agent instruction, content-vetted). **Closed four-key schema** makes OA-4 unrepresentable rather than merely forbidden — no `permissions`/`budget` key exists and an unrecognized key fails validation. Pool is read by the deviation recorder, **never by the hire gate** (OA-3). Three deviation counters + explicit `unvalidated` state (OA-9). Ships `software-engineering` (18-role pool, **empty seed**); `advertising-agency` and `finance-department` declared **blocked** — the role catalog holds 18 technical vs 5 business roles and lacks the 9 specialties they require, each of which must clear ROL-9 in a separate l2-role-catalog amendment. Finding: every examined domain seeds zero roles, since WSL-5's manager already performs all first-contact work | Stable | 2 | 1.0.1 |
| [l2-model-router.md](specifications/l2-model-router.md) | Model selection: local-first, difficulty/cost, fallback cascade, semantic cache, routing commands; semantic router pool (embedding-based, tolerance-threshold, cost-optimal selection); three-layer provider resilience (circuit breaker CLOSED/DEGRADED/OPEN/HALF_OPEN + adaptive backoff, connection cooldown with terminal states, model lockout); 9-factor weighted scoring (health/quota/cost_inv/latency_inv/task_fit/specificity/stability/tier × 2), 4 mode packs (ship-fast/cost-saver/quality/offline), LKGP, reset-aware tiebreaker, 5% bandit exploration; hardware-fit layer (FitLevel taxonomy Perfect/Good/Marginal/TooTight, RunMode taxonomy with TPS factors, TPS estimation bandwidth-based + fixed-constant fallback, memory estimation formula with KV cache + overhead); per-use-case scoring weights (quality/speed/fit/context × 6 use-cases); hardware planning protocol (PlanEstimate: run paths + upgrade deltas + KV alternatives, TurboQuant gating); post-processing pipeline (FinishGuard RAII, prompt template ${output} stripping, invisible-char sanitization, structured output json_schema, per-provider auth, reasoning model config) | Stable | 2 | 1.0.4 |
| [l2-model-runtime.md](specifications/l2-model-runtime.md) | Model transport & provider connectivity (`Implements: l1-model-runtime`): a NEW `InferenceBackend` trait in the contract crate (the streaming call surface the routing-metadata `ModelProvider` lacks) realized by a synchronous streaming REST transport crate (`model-local`) over the federated six-provider local catalog (stack §4.4) — endpoint profiles consuming the router `api_base`, pull-based token stream with first-class cancellation, bounded worker-thread concurrency (no async runtime), host-side nodus bridge that collapses the stream to satisfy `nodus::ModelProvider` (LP-1 preserved), credential-lane pass-through (RTG-10, rotation stays upstream), egress-gated remote profiles; MR-3/4/5 delegated-with-disclosure to the federated server in v1; closes the audit-identified gap: routing/selection implemented while no seam could perform the call | Stable | 2 | 1.0.1 |
| [l2-model-error-recovery.md](specifications/l2-model-error-recovery.md) | Error taxonomy (FailoverKind), priority classification pipeline, retry/compress/rotate/fallback loop, credential pool; provider health probe (ProviderHealthStatus, multi-hop subprobes, context window discovery) | Stable | 2 | 1.0.1 |
| [l2-agent-session.md](specifications/l2-agent-session.md) | Turn skeleton: TurnContext, IterationBudget, ContextEngine interface, prologue steps; tool-call loop seams, KV-cache stability, oversized-result summarizer, stop hooks, InterruptFence, post-turn hooks; text loop detection (TEXT\_LOOP\_TRIGGER\_COUNT=3, mild/strong recovery), goal re-entry cap (MAX\_GOAL\_REACT=12); daemon session server (cronus serve, ACP Streamable HTTP /acp endpoint, ConnectionRegistry max=64/idle-TTL, permission round-trip, dual-transport additive policy); MCP tool session contract (ToolCallOutcome: Success/GuidedRecovery/HardRefusal, MAX\_QUERY\_INPUT\_CHARS=10\_000, MAX\_OUTPUT\_CHARS=15\_000, ToolSurfaceProfile full/core/headless); durable prompt admission (session\_input row persisted before execution, status pending→processing→done, one llm.stream per turn, startup recovery pass); per-session runner lifecycle (RunnerMap Map keyed by SessionId, idle/busy/interrupt callbacks, assert\_not\_busy, orphaned tool\_use cleanup with interrupted=true); two-level agent loop (outer=getFollowUpMessages re-entry, inner=getSteeringMessages injection per turn, getApiKey per-call OAuth refresh); session entry taxonomy (9 types: message/custom\_message/compaction/branch\_summary/thinking\_level\_change/model\_change/custom/label/session\_info; CustomEntry NOT in LLM context; CustomMessageEntry IS in LLM context as user role); buildSessionContext tree traversal (leaf→root path, compaction-aware assembly, thinkingLevel+model extraction); session migration (CURRENT\_SESSION\_VERSION=3, v1→v2 id/parentId tree, v2→v3 hookMessage→custom) | Stable | 2 | 1.0.6 |
| [l2-context-router.md](specifications/l2-context-router.md) | Memory/rules/session routing, most-specific-first, session continue/retire | Stable | 2 | 1.0.0 |
| [l2-doctor.md](specifications/l2-doctor.md) | Health check suite, safe-repair vs escalate, crash recovery, extensible check registry, doctor command; extended runbook probes: prereqs, config, token, vault-zones (4-zone integrity), build-artifacts, server port, skill count, bridge integrity, search-tool detection (optional free-CLI path) | Stable | 2 | 1.1.0 |
| [l2-security.md](specifications/l2-security.md) | Secret storage, gitignore defaults, egress gate, execution sandbox, audit log, SSRF protection (scheme+link-local+private-IP guard), internal tool loopback (startup token, require\_admin); config integrity shields (three-state lock, SHA-256 seal, drift detection); credential storage modes (AuthCredentialsStoreMode: File/Keyring/Auto/Ephemeral; AuthKeyringBackendKind: Direct/Secrets; OAuthCredentialsStoreMode for OAuth tokens); workspace trust model (first-run discovery scan, DiscoveryScan catalog, TrustLevel: trusted/trusted-parent/denied, trusted\_workspaces.json registry, safe mode when denied, hook fingerprint rotation on git-pull); telemetry data contract (TelemetryEnvelope with random-UUID machine\_id, TelemetryEvent variants install/index\_complete/usage\_rollup/uninstall, FileCountBucket/DurationBucket coarse bucketing, DO\_NOT\_TRACK=1 opt-out priority chain, 256 KB bounded buffer, never-collected list: source code/file paths/symbol names/IPs/usernames); pre-parse safety guards (DATA\_FILE\_SIZE\_CAP\_BYTES 512 MiB memory-bomb prevention, path containment guard, metadata recursive sanitization, DNS rebind TOCTOU prevention via single-resolve-then-connect); credential finding protocol (file:line + credential type only, never the value; rotation-first remediation; agent behavior on incidental discovery); API key redaction pattern (SecretMap newtype with [REDACTED] Debug, platform feature availability guard) | Stable | 2 | 1.0.8 |
| [l2-sandbox-policy.md](specifications/l2-sandbox-policy.md) | Sandbox network egress: deny-by-default named entries (endpoint rules + binary allowlists), isolation tiers (restricted/balanced/open), preset catalog, PolicyContext agent visibility, access failure classification | Stable | 2 | 1.0.0 |
| [l2-github-issue.md](specifications/l2-github-issue.md) | Consent + scrub + dedup pipeline, GitHub issue filing, report commands; error fingerprinting (BLAKE3 hash of normalized error text, cross-episode dedup, occurrence count, prior-resolution surfacing) | Stable | 2 | 1.0.1 |
| [l2-backup.md](specifications/l2-backup.md) | State-tier backup minus secrets/cache, restore-by-copy, backup commands | Stable | 2 | 1.1.0 |
| [l2-dashboard.md](specifications/l2-dashboard.md) | Statistics metrics + sources, per-office + home aggregate, dashboard command; 7-surface catalog (Today/Skills/Knowledge/Journal/Sources/Automations/Settings); session health score formula (error pressure + memory + engagement + trend delta, [0..100]); alert thresholds (critical/warning/info); graceful degradation to disk fallback when runtime offline; §4.8 runtime analytics (per-session/per-model token counters + cost estimates + time-series ring buffer last 100 sessions; flushed to analytics.db + analytics_summary.json; feeds doctor + self-improvement + model-router scoring) | Stable | 2 | 1.0.2 |
| [l2-workflow-runtime.md](specifications/l2-workflow-runtime.md) | In-tree Rust runtime crate (lexer/parser/validator/executor/transpiler), in-process, subsystem-bound; port architecture; step-file architecture for disciplined workflow execution (JIT loading — one step file at a time; sequential enforcement — no skipping; stepsCompleted[] frontmatter state tracking on output doc; append-only building with [ASSUMPTION] tags; HALT conditions at menus and decision points); platform-native capability lookup (pre-generation check: stdlib/native/existing-dep before custom code; exhaustive lookup table by domain: HTML/CSS/JS/Node/Python/Rust/SQL; advisory log "native: used X instead"; skip-native-check override flag); §4.8 nodus language reference: file sigils (§wf/§schema/§config), section declarations (@ON/!!NEVER/!!ALWAYS/!PREF/@in/@out/@ctx/@err/@steps/@test/@macro), step-line syntax (+modifier/^validator/~flag/→pipeline), control flow (?IF/?ELIF/?ELSE/~FOR/~UNTIL MAX:n/~PARALLEL~JOIN), built-in command vocabulary by category (schema v0.4.5: memory/generation/analysis/io/routing/execution/filesystem/system), 16 reserved variables ($in/$out/$error/$meta…), 7 tone values, Value type enum (Null/Bool/Int/Float/Text/List/Map — Map preserves insertion order via Vec), 11 NODUS:* error codes (RULE_VIOLATION/PARSE_ERROR/MAX_REACHED/EXECUTION_FAILED/UNDEFINED_VAR/…), 6 library API functions (scaffold/validate/run/transpile/test/run_with_provider), ModelProvider trait + StubProvider, executor boot sequence (6 steps: load schema → !! rules → !PREF → register inputs → match triggers → execute steps); §4.9 upstream parity gap schema v0.4.6→v0.7 (target surface: ?SWITCH/~MAP/~RETRY/!HALT/!PAUSE control, MATCHES/?./??/WHERE/FIRST/LAST/string-interpolation operators, ASK/CONFIRM HITL dialog, @needs selective schema loading, 24-code error registry, closed flag/validator/type registries, macro execution, Status::Paused, @ON priority; authoritative gap owned by nodus workspace) | Stable | 2 | 1.3.1 |
| [l2-extension-registry.md](specifications/l2-extension-registry.md) | Extension manifest, preset/custom locations, MCP client + sandbox, skill generation, plugin config schema, remote skill catalog (trust levels, GitHub source); component auto-discovery, SKILL.md format (trigger[]/integrations[]/inputs[]/permissions[]/parameters[]; Parameter.description ≤ 500 chars), skill pack override; agent definition format (mode primary/subagent/all, hidden bool, color as hex or named, tools as allow/deny map {"*":false,"tool":true} with backward-compat String[] form); command definition format (subtask bool, model with provider-qualified strings `"provider/model-id"`, dynamic tokens $ARGUMENTS/$1/!cmd); MCP transport variants (stdio/sse/http/ws); channel plugin kind; installer target interface (AgentTarget, WriteResult FileAction taxonomy, surgical JSON/TOML editing, idempotency invariant); MCP permission audit at activation (LP1–LP4 + TP1–TP4 checks); skill name validation (MAX\_NAME\_LENGTH=64, lowercase a-z 0-9 hyphens, no --, no leading/trailing -); SKILL.md discovery algorithm (SKILL.md stops recursion — dir with SKILL.md is a unit, else .md children are individual skills); extension event taxonomy (30+ events: session lifecycle/agent lifecycle/tool events/model select/user\_bash/input/project\_trust); project trust requiring config (7 resources gated on explicit trust: settings.json/extensions/skills/prompts/themes/SYSTEM.md/APPEND\_SYSTEM.md); ExtensionContext surface; ToolDefinition with promptSnippet/promptGuidelines/executionMode/renderCall/renderResult; skill store paths re-rooted to tier top level (skills at program/state root, other kinds under extensions/) | Stable | 2 | 1.2.0 |
| [l2-learning-loop.md](specifications/l2-learning-loop.md) | Post-turn background review fork, skill package format (canonical: no interpreted scripts, optional nodus workflow), idle-triggered curator with lifecycle transitions | Stable | 2 | 1.1.0 |
| [l2-skill-system.md](specifications/l2-skill-system.md) | Two-tier skill stores: read-only preset store in the program tier + mutable user/generated store in the state tier (visualized as .release/program/skills/ and .release/state/skills/); canonical execution stack — nodus workflows for procedure logic + closed versioned built-in Rust command surface for operations, no interpreted scripts on the execution path; conversion pipeline for imported skill packages (verify witness → classify → retain → transpile → degrade to instruction-only → report, originals preserved under origin/); prompt synthesis path (user prompt → canonical package, source: generated, status: discovered); name-shadowing precedence workspace > state > preset; skill import/create/status command surface | Stable | 2 | 1.0.0 |
| [l2-source-layout.md](specifications/l2-source-layout.md) | Dev monorepo layout: crates/apps/packages, dependency direction, in-tree workflow-runtime crate. §4.5 resolves the crate-granularity TBD by delegating to l2-crate-topology.md (dependency/seam axis, not domain axis); §4.1/§4.2 describe pre-migration state  ; v1.2.1 added INV-8 to §3 (the `crates/` workspace layout structurally realizes the monolith; crate boundaries are compile-time seams) — completeness fix; INV-9/10 noted N/A for a directory layout  ; v1.2.2 filled §3 to a full INV-1…INV-10 (strict gate): added layout-honest rows for INV-5/6/7 (crate-housing, build-subset workspace members, `.gitignore` secret hygiene; behavior realized in filesystem-layout/core/adapters) and INV-9/10 (surface = frontend property; representation isolation = code property, this layout its precondition); trimmed the redundant 9/10 note from the INV-8 row ; v1.3.0 added §4.6 delegating the internal decomposition of `packages/ui` to the new l2-ui-module-topology.md, symmetrically with §4.5's delegation of `crates/core` to l2-crate-topology.md; records why the two delegations differ in kind (Rust receives module boundaries from the crate graph, so the crate spec need only decide where to cut; TypeScript has no compile-time module boundary within a package, so the UI spec must also name what enforces the cut) | Stable | 2 | 1.3.0 |
| [l2-crate-topology.md](specifications/l2-crate-topology.md) | Crate decomposition of `crates/core` into contract · domain · store-local · auth-local · facade. Decomposition axis is dependency weight + DN-2 provider seams, not domain area (measured: 11 inter-module edges across 53 modules; 6 modules hold every external dependency; 82% of code is pure-std). Crate-minting rule (external dep / provider plane / external consumer — never size); DN-2 seams (`UserDataStore`, `AuthProvider`, `IdentityProvider`) realized as crate boundaries; the single inverted `context_router → MemoryStore` edge as migration pivot; domain dependency allowlist (no I/O, no C, no platform); crate boundary ≠ process boundary under INV-8; 7-step ordered migration ending in a CI boundary guard. Findings: DN-2 seams absent from the tree, INV-2 violation in CLI via codegraph's leaked `rusqlite::Connection`, async-runtime gap vs l2-core-library §2; §4.2 registers cronus-model-local as a fourth adapter crate (InferenceBackend transport, model-runtime), not a DN-2 plane ; v1.1.0 addressed new L1 INV-10 (representation isolation) — Invariant-Compliance row + §4.5 note: an adapter crate's storage/wire representation is **private to it** (not re-exported from `cronus-contract`, unnameable by `cronus-domain`) and mapped to/from contract types at the trait seam, so the SQLite schema / on-disk record / future remote DTO evolve inside their crate without touching the domain — the data twin of the §4.6 behavioral trait inversion, and what lets the pure-`std` domain compile and unit-test with an in-memory provider; no structural change to the crate set  ; v1.1.1 added the INV-9 (shipped-surface honesty) Invariant-Compliance row — completeness fix: the table had jumped INV-8 → INV-10, leaving the L1 all-invariants-addressed gate unmet for a Stable L2; INV-9 is chiefly a frontend-surface invariant (enforced in l2-cli/l2-tui), the topology's bearing being that the facade `cronus` is the sole owner of the capability contract so an advertised verb has one binding target, and that the §6.4 codegraph fix removes a CLI affordance bound to a leaked adapter type; also normalized the history table to 3 columns + ascending order | Stable | 2 | 1.1.1 |
| [l2-ui-module-topology.md](specifications/l2-ui-module-topology.md) | Module decomposition of `packages/ui` — the TypeScript twin of l2-crate-topology, facing a language with no compile-time module boundary inside a package. Four tiers (composition root → shell → surfaces → shared) with a total one-way import order; UMT-1…UMT-7: tier order with no lateral surface-to-surface edge, a published surface API narrower than its file set (mount + props + consumed projections only, never sub-components or private state shapes), imports terminating at the declaration (no deep imports past it), grouping by surface never by file kind with a folder-minting threshold (a surface earns a folder at its second private module — bounded scope, never size), shared tier as a strict leaf, a single IPC seam (one shared module performs core invocation), and total zone coverage. INV-10's lateral analogue: a surface's internals are unnameable by its siblings, as an adapter's representation is unnameable by the domain. Motivated by l2-crate-topology §6.4's real INV-2 violation — a module published more than it meant to and a downstream call site took what was offered. §4.4 resolves l2-quality-pipeline §4.1's preset-versus-custom-zones TBD in favour of custom zones, rejecting the bundled `feature-sliced` preset (its entities/features layers organize client-side domain logic that INV-2 forbids); enforcement mapped onto tripwire discipline TW-1/2/3/4/7/10/11 with the judgment part of UMT-2 declared a review obligation rather than faked as a check. §6 records full FSD adoption as considered and rejected, its three transferable rules adopted individually. Migration is a behavior-neutral relocation: tier membership is currently indistinguishable by path, which is why no rule is expressible today | Stable | 2 | 1.0.0 |
| [l2-application-shell.md](specifications/l2-application-shell.md) | React 19 + Tauri v2 realization of the reactive frontend runtime AS-1…AS-13 — the L2 the application-shell model had never received, and the spec that settles a fork present in the source: `packages/ui` exports **two composition roots** (the shipped workbench the desktop app mounts, and a shell frame that is built, exported, tested and rendered by nothing). One root, with the earlier composer remounted as the office surface behind the router (R-1 one mount · R-2 the router owns placement · R-3 the root injects, no module-level singleton). Three state domains separated by *who owns the truth* — view (frontend), projection (core), session status — held in hand-rolled subscribable stores over a scoped external-store subscription, no state library added to a package that holds no domain state; a projection snapshot is *unrequested / pending / loaded / unavailable-with-reason*, never a default-empty a surface could mistake for a real empty result. The single seam gains an event direction (push, no polling) under a **capability-admission rule**: a bridge method may be added only when it binds a core capability already bound by another frontend (INV-3), so the seam cannot grow a frontend-only feature; unbacked projections stay explicit placeholders (INV-9). Namespaced actions with mandatory descriptive labels; keystrokes resolve against a focus-derived **context stack** with predicates, multi-key prefixes, most-specific-then-most-recently-layered precedence, and fall-through on unbound; bindings merge in three deterministic layers (base → platform → user) with origin shown. Workbench stated in the L1 vocabulary (workspace/center-panes/docks-panels/floating) with the shipped one-pane center declared a populated subset; versioned layout record persisted by the core (INV-5), restored field-wise so it can never block startup. Cancellation-owned async: an unmounted owner cancels, a late response is discarded, every rejection resolves to a visible state. States honestly where the stack lacks the L1 mechanism (AS-2 handles, AS-11 executors) and how the intent is met instead, rather than renaming primitives to make the table read uniformly | Stable | 2 | 1.0.0 |
| [l2-service-activation.md](specifications/l2-service-activation.md) | Implements l1-background-activation.md. Per-OS realization under one governing constraint — the background engine runs **as the user, never as a superuser** — which rejects the `LocalSystem` Windows Service and the root `LaunchDaemon` (both lose the per-user state root). Windows: `HKCU\…\Run` (login) / S4U scheduled task with At-startup trigger, `RunLevel=Limited` (system), reading `StartupApproved` veto alongside the Run value. macOS: `SMAppService.loginItem` / `LaunchDaemon` with `UserName`, mapping `requiresApproval` to a first-class state. Linux: systemd user unit (login) / system unit with `User=` **or** lingering user unit (system — two realizations, one mode). `ActivationRegistry` seam with `observe()` and no persisted mirror; three structural barriers enforcing BA-4 (no agent tool, no sandbox filesystem write path, no elevation); remove-then-register with read-back verification; state-root lock realizing attach-never-duplicate. `cronus activation status\|enable --mode\|disable`; non-interactive `enable` refuses without an acknowledgement flag. Resolved at promotion: non-systemd Linux → login-scoped via XDG autostart + system-scoped `Unsupported` (BA-10); `tauri-plugin-autostart` rejected (std-first dependency policy); Windows S4U is design-settled, only At-startup launch-timing remains a build-time validation gate (with an `At log on` fallback). Divergence reconciled — `l2-technology-stack` amended to name the S4U task | Stable | 2 | 1.0.1 |
| [l2-agent-constitution.md](specifications/l2-agent-constitution.md) | Per-workspace identity file system: SOUL, PROFILE, MEMORY, HEARTBEAT, BOOTSTRAP files and bootstrap ritual; persona wizard (3-question interactive setup: name/voice/focus); communication contract (BLUF, no hedging, contradiction flagging, does/does-NOT-do invariant list); 8-step agent activation sequence (prepend steps → adopt persona → persistent_facts → config → greet → append steps → menu; icon prefix convention throughout session); 3-file customization merge (base→team→user TOML; scalars override, tables deep-merge, keyed-arrays replace+append); 4-layer system config resolver (installer-base team/user + team-override/personal tiers); project-context.md as AI-rules document (unobvious implementation details for LLMs: critical patterns, hidden invariants, anti-patterns, tribal knowledge); multi-platform rule distribution (canonical SKILL.md/SOUL.md → thin adapters per host: skill-plugin/extension-manifest/instruction-copy/steering-rules/MCP/AGENTS.md; alignment verification via hash check; mode-marker filtering `[[lite]]`/`[[full]]`/`[[ultra]]` in canonical source); profile delivery modes (profiles: minimal/standard/full/custom; delivery: skills/commands/both; install scope: global/project/both; regeneration via `cronus install`; alignment check `cronus install --check`); lifecycle hooks for workflow integration (before/after_spec/plan/mission/task/review/install hook points; optional flag; CRONUS_WORKSPACE/MODE/HOOK_POINT env; non-optional failure aborts workflow); managed-marker agent context injection (CRONUS:START/END sentinels; configurable marker_prefix per adapter; 4 injection rules: create/update/mismatched-sentinel-abort/append; multi-file support with distinct prefixes); agentic readiness checklist (8 signals: context-file/skills/agents/templates/hooks/isolated-runtime/MCP/freshness; critical×20+warning×12+info×6 scoring; Ready≥80/Partial≥50/NotReady<50; `cronus workspace check` output with ✓/✗ + one-line fix per failed signal; auto-runs on install); cognitive frame activation (cognitive_frame: field in AGENTS.md; names mental model not steps; activates latent knowledge; preamble tier hierarchy: 1-foundational/2-specialist/3-orchestration enforced by orchestrator; taste decision gate: [TASTE] flags subjective calls in CONTEXT.md Taste Decisions section); writing style protocol (jargon-glossing on first use per invocation: "Term (definition)"; outcome-framing in AskUserQuestion: pain-avoided or capability-unlocked + one concrete outcome per option; user-impact closure: ends with sees/waits-for/loses/gains; filler-word blacklist: delve/crucial/robust/comprehensive/…; builder-to-builder tone); multi-session context disambiguation (≥3 concurrent sessions → [Session {letter}] ./{repo} @ {branch} / {task-summary} header on every question; .planning/sessions/{ppid}.json TTL 120 min; letter = oldest-first alphabetical); question tuning + preference learning (.planning/question-preferences.jsonl; 3× same response → suppressed: true; [Preference applied] one-line notice; cronus workspace tune / --reset / --ask-all; global_preferences.jsonl home-workspace defaults inheritable by local); model overlay system (config.json model_overlays map; overlays/{model}.md sections Tool Discipline/Explanation Style/Interaction Style; CRONUS_MODEL env activation; conditional prose — no runtime branching; precedence: overlay < skill < user message); three layers of knowledge (Layer 1 tried-and-true high-trust; Layer 2 new-and-popular scrutinize before applying; Layer 3 first-principles self-verified when 1+2 conflict; labelled decisions; eureka moments logged with pattern_matched=first-principles-correction); proactive drafting posture (draft-first rule: complete artifact before any question; 3 blocker conditions only: ambiguous target/contradictory requirements/absent dependency; ≤2 questions per task; optimistic default with single inline assumption note; "Before I start, I have a few questions…" is always wrong); FR-6 disclosure — the 8-step activation sequence executes as a no-op seam pending agent-session wiring | Stable | 2 | 1.0.10 |
| [l2-tool-security.md](specifications/l2-tool-security.md) | Two-layer tool defense: static skill scanner + optional LLM deep scan (`ext scan --deep`) + runtime tool guard (10 threat categories, approval escalation), ToolPolicy composition (plan_mode/guide_only), prompt injection hardening (UNTRUSTED_CONTEXT_POLICY, guard markers); request guardrail pipeline (BaseGuardrail contract, fail-open priority-ordered execution; 3 built-in: vision-bridge/pii-masker/prompt-injection); repository content as data rule; 17-category extended vulnerability taxonomy (P1-P8/E1-E4/PE1-PE3/SC1-SC6/EA1-EA4/OH1-OH3/MP1-MP3/TM1-TM3/RA1-RA2/TR1-TR3/AST1-AST8/TT1-TT5/YR1-YR4/LP1-LP4/TP1-TP4/RP1-RP3; MCP rug-pull / manifest-drift RP1-RP3 — pin approved tool-manifest hash, re-verify on refresh/reconnect/`tools/list_changed`, re-gate on post-approval drift, temporal complement to static LP/TP; auto-CRITICAL: AST8/TT3/TT5/TP2; SC4 live OSV.dev CVE lookup with 1h cache + offline fallback; eval dataset exclusion heuristic); two-stage scan pipeline (Stage1=regex/AST/taint synchronous, Stage2=LLM semantic `--deep`; AnalyzerPlugin protocol; token-budget batching CHARS\_PER\_TOKEN=4/CHUNK\_OVERLAP\_LINES=50; confidence<0.6 filtered); risk scoring formula (CRITICAL+50/HIGH+25/MEDIUM+10/LOW+5; 1.3× for executable scripts; bands 0-20=SAFE/21-50=CAUTION/51-80=DNI/81-100=DNI-hard); anti-jailbreak LLM prompt pattern (CRITICAL INSTRUCTIONS preamble + delimiter separating analyzer instructions from adversarial skill content); SARIF 2.1.0 output format | Stable | 2 | 1.2.0 |
| [l2-mission-mode.md](specifications/l2-mission-mode.md) | Two-phase autonomous goal execution: PRD generation → user checkpoint → story-verified execution loop; discuss-phase decision capture (CONTEXT.md format, D-NN decision IDs, deferred ideas, scope guardrail, gray area identification); phase lifecycle state machine (Pending/Planned/In Progress/Executed/Needs Review/Complete; Complete→replanned hard block; prior-phase completeness scan); SUMMARY.md rich frontmatter (requires/provides/affects dependency graph, tech-stack, key-decisions, patterns-established, requirements-completed); operation mode ladder (lite/full/ultra/off; resolution: env var CRONUS_MISSION_MODE → workspace config.json → default=full; flag file .mission-mode for statusline/hooks); proposal artifact (proposal.md root node of artifact DAG; frontmatter: change-id/created/status/mode; sections: intent/scope/approach/capabilities/impact/rollback-plan; status FSM draft→ready→accepted or rejected; immutable once accepted; mode-specific behavior table); spec persistence model (flow-forward append-only default/flow-back manual reconcile/living spec regenerated from PRD; selected in config.json mission.spec_persistence; enforcement per model); structured clarification protocol (.planning/clarifications.md; up to 7 Q&A on scope/acceptance/dependencies/NFR/out-of-scope; source: user/prd/inferred; locked: true inputs immutable for planner; skipped: true with rationale; iterative via `cronus mission clarify`); session intent classification (work_type: feature/bugfix/refactor/review/docs/test/config; phase_intent: Planning/Implementation/Debugging/Review/Verification/Exploration; SUMMARY.md work_type+phase_intent+intent_confidence; routing rules per work_type: bugfix→rework-gate/refactor→arch-gate/config→env-safety-checklist) | Stable | 2 | 1.0.5 |
| [l2-budget-engine.md](specifications/l2-budget-engine.md) | Hierarchical budget policies (office/project/agent), cost events, warn/hard-stop thresholds, monthly reset, cascade invariant | Stable | 2 | 1.0.0 |
| [l2-execution-workspace.md](specifications/l2-execution-workspace.md) | Isolated execution environments (local/worktree/ssh/sandbox), no-remote-git contract, finalize write-back, workspace forking, cleanup lifecycle; git worktree lifecycle (slug naming, boot sequence, isPristine, reset+prune, events) | Stable | 2 | 1.0.1 |
| [l2-multi-user-auth.md](specifications/l2-multi-user-auth.md) | Multi-user auth: bcrypt passwords, 7-day session tokens, TOTP 2FA + 8 backup codes, privilege map, admin promote/demote with stash, reserved sentinel usernames, atomic file writes | Stable | 2 | 1.0.0 |
| [l2-context-management.md](specifications/l2-context-management.md) | Adaptive input-token budget (85% headroom, 200k hard-max), 8-step trim cascade with `_protected` messages, LLM-driven compaction at 85% threshold, tool message sanitization; context engine registry (ContextEngineHostCapability, AssembleResult.promptAuthority, ContextEngineProjection per_turn/thread_bootstrap, runtime modes, idempotent registration); tool-output compaction constants (PRUNE\_MINIMUM=20\_000, PRUNE\_PROTECT=40\_000, TOOL\_OUTPUT\_MAX\_CHARS=2\_000, PRUNE\_PROTECTED\_TOOLS=["skill"], preserveRecentBudget=25% of usable clamped [2k,8k]); truncateToolOutput standard annotation format; turn-aware compaction pass (completedCompactions + turns + splitTurn binary search); compaction trigger (shouldCompact: contextTokens > contextWindow − reserveTokens=16\_384, keepRecentTokens=20\_000 floor); CompactionDetails file-op tracking (readFiles/modifiedFiles accumulated across compactions, XML `read-files`/`modified-files` tags in summary prompt); tool output truncation constants (DEFAULT\_MAX\_LINES=2\_000, DEFAULT\_MAX\_BYTES=50KB, GREP\_MAX\_LINE\_LENGTH=500; truncateHead for reads, truncateTail for bash output); serialization (TOOL\_RESULT\_MAX\_CHARS=2\_000, serializeConversation plaintext format, SUMMARIZATION\_SYSTEM\_PROMPT constant); FR-6 disclosure — shipped compactor is a no-op placeholder, compaction inert until a summarization model is bound [v1.1.0] Compaction is no longer destructive — EA-1/EA-2/EA-3/EA-10 compliance: the range is archived **before** trim/truncation/compaction removes it, an archive-write failure aborts that reduction instead of dropping content, and summary metadata plus truncation annotations carry the handle that expands back; a truncated tool output is recovered by handle rather than by re-running a command that may be irreproducible. | Stable | 2 | 1.1.0 |
| [l2-agent-migration.md](specifications/l2-agent-migration.md) | Source-neutral migration manifest v1: two-layer model (archives vs memory candidates), staged apply (dry-run→backup→archives→review→skills→skip_secrets), source adapters | Stable | 2 | 1.0.0 |
| [l2-deep-research.md](specifications/l2-deep-research.md) | Iterative Think→Plan→Search→Extract→Synthesize engine: date-grounding, ResearchPlan (sub-questions + success criteria), untrusted content wrapping, max_rounds circuit breaker, `_protected` report; long-running operation lifecycle (§4.10): detached/attached start, streaming ResearchProgress events (phase/round granularity, counts-only no untrusted content), blocking wait, threaded continuation inheriting parent plan+report+citations with `parent_job_id` lineage (trusted prior-output vs untrusted new pages), caller-supplied output-format skeleton validated via output-contracts with one repair pass, cost/time/token transparency (pre-run estimate + post-run usage accounting + hard-threshold budget gating); extended command surface (stream/wait/continue, `--format`/`--detach`/`--json`/`--raw`); realizes l1-deep-research (DR-1…DR-11), re-parented from orchestration-only | Stable | 2 | 1.3.0 |
| [l2-agent-autonomy.md](specifications/l2-agent-autonomy.md) | Autonomy ladder (supervised/semi_autonomous/autonomous), CommandRiskLevel classifier, SecurityPolicy gate matrix, ActionTracker rolling hourly cap, approval gate lifecycle (10-min TTL, background bypass); ApprovalRecord manager (create/register separation, RESOLVED_ENTRY_GRACE_MS=15s, idempotent register); SEC-9 realization — approval step gains "Allow always (scope)" (offered only for promotable calls), durable `AllowRule` store (scope ladder action/action_class/office/global, stable-signature key, revocable, governance-clamped, fail-closed, destructive/always-forbidden non-promotable), `gate_decision` consults rule store before Prompt + audits auto-allowed calls, `rules list`/`rules revoke <id>` command surface | Stable | 2 | 1.1.0 |
| [l2-trigger-triage.md](specifications/l2-trigger-triage.md) | Trigger intake pipeline: TriggerEnvelope normalization, 4-outcome TriageDecision (Drop/Notify/SpawnReactor/SpawnOrchestrator), local CPU classifier + cloud fallback + rule fallback, dedup cache, rate limiting; FR-6 disclosure — shipped classifier stub always reports 0.0 confidence, classification resolves via the rule-based fallback until a real classifier ships | Stable | 2 | 1.0.1 |
| [l2-automation-pipeline.md](specifications/l2-automation-pipeline.md) | Runtime automation engine (crates/core): single PipelineEngine behind implicit `@ON:` + explicit canvas modes, topological node executor (filter/transform/branch/delay/aggregate/loop/action/subpipeline/observer), dedup window, scoped node/pipeline state over volatile/durable backend registry, control plane (enable/disable/trigger edges), lifecycle observers, composition, portable definitions-only bundles, dev/pinned runs; `Implements: l1-automation-pipeline.md`; maps AP-1…AP-15 | Stable | 2 | 1.1.0 |
| [l2-automation-canvas.md](specifications/l2-automation-canvas.md) | Visual automation canvas (React 19 + Tauri): three-panel flow-graph projection of the pipeline engine, node rendering with live AuditProvider state, explicit read-write editing + connection/invariant validation, read-only implicit `@ON:` surfaces + convert-to-explicit, dev-run requests (AC-7, engine executes not canvas), observer scope overlay (AC-8), local persistence + offline graceful degradation; `Implements: l1-automation-canvas.md`; maps AC-1…AC-8 | Stable | 2 | 1.0.0 |
| [l2-voice-input.md](specifications/l2-voice-input.md) | On-device voice pipeline (crates/core + shell): cpal 16 kHz capture, ONNX VAD, pluggable TranscriptionEngine trait (accel-if-present/CPU/streaming/platform-native), optional post-transcription transform (deterministic shaping + consent-gated LM via egress gate), system-level review overlay, model acquire/load/idle-unload lifecycle, clipboard-safe injection, optional on-device history; `Implements: l1-voice-input.md`; maps VI-1…VI-10 | Stable | 2 | 1.0.0 |
| [l2-deliberation.md](specifications/l2-deliberation.md) | Deliberation engine (crates/core): orchestrator-initiated round runner, parallel independent arguments (no cross-read, wave execution), synthesis with per-argument attribution + orchestrator finality, budget-bounded with visible truncation, immutable log over the inbox store (`deliberation_round` type) surfaced in Channels; `Implements: l1-deliberation.md`; maps DL-1…DL-5 | Stable | 2 | 1.0.0 |
| [l2-version-control.md](specifications/l2-version-control.md) | Version control (crates/core): virtual staging area over execution-workspace worktrees, orchestrator-enforced role authority table (VC-1/VC-5), quality-gated commit production (VC-2), card-aligned commit boundary + Conventional Commits with mandatory card footer (VC-3), trunk-based/Git-Flow strategies, platform-neutral remotes; `Implements: l1-version-control.md`; maps VC-1…VC-6 | Stable | 2 | 1.0.0 |
| [l2-inner-monologue.md](specifications/l2-inner-monologue.md) | Inner monologue (crates/core): heartbeat-gated background cycle (Active/Idle + foreground-idle guard, IM-1), read-only office snapshot, token-bounded reflection, typed intentions (ProactiveMessage/AutomationTrigger/MemoryWriteProposal/TaskProposal/NoAction), log-before-dispatch to Pulse log over the inbox store (`pulse_monologue` type), proactivity threshold, pauseable via office-control; `Implements: l1-inner-monologue.md`; maps IM-1…IM-5 | Stable | 2 | 1.0.0 |
| [l2-lookahead-planning.md](specifications/l2-lookahead-planning.md) | Lookahead engine (crates/core): trigger-catalog detector (LP-1), budget-bounded no-real-exec consequence simulator over the orchestrator context model + codegraph static analysis (LP-2/LP-3), conclusion dispatcher CONFIRM/MODIFY/ESCALATE/BUDGET_EXHAUSTED with ORC-9 fallback (LP-4/LP-5), append-only decision log feeding self-improvement (LP-6); `Implements: l1-lookahead-planning.md`; maps LP-1…LP-6 | Stable | 2 | 1.0.0 |
| [l2-global-orchestration.md](specifications/l2-global-orchestration.md) | Building-level coordinator (crates/core, home manager): event-bus aggregate OfficeState/kanban/budget/session view (GO-4), ACP relay routing decision layer (GO-5, envelope-only), phase-awareness card annotation with mandatory cross-cutting concerns (GO-3), non-intrusive coordination (GO-2), building escalation → resolve/cross-office deliberation/HITL (GO-6); `Implements: l1-global-orchestration.md`; maps GO-1…GO-6 | Stable | 2 | 1.0.0 |
| [l2-codegraph.md](specifications/l2-codegraph.md) | Implements l1-code-intelligence.md (re-parented from l1-storage-model; §3 maps CI-1…CI-13, implemented vs roadmap). §3 maps CI-1…CI-17 incl. roadmap CI-14 node-summaries / CI-15 vocab-query / CI-16 resolution-synthesis / CI-17 coverage. In-workspace code intelligence index: language extraction (tree-sitter grammars), SQLite symbols + FTS5 + sqlite-vec embeddings, RRF fusion, auto-index on first use, incremental mtime updates; three-pass pipeline (local AST / local audio-seeded / LLM subagent for docs); confidence taxonomy EXTRACTED/INFERRED(0.95–0.55)/AMBIGUOUS; hyperedges; dual-layer cache (AST versioned by extractor, semantic unversioned; stat fastpath; body-only MD hash); Leiden community detection (hub exclusion, splitting, cohesion score, ID stability); entity deduplication (entropy gate → MinHash/LSH → Jaro-Winkler → community boost → union-find → optional LLM); graph analysis (god nodes, surprising connections, graph diff, import cycles, suggested questions); affected subgraph BFS; global multi-project graph (node prefixing, external-node label dedup, source-hash skip) | Stable | 2 | 1.3.0 |
| [l2-memory-encryption.md](specifications/l2-memory-encryption.md) | Per-chunk AES-256-GCM encryption for the memory store: Argon2id KDF from user credentials, OS keychain key storage, transactional key rotation, key lifecycle (session/workspace delete/lock) | Stable | 2 | 1.0.0 |
| [l2-agent-registry.md](specifications/l2-agent-registry.md) | Built-in and custom agent catalog: mode (primary/subagent/all), permission layer stack (defaults→built-in→user config), fork-agent checkpoint-writer contract, generate-from-description API, default agent resolution | Stable | 2 | 1.0.0 |
| [l2-inbox.md](specifications/l2-inbox.md) | SQLite inter-actor inbox: send (ESRCH check + insert + wake), drain (synthetic user message, MAX_DRAIN_PER_TURN=100), GC_TTL_MS=7 days, InboxArrived bus event, deferred drain on no-prior-message | Stable | 2 | 1.0.1 |
| [l2-session-checkpoint.md](specifications/l2-session-checkpoint.md) | Session context-window checkpoint: three-file hierarchy (checkpoint/memory/notes.md), section-budgeted reads, fork-agent prefix-cache parity write, boundary invariant, system reminders (loop/stop/continue), progress reconcile, auto-memory triggers; file-level snapshot contract (shadow git repo at state/snapshots/<project_hash>/, timestamp+filename+tool naming, 50-snapshot retention, SNAP-1..5 non-interference invariants, /snapshot restore command) | Stable | 2 | 1.0.1 |
| [l2-plugin-hooks.md](specifications/l2-plugin-hooks.md) | Runtime plugin hook system: actor.preStop/postStop ReAct loops (MAX_PRE_REACT=3/MAX_POST_REACT=3), ActorMatcher filter, aggregated decision, file hooks auto-discovery, sequential external plugin loading, HookEvent observability, fail-forward error handling; tool-event hook API (12 events: PreToolUse/PermissionRequest/PostToolUse/PreCompact/PostCompact/Stop/SubagentStart/SubagentStop/SessionStart/SessionEnd/UserPromptSubmit/Notification, prompt+command+agent handler types, commandWindows+async flags, parallel execution, hooks.json format), rule evaluation engine (block/warn, AND conditions, 6 operators, LRU regex cache), hook security model (input validation, path safety, variable quoting, timeouts); hook state tracking (HookState: enabled+trusted_hash — hash-pinned tamper detection for hook files); model-level hook events (BeforeModel: request modification + synthetic response injection; AfterModel: response redaction/replacement; BeforeToolSelection: dynamic tool list filtering with mode=auto/any/none); AfterTool tailToolCallRequest (chain a follow-up tool whose result replaces the original); AfterAgent clearContext flag | Stable | 2 | 1.0.3 |
| [l2-config-hotreload.md](specifications/l2-config-hotreload.md) | Configuration hot-reload: file watcher with bounded backoff + polling fallback, prefix-keyed ConfigReloadPlan (restart/hot/none rules), subsystem-scoped actions, skills snapshot invalidation, no-op detection  ; v1.0.1 added INV-8 to §3 (reload/restart act on the single deployable — no service to roll independently) — completeness fix; architecture is a compose parent, INV-9/10 are not reload concerns  ; v1.0.2 filled §3 to a full INV-1…INV-10 (strict gate): INV-1/2 inherited (a module inside the core, itself core logic), INV-7 folded into the existing SEC-5 no-secret-egress row, INV-3/4/5/6/9/10 honest N/A (an internal reload module has no frontend surface, no state ownership, no capability set, and names no adapter representation); trimmed the redundant 9/10 note from the INV-8 row | Stable | 2 | 1.0.2 |
| [l2-self-improvement.md](specifications/l2-self-improvement.md) | Agent self-improvement: calibration buckets (overconfidence=1−verified/declared, warn below 50%), mistake log (by project/category/files), should-have-asked (trigger→question→answer), ask-backs (at-most-one-pending per project via partial UNIQUE INDEX), reasoning templates (task_type+domain → steps, dream cycle extracted), brief surface (5-signal join at task start, cross-project mode); advisor-executor handoff protocol (role invariants, dispatch contract, verdict taxonomy APPROVE/REVISE/BLOCK, max 2 revision rounds); plan backlog lifecycle (TODO/IN\_PROGRESS/DONE/BLOCKED/REJECTED status machine, reconcile procedure, GitHub issue publication preflight); phase retrospective format (what-built/worked/inefficient/patterns-established/key-lessons/cost-observations); cross-milestone trends table (sessions/phases/key-change per milestone); MILESTONES.md entry format (delivered/phases/accomplishments/stats/git-range/what-next); behavior gate verification (measurement probes vs gate probes; probe spec format name+description+pass\_condition+fail\_indicator+grader; scorecard LOC/cost/latency/correctness; failed gates require mistake log entry before milestone Complete); multi-dimensional spec quality scoring (5 categories: arch-readiness/process-compliance/decision-freshness/verification-coverage/spec-specificity; severity penalties −12/−7/−3; good≥70/needs-improvement/critical<40; 3-phase rolling trend ↑↓→; critical category blocks milestone Complete); decision velocity and flow state (cycle-time fast/normal/slow/stuck; phase flow score rapid-task-rate+latency+duration+density; deep/moderate/shallow/fragmented labels; ⚠️ BOTTLENECK flag on fragmented+stuck; adaptive thresholds from 60th/90th percentile after 20 phases); learnings journal (.planning/learnings.jsonl; session_id/timestamp/skill/phase/pattern_matched/outcome/lesson; ≤3 per session; top-3 relevant in brief surface; differs from mistake log: lightweight+may be success); session context recovery (timeline scan at skill start; checkpoint read; welcome-back 1-line summary; prevents re-derivation); timeline logging (.planning/timeline.jsonl; skill/event/session_id/duration_sec/outcome/phase; feeds flow score §4.12); skill activity tracking (.usage.json per skill: use_count/view_count/patch_count/last_activity_at/state/pinned; state machine active→stale(30d)→archived(90d), archived→active on any event, pinned blocks transitions, never deleted; curator sweeps hourly(stale-detect)+daily(archive)+on-demand via `cronus workspace curator`; bundled+hub skills excluded; discovery: active=routing, stale=check-notice, archived=excluded from all); iterative skill document evolution (six-stage pipeline rollout→reflect→aggregate→select→update→gate; edit ops append/insert_after/replace/delete; cosine/linear/constant/autonomous LR scheduler; patch vs rewrite-from-suggestions update modes; file layout .planning/skill-training/{run-id}/; api_key redaction in config.json); longitudinal momentum update (improved/regressed/persistent_fail/stable_success categories; force-inject guidance into current+best skill without gate; [slow-update] placeholder at epoch 1; epoch 2+ longitudinal comparison); cross-epoch strategy memory (optimizer-side meta-skill.json; injected as reflect context in subsequent epochs; accumulates across epochs; distinct from momentum: optimizer receives, not target agent); analyze-fix-validate quality loop (Phase 1 combined semantic pass: 6 categories contradiction/ambiguity/persona_inconsistency/cognitive_overload/coverage_gap/composition_conflict; Phase 2 targeted fix: exact relevant_text edits only, no net-new sections; Phase 3 gate validation: accept only if score >= pre-fix baseline; fingerprint cache; runs before Gate §4.15 step 6; disabled via quality.static_analysis=false) | Stable | 2 | 1.0.8 |
| [l2-development-workflow.md](specifications/l2-development-workflow.md) | Cronus implementation of agent-assisted development workflow: bundled skill catalog (12 skills: design/workspace-setup/writing-plans/coordinator/task-reviewer/code-review/workspace-finish/tdd/debugging/verify-completion/parallel-agents/receiving-review); session-bootstrap hook (SessionStart injects bootstrap skill as additionalContext); implementer dispatch template (DONE/DONE_WITH_CONCERNS/NEEDS_CONTEXT/BLOCKED protocol); reviewer dispatch template (two-verdict format: Spec Compliance + Task Quality); model-tier selection table (cheapest/standard/most-capable tiers by task type); progress ledger format (.cronus/dev/progress.md git-tracked per branch); script helpers (task-brief/review-package/progress-ledger); workspace-setup lifecycle (EnterWorktree + git worktree fallback + baseline test); workspace-finish lifecycle (four-option menu, DW-8 human checkpoints, worktree cleanup) | Stable | 2 | 1.0.0 |
| [l1-execution-graph.md](specifications/l1-execution-graph.md) | Execution graph model: directed graph of nodes with typed state channels (LastValue/Accumulator/Topic), superstep atomicity, conditional routing, dynamic spawn directive (EG-9), unified control object (EG-10), immutable invocation context (EG-11), interrupt/resume, checkpoint durability modes, step budget; EG-12 deferred asynchronous execution with correlated resumption (§4.8) — a node MAY launch a long/external/background operation, yield a correlated pending token, and resume its continuation only on a typed completion carrying that token (staged at the next superstep boundary, EG-4 atomicity preserved); non-blocking (other branches proceed, else run suspends resumably EG-6), bounded per-operation timeout→error path on expiry, stray/expired completion rejected; unifies human-decision pause (EG-7, synchronous whole-graph special case) with background/external/client-side execution under one launch→suspend→correlated-resume mechanism; nodus realization = l1-nodus-language NL-12 (tech-agnostic) | Stable | 1 | 1.2.0 |
| [l1-tool-composition.md](specifications/l1-tool-composition.md) ; v1.2.0 TC-8 — **effect-class affordance selection**: the exposed capability set must be expressible as a predicate over the AG-2 consequence classification (read-only / non-destructive / workspace-scoped), reusing the gating taxonomy as the selector for the INT-6 strip axis rather than inventing a second one; a **newly added capability joins the correct sets by being classified** rather than by every call site being found and edited (the name-list failure is silent — nothing errors when the new capability is simply absent from a restriction it belongs to), and an **unclassified capability is excluded from every class-derived set**, since such a set is a claim about what the agent can do to the world and an unclassified member falsifies it in the dangerous direction; enumerated sets remain available and compose, each named addition/removal visible as such | Tool composition: toolkit as named group with auto-derived dispatcher, dependency DAG with parallel/sequential enforcement, nested toolkits, single authorization surface; deferred tool resolution at scale — past a catalog-size threshold the agent surface reduces to a relevance-searchable index with on-demand schema materialization, complementing static grouping by bounding context cost, materialized tools retain membership/auth/deps/routing, reduction observable (TC-7) (tech-agnostic) | Stable | 1 | 1.2.0 |
| [l1-input-binding.md](specifications/l1-input-binding.md) | Input binding — the **consumer-side twin** of `l1-output-contracts`, closing the asymmetry where nothing governed how a work unit obtains its inputs or what happens when it cannot (each surface parsed its own payload, invented its own failure shape, and discovered the problem after starting work). Declared ordered typed **binders** are the single source for both runtime supply and the advertised input schema, and the body may not reach into the raw envelope (IB-1). **Bind-before-invoke** makes *did not run* a structural outcome rather than a self-report, and the whole rejection class safely re-invocable (IB-2). A rejection is a typed first-class outcome on the normal channel with an effectively **uninhabited fault channel**, so a binding failure cannot be dropped by a forgetful intermediary (IB-3). Four distinguishable **located** modes — absent / unreadable / malformed / ill-shaped — because each implies a different corrective action *and a different responsible actor*, and the location turns a search into one edit (IB-4). **Optional means absent, never invalid** — a present-but-malformed value still rejects, closing the quiet-corruption path where a bad value becomes a default (IB-5). Two supply phases with **composition-phase needs sealed before mounting**, so an unmet dependency fails at assembly instead of on the first production call (IB-6); the untyped dynamic channel is a bounded fallback that never substitutes for a declarable need (IB-7). **Once-only inputs bind once and last, enforced at declaration** — two declared consumers is a construction error, not a runtime race (IB-8). Ordered short-circuit binding whose first rejection is stable and therefore learnable (IB-9); a **default ceiling on every caller-sized input**, the input-side twin of the ATE-12 output overflow guard (IB-10); composing/wrapping binders as the single-parse reuse mechanism (IB-11); and a **separate rejection channel**, since a rising rejection rate indicts the *advertisement*, not the implementation (IB-12). **nodus projection, no new construct**: move `@in` enforcement ahead of the first step (validation-class, like undefined-command/undefined-variable — recorded as a candidate amendment to the typed-I/O invariant, deliberately not applied), split the input error taxonomy into the four modes, and fix optional-input semantics onto the existing null value; ceiling and wrapping stay host-side. Composes output-contracts / agent-tool-ergonomics (ATE-2/ATE-12/ATE-13) / tool-call-transport (TCT-4, TCT-8) / tool-composition (TC-2) / interception-model (INT-10 boundary) / execution-graph (EG-2, EG-11) / declarative-configuration / security / completion-verification / practice-analytics (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-output-contracts.md](specifications/l1-output-contracts.md) ; v1.1.0 adds OC-8 — **exhaustion resolves to a named disposition, never to a loop**: an OC-6 escalation MUST terminate in exactly one of a **closed** set (different producer / decompose / revise approach-or-contract / accept-with-recorded-limitations / defer), with "try again the same way" structurally absent since that budget was already spent proving it fails, and carrying the full attempt history + root-cause reading + blast radius the choice requires; *accept with limitations* is legitimate **and** recorded, so a knowingly-accepted gap never later passes for a clean result | Inline output validation: schema + callable + criteria (LLM-evaluated) validators on task/node output, retry budget with verdict injection, escalation to ORC-11 on exhaustion — complement to post-task quality gates; v1.0.1 links `l1-input-binding` as the consumer-side twin (same unit, opposite machinery: producer retries with verdicts, consumer rejects without retry) (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-resource-sharing.md](specifications/l1-resource-sharing.md) | Uniform fine-grained access model: single grant primitive (resource_type, resource_id, principal, permission), three principal types (user/group/public wildcard), default-private, owner-invariant, additive grants, audit trail — applies to knowledge/skills/tools/notes/channels/files/prompts; v1.1.0 RS-9 **grants are non-escalating; a read surface is read-only by construction** — create/extend/delegate authority belongs to owner and `write` holders only, so a read grant is a terminal leaf in the access graph, the interface behind it exposes *no* mutating operation (absent, not present-and-refused), and any credential minted *for* a read share is issued only by a `write` holder; the sharing-surface expression of SEC-10 (granted downward by someone who holds it, never sideways or upward by someone who received it) (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-knowledge-base.md](specifications/l1-knowledge-base.md) | Knowledge collections: named access-controlled document sets with hierarchical directory structure, incremental indexing, source attribution per chunk, hybrid semantic+keyword retrieval (RAG), non-authoritative recall; authorship zones with storage-enforced human/agent write boundary (KB-9); curation lifecycle draft→reviewed→stable (KB-10); optional pre-retrieval query preparation — keyword extraction/expansion (multi-language) and/or compound-query decomposition, transparent + fallback-floored to the raw query + attribution/access-preserving (KB-11, §4.8); deep document-understanding stage before chunking (KB-12, l1-document-understanding); structure-aware breadcrumb-carrying segmentation with atomic tables/figures (KB-13, l1-content-segmentation); optional multi-representation index — corpus knowledge graph + hierarchical summary tree beside the mandatory flat index (KB-14, l1-knowledge-graph/l1-hierarchical-summarization); multi-channel fused retrieval + rerank, precision-only never widening access (KB-15, §4.9); structured-constraint separation — self-query extracts a metadata-filter predicate from the NL query (composing KB-11) leaving a residual semantic query, applied as a narrow-only candidate filter (KB-4, never privilege-escalation) with attribution preserved (KB-6), transparent + fallback-floored to plain semantic search (KB-16, §4.10); index-side query-bridging representations — a chunk indexed under generated proxy match surfaces beyond its own text (questions it answers, summary, keywords, figure caption/image), each returning the SAME source chunk with its attribution (KB-6, never the proxy as source), the index-side dual of KB-11 query prep, composing fused retrieval (KB-15), opt-in/bounded/host-supplied (KB-17, §4.11) (tech-agnostic) | Stable | 1 | 1.5.0 |
| [l1-file-management.md](specifications/l1-file-management.md) | File subsystem: explicit ingestion, content-addressed deduplication (SHA-256), metadata decoupled from blobs, access-controlled download, reference tracking for GC, size enforcement, immutable blobs (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-notes.md](specifications/l1-notes.md) | Notes as persistent artifacts: rich structured content, access control, per-user pinning, agent authorship, append-only edit history, CRDT concurrent merge, soft deletion (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-multi-device-sync.md](specifications/l1-multi-device-sync.md) | Multi-device state synchronization — keeps one user's office state consistent across their own devices (desktop, laptop, always-on phone-as-personal-server) with no central coordinator and no third party: strong eventual convergence (SY-1), no-master any-replica writes (SY-2), offline-first partition tolerance (SY-3), **data-class reconciliation routing** — CRDT-merge for collaborative/append state (notes/memory/board/chat/logs), reviewable 3-way merge for structured artifacts (specs/plans/workflows, the l1-change-merge path, never auto-merged), supersede for single-authoritative facts (operational ledger) (SY-4), causal metadata not wall-clock (SY-5), convergent deletion (SY-6), bounded compacted sync metadata (SY-7), on-device end-to-end no-third-party opt-in (SY-8), explicit authenticated device membership/pairing (SY-9); ties together l1-notes (CRDT) + l1-change-merge (3-way) + l1-operational-ledger (supersede); complementary to l2-backup (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-derived-artifact-handoff.md](specifications/l1-derived-artifact-handoff.md) | Derived-artifact handoff — "share the expensive rebuildable index instead of recomputing it, without ever merging it": the discipline for portable handoff of a deterministically-rebuildable derived artifact (code knowledge graph, memory embeddings/signals, any precomputed index) so a second device or teammate imports a snapshot and fills only its local diff, skipping the full recompute. **Categorically distinct from l1-multi-device-sync** (which converges *authoritative* data via CRDT/3-way/supersede) — a derived artifact has no independent truth, so it is mergeless: derived-only never authority, discard costs only recompute (DAH-1, composes CI-3/MC-5); import-then-incremental bootstrap, absent ⇒ full build (DAH-2); integrity+schema-checked import with full-rebuild fallback, never poison/half-import (DAH-3); mergeless by construction — ours-wins + rebuild the diff, MUST NOT route through SY-4 (DAH-4); two-tier thorough/fast export (DAH-5); opt-in + inspectable presence/tier/staleness/provenance (DAH-6); source-confidentiality-inheriting + CI-13 secret-exclusion floor + user-controlled transport, no third-party egress (DAH-7). Ideas-to-Adopt (recorded, no Stable-neighbor churn): embedded type-aware resolution without an LSP process (third path past the self-contained-vs-heavy-LSP framing in CI-16/l2-codegraph Drawbacks), transparent search-reflex augmentation (interception-model + agent-tool-ergonomics, fail-open, never gate read), interactive graph-explorer UI (l2-office-view + CI-12 projection). nodus: a validated/transpiled workflow + workflow-graph is itself a derived artifact, handoff-eligible with source .nd as sole authority (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-project-vocabulary.md](specifications/l1-project-vocabulary.md) | Project vocabulary — the governing artifact that lets a project say something **short and exact**, instead of re-describing a concept in twenty words every time it comes up. One canonical term per concept with its definition, because two live terms force every reader and producer to decide whether the difference carries meaning and they decide inconsistently (VOC-1); **displaced synonyms listed with the entry, never merely absent** — an absent synonym is invisible guidance, a listed one corrects *before* the wrong word is written (VOC-2, a negative specification at term granularity); **relationships** between terms, since confusion is almost always about connection rather than one word's meaning (VOC-3); ambiguities **resolved on the record**, so the same question is not re-litigated and older material stays decodable (VOC-4); the vocabulary governs **produced artifacts** — identifiers, module names, work-item titles, records — because one honoured in prose while the code says otherwise has *doubled* the terminology, and the code's version is what future readers believe (VOC-5); authored **during the work** when the distinction is live, since a vocabulary compiled afterwards captures what was already agreed and omits exactly the contested terms (VOC-6); a term **earns** canonical status through the codification pathway and a generating actor never self-promotes its own coinage (VOC-7); **authoritative, distinct from the client-facing glossary**, which is a derived plain-language projection — conflating them yields a governing artifact rewritten for readability and an outward document nobody may edit (VOC-8); precision the goal and volume a cost, an unconsulted vocabulary being **worse than none** since artifacts still claim to follow it (VOC-9); adherence and **compression** measured, the honest measure being that a settled term saves symbols on every occurrence forever while a term nobody uses saves nothing (VOC-10). **nodus projection, no new primitive**: the schema vocabulary is already a validated controlled vocabulary enforcing one name per operation (unknown command fails at validation), selective disclosure is already the volume control, and author-chosen names carrying a displaced synonym are lintable at the validate-before-run stage. Composes project-wiki (the boundary) / pattern-codification (the ratification pathway) / negative-specification / operational-ledger (OL-4 paraphrase drift, cured upstream) / content-language (orthogonal axis: which language vs which terms) / code-intelligence / cache-stable-context / knowledge-base / memory-model / invariant-tripwires (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-project-wiki.md](specifications/l1-project-wiki.md) | Project wiki — client-facing, office-maintained living documentation per project (what it is, what's built, key decisions, how to use deliverables) the client browses to stay informed without managing: plain-language client surface (PW-1), office-maintained never client-curated (PW-2), projection-not-source regenerated from real artifacts (PW-3), grounded & attributed anti-hallucination — every claim cites a decision/work-product/board-item/ledger-fact (PW-4), living & freshness-honest with stale flags + changelog (PW-5), navigable & searchable (PW-6), scoped per-office & access-controlled (PW-7), distinct from agent knowledge-base and internal design/SDD artifacts, no internal-detail leak (PW-8); event-driven incremental regeneration; reached via the Wiki navigation tab (tech-agnostic); v1.0.1 links `l1-project-vocabulary` as the authored authoritative source of which the PW-1 glossary page is a plain-language client-facing projection (PW-3) | Stable | 1 | 1.0.1 |
| [l2-project-wiki.md](specifications/l2-project-wiki.md) | Implements l1-project-wiki. Per-office SQLite projection CACHE (workspace wiki/wiki.db), not loose Markdown: wiki_page (parent/ord tree, citations JSON, source_fingerprint, stale flag) + wiki_changelog + wiki_page_fts (FTS5 search); event-driven incremental transactional regeneration from ground truth board/decisions/ledger/deliverables (PW-3/4/5); rebuild() as operational proof the store is a rebuildable cache; read-only client access — no write path into wiki rows (PW-2); per-office scoping + access-grants Read gate when shared (PW-7); distinct from knowledge-base with an internal-detail content filter (PW-8); contrast recorded — wiki store = cache of upstream truth vs memory learned-tier where the store IS truth (MEM-4) | Stable | 2 | 1.0.1 |
| [l1-groups.md](specifications/l1-groups.md) | User groups: flat named sets of users as a principal type in the resource-sharing model; admin-managed membership; additive access; audit trail (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-evaluations.md](specifications/l1-evaluations.md) | Per-message feedback: discrete sentiment + structured tags + free text; optional post-delivery; immutable audit record; privacy-first (no provider sharing without consent); feeds router scoring and analytics (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-folders.md](specifications/l1-folders.md) | Session folder organisation: personal hierarchical containers; non-destructive delete (sessions move to root); sortable; unique sibling names; no sharing (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l2-resource-sharing.md](specifications/l2-resource-sharing.md) | Access grant implementation: single SQLite access_grant table (resource_type/resource_id/principal_type/principal_id/permission unique constraint); has_access resolution (owner→user→group→public); batch grant loader for N+1-free listing; group membership cache; access-grants crate layout | Stable | 2 | 1.0.0 |
| [l2-knowledge-store.md](specifications/l2-knowledge-store.md) | Implements l1-knowledge-base. Knowledge store implementation: SQLite schema (collection/directory/document/chunk); sqlite-vec for ANN; FTS5 for keyword search; RRF hybrid fusion; async ingestion pipeline with file/URL/record adapters; soft-delete GC; storage-enforced authorship zones (origin human/agent, read-only human zone w/ audited WriteOverride token at the single db.rs write seam — KB-9) + curation lifecycle draft→reviewed→stable w/ human-gated transitions + min_curation retrieval trust floor (KB-10) + optional QueryPreparer seam fallback-floored to raw w/ sub-query RRF merge (KB-11); knowledge-store crate layout | Stable | 2 | 1.1.0 |
| [l2-notes.md](specifications/l2-notes.md) | Notes implementation: SQLite schema (note/pinned_note/note_version/note_crdt_update); ProseMirror-compatible JSON content tree; Yjs CRDT binary update log; version history policy; in-memory Yjs doc cache with idle eviction; soft-delete GC; notes crate layout | Stable | 2 | 1.0.0 |
| [l2-file-store.md](specifications/l2-file-store.md) | File store implementation: SQLite schema (file/file_reference); StorageBackend trait (local FS default, S3-compatible plug-in); SHA-256 content-addressed dedup (hash-to-path sharding); async upload pipeline; magic-byte MIME detection + allowlist; GC scheduler; file-store crate layout | Stable | 2 | 1.0.0 |
| [l1-spec-driven-governance.md](specifications/l1-spec-driven-governance.md) | Spec-driven governance meta-spec for autonomous agent work: artifact-before-action (SDG-1); two-layer Concept/Implementation model with parent-gated promotion (SDG-2); bounded lifecycle with encapsulated auto-promotion (SDG-3) + adaptive-weight completeness gate (SDG-4); quarantine cascade on foundation destabilization (SDG-5); registry parity with orphans-as-blockers (SDG-6); drift-guard family — version/rules/tooling/config (SDG-7); autonomous decision protocol — decide-and-narrate [DR] grammar + closed escalation whitelist + session-persistent posture (SDG-8); two-stage review — content critic then instruction-quality pass (SDG-9); navigable artifact knowledge graph — god-node/orphan/coverage analysis + token-budgeted recall (SDG-10); one-way traceability containment (SDG-11); finalize-never-auto-commit (SDG-12); closed-loop retrospective with DORA delivery metrics (SDG-13); session continuity + single-next-step briefing (SDG-14); two operating profiles trust/audit over one engine (SDG-15); ideas-to-adopt mapping to existing concept specs (tech-agnostic) | Stable | 1 | 1.0.1 |
| [l1-practice-analytics.md](specifications/l1-practice-analytics.md) | Practice analytics & coaching engine — diagnostic layer over normalized session traces: detector/rule separation (measure vs judge, PA-1); data-driven detection as portable rule documents with an embedded metric DSL (PA-2); layered rule sources built-in<personal<project with id-override (PA-3); trust-on-first-use content-hash gate with edit revocation (PA-4); bounded pure side-effect-free evaluation with guarded regex (PA-5); normalized cross-runtime trace model at session/request grain with provenance (PA-6); honest typed data-gap accounting — pending/errored/no-data/missing excluded from denominators, never imputed (PA-7); minimum-sample gating (PA-8); severity-weighted trended practice scoring with dynamic per-occurrence severity (PA-9); read-only & on-device, consent-gated egress (PA-10); actionable templated findings carrying remedy+examples (PA-11); workflow-to-skill mining hands off candidates, never auto-installs (PA-12); context/config health auditing reusing the existing agentic-readiness checklist (PA-13); testable-before-activation authoring with load-time fixtures (PA-14); five practice groups (prompt-quality/session-hygiene/code-review/tool-mastery/context-management); PA-15 agent-initiated retrospective → consolidated plan (§5.4) — the office may initiate its own "where am I losing time?" retrospective running the same detectors/rules across a chosen scope (session/office/all-history) and consolidate findings+scores+mined clusters into one prioritized improvement plan (highest-cost habit first, each with its remedy PA-11 and any skill candidate PA-12), read-only+honest (PA-7/PA-8/PA-10), handed off never auto-applied — self-diagnosis as a first-class agent-initiated act over the existing engine not a new engine; ideas-to-adopt mapping (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-invariant-tripwires.md](specifications/l1-invariant-tripwires.md) | Invariant tripwires — the **enforcement half** the rule-producing layers never supplied: codification promotes a pattern to a ratified rule and specs state invariants, but neither asks *who notices when the rule is broken next time*, and the answer in practice is memory — the mechanism whose failure created the rule. A tripwire is a narrow automated check living with the code that fails when **one specific forbidden shape reappears**. A mechanically checkable rule carries a mechanical check (TW-1); the tripwire is authored **in the same change that earns the rule**, when the offending shape, the reason, and the alternative are all still precise and before they decay to folklore (TW-2); one tripwire per rule, named for the rule, so a failure reads as *this rule was violated* (TW-3); the failure names rule, site, reason, and sanctioned alternative — a check that teaches nothing gets suppressed rather than satisfied (TW-4); targeted and cheap, never a general style rule, since precision is the source of its authority (TW-5); **structural checks and behavioral tests are distinct kinds and both required** — a system can pass every behavior test while violating every structural rule, because the behavior is still correct *today* (TW-6); exemptions explicit, narrow, and reasoned **inside** the tripwire, never by widening the pattern (TW-7); a rule that stops holding is retired **with** its tripwire rather than quietly weakened, the worst state being a rule that looks enforced while matching nothing (TW-8); the enforcement **inventory is legible** — the uncovered set is the honest measure of how much governance runs on memory (TW-9); and the gate is on the **delta**, never accumulated state, since a tripwire failing on pre-existing debt is disabled and protects nothing (TW-10). **nodus projection, no new primitive**: the validate-before-run stage hosts authored-workflow tripwires, and the canonical-form round-trip guarantee gives them a **reformat-proof structural target** instead of a surface-text pattern. Composes pattern-codification (the producer) / convergence-gate (placement + delta) / quality-standards (a tier, not a parallel gate) / change-containment / completion-verification (CMP-2) / evaluation-suites (the behavioral sibling) / optimization-integrity / spec-driven-governance (tech-agnostic) ; v1.1.0 TW-11 a tripwire **declares what it cannot see** and records its precision trade — uncovered shapes named as known limits, accepted false positives recorded with why tightening costs more; prevents both broadening-until-noise (which gets the tripwire disabled) and the unstated gap that lets the TW-9 inventory count a rule as enforced while a class of violation walks past, a false assurance worse than no check; a declared limit is the honest input to TW-6 | Stable | 1 | 1.1.0 |
| [l1-evaluation-suites.md](specifications/l1-evaluation-suites.md) ; v1.6.0 ES-21 added - a **generated** evaluation item never leaks its answer through surface form: where options, distractors, or paired alternatives are generated rather than hand-authored, length, specificity, hedging, formatting and terminology are all channels by which the correct option becomes identifiable from shape rather than content, producing a task solvable **without the capability under test** and a score measuring the generator's stylistic habits; generation normalizes surface form across options and the property is **checked, not intended**, since a generator told to avoid the tell reproduces it | Evaluation suites — declarative, version-controlled test suites for agent customizations (skills/roles/workflows), the dynamic half of customization quality complementing the static document lint: suite-as-companion-artifact bound to its target (ES-1) with addressable golden tasks declaring inputs+expectations+graders (ES-2); typed grader taxonomy across five families — activation/invocation/behavior-budget/output-shape/judgment — composing existing validators, never redefining them (ES-3, reuses l1-output-contracts + behavior budget); global+task grader scopes (ES-4); weighted thresholded metrics with per-metric hard-gate, no opaque single score (ES-5); stability via repeated trials with variance reporting (ES-6); frozen+isolated-per-run, fixture-seeded, state-reset (ES-7); immutable attributed diffable results with full provenance (ES-8); baseline comparison + regression gate feeding skill evolution (ES-9); first-class activation-correctness testing — over/under-triggering as failures (ES-10); sandboxed no-production-side-effects reusing AP-13 §4.9 quarantine (ES-11); scaffold-then-maintain authoring, scaffold never authoritative coverage (ES-12); static-vs-dynamic two-halves framing; v1.1 control-arm attribution — credit only the margin over the best naive same-intent control, not baseline (ES-13); separated non-negotiable quality/safety tier with implicit-requirement adversarial tasks, no efficiency gain buys back a quality regression (ES-14); judge trust self-test + cite-the-construct auditability (ES-15); signed metrics never penalizing necessary additions as bloat (ES-16); sliceable results by orthogonal declared labels — per-slice macro-averaged reporting so a regression localizes to a capability/complexity/scenario/modality/source slice, not just the aggregate (ES-17), the suite-level companion to the l1-agent-coevaluation matrix; reference-free comparative judgment — a relative bias-guarded pairwise preference (A vs B) for open-ended tasks with no ground-truth reference, order/verbosity/self-enhancement guarded and non-transitivity-honest, a relative non-authoritative signal consumed by harness-optimization/iterative-refinement/competitive-execution and never an absolute metric (ES-18, §4.10); capability-dependency ordering — a task MAY depend on prerequisite tasks (a capability it builds on), evaluation topologically ordered, a dependent whose prerequisite failed reported blocked + attributed (by default not run, no budget on a foundation-broken capability), dependency on the prerequisite's verdict only never its state so ES-7 isolation preserved, a broken chain localized to its root (ES-19, §4.11); v1.5.0 ES-20 **dependency-selected, cost-tiered execution** — a slow or metered suite is not run in full on every change, since a check too expensive to run gets skipped and a skipped check is worse than an absent one (the suite still looks protective): tasks declare the artifacts whose change could alter their outcome (a designated global set selects everything; full runs stay on demand) and a declared tier — deterministic/fast/safety-relevant **gate** blocking every change vs non-deterministic/expensive/externally-dependent **periodic** reporting on a schedule; a partial run names what it skipped and yields an explicitly partial verdict, never a full-suite pass, and a safety-relevant task is never demoted to periodic to speed the gate (tech-agnostic) | Stable | 1 | 1.6.0 |
| [l1-agent-coevaluation.md](specifications/l1-agent-coevaluation.md) | Agent co-evaluation methodology — makes agent quality diagnostic not merely ranked, on the claim Performance = f(Model, Harness): the (model, harness) pair is the unit of evaluation, an aggregate that conflates the two factors is never the only signal (ACE-1); the independent-factor M×H matrix reads a harness-gap (fixed model, vary harness) and a model-gap (fixed harness, vary model) from one run set (ACE-2); orthogonal declared task taxonomy — scenario/capability/complexity/modality/environment/source (ACE-3); per-slice macro-averaged reporting localizing brittleness, a populous slice can't mask a small brittle one (ACE-4); harness comparability contract freezing prompt/workspace/timeout/transcript/result-schema, non-comparable harnesses scored separately (ACE-5); failure attribution across the model/harness/grader triad, grader-first so a false rejection isn't mis-blamed (ACE-6); reuse-&-normalize external task import with provenance-as-slice (ACE-7); targeted-slice iteration crediting a harness change only when its targeted slice moves, HE-5/HE-3 composed (ACE-8); diagnostic preservation of transcript/workspace/final-state per cell for replay (ACE-9); drives l1-evaluation-suites, composes l1-harness-engineering/l1-dynamic-harness/l1-nodus-environment; public-leaderboard machinery rejected as out-of-scope (single-principal, not a public benchmark) (tech-agnostic) | Stable | 1 | 1.0.1 |
| [l1-browser-control.md](specifications/l1-browser-control.md) | Agentic browser control — reliable agent-driven web browser for QA/dogfooding/web tasks: persistent control daemon with auto-start/idle-shutdown and cross-command state (BC-1); accessibility-tree handle addressing, never model-authored CSS/XPath or DOM mutation, CSP/framework/shadow-root robust (BC-2); fail-loud handle invalidation on navigation/mutation with existence-check (BC-3); side-effect-classified commands READ/WRITE/META governing retry- and parallel-safety (BC-4); untrusted web content with layered injection defense + deterministic system-prompt canary stop + ensemble-agreement BLOCK rule (BC-5); loopback-bound control plane + per-session capability tokens, remote exposure via separately-bound allowlisted scoped-token surface with physical socket separation not header inference (BC-6); agent-actionable error rewriting (BC-7); fail-fast crash recovery — control plane exits, client restarts clean (BC-8); non-blocking bounded ring-buffer observability (BC-9); user-approved in-memory-only credential access, read-only real cookie store, no secret values in logs (BC-10); library-first surface with CLI/TUI bindings (BC-11); per-workspace multi-instance isolation (BC-12); ideas-to-adopt mapping incl. nodus side-effect step-classification candidate (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-facilitation.md](specifications/l1-facilitation.md) ; v1.3.0 FC-14/15/16 added - **stake tiering inside the frontier**: FC-13 decides which questions may be asked now (dependency), FC-14 decides how (consequence) - load-bearing questions asked **singly and in sequence** because each answer reshapes the rest, everything else issued as one **veto-by-exception** block, and every question carrying why-it-is-load-bearing + a committed recommendation + a **named consequence of guessing wrong**, the third being the **admission test** rather than decoration (a weak consequence line demotes the question), which is the mechanical fix for the low-value-questioning defect FC-13 names and deliberately leaves unnamed; **bulk acceptance is a granted delegation over a named set** - offered never assumed, naming the resolved set it covers at the moment it is given (CB-1/CB-2), and marking every decision it locks as **accepted-by-default** so a later reader can tell a deliberated choice from a defaulted one - absent all three it is a fabricated counterparty (XPL-4) in a shortcut's clothes, since silence is not the grant; **the resolved set presented once as a sourced batch** before questioning begins, each entry naming what settled it, and a **correction re-opening that entry as a question** along with everything downstream that rested on it ; v1.2.0 FC-13 added - **frontier-batched elicitation**: open questions form a dependency tree, a question depending on another still-open question belongs to a later round, and the agent asks the whole **frontier** in one round with a **recommended answer** attached to each, completing when the frontier empties rather than at a count; **facts are the agent's job, decisions the user's** (environment-discoverable facts looked up in parallel, blocking only their downstream questions); reconciles the apparent tension with FC-4 explicitly - FC-4 governs the *process* menu where a recommendation cannot substitute for the user's pick, while a content question carrying a recommendation is a proposal, not a wall; a hard question cap is rejected as conflating an under-specified subject with low-value questioning | Facilitated cognition — exploratory, advisory thinking-facilitation subsystem (never decides): portable data-driven method catalog with context-aware bounded selection + goal→technique routing + layered override (FC-1/FC-2); elicitation loop applying named techniques to recent output with change-gated apply (FC-3); non-committal reshuffle/list-all/proceed surface (FC-4); three ideation stances facilitator/partner/autonomous held for the run (FC-5); divergence-before-convergence with quarantined judgment, diverge→converge→finalize lifecycle (FC-6); append-only resumable session memory as single source of truth (FC-7/FC-8); multi-persona roundtable preserving persona integrity with injected dissent (FC-9); run-mode degradation inline↔delegated without failure (FC-10); interactive/headless contract separation (FC-11); advisory boundary deferring binding decisions to deliberation + decision protocol (FC-12); ideas-to-adopt + nodus-relevance mapping (catalog-as-vocabulary, lifecycle-as-control-flow, memlog-as-observability, surface-as-HITL); v1.1 catalog gains optional counters(failure-mode)/activity_scope fields → failure-driven + activity-scoped selection (FC-1) and inter-technique conflict-resolution ordering (FC-2) (tech-agnostic) | Stable | 1 | 1.3.0 |
| [l1-operational-ledger.md](specifications/l1-operational-ledger.md) | Operational knowledge ledger — exact, id-addressable operational ground truth for agent dispatch, a third knowledge kind beside semantic recall and RAG: atomic class-grouped predicates one-fact-per-entry (OL-1) with stable supersede-don't-mutate identity (OL-2); canonical-source precedence chain resolving disagreement (OL-3); verbatim-citation no-paraphrase brief grounding as anti-drift (OL-4); mandatory required-reading before any action (OL-5); learnings as provenance-stamped accretion + segregated append-only session log (OL-6); exact grounding not similarity recall (OL-7); bounded observable selection, never dump the whole ledger (OL-8); severity-gated conflict check on ingestion — blocker halts / warning needs approval / info transparent, never auto-merge (OL-9); on-device-default + pointer-not-value for secrets (OL-10); ideas-to-adopt + nodus-relevance mapping (predicates-as-@ctx, required-reading-as-selective-load, no-paraphrase-as-data-not-instructions guard, conflict-gate-as-validator) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-operational-health.md](specifications/l1-operational-health.md) | Operational health & insights — on-device observability over runtime traces, distinct from telemetry (outward sharing), self-healing (repair), and practice analytics (user coaching): read-only on-device observation (OH-1); explainable bounded health score with per-signal breakdown (OH-2); severity-graded deduplicated threshold alerts (OH-3); rolling-window trend/anomaly detection — activity-drop, runaway cost/token/session-length growth (OH-4); context-pressure signal via msgs/session as a context-bloat leading indicator (OH-5); cost & per-model usage accounting as first-class signals (OH-6); measure-don't-act boundary deferring repair to self-healing and coaching to practice analytics (OH-7); inspectable & exportable health snapshot (OH-8); v1.1.0 alert-discipline amendment — rules bind to *classes* with label narrowing and auto-attach to instances created after the rule was written, instance-specific rules taking precedence (OH-9); separately expressible raise/clear thresholds plus raise/clear settle delays so a rule cannot flap, a distinct mechanism from OH-3 dedup (OH-10); a distinct **undetermined** status for un-evaluable rules that never reads as clear and contributes no healthy weight, completing the stack-wide absence-is-not-good-news discipline beside OR-3 gaps and AC-6 unscored (OH-11); silencing as a scoped/expiring/attributed operation suppressing notification only while evaluation and snapshot visibility continue (OH-12); rules address **roles** with the role→channel mapping configured separately as its own declarative surface (OH-13); §4.4 alert model extended with rule/status/to_role/silenced, new §4.6 rule binding + hysteresis + status lifecycle; v1.2.0 OH-14 — **bursts are digested, never streamed, and a digest drops nothing**: several *distinct* notifications becoming due within a short window are held and delivered as one digest grouped by rule and unit, the holding window **extending on each new arrival** up to a declared maximum so a continuing storm yields one late complete digest rather than a rapid series of small ones while the maximum keeps it arriving while it matters; a digest loses nothing (packaging changes, content never does) and stays distinct from dedup (repeats of the *same* signal, OH-3) and silencing (a *chosen* signal withheld, OH-12); a burst delivered as a stream is the reliable way to make a person stop reading the channel, after which every later signal including the one that mattered is unread by default (tech-agnostic) | Stable | 1 | 1.2.0 |
| [l1-policy-governance.md](specifications/l1-policy-governance.md) | Administrative policy governance — control layer over configuration, distinct from client security (execution/secrets) and authentication (identity): layered tier precedence managed→cli→project→user→default with resolvable provenance (PG-1/PG-3); authoritative un-overridable managed tier set out-of-band (PG-2); surface lockdown — managed-only extensions/hooks/permission rules (PG-4); trusted extension-source restriction (PG-5); escape-hatch governance — disable safety-reducing modes like permission-bypass (PG-6); fail-open when unmanaged / fail-safe + auditable when managed (PG-7); integrity-verified policy source, tamper ≠ no-policy (PG-8); **PG-9 per-parameter override visibility & revert-to-default** — the settings surface shows per parameter whether the value is default or an override and offers, for a principal-writable override, a revert that *clears the override at the principal's own writable tier* so the value falls back through the precedence chain (PG-1), never editing a non-writable tier; affordance appears only when there is a principal-writable override to clear — a default-valued or managed-locked (PG-2) parameter shows locked-not-resettable; revert is a governed auditable write (PG-7); "default" = value absent this principal's override (may be a lower tier, not necessarily built-in baseline); the inline reset-icon affordance is the l2-app-ui realization, the precedence-and-lock-aware semantics are L1; v1.2.0 PG-10 — a **newly introduced or tightened** managed restriction (surface lockdown PG-4, narrowed trusted sources PG-5, a lowered cap) ratchets **observe → warn → enforce** rather than binding on first contact: observe blocks nothing and records exactly what it would have blocked, warn lets the operation succeed while telling the principal the restriction now covers it, enforce blocks; the reason is tier-specific — a managed restriction is set out-of-band (PG-2), applies to everyone simultaneously, and cannot be overridden by the people it breaks (PG-1), so it has the largest blast radius and the fewest escape routes of any change in the system while the measurement that would have sized it correctly is free before it binds; governs introduction only (an already-enforcing restriction untouched, PG-7 fail-safe-presence unchanged), with `enforce → warn` demotion as the immediate remedy for a mis-sized restriction without editing a threshold; full ratchet semantics incl. the derived warn threshold owned by l1-staged-rollout SR-10 (tech-agnostic) | Stable | 1 | 1.2.0 |
| [l1-code-execution.md](specifications/l1-code-execution.md) | Code-execution tool surface — agent composes a multi-step task as one typed, sandboxed program with in-language capability calls instead of N discrete tool-call round-trips: program-as-orchestration with control flow + intermediates (CE-1); typed capability projection auto-derived from existing tool schemas (CE-2); confinement parity — every nested call gated/audited exactly as the discrete call, never an escape hatch (CE-3); stateful addressable cells with explicit lifecycle (CE-4); budget-bounded yielding execution with wait primitive (CE-5); structured attributed results (CE-6); per-call equivalence + discrete-call fallback (CE-7); observable/replayable trace (CE-8); workflow-language compiles onto this surface (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-process-integrity.md](specifications/l1-process-integrity.md) | Process integrity & hardening — protect the live process address space holding in-use secrets, the in-memory complement to secret-at-rest isolation and the sandbox: no crash-dump leakage (PI-1); refuse debugger/tracer attach (PI-2); scrub injection-vector env (preload/loader-override) (PI-3); early pre-secret enforcement (PI-4); sanitized-env child inheritance (PI-5); visible (audited) degradation when OS cannot honor a step (PI-6); scope boundary — layered with SEC-1/SEC-6, not a privileged-adversary defense (PI-7) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-messaging-gateway.md](specifications/l1-messaging-gateway.md) | Messaging gateway — one gateway making the agent reachable from external messaging platforms via pluggable per-platform adapters, the outward counterpart to the internal inbox: one gateway / many adapters, agent stays platform-agnostic (MG-1); normalized inbound/outbound message contract honoring per-platform constraints — length units, reply/thread semantics, media/voice (MG-2); verifiable identity pairing, unpaired senders untrusted (MG-3); cross-platform conversation continuity keyed to principal not platform (MG-4); authorization by resolved principal, reachability ≠ permission (MG-5); faithful ordered encoding-safe delivery (MG-6); loopback-default exposure safety (MG-7); transit privacy + on-device voice transcription (MG-8); per-platform fault isolation (MG-9) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-content-language.md](specifications/l1-content-language.md) | Content language — the language of **model-generated content**, the question interface localization does not answer and nothing else owned (resource bundles select among strings someone already wrote; nothing decides what language a model writes a *new* memory record in). Declared per surface, never emergent from the prompt, the model's bias, or the provider (CL-1); keyed on **artifact kind, not one global locale**, because the real requirement is usually split — conversation in the person's language, technical artifacts in the project's — and one locale makes one of them wrong by construction (CL-2); **one stable language per durable corpus kind** as a *retrieval-integrity* requirement, since drift silently halves lexical recall, defeats duplicate detection, and blocks consolidation merges (MC-6) while reporting nothing (CL-3); **language of record separate from language of display** — a translation is a derived projection with provenance, never an in-place rewrite (CL-4); quoted and technical material — user words, tool output, error strings, identifiers, paths — reproduced never translated, since exactness is their value (CL-5); enforced structurally where the field is not model-authored, **checked** where it is, instruction-only acceptable solely where nothing durable depends on it (CL-6); unstated preference grounded declaration → user model → default, otherwise a recorded correctable assumption (IR-3/IR-6) (CL-7); a **cross-language retrieval gap bridged or stated, never rendered as a bare empty result**, which asserts absence where the truth is *not found the way you asked* (CL-8); language changes forward-only with an explicit audited reversible migration (CL-9); consistency measured from the corpus, not asserted (CL-10). **nodus projection, no new primitive**: the validated `§config` surface carries the policy field, value provenance already expresses the derived-translation relation, and schema/vocabulary prose classifies with the technical artifacts. Composes memory-model (MEM-4) / memory-intelligence / memory-consolidation (MC-6) / user-model (UM-4) / intent-resolution / generation-shaping (sibling axis) / search / data-lineage / project-wiki / notes / knowledge-base (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-user-model.md](specifications/l1-user-model.md) | User model — the agent's persistent, evolving representation of the person (preferences, goals, traits, working style, context, relationships), a distinct knowledge kind beside conversation memory, the operational ledger, and knowledge collections: distinct subject-model answering "who is this user" not "what happened" (UM-1); inferred, evidence-backed, confidence-scored, non-authoritative (UM-2); cross-session anti-drift accretion (UM-3); explicit-statement overrides inferred with provenance (UM-4); privacy-first, principal-scoped, on-device, inspectable + erasable (UM-5); bounded observable application — relevant attributes only, never wholesale (UM-6); consent-gated, depth-configurable formation (UM-7); service-not-manipulation ethical boundary (UM-8) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-perspective-model.md](specifications/l1-perspective-model.md) | Perspective model (theory of mind) — generalizes the user model into perspectival representation: beliefs keyed by (observer→subject), self-knowledge when they coincide, theory-of-mind when they differ (PM-1); defeasible belief not asserted truth, distinct from the operational ledger (PM-2); reasoning-first conclusions-with-evidence derived in background (PM-3); bounded observer set — exactly two uses, NOT a social graph (PM-4); the agent's model of the **user's epistemic state** (what they know/understand/misunderstand) drives right-level communication for a non-technical client (PM-5); the orchestrator's model of **each agent's knowledge** drives non-duplicating briefing/delegation realizing ORC-8 (PM-6); privacy/consent/inspect/erase incl. the belief model (PM-7); bounded observable application of the relevant perspective slice (PM-8); l1-user-model is its observer=agent/subject=user special case (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-execution-sandbox.md](specifications/l1-execution-sandbox.md) | Execution sandbox confinement — OS-level confinement of the code/commands the agent runs, the execution-confinement layer of defense-in-depth distinct from network egress (l2-sandbox-policy) and own-process integrity (l1-process-integrity): deny-by-default across four axes — operations/privileges/resources/filesystem+image (ES-1); operation/syscall allowlist enforced at the boundary (ES-2); least-privilege capability drop + no-new-privileges (ES-3); per-sandbox CPU/memory/process/IO resource bounds, breach hits sandbox not host (ES-4); read-only root + minimal declared scratch (ES-5); runtime-image digest pinning, never a mutable tag (ES-6); explicit audited narrowly-scoped escalation (ES-7); fail-closed for untrusted code when a control can't be applied (ES-8); independent layered barriers — one bypass doesn't relax others (ES-9) (tech-agnostic) | Stable | 1 | 1.0.1 |
| [l1-generation-budget.md](specifications/l1-generation-budget.md) ; v1.1.0 GB-2 amended — the capped capacity is **not exclusively the visible answer's**: on a deliberating model the allowance is consumed **first** by generation the caller never receives, so truncation has **two causes with opposite remedies** — answer cut off mid-production (escalate, then GB-3 continue from the partial) vs allowance **exhausted before the answer began** (raise the cap or lower depth per GS-3; there is no partial, and running GB-3 here re-spends the whole budget on deliberation again) — mechanically separable by **zero delivered output**; the accounting for that class moves to `l1-reasoning-spend` RSN-2 | Output generation budget — the output-side member of the token economy (symmetric to context-compression on the input side): minimal default output reservation sized to the common case, not the worst case, so a request never over-reserves generation capacity/GPU-slot/cost (GB-1); detect length-truncation and escalate the output cap toward the model's real limit, never accepting a cut-off response as complete (GB-2); continue from the preserved partial across bounded turns, stitching the full result, never discarding (GB-3); bounded escalation+continuation so a runaway generation cannot loop (GB-4); decomposition fallback on exhaustion — force skeleton-then-fill instead of retrying the monolith (GB-5); truncation-safe artifacts — a partial write/edit is rejected, never half-applied (GB-6); distinct axis from input-context budgeting and from the output contract (GB-7); accounted/observable feeding operational-health (GB-8); composes with l1-context-compression / l2-context-management / l1-output-contracts / l1-model-runtime / l2-model-error-recovery (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-context-compression.md](specifications/l1-context-compression.md) | Context compression — the third token-economy stage beside selection (trim) and summarization (compaction): reversible/fidelity-bounded dense re-encoding of high-volume structured content (tool outputs, diffs, logs, dumps) to cut token cost while preserving usable information. Distinct from dropping/paraphrasing (CC-1); lossless-or-bounded-declared-loss (CC-2); recoverable original on demand (CC-3); content-aware eligibility — bulky structure yes, dense prose / actively-edited code no (CC-4); model legibility with expand-on-demand (CC-5); measured/accounted savings (CC-6); runs before eviction cascade (CC-7); composable, no double-compress corruption (CC-8); protected-region deny-list — caller-tagged/safety/crypto-thinking never compressed regardless of size, overrides CC-4 (CC-9); §4.5 compress-cache-retrieve realization of CC-3 (content-addressed retrieval handle + pluggable store + TTL, lossy-on-wire/lossless-end-to-end, retrieval as a normal tool call); live-zone-only per l1-cache-stable-context CSC-2; CC-10 memory-safe lossy reduction (§4.6) — the lossy stages (summarization/compaction + hard eviction) MUST be preceded by durable capture into the persistent memory store so a lossy reduction is loss-of-detail never loss-of-knowledge (user prefs/decisions/commitments/in-flight task state secured before the transcript carrying them is condensed); distinct from CC-3 (which keeps a compressed original recoverable by handle) — CC-10 protects the lossy stages where no inline original survives so the safety net is memory not a handle; satisfied by continuous per-turn capture while it keeps pace, backstopped by an ordered-before best-effort pre-reduction capture pass (failure non-fatal/retried, never blocks the turn); couples the token-economy family to l1-memory-intelligence/l1-memory-model so an indefinitely-running session sheds transcript bulk without losing what it learned (tech-agnostic); CC-11 content-type-routed signal-preserving reduction (§4.7) — the compression transform is selected by detecting the content's type or producing command (log/diff/JSON/code/markup/search or a recognized package-manager/container/test-runner command output) and each type's reducer keeps the high-value signal characteristic of that type (errors+traces in logs, hunk headers+changed lines in diffs, imports+signatures in code, schema+anomaly rows in JSON, readable text in markup, top matches+counts in search) while collapsing only the redundant remainder; deterministic-first — unstructured prose passes through untouched unless the host enables a semantic model-based reducer, blanket structure-blind truncation forbidden; sharpens CC-1 (type-specific not one blanket transform) + CC-4 (eligibility gains a how: detect→route→preserve-signal) [v1.4.0] CC-12 archive-backed recoverability — CC-3's recoverable original rests on a durable substrate (`l1-evidence-archive` EA-1/EA-2/EA-3) rather than expiring when content leaves the live context, and archive-before-reduce extends the discipline to the lossy stages CC-1 deliberately excludes. | Stable | 1 | 1.4.0 |
| [l1-cache-stable-context.md](specifications/l1-cache-stable-context.md) | Cache-stable context — prompt-cache stability discipline elevating scattered L2 precautions (l2-agent-registry prompt-cache parity, l2-session-checkpoint frozen blocks, l2-learning-loop cache isolation, l2-dashboard cache_read accounting) into one L1: provider cache key as a first-class cost lever (CSC-1); frozen-prefix/live-zone partition with a computed boundary — only the tail live zone is mutable, system+tools+prior turns frozen (CSC-2); append-only history (CSC-3); byte-faithful passthrough — no reserialization/key-reorder drift (CSC-4); deterministic live-zone edits, no timestamps/random (CSC-5); position preservation (CSC-6); sacrosanct crypto/thinking fields never altered (CSC-7); schema parity across parent↔fork (CSC-8); billing-mode-gated aggressiveness — metered aggressive, subscription/session conservative-passthrough never voiding scope (CSC-9); observe-and-confine never rewrite-to-align anti-pattern (CSC-10); separated cache_read/cache_creation/input/output accounting so a cache bust is detectable from telemetry not the invoice (CSC-11); orthogonal to context-compression (what to shrink) — this governs where mutation is allowed at all (tech-agnostic); CSC-12 the boundary is a stable seam not a byte offset — the partition point CSC-2 computes is a position the encoder declares stable (one no continuation can move), not an arbitrary offset into the bytes, because encoding is not composable (l1-tokenization-boundary TB-2) and content appended in the live zone can retroactively re-encode the tail of the frozen prefix while leaving every one of its bytes untouched (key byte-identical, symbol sequence changed); CSC-4 byte-faithful passthrough and CSC-6 position preservation are therefore necessary but NOT sufficient conditions for a hit; anchoring the boundary on a control symbol (a message/turn frame marker) satisfies the rule by construction (TB-4), which is why the natural partition falls between whole messages and never mid-message, and a boundary whose stability the encoder does not assert is unstable (TB-3); the storage-side statement of the same rule is l1-inference-cache IC-10 | Stable | 1 | 1.1.0 |
| [l1-context-degradation.md](specifications/l1-context-degradation.md) | Context degradation & health — the failure-model the context-economy family exists to prevent, and the discipline that keeps a long context healthy not merely small: degradation is a **continuum managed on a risk signal, not the token limit** — acting only at the hard limit means already operating degraded (CD-1); **positional salience** — attention is positionally non-uniform (strong head/tail, weak middle), so load-bearing content (task, hard requirements, findings-to-act-on) goes in the high-attention head/tail zones never buried mid-context, placement a deliberate assembly decision composing the cache-stable frozen-prefix(head)/live-zone(tail) (CD-2); **non-adversarial poisoning** as a distinct failure from adversarial injection — a wrong/stale/hallucinated fragment entering on an honest path (bad tool result, stale doc, hallucinated summary) contaminates downstream reasoning and persists despite correction, defense is reasoning-hygiene not a boundary check (CD-3, complement of context-provenance CP); **recovery is excision to a verified state, not correction-in-place** — appending "ignore that" leaves poisoned references and the contamination compounds; only verified content carried forward, durable knowledge preserved via CC-10 (CD-4); behavioral not size-only detection (quality drop on previously-successful work, tool misalignment, persistent error, contradictory outputs) feeding operational-health, kin to OI-1 (CD-5); taxonomy-matched mitigation write/select/compress/isolate/excise/reconcile never one blunt reduction — volume-reduction on a poisoning/clash problem treats the symptom (CD-6); working-context clash surfaced+reconciled to one basis never silently averaged, the MI-4 counterpart (CD-7); owns the degradation phenomenology l1-context-compression/l2-context-management/l1-progressive-disclosure/ORC-5 each answer without a unifying model; content-side sibling of l1-optimization-integrity; nodus-relevance needs no new NL invariant — host-side salience over NL-15 + re-run-from-verified-checkpoint over the execution-graph/pinned-partial seam (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-optimization-integrity.md](specifications/l1-optimization-integrity.md) | Optimization integrity — the cross-cutting verification discipline for **silently-degradable optimizations** (context compression, cache-prefix stability, output shaping, ranked recall, inference-cache warmth) whose breach produces no error and well-formed output, degrading only cost/latency/answer-quality (a busted cache returns correct answers at 10× the bill — nothing throws, no functional test fails): named silent-failure class with mandatory integrity coverage, never relying on exceptions/schema/functional tests a silent breach doesn't trip (OI-1); runtime violation sensor for byte-exact/emergent contracts — a must-stay-zero counter alarmed on first live divergence (e.g. bytes-modified on a promised byte-faithful passthrough path), because a bust emergent from live per-instance state cannot be staged by a static test (OI-2); correctness parity measured-not-asserted via version-controlled fixture (with vs without) or held-out control (OI-3); efficacy parity — per-stage saving, counterfactual reported as estimate+confidence-range or held-out measured number, never a fabricated exact figure (OI-4); tail-aware distribution parity/regression gate — p50/p95/p99 AND mean, failing if ANY regresses, so a tail-only regression can't hide behind the median when a change/impl-swap is admitted (OI-5, generalizes retrieval-evaluation RE-6); bounded-cardinality code-closed observation dimensions — every label a closed code-fixed vocabulary, untrusted/unbounded input mapped to `other` and surfaced, never a raw dimension (OI-6, cost/DoS + forgery hardening composing LL-6/CP-1); verify-don't-repair-or-hide measure-and-surface boundary feeding operational-health, remediation to l1-doctor (OI-7); fail-visible — an unrun check is `unverified`, never rendered as `verified` (OI-8); unifies scattered per-surface guards CSC-4/CSC-11, IC-7, RE-6, GS-6 into one contract; nodus-relevance needs no new NL invariant (host-side AuditProvider HO-8/HO-9 + `!!` + RUN(@macro) reuse gating) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-artifact-derived-observation.md](specifications/l1-artifact-derived-observation.md) | Artifact-derived observation — the **uninstrumented-source** sibling of telemetry: how the office learns about a tool it does not control (an external agent, a coding tool the human runs beside it, a build system) by reading the durable trail that tool already writes for its own purposes. **Out-of-band by construction** — never wrap, proxy, shim, or inject, and never modify what is read; the guarantee bought is that the observer's failure cannot become the observed tool's failure, and it is void the moment anything enters that path (ADO-1). The artifact's shape is **discovered, never promised**: each reader declares what it expects and validates first, so a mismatch is a typed per-source coverage gap, never a run-ending error and never a silent reinterpretation yielding plausible numbers from an unrecognized structure (ADO-2). **Absent / unreadable / empty are three different answers** — collapsing them produces the characteristic failure of the whole approach, a confident zero read as a fact about the world; only the third is a real zero (ADO-3). One independently-failing adapter per source (ADO-4); discovery **declared, never a disk sweep** that reads what nobody agreed to expose and makes the observer's footprint unauditable (ADO-5); adapters loaded only on evidence their tool exists, so supporting N tools costs nothing to the user who has one (ADO-6). **One underlying event counted once** across overlapping sources via a derived event identity, with an unresolvable case **disclosed** rather than quietly inflated or quietly deleted — inflation is visible and correctable, deletion is neither (ADO-7). Incremental by cheap fingerprint with a versioned **interpretation**, so a corrected reader cannot leave stale conclusions standing behind an unchanged input (ADO-8). **Foreign artifact text is untrusted input** — a transcript another agent wrote can carry an injected instruction, and a file on the user's own disk is a location, not a provenance (ADO-9). Derived never authoritative, on-device, secret-safe (ADO-10). **Coverage travels with every number** — a figure from partial coverage presented as a total is this approach's defining dishonesty (ADO-11). **nodus projection, no new primitive**: reading is a declared read-only host effect, and existing value-provenance + origin-taint labels already carry *derived* and *untrusted* independently. Composes telemetry (boundary) / interception-model (the in-path alternative declined for foreign tools) / context-provenance / practice-analytics (PA-6/PA-7) / progressive-disclosure / agent-federation (the cooperating-peer complement) / security (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-outcome-attributed-cost.md](specifications/l1-outcome-attributed-cost.md) | Outcome-attributed cost — the closing half of the cost family, which measured **outflow** (meter → rate → allowance → enforce) and could not distinguish an expensive session that shipped from an equally expensive one reverted the next morning. Every unit of accounted spend is attributed to the durable output it produced **or explicitly to none**, with no third implicit state and a large unattributed share reported as *the finding* rather than excluded from the denominator (OAC-1). **Survival is a later, separately-observed fact** over a declared window, never inferred at production time — *unresolved* is an honest state, and marking work "shipped" when it is produced records an intention as an outcome (OAC-2). A **closed outcome vocabulary** — kept / superseded / reverted / abandoned / **discarded-by-design** / unresolved — whose load-bearing separation is that a deliberate best-of-N's losing branches (CE-*) are the declared price of a strategy, not a failure: fuse them with *reverted* and an office running competitive execution reads its own working mechanism as its largest waste (OAC-3). **Spend with no durable output is a measured category carrying no implicit verdict**, since the same number means opposite things for exploration and for a tenth attempt at one bug (OAC-4). Attribution **reconstructed from records already kept** (ledger, work item, change history, receipts) — a parallel hand-maintained mapping is forbidden because a drifted attribution still produces confident numbers (OAC-5); shared/uncertain attribution expressed, never resolved by an undeclared tiebreak (OAC-6); per-unit figures reported with denominator, dispersion, and minimum-sample abstention (OAC-7); **never a verdict on a person** — structurally absent, not filtered at presentation, because a measure that scores people is gamed and a gamed measure still carries a number's authority (OAC-8); **retrospective, never an in-flight gate**, since wiring survival statistics into live selection closes a loop in which the measure shapes what it measures (OAC-9); counterfactual honesty, no fabricated savings (OAC-10). **nodus projection, no new primitive**: the run record carries the cost half and gains a declared outcome slot; competitive selection labels its own discards at the source; a later survival verdict arrives through the existing correlated-later-completion path. Composes cost-rating / usage-allowance (UA-8) / competitive-execution / version-control (VC-3 join) / operational-ledger / practice-analytics (PA-15 gains a money dimension) / artifact-derived-observation / user-model (UM-8) — distinct from change-attribution (statistical regression blame) and value-settlement (outbound payment) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-cost-rating.md](specifications/l1-cost-rating.md) ; v1.1.0 CR-2 amended — the per-class rate list gains **generation that is billed but never delivered**, a class in its own right rather than a variant of `output` (unreadable, not amortized by prefix reuse, governed by a depth dial not a length target, unreported by some providers): pricing it as `output` makes a bill that moved because deliberation deepened indistinguishable from one that moved because answers got longer; unavailable counts make the class **unmeasured** and containing aggregates **declared lower bounds**, the count-side twin of CR-4's fail-visible unknown *rate* | Cost rating & pricing — the owned middle step of the cost pipeline (**meter → rate → charge**) that turns a metered usage record into an honest monetary figure, distinct from metering (unit counts) and charging (budget enforcement): single-producer derivation so its honesty rules live in one place (CR-1); per-class rates never one blended rate — fresh input/output/cache_read/cache_creation/modality units each at their own rate, composing CSC-11 + HO-8 class separation (a cache-read unit bills at a fraction of fresh input) (CR-2); honest rate resolution — exact then family/base with dated-suffix normalization, an inferred rate marked never passed as exact, never a different model's rate substituted (CR-3); **fail-visible unknown rate — an unpriced model is priced `UNKNOWN`, never a silent $0** that under-reports spend and corrupts every aggregate (the cost-side of OI-8), aggregates including unknowns disclosed as lower bounds (CR-4); estimate-vs-provider-reported basis labeling with authoritative supersession, an estimate never dressed as a bill (CR-5); dated/sourced/staleness-flagged rates degrading a stale/default rate to indicative paralleling MB-7 (CR-6); explicit currency with immutable native amount, conversion an explicit dated provenance-marked transform never a hardcoded factor (CR-7); reconstructable/auditable priced record (resolved rate+provenance+basis, per-class counts, per-class+total cost) so mispricing is detectable from the record not the invoice, feeding OH-6 + OI-4 (CR-8); owns the `catalog pricing` MB-3/l2-dashboard/l2-model-router/l2-deep-research assumed without a contract; consumers = l2-budget-engine (enforce) / l1-usage-allowance (windows) / l1-routing (cost signal) / l1-dashboard (display); nodus-relevance needs no new NL invariant — HO-8 meters per-class counts `counts-only`, the host rates them via an LP-2 pricing seam (additive); v1.0.1 links `l1-outcome-attributed-cost` as the consumer closing the loop rating leaves open — rating derives *what was spent*, attribution answers *what it produced and whether it lasted*; CR-5 basis labeling and CR-8 auditable inputs pass through unchanged (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-generative-surface.md](specifications/l1-generative-surface.md) | Generative surface — agent-rendered interactive visual artifacts (charts, forms, document previews, mini-tools) as a turn output, distinct from chat text and the product's fixed screens; generalizes the automation canvas to arbitrary agent-authored surfaces: agent-authored response artifact, never an interface hijack (GS-1); sandboxed rendering of untrusted generated content, no ambient host/secret access (GS-2); closed perception loop — agent perceives the rendered surface + user interactions and updates incrementally (GS-3); user-controlled, dismissable, never a forced modal (GS-4); portable representation that degrades across frontends (GS-5); projection-not-source, edits flow back as explicit changes (GS-6); bounded generation (GS-7); explicit lifecycle + inspectable/recoverable (GS-8); durable scoped surface state via host storage seam — personal/shared with disclosure, quotas + key grammar, concurrency honesty, user inspect/erase, no source-of-truth capture (GS-9); brokered surface inference — host-injected credentials, host-pinned model + budget, stateless exchange, gate/audit parity, typed refusal (GS-10) (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-adjustment-vocabulary.md](specifications/l1-adjustment-vocabulary.md) | Adjustment vocabulary — the client's cheap, named handle on a produced artifact's own dimensions, replacing free-form revision (expensive at both ends, ambiguous in both directions, and reliably answered with a regeneration that discards accumulated agreement). An adjustment **edits** the existing artifact, never regenerates it — the single most expensive failure the concept prevents (ADJ-1); the vocabulary is **closed, named, and shared**, since re-interpretation per turn is free-form revision with ceremony (ADJ-2); every axis is **bidirectional**, because a one-way verb is a ratchet whose only remedy for overshoot is the rewrite ADJ-1 forbids (ADJ-3); one axis per adjustment, or the move is unattributable (ADJ-4); re-applicable and bounded, with the producer **saying so at the limit** rather than moving something else to appear responsive (ADJ-5); reversible along the axis — an "opposite" yielding a third unrelated result is no axis at all (ADJ-6); axes declared **per artifact kind**, since a universal set collapses into *more*/*less* (ADJ-7); an adjustment carries **no verdict** on the previous version, so steering is not read as failure and answered with defence or over-correction (ADJ-8); free-form direction always available with a promotion path into the vocabulary (ADJ-9); the trail recorded as axis+direction+version — replayable, truncatable, and readable as **revealed preference** feeding the user model (ADJ-10). **nodus projection, no new primitive**: an adjustment is a revise step with a declared axis, the human-in-the-loop dialog channel carries it, the memoizable-decision path turns a repeated steer into a standing preference, and per-step trace annotations hold the trail. Distinct from iterative-refinement (automated grader loop, frozen rubric) and from directability (steers office substrate, not artifact dimensions); composes generation-shaping / design-identity (craft floor) / conversational-control / pattern-codification / user-model / conversation-rewind (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-negative-specification.md](specifications/l1-negative-specification.md) ; v1.1.0 NEG-11 added - the **positive target is the primary form** an exclusion takes when it reaches a generator: steering by prohibition places the excluded behaviour into the generator's context where it becomes *more* available, the negation being a weak modifier over a strongly activated concept, so NEG-5's name-the-alternative is hardened into an ordering (render the exclusion as its positive target, prohibition as justification) with the bare ban reserved for hard guardrails no positive form can carry; the checker side NEG-9 is explicitly unaffected, since detection and steering have opposite requirements and only steering pays the availability cost | Negative specification — the named shared shape behind four locally-invented instances (a corrected assumption promoted to an anti-pattern, generic-default tells blocked at a craft bar, a forbidden construct under a source tripwire, a banned register in generated prose): a declared, provenanced, bounded statement of what an output must **not** be. An exclusion is a first-class artifact with identity, rationale, origin, and an affirmation date — not a footnote or a conversation (NEG-1); **positive and negative are independent and neither implies the other**, so a system modelling only the target passes every check while producing exactly what the principal asked to avoid (NEG-2); three origins with different conflict authority — principal-declared / derived / built-in (NEG-3); the exclusion **reaches the generator before production**, review enforcement being the second line and never the only one, since a negative caught after the fact costs a rewrite while the same constraint in context costs tokens (NEG-4); the **alternative is named** wherever one exists, because a bare prohibition moves the generator to the next-most-default option (NEG-5); bounded, dated, revisited — an unbounded avoid-list is unsatisfiable and then ignored wholesale (NEG-6); an explicit principal request **outranks** an exclusion with the conflict surfaced once, an exclusion being no veto over its author (NEG-7); a violation names the matched signal and its origin, not an opinion in a finding's clothes (NEG-8); the checkable subset checked and the taste remainder named advisory, boundary stated (NEG-9); firing rate measured, where never-fires and always-fires are both actionable, the latter indicting the *positive* spec (NEG-10). **nodus projection, no new primitive**: the validated `§config` surface carries the set, the validation stage hosts the checkable subset, cache-stable composition places it in the byte-stable prefix, and an externally-sourced avoid-list carries provenance like any other content. Composes specialty-exemplars (the positive counterpart) / design-identity (DI-5/DI-6/DI-7, the worked instance) / invariant-tripwires (the enforcement mechanism for the checkable subset) / intent-resolution (IR-7 derived origin) / pattern-codification / content-language / user-model / cache-stable-context (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-design-identity.md](specifications/l1-design-identity.md) | Design identity — the office's swappable visual language + craft bar for user-facing visual output (its own UI and agent-generated surfaces): a design system as a named, versioned, schema-validated DATA package (declared tokens color/type/spacing/radius/motion + optional component fixtures + prose guidance + preview), discoverable by manifest, never hardcoded (DI-1); layered catalog built-in<project<personal with id-stable override, exactly one active per rendering scope, switching instant + cosmetic-only never behavioral (DI-2); token contract as the single visual source of truth — surfaces consume tokens, hardcoded visual values are a craft defect (DI-3); provenance-tagged (source kind bundled/local/repository/registry + reference) and fidelity-declared import mode verbatim/normalized/hybrid, integrity-witness-verified before activation + version-pinned reproducible resolution (DI-4, composes l1-attestation/l1-extension-marketplace); tiered craft-conformance bar must-fix(blocking, mechanically checked where expressible)/should-fix/nice-to-fix with the auto-enforced vs advisory boundary NAMED not blurred, identity declares applies/suggested/exempt (DI-5); distinctiveness-over-default — blocks a named set of generic default tells (default accent, default display font, templated filler, meaningless decoration, invented figures) and expects an intentional identity-specific signature, ~proven-majority + deliberate-distinctive-minority (DI-6); craft rules themselves a layered extensible data catalog, per-project/user re-tunable (DI-7); uniform across office UI + generated surfaces, portable/degradable across frontends, resolving the app-ui custom-theme question — user/imported identities are the catalog's user layer (DI-8, composes l1-generative-surface GS-5/INV-3); local-first, egress-gated import, secret-safe, presentation-asset-never-source-of-truth (DI-9); complements l1-quality-standards (code gates) + l1-output-contracts (structural bounds) with the aesthetic/craft dimension neither owns; ideas-to-adopt mapping (design-system-as-validated-data catalog + anti-default craft rubric) + nodus disposition Reuse behind LP-4 schema-provider / LP-13 import seams, no new nodus invariant (tech-agnostic); v1.0.1 links `l1-negative-specification` (DI-6 default tells = the built-in negative layer, a project avoid-list = DI-7's user layer, DI-5 auto-vs-advisory generalized as NEG-9) and `l1-adjustment-vocabulary` (client steering within the acceptable region, craft bar as the floor) ; v1.0.2 links `l2-design-system` (the L2 realization of DI-1/DI-3) and clarifies §4.2 that the host UI realizes the catalog as a **colour-scheme** axis orthogonal to an OS-appearance **mode** axis — clarification + link only, no invariant changed | Stable | 1 | 1.0.2 |
| [l2-design-system.md](specifications/l2-design-system.md) | Frontend token contract (React 19 + Vite + Tailwind CSS v4) — the Layer 2 realization of design-identity DI-1/DI-3: a canonical token taxonomy (colour surface/text/line/accent/semantic roles, typography, spacing, radius, motion, elevation) every visual attribute derives from (§4.1); the two-axis resolver composing an OS-appearance **mode** (system/light/dark) with a named **colour scheme** into the active token set, applied on the surface root as `data-theme`/`data-scheme` + custom properties (§4.2); the Tailwind v4 `@theme` binding exposing tokens as utilities + CSS custom properties, no literal hex/font/radius in components (§4.3); the colour-scheme package shape — `manifest.json` (schema-validated: id, name, category, provenance kind bundled/local/repository/registry, fidelity verbatim/normalized/hybrid, file map) + `tokens.light.css` + `tokens.dark.css` + optional guidance/preview — making a scheme a dropped-in data package not code (§4.4); layered catalog built-in<project<personal with id-stable override + `app.json` `theme`+`colorScheme` persistence via serde-default load_or_create (§4.5); tiered craft lint with a mechanically-enforced must-fix subset (no un-tokenized colour/font/radius, no default-accent tell) blocking the build and advisory rules surfaced, tiers+auto/advisory flag as layered config not engine code (§4.6); presentation-only, cosmetic-only switching, on-device-first, import path additive/deferred; maps DI-1…DI-9; `Implements: l1-design-identity.md` | Stable | 2 | 1.0.0 |
| [l1-loop-governance.md](specifications/l1-loop-governance.md) ; v1.2.0 links `l1-acceptance-oracle` - the integrity complement to LG-4 oracle **ownership**: LG closed criteria drift and put the oracle outside the mutable set but assumed it meaningful; AO covers the independent failure where a properly owned, correctly frozen oracle is one that **cannot fail**, under which an execution loop terminates on its first iteration forever and an evolution loop optimizes against a constant | Loop governance keystone — classifies and constrains every autonomous loop above harness-engineering/dynamic-harness/agent-framework: declared loop class execution-vs-evolution with composition (LG-1); mutation-rights manifest over a closed artifact taxonomy, undeclared=immutable (LG-2); criteria-immutability anti-drift spine — an actor never edits the contract that judges it (LG-3); oracle ownership — right to declare done separated from the actor (deterministic/independent-judge/human), same-lineage = recorded reduced-confidence, generalizing DH-11 to termination authority (LG-4); state externalization reconstruct-not-inherit (LG-5); independent termination ceiling separate from actor and oracle (LG-6); tier-escalation gate for self-evolution/criteria change requiring separated oracle + external novelty + held-out improvement (LG-7); append-only auditable mutation ledger with machine-checkable predicted flips for evolution loops (LG-8); cheapest-trustworthy-oracle preference (LG-9); five-tier mutation ladder (same-prompt/externally-verified/artifact-evolving/+separated-verifier/self-evolving) with Tier-3 default; six-question loop design checklist; LG-10 objective persistence across in-session reduction (§4.4 two-realizations table) — the persistent-session counterpart to LG-5 fresh-context-per-iteration: a continuous-session loop that compacts in place MUST re-project the standing objective + progress into every turn from the durable slot so mid-session compaction/eviction never drops the north-star (compaction-immune presence via CC-9 protected + re-projected-if-evicted), and the objective must be idempotent/resumable so a post-reduction turn resumes not restarts (durable progress captured before the lossy reduction CC-10; ledger outranks the compacted transcript after DW-5); satisfied-by-construction in nodus (no in-place compaction, @ctx/EG-11 immutable invocation context always present); ideas-to-adopt + nodus-relevance mappings (tech-agnostic) ; v1.1.0 LG-11 — **scope-restart authority is the scope's own boundary**: a loop whose *body is a whole scope re-run from the top* (the entire workflow restarted from its first stage, re-reading its own inputs and configuration, rather than a step re-attempted with carried state) is a legitimate class for self-correction and self-reconfiguration, governed like any loop — declared class (LG-1), a **counted, visible** restart ceiling so it is bounded not unbounded self-restart (LG-6), externalized state so each restart reconstructs from durable artifacts not the prior run context (LG-5), append-only restart ledger (LG-8); the addition is **who may request it** — the authority to restart a scope belongs only to that scope's own boundary (its run-level open/close), **never to a nested per-item/branch/child context inside it**, because a restart from inside one item's context is undefined about which context resumes and what in-flight siblings survive, so it is refused at the nested site and legal only at the boundary that owns the scope; re-reads inputs/config (composing l2-config-hotreload), the safe alternative to the mid-iteration self-config-mutation LG-3 forbids; nodus realization = NL-23 | Stable | 1 | 1.2.0 |
| [l2-loop-runner.md](specifications/l2-loop-runner.md) | Loop runner + governor mechanic (I/O-free logic in crates/domain, adapter-touching composition wired in the crates/core facade, composing execution-workspace/budget-engine/agent-autonomy/model-router/session-checkpoint/version-control + nodus provider seams): LoopSpec/MutationManifest/Oracle/Ceiling types with MutableArtifact lacking a Criteria variant (LG-3 enforced by the type system); execution-loop runner (isolated worktree → fresh-context turn → governor write-guard → oracle verdict → finalize or VC-4 rollback); evolution-loop runner wrapping harness EVALUATE→ANALYZE→IMPROVE with nested execution scoring; single-point Governor (check_ceiling/guard_writes/judge); oracle wiring (deterministic output-contract validators / model-router distinct-lineage judge / autonomy approval-gate human, reduced_confidence on lineage match); escalation path for criteria change & self-evolution; LG-10 objective persistence via an ObjectiveSlot re-projected every turn for continuous-session loops; loop CLI/TUI/library surface; crate placement | Stable | 2 | 1.0.0 |
| [l1-change-merge.md](specifications/l1-change-merge.md) | Concurrent change merge — lets parallel workers/sessions modify the same structured artifact (spec/plan/workflow/knowledge) without silent loss, the reviewable-delta counterpart to notes' CRDT prose merge: change-as-typed-delta added/modified/removed/renamed against a known base, never full-document replace (CM-1); sub-unit granularity — scenario/row/step, so disjoint edits never collide (CM-2); base fingerprinting persisted per addressed unit (CM-3); divergence-blocked integration — stale base blocks, silent replace prohibited (CM-4); deterministic three-way merge + rebase with conflict markers (CM-5); no silent loss + disjoint edits commute regardless of integration order (CM-6); dependency-ordered concurrent proposals reusing existing change metadata, pre-work overlap warnings (CM-7); atomic integrate-then-archive, changes ephemeral (CM-8); blockers (cycle/missing-predecessor/stale-base/malformed) vs advisory warnings (CM-9); ideas-to-adopt + nodus-relevance mapping (delta-granularity↔DSL steps/macros, fingerprint+3-way-merge as DSL tooling, integration-as-validator-gate) (tech-agnostic) ; v1.1.0 CM-10 — **self-mis-anchoring is repaired, not merged**: a stale-base divergence (CM-4) has two distinct causes needing different remedies — **concurrent divergence** (another in-flight change moved the live unit — reconciled by the CM-5 three-way merge, a genuine competing edit exists) versus **self-mis-anchoring** (the producer's delta was authored against a stale or mis-remembered base with no competing change — nothing to three-way-merge, the delta is simply wrong about the unit's content); the latter is repaired by a **bounded automated re-anchoring** — the producing actor is handed the *precise* mismatch (which unit, recorded base, actual current content) and re-emits *just the failed delta* re-anchored against truth under a declared retry budget (composing output-contracts OC-4/OC-5), the terminal stale-base blocker (CM-9) deferred until the budget exhausts not raised on first failure; three rules — precise-diagnostic targeted repair not regenerate-from-scratch (the intent was sound, only its anchor stale), producer-side re-anchoring never a silent overwrite (the CM-4 prohibition holds), exhaustion surfaces the precise failure atomically (CM-8/CM-9, a partially-anchored edit set never committed); delta *encoding* bears on how often mis-anchoring occurs (whole-unit replace has no anchor to miss, a diff the most), an implementation MAY offer interchangeable encodings trading verbosity for apply-robustness with the repair contract independent of which | Stable | 1 | 1.1.0 |
| [l1-human-intervention.md](specifications/l1-human-intervention.md) ; v1.1.0 HI-7 — **declared ownership regions**: a genuinely co-owned artifact (shared doc, issue body, plan file with human prose) MAY declare delimited owner-tagged regions via durable **human-visible** markers, so a machine-owned region is rewritten wholesale without touching surrounding human text and an edit confined to one owner's region is not a collision at all; **partition first, reconcile second** (HI-4 unweakened, still handling everything the partition does not resolve), the machine-owned region **regenerable and holding nothing else** (DAH-1) with a human edit *inside* it still a detected authoritative intervention rather than an ownership-justified overwrite, and **missing/malformed/nested/unterminated delimiters fail safe** — no rewrite, fall back to full reconciliation, since a boundary inferred from a damaged marker is how a whole-file clobber happens under an ownership rule | Human intervention & reconciliation — the human stepping **directly into** autonomous work by hand-editing a work artifact (task/card/plan/spec/document) **out-of-band**, bypassing the agent delta pipeline (distinct from ORC-12/ACP-10 steer-the-live-turn and SEC-10 authority-plane): out-of-band hand-edit as a first-class always-available action (HI-1); **baseline-comparison drift detection by version/timestamp/content-fingerprint** — the agent records a baseline when it reads/claims an artifact, and any drift is detected **before** the agent writes back its stale result, never assumed away, any-of-three-signals-differ = drift while all-match = no-ceremony common case (HI-2); human authorship attributed + the human edit treated as **authoritative intent**, never silently reverted/clobbered by stale agent work (SEC-10 applied to the work itself, authority-asymmetric reconciliation: human wins) (HI-3); **reconcile-never-clobber** — disjoint edits 3-way-merge, genuine overlaps surfaced as conflicts with the human side preserved, no agent write against a human-moved baseline without reconciling (composes change-merge CM-4/CM-5) (HI-4); dependents **re-validated** via task-graph re-plan + invalidated in-flight work **paused/re-based** via work-liveness rather than run to a now-wrong result, incremental not blanket-restart (HI-5); intervention observable/attributed/auditable, human-authored vs agent-authored state distinguishable (HI-6); owns the human-intervention lifecycle change-merge doesn't model (no-delta hand-edit, authority asymmetry, re-plan/pause) and **drives** change-merge for the merge step; nodus-relevance needs no new NL invariant — host-recorded baseline + change-merge DSL tooling candidate + re-run-invalidated-subgraph + AuditProvider (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-requirement-checklists.md](specifications/l1-requirement-checklists.md) ; v1.0.1 links `l1-acceptance-oracle` - same falsifiability instinct, different subject: RQ audits the *statement of need*, AO audits the *instrument that measures it*; a perfectly specified requirement can still be gated by a check incapable of failing | Requirement-quality checklists — "unit tests for the spec": generated, domain-tailored, falsifiable questions validating requirements quality before any code, the static requirements-side counterpart to behavioral eval and the generated complement to automated lint: tests requirements not implementation as the defining boundary (RQ-1); generated+domain-tailored from spec signals, not a fixed form (RQ-2); falsifiable yes/no items tagged by quality dimension completeness/clarity/consistency/measurability/coverage/edge-case/NFR/dependency/ambiguity (RQ-3); each item section-anchored or [Gap]-marked (RQ-4); stable-id append-only (RQ-5); focused, context-bounded, multiple-per-spec (RQ-6); pre-planning quality gate blocking advance on unresolved gaps (RQ-7); scaffold-then-maintain authoring (RQ-8); placement in the quality family (lint≠behavioral-eval≠requirement-checklist); ideas-to-adopt + nodus-relevance mapping (checklist over workflow defs — error-path/loop-bound/typed-IO/branch-exhaustiveness coverage) (tech-agnostic) | Stable | 1 | 1.0.1 |
| [l1-model-benchmarking.md](specifications/l1-model-benchmarking.md) ; v1.1.0 MB-10 — **retry policy is part of the measured unit, and it is asymmetric**: **availability** retries (rate-limit/throttle/unavailable) measure the provider's queue on the day, not the candidate, so they MAY be relaxed generously; **output-validity** retries **are the measured property** and MUST stay at the production value across candidates, since a candidate that produces valid output on the third attempt is not a candidate that produces valid output and a suite retrying until it passes publishes a first-attempt compliance rate no user will ever see; attempts counted and reported in both classes (a hidden retry count makes a score unreproducible, and the count is itself a result), configuration joins MB-7 provenance and MB-5 comparability so different output-retry allowances MUST NOT be pooled | Model benchmarking — base-model fitness measurement per task class via a small hardcoded probe set, the missing member of the eval family (evaluations = subjective human feedback, evaluation-suites = customization not base model, agent-coevaluation = (model,harness) pairs, retrieval-evaluation = ranked recall): version-controlled human-authored probes with stable identity, never model-generated, comparable only within one probe-set version (MB-1); closed three-class taxonomy code / content / instruction-compliance mapping 1:1 onto router task classes, extension = versioned amendment (MB-2); three-dimensional scorecard quality + time (incl. time-to-first-output) + tokens/derived-cost, reported separately never collapsed into one opaque number (MB-3, RE-3 discipline); deterministic-first grading — mechanical checks primary, LLM judge complementary under a hybrid floor (may lower, never rescue), never self-grading, graders compose the ES-3 taxonomy not a parallel engine (MB-4); unit under test = base model (identity+lane+quantization) through a fixed minimal versioned harness identical across candidates so deltas attribute to the model (MB-5, ACE-5 comparability); per-(model,class) fitness profile consumed by the model router as one RTG-1 signal, never a policy override / catalog rewrite, absent profile = neutral default never fabricated (MB-6, EVL-5 pattern); full provenance + baseline deltas + explicit staleness fresh/indicative/stale with re-measurement flagging (MB-7); budget-capped isolated off-hot-path execution, metered lanes only under explicit budget or user action — never a silent background cost (MB-8); honest failure accounting — timeout/refusal/malformed/provider-error typed and counted against quality, declared trial count with variance disclosure, grader/harness faults invalidate the run never scored against the model (MB-9); calibration source for RTG-9 auxiliary-role economical bindings; contamination mitigated by probe-set versioning (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-specialty-exemplars.md](specifications/l1-specialty-exemplars.md) | Specialty exemplars — per-specialty competency instrument, sibling to model-benchmarking (task-class fitness for the router) pointed at a different question, staffing competency: one small concentrated suite per office specialty organized by the role catalog not the router's abstract classes (SE-1/ROL-1), each probe shipping a human-authored convention-perfect gold-standard reference exemplar as the grading anchor, never model-authored/edited, never self-graded (SE-2); two-axis scoring correctness (does it work) and craftsmanship (convention/principle/idiom adherence vs the exemplar) reported separately, never fused into one opaque number so right-but-sloppy and elegant-but-wrong stay distinguishable (SE-3); concentrated budget-bounded probes, one-to-a-few per specialty sometimes a single one, breadth traded for cost (SE-4); deterministic-first exemplar-anchored grading — mechanical correctness spine + convention rules, LLM judge complementary anchored to the exemplar under a hybrid floor may-lower-craft never-rescue-correctness, composing the ES-3 grader taxonomy not a parallel engine (SE-5); fixed whole-set-versioned exemplar set, cross-version comparison forbidden, contamination-hygiene via versioned refresh (SE-6); craft standard drawn from the office quality standards QLY-2/QLY-4 not a separate rubric — an exemplar would pass the specialty's own gates (SE-7); per-(model,specialty) competency profile consumed by staffing as one signal weighed with cost/availability/privacy, never a policy override / availability override / catalog rewrite, absent profile = neutral default never fabricated (SE-8); budget-capped isolated off-the-staffing-hot-path, metered lanes only under explicit budget or user action (SE-9); full provenance + baseline deltas + explicit staleness fresh/indicative/stale with re-measurement flag (SE-10); honest failure accounting typed timeout/refusal/malformed/provider-error counted against the run, declared trials + variance, correctness-vs-craft never conflated, grader/exemplar/harness fault invalidates the run never scored against the model (SE-11); demarcated from model-benchmarking/evaluation-suites/agent-coevaluation/evaluations in §4.6 (tech-agnostic); v1.0.1 links `l1-negative-specification` as the negative counterpart — distance from a gold-standard exemplar is silent about proximity to a declared anti-reference (NEG-2), so a candidate can score well and still land on an excluded outcome | Stable | 1 | 1.0.1 |
| [l1-crash-recovery.md](specifications/l1-crash-recovery.md) | Crash recovery & state snapshots — the mechanism layer behind the resume-after-crash promises (STO-2 durability, HEAL-6 self-recovery, WL-5 reconciliation, ORC-10 resumable plans declare the outcome; this owns the how): unclean-shutdown detection via durable run marker, every startup classifies previous termination and routes unclean through recovery before normal operation, never silent (CR-1); automatic consistent point-in-time snapshots — single-instant capture (technique = L2 choice), self-sufficient restore points verified at capture with integrity checksum, failed verification = discarded + flagged never counted (CR-2); crash-safe routine writes atomic-or-journaled so live state is the FIRST recovery source, snapshots the second line never a license for torn writes (CR-3); strict recovery ladder live → newest verified snapshot → older → escalate (HEAL-3), silent-empty start forbidden while recovery material exists, damaged state quarantined never deleted (CR-4); bounded declared loss window — cadence + event triggers (work-unit completion, pre-migration STO-9 as an event-instance of this same mechanism, pre-risky-repair) + manual, policy-configurable (CR-5); work resumption not data reboot — restored state passes through WL-5 reap/resume/reconcile + WL-3 next-move + ORC-10 reload so every unit continues/re-queues/surfaces-stalled, never running-limbo (CR-6); honest recovery accounting — durable report: source+age, restored, lost window, unrecovered named never dropped, lossy never presented as lossless (CR-7); secrets never roll back — snapshots exclude the secret store (STO-6/SEC-1), revoked/rotated credentials not resurrected by rollback, secret store protected by CR-3 alone (CR-8); recovery itself crash-safe — non-destructive, idempotent re-entry, logged (HEAL-5), bounded escalation, quarantine under CR-5 retention (CR-9); demarcated from l2-backup (archival restore-anywhere) / l2-session-checkpoint (LLM-turn resume) / STO-9 (per-artifact migration backup); snapshot command group with CLI/TUI/GUI parity, explicit restore = destructive-class confirmation-gated (tech-agnostic) ; v1.1.0 CR-10 a held resource is reclaimed only from a **provably dead** holder — identity absent, or a reuse-resistant identity fingerprint no longer matching; an unresponsive/suspended/undeterminable holder is never broken (breaking a live holder is unrecoverable, waiting is merely slow), and a bare private token cannot supply the evidence; the hold is published atomically **and fully populated** so an unreadable hold record is genuine corruption rather than a race, and release is owner-checked | Stable | 1 | 1.1.0 |
| [l1-conversation-rewind.md](specifications/l1-conversation-rewind.md) | Conversation rewind — user goes **back** to an earlier chat message and **reuses** it (verbatim or edited), continuing from that point; in a side-effecting office this is reconcile-and-surface, **not** transcript truncation: message-anchored rewind points capturing conversation + derived office state per user-turn boundary (RW-1); reuse restores the *starting state* and the office re-derives — a re-run MAY differ, never a guaranteed replay (RW-2); **fork-not-destroy** — the abandoned continuation is preserved as an inspectable branch so rewind is itself reversible, history is a tree (RW-3); reversible derived state (context/cards/plans/memory) restored atomically-or-reported, rewound board work reconciled via the WL-5 liveness sweep onto the one board never silently deleted (RW-4); **irreversible/externally-observable effects (sent email, connector-created record, pushed commit, spent budget) surfaced honestly, never pretended-away or falsely rolled back** — reconcile what it can, name what escaped (RW-5, composes CR-7 honesty + DIR-9 honest-control + effect-sink boundary); safe-quiesce the current atomic step before restore, never tear mid-step (RW-6, OC-1); legible/attributed/auditable + undoable (RW-7); office-isolation + access-scope bounded (RW-8); user-initiated & optional, never an autonomous office behavior — course-correction inside a run is re-planning, a distinct concept (RW-9); reuses crash-recovery restore/reconcile/honesty machinery under a user-selected, message-anchored trigger; composes directability/office-control/work-liveness/work-convergence/version-control; main-only host mechanic (tech-agnostic) ; v1.1.0 RW-5 amended — surfacing the irreversible is the honest **floor** not the ceiling: where a surfaced effect has a **declared compensating action** (l1-compensation CO-2) the rewind MAY **offer to compensate** it (execute the business inverse) rather than only naming and orphaning it, and where compensation is unavailable or fails RW-5 honest surfacing still holds with a live un-compensated effect never reported cleanly rewound (CO-10) [v1.2.0] RW-1 amended — rewind points anchor to **archived ranges** (EA-1/EA-2), so depth is bounded by the archive horizon, never by an automatic compaction; a target beyond the horizon is refused with the horizon named. Closes RW-3's one-sidedness (deliberate rewind preserved history while automatic reduction destroyed it). | Stable | 1 | 1.2.0 |
| [l1-order-independent-production.md](specifications/l1-order-independent-production.md) | Order-independent production — the property that makes splitting a multi-unit artifact **safe**, assumed by the staffing and decomposition specs and never stated: the unit at position *i* is a **pure function of *i* and a frozen input set, and of nothing else** (OIP-1), from which arbitrary concurrency, resumption, partial repair, and machine-independent results all follow. Four named prohibitions inside a producer: production order / neighbour residue; **wall-clock** (position is the clock); **unseeded randomness** — deliberate variation draws from a **position-derived seed**, stable per unit and still different across units; and live sources not pinned into the frozen set (OIP-2). One frozen input identity shared identically, since a differing parameter yields a *different artifact* assembled from parts of two (OIP-3); every producer receives its **global offset**, not merely a local extent, or it produces wrong units and reports success — invisible until assembly (OIP-4); uniform partitions with the remainder last, a **production** constraint because partition size decides where seams land (OIP-5); a declared, ordered, **seam-safe** join, seam-safety being a property of how parts were produced and unrecoverable at assembly (OIP-6). **The characteristic failure is cross-unit inconsistency, not a crash** — each unit individually valid, so a per-unit gate passes a thoroughly broken artifact and detection must be cross-unit: boundary continuity, monotonicity of running quantities, shared-fact agreement, and re-production equality (the cheapest, catching all four prohibitions) (OIP-7). **Reducing concurrency is a diagnosis, never a remedy** — serial production supplies the very ordering the units illegitimately relied on, so shipping width=1 leaves the defect, forfeits the capabilities, and stays machine-dependent while looking correct on the machine that made it (OIP-8). Asynchronous readiness is a **named, bounded** hold on one unit — the name making a timeout attributable to the work that stalled, the bound preventing a parallel run from silently becoming a hang (OIP-9); any subset independently re-producible, the basis for partial repair and resumption (OIP-10). **nodus projection, no new primitive**: a positional loop is the carrier (its loop variable *is* the position), the deferred-step mechanism is the named hold, and three of the four prohibitions are statically flaggable at the validate-before-run stage. Distinct from competitive-execution (alternatives where one survives) and change-merge (overlapping edits needing conflict resolution); composes parallel-staffing / incremental-execution (IE-4) / recursive-decomposition (RD-7) / execution-graph (EG-12) / reproduction-recipe (RR-8) / invariant-tripwires (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-parallel-staffing.md](specifications/l1-parallel-staffing.md) ; v1.1.0 PS-10 added - an ephemeral instance's **execution tier** (model class, reasoning depth, effort) is assigned from what **its own partition** demands and stated alongside the width, **never inherited from the coordinator**: inheritance is the default fan-out makes expensive, since the coordinator's tier is set for the hardest thing *it* does, fan-out multiplies that per-unit rate by the width, and the widest fans are characteristically the **shallowest** work (gathering, scanning, summarizing) where the coordinator's tier buys nothing; symmetric and neither direction a saving - a partition genuinely needing the higher tier gets it, and flattening the whole fan to the cheapest tier is a quality decision disguised as an economy - what is forbidden is the tier following **where an instance was spawned from** rather than what it was spawned to do; demarcated against CE-10 proxy-fidelity selection, which belongs to the contest ; v1.0.2 links `l1-fanout-attestation` - the orthogonal contract over the **launch act**: PS decides what is split and that every partition is integrated, but is silent on whether the partitions were actually launched together, and a coordinator that launches one instance, waits, then launches the next produces an identical record; PS-2 disjointness is the ownership precondition of a launch episode | Parallel staffing — same-specialty scale-out: a large task MAY be worked by several parallel workers of ONE specialty (subagent instances of one role), the horizontal complement to ORC-2 vertical topology growth and l1-deliberation perspective fan-out: volume-justified recorded scale-out decision, one worker is the default never silent width (PS-1); parallelism ONLY through disjoint decomposition — never two workers on one unit, task-graph partitions claimed exclusively (WL-1) dispatched exactly-once (WL-9), non-decomposable serial residue stays serial — no promised speed-up it cannot deliver (PS-2); ephemeral same-role-definition instances under ONE accountable hired lead — task-scoped capacity never permanent hires as a side effect (ROL-5/ROL-9), accountability never leaves the lead (PS-3); bounded width — config max + per-instance budget within run budget (ORC-7) + coordination-cost guard, overflow partitions queue, width decisions only at lead/orchestrator level (no nested instance-initiated scale-out) (PS-4); isolated siblings, mediated observable coordination (ORC-5/ORC-8/ORC-12), shared artifacts via change-merge never blind co-editing (PS-5); first-class fan-in — lead integrates, reviewable merge (CM), completion = integrated whole passes quality gates not last-partition-done (PS-6); learning consolidates to the single hired employee's memory scope attributed per instance — no knowledge fragmentation across throwaway identities (PS-7); failure containment per delegation boundary (ORC-11), stranded partition reaped-then-reassigned (WL-5-consistent), partial output salvaged only via merge (PS-8); observable attributed width — decision, instances, partitions, per-instance cost in the live projection and record (PS-9); composes l2-agent-session subagent spawn/budgets (tech-agnostic); v1.0.1 links `l1-order-independent-production` — the production-side property PS-2/PS-6 assume without stating: isolation stops siblings interfering but not depending on *when* they ran, and a perfectly staffed set of isolated workers still yields an incoherent artifact if each reads the clock | Stable | 1 | 1.1.0 |
| [l1-employee-availability.md](specifications/l1-employee-availability.md) | Employee availability states — the workforce-level state model between ROL-3 hire and ROL-4 fire, the office metaphor's honest rendering of runtime facts: closed exclusive taxonomy available / working / resting (short resource-driven day-off) / on-leave (planned vacation) / truant (detected anomaly), released = terminal lifecycle exit with re-hire → available, extension = versioned amendment (EMP-1); office state dominates — Paused/Hibernating suspends everyone uniformly, employee states are staffing/resource facts within a running office never a worker-side pause override (EMP-2, OC); truancy detected never declared — assigned + silent + no declared cause via the WL-8 watchdog, no setter command exists, surfaces + opens WL-6/doctor recovery, reclassifies on evidence, never resolves silently (EMP-3); rest is resource-honest with automatic return — declared cause only (model quota/rate/cost/backoff after OC-3 substitution attempted), per-employee OC-4 analog, the employee-level pressure valve preceding office hibernation (EMP-4); leave is planned manager-owned (ROL-5) with drain-or-hand-off first — no unit stays owned by a vacationer (WL-3 preserved), auto-return at window end or recall (EMP-5); no delegation to the unavailable — orchestrator substitutes/staffs (OFF-4/WSL-6) or queues with visible reason; delegation only — status queries and conversation remain served (EMP-6); visible evented causal — state+cause+expected return in the live projection, event before transition completes, auditable history (EMP-7, OC-5 discipline); availability never destroys knowledge — memory/skills/hierarchy untouched, only release archives and non-destructively (EMP-8); honest metaphor binding — mechanical truth always exposed alongside the metaphor, a lens never a curtain (EMP-9) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-project-priority.md](specifications/l1-project-priority.md) | Project priority — cross-office resource-arbitration signal: an explicit user/board-set ordered priority per office/project governing how a finite resource shared across offices (a building-level token/budget pool, or an external provider rate/quota all offices call through) is split when it becomes scarce and more than one office contends — the economic layer neither budget-engine (per-scope ceilings, hard-stop, no redistribution by design) nor global-orchestration (coordination/routing, sees budget but never ranks offices) supplies: explicit ordered priority per office, declared never inferred, the single arbitration key (PP-1); inert under abundance — every active office runs unconstrained regardless of rank, active ONLY under contention (PP-2); scarcity an explicitly detected evented condition — shared pool nearing cap / provider rate-limit / global ceiling pressure, never a guess, clears when the resource recovers (PP-3); priority-ordered/weighted allocation of the scarce remainder, declared strict-vs-weighted policy never a hidden heuristic, higher rank never served worse (PP-4); fairness floor — bounded starvation via floor/aging, low-priority slowed not silently zeroed, hard-preempt only as explicit opt-in surfaced with cause (PP-5); graceful yield — degrade NEW grants first, running units finish or checkpoint (CR-5), never a violent mid-op kill (GO-2), priority change affects future allocation only never retroactive clawback (PP-6); single building-level arbitration point at the global orchestrator that alone sees all offices' budgets/states (GO-1/GO-4), offices never negotiate the pool among themselves, no global orchestrator = no arbitration (PP-7); only actively-drawing offices contend — Paused/Hibernating/Idle consume no share (PP-8); an allocation signal strictly within budget/rate/safety/privacy ceilings, never a limit override or privilege-escalation path (PP-9); visible evented auditable arbitration — contenders, ranks, scarcity trigger, grants/deferrals recorded so a user sees WHY a project slowed, honest metaphor a lens never a curtain (PP-10); composes with l2-budget-engine ceilings, demarcated from budget-engine/global-orchestration/scheduler-model/office-control in §4.6 (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-project-support.md](specifications/l1-project-support.md) | Project support — the office's operational posture for a delivered/live project, the ongoing upkeep that begins where the build ends and neither development-workflow (per-change build pipeline) nor issue-reporting (user telling Cronus's makers about Cronus itself) models: a distinct declared build/support/mixed posture entered after first delivery, per project and per component, declared never inferred (SUP-1); reactive multi-source intake — inbound issues/bug reports, content-staleness signals, user/client requests, dependency & security upkeep, office-surfaced improvements — each captured as a work item with source recorded, never lost (SUP-2); triage-before-work by category/severity/priority reusing the office's existing inbound triage, no parallel intake (SUP-3); continuous with NO terminal done, right-sized to a steady maintenance load by adaptive staffing WSL-6 (SUP-4); every support change through the SAME mandatory quality gates as build work, regression the defining risk, no small-change bypass (SUP-5); closed typed category taxonomy corrective/content/perfective/preventive-adaptive each with its own priority driver (SUP-6); severity-driven expedite/hotfix path that changes urgency only never correctness discipline — still passes gates and authority boundaries (SUP-7); end-to-end issue→change→verification→delivery traceability, resolved/deferred/rejected explicitly never silently closed (SUP-8); same human-in-the-loop authority/budget/security/egress boundaries as any work (SUP-9); honest observable status — open items by category/severity, ages, deferral reasons surfaced not buried, a lens never a curtain (SUP-10); each item flows through the existing development pipeline + gates, support supplies the reactive front and honest back; demarcated from development-workflow/issue-reporting/workspace-lifecycle/kanban-model/project-priority in §4.5 (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-competitive-execution.md](specifications/l1-competitive-execution.md) ; v1.2.0 CE-11 added - **design-space** contests: N independent samples of one brief converge, so CE-7's budget buys near-duplicates, and such a contest **assigns each attempt a different declared constraint** (minimize the interface, maximize extensibility, optimize the common caller, design around the seam) so variants are *made* to differ, comparing on **axes declared before the attempts start**; the second half is a **boundary rather than an exception** - a design exploration's output is a *decision* not an artifact to integrate, so CE-4/CE-5 stand unchanged for artifact tournaments and a design contest hands its candidates to the **deliberation** protocol, where a reasoned blend is legitimate and recorded with named provenance | Best-of-N quality-selection fan-out — the third coordination-family mode beside parallel-staffing (throughput/integrate-all) and deliberation (synthesis/blend-all): when a work unit's outcome quality is genuinely uncertain and budget allows, the office runs N independent **complete** attempts at the **same whole unit**, each in an isolated context / scratch worktree (CE-2/CE-3, deliberately inverting parallel-staffing PS-2's disjoint-partition rule — made safe because attempts never co-write mainline and only one survives), an **independent judge** selects exactly one whole winner by a declared criterion (gate-objective / scored / judged, CE-4 — not a vote, not a blend), the **losing attempts are discarded never merged** (CE-5 — salvaging a loser's idea is a separate recorded act), and the winner is **integrated + gated as one whole** (CE-6 — a judge pick is a ranking, not completion); uncertainty-justified & recorded (CE-1), bounded width & honest afford / no nested tournaments (CE-7), contained failure & honest all-fail (CE-8), observable & attributed contest incl. the discard (CE-9); composes ORC-5/ORC-6/ORC-7/ORC-11/ORC-12 + CM + quality-gates + WL claim/reap; nodus realization = l1-nodus-language NL-13 competitive `~PARALLEL` `+select`/`~PICK`; v1.1.0 CE-10 **proxy-fidelity selection** — the N attempts MAY run at a reduced, *declared* fidelity used only to select, the winner re-produced at full fidelity before entering mainline (CE-6), changing the tournament's economics from N full attempts to N cheap plus one full and making width affordable where CE-7's budget headroom would otherwise cap N at one; three binding conditions each closing a way the saving becomes a lie — **uniform fidelity across attempts** (a contest of unequally-resourced candidates measures fidelity rather than quality and crowns whichever tried hardest), **the proxy validated not assumed** (a low-fidelity ranking is a hypothesis of agreement with the full-fidelity ranking, measured periodically against full-fidelity contests — a proxy that does not rank alike is worse than no tournament since it selects wrongly with confidence and pays to do so), and **the proxy recorded** in the result (CE-9) and in the winner's reproduction recipe so the shipped artifact is never confused with the sketch that won; on unvalidatable or degraded agreement the contest reverts to full fidelity and reports that it did (tech-agnostic); v1.1.1 links `l1-fidelity-variants` as the general axis CE-10 is the worked special case of — proxy attempts are declared variants, the winner's re-production is a move to the reference variant | Stable | 1 | 1.2.0 |
| [l1-component-scanning.md](specifications/l1-component-scanning.md) | Agent-component threat scanning / admission vetting — statically vetting a third-party component (skill / tool-server / plugin / tool-set / imported bundle) for embedded adversarial content before it crosses the trust boundary: inventory-precedes-trust across all config scopes system/user/project/bundle (CS-1); admission-time static vetting gate between discover and activate, orthogonal to the capability-grant and integrity gates (CS-2); content-threat-≠-integrity — attestation and vetting are separate both-required checks, an authentic component can still be malicious (CS-3); closed severity-graded finding taxonomy with evidence excerpt, a finding is data not action (CS-4); threat-class coverage — instruction-poisoning, cross-component shadowing, obfuscation (rendered-view≠model-view, hidden payload decoded as evidence), malicious payload/RCE/exfil + suspicious download URLs, secret mishandling, unpinnable dependency (CS-5); cross-component toxic-flow composition (untrusted-input × private-data × egress) with per-leg attribution even when no single component is flagged (CS-6); consent-gated sandboxed execution-dependent vetting, default-deny on ambiguity/timeout/EOF, declined-never-executed, batch needs a named dangerous-override (CS-7); advisory-by-default, gating-on-critical, explicit per-finding disposition (CS-8); data-safety of the vetting channel — secret redaction (env/args/headers/query/NL-embedded) + path scrub + consent-gated never-silent egress, evidence never echoes a raw secret (CS-9); continuous re-vetting on mutation/interval + unpinnable-dependency flag (CS-10); composes l1-extensions (EXT-2 lifecycle gate) / l1-attestation (integrity pairing) / l1-context-provenance (runtime injection complement) / l1-interception-model (decide-class admission gate) / l1-security; the host-neutral nodus realization is l1-nodus-portability LP-12 (imported-bundle vetting seam, attest→vet→run) | Stable | 1 | 1.0.0 |

| [l1-iterative-refinement.md](specifications/l1-iterative-refinement.md) | Iterative refinement (generate–evaluate–refine) — the temporal member of the coordination family, trading depth for quality beside the three width fan-outs (parallel-staffing integrate-all, deliberation synthesize, competitive-execution select-one): one artifact improved in place across bounded revise cycles, no fan-out/fan-in (IR-1); producer/grader independence — the grader reasons only from artifact + rubric, not the producer's context, self-grading is self-endorsement not refinement (IR-2, ORC-6 judge); rubric-driven actionable localized feedback not a bare score, and a rubric pass ≠ definition-of-done (IR-3); grounded verification not plausibility — a groundable criterion is actually checked (IR-4, claim-verification); bounded convergence with an honest best-so-far + named-unmet-criteria exit, stall/oscillation halted honestly (IR-5); frozen criteria / no goalpost drift — the rubric is outside the producer's mutable set (IR-6, loop-governance oracle-ownership); observable attributed refinement trace, intermediate versions retained, pass-vs-exhausted distinguished (IR-7); uncertainty-justified not reflexive (IR-8, generation-budget); composes l1-loop-governance / l1-competitive-execution / l1-deliberation / l1-quality-standards / l1-requirement-checklists / l1-claim-verification / l1-orchestration; the host-neutral nodus realization is l1-nodus-language NL-14 (`~UNTIL +grade` grader-gated loop, the depth complement to NL-13 `~PARALLEL +select` width); v1.1.0 IR-9 — the IR-8 entry signal is a **measured** outcome-confidence estimate produced independently of the producer, never a producer's self-report (a self-declared uncertainty is a claim, and letting it trigger the loop lets a producer choose its own budget), and the estimate's **named contributors become the loop's revision targets** so an iteration is aimed rather than a generic whole-artifact retry spending budget on the parts already fine; the estimate informs entry and targeting only — it may not relax the frozen rubric (IR-6), extend the independent budget ceiling (IR-5), or decide the pass/continue verdict (IR-2, still the grader's), and a high estimate never waives a mandated definition-of-done gate (IR-3); v1.2.0 IR-10 — the independent grader MAY be a **human reviewer**, a **request-revision** being the loop's continue-verdict and an **approve** its pass, with the reviewer's **free-form feedback** the actionable signal IR-3 threads into the next attempt in place of a rubric line; the human case relaxes no discipline (bounded honest exit IR-5, frozen criteria so a reviewer cannot move goalposts mid-loop IR-6, producer independent of its own judge IR-2, every cycle recorded IR-7); what it *adds* — the solicited pause, the closed approve/revise/reject set, return-to-producer routing, default-review-on-the-terminal-step — is owned by the new l1-review-checkpoint (RC-1…RC-10), IR-10 recording only that a human review is an instance of grader-gated refinement so the two compose (IR-3) | Stable | 1 | 1.2.0 |

| [l1-extension-marketplace.md](specifications/l1-extension-marketplace.md) ; v1.2.0 XM-5 gains **originality as a third admission signal** beside safety and truthfulness — a catalog degrades through **re-skins** (an entry re-published with its domain nouns swapped) without any entry being malicious or mis-described, the harm landing collectively on **discovery** (ORI-11) exactly where XM-4 trust tiers are silent, since curation attests trustworthiness and never that an entry is not the fourth copy of something already catalogued; candidates clear `l1-corpus-originality` against catalog + pending batch, deliberate localizations admitted by distinctness or a **declared** derivation, never by exempting a catalog region; also records `l1-host-native-rendering` as this contract's **outbound twin** (XM-2 ↔ HNR-6) | Extension marketplace & distribution — the origin/discovery dimension the local registry lifecycle does not name: a curated catalog of addressable sourced entries (XM-1); stable immutable identity slug + governed rename migration, since renaming breaks every existing install (XM-2); reproducible install via pinning to a concrete content revision, updates being an explicit reviewable move not a floating auto-advance (XM-3); explicit + honest trust tiers — curation is a signal not a guarantee, the owner never controls third-party payloads (XM-4); publishing gate judging responsible data handling + description-matches-behavior over the whole shipped payload incl. dormant/hidden files, not merely non-malice (XM-5, invokes component-scanning as mechanism); structured truthful discovery metadata — category/description/author/homepage (XM-6); an entry MAY bundle several kinds or curate an explicit subset, the entry being the manifest of intent not the ambient source contents (XM-7); distribution is a source not a trust bypass — a catalogued extension is still default-deny + vetted + attested before activation, update re-runs the gates against the new pin (XM-8); explicit legible sync with no silent supply-chain drift (XM-9); composes l1-extensions (feeds EXT-2 lifecycle under EXT-3 default-deny) / l1-component-scanning (per-install vetting) / l1-attestation (pin as integrity anchor) / l1-version-control / l1-security; the host-neutral nodus realization is l1-nodus-portability LP-13 (addressable versioned import resolution, resolve → attest → vet → run); XM-10 provider choice stays with the user — suggest-and-let-pick for unnamed providers, urgency no exception, no answer-withholding as adoption pressure, no re-surfacing declined suggestions, commerce-class only-when-named, specific-value phrasing | Stable | 1 | 1.2.0 |

| [l1-inference-cache.md](specifications/l1-inference-cache.md) | Inference-state cache — the storage-side companion to cache-stable authoring: a durable tiered prefix-addressed cache of computed inference state the local runtime owns, turning re-ingesting a shared context prefix from a recompute into a lookup. Prefix-addressed reuse computing only the divergent tail (IC-1); a hit equals a recompute, content-keyed so a key match guarantees state equivalence (IC-2); tiered residency (fast volatile + durable persistent) with write-back so eviction is a slower hit not a lost recompute (IC-3); warmth survives process restart (IC-4); copy-on-write prefix sharing so context common to many conversations costs storage once (IC-5); bounded policy-governed admit-by-evict eviction that never breaks the memory budget (IC-6); honest observable hits/misses/tiers/evictions/write-backs so a silent warmth regression is diagnosable (IC-7); on-device confinement + auxiliary-lane isolation from the primary session's warm state (IC-8); reuse requires a byte-stable prefix — the storage-side counterpart to l1-cache-stable-context authoring, the cache rewards stability never forces it (IC-9); §4.3 two-residency-budgets (model weights MR-6 vs cached state) coordinated on one memory so caching never starves model residency; composes l1-cache-stable-context / l1-model-runtime / l1-storage-model / l1-file-management / l1-security; the host-neutral nodus realization is l1-nodus-language NL-15 (cache-stable prompt composition, byte-stable reusable prefix); IC-10 a shared prefix is a *symbol* prefix cut at a stable seam — IC-9's byte-stability is necessary but not sufficient, because encoding is not composable (l1-tokenization-boundary TB-2): a byte-identical leading context is a symbol prefix of the full request only when its boundary falls on a declared stable seam a continuation cannot move; a prefix whose final content can merge across the cut with the divergent tail has a stable key and an unstable sequence, so the cache either misses forever (a warmth regression IC-7 surfaces as a mystery) or serves state computed for a symbol sequence the request does not have — a silent IC-2 violation no key comparison can detect; prefix keys are therefore anchored at stable seams (a control symbol is one by construction, TB-4), an unknown boundary is treated as unstable, and seam stability is asserted by the encoder (TB-3) never inferred from the bytes | Stable | 1 | 1.1.0 |

| [l1-progressive-disclosure.md](specifications/l1-progressive-disclosure.md) | Progressive disclosure (tiered on-demand context loading) — the lazy-loading member of the context-economy family, orthogonal to compression (defer-loading vs shrink-loaded): every disclosable artifact (skill/tool/knowledge/schema) keeps only a minimal always-resident descriptor in context (PD-1), expands its body on trigger never preemptively (PD-2), reaches deeper bundled resources lazily level-by-level (PD-3); the descriptor is a routing contract whose accuracy is a correctness property — over-claim causes mis-trigger rot, under-claim causes missed use (PD-4); selective loading decouples catalog breadth from resident cost and bounds context rot (PD-5); deferral is lossless + reversible with collapse-back to the descriptor (PD-6); tiers are addressable/stable and the descriptor index sits in the frozen prefix so it stays cache-stable (PD-7); honest expanded-vs-deferred state so availability ≠ loaded (PD-8); composes l2-skill-system (the canonical 3-tier artifact) / l1-context-compression (the shrink-loaded axis) / l1-cache-stable-context (frozen descriptor index) / l1-harness-composition / l1-tool-composition; the host-neutral nodus realization is l1-nodus-language NL-16 (`@needs` selective vocabulary disclosure) | Stable | 1 | 1.0.0 |

| [l1-agent-federation.md](specifications/l1-agent-federation.md) | Agent federation — interoperation with independent external agents as verifiable peers (the missing counterparty class beside human client / internal roles / SaaS connectors), deliberately crypto-agnostic (owns the trust contract, defers scheme/transport/settlement to L2/host): sovereign self-held credential that is simultaneously identity + authenticator + authorization root, no third party holds the secret, the credential IS the identity (AF-1); human-rooted never self-minted, issuance/rotation/revocation are principal acts composing SEC-10 (AF-2); signed capability advertisement (agent card) + open capability-based discovery, the card a minimal descriptor PD-1 (AF-3); authenticated-confidential E2E peer channels bound to both identities (AF-4); coordinate-without-controlling relay neutrality — routes ciphertext + settles, holds no plaintext/keys, cannot forge/read/censor (AF-5); default-deny peer trust — discoverability grants nothing, peer output untrusted (provenance), reaching a peer is an egress act (AF-6); accountable verifiable append-only exchange anchored to a both-sides-checkable proof, optional shielding + escrow, an event-log-plus-verifier not an asserted balance (AF-7); auditable attribution to the principal, public activity views are projections not the record (AF-8); §4.3 demarcated from l1-orchestration / l1-global-orchestration (own agents, shared authority) and l1-acp (a caller to one office); composes l1-security / l1-attestation / l1-tool-receipts / l1-context-provenance / l1-operational-ledger / l1-extension-marketplace; the host-neutral nodus realization is l1-nodus-portability LP-14 (verified-peer delegation seam) | Stable | 1 | 1.0.0 |

| [l1-deployment-neutrality.md](specifications/l1-deployment-neutrality.md) | Deployment neutrality — local-first server-free default as the whole product, with a pluggable door open for auth + remote user-data backend: single local principal, no account/sign-in/remote-service/egress out of the box (DN-1); principal identity, authentication, and user-data persistence/sync are abstract provider seams with on-device defaults, the core assuming no server (DN-2); a remote backend is additive and opt-in — the *same core* runs solo-local, self-hosted sync server, or multi-tenant provider platform by supplying seam implementations only, without editing the core (DN-3); remote persistence and egress are consent-gated and never silent, covert off-device persistence unrepresentable (DN-4); a backend provider is an infrastructure-class extension (l1-extensions lifecycle, default-deny, manifest, sandbox, attestation, vetting), one active provider per plane, backing a core plane rather than adding a capability (DN-5); multi-tenancy is an optional hosted overlay not a core assumption, solo installs pay no tenancy tax (DN-6); the principal's data is portable across backends with the local default always a valid destination — no lock-in (DN-7); a backend moves data + authentication, never the agent's right to act — authority still descends from the human principal (DN-8, composes SEC-10 / AF-2); composes l1-architecture (INV-8) / l1-security / l1-extensions / l1-storage-model / l2-multi-user-auth / l1-multi-device-sync / l1-agent-federation; the host-neutral nodus realization is l1-nodus-portability LP-15 (host-supplied durable-state seam, StorageProvider concretized) | Stable | 1 | 1.0.0 |

| [l1-data-lineage.md](specifications/l1-data-lineage.md) | Data lineage — derivation traceability across a dataflow, answering "what produced this datum, through which transformations", the fourth provenance-family contract distinct from trust (context-provenance), integrity (attestation), and grounding (claim-verification): item-grained dataflow with per-item mapping as the traceability granularity (LN-1); every derived item records its immediate source at production time, runtime-captured not inferred (LN-2); transitive end-to-end traceability by walking the links (LN-3); faithful links neither fabricated nor lost, recording true fan-out/merge/filter/aggregate correspondences (LN-4); side-band metadata that never alters payload or logic, stripping it yields identical business results (LN-5); composes the operational ledger + grounding at the data grain, distinct from trust/integrity/claim contracts (LN-6); survives suspension/resume and sub-flow boundaries (LN-7); references-not-copies, retention/consent-bounded, never a covert second copy of user data (LN-8); composes l1-automation-pipeline / l1-execution-graph / l1-operational-ledger / l1-claim-verification; the host-neutral nodus realization is l1-nodus-observability HO-13 (per-item derivation lineage as optional side-band event metadata) | Stable | 1 | 1.0.1 |

| [l1-pattern-codification.md](specifications/l1-pattern-codification.md) ; v1.1.0 PC-4 clarified and the **missing axis named** — the pathway measured recurrence across **time** only, so observations repeated on distinct dates inside **one** context cleared every gate and produced a ratified rule silently claiming to hold everywhere; time-recurrence now explicitly establishes the pattern is **real**, never that it **generalizes**, with applicability decided by the separate independent-context gate in the new `l1-scoped-generalization` (SG-3), the two axes orthogonal (SG-11) so a rule may be ratified and **narrow** — normal and common, since the strictest conventions are usually the most local; no PC invariant relaxed, an unintended universality claim removed | Pattern codification — the disciplined memory-to-governance pathway by which a stable, dated behavioral pattern becomes an enforceable rule (distinct from memory consolidation, which abstracts within the corpus, and from EXT-7 skill distillation, patterns→capabilities; here patterns→rules): earned not automatic — recurrence is only a candidate signal, a binding rule additionally needs durability + explicit human ratification (PC-1); propose → ratify → write, the agent observes/proposes/writes and the human is the sole author of binding norms, never hand-editing the corpus and the agent never self-promoting (PC-2); tiered bindingness rising only with evidence — observation (non-binding) → advisory pattern → ratified rule (PC-3); recurrence detectable because observations are dated, the date the load-bearing unit of the promotion query (PC-4); rules carry ratification + last-review provenance, are periodically re-validated and reversibly demoted when contradicted, a rule is a standing hypothesis not a decree (PC-5); provisional/experimental patterns quarantined from direct promotion, graduated first (PC-6); auditable evidence-and-ratification pathway traceable back to the dated observations (PC-7); feeds governance without becoming it and never self-grants authority, composing SEC-10/LP-10 human-rooted authority (PC-8); generalizes SEC-9 permission-promotion to any behavioral norm; composes l1-memory-consolidation / l1-security / l2-agent-constitution / l1-practice-analytics / l1-operational-ledger; the host-neutral nodus realization is l1-nodus-dialog DG-10 (promotable remembered decision); v1.0.2 links `l1-project-vocabulary` as a consumer of this pathway — a domain term earns canonical status through PC-1/PC-2 and retires through PC-5, and a generator never self-promotes a coinage (VOC-7) | Stable | 1 | 1.1.0 |

| [l1-provenance-taint.md](specifications/l1-provenance-taint.md) | Provenance taint — the at-rest, persistent, propagating origin-trust taint on stored data, the missing "at-rest" complement to render-side trust-composition and the fifth provenance-family member ordered by data-life phase (origin-trust → derivation → authenticity → render-trust): taint captured at ingest by origin, internal-authored (first-party) vs external-synced (third-party) (PT-1); external/unknown untrusted by default, trust affirmative never assumed (PT-2); persistent + immutable-downward — the store never launders taint, downgrade free but upgrade untrusted→trusted human-rooted only, recorded (PT-3, composes SEC-10); propagates through every derived representation — a summary/entity/embedding/index inherits its source's taint, taint cannot be escaped by transformation (PT-4, rides data-lineage edges); most-untrusted-wins on combination, a single untrusted source taints the whole derivation (PT-5); sets runtime provenance on recall — a tainted memory recalled into a prompt is neutralized-as-data by default, feeding not duplicating the render-side contract (PT-6); inspectable + auditable enumeration of external-tainted content and its influence (PT-7); composes component-scanning + data-lineage, distinct from attestation-authenticity (PT-8); composes l1-context-provenance / l1-data-lineage / l1-component-scanning / l1-memory-intelligence / l1-security; the host-neutral nodus realization is l1-nodus-language NL-17 (origin-taint provenance, never relaxing NL-11's safe default) | Stable | 1 | 1.0.1 |

| [l1-action-gating.md](specifications/l1-action-gating.md) ; v1.0.3 links `l1-consent-binding` - the question AG leaves unasked after assigning a tier: *what was the grant for, and when does it stop applying?* AG-7 records that an act was approved and by whom, not with enough precision to decide whether the next attempt is the same act; CB gives AG-6 de-escalation rules a bound identity (a resolved invocation, never a name or category) and a lapse rule - which matters most precisely where AG-5 correctly minimizes prompts | Action gating — authorization friction proportional to an action's consequence (the how-much-friction contract, distinct from the threat-guard that blocks malicious and from config-lockdown governance): friction proportional to consequence, uniform gating (everything/nothing) forbidden (AG-1); consequence classified by explicit legible axes — reversibility, blast radius, external visibility, value at stake (AG-2); closed ordered tier ladder auto/confirm/approval, tier = highest axis demand (AG-3); read-safe frictionless, irreversible/external/value never auto, unclassifiable fails to the higher tier (AG-4); friction-fatigue a first-class failure — over-gating is a defect not caution, blanket-approval trains rubber-stamping and nullifies the gate on what matters (AG-5); learned scoped revocable human-ratified de-escalation composing SEC-9, never self-escalation-bypass, top tier non-de-escalatable (AG-6); every gate decision auditable with tier + axes + approver (AG-7); governable stricter never silently laxer, top-tier floor un-removable composing policy-governance (AG-8); composes l2-tool-security (the guard that enforces the tier) / l1-interception-model / l1-policy-governance / l1-security (SEC-9 de-escalation) / l2-orchestration / l2-agent-autonomy; the host-neutral nodus realization is l1-nodus-portability LP-16 (effect risk-class declaration) | Stable | 1 | 1.0.3 |

| [l1-deep-research.md](specifications/l1-deep-research.md) | Deep research — autonomous investigation of an open-ended question over external, open, *discovered* sources ending in a claim-verified, source-attributed, coverage-honest report; the concept parent of the existing l2-deep-research engine: open discovered sources as an explored environment not an ingested payload (DR-1); bounded sub-question investigation tree reusing recursive decomposition (DR-2); gather→read→synthesize never gather-and-dump (DR-3); grounded claim-verified synthesis, coverage declared not faked (DR-4); untrusted-by-default fetched content neutralized at the boundary (DR-5); fixed budget with progress measured against the question not the fetch count (DR-6); monotonic finding accretion with an append-only keep/discard ledger (DR-7); frozen criteria + independently-gated faithfulness, no mid-run goalpost-moving and no self-grading (DR-8); autonomous never-stall operation within a hard ceiling (DR-9); observable cost-rolled-up resumable investigation (DR-10); attributed coverage-honest deliverable (DR-11); composes l1-recursive-decomposition / l1-claim-verification / l1-browser-control / l1-context-provenance / l1-loop-governance / l1-generation-budget / l1-data-lineage / l1-orchestration / l1-work-liveness; the disciplined-loop family (harness-engineering/optimization, loop-governance) owns the shared iterate-evaluate-keep/discard machinery — deep research composes it over a different subject (an external question, not the agent's own harness) rather than duplicating it | Stable | 1 | 1.0.0 |
| [l1-recursive-decomposition.md](specifications/l1-recursive-decomposition.md) | Recursive decomposition — the third stance on an over-window input beside compression (shrink) and progressive disclosure (defer), processing it in place by bounded recursive self/sub-calls over pieces so effective context exceeds the raw window: an over-window input is an external addressable environment the model navigates programmatically, not a payload swallowed whole (RD-1); recursive processing over bounded within-window pieces, a piece still too large decomposed again so every actual call sees a within-window slice (RD-2); combine map-then-reduce never re-concatenate, the reduce itself bounded (RD-3); bounded depth + breadth by construction, fail-fast on exceed, halt-and-surface an unboundable piece never force it through (RD-4); observable cost-rolled-up correlated tree — every recursive child a first-class parent/root-correlated run whose usage+cost roll up (RD-5); a third orthogonal stance chosen by input shape — compress redundant structure, disclose a catalog, decompose a heterogeneous whole (RD-6); deterministic replayable slicing attributable to pieces (RD-7); grounded reduction — coverage may be lost but never faked, unexamined regions declared not glossed (RD-8); composes l1-context-compression / l1-progressive-disclosure / l1-orchestration / l1-loop-governance / l1-generation-budget / l1-data-lineage / l1-claim-verification; the host-neutral nodus realization is l1-nodus-language NL-18 (bounded recursive `RUN` with mandatory MAX_DEPTH) | Stable | 1 | 1.0.0 |
| [l1-tokenization-boundary.md](specifications/l1-tokenization-boundary.md) | Tokenization boundary — the frontier where the system's characters become the receiving model's symbol alphabet, and the contract every size/cut/cost/trust decision across that frontier must satisfy: the model's alphabet is the ground truth for size, cost, cut points and cache identity (TB-1); encoding is NOT composable — appending content retroactively re-encodes content before it, so a span's encoding is not a prefix of the whole's and any design that splits content, encodes the pieces independently and assumes they concatenate is wrong by construction (TB-2); cut only at seams the encoder declares stable, an unknown boundary being unstable, stability asserted never inferred (TB-3); control symbols (frame/turn/role/channel/stop markers) occupy a disjoint sub-alphabet structurally unreachable by encoding any content whatsoever — a property of the encoder not a sanitizer after it — which is also exactly why they are the only provably stable seams, the two properties being one fact seen from two sides (TB-4); minting a control symbol is a per-symbol authority held by trusted scaffolding and never by content, and the boundary's default for control-looking content is REFUSAL, with inert-encoding and minting each an explicit per-surface per-symbol disposition — neither silent inerting (hides an attack) nor silent promotion (is the attack) permitted (TB-5); the measure is the receiving model's own encoder and counts under different encoders are different quantities never substituted, compared, or aggregated (TB-6); an unidentifiable encoder fails loudly rather than defaulting, family rules are declared with stated reach, and a declared estimate never grounds an irreversible decision — no truncation, eviction, hard budget halt, or asserted cost figure rests on one (TB-7); an encoder is a versioned digest-verified self-validating artifact whose identity covers its control sub-alphabet, so two encoders sharing content symbols but differing in control symbols are different encoders and must not share a name, with structural invariants (bijective mapping, declared size, no duplicate ids) checked never assumed (TB-8); round-trip fidelity with declared — never silently default — lossy decode, and a truncated symbol sequence is not a text prefix so a streamed partial symbol must not be rendered as complete (TB-9); symbol↔source-span correspondence is reportable because symbol and character boundaries do not align (TB-10). Supplies the structural floor beneath l1-context-provenance CP-2/CP-6 (→ CP-9), the seam rule beneath l1-cache-stable-context CSC-2 (→ CSC-12) and l1-inference-cache IC-1/IC-9 (→ IC-10), and the measure beneath l1-generation-budget / l1-context-compression / trace-derived cost accounting; nodus realizations = l1-nodus-language NL-19 (unforgeable frame markers & seam-anchored segmentation) + l1-nodus-environment NE-14 (declared budget measure) | Stable | 1 | 1.0.0 |
| [l1-session-reinforcement.md](specifications/l1-session-reinforcement.md) | Session reinforcement — host-side runtime control plane against in-session drift: typed advisory classes on declared classifier/threshold/state triggers (SR-1/SR-2); tighten-only direction monotonicity — an injection that would relax or contradict the standing baseline is invalid by construction, which makes a forged "system" advisory structurally detectable regardless of how authentic it looks (SR-3); host-only issuance — advisory-shaped content in any data channel keeps its provenance and is never honored (SR-4, composes context-provenance + SEC-10); long-horizon re-anchoring from a curated versioned baseline core, verbatim-or-curated never paraphrased (SR-5); supervisor probes — suspend-answer-resume, precedence over in-flight instructions within SR-3 bounds, no agent self-exit, answers recorded (SR-6); context-not-authority — no capability granted, no per-effect gate substituted (SR-7); budgeted, observable, non-nagging emission with declared re-issue policy (SR-8); delivered over the dynamic-harness environment-injection seam (DH-3/DH-5); nodus workflow side already covered by NL-11/NL-15/NL-19/LP-10 — no new language invariant needed | Stable | 1 | 1.0.0 |
| [l1-knowledge-horizon.md](specifications/l1-knowledge-horizon.md) | Knowledge horizon — generation-side anti-confabulation discipline (micro-spec): declared model knowledge horizon + host-injected current date as volatile context outside the stable prefix (KH-1); retrieval-before-assertion for unrecognized or post-horizon entities, honest unverified marking when retrieval is unavailable — recognizing a franchise is not knowing its latest member (KH-2); now-anchored query formulation + present-tense current-status routing (KH-3); post-retrieval trust calibration — retrieved evidence outranks parametric memory with declared skepticism classes requiring corroboration (KH-4); **KH-5 version-anchored dependency grounding** — parametric knowledge of a library is a version-blur skewed to the training-era-common release, so recognition ≠ knowing the pinned version's API surface; external-dependency usage is grounded against the pinned version's authoritative docs (resolved from the project's dependency/lock manifest via l1-code-intelligence), not parametric memory, and a recognized pre-horizon library still triggers grounding on version drift (different major/minor, renamed/deprecated API) else usage is marked unverified — extends KH-2 beyond the unrecognized/post-horizon trigger to the recognized-but-version-drifted case (the confident wrong-version API call); nodus realization = existing SchemaProvider schema-aware validation, no new NL invariant; the generation-side complement of l1-claim-verification (which audits claims already produced) | Stable | 1 | 1.1.0 |
| [l1-generation-shaping.md](specifications/l1-generation-shaping.md) | Generation shaping — the third output-economy concern beside the length budget (l1-generation-budget) and the output contract, governing the *character & effort* of a turn's generation rather than its length: turn-classified modulation keyed on what the turn is — routine resume-after-tool vs fresh/error/ambiguous (GS-1); verbosity steering as a request-instruction that suppresses ceremony/preamble/restatement, never a post-hoc trim of produced output (GS-2); floored effort/reasoning-depth modulation — lowered toward a floor on demonstrably-routine turns, kept full on new/error/ambiguous/high-stakes (GS-3); correctness supremacy as the load-bearing floor — shaping never trades correctness/completeness/safety for brevity or speed, steps aside on any hard/failing/ambiguous signal (GS-4); cache-stable steering placement — the persistent directive sits at a stable seam so it never busts the KV cache it is trying to cheapen, composing CSC-2 (GS-5); counterfactual measurement honesty — output savings are unobservable so a report is an estimate+CI or a held-out control-group measured number, never a fabricated exact figure, applying solution-frugality FR-10 (GS-6); opt-in/off-by-default/reversible/live-reconfigurable with no cold restart (GS-7); learned-and-overridable level inferred from implicit behavioral signals (interrupts) via the user model UM-2/UM-4 (GS-8); accounted & observable as a distinct signal (GS-9); nodus mapping needs no new NL invariant — host-side over NL-15 volatile suffix + ModelProvider options + @err/!! + HO-8 cost classes | Stable | 1 | 1.0.0 |
| [l1-value-settlement.md](specifications/l1-value-settlement.md) | Outbound value settlement — paying an external party (API/service/charging peer) for value received, the third budget axis distinct from inbound usage-allowance and per-request generation-budget (VS-1); principal-set spending envelope (per-payment + cumulative ceiling, allowlisted recipients, rate limit) on the human-write-only authority plane, agent-read-only never agent-set (VS-2, SEC-10); bounded-authority custody — scoped spend the agent cannot exceed even if compromised, funding secret never in context (VS-3, SEC-1); every payment gated at the top of the action-gating ladder, allowlisted recipient a precondition not a waivable friction (VS-4, AG-1/AG-2/AG-4/AG-8); fail-closed — path down/envelope exhausted/denied blocks the paid action, never falls back to unmetered access or unbounded retry (VS-5, INT-3); bounded machine-negotiable payment-required handshake that settles once or aborts, never loops (VS-6); every payment provable via unforgeable receipt + operational ledger, unreceipted = unaccounted (VS-7, TR-4); opt-in/local-first/rail-neutral provider seam — core performs no payment and names no rail, peer payee = federation settlement case, enabling payment adds a capability not a server dependency (VS-8, deployment-neutrality/AF-5/AF-6); composes l1-security/l1-action-gating/l1-usage-allowance/l1-generation-budget/l1-tool-receipts/l1-operational-ledger/l1-interception-model/l1-agent-federation/l1-deployment-neutrality/l1-policy-governance; nodus realization = l1-nodus-portability LP-17; concept-only (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-document-understanding.md](specifications/l1-document-understanding.md) | Deep, layout-aware document understanding as the high-fidelity front of ingestion: one structured-document contract across heterogeneous formats, reading-order reconstruction, typed element taxonomy (title/text/list/table/figure/caption/header/footer/equation/reference), model-legible first-class tables, optical understanding for scanned/image sources, honest full/reduced fidelity self-report, per-element positional provenance, host-supplied local-first capability seams that degrade-and-record never fail the ingest (DU-1…DU-9); feeds l1-content-segmentation; the parse-front KB-12 depends on; concept-only (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-content-segmentation.md](specifications/l1-content-segmentation.md) | Structure-aware segmentation replacing naive fixed-window chunking: boundaries follow document structure, heading-breadcrumb context travels with each chunk, atomic tables/figures/code/equations, per-genre strategy family (prose/slides/spreadsheet/Q&A/records/long-form), structural overlap, preserved positional provenance, explainable+adjustable boundaries, deterministic+idempotent for cheap incremental re-index (SEG-1…SEG-9); multi-granularity linked segments — fine child match-units linked to coarse parent return-units so retrieval matches the small child (precise) but returns the large parent (context-sufficient), the small-to-big precision-vs-context resolver, provenance-preserving, opt-in/additive (SEG-10, §4.5); consumes l1-document-understanding, feeds knowledge-base/graph/summary; the chunking-front KB-13 depends on; concept-only (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-knowledge-graph.md](specifications/l1-knowledge-graph.md) | Corpus-derived entity–relation graph as a complementary retrieval substrate beside the flat chunk/vector index for relational + corpus-global questions flat retrieval cannot answer: grounded/attributed per-segment extraction, conservative auditable/reversible entity resolution, bounded description synthesis, community structure with hierarchical reports, local (neighbourhood-expand) + global (community-report) retrieval modes, non-authoritative evidence-not-truth subject to claim-verification, incremental/idempotent/resumable/observable construction, host-supplied local-first opt-in enrichment never a hard dependency (KG-1…KG-10); optional KB-14 representation; nodus realization = bounded recursive map-reduce (NL-18) + provenance-tracked extraction (NL-11) + lineage (HO-13); concept-only (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-hierarchical-summarization.md](specifications/l1-hierarchical-summarization.md) | Hierarchical multi-resolution summarization as a persistent retrieval index: recursive cluster-then-summarize builds a tree of increasingly abstract summaries over segments (or graph communities), bounded+terminating construction (a non-shrinking level collapses to one parent), provenance carried to the root (each summary names its covered source leaves), level-tagged directly-retrievable summary peers distinguishable from primary sources, faithful non-authoritative lossy reduction, deterministic-given-policy, host-supplied local-first opt-in (HS-1…HS-8); distinct from l1-context-compression (window economy) and l1-recursive-decomposition (one-shot over-window processing); optional KB-14 representation + KB-15 recall channel; nodus realization = ~UNTIL MAX + ~MAP/~JOIN + HO-13 lineage; concept-only (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-live-diagnostics.md](specifications/l1-live-diagnostics.md) | Live diagnostics — the active, on-demand, targeted, non-suspending complement to the always-on observability plane (l1-telemetry / nodus HO-*), the "attach a debugger to production without a restart" capability the passive planes lacked: a probe opened against one specific target for a bounded window, never a second always-on trace (LD-1); non-suspending observer — diagnosis observes, never intervenes (LD-2); deep FULL-fidelity capture (real args/return/exceptions the always-on trace excludes) under a consent-gated, session-confined, secret-redacted data-safety exception, the inversion of the no-raw-content floor (LD-3); attaches to an already-running target with no pre-instrumentation and no restart, to catch an unforeseen bug a restart would vanish (LD-4); bounded + self-terminating (LD-5); caller + latency attribution composing HO-7/RD-5 — who called this, where the time went (LD-6); deterministic mode-marked capture-and-replay reusing the replay-validation seam, distinct from l1-simulation modeled play-out (LD-7); non-authoritative + traceable, findings feed l1-doctor/l1-office-control (LD-8); host-supplied local-first opt-in (LD-9); nodus realization = a targeted deep AuditProvider observer variant + replay-validation seam, no new primitive; v1.1.0 LD-10 — **published introspection routines** as the *anticipated* half of live diagnosis beside the probe's unanticipated half: named, catalogued, discoverable-not-guessed read-only routines returning a live-state snapshot, each declaring the authority level it requires (so a sensitive routine is unavailable rather than offered-then-refused over a lower-trust channel) and a timeout, cancellable, mutating nothing; catalogues propagate toward the supervisor so a routine can be invoked on a subordinate through its supervisor with every answer naming the producing node; §4.1 extended with the three-surface cheap-to-expensive escalation table (trace → routine → probe); concept-only (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-incremental-execution.md](specifications/l1-incremental-execution.md) | Incremental workflow re-execution — re-running a graph after a small change without redoing the work the change did not touch (the incremental-build discipline applied to a workflow graph): per-step output memoization under a content-addressed key over transitive input identity — own inputs + ancestry signature (IE-1); recompute only the affected downstream cone, never the whole graph nor less (IE-2); cache-hit ≡ recomputation — same value/type/provenance, a wrong hit is a correctness defect not an optimization (IE-3); determinism-gated memoizability — only a pure step is keyed on input identity, effectful/external/stochastic steps must not be silently cached (IE-4); fail-safe freshness escape — a step that can change with no declared-input change declares a change token else is treated as always-changed/recompute, never stale reuse (IE-5); invalidation by input identity not time — no stale-serving TTL, bounded eviction only forces recompute (IE-6); opt-in + observable + disable-to-diagnose (IE-7); host-supplied local-first rebuildable cache — cold cache costs recompute never data/correctness (IE-8); distinct from EG checkpoint/resume (continues an interrupted run) and l1-inference-cache (prompt-prefix not step-output); nodus realization = determinism NL-6 (the IE-4 precondition) + NL-12 effect distinction + memoized-approval precursor + LP-15 StorageProvider, no new primitive; concept-only (tech-agnostic); v1.0.1 links `l1-order-independent-production` — the same determinism instinct across *positions of one artifact* rather than across runs of one step; IE-4 protects a cache and needs no global index, uniform partitioning, or seam-safe join  ; v1.1.0 adds IE-9 — **explicit, scoped invalidation** for supplier-held state the engine's input identity cannot see, with the prior output retained until its replacement resolves (no hole, no flicker through a fallback); the imperative counterpart to IE-5's standing freshness policy | Stable | 1 | 1.1.0 |
| [l1-observation-retention.md](specifications/l1-observation-retention.md) | Observation retention — how a continuously-growing operational record stays answerable at every horizon (last minute and last year) while consuming a bounded declared amount of local storage, without either keeping everything or deleting the long horizon exactly when a trend question needs it: one logical series retained simultaneously at a full-resolution recent tier and progressively coarser derived tiers with the layer selecting the tier and the consumer never naming one (OR-1); **aggregate sufficiency** — a coarse point carries sum, count, min, max, anomalous fraction, and covered interval, so average/min/max/anomaly-rate over any range are computed *exactly* from a coarse tier and only intra-interval shape is lost, a mean-only tier being forbidden because it makes extremes unrecoverable and systematically understates spikes (OR-2); **a gap is a value** — an interval where measurement was impossible records an explicit marker distinguishable from every real value including zero, with covered-vs-expected counts so quiet and not-observed are never conflated, zero-substitution / last-value-carry-forward / silent omission each forbidden as converting unavailability into a false statement (OR-3); budget-bounded not horizon-promised, with the actual retained horizon measured and reported and reclamation by whole-oldest-segment rotation (OR-4); append-only immutable history where a correction is a new observation never an edit (OR-5); coarse tiers derived incrementally and backfilled from the finest tier still holding the interval, two independent collections of one quantity forbidden since they can disagree (OR-6); every answer states the resolution actually used and carries an explicit truncation marker beyond the retained horizon, silent shortening and upsampling-to-look-fine-grained both forbidden (OR-7); working set bounded by active series and tier count, never by horizon or query range, long-range queries streaming (OR-8); retention local, inspectable, exportable, never silent (OR-9). Nodus source-side counterpart = HO-14 aggregation-safe measurements. Concept-only (tech-agnostic) [v1.1.0] §2 amended — the non-numeric exclusion now names its owner: `l1-evidence-archive` takes the raw evidentiary trace, inheriting OR-5 immutability and OR-7 resolution honesty but **not** the tiering (text has no loss-bounded coarsening — its only "coarse" form is a summary, the artifact that spec exists to keep verifiable). | Stable | 1 | 1.1.0 |
| [l1-anomaly-consensus.md](specifications/l1-anomaly-consensus.md) | Anomaly consensus — always-on unsupervised per-series anomaly marking whose false-positive rate is controlled **structurally** rather than by per-series threshold tuning nobody has time to do: each series carries its own cheap models trained unlabelled on its own history with per-sample cost bounded independently of history length, many light models never one shared heavy one, no user content and no generative model as input (AC-1); **the unanimity gate** — several models per series trained on *different staggered* historical windows collectively spanning far more history than any single window all score every sample and a sample is marked anomalous only if **every** model agrees, a majority or weighted vote not being an anomaly, this being the layer's sole false-positive control and an explicit replacement for per-series tuning (AC-2); **the verdict is one flag carried inline with the observation itself** — not a parallel series, not a side table — so marking costs no additional storage and the anomaly *rate* over any range or grouping is the ordinary mean of that flag computed by the normal query path and preserved exactly through coarse retention tiers, value and verdict being one physical record that cannot drift apart (AC-3); anomaly rate first-class at two grains, per-series-over-time and per-unit-at-an-instant, both ordinary observable series in their own right (AC-4); **assistance not paging** — a mark never by itself raises a graded alert or notifies, escalation staying exclusively with the rule layer, only a sustained elevated *unit-level* rate surfacing as a low-urgency event, so notification volume is bounded by construction whatever the mark count (AC-5); unscored is never normal — an unscorable sample is marked unscored and excluded from rate denominators, so a cold or falling-behind system never appears healthy for lack of verdicts (AC-6); training and scoring out of the observation path, budgeted, degrading to unscored, and switchable off entirely (AC-7); explainable and reproducible verdicts — participating models, their training windows, their scores and thresholds, or the verdict is not shown (AC-8); concept drift handled by recorded periodic retraining so a durable regime change becomes the new normal rather than a permanent anomaly stream, the retraining boundary recorded so world-changed stays distinguishable from baseline-moved (AC-9); local-first — models, windows, verdicts never leave the device (AC-10). Nodus carrier = HO-16. Concept-only (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-change-attribution.md](specifications/l1-change-attribution.md) | Change attribution — the mechanism turning "something went wrong around 14:32" into "these twelve signals moved, ranked", the entry point of an investigation and never its conclusion: window-scoped ranked scoring over **all** eligible series in scope rather than a pre-chosen subset, output ordered with a score per series and never a yes/no, a design requiring the user to name candidates in advance defeating the purpose (CA-1); the comparison baseline **derived from the window itself** (a longer immediately-preceding interval) and always reported, so the operator selects one interval and never constructs a control, an explicitly supplied baseline permitted and then reported as supplied (CA-2); **two named scoring families** — distributional (distribution shift, sensitive to variance and shape changes at similar means) and magnitude (relative aggregate change, robust for sparse and on/off series) — both always available with the family and aggregation disclosed on every result, a single unnamed relevance number forbidden (CA-3); the same machinery ranks raw values **or** anomaly rates, which-signals-behaved-unusually being a distinct and often more discriminating question than which-signals-moved (CA-4); **shortlist never a cause** — the result is candidates for investigation, and presenting a rank as a cause, recording it as root cause, or feeding it to automated remediation are all forbidden since a wrong automatic cause stops the investigation (CA-5); bounded candidate set with an explicit truncation marker naming how many were dropped and by what rule, a silently truncated ranking being indistinguishable from a complete one (CA-6); read-only, non-perturbing, cancellable with partial progress, unable to starve the running system so the diagnosis never becomes the incident (CA-7); **refuse rather than answer weakly** — a too-short window or insufficient retained resolution is refused with the reason rather than answered with a low-confidence ranking that looks normal, with resolution and coverage stated on every result (CA-8); composable iterative narrowing by window, scope filter, quantity, and family with every run's parameters recorded so an investigation is reconstructable (CA-9); evaluated by the holder with the widest view, naming the evaluating holder and any series it could not see, partial coverage disclosed never silently narrowed (CA-10). Complements l1-data-lineage (structural derivation) with statistical association. Concept-only (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-declarative-configuration.md](specifications/l1-declarative-configuration.md) | Declarative configuration surfaces — one mechanism for everything configurable, with no editor hand-written for any of it: the owning component publishes the **single** machine-readable declaration of its configuration (fields, types, ranges, defaults, required-ness, secrecy, descriptions) and a second separately-maintained copy anywhere — a hand-written form, a duplicated docs table, a parallel schema — is forbidden because it will drift invisibly (DC-1); every editing surface graphical/terminal/agent-facing is **generated** from the declaration so a new configurable component adds zero editor code, and a component with no declaration is not editable through the generic surface at all rather than editable by guesswork (DC-2); **the owner validates** — a proposed change goes to the owning component before taking effect, shape-validity being necessary and never sufficient since environmental, cross-instance, and semantic validity are structurally unavailable to any editing surface, with a typed explanatory rejection naming the offending field (DC-3); nothing effective until accepted and application atomic, the previously effective configuration remaining fully effective until then so a rejected or failed change never leaves a component unconfigured (DC-4); **three kinds — single, template, instance** — with the available actions determined by kind (a template is instantiated but never enabled; an instance is enabled/disabled/tested/removed), making configure-fifteen-of-the-same-thing a first-class operation instead of a copy-paste discipline (DC-5); durable, attributed (who changed what when), and revertible to the prior version (DC-6); identical mechanism for built-in and extension-supplied components with no privileged internal path (DC-7); stable hierarchical addressing (owner → category → name) used by audit, permissions, and automation, renaming being a recorded migration never a silent re-point (DC-8); secrets write-only through the surface — settable, replaceable, clearable, but never returned by a read, rendered, exported, or traced, honoured automatically by every generated surface (DC-9); permission-gated and authority-preserving — a consequential change passes the same risk-proportional gate as any other effect and configuration can never grant a capability, relax a policy, or raise a limit the authority plane owns (DC-10). Ends at acceptance; l2-config-hotreload owns application. Nodus realization = NL-20 `§config`. Concept-only (tech-agnostic)  ; v1.1.0 adds DC-11 layered resolution with **per-field authority** — each field declares the lowest-trust layer permitted to set it: environment-owned fields (bindings, local tool paths, credentials) are operator-only, a lower-trust value never feeds an exec-adjacent decision, and lower-trust configuration is disclosed rather than silent | Stable | 1 | 1.1.1 |
| [l1-outcome-confidence.md](specifications/l1-outcome-confidence.md) | Outcome confidence — a calibrated, decomposed estimate of the probability that delivered work actually satisfies what was asked, computed at the delivery boundary from the *whole trajectory* rather than the final artifact, as the instrument against **silent incompleteness** (work that ran cleanly, passed every check it chose to run, and still did not do what was asked — the region where no claim was made, so no check can falsify anything): the object of judgement is the outcome read from the trajectory, since the evidence of incompleteness lives in the process (a test never run, a scope silently narrowed, a broken dependency worked around unsaid) not in the deliverable (OC-1); **decomposed into named contributors from a closed declared vocabulary, never a bare scalar** — "68%" is unactionable, "68%, because no test was executed and error handling was never addressed" is a work order (OC-2); **anticipated follow-up as a first-class contributor family** — estimating the specific ways the requester will have to come back is the only question that reaches past the checks that were run to the ones never conceived (OC-3); **calibration as an obligation** — a value shown as a probability is measured against realized outcomes (did the requester have to come back?) and, where calibration is unknown/stale/drifted, is shown as an ordinal or withheld, since an unmeasured percentage borrows the authority of measurement and is trusted more than it deserves (OC-4); **confidence never substitutes for evidence** in the most dangerous direction — a high estimate may never satisfy/waive/shorten a required check, verification deciding *done* while confidence decides *how hard to look and whom to involve* (OC-5); a low estimate carries a declared behavioural consequence (continue on the named contributors / run a specific check / escalate / hand over with uncertainty stated), never merely a hedged sentence, because a decorative signal teaches the user to ignore the channel (OC-6); producer-independent and never self-declared, since producers that are wrong most often sound most certain (OC-7); bounded, degradable, and never default-confident — *unassessed* is a visible state (OC-8); reproducible from a recorded evidence span with assessor identity and per-contributor scores (OC-9); comparable across candidate outcomes but **never aggregated into a reputation score** for a worker/role/person, which would both corrupt the metric (it becomes a target) and the work (it rewards looking confident over being correct) (OC-10). Supplies the IR-8/IR-9 entry signal and revision targets. Concept-only (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-environment-lifecycle.md](specifications/l1-environment-lifecycle.md) | Execution environment lifecycle — the lifetime contract for the **ephemeral** environment agent work runs in, deliberately distinct from the sandbox confinement policy (authority — what code may do) and from organizational workspace lifecycle (durable, human-scale offices): specification/instance split so an environment is reproducible from a name and never assembled ad hoc, a specification carrying base template + entry command + initial variables + working root + resource class + the *names* of required secrets (EL-1); a closed observable status set (provisioning · ready · suspended · resuming · failed · gone) where provisioning and resuming expose progress rather than an opaque block and **failed** stays distinct from **gone** (broken-retry-may-help vs over-do-not-wait) (EL-2); **intent accepted before the environment exists** — the request is taken immediately, given a provisional identity, and its intent durably queued, because a blocking provisioning wait spends the human's time and pushes them into re-sending, which manufactures duplicate work (EL-3); **exactly-once, in-order reconciliation** of the provisional identity onto the real one with queued intent surfaced on provisioning failure — losing it, delivering it twice, and leaving a dangling provisional identity are one invariant's three defects (EL-4); **every exit path names its capture owner** — explicit destroy, idle/expiry reap, quota eviction, host crash, failed resume, deliberate abandonment — with ownership following the *power to destroy* rather than the intention to preserve, since work is preserved on the well-tested path and vanishes without an error on the routes nobody enumerated, and the reap (which never reaches the requesting component) is the dominant one by volume (EL-5); residue captured as a **verifiable base-anchored delta** naming its base's source/revision/root, not an opaque blob — cheap enough to be mandatory on every path, reviewable, and applicable elsewhere, three properties an environment snapshot lacks (EL-6); suspension preserves while destruction captures, and a resume restores **the same instance** or reports a failed resume rather than silently substituting a fresh one the caller believes is the old one (EL-7); bounded lifetime with a declared reaper — an instance nothing owns the expiry of is a standing cost and exposure (EL-8); scoped revocable credentials never issued before *ready* and revoked on suspend/fail/destroy, with exposed services enumerated by name so a consumer never guesses an endpoint (EL-9); provisioning secrets injected and never present in the specification, instance record, conversation record, trace, or residue — so a specification is safe to store, share, version, and review (EL-10). Nodus counterpart = l1-nodus-portability LP-18. Concept-only (tech-agnostic) | Stable | 1 | 1.0.1 |
| [l1-staged-rollout.md](specifications/l1-staged-rollout.md) | Staged rollout & progressive enforcement — how a running system changes **itself** without betting everything on being right, the *release* half of self-improvement (the offline optimizer decides which candidate is better; this decides how it reaches real work): one rule — **nothing goes from absent to universal in a single step, and the counterfactual is measured before the change becomes the only path**. Every behaviour change ships behind a named switch restorable without a rebuild, reinstall, or restart of in-flight work — a change undoable only by shipping another version is a bet, not a rollout (SR-1); assignment is a **pure deterministic stateless function** of (switch identity, subject's pinned key), never drawn at random and never stored, giving reproducibility offline, no assignment state to lose or let drift between assigner and reporter, and an always-answerable "why did this get that variant?" (SR-2); switches are mutually independent by salting on switch identity, so two changes at 10% never land on the same 10% and confound each other (SR-3); **monotone under widening** — raising the fraction only adds, so tuning a percentage never re-shuffles subjects and silently destroys the comparability of everything measured on either side of the tuning, plus its temporal half — a decision taken for a unit of work in flight is frozen for that unit's lifetime, so changing or disabling a switch governs future decisions only and no run straddles two arms (a straddling run is attributable to neither and poisons every comparison it enters, so it must be unrepresentable rather than merely discouraged) (SR-4); the bucketing key is pinned and survives identity transitions, a subject changing arm mid-flight corrupting both its experience and every measurement it contributed to (SR-5); explicit human opt-in short-circuits all percentage and targeting rules and is excluded from the comparison population, since a self-selected group is not a sample (SR-6); a **reserved cross-switch holdout** — the only construct answering whether the *accumulation* of everything shipped beat where the system started, a question no individual comparison asks because each baseline is the previous already-changed state, and one that must be reserved in advance since it cannot be reconstructed after the fact (SR-7); every decision returns the **most informative** reason from a closed vocabulary including the negatives — "outside the exposure fraction" says targeting matched and only the dice did not, which "no match" does not, and a rollout that can only say *no* gets turned off rather than fixed (SR-8); local, offline, fail-closed-to-a-declared-safe-value evaluation with bounded observable staleness, no network round trip per decision, and no secret riding in a distributed ruleset (SR-9); **new constraints ratchet observe → warn → enforce** with the warn threshold *derived* from the enforce threshold so the two cannot drift apart — a limit whose would-have-blocked set was never measured has an unknown blast radius and will discover it by breaking something (SR-10); pre-declared frozen stopping rule with independently-tripping guardrails and an honest *undecided* outcome when the declared minimum is not reached, since deciding by watching until the numbers look favourable is a search for a moment, not a measurement (SR-11). Consumers: l1-harness-optimization HX-12, l1-policy-governance PG-10. Nodus counterparts = l1-nodus-portability LP-19 + l1-nodus-observability HO-18. v1.1.0 SR-12 (shadow evaluation) — a **third** introduction mode beside fractional exposure and the ratchet, for a **recognizer** (detector, classifier, grouping rule, scorer — something deciding *what things are* rather than *what happens to them*): run against all live input while producing no user-visible effect, output compared against the incumbent before either replaces the other; shadow answers what a fraction cannot, since a recognizer's value is its findings across *everything* rather than how a fraction of subjects fare, and conversely a fraction leaves a record with two incompatible classifications in it; safe only under two rules — **quarantined output** (findings raise nothing, notify nobody, enter no record read as real) and **no shared identity namespace with the incumbent**, the trap that makes shadow mode dangerous rather than merely useless when done wrong, because a shadow writing into the same identity space makes every finding *already recorded* by promotion time, so the promoted recognizer sees a world where nothing is new and the entire first-occurrence path (notification, assignment, escalation, initial triage) is silently skipped for exactly the findings the change existed to surface — invisibly, permanently for those findings, and indistinguishable from "it found nothing"; new §4.6 three-modes table. Concept-only (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-fault-lifecycle.md](specifications/l1-fault-lifecycle.md) | Fault identity & lifecycle — what makes two failures **the same failure**, and what happens to that failure over its life; the layer upstream of reporting (reporting decides whether and how a failure is filed outward, this decides what the thing being filed *is*). Occurrences collapse into a **fault** every surface addresses, never per-occurrence, since per-occurrence presentation makes frequency the loudest signal in the room and buries the fifty distinct problems under the one firing in a loop (FL-1); identity produced by a **named, versioned** grouping configuration that is migrated rather than edited, because changing what "the same failure" means silently re-partitions all history — a fault that splits or merges keeps its name, counts, resolution, and owner while now describing something else, a corruption that raises no error and is therefore never noticed (FL-2); a **layered explainable ladder** from declared identity down through structural location and typed class to a normalized descriptor of last resort, recording which component contributed and which did not and why, with free text deliberately last since interpolated values shatter one fault into thousands (FL-3); declared identity outranking computed — computed grouping is a default for the failures nobody anticipated, never an authority over the ones somebody did (FL-4); merges that keep the absorbed identity **resolvable** so no filed report, link, ticket, note, or automation dangles — a duplicate is visible and a dead reference is not (FL-5); status refined by **substatus** — unresolved as new / ongoing / regressed / escalating, archived as until-escalating / until-condition / permanent — because a flat open-closed model renders "appeared five minutes ago", "scrolled past for a month", and "the fix did not hold" identically, and a list whose entries all look alike is triaged once then ignored (FL-6); **resolution as a version-anchored claim the system checks** — a recurrence at or after the claimed-fixed version reads as *regressed* while an earlier-version recurrence is explicitly not a regression, so a repair that keeps failing stays visible instead of returning as unrelated strangers (FL-7); **escalation re-surfaces a known fault** whose rate rose above its own baseline, because being known must not be a hiding place — a tenfold increase otherwise produces no signal at all since nothing new appeared (FL-8); archival conditional by default with a stated condition for return, suppressing notification but never recording or counting, permanent archival being an explicit attributed choice since a default-permanent archive is indistinguishable from a fix (FL-9); bounded representative occurrence retention (first, latest, a sample between, plus aggregates) so cost scales with faults not occurrences and the loudest fault is not the most expensive to have (FL-10); the recording pipeline's own failures recorded as **visible faults attributed to their source**, never discarded or buried in an operator-only log, since data that fails to become a record is the data whose absence nobody notices and a silently-dropping pipeline reports a world cleaner than the real one (FL-11). Consumers: l1-error-reporting ERR-7, l1-operational-health OH-14, l1-staged-rollout SR-12. Nodus counterpart = l1-nodus-observability HO-19. Concept-only (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-reproduction-recipe.md](specifications/l1-reproduction-recipe.md) | Reproduction recipe — a delivered artifact carries **embedded in itself** the complete versioned recipe that produced it, so a holder of only the artifact can re-derive it or change one thing and re-derive a variant; deliberately the opposite placement to data lineage, which stays in the trace (LN-5) and therefore becomes unreachable at the exact moment an artifact is exported, attached, or handed over and acquires an audience that cannot query anything. The recipe travels with every copy, export, and hand-off (RR-1); **three layers or it is a lie** — the request, the components, and the **ambient configuration in force** that affected the outcome without being asked for, the third being the layer designs forget because at production time it is the environment rather than an input, and whose omission yields a recipe that looks complete, replays under different settings, and reports success (RR-2); components by **content identity** with names for legibility only, since names are the part of a system that drifts fastest across the very interval a recipe exists to cross (RR-3); **the producing version recorded and honoured on replay**, or refusal naming the fields it cannot honour — replaying an old recipe under current semantics and presenting the result as a reproduction fails silently, looks successful, and hides the difference from the person who most needs it (RR-4); every influencing component contributing its own fields, since a silent contributor makes the recipe **undetectably** incomplete (RR-5); completeness not configurable and omissions declared present-but-unavailable with a reason, because a quietly short recipe is read as one that had nothing to omit (RR-6); best-effort per field so one failure does not convert a small gap into total loss at the moment of delivery (RR-7); **determinism stated, never implied** — deterministic / deterministic-given-controls / non-deterministic, since a recipe promising exactness it cannot deliver teaches readers to distrust every recipe (RR-8); secrets named-and-redacted rather than carried or silently dropped, so a reader learns a credential participated without learning it and without being misled into thinking none was needed (RR-9); and a recipe explicitly **not** a claim of authenticity — trivially editable, travelling beside a verifiable witness rather than substituting for one (RR-10). Consumers: l1-competitive-execution CE-10, l1-model-runtime MR-15. Nodus counterpart = l1-nodus-observability HO-20. Concept-only (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-convergence-gate.md](specifications/l1-convergence-gate.md) | Convergence gate — put the deterministic check **where all paths converge**, not on each path that leads there, so a policy holds regardless of *who or what* produced the change, including producers nobody anticipated: guard the single boundary every change must cross to become real (durable, shared, shipped, installed) rather than each producing path, making coverage **source-independent by construction** — it catches the agent, a human's manual edit, an automation, an import, or a paste identically, closing the class INT-8 can only name (CG-1); the gate is deterministic code that always runs at its boundary, never an instruction a producer may skip, with an advisory layer allowed on top for legibility but never mistaken for the guarantee (CG-2); it **judges the delta a change introduces, not the accumulated state**, since an absolute-state gate blocks every change on pre-existing debt and is rationally disabled wholesale — a regression is blockable, standing debt is surfaced (CG-3); the enforcement moment is one deliberate choice, not the same policy stacked at many moments returning conflicting verdicts with no authority among them (CG-4); a **deferred obligation** lets an earlier event gate a later dependent effect, discharged only on genuine completion of the required action and never on an attempt (CG-5); an obligation left open at scope end is **surfaced, never silently dropped**, since a forgotten obligation is a policy that quietly did not apply, indistinguishable from one satisfied (CG-6); a **recorded, attributed bypass** — never none (a gate without one is removed at the first emergency) and never silent (an invisible bypass is no gate) (CG-7); fail-closed to block and fail-forward to observe (CG-8); the gate lives with the boundary so **every actor who crosses it inherits it**, including those onboarded after it was placed (CG-9); and it composes inline guards rather than replacing them, the inline guard telling a producer *now* and the boundary gate guaranteeing *regardless of producer* (CG-10). The architectural answer to l1-interception-model INT-8/INT-9. Nodus counterpart = l1-nodus-portability LP-20. Concept-only (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-review-checkpoint.md](specifications/l1-review-checkpoint.md) ; v1.1.0 RC-11/RC-12 added - **placement and payload**, the two properties RC-1...RC-10 left to whoever wired the checkpoint: where the position is a choice the checkpoint sits at the **latest still-reversible point** so the human is asked once, late, with everything prepared, bounded in both directions (never past the first irreversible/outward/expensive act, which carries RC-6's own gate, and never past a decision the remaining work depends on, since deferring a question past the work it gates answers it by assumption); and the checkpoint MAY lead with a **decision-ready brief** provided the brief **reaches** the artifact, the artifact stays authoritative, and it asserts no property the reviewer cannot check by following the path - sharpening RC-7, which forbids reviewing a claim *instead of* the work, not leading with a brief | Review checkpoint — the *solicited, in-band* human review: a declared point in a work item's lifecycle where progress **halts and the system asks** a human for a bounded decision before the item advances (“checkpoint” here a review station, not a state snapshot). The in-band solicited complement to out-of-band intervention (HI: human acts unasked, system detects), the human-grader case of iterative refinement (IR: automated grader, rubric verdict), and distinct from action-gating (AG: an *effect* gated by consequence tier, permit/deny only). Halt-and-ask, never advance-while-awaiting (RC-1); a **closed decision set with a revise arm** — approve / request-revision / reject — since approve-or-reject discards the common “yes, but change this” and forces substandard work through or a blind restart (RC-2); the producer neither configures nor clears the review of its own work, the actor with the strongest incentive to pass never holding the gate (RC-3, composes SEC-10); **request-revision returns to the same producer with feedback threaded and the prior attempt preserved** — never discarded, restarted, or reassigned by default, because that context is what makes a revision an edit not a rewrite (RC-4, composes IR-1); the revise↔review loop bounded with a declared honest exit so a demanding reviewer is finite (RC-5, composes IR-5); the terminal/expensive/irreversible step checkpointed **by default** with auto-approve an explicit recorded opt-out, friction defaulting on where a wrong output is costliest (RC-6, composes AG); the reviewer shown the **actual artifact, not a claim about it**, or silent incompleteness re-enters at the moment review exists to catch it (RC-7, composes completion-verification); every review attributed and recorded — who, what, what feedback, which attempt, when (RC-8); a held item pausing itself and its dependents but **never the whole office**, since a review that freezes everything gets disabled to keep moving (RC-9); and pending review a **distinct, visible, owned state** so the decision actually gets made (RC-10). Nodus realization = existing l1-nodus-dialog NL-12 pause + NL-14 grader-gated loop (no new dialog surface). Concept-only (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-model-adaptation.md](specifications/l1-model-adaptation.md) | Model adaptation — the office's **second plane of learning**: specializing the local model itself to the office's domain as the deep complement to the context-plane improvement it already does (rules/skills/prompts/harness), by distilling its own recorded successful work into a **small separable adaptation over a frozen base**. Two distinct planes never conflated — context-plane change instant/inspectable/edit-reversible, model-plane change distilled/opaque/detach-reversible (MA-1); a **frozen base with a separable delta, never a base mutation**, giving cheap/reversible/composable/attributable specialization where mutating the base gives none of the four and destroys shared-base dedup (MA-2); composable and per-task attachable so one base serves many domains rather than many base copies (MA-3); distilled from the office's **own verified** experience with provenance preserved, since training on unverified output amplifies the office's own errors into its model — the self-training collapse trap (MA-4); **earned and ratified, never automatic and never self-adopted**, with a gate *at least as strict* as the context plane's because an opaque model-plane change is higher-consequence (MA-5, composes PC-1/PC-2/SEC-10); **measured against the base on a held-out set and staged reversibly** (HX-8 acceptance + staged-rollout), never flipped to default on a training-loss number since lower loss measures fit to the shown data which may be the office's mistakes (MA-6); content-addressed/versioned/attested like any model artifact, an unknown adapter being an unverified capability (MA-7); reversible and non-destructive — detach returns exactly to the prior state, a bad adaptation removed by detaching not by training back (MA-8); **changes dispositions (fluency/style/format) not the knowledge horizon or correctness guarantees** and never relaxes a safety/verification gate, measured against ground truth not self-consistency since a more-fluent model is not a more-correct one (MA-9); optional, local-first (training data and adapter stay on-device), never a runtime dependency (MA-10). Realizes the MR-5 optional-adapters field's lifecycle; the model-plane sibling of pattern-codification (context plane). Concept-only (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-voice-output.md](specifications/l1-voice-output.md) | Voice output — the output half of the voice loop (text → speech), the symmetric complement to voice input, whose contract centers on the two things engine choice does not solve: **audio is a derived rendering of canonical text, never the source of truth** — editing the waveform as the record, or treating a transcript-of-the-audio as authoritative, is forbidden, since it creates two versions of what the office said with no authority between them (VO-2, composes derived-artifact / reproduction-recipe); and a **voice is impersonation-capable identity data**, not a stylistic parameter. On-device synthesis with no network dependency (VO-1, mirrors VI-1); a voice profile as a **provenance-bound identity artifact** with a recorded origin class — preset / cloned / generated (VO-3); **cloning a real person's voice requires a recorded consent-of-source**, since the system cannot verify ownership and must not pretend to — clone-without-consent is a **blocked** operation, not a silent capability, converting misuse from a default into a deliberate lie on the record (VO-4); synthetic speech **disclosed/watermarked** where it could be mistaken for a genuine recording of a real person, stripping the disclosure to misrepresent forbidden (VO-5); impersonation-without-consent and the prohibited-use classes (fraud, voice-auth bypass, non-consensual content, misleading official communications) an **enforced policy boundary** the managed tier can harden, not a footnote (VO-6, composes policy-governance / security); pluggable engine whose swap changes the sound never the identity/consent guarantees (VO-7, mirrors VI-7); on-device model lifecycle (VO-8, mirrors VI-8/MR-6); speaking an **explicit, gated, interruptible** output mode so the office never speaks unbidden (VO-9, mirrors VI-3, composes action-gating); rendering never mutating the canonical text, retryable and versioned with lineage (VO-10). The identity/consent/disclosure contract and the derived-rendering discipline are core; the synthesis engine, effects, and UI are out of scope. Concept-only (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-confidentiality-flow.md](specifications/l1-confidentiality-flow.md) | Confidentiality flow control — the **outbound** half of information-flow control (how sensitive is this, and where may it flow?), the orthogonal dual of the provenance/taint **integrity** axis (how trustworthy is this?) Cronus already tracks; the injection defense is inbound, this is the exfiltration defense outbound. Two axes never collapsed — a single trust label cannot hold a trusted-and-secret credential or an untrusted-and-public injection, the two orthogonal because the two questions are (CF-1); confidentiality an ordered lattice defaulting to **most-restrictive**, the don't-leak fail-safe dual of untrusted-by-default (CF-2); **sticky, most-confidential-wins propagation** through every derivation so a paraphrase of a secret does not launder its sensitivity — the exact dual of most-untrusted-wins taint (CF-3); **every outbound sink declares a capacity and over-capacity flow is blocked**, generalizing the single SEC-3 device-edge egress gate to every sink (a tool call, a log, an outbound message, a prompt an untrusted party can read) at its own capacity (CF-4); **both axes required** since the dominant attack chains untrusted-in to secret-out and an injection defense with no exfiltration defense is routed around not stopped (CF-5); confidential content carried as an **opaque reference** so an agent routes a secret it never holds in a leakable surface (CF-6); declassification explicit/authorized/recorded, never implicit or self-granted, the dual of explicit trust elevation (CF-7, composes SEC-10); enforced by **mechanism at the sink, not by instructing the model** since a prompt 'don't leak' is defeatable by the very injection this defends against (CF-8, composes INT-5); inspectable and auditable with blocked and permitted flows recorded (CF-9); local-first with the label side-band metadata that never leaks the value it protects (CF-10). Nodus realization = l1-nodus-language NL-21 (the confidentiality dual of NL-11 provenance). Concept-only (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-compensation.md](specifications/l1-compensation.md) | Effect compensation — the **third answer** to a committed-then-failed effect, the middle rung the recovery stack lacked between state rollback (crash-recovery, reversible internal state) and honest surfacing (conversation-rewind RW-5, irreversible and orphaned): **semantic undo by a declared forward inverse, never a state rollback** — a committed external effect (payment, provision, sent message) is cancelled in its *business meaning* by executing a different declared action (refund, deprovision, retract), not by restoring prior state which structurally cannot reach it, the two complementary and never conflated (CO-1); a per-step compensating action that is a **first-class fallible effect** passing the same gate, a step without one **honestly un-compensable** not silently assumed reversible (CO-2); only successfully-completed effects compensated, still-running work cancelled not compensated (CO-3); **reverse completion order (LIFO) as a correctness requirement** since later effects were built on earlier ones so forward-order undo acts on unremoved dependencies (CO-4); scoped to a declared compensation boundary, not reaching across unrelated scopes (CO-5); triggered by failure/cancellation/explicit request, **never automatic on success** where a completed scope keeps its effects (CO-6); bounded and its **own failure surfaced as an incomplete compensation, never swallowed**, since a failed compensation leaves the business effect live and reporting it undone converts a known problem into a hidden one (CO-7); idempotent and safe to re-drive after a crash mid-compensation (CO-8); recorded and auditable with the ledger showing both the effect and its countervailing compensation, never erasing history (CO-9); and an **honest net-effect statement** — what committed, what was compensated, what remains live — so a partially-compensated scope is never presented as cleanly undone, completing RW-5's surfacing with the remediation path it lacked (CO-10). Nodus realization = l1-nodus-language NL-22. Concept-only (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-host-native-rendering.md](specifications/l1-host-native-rendering.md) | Host-native rendering — the **outbound** distribution contract (the mirror of `l1-extension-marketplace`'s inbound path): how a definition authored once here is materialized into a **foreign host's own native format**, expanding the shape AFS-14 only names. One authored source with derived, never-edited renditions (HNR-1) regenerable from source alone so no information lives only in a rendition (HNR-12, the DAH-1 derived-only-never-authority argument); **named render contracts** whose output is byte-identical for the same source+format+version, two targets sharing a format only if their artifacts are identical, non-determinism a defect because it makes renditions undiffable (HNR-2); **declared fidelity classes** — per-unit / collapsed / opaque — that consumers **branch on** rather than infer from a file extension (HNR-3), with the loss a class cannot carry **declared at plan time** and a collapsed rendition never reported in terms implying per-unit behavior it cannot deliver (HNR-4); **section roles as the portability contract** — identity-vs-operation planes split on declared roles never prose heuristics, and a definition that classifies into neither flagged at authoring time when the author is still present (HNR-5); **stable machine slug distinct from the drift-prone display name**, every reference pointing at the slug, collisions a build failure, renames a governed migration (HNR-6, twin of XM-2); declared detection evidence / install scopes / destination templates derived by **every** consumer from one catalog declaration per DC-1, a hardcoded second target list being the defect that silently drops targets (HNR-7); **plan before write** — previewable, idempotent, per-source attributable so removal is credible (HNR-8); explicit **snapshot vs live binding** mode, the two having different truth semantics (HNR-9); selection as a first-class catalog filter, load-bearing on collapsed targets (HNR-10); consented enumerable out-of-workspace materialization that moves instruction text and **never authority** (HNR-11). §4.5 applies PD-1/PD-5 **across the system boundary**: a large catalog renders as a small fixed surface plus an on-disk index rather than N eagerly-resident descriptors, since eager host catalogs spend the *receiving* system's context budget. Composes l1-agent-framework-skeleton (AFS-14) / l1-extension-marketplace (inbound twin) / l1-derived-artifact-handoff / l1-declarative-configuration (DC-1) / l1-progressive-disclosure / l1-deployment-neutrality / l1-corpus-originality (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-corpus-originality.md](specifications/l1-corpus-originality.md) | Corpus originality — the **admission gate on sameness** for curated, selected-from corpora (roles, skills, archetypes, personas, templates, catalog entries), catching the one defect class every other gate is blind to: the **re-skin**, an admitted member re-published with its domain/platform/market nouns swapped, which is well-formed, accurate, individually defensible, and invisible to any reviewer who cannot hold the whole corpus in memory. Novelty as an admission criterion beside well-formedness, which is necessary and never sufficient (ORI-1); measured against **the corpus and the pending change set**, closing the two-identical-newcomers hole a corpus-only check cannot see (ORI-2); comparison over the substantive body with **find-replace-prone tokens neutralized** — proper nouns, platforms, markets, the unit's own name — since a comparison that treats a swapped noun as difference measures the disguise (ORI-3); **graded pass/warn/fail**, a single boundary forcing a choice between blocking legitimate neighbours and admitting re-skins (ORI-4); **thresholds calibrated against the corpus's own measured similarity distribution with a published baseline and stated margin** — a threshold with no published baseline is a guess wearing the authority of a number and gets deleted the first time it fires (ORI-5); the baseline **re-measured over time**, upward drift read as the corpus **converging** and answered by consolidation or codification, never by relaxing the gate into silence (ORI-6); findings that name the **nearest neighbour and the score** (ORI-7, TW-4 teach-or-be-suppressed); legitimate overlap resolved by substantive distinctness or a **declared** variant/specialization relationship that stores a delta instead of a second copy — never by threshold relaxation or blanket exemption (ORI-8, TW-7); **blocking to the agent, human-overridable with a recorded reason** (ORI-9, PC-2); **one mechanism at two moments** — admission gate plus whole-corpus convergence audit feeding MC-6 redundancy→merge, since a gate without the audit never repairs pre-existing duplication (ORI-10); and the stated cost model: the harm is **routing ambiguity** under PD-4 — two near-identical descriptors give a router no principled basis to choose — **not** storage or tidiness (ORI-11). §4.5 demarcates it from memory consolidation (observations, after the fact), AFS-13 generational novelty (a lineage in time, not a catalog at rest), and ROL-9 justification (reasons, not content) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-reasoning-spend.md](specifications/l1-reasoning-spend.md) | Reasoning spend — the accounting contract for **generation the principal pays for and never receives**: a deliberating model's internal reasoning, produced under the same call as the answer, billed at generation rates, consuming the same capacity, and on most providers never delivered at all. The system already owned the **dial** (`l1-generation-shaping` GS-3 depth modulation) and the **length budget** (`l1-generation-budget`) but never the **meter** between them, so a quantity routinely larger than the answer it precedes was implicitly folded into delivered output. A distinct unit class extending CR-2's per-class rates, justified by differing on delivery, amortization, governance, and reportability — a blended figure cannot answer *did the bill rise because answers got longer or because the model started thinking harder* (RSN-1); **shared capacity with deliberation produced first**, splitting GB-2's truncation signal into two causes with **opposite** remedies — answer-cut-off (escalate, continue from the partial) vs capacity-exhausted-before-the-answer (raise the cap or lower depth), where GB-3 continuation from a nonexistent partial silently re-spends the whole budget on deliberation again, the two separable by zero-delivered-output (RSN-2); three never-conflated visibility states content / count-only / **not reported**, the last a gap marker never a zero, since a turn that did not think and a turn whose thinking went unreported are the same number and opposite facts (RSN-3); **not amortized by prefix reuse** (IC-1 cheapens re-ingesting context, never thinking again), so a cost model projecting warmth-driven decay under-forecasts worst on the deepest turns (RSN-4); depth as a **declared fidelity axis** moving cost and quality together, making cross-depth figures incomparable without the depth (RSN-5); attributed under OAC-1 **especially on turns that produced nothing** — the case most likely to be dropped from a denominator and most distorting when it is, since dropping it makes an unproductive session look efficient (RSN-6); **separately reported under measurement** (MB-3 discipline), since equal answers with a tenfold deliberation gap are economically different (RSN-7); visible reasoning is untrusted model-authored **trace, never a record of action** — a claim is never verified against it, the acting record is the effect record (RSN-8); counted against the provider allowance so a readout has no unexplained falling balance (RSN-9); honest degradation as **declared lower bounds** when unmeasured, CR-4's fail-visible discipline applied to the **count** rather than the rate (RSN-10). §4.5 demarcates it from the system's own inner-monologue channel — separate budgets, and an inner-monologue call has its own reasoning spend under this contract. Composes generation-shaping / generation-budget / cost-rating / usage-allowance / outcome-attributed-cost / model-benchmarking / inference-cache / fidelity-variants / claim-verification; the nodus realization is `l1-nodus-environment` NE-15 (declared budget scope) | Stable | 1 | 1.0.0 |
| [l1-scoped-generalization.md](specifications/l1-scoped-generalization.md) | Scoped generalization — **where a learned pattern applies**, the axis the codification pathway never carried. PC-1/PC-4 measure recurrence across **time** and decide *bindingness*, so five observations on five days inside one project clear every existing gate and yield a project convention wearing the authority of a law; the leak is confident (indistinguishable at the point of use from an earned rule), invisible (nothing fails — the agent just works in the wrong style), compounding (mis-scoped rules shape the next observations), and self-discrediting (the user's conclusion is *the learning is unreliable*, and the well-earned rules get disabled along with it). Born narrow with an absent scope **unusable rather than universal** (SG-1); **durable context identity** surviving relocation and distinguishing coinciding things — HNR-6's argument applied to contexts, since an identity that is really a location orphans patterns when the thing moves and cross-contaminates when two things share a path (SG-2); **widening requires independent recurrence** — repetition *within* one context evidences the pattern is real, never that it generalizes, and independence is a **declared judgment** with a stated reason since two checkouts of one repository are one context (SG-3, the load-bearing invariant); widening a distinct **ratified** act recording which contexts supplied the evidence, never a counter crossing a threshold (SG-4); **contradiction narrows rather than excepts**, a wide rule with a growing exception list being one whose behavior nobody can predict (SG-5); scope checked **at application** and not only at storage, since a write-time filter leaves the corpus permanently contaminated by everything predating the model and by every import (SG-6); **imported patterns arrive narrow** regardless of the scope claimed at source — another corpus's *global* is evidence about their world and a hypothesis about yours (SG-7); cross-context leakage a **first-class defect worse than not learning**, detectable and reportable (SG-8); candidate decay so the pending pool never becomes a second unaudited rule corpus whose size later argues for bulk promotion (SG-9); scope explainable at the point of application, an unarguable rule being either obeyed blindly or disabled wholesale (SG-10); and **scope orthogonal to bindingness**, since collapsing the axes forces *important* to mean *everywhere* (SG-11). §4.5 demarcates from IMP-2 generalize-at-capture (what *leaves* vs how far what stays may *reach*). Prior worked instance recorded: nodus LP-3's two-independent-host rule is SG-3 for the library-extraction case | Stable | 1 | 1.0.0 |
| [l1-output-repair.md](specifications/l1-output-repair.md) | Output repair — the **deterministic post-generation correction layer** every model-consuming system grows and almost every one grows *anonymously*: a `.replace()` in a parser fixing leaked tool-call syntax emitted as prose, an internal field escaped into user text, a misplaced marker, an unparseable envelope. The repairs are legitimate; what is missing is that each one is **evidence** — a claim that a specific model misbehaves in a specific way, whose firing rate answers questions nothing else does. Named declared repairs with no anonymous fixups, since an unnamed correction cannot be counted, tested at its trigger, retired, or attributed (RPR-1); **form only, never meaning** — a transformation that changes what the output *says* is an unreviewed edit by a component with no authority to make it (RPR-2); deterministic, idempotent, order-declared (RPR-3); **every firing counted and attributed to (repair, provider, model, version)**, since an uncounted repair converts a measurable model defect into invisible maintenance and makes *did the new model get better* unanswerable by construction (RPR-4); **both rate extremes actionable** — never-fires is dead code encoding an unchecked guarantee, always-fires is an upstream prompt/contract defect being papered over while its evidence is hidden (RPR-5, NEG-10 discipline); **silent to the model, never silent to the record**, because a corrected output indistinguishable from a correct one destroys exactly the information the layer produces (RPR-6); **no streaming counterpart means no protection on the streaming path** — a repair written over a complete string silently does not apply to the incremental path the product usually ships, so the streaming form is declared or the gap is (RPR-7); repair for decidable **form**, contract validate-and-retry for **semantics**, where repairing a semantic defect is fabrication and retrying a form defect is waste (RPR-8); **repairs as model-scoped hypotheses** recording the versions they were observed against and re-opened on a model change, since a stale repair corrupts output a new model produces legitimately (RPR-9, SG-3 argument); **a repair never widens trust or promotes content to control** — a helpful reconstruction of a malformed control token is the injection TB-5 exists to prevent (RPR-10). §4.4 reconciles silent form-editing with CV-6's never-silently-edits by locating the boundary at meaning; §4.5 nodus disposition: no new invariant, the layer is host-side behind the model-provider seam | Stable | 1 | 1.0.0 |
| [l1-computed-grounding.md](specifications/l1-computed-grounding.md) | Computed grounding — **the model narrates, the code decides**: where a request's factual core is mechanically computable from state the system already holds (which of my things are in state X, how many, what total, when last), the system computes it and the model is reduced to **expressing** it. Asking a model to derive a filter-and-count from serialized context when the exact operation is one call away is a design defect, not a prompting problem — and the failure has an unusually trust-destroying shape because the wrong answer is *specific*, inviting belief rather than checking (CGR-1); the computed result enters as a constraint that **states its own authority**, since a computed value offered as mere context is one input among many and loses to a plausible derivation, and it must say exactly what to reproduce with nothing added or dropped (CGR-2); **narrow, conservative, observable detection** — a false fire is **worse than no grounding**, forcing a confidently wrong answer carrying full authority, so the detector is tuned against that asymmetry rather than for coverage, and the pressure to widen always arrives as a report about a miss and never about the invisible false fire (CGR-3); computed from the **same snapshot** the model reads, or the constraint contradicts the context and forces a choice between two authorities (CGR-4); **empty as a first-class answer**, the case a model handed a question is most tempted to fill (CGR-5); **extension fixed, expression free** — the split that makes the same question in another language return the same set while keeping the answer from degenerating into a table (CGR-6); ungrounded as a **declared** degradation, since a silent downgrade turns a known reliability boundary into an unknown one (CGR-7); **prevention and verification both kept** — grounding is narrow and nearly free, claim-verification is general and pays for the wrong answer before catching it, so neither may be dropped for the other (CGR-8); grounded-ness inspectable, without which neither the model's real accuracy nor the layer's coverage is measurable (CGR-9). The generation-side sibling of OL-7's exact-grounding-not-similarity-recall; §4.5 nodus disposition: no new invariant, a computed value is an ordinary bound input | Stable | 1 | 1.0.0 |
| [l2-tool-receipts.md](specifications/l2-tool-receipts.md) | Implements l1-tool-receipts. The realization of model-unforgeable per-action execution receipts as a **keyed-BLAKE3 MAC over a length-prefixed action binding**, closing a gap found and disclosed during the developer-office build rather than hidden. Zero new crates — `blake3` is already a `cronus-domain` dependency and already on the domain boundary-guard allowlist, which is what makes TR-1's unconditional coverage affordable. Domain/facade tier split on the established `*_bootstrap.rs` precedent: deterministic `ReceiptKey`/`ActionBinding`/`mint`/`verify` in `crates/domain/src/tool_receipts.rs`, OS entropy + dispatch wiring in `crates/core/src/receipts_bootstrap.rs`. **Corrects a real hazard in the parent's reference sketch**: literal concatenation is forgeable by field-boundary shifting, so the binding is length-prefixed and injective, domain-separated, with a constant-time tag compare (TR-2/TR-3). TR-1 held **structurally** — `Receipted<T>` has no public constructor and `ReceiptedDispatch::invoke` is the only execution path, so a receipt-free path does not compile (the BA-4/OA-4/DVO-3 lineage applied to execution authenticity, and what an append-to-a-log baseline can never provide). TR-4 realized as a **default-deny on the fact-recording path** via `ReceiptLedger::status`, explicitly not prose parsing: no API exists that upgrades an unreceipted claim to fact. TR-8's `Pending` is a distinct state, never a receipt over a placeholder result — that would be a valid receipt for a false claim. TR-5's key is ephemeral, non-`Debug`/`Serialize`, zeroed on drop, with the volatile-write residual stated honestly; TR-9 strengthens the existing SEC-7 audit log and records a verify mismatch as its own auditable event. nodus: no new primitive — HO-9 stores and echoes an opaque host token, never mints | Stable | 2 | 1.0.0 |
| [l1-scoped-capability-layers.md](specifications/l1-scoped-capability-layers.md) | Scoped capability layers — the **visibility half** of the plugin architecture, which `l1-extension-points` (where a contribution attaches) and `l1-extensions` (what it is) never covered: whom a contribution is visible to. One registry with per-actor layers, never a registry per actor, since forking multiplies the surface by the staff count and drifts on the first amendment (SCL-1); the registration site drives **visibility and lifetime as one fact**, making "visible here, disposed there" unrepresentable (SCL-2); one parent chain read in **opposite directions** — supply inherits down, observation admits up — because a composition supplies what it composed and observes activity from it (SCL-3); nearest-wins shadowing as **replacement, never merge**, a surface assembled from halves of two authored things having no author (SCL-4); governing contributions read **exact-scope**, since inherited governance is a decision nobody made and transitivity belongs to the guard plane INT-5 (SCL-5); restrictions narrowing the shared set by **intersection**, scope-local merging after, so restriction and contribution never race (SCL-6); a filtered-away capability **absent from the surface and refused on invocation**, both halves required (SCL-7); a bounded **setup window** closing before publication whose occupant registers but never drives, with whole-creation rollback (SCL-8); scoping declared **not** an authority boundary, stated in the contract because a mechanism shaped like isolation is used as one unless it says otherwise (SCL-9); isolation claimed only by planes that declare they file by scope (SCL-10); reachability inherited from the minter and narrowed by minting narrower (SCL-11); per-plane default membership declared, an undeclared default failing as silent over-capability (SCL-12); lineage carried as **data** so a topology change is never a capability change (SCL-13) | Stable | 1 | 1.0.0 |
| [l1-composition-binding.md](specifications/l1-composition-binding.md) | Composition binding — the **runtime half** the role/archetype catalog specs never covered: what happens when a composition file is edited while sessions run on it. A composition mounts **once** and is joined by many (CBD-1); a binding names a **generation**, and a joined generation outlives its source being edited or deleted, because an actor whose world follows its file has a history its own record cannot explain (CBD-2); a child joins its parent's exact generation **by reference**, never re-resolving the name — re-resolution hands it a different generation after an edit and fails it outright after a delete while the parent runs on (CBD-3); **started-with** is a frozen creation fact and **runs-on** is resolved by every reconstruction path, since reading the header rebuilds a switched actor under a composition that cannot account for its own recorded actions (CBD-4); a rebind is a durable event appended after it commits (CBD-5); rebinding permitted only while the actor has produced nothing, checked where the history is in hand (CBD-6); rebind atomic and restoring, two compositions never coexisting (CBD-7); **two dispositions** for an unusable artifact — an unclaimable identity skipped, unloadable content *listed with its reason*, because a skipped artifact still occupies its identity while every surface shows nothing to delete (CBD-8); discovery re-read rather than memoized (CBD-9); **copy-only authoring**, which can mint neither an unloadable artifact nor capability (CBD-10); containment as a property of the **identity** rather than a later path check (CBD-11); display metadata forbidden from claiming identity or trust (CBD-12); declared per-kind resolution anchors (CBD-13) | Stable | 1 | 1.0.0 |
| [l1-composition-layering.md](specifications/l1-composition-layering.md) | Composition layering — assembling the **set of components** from ordered layers; the set-valued sibling of `l1-declarative-configuration` DC-11's field-valued resolution, demarcated in §4.6. Author-assigned stable entry identity as the **sole** reconciliation basis, since a generated identity turns every edit into remove-plus-add and restarts components nobody changed — a correctness-preserving failure nobody can attribute (LAY-1); **whole replacement, no deep merge**, because merge semantics for absent keys, explicit nulls, and collection order are decided by an algorithm no layer author reads (LAY-2); disabled as a durable state of the entry rather than its removal, present-and-off and absent being different facts with different remedies (LAY-3); an override matching nothing **reported**, since typo and base-changed-underneath are both actionable and present identically (LAY-4); empty declaration distinguished from absent, a parse-to-nothing being an error because a blanked file is more often a mistake than a statement (LAY-5); **one** composition algorithm serving the run path and every inspection surface, two implementations agreeing until the moment someone needs the report (LAY-6); a distributed unit contributing into the stack so its insertions stay overridable from above (LAY-7); ownership decided **structurally** — exact match to the shipped form is system-owned, any deviation user-owned and untouched — a stored "modified" flag being a second source of truth wrong in both directions (LAY-8); declared total layer order (LAY-9); transactional recomposition leaving the last good set running on failure (LAY-10) | Stable | 1 | 1.0.0 |
| [l1-execution-locus.md](specifications/l1-execution-locus.md) ; v1.1.1 links-only - added l1-remote-liveness-verdict to Related Specifications: RLV-8 states the same-underlying-resource-one-registration rule this spec's substitution model assumes, and RLV supplies what a locus's liveness capability returns once its channel (not its capability set) becomes unavailable; no invariant changed | Execution locus — the previously-unmodeled **execution-host dimension**: one coherent world (filesystem, process table, network position, acting identity), with **the locus as the unit of substitution, never the individual capability**. Every world-touching capability declares its locus and none assumes the host (LOC-1); co-resident capabilities move **as a set**, since a shell in one world and a file reader in another yield tools that individually succeed and jointly describe an impossible world — a failure that errors nowhere, spends the agent's turns, and produces a confidently wrong account (LOC-2); handles are locus-bound and crossing is an explicit transfer (LOC-3); moving the locus must not **fork the consumers**, which is the leverage — one substitution, whole-product effect, no remote variants of shell/file/terminal/indexer (LOC-4); the active locus is observable and **stamped** on every artifact claiming something about a world, an unstamped observation being evidence about nowhere (LOC-5); a capability absent from the active locus refuses visibly and **never falls back**, a host fallback recreating the split world at the exact seam the locus closed (LOC-6); confinement composes with a locus and never defines one, conflating them producing the unmade claim that remote is safe (LOC-7); locus selection is a **composition** decision, not a per-call argument that hands the choice of where effects land to the least accountable participant (LOC-8); world-derived state keyed by locus and invalidated or explicitly carried, which is what stops a stale local index answering confidently about a remote world (LOC-9); a locus declares the operational properties that change how it must be used (LOC-10). Demarcated from `l1-execution-sandbox` (how confined) and `l1-environment-lifecycle` (does the instance exist) ; v1.1.0 LOC-11/LOC-12 selection across many surfaces — **one decider per application** delivers the default locus to every surface (N surfaces deriving it drift the moment inputs change at different times, giving the product several simultaneous internally-consistent opinions about where work runs), changed by exactly one gesture; and two declared binding disciplines — a **preference** resolves to its target while it can serve and to the default while it cannot, auto-following and returning on its own (safe because it holds no locus-bound state), while a **binding** is fixed for its container's lifetime because the container holds LOC-3 handles, so relocation is **clone-not-migrate** and availability is established at creation rather than polled (§4.6) | Stable | 1 | 1.1.1 |
| [l1-peer-compatibility.md](specifications/l1-peer-compatibility.md) ; v1.1.0 PCO-15 added - a **content or meaning change on a structurally unchanged surface** is a compatibility change PCO-5's structural fingerprint cannot see by construction: a field the emitter quietly stops populating, a value whose meaning/units/nullability shifts with no type change, or a message that starts/stops flowing on an existing path with no new opcode produce **no structural diff to key on**, and PCO-7's parse-tolerance rule doesn't apply either since the field is present and well-typed. Declared **on the contract itself** as a reviewed claim, the same way PCO-11 declares emitter behavior no structural diff can see; undeclared, the change is breaking and silence is never read as the compatible case | Peer compatibility under **version skew** — the contract between two independently released halves of one product (core↔frontend, engine↔mobile client, office↔remote office, host↔extension, runtime↔workflow bundle) that are never guaranteed to be the same age. Skew is the normal operating state, not a fault, and compatibility must be *implemented and machine-verified* rather than asserted in a changelog. Contract, distribution and persistence versions are three independent systems (PCO-1); the surface is a set of **independently versioned capability lines**, never one protocol version that lets the most volatile capability set the floor for all (PCO-2); **the newer peer adapts and the older peer never learns**, which bounds each peer's obligation to its own history instead of the population — three of five negotiation arms are yes-because-I-am-older (PCO-3); compatibility is a declared **executable transform**, so a hole in the chain is an incompatibility not a documentation gap (PCO-4); the version label is verified against a normalized structural fingerprint, removing the author's ability to adjudicate their own diff (PCO-5); **direction decides severity** — the same value-set growth is safe inbound and fatal outbound, because the older peer decodes what it is handed unconditionally (PCO-6); **parse tolerance is not compatibility** — optionality proves the parse succeeds and nothing about whether the payload carries the key or whether every consumer runs the parse (PCO-7); a fail-closed **floor** plus an **optional** surface degrading per capability as *unsupported* or as an adapted *fallback*, with new capabilities barred from the floor (PCO-8); the oracle compares against surfaces captured from **released artifacts**, immutable and never edited to make a check pass (PCO-9); severities copied from the transport's observed failure behaviour (PCO-10); emitter properties no fingerprint can see recorded as reviewed declarations, silence never read as the claim (PCO-11); exceptions as identifier-agnostic standing policy, never per-change sign-off (PCO-12); a static mirror pinned to the runtime oracle by test (PCO-13); and a verdict naming capability, both versions and **which side to upgrade**, derived per capability (PCO-14). Demarcated from surface parity (same-age divergence), extension points (seam definition) and record evolution (stored meaning); nodus mapping supplies LP-23 (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-record-evolution.md](specifications/l1-record-evolution.md) | Record evolution — what a stored or transmitted record means to a reader that did not write it, and how its shape may change without destroying that meaning. **The reader population sets the regime, not the record's contents**: same-build, independently-shipped, or **re-publishing** — the strictest, where whatever a reader drops on the way through is *destroyed*, not ignored, and under content addressing a lossy pass mints a new identity for an unchanged cohort (REC-1/REC-2). An **interpreted core** versus an **opaque writer-private region** with strictly one-way promotion makes the free lane the default and keeps the committed surface small (REC-3); designated-slot **variant passthrough** retains the intact subtree and reclassifies variant *addition* to compatible, with tolerance explicitly not inherited by nested slots (REC-4); **residual capture** of unmodelled fields makes losslessness mechanical rather than a discipline future authors must remember, under an enumerated capture-level manifest whose identifiers are never reused (REC-5); tolerance is bounded to absent vocabulary and never to damage — a known-but-malformed variant still rejects (REC-6); **separate authoring and acceptance schemas**, because reading with the authoring schema makes the tolerant path unreachable in exactly the case it was written for, and unreachable in tests too (REC-7); refusal taken on the cheapest already-held signal before any fetch, making do-no-further-work structural instead of a remembered early return (REC-8); a minimum-reader declaration reserved for changes an older reader cannot safely *interpret*, never for preservation (REC-9); **openness decided by reader population**, with the observation that one strict leaf defeats a tolerant envelope and rejects the whole record (REC-10); a content address proves the bytes while a separate cross-check proves they mean what the referrer assumed (REC-11); a **frozen wire-form guard that fails on all drift including compatible additions**, generated never hand-edited, with the wire form projected explicitly and the projection itself asserted since a frozen surface can be frozen and wrong (REC-12); record/contract/distribution versions independent (REC-13). The persistence twin of l1-peer-compatibility (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-resumable-subscription.md](specifications/l1-resumable-subscription.md) | Resumable subscription — the contract between a durable **ordered log** and a subscriber that will disconnect and come back (session transcripts, agent inboxes, activity feeds, run traces, communication views). Append-only immutable records give a durable position identity (RSB-1); the **durable write is the source of truth and live notification is only a wake-up**, so a dropped notification costs a reconnect and never an event (RSB-2); the record's own position **is** the resume cursor — no side-channel sequence, no window re-request, no de-duplication — and the cursor tracks what was *applied*, not received (RSB-3); the guarantee is precisely bounded to exactly-once, ascending, gap-free above the cursor **for representable records**, and nothing else (RSB-4); an unrepresentable record is **skipped and the cursor advances**, because holding it stalls the subscription permanently behind one row and retrying loops — a negotiated version is the real remedy, the skip the interim (RSB-5); therefore **a position gap is not evidence of loss** and must never be retried, since the skip is deterministic and recurs (RSB-6); **framing carries no world semantics** — a bounded initial batch is a transport decision, batch-ended is not caught-up, and novelty derives from record content and subscriber state, never from which frame carried a row (RSB-7); closed vocabulary only where an unknown value makes the record unreadable, open where it merely annotates, which decides whether an addition costs a version (RSB-8); a cursor predating retention is reported explicitly, distinctly from nothing-new (RSB-9); independent logs merged consumer-side with no invented global order, safe exactly because causally related records share one log (RSB-10); per-source degradation so one failing source never darkens a multi-source view (RSB-11). Demarcated from the in-process event mesh (EM-9), retention, work liveness and device sync (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-inverse-derivation.md](specifications/l1-inverse-derivation.md) | Inverse derivation — running the production arrow **backwards** over work the office did not produce: from delivered artifacts to the units of work, the plan, the framing, and the originating intent, using the work's own record as evidence. The **episode** as the unit of reconstruction — a completed arc with a recoverable before-state, an after-state, executable checks, and a trajectory — never a whole product, which has no checkable starting point, no bounded cost, and no possibility of replay (IVD-1); every recovered link graded **attested / inferred / assumed**, with grades non-inheriting along the chain, because the most confident-sounding parts of a reconstruction are typically its least supported — nobody wrote down what nobody thought (IVD-2); the **trajectory as first-class output** — attempts, reversals, abandonments, ordering — on the grounds that the finished artifact already shows the answer and only the record shows the search (IVD-3); reconstruction derived, re-derivable, never authoritative over or mutating its source (IVD-4); an explicit human-edited registry of **pinned, fingerprinted** entries carrying its own version, closing the silent-corpus-drift hole where a maintainer rewrites the history a revision points at and every measurement taken against it is invalidated with no error reported — on mismatch the entry is **suspended** and dependent measurements marked unsound, never silently re-pinned to whatever the source now holds (IVD-5); automated discovery permitted to **propose** candidates and forbidden to admit them (IVD-6); selection on **record quality, not product popularity** — proxy metrics rank product success, which is near-uncorrelated with reconstructability, so a proxy-ranked corpus fills with exactly the sources that cannot be reconstructed (IVD-7); mechanical prefilter plus an irreducible human judgment on sampled episodes (IVD-8); foreign record content treated as untrusted data under CP-1/CP-2, since reconstruction reads adversarial material by design (IVD-9); thin records degrading honestly with reported coverage rather than narrative gap-filling (IVD-10); output abstracted to **method**, verbatim carry-over refused through the ORI-1/ORI-3 gate (IVD-11); materialized corpora offline-complete (IVD-12); bounded runs naming the links they did not reach (IVD-13). Demarcated from code comprehension (artifact vs process), `l1-reproduction-recipe` (recorded by the producer vs asserted by a reader), and `l1-specialty-exemplars` (authored vs recovered) | Stable | 1 | 1.0.0 |
| [l1-capability-leveling.md](specifications/l1-capability-leveling.md) | Capability leveling — the closed loop that **narrows the spread between models on long-horizon construction work** by replaying reconstructed episodes and converting each divergence into a durable, transferable correction. Objective stated as **spread collapse at a bar, never reproduction**, with similarity-to-exemplar explicitly rejected as an optimization target since it selects for imitating one body of work (LVL-1); a measured **two-part marginal stopping rule** — the weakest performer stopped rising and the spread stopped narrowing — replacing the non-terminating "iterate until identical", which is unreachable because two competent engineers never produce identical work — and reporting **which of two endings** it reached, converged at the bar or **plateaued below it**, the latter being the loop's most misleading state since a collapsed spread among models that all fail puts the headline metric at its best exactly where nothing was achieved (LVL-2); comparison over outcome **and recorded trajectory**, with an implausibly straight path recorded as a **contamination signal rather than an outstanding result** — the structural answer to a framing reconstructed from finished work already knowing the answer (LVL-3); mandatory four-way cause classification of every divergence — knowledge / capability / framing / **arbitrary** (LVL-4); arbitrary divergences discarded with an explicit **retention bias** on uncertain calls, justified by asymmetry: a wrongly-kept lesson is visible and falsifiable, a wrongly-discarded one is silently gone (LVL-5); **carrier chosen by who executes the lesson** — the five-rung ladder from enforced check through enforced procedure, triggered instruction, and standing rule to precedent — because filing every lesson as instruction puts it in the highest-variance carrier for the problem *defined* by execution variance (LVL-6); executable carriers gated on an **objective oracle**, judgment lessons kept instructional rather than converted into mechanical proxies that get optimized against (LVL-7); hardening refused where it degrades strong performers, forbidding leveling by ceiling loss (LVL-8); laboratory isolation defined by **where carriers are materialized**, since retrieval does not read draft markers (LVL-9); a **single human ratification gate on the assembled body of lessons** at the boundary all lessons cross, per-lesson ratification rejected as rubber-stamping that reads as authority while providing none (LVL-10); scope earned across **independent** contexts, which is what gives varied technologies and domains their instrumental role and what stops one exemplar's house style being promoted to universal practice (LVL-11); evidentiary grades inherited from the reconstruction and surviving aggregation (LVL-12); **one learning machinery** — a new source of experience for the existing distillation surfaces, never a second store whose winner is decided by retrieval accident (LVL-13); **context plane only**, no weight adaptation, canonical lessons rendered per host with honest degradation (LVL-14); budgeted runs reporting unreached episodes and models rather than averaging partial rounds as complete (LVL-15); episode de-duplication before measurement, so a corpus does not measure one situation repeatedly and report it as breadth (LVL-16); measurements comparable only within a named corpus version (LVL-17). Demarcated from `l1-simulation`, `l1-evaluation-suites`, `l1-model-benchmarking`, `l1-specialty-exemplars`, and `l1-competitive-execution` | Stable | 1 | 1.0.0 |
| [l1-evidence-archive.md](specifications/l1-evidence-archive.md) | Evidence archive — the durable, immutable, device-local record of **what was said and done**, kept beneath every reduction that acts on it. Resolves the asymmetry in which a user's deliberate rewind is non-destructive (RW-3) while the system's automatic compaction is not: archive-before-reduce as a precondition, not a side effect (EA-1); reduction artifacts become **addresses** into the archive rather than replacements for it, turning compaction into an indexing operation (EA-2); expansion on demand as an ordinary capability, not a recovery procedure (EA-3); append-only immutability with supersession instead of edits (EA-4); removal only by explicit user act or declared budget, always reported, never a silent thinning or a plausible partial (EA-5); device-local and never a source for any outbound path (EA-6); secret-safe on the way **out**, not on the way in — a redacting write destroys the fidelity the record exists for without un-happening the secret (EA-7); queryable with an honest horizon, OR-7's discipline on the non-numeric plane (EA-8); four-plane separation — evidence (said/done) vs memory (learned) vs ledger (decided) vs diagnostics (machinery) (EA-9); evidence never ambient, the archive is what makes keeping *less* in context safe (EA-10); provenance survives the round trip so storage is not laundering (EA-11); cheap, bounded, non-fatal — an archive failure fails the reduction, never the turn (EA-12). Claims the non-numeric retention plane `l1-observation-retention` scopes out and no other plane owned (diagnostics ephemeral DL-5, memory pruned MEM-5, receipts session-bound TR-5); main-only host concept (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-fanout-attestation.md](specifications/l1-fanout-attestation.md) | Fan-out attestation - **concurrency is a claim, and a claim needs evidence**: a coordinator that launches one worker, waits, then launches the next produces the same artifacts, logs and completion report as one that genuinely fanned out, so the failure is invisible until a deadline is missed. The **launch barrier**: an episode declares its exact member set, records a **host-issued** launch handle per member, and **seals before the first wait** (FAN-2) - an ordering the serial launch-wait-launch pattern structurally cannot satisfy; handles are host-issued, never coordinator-authored, and a foreground result recorded as a start is a forged attestation (FAN-3); a partial launch keeps the episode **open** and ends in retry or a reasoned **terminal, non-successful abandonment**, never a seal around the gap (FAN-4); the attestation **bounds its own claim** to acceptance ordering and proves nothing about wall-clock overlap, write isolation, worker honesty, or integration (FAN-5); a return is scheduler completion, not verification (FAN-6); rolling episodes bounded by host concurrency, results reported in **declared** order not completion order (FAN-7); a **declared sequential fallback** where the host cannot fan out, never a parallel description of a serial run (FAN-8); native scheduling over detached process farms that surrender cancellation/observability/cost attribution, with no episode opened where no per-member identity exists (FAN-9); append-only episode state refusing impossible transition sequences (FAN-10); check-level parallelism excluded from attestation (FAN-11). **Orthogonal** to the coordination family - parallel-staffing, competitive-execution and deliberation each decide *what* is fanned out and *how fan-in resolves*, none governs the launch act, and all three can claim a concurrency they did not perform. Nodus realization: NL-24 (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-acceptance-oracle.md](specifications/l1-acceptance-oracle.md) | Acceptance oracle integrity - the criterion that decides *done* is **the one artifact in the verification stack that nothing checks**, and every layer beneath it faithfully certifies whatever it says. Distinguished sharply from CMP-4 (which forbids a *weaker but real* proxy): this forbids an instrument **structurally incapable of failing**, and moves enforcement to **authoring time**. Criteria precede the work and are what completion is judged against (AO-1); one **observable outcome** per criterion, never an activity that cannot be failed (AO-2); **runnable or judged, declared** - a half-bound criterion is refused, never silently downgraded (AO-3); success signalled only by what success can produce, requiring **both** successful termination and a distinctive signal, since failure output uses ok/done/passed/0 as readily (AO-4); an **absence claim needs a positive control** - untested, it is indistinguishable from a check looking in the wrong place, the most common way a green result carries no information (AO-5); a supplied figure is derived from the source of truth, never restated as its own proof (AO-6); a **mandatory-as-a-step, advisory-per-finding, non-executing falsifiability review** catching the mechanical classes before work starts, usable as a criterion of its own (AO-7); a mostly-judged set is a **disclosed condition**, not mechanical verification wearing a checklist's clothes (AO-8); judged criteria carry consequence-proportional evidence and are **never assumed low-risk** - checkability and consequence are independent axes (AO-9); an empty or malformed set is **refused, never satisfied** - *nothing to check* must never reduce to *everything checks out* (AO-10); stable explicit criterion identities, never position-derived (AO-11) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-consent-binding.md](specifications/l1-consent-binding.md) | Consent binding - the identity and lifetime of a grant, the question action-gating leaves unasked after deciding *how much* friction an act deserves. Consent binds the **fully resolved invocation** (operation, expanded arguments, location, interpreter, ambient name-resolution environment, limits, platform), never a name, category, tool, or intention (CB-1); the approver is shown the **resolved** form, since consent for the authored text is consent for an act the system will not perform, and the difference is exactly where the dangerous cases live (CB-2); **any change to a bound element lapses the grant** - no partial reuse, no close-enough match (CB-3); an unmatched act is **disclosed and stopped**, a property of the unmatched state and never a permanent dry-run mode (CB-4); consent is **authorization, never containment** - the permitted act keeps its full ambient reach, and presenting a grant as isolation misleads exactly the person being asked to decide (CB-5); the **coverage boundary is disclosed, not implied** - a grant does not reach called scripts, fixtures, or dependencies, so a byte-identical act runs under an old grant when its inputs change, and a workflow needing that enforced puts the inputs' identity *inside* the bound invocation (CB-6); grant records are principal-owned, stored outside the material they authorize, integrity-checked, and **fail closed** (CB-7); reviewed material is **data**, never instruction, and can never cause its own approval or install its own approver (CB-8); consent is **never evidence of correctness** (CB-9); grants and lapses are both auditable, the lapse naming the changed element (CB-10); grants are narrowable by the principal and **never self-widening** by the holder or the act (CB-11). Scopes AG-6 learned de-escalation, so the mechanism spending least approver attention is the most exact about what it covers (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-context-transition.md](specifications/l1-context-transition.md) | Context transition - **which disposition of a working context, and when taking one is legitimate at all**: the decision the token-economy family (compression, summarization, handoff, delegation) specifies transforms for and never makes. Every transition converts a **primary source** into a secondary one and what it loses is the *why* - an artifact records what was decided, never the alternatives rejected on the way (CXT-1); the decision belongs **at a work boundary and nowhere else**, so a budget threshold *signals* and never *fires*, because a mid-chunk reduction removes exactly what the work is still using (CXT-2); a closed, **cost-ordered** disposition set - continue / discard / externalize / delegate / compress - first admissible wins, no reordering and no sixth rung (CXT-3); continue is examined first as the only free move (CXT-4); discard requires **positive evidence of irrelevance**, absence of a reason to keep is not evidence (CXT-5); externalize only for a real crossing - host, location, principal, forked side thread (CXT-6); delegate only what runs unattended and returns a report, the one rung leaving the caller's primary source intact (CXT-7); compression is the default and **never the first reach**, bound by being directed at what the next chunk needs (CXT-8); hiding later work needs a real context boundary - an inline call leaves them resident and clears nothing (CXT-9); a declared **sharp-reasoning band**, transition taken at the nearest boundary before it is exhausted (CXT-10); every transition recorded with what it converted (CXT-11); the order is the contract, the verdict is not claimed mechanical (CXT-12) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-capability-reachability.md](specifications/l1-capability-reachability.md) ; v1.1.0 REA-11 added - reachability is a property of the **(capability, executor) pair** wherever more than one executor may do the work: REA-1 answers whether an actor may start a capability, not **which** actor holds it, so a plan naming a capability names the executor it was **established on** (establishing it on the commissioning side proves nothing about the side that will run it), **asymmetry is recorded rather than discovered at handover** and is a legitimate routing reason, and a capability never exercised in the **invocation mode it will actually run in** - typically a non-interactive/headless mode that loads and resolves differently from the interactive one - is an assumption, not an establishment; discovery of what an executor holds is **input to a plan, never an activation** | Capability reachability - **who may invoke a capability**, the property progressive disclosure and the extension-point model both presume and neither declares. A closed two-class partition: **agent-reachable** (resident descriptor, actor may initiate, composable by other capabilities, standing context cost) versus **human-only** (no descriptor, only a person, reachable by nothing else including by naming it, zero standing cost); undeclared defaults to agent-reachable (REA-1); two budgets of which only one is minimized - standing context is minimized, a person's **recall is spent deliberately** as the price of human agency, and treating it as waste converts every human-only capability and hands away the judgement it kept (REA-2); the class test is **autonomy, not reuse** - reuse justifies extraction, never reach (REA-3); human-only capabilities are structurally uncomposable, so material two of them share lives in **neither** and is externalized (REA-4); a human-only precondition is phrased as an action for the person, never attempted, since an attempt diagnoses a broken surface where a person's action is missing (REA-5); composition is an explicit invocation act naming one target, never a bare mention nor a path into internals (REA-6); a router hints and cannot fire, is updated in the same change as any reachable-capability change, and a stale one is a **correctness defect** read by the person least able to catch it (REA-7); precondition classes - **hard** (output wrong) carries a remediation pointer, **soft** (output blunt) carries none (REA-8); the discoverable set is declared once and rendered, never restated per surface (REA-9); a class change is an announced surface change (REA-10) (tech-agnostic) | Stable | 1 | 1.1.0 |
| [l1-rejection-memory.md](specifications/l1-rejection-memory.md) | Rejection memory - the durable record of **declined proposals**, closing the re-proposal loop every recurring survey and every fresh session reopens. Keyed by **concept, never by request**, so the store scales with decisions rather than traffic (REJ-1); a **deferral is not a rejection** - circumstance expires, an entry does not, and recording one permanently closes what nobody decided against (REJ-2); **already-satisfied is not rejected** - closed by pointing at where the capability lives, since an entry there contaminates matching with a false rejection about the project's own contents (REJ-3); matched by concept, not wording, the false-negative direction being the expensive one (REJ-4); a match is **surfaced and re-asked, never auto-applied**, resolving to affirm / reconsider / distinguish (REJ-5); accumulated **independent demand** rides on the entry and is the strongest reconsideration signal there is (REJ-6); the store is read at **generation** time, not only at decision time (REJ-7); reconsideration retires the entry while previously closed requests stay closed (REJ-8); proposals only - a declined defect is never an entry, or the store becomes where broken things are argued away (REJ-9); entries are authored to be **applied cold** (REJ-10); authored on the decider's authority, never by the party that would otherwise do the work (REJ-11); an entry is **not** a generator constraint until deliberately promoted into one under the exclusion contract (REJ-12) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-instruction-economy.md](specifications/l1-instruction-economy.md) | Instruction economy - the **sentence-grain** member of the anti-bloat family, below harness composition (components) and capability reachability (descriptors). A line earns its place by **changing behaviour against the actor's default**, and plausibility is not the test (IEC-1); the no-op verdict is **actor-relative, settled by running rather than arguing, and expires when the actor changes** - a swapped model has a different default, so the set is re-measured, never inherited (IEC-2); a failing line is deleted whole, since trimming a no-op yields a shorter no-op (IEC-3); the environment is a source of truth and an instruction restating it is a **cache of a lookup** - cache what cannot be looked up: the unwritten convention, the reason, the trap no declaration confesses (IEC-4); duplication and **scattering** are opposite failures with opposite fixes, removal versus co-location (IEC-5); standing rules are placed with the role that has **attention to spare**, since a body of standards loaded onto the actor exploring, writing and debugging is dropped exactly when the work is hardest (IEC-6); steer by the target behaviour, since a bare prohibition makes the forbidden thing *more* available (IEC-7); density is bought with terms the actor already holds, a coinage is paid for in definition at every use (IEC-8); a completion criterion is both **checkable** and **demanding**, and its demand is the legwork it produces (IEC-9); accumulation is the default fate, so pruning is **scheduled** and a review producing only additions has not run (IEC-10); length is a failure mode independent of per-line quality, and its remedy is structural (IEC-11) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-concept-grounding.md](specifications/l1-concept-grounding.md) | Concept grounding - the undeclared dependency structure inside every artifact a reader traverses **in order** (a report, a walkthrough, an onboarding path, a generated explanation). Each unit declares what it **requires** and what it **establishes** (GRD-1); the dependency is the **concept, never its name**, so a passage with no unfamiliar vocabulary can still lose the reader and a term scan explicitly does not satisfy the check (GRD-2); two grounding origins only - **assumed** (brought by the audience, fixed and declared) and **established** (landed by an earlier unit); *mentioned* is not grounded, and the set is monotone, so a second establishment is a bug report on the first (GRD-3); a unit is admissible only when all its requirements are grounded, and proceeding anyway is not an option because the cost compounds downstream (GRD-4); the grounded set **travels with the position and is never inferred by reading back**, which is what makes an ordered artifact compatible with concurrent, bounded-context production (GRD-5); a term is landed with its idea in one unit, since a name alone grounds nothing while appearing to (GRD-6); exactly two remedies for an ungrounded requirement - establish before, or promote to an assumption, the second being a **change of audience** (GRD-7); the assumption set is published in the artifact (GRD-8); termination is by **destination, not by exhaustion of source material**, and leftovers are expected (GRD-9); a gap in the source is named and resolved by supplying or cutting, never by plausible invention every downstream unit inherits (GRD-10) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-exploratory-planning.md](specifications/l1-exploratory-planning.md) | Exploratory planning - the phase **before** TG-1's precondition holds, where no requirements artifact exists and none can yet be written (a greenfield effort, a large migration, a subsystem whose shape is undecided). Output is **decisions, not deliverables**, and the phase's completion tell is the **urge to build**, which signals the route is clear rather than a temptation to resist (XPL-1); the **destination is named first and fixes the scope**, supplying the termination condition and the basis for every boundary (XPL-2); the decision unit completes on a **recorded answer**, sized to one session, typed by how it resolves - investigate / make-something-to-react-to / converse / unblock-by-doing - with the acting type earning its place only by unblocking a decision (XPL-3); **a unit needing a human is never completed by the actor supplying the human's side** - an actor that asks its own questions and answers them has fabricated the counterparty, detected by a record containing only the actor's contributions or an answer attributed to nobody, and where no person is available the honest outcome is an unresolved unit (XPL-4); deliberate incompleteness whose admission test is **whether the question can be stated sharply now, not whether it can be answered** - sharp-but-blocked is a unit, not-yet-sharp is fog, and pre-slicing fog manufactures precision that does not exist (XPL-5); fog graduates one patch at a time and leaves the fog region as it does (XPL-6); **out of scope is terminal, is not fog, and is not part of the route** - scope not sharpness lands it there, a mis-scoped unit is closed to leave the frontier unambiguously, and it stays out of the decisions record because a scope boundary is not a step on the walked route (XPL-7); the map is an **index, not a store** (XPL-8); claim before work, frontier = open + unblocked + **unclaimed** (XPL-9); structure in the host's **native relations** so the frontier renders where people already look (XPL-10); human-facing renderings **name** units, identifiers riding inside names (XPL-11); completion **hands off** by collapsing into the artifact TG-1 names, since feeding units straight to an executor discards the linked reasoning and leaves execution working from titles (XPL-12) (tech-agnostic) | Stable | 1 | 1.0.0 |
| [l1-foreign-agent-invocation.md](specifications/l1-foreign-agent-invocation.md) | Foreign agent invocation - the **commissioned** direction of the foreign-tool relationship, the one nothing owned: the marketplace governs what comes **in**, host-native rendering governs what goes **out**, artifact-derived observation governs what is **read afterwards** out-of-band having asked for nothing - none covers *this system authoring the order, starting a foreign agent runtime, and owning what it produced*. Centre of gravity is one property: **a foreign runtime's failures are predominantly silent, and its deviant paths do not default the way its primary path advertises**, so every failure here is shaped like success. The work order is the entire brief since the delegate holds zero shared state, and an order that cannot be written without making the decisions is design, which does not delegate (FAI-1); **containment posture asserted per call, never inherited** - a launch and a continuation are independent surfaces, a flag one accepts the other rejects, and a rejected flag falls back to an ambient configuration this system does not own, so an unassertable posture refuses the call (FAI-2); every idle input channel closed at launch, the failure being not an error but a process waiting forever on an unterminated input at zero load, indistinguishable from long work (FAI-3); **the deadline comes from the operation, not the caller's default** - a short ceiling kills real work and misreports it as the work's failure, no ceiling is an unbounded silent wait, and a trip is a failed run never a blind re-invocation of possibly-partial effects (FAI-4); a continuation names its **captured** session identity, never an implicit most-recent selector, because a wrong-target continuation returns a coherent well-formed result about different work (FAI-5); resolved configuration disclosed before the first billable call, since it is assembled from ambient files the principal may not know they wrote (FAI-6); **success decided by the produced artifact**, never a quiet diagnostic channel or a bare exit - healthy runs emit benign noise and every failure this contract exists for can exit cleanly (FAI-7); write authority requires a **recorded pre-state**, the diff against it being the only instrument of attribution and the only route to reversal for a black-box actor, so an unrecordable location is a stop not a warning (FAI-8); the delegate's report is a claim - the delegator reads the whole difference and **re-executes the checks itself** (FAI-9, CMP-3/CMP-8/AO-6); delegation bounded by a **floor** below which the delegator acts directly and a **ceiling** above which it takes the work over rather than escalating a cost it was engaged to absorb (FAI-10); capability established per **(capability, executor)** pair and in the invocation mode it will actually run in (FAI-11, REA-11); out-of-band completion announced before it is acted on, being the visible edge of a state transition that otherwise has none (FAI-12); everything the delegate emits is untrusted data (FAI-13, ADO-9/CB-8); demarcated against l1-host-native-rendering (authority-neutral outbound write) and l1-artifact-derived-observation (asked for nothing, derived never authoritative); nodus mapping - the deferred external step is the language-grain launch/return shape, and its missing piece is a declared deadline (tech-agnostic) | RFC | 1 | 1.0.0 |
| [l1-remote-liveness-verdict.md](specifications/l1-remote-liveness-verdict.md) | Remote liveness verdict - answering **is the remote entity still alive, or did we just lose the wire** honestly, closed to a three-state verdict never a boolean: **alive / unverifiable / confirmed-gone**, with *unverifiable* structurally distinct and never collapsible into either neighbour (RLV-1); confirmed-gone requires **positive evidence sourced from the party that actually holds the resource** - a timeout, closed socket, or local lookup miss is unverifiable by construction regardless of what field it is written into (RLV-2); **correlated silence across every entity behind one channel is diagnosed once, at the channel**, never multiplied into independent per-entity deaths (RLV-3); a termination signal is trusted only when it names the entity's **current incarnation**, generalizing WL-2's claim-release compare-and-clear from work ownership to liveness verdicts (RLV-4); a returned status is checked against **durable state**, never trusted on its own report (RLV-5); artifact evidence is stronger than a liveness signal but proves only the **narrower claim it actually names** - a matched commit proves the commit arrived, not that this run produced it or nothing later is missing (RLV-6); disconnection is **not by default termination**, the applicable policy and its schedule are disclosed rather than assumed, and losing the control plane is distinguished from losing the underlying execution (RLV-7); and **one physical resource carries exactly one liveness identity at a time** - two concurrent locus registrations of the same machine split its inventory and answer differently depending on which one is asked (RLV-8, extends LOC's substitution model); demarcated from l1-work-liveness (progress staleness assuming an intact channel, vs. existence classification when the channel itself is in question) and from l1-foreign-agent-invocation (one-shot commissioned execution vs. a continuously-reachable remote host classified repeatedly over its life) (tech-agnostic) | RFC | 1 | 1.0.0 |
| [l1-cooperative-status-projection.md](specifications/l1-cooperative-status-projection.md) | Cooperative status projection - normalizing **many concurrently-running, vendor-foreign coding-agent CLIs**, each with its own incomplete hook contract, into one closed-vocabulary status feed: status is read from the vendor's cooperative hook channel, **never a terminal title or output heuristic**, and always resolves to a small closed vendor-neutral set (ASP-1); a missing-transition inference fallback is declared **per (vendor, transition) pair, never applied universally**, and withheld where a known innocent-silence condition (a still-active child, an explicitly-reported background op) explains the gap (ASP-2); a settled or inferred verdict is **generation-fenced** - only a signal correlated to the current turn may supersede it, a hook delayed for the *same* turn never resurrects it, while a genuinely new turn freely re-opens working, generalizing RLV-4/WL-2's incarnation-matching to a status verdict (ASP-3); addressing identity is authorized by a **verified live transfer**, never by mere registration or persisted-record possession - a restored alias does not retroactively acquire routing authority, composing CB-3's grant-lapses-on-bound-element-change rule (ASP-4); persisted status passes a **validation ladder** on load (version check, bounded-age cutoff, per-entry isolation so one corrupt row doesn't cost the rest, unconfirmed-until-reconfirmed with the unconfirmed marker itself never persisted) rather than being trusted on load (ASP-5); and a local ingest channel's **own protective bound never inflates its own external-anomaly count**, with a repeated failure from the same cause deduplicated into one reported incident (ASP-6); demarcated from l1-foreign-agent-invocation (a one-shot commissioned task vs. a continuously-observed interactive peer) and l1-artifact-derived-observation (an out-of-band, ask-for-nothing read vs. an in-band cooperative push channel) (tech-agnostic) | RFC | 1 | 1.0.0 |

## Meta Information

- **Maintainer**: Core Team
- **Last Updated**: 2026-09-03 (v1.0.184 — **application-shell runtime** via `/magic.spec` blank trigger (Creative Spark → DA-3 highest-coverage gap). NEW `l2-application-shell` 1.0.0 Stable: the Layer-2 realization `l1-application-shell` (Stable since 2026-06-25) had never received, selected because the gap was **verified by execution rather than inferred** — the previous GUI build phase disclosed in its own evidence that the shell frame it shipped is mounted by nothing, and a check of the host entry point confirmed the application still renders the earlier workbench. Owns the four contracts no other frontend L2 claims (state/reactivity, action/dispatch, workbench composition/restoration, async ownership), collapses the two-composition-root fork, and adds the capability-admission rule that keeps the single IPC seam incapable of growing a frontend-only feature. `l1-application-shell` → 1.0.1 (link-only patch: Related Specifications now names its implementation). Also corrects a stale **Spec Count** (281 → 283): the v1.0.183 row was added without bumping the count line.
- **Prior**: 2026-09-03 (v1.0.183 — **frontend module topology** via `/magic.spec`, from an architectural assessment of whether to adopt Feature-Sliced Design in `packages/ui`. The assessment's verdict was *no wholesale adoption* — FSD's `entities`/`features` layers organize client-side domain logic that INV-2 forbids the frontend to hold — but its three transferable rules were adopted individually, which is what this wave formalizes. New L2 `l2-ui-module-topology` v1.0.0: the TypeScript twin of `l2-crate-topology`, facing a language with no compile-time module boundary inside a package; four tiers (composition root → shell → surfaces → shared) with UMT-1…UMT-7 and enforcement mapped onto tripwire discipline; takes `l2-crate-topology` §6.4's leaked-public-API incident as the motivating precedent, and notes that tier membership is currently indistinguishable by path — the precondition problem that makes the rules inexpressible today. `l2-source-layout` v1.3.0: §4.6 delegates `packages/ui` internals here, symmetrically with §4.5's crate delegation. `l2-quality-pipeline` v1.3.0: resolves the standing §4.1 `.fallowrc.json` TBD in favour of custom zones over the bundled `feature-sliced` preset, splitting ownership — the pipeline keeps the gate, the topology spec owns the rules. `l2-app-ui` v1.4.1 cross-link. Previously v1.0.182 — **application-shell frame + design-system slice** via `/magic.spec`, from an approved Claude Design desktop mockup ("Cronus Building") realizing the four-layer building navigation. Not a reference-mining wave — a UX-stage resolution of carried TBDs plus one new L2. `l1-navigation-model` v1.3.0: §4.1 sidebar catalog revised (primary run reordered + Dashboard/Employees/Automation added, Automation split from the old combined "Office" entry; a foot utility group Channels/Security/Providers-ACP/Settings; the standalone Pulse tab folds into Schedule as an L3 facet); §4.5 gains the per-subsystem L3 facet catalog; new §4.6 fixes the File/Edit/View/Help leaf lists; NV-7 extended with an L0 project file-tree dock + command palette; NV-1…NV-10 wording unchanged, NV-1's runtime no-reorder still binds end-users. `l2-navigation` v1.1.0 tracks it: `SIDEBAR_PRIMARY` + foot `SIDEBAR_UTILITY`, component tree adds the file-tree dock + command-palette overlay (AS-10) + full-screen global-settings overlay, new §4.6/4.7/4.8. `l2-app-ui` v1.4.0: §4.5 theming split into two orthogonal axes — OS-appearance **mode** (system/light/dark) × named **colour scheme** (a design-identity token package; one built-in `default` ships, more as data); §4.1 surfaces regrouped, deferring order to l2-navigation §4.3; Document History section added. `l1-design-identity` v1.0.2: links the new L2, clarifies §4.2 the host UI realizes the catalog as the colour-scheme axis (link + clarification only, no invariant changed). New `l2-design-system` (DI-1…DI-9): the frontend token contract — canonical token taxonomy, the `(mode × scheme)` resolver, the Tailwind v4 `@theme` binding, the scheme package shape (manifest + per-mode token files), the layered catalog + `app.json` persistence, and the tiered craft lint enforcing "tokens are the single source of visual truth". Approved slice decisions: thin runtime (defer the full AS-1…AS-13 machinery), expand the canonical catalog to match the mockup, surfaces render as explicit INV-9 placeholders — no core/bridge wiring in this slice.)
- **Prior**: 2026-09-01 (v1.0.181 - **dispatch-authority and remote-liveness wave** via `/magic.spec` from a re-mining pass over a reference already mined once (a shipping desktop multi-agent orchestrator; twin of the earlier commissioned-execution wave's reference on the review-loop half, but a distinct product) — its flagship best-of-N feature was absorbed last time into `l1-competitive-execution`; this pass worked through the six candidates flagged unmined then, cross-checking each against the corpus as it now stands rather than trusting the prior summary. Two were found already covered: the execution-host-as-addressable- dimension candidate by `l1-execution-locus` + `l1-environment-lifecycle` + `l1-doctor` (all four of precondition/output/terminal-states/participant matched); the self-driving-CLI candidate turned out to be the same control-plane mechanism the new remote-liveness spec already names, not a separate one. Two yielded genuinely new L1 concepts. New `l1-remote-liveness-verdict` (RLV-1...RLV-8, RFC, concept-only) closes a question no existing spec answers: when the only access to a remote entity is a channel that can fail independently of the entity, is it alive, gone, or simply unreachable right now — a closed three-state verdict where *unverifiable* is structurally distinct and never collapsible, *confirmed-gone* requires positive evidence sourced from the party that actually holds the resource (a timeout is never that evidence, regardless of what field it is written into), correlated silence across every entity behind one channel is diagnosed once at the channel rather than multiplied into independent deaths, and one physical resource carries exactly one liveness identity at a time. New `l1-cooperative-status-projection` (ASP-1...ASP-6, RFC, concept-only) closes the adjacent but distinct problem of normalizing many vendor-foreign coding-agent CLIs' incomplete status hooks into one feed — a declared, per-(vendor, transition) inference fallback, generation-fenced so a delayed same-turn signal never resurrects a settled verdict, and addressing authority earned only by a verified live transfer, never by mere persistence or registration. Both closed the same recurring pattern the reference reinforced three separate times (execution-host liveness, dispatch capability tokens, and agent-status hooks): a verdict or authority bound to a specific generation is never superseded by a signal that does not name that generation — generalized from `l1-work-liveness` WL-2's compare-and-clear at the work-claim grain. Two refinements: `l1-orchestration` v1.3.0 (ORC-13 a handoff release ends orchestration bookkeeping without touching the delegate's still-running process, which continues as an explicitly unsupervised entity; ORC-14 dispatch authority as a minted, generation-bound grant revoked exactly once at the first terminal transition, composing `l1-consent-binding` CB-3's bound-element-change rule for inter-agent delegation) and `l1-peer-compatibility` v1.1.0 (PCO-15 — a content or meaning change on a structurally unchanged wire surface is a compatibility break the structural-fingerprint checker cannot see by construction, since nothing about the shape moved; declared on the contract itself as a reviewed claim, the same way PCO-11 already declares emitter behavior no diff can see). One candidate (automation precheck + selective rerun) was found too shallow to formalize beyond what `l1-automation-pipeline` AP-3/AP-6 already require generically. A dedicated Nodus lens found nothing to add at the language grain: `l1-nodus-language` NL-12's correlation-token/deadline pair already realizes the generation-fencing principle at the DSL's own level, and the orchestrator's unsupervised-handoff disposition was deliberately NOT ported to nodus's `RUN`/recursion model, since it would break the bounded-recursion cost-rollup guarantee NL-18 already commits to.)
- **Prior**: 2026-09-01 (v1.0.180 - **commissioned-execution wave** via `/magic.spec` from a reference that is a small cross-model plan-hardening skill collection: one agent locks intent with the user, a *rival vendor's* agent attacks the locked plan read-only round after round, then the two swap jobs to build it, on the doctrine that whoever made a thing never checks it. Almost all of its review-loop core was already owned - producer/grader independence (IR-2, CV-5 including the same-model-lineage reduced-confidence rule), bounded convergence with an honest non-convergent exit (IR-5), frozen criteria (IR-6), the audit trace (IR-7/DL-4/OL), the human gate (RC), a dismissal needing the same evidence as a claim (CMP-7), and a delegate's self-report never relayed as completion (CMP-3/CMP-8/AO-6). What it *did* own alone was the mechanics of **driving another vendor's agent as a subordinate executor**, which nothing covered: the marketplace governs what comes in, host-native rendering what goes out, artifact-derived observation what is read afterwards out-of-band - and none of them the act in the middle. New `l1-foreign-agent-invocation` (FAI-1...FAI-13) names that third direction around one property: **a foreign runtime's failures are predominantly silent and its deviant paths do not default the way its primary path advertises**, so a continuation silently inherits a wider containment posture than the launch it resumes, a process waiting on an unterminated input stalls forever at zero load, a continuation addressed by *most recent* returns a coherent result about different work, and a caller's default deadline kills real work and misreports it as the work's failure - each shaped exactly like success. Three refinements followed: `l1-facilitation` v1.3.0 (FC-14 stake tiering inside FC-13's frontier with the consequence line as the **admission test** - the mechanical fix for the low-value-question defect FC-13 names and leaves unnamed; FC-15 bulk acceptance as a granted delegation over a *named, resolved, marked* set rather than an inference from silence; FC-16 the resolved fact set presented once, sourced, a correction re-opening the question), `l1-capability-reachability` v1.1.0 (REA-11 reachability as a **(capability, executor)** property, established in the invocation mode it will actually run in), and `l1-parallel-staffing` v1.1.0 (PS-10 execution tier assigned per partition class, never inherited from the coordinator, since inheritance multiplies the coordinator's rate by the fan width over characteristically shallow work). Nodus took its own pass: `l1-nodus-language` v1.17.0 NL-25 generated-value branch contract - the point where the language's provenance discipline finally reaches **control flow** rather than content - and NL-26 the deadline on a suspended external step.)
- **Prior**: 2026-08-26 (v1.0.179 — **pre-requirements planning follow-up** via `/magic.spec`, a targeted second pass over the same external engineering-practice toolkit after the user flagged its large-effort mapping practice specifically. The finding that justified a spec rather than a refinement: TG-1 states a task graph is *generated from an explicit requirements artifact*, and **nothing owned the phase where no such artifact exists and none can yet be written** — so that phase was being run through an execution-shaped planner, which satisfies the precondition by inventing scope. New `l1-exploratory-planning` (XPL-1…12): a phase whose **output is decisions, not deliverables**, whose completion tell is the **urge to build**; a **named destination that fixes the scope**; a decision unit completing on a recorded answer; **deliberate incompleteness** with the admission test being *can the question be stated sharply now*, not *can it be answered* (sharp-but-blocked is a unit, not-yet-sharp is fog, and pre-slicing fog manufactures precision that does not exist); **out-of-scope as terminal and distinct from fog**, kept out of the decisions record because a scope boundary is not a step on the walked route; map-as-index; claim-before-work with the frontier defined as open + unblocked + **unclaimed**; and a **handoff obligation** that collapses the map into TG-1's artifact rather than feeding units straight to an executor. The load-bearing invariant reaches past planning: **XPL-4** — a unit needing a human is never completed by the actor supplying the human's side, an actor that asks its own questions and answers them having fabricated the counterparty. Three refinements: **TG-15** (generation refused rather than improvised when the source artifact is absent or substantially unsettled, naming the upstream phase), **CONV-9** (a decision-shaped unit converges on the one board by construction, closing the off-board planning loophole that is CONV-1's shadow work wearing a phase name), **CMP-10** (the general form of XPL-4: a completion requiring a counterparty is not satisfied by the actor's own contribution — the class every one of CMP-1…CMP-9 misses, because the work was real and the report honest while the *participant* was fabricated))
- **Prior**: 2026-08-26 (v1.0.178 — **working-context and standing-instruction wave** via `/magic.spec` from a reference that is a composable set of engineering practices for coding agents, built as small adaptable units rather than a process that owns the workflow. Five new Stable L1s claiming grains the corpus specified transforms for and never made decisions about. `l1-context-transition` (CXT-1…12): the token-economy family specifies *how* each transform works and never *which one, when* — a cost-ordered ladder (continue / discard / externalize / delegate / compress) evaluated **only at a work boundary**, with the primary→secondary source conversion named as the thing that loses the *why*, a threshold that signals but never fires, discard requiring positive evidence of irrelevance rather than absence of a reason to keep, and compression as the default that is never the first reach. `l1-capability-reachability` (REA-1…10): **who may invoke a capability** — agent-reachable versus human-only as a closed partition, with a person's **recall treated as a budget spent deliberately rather than minimized**, the class test being autonomy rather than reuse, human-only capabilities structurally uncomposable (so shared material lives in neither), and a stale router graded a correctness defect. `l1-rejection-memory` (REJ-1…12): the durable record of declined proposals, keyed by concept and consulted at generation time, with the two contamination guards stated as invariants — a **deferral is not a rejection**, and **already-satisfied is not rejected**. `l1-instruction-economy` (IEC-1…11): the sentence grain below harness composition and reachability — a line earns its place by changing behaviour against the **actor's own default**, a verdict that is measured by running rather than argued and **expires when the actor changes**, plus documentation-as-cache, scattering versus duplication, placement by context pressure, and prohibition-raises-what-it-forbids. `l1-concept-grounding` (GRD-1…10): the dependency structure inside any artifact read in order — assumed versus established, **the concept and not its name**, the grounded set carried with the position (which is what reconciles ordered artifacts with order-independent production), and termination by destination rather than by exhausting the source. Six refinements: RC-11/RC-12 (latest-reversible checkpoint placement; a decision-ready brief that *reaches* the artifact), FC-13 (frontier-batched elicitation with recommended answers, and the fact/decision split, reconciling the apparent tension with FC-4), NEG-11 (the positive target is the primary form an exclusion takes at the generator), CTN-12 (wide mechanical change sequenced expand→migrate→contract), ES-21 (a generated evaluation item never leaks its answer through surface form), CE-11 (design contests assign each variant a declared constraint, and settle by deliberation rather than by CE's one-whole-attempt rule). Cross-workspace: nodus `l1-nodus-dialog` v1.4.0 (DG-11 latest-reversible dialog placement and a decision-ready payload carrying a reference rather than inlined content))
- **Earlier**: 2026-08-20 (v1.0.174 — **evidence-archive wave** via `/magic.spec` from a design scheme placing a never-deleted, never-uploaded raw local record beneath a compaction layer that stores exact ranges rather than replacing them. New `l1-evidence-archive` (EA-1…EA-12) claims the raw non-numeric evidentiary plane no spec owned, and inverts the destructive default: the system's automatic compaction becomes an indexing operation while destruction becomes an explicit act. Refinements: `l1-context-compression` v1.4.0 (CC-12 archive-backed recoverability), `l1-conversation-rewind` v1.2.0 (RW-1 anchored to archived ranges), `l1-observation-retention` v1.1.0 (non-numeric plane owner named), `l1-search` v1.1.0 (SRCH-9 history scope), `l2-context-management` v1.1.0 (archive-before-reduce cascade))
- **Earlier**: 2026-08-20 (v1.0.173 — **interop-contract wave** via `/magic.spec` from a reference that is a multi-provider agent-orchestration desktop client whose client and host ship as separately released artifacts: 3 new L1 — `l1-peer-compatibility` (PCO-1…PCO-14, version skew as the normal state; per-capability version lines, newer-side-adapts, machine-verified additivity, direction-decides-severity, parse-tolerance-is-not-compatibility, fail-closed floor vs degrading optional surface, oracle against released artifacts, verdict names the side), `l1-record-evolution` (REC-1…REC-13, the reader population sets the regime; re-publishers preserve rather than tolerate, core-vs-opaque with one-way promotion, variant passthrough + residual capture, separate authoring/acceptance schemas, frozen wire-form guard failing on all drift), `l1-resumable-subscription` (RSB-1…RSB-11, position-as-cursor, write-then-wake, representability skip and gaps-are-not-loss, framing carries no world semantics); 4 refinements — LOC-11/LOC-12 one decider + preference-vs-binding/clone-not-migrate, TW-11 declared blind spots, ERR-8 refusal names its blockers not a correlated count, CR-10 provably-dead-holder reclamation; nodus LP-23 versioned capability manifest + per-capability degrade)
- **Earlier**: 2026-08-20 (v1.0.172 — **inverse-derivation pair** via `/magic.spec` from a design session on learning construction method from exemplary work rather than from the office's own sessions. Two new Stable L1s forming a supplier→consumer pair with a deliberately narrow, one-directional seam. `l1-inverse-derivation` (IVD-1…13): running the production arrow backwards over work the office did not produce, on the **episode** grain — a completed arc yielding a recoverable before-state, an after-state, executable checks, and a trajectory — with every recovered link graded **attested / inferred / assumed** and grades non-inheriting, the **trajectory as first-class output** on the grounds that the artifact already shows the answer and only the record shows the search, an explicit **pinned and fingerprinted** corpus registry closing the silent-drift hole where a rewritten upstream history invalidates every measurement with no error reported, discovery permitted to propose and forbidden to admit, and selection on **record quality rather than product popularity** since proxy metrics rank product success and would systematically acquire the un-reconstructable. `l1-capability-leveling` (LVL-1…17): the closed replay loop whose objective is **spread collapse at a bar, never reproduction**, terminating on a measured two-part marginal rule that replaces the non-terminating "iterate until identical"; comparison over outcome **and trajectory**, with an implausibly straight path recorded as a **contamination signal rather than a success** — the structural answer to a framing that, being reconstructed from finished work, already knows the answer; four-way divergence classification with **arbitrary** discarded under an explicit retention bias; **carrier chosen by who executes the lesson** across a five-rung ladder, since filing every lesson as instruction places it in the highest-variance carrier for the problem defined by execution variance; hardening refused where it degrades strong performers; and a **single human ratification gate on the assembled body of lessons** at the boundary, with the loop feeding the existing distillation machinery rather than standing up a second store.)
- **Earlier**: 2026-08-19 (v1.0.171 — **plugin-architecture wave** via `/magic.spec` from a reference that is a plugin-framework-based agent harness whose every part, including its model adapter and its agent loop, is a replaceable plugin. Four new Stable L1s covering the three dimensions the existing extension model never reached — visibility, binding, and world. `l1-scoped-capability-layers` (SCL-1…13): one registry with per-actor layers, the registration site driving visibility and lifetime as one fact, and one chain read in opposite directions (supply down, observation up); with scoping declared **not** an authority boundary, because a mechanism shaped like isolation is used as isolation unless it says otherwise. `l1-composition-binding` (CBD-1…13): a binding names a **generation**, so a running actor outlives its composition file being edited or deleted, a child joins the parent's exact generation by reference rather than re-resolving its name, and every reconstruction resolves *runs-on* rather than reading the frozen *started-with*. `l1-composition-layering` (LAY-1…10): author-assigned entry identity as the sole reconciliation basis, whole replacement with no deep merge, one composition algorithm serving both the run path and every inspection surface, and ownership decided structurally — exact match to the shipped form is system-owned, any deviation user-owned. `l1-execution-locus` (LOC-1…10): the locus as the **unit of substitution, never the individual capability**, closing the previously-flagged execution-host dimension — a shell in one world and a file reader in another produce tools that individually succeed and jointly describe an impossible world. Five amendments: IB-13 (deferred satisfaction legitimate until a declared settlement point, a startup failure after it, enumerating per unit the needs never supplied), INT-11 (declared dispatch discipline, machine-checked against the dispatch site), EP-13 (registration as a reversible effect whose teardown reaches quiescence rather than requesting it), WL-10 (where intake is shared, a result belongs to an interval and never to a message), and nodus LP-22 (a run pinned to the definition generation it validated against).)
- **Earlier**: 2026-08-13 (v1.0.170 — **co-presence and surface-integrity wave** via `/magic.spec` from a reference that is a review-first terminal viewer for agent-authored changesets, steerable from a second terminal. Four new Stable L1s: `l1-attention-steering` (AST-1…AST-11 — the **inverse of directability**: the office moving a co-present human's live view, out-of-band and metered, with replacement withdrawing open decision requests), `l1-semantic-addressing` (SA-1…SA-10 — one address grammar, ternary resolution, all-or-nothing placement; consolidates the anchoring clauses scattered across code-intelligence/document-understanding/change-merge), `l1-surface-parity` (SP-1…SP-11 — the maintenance contract behind INV-8/9/10: finding inventory, tombstones, one conformance corpus through each surface's *real* projection, extract-before-the-second-implementation, converge-first-correct-second, shared action catalog), `l1-derived-instructions` (DIN-1…DIN-10 — the agent-facing document generated from the surface, with one message catalog pinned by contract tests). Three amendments: `l1-extension-points` v1.1.0 (EP-10 attribution + interrupt ceiling, EP-11 reserved core namespace, EP-12 the core uses its own public seam), `l1-declarative-configuration` v1.1.0 (DC-11 per-field authority floor across the layer stack), `l1-incremental-execution` v1.1.0 (IE-9 explicit scoped invalidation with retention until replacement). Cross-workspace: nodus `l1-nodus-portability` v1.15.0 (LP-21 declared role-provider resolution — explicit choice, then locus proximity, then same-locus priority, then registration order))
- **Spec Count**: 283 (206 L1 + 77 L2)
