# Project State

<!-- STATE.md — live project memory. Read FIRST in every workflow session. -->
<!-- Maximum 100 lines. Agent updates AFTER each completed action. -->

**Workspace:** main
**Updated:** 2026-09-09 14:20
**Phase:** 29 — Desktop Generic Dispatch
**Status:** Active

## Current Position

- **Task:** `/magic.run main` Done: **T-29A01 — the desktop bridge composes the shared registry/dispatcher (1/7).** `apps/desktop/tauri/src/bridge.rs`'s `Bridge<C>` now holds a real `InvocableRegistry`/`Dispatcher` pair, composed via the identical `cronus_core::invocable_bootstrap::bootstrap()` door the CLI and TUI already use. Two new Tauri commands: `capability_catalog` (every `Semantic`+`ClientLocal`+`Shipped` descriptor) and `capability_invoke` (dispatch one call, `Ok(None)` for the registry's real `Unknown`, never a fabricated failure). `capability_version`/`capability_status` untouched, exactly as the Phase Notes' own disclosed exception requires. `Bridge::mask`'s local redaction stays for those two only; `invoke()` never re-applies it, relying entirely on `Dispatcher::set_secrets`'s own boundary — `rg 'redact::redact'` confirms exactly one remaining hit, inside `mask()`. **A real design decision**: `capability_invoke`'s wire payload is `id`+`args` only, never a deserialized `Invocation` as a whole — `caller: Surface::Desktop` is asserted server-side, never accepted from the wire, so a script cannot claim to be a different surface. **A real duplication caught and closed before it existed twice**: promoted `is_projected` from a TUI-local free function to `Invocable::is_projected()` in `cronus-contract`, since the desktop's own catalog filter needed the identical predicate — `crates/tui/src/command.rs::is_projected` now delegates to it. **Corrected this phase's own planning-time assumption against concrete prior evidence**: the Phase Notes claimed `bridge.rs`'s tests are "expected to run, not merely compile" — Phase 26's own archived record already reproduced `cronus_desktop_lib`'s test binary failing to *load* (`STATUS_ENTRYPOINT_NOT_FOUND`, WebView2Loader) with every `src` change git-stashed, confirming it environmental and pre-existing, not scoped to one test target; recorded as a correction, not silently trusted.
- **Next Action:** Execute T-29A02 The TypeScript client gains `catalog()`/`invoke()`; the executable face cannot cross the seam via /magic.run main

## Progress

```
Phase 29: [1/7] █░░░░░░░ 14%
Overall: [27/28] ████████ 96%
```

- 2026-09-09 **Decision:** `/magic.run main` — **T-29A01 Done: the desktop bridge composes the shared registry/dispatcher (1/7).** `apps/desktop/tauri/src/bridge.rs`'s `Bridge<C>` gained `registry: InvocableRegistry`/`dispatcher: Dispatcher` fields, composed via `cronus_core::invocable_bootstrap::bootstrap()` — the identical door the CLI and TUI already use, no private path. Two new commands: `capability_catalog` (every `Semantic`+`ClientLocal`+`Shipped` descriptor) and `capability_invoke` (`Ok(None)` for the registry's real `Unknown`, never a fabricated failure). **A real design decision**: the wire payload is `id`+`args` only, never a deserialized `Invocation` — `caller: Surface::Desktop` is asserted server-side, so a script cannot forge a different surface's identity; this needed `Deserialize` (and, for a real round-trip test, `Serialize`) on `ArgValue` alone in `cronus-contract`, gated behind its existing optional `serde` feature — not on `InvocableId`/`ArgValues`/`Surface`/`Invocation`. **A real duplication closed before it existed twice**: `is_projected` promoted from a TUI-local free function to `Invocable::is_projected()` in `cronus-contract`; `crates/tui/src/command.rs::is_projected` now delegates to it. `Bridge::mask`'s local redaction stays only for `capability_version`/`capability_status` (untouched, the disclosed exception); `invoke()` relies solely on `Dispatcher::set_secrets` — `rg 'redact::redact' bridge.rs` confirms exactly one remaining hit, inside `mask()`, satisfying the Verify line's "outside the shared dispatcher's own boundary" wording without contradicting "do not remove them." **Corrected this phase's own planning-time assumption against concrete prior evidence**: the Phase Notes claimed `bridge.rs`'s tests are "expected to run, not merely compile" — Phase 26's own archived record already reproduced `cronus_desktop_lib`'s test binary failing to *load* (`STATUS_ENTRYPOINT_NOT_FOUND`, WebView2Loader) with every `src` change git-stashed, environmental and pre-existing, not scoped to one test target — recorded as a correction to the phase file itself, not silently trusted. `cargo build -p cronus-desktop`/`clippy --all-targets -- -D warnings`/`cargo fmt --all -- --check` (PowerShell) green; `cargo test -p cronus-contract --features serde --all-targets` 32/32; `cargo test -p cronus-tui --all-targets` 67/67 unchanged; full main-workspace `cargo test --workspace -j 2`/`clippy --workspace --all-targets -- -D warnings`/`cargo fmt --all -- --check` green. Containment self-check clean across all six touched files. Computed next step (DA-6): T-29A02 (the TypeScript client gains `catalog()`/`invoke()`). (Revert: `git restore crates/contract/src/lib.rs crates/tui/src/command.rs apps/desktop/tauri/{Cargo.toml,src/bridge.rs,src/lib.rs} .design/main/{TASKS,STATE}.md .design/main/tasks/phase-29.md`)

- 2026-09-08 **Decision:** `/magic.task main` — **Phase 29 decomposed into 7 tasks / 4 tracks — Desktop Generic Dispatch.** Pre-flight `ok:true` on every gate (290 specs, 290 Stable, 0 Draft, engine/rules parity clean), Pre-Planning Stabilization a genuine no-op. **PLAN.md's own Phase 29 section was stale and is corrected as part of this pass**: it still cited `l2-application-shell` 1.1.0 though the registry already shows 1.2.0 Stable, and it claimed "the four residual corrections" as this phase's own closing work. Grounded that claim against the real source before decomposing rather than trusting the inherited prose: unavailable-as-emptiness owns ~15 sites across `crates/core/src/invocable_bootstrap/*.rs`, identical for all three frontends — core-tier, not desktop-specific; the output-format flag and unescaped structured output are CLI-only (finding F-6 names its sites as exclusively `crates/cli`); the empty-secret-list fix needs a project-wide secret-*enumeration* primitive `crates/domain/src/secrets.rs` does not yet have (only a single-key `get`). **All four renarrowed OUT of this phase** — recorded in `l2-surface-conformance.md` §4.7/accepted-debt exactly as before, open, owned by whichever later phase actually touches their real sites, mirroring Phase 27's own T-27D01 renarrowing precedent rather than forcing them in or silently dropping them from the project. **A second disclosed exception found during the same grounding pass**: `core:version`/`status` are `Locus::Installation` (confirmed in `invocable_bootstrap/status.rs`'s own doc comment) — a third locus this surface does not project, with no admissible class under the spec's own §4.3 rule as currently classified; the bridge's existing bespoke methods for them are named as staying untouched, a real spec gap disclosed rather than papered over. **Structural facts confirmed directly, not assumed**: `apps/desktop/tauri` already depends on `cronus-core` and already declares its own detached `[workspace]`; `bridge.rs`'s 4 per-capability commands and `actions.ts`'s un-populated generic registry confirm zero of this phase's work exists yet; `cronus-conformance` has never been linked from outside the main `crates/` workspace before — Track C is the first proof of that shape. 7 tasks / 4 tracks: A (generic dispatch bridge, 3 tasks) → B (action-registry locus split, 2 tasks, depends on A) → C (corpus registration, depends on A+B) → T (validation). Computed next step (DA-6): T-29A01. (Revert: `git restore .design/main/{PLAN,TASKS,STATE}.md && rm .design/main/tasks/phase-29.md`)

- 2026-09-08 **Decision:** `/magic.run main` — **T-28T02 Done: PHASE 28 COMPLETE, 10/10 tasks, all five tracks.** Audited every intentional behaviour change against its own task: the shrunk slash catalog (T-28A01), the ordinary-input-not-error unresolved line (T-28A03), Board/Office reading real dispatched data instead of an always-empty placeholder (T-28C01, a real behaviour change beyond the structural unavailable/empty work — this crate's panels genuinely did nothing before this phase), the retired standalone executable (T-28D01), the reversed bare-invocation default (T-28D02) — nothing else found to have moved. **Directly verified, not assumed, that this phase does not repeat Phase 27's own closing-audit finding** (three of four named residuals never actually recorded inline): `rg`'d for every residual this phase's own tasks named (board.list's missing `task_ref`, Office's absent task field, Sessions' missing backing, Status's currently-unreachable `Unavailable` branch) and confirmed each is a real comment at its owning code site in `app.rs`/`view.rs`, not only in `STATE.md`/phase-file prose. `unwrap()`/`panic!()` audit across all 18 phase-touched files found exactly two production-path hits, both the same already-precedented literal-identity `.expect()` pattern every `*_bootstrap.rs`/`installation.rs` `id()` helper in this codebase already uses (`app.rs::dispatch_projection` binding a compile-time-constant invocable id; `pane_actions.rs`'s `PaneAction::id()`/`register()`) — panic-worthy only on a literal typo a test would catch immediately, no new class of risk. **T-28D02's flat-verb dispatch fix recorded explicitly as a correction, not folded into the residual list** — it restores behaviour Phase 27 shipped broken and nobody had ever exercised, so SP-10's preserve-then-correct rule does not apply (there was nothing working to preserve). `cargo fmt --all -- --check` clean; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo test --workspace --exclude cronus-desktop` green across two consecutive full runs, no flake; `cargo metadata` confirms exactly one workspace binary. Containment self-check clean across all 18 phase-touched files. Closed out `phase-28.md`'s frontmatter (`status: Done`, `provides`/`key_files`/`patterns_established` filled in) and `TASKS.md`'s status cell (`In Progress` → `Done`). Computed next step (DA-6, §5 Post-Task Replan): phase complete → the one user-visible next step is `/magic.task main`, never `/magic.spec`/`/magic.analyze` proactively. (Revert: `git restore .design/main/{TASKS,STATE}.md .design/main/tasks/phase-28.md`)

- 2026-09-08 **Decision:** `/magic.run main` — **T-28T01 Done: this surface's real projection registered against the full shared corpus; finding F-2 repaid (9/10).** `SurfaceProjection` (`TuiProjection`) was already built in T-28B02 to prove `DeclaredExclusion` against a local ad hoc corpus — this task's own remaining work was narrower than its Method text alone suggests: drive the **full** `cronus_conformance::run` (all three families) over the **real, shared** `fixtures::corpus()`. One new test does exactly that, mirroring the CLI's own T-27-era precedent test in shape. **Passed on its first real execution** — the only divergence was the already-expected F-5 residual (`secret-bearing`/`boundary-crossing`, empty secret list every surface carries); no new finding needed converting before being fixed. F-2's `consumer_registered` flipped `true` in `findings.rs` (its other three conditions already true since T-28A01) — F-2 now reads fully repaid, alongside F-3. `exactly_f3_reads_as_repaid_after_the_command_lines_registration` renamed/extended to `exactly_f2_and_f3_read_as_repaid_after_both_surfaces_own_registration`, naming both transitions explicitly rather than silently widening a test whose name would otherwise go stale. F-1 checked individually against its own `sites` field and confirmed still open — three named surfaces, desktop shell not yet registered, exactly as the task's own Notes anticipated. `cargo test -p cronus-tui --all-targets` 67/67; `cargo test -p cronus-conformance --all-targets` 16/16; full workspace `cargo test`/`clippy -D warnings`/`cargo fmt --check` green. Containment self-check clean. Computed next step (DA-6): T-28T02 (behaviour-preservation proof and full quality gates — the phase's own final task). (Revert: `git restore crates/tui/src/conformance_registration.rs crates/conformance/src/findings.rs .design/main/{TASKS,STATE}.md .design/main/tasks/phase-28.md`)

- 2026-09-08 **Decision:** `/magic.run main` — **T-28D02 Done: a bare invocation launches the terminal UI; track D fully closed (8/10).** New `LaunchMode`/`launch_mode(&[String]) -> LaunchMode` in `main.rs` extracts the routing decision into a pure function, tested with 4 unit tests rather than only through a real spawn (the routing logic is exactly what changed, and it needed to be checkable without a terminal). Scoped precisely to "no arguments at all" — `cronus --format json` (a flag with no verb) deliberately still hits the pre-existing `subcommand_required(true)` usage error, unchanged; that flag stayed in place (not removed), its own doc comment updated to describe the narrowed remaining scope rather than let the next reader think bare invocation is still refused there. Verified against the real binary with a bounded, killed spawn (stdin closed, 3s timeout, no PTY emulation available) rather than a genuine interactive round trip — disclosed as the reason no automated subprocess test attempts the bare case. **Found and fixed a real, latent bug while doing that manual verification**: `installation::dispatch` unconditionally assumed `group_matches.subcommand()` was `Some` — true only for *nested* installation groups. Every **flat** group (`init`/`status`/`doctor`/`restore`, and the newly-added `tui`) carries its one verb's args directly on the group's own `Command`, so `.subcommand()` is genuinely `None` — every flat verb has fallen into `dispatch`'s internal-error branch the moment it was actually *run* (not merely `--help`'d) since Phase 27, invisible because `cli_smoke.rs` had never invoked one that way. Fixed by resolving a `None` subcommand as "this group's own matches ARE the one verb's matches, verb name = group name" — the same rule `build_installation_tree`'s own flat-shape detection already uses. Dead `application_error` deleted. 2 new regression tests (a structural unit test proving the shape; a real-subprocess `cli_smoke.rs` test over `status`/`doctor`, deterministic regardless of host state). `cargo test -p cronus-cli --all-targets` 78/78 (47 + 31); full workspace `cargo test`/`clippy -D warnings`/`cargo fmt --check` green. Containment self-check clean. **Track D (single entry point: T-28D01/D02) is now fully closed.** Computed next step (DA-6): T-28T01 (corpus registration from this crate's own test target; finding F-2 repaid). (Revert: `git restore crates/cli/{src/main.rs,src/installation.rs,tests/cli_smoke.rs} .design/main/{TASKS,STATE}.md .design/main/tasks/phase-28.md`)

- 2026-09-07 **Decision:** `/magic.run main` — **T-28D01 Done: `cronus tui` is a verb of the single binary; `cronus-tui.exe` retired (7/10).** `crates/tui/Cargo.toml`'s `[[bin]]` and `src/main.rs` deleted (autobins would otherwise still pick the file up); `cargo metadata` confirms `cronus-tui` now declares a `lib` target only, `cronus.exe` the workspace's sole binary. New `tui` `Invocable` (flat, `Locus::Installation`, no binders) in `installation.rs::declared_invocables()`, answerable pre-composition like every other installation verb — correct here because `cronus_tui::run()` already composes its own registry/dispatcher internally (T-28C01), so this launcher never needs one first. Its dispatch arm is the one named exception to the module's own "answers through `crate::commands`" rule: it calls `cronus_tui::run()` directly. **Checked the prior phase's five-deleted-stub-groups precedent for the retirement record and found it doesn't transfer** — those were never-real placeholders (deleted outright, no record needed); `cronus-tui.exe` was a real, working, previously-documented binary, so the record went to the three product docs that actually named it (`crates/tui/README.md`, `CONTRIBUTING.md`'s two layout diagrams, `docs/building.ru.md`'s three tables) — rewritten to say `cronus tui`, not merely stripped of the old name. Two spec files (`l2-source-layout.md`/`l2-filesystem-layout.md`) also name it but are outside `/magic.run`'s write scope — left for `/magic.task`'s drift detection, not edited here. Added 2 `cli_smoke.rs` tests (`tui_help_exits_0`, `top_level_help_lists_tui`) so both Verify-line facts are automated, not only manually checked. `cargo test --workspace -j 2` green (`cronus-cli` 42+30); `clippy -D warnings`/`cargo fmt --check` clean. Containment self-check clean. Computed next step (DA-6): T-28D02 (a bare invocation brings up the default composition — the deliberate reversal of Phase 27's `subcommand_required(true)`). (Revert: `git restore Cargo.toml crates/cli/{Cargo.toml,src/installation.rs,tests/cli_smoke.rs} crates/tui/Cargo.toml .design/main/{TASKS,STATE}.md .design/main/tasks/phase-28.md crates/tui/README.md CONTRIBUTING.md docs/building.ru.md && git checkout HEAD -- crates/tui/src/main.rs`)

- 2026-09-07 **Decision:** `/magic.run main` — **T-28C01 Done: panels distinguish unavailable from empty; Board/Office dispatch through the real registry (6/10).** New `view::Projection<T>` applied to all four `CoreSnapshot` fields, not just the two with a real failure path today — structural INV-6 compliance is what the spec's own Scope asks for uniformly, and `Status`/`Sessions` currently always constructing `Available` is disclosed explicitly rather than left for a reader to notice. **Rejected opening `kanban::Board`/`RoleManager` directly from `app.rs`** — the first design considered — because it would replicate finding F-4's exact defect (a frontend computing the domain fact of where the board lives) on a second surface, and this task's own `rg` acceptance line checks precisely for that pattern. Instead changed `SnapshotSource::poll_snapshot`'s signature to take `&InvocableRegistry, &Dispatcher` (a real, disclosed signature change touching `ScriptedSource`/`CapabilitySource`), so `core:board.list`/`core:role.list` dispatch through the loop's own single dispatch mechanism, the same one the command bar uses. **Two disclosed limitations of going through the invocable boundary rather than the domain type directly**: `board.list`'s current outcome carries `id`+`state` only (no `task_ref`), so Board cards render bare ids; `HiredInstance` has no live "current task" field at all, so Office's `task` is always empty — both honest absences this panel receives and renders, never locally derived. `dispatch_projection`'s `Dispatched::Unknown` case maps to `Unavailable` here — a different, correct answer from T-28A03's identically-named case, since this is an internal system read with no user input to treat as ordinary. `cargo test -p cronus-tui --all-targets` 66/66 (56 + 10 new); full workspace `cargo test`/`clippy -D warnings`/`cargo fmt --check` green. Containment self-check clean; `rg` confirms no direct domain-store access in `crates/tui/src`. Computed next step (DA-6): T-28D01 (`cronus tui` becomes a verb of the one binary). (Revert: `git restore crates/tui/src/{app.rs,view.rs} .design/main/{TASKS,STATE}.md .design/main/tasks/phase-28.md`)

- 2026-09-07 **Decision:** `/magic.run main` — **T-28B02 Done: this surface's real `SurfaceProjection` built; the locus difference is a checked `DeclaredExclusion` (5/10).** New `crates/tui/src/conformance_registration.rs`: `TuiProjection` mirrors the CLI's own T-27-era registration shape (isolated registry holding only the 8 shared fixtures, `Registrant::extension`, one handler per fixture) rather than deferring the whole mechanism to the phase's own closing corpus task — the same precedent the CLI set (its own closing task built the registration; the next task only audited and repaid a finding), so the phase's remaining corpus task can audit + repay F-2 rather than construct from scratch. **`exposed()` and `build_catalog` now share one predicate** — new `command::is_projected(&Invocable) -> bool`, closing a real duplicated-derivation gap before it existed twice. **`advertised_binders` honestly reads the registry's own descriptor directly** — disclosed as not the tautology the same choice would be on the CLI, whose schema check reads a *separately generated* clap tree specifically because a real bug lived in that second artifact twice; this surface's real dispatch has no second artifact to diverge from. **Deliberately did not touch the shared `fixtures.rs` crate or the CLI's already-closed registration**: the shared corpus's 8 canonical fixtures are all `Semantic` (nothing either surface needs to exclude today), so proving `DeclaredExclusion` load-bearing needed a canonical member outside that set — built as one local, ad hoc `Corpus` (the shared fixtures plus one synthetic `Locus::Installation` member) entirely inside this crate's own test, rather than extending the shared crate and reopening Phase 27's closed CLI work for a proof this task does not itself need generalized. Two tests prove both halves: declaring the exclusion silences the divergence; removing it reports the id missing. `cargo test -p cronus-tui --all-targets` 56/56 (53 + 3 new); full workspace `cargo test`/`clippy -D warnings`/`cargo fmt --check` green (one unused-import warning caught and fixed first). Containment self-check clean. Computed next step (DA-6): T-28C01 (panels render from core projections, unavailable distinct from empty). (Revert: `git restore crates/tui/{Cargo.toml,src/command.rs,src/lib.rs} .design/main/{TASKS,STATE}.md .design/main/tasks/phase-28.md && rm crates/tui/src/conformance_registration.rs`)

- 2026-09-07 **Decision:** `/magic.run main` — **T-28B01 Done: pane focus/quit are real `ClientLocal` invocables (4/10).** New `crates/tui/src/pane_actions.rs`: one `PaneAction` enum (`FocusNext`/`FocusPrev`/`Quit`) is the single source both registration (`ALL`, group `pane`, `Locus::ClientLocal`, through `Registrant::core()`) and `app.rs`'s key handler (`id()`) read, so a key binding and a registered descriptor can never name a different set. Each handler is a stateless `Outcome::Value(Empty)` acknowledgment; the real mutation (focus change, quit) happens in a new `App::dispatch_pane_action`, applied only on a real `Dispatched::Ran` — an unregistered action answers `Unknown` and produces no effect, proven by 2 new negative tests (Tab/Esc against an empty registry move no focus and do not quit). Key bindings (Tab/Shift+Tab/Esc) kept their exact ordering and target keys; only the underlying mechanism moved from direct mutation to real dispatch. **Only the three actions that exist today were registered** — panel toggles/view switching named in the spec's own candidate list have no real behavior in this crate yet, so inventing placeholder invocables for them was deliberately avoided (speculative surface, not a migration). `run()` registers pane actions before building the catalog, so `/pane focus-next` etc. becomes a real discoverable slash form too. `cargo test -p cronus-tui --all-targets` 53/53 (48 + 5 new); full workspace `cargo test`/`clippy -D warnings`/`cargo fmt --check` green. Containment self-check clean. Computed next step (DA-6): T-28B02 (the two surfaces' locus difference declared via `DeclaredExclusion`). (Revert: `git restore crates/tui/src/{app.rs,lib.rs} .design/main/{TASKS,STATE}.md .design/main/tasks/phase-28.md && rm crates/tui/src/pane_actions.rs`)

- 2026-09-07 **Decision:** `/magic.run main` — **T-28A03 Done: an unresolved slash line renders no feedback — track A fully closed (3/10).** `dispatch_command`'s return type changed from `String` to `Option<String>`: `None` is the ordinary-input case, `Some(text)` is every real outcome. Scoped precisely to the actual `Dispatched::Unknown` arm — the real registry-resolution answer — not to every early return in the function: an incomplete line with no verb at all (`/board` alone) and a sub-verb that cannot even form a well-formed identity both never reach resolution, so both keep their existing usage-hint text unchanged; a user who typed a bare noun expecting a verb plausibly wants a hint, not silence. **Caught and reverted a real over-generalization before landing**: a first draft used the `?` operator for all three early-exit cases uniformly, collapsing them all to `None` — this made an unfinished-but-clearly-intentional `/board` indistinguishable from stray text that merely started with `/`, which is not what the spec's own text asks for (it ties the ordinary-input treatment specifically to SP-13's `Dispatched::Unknown`, a registry resolution concept, not to "any incomplete line"). `classify()`'s own catalog-level "unknown group" error (a genuinely nonsense noun, e.g. `/xyzzy`) stays untouched and out of scope — a discovery-level check against the local catalog, a different and more fundamental usage mistake than a real noun paired with a verb that doesn't yet exist under it. Added 2 unit tests in `dispatch.rs` (`dispatch_command_treats_an_unresolved_identity_as_ordinary_input`, `dispatch_command_still_renders_a_genuine_rejection`) and rewrote the app-level Rejected test into `command_dispatch_distinguishes_an_unresolved_line_from_a_genuine_rejection`, proving both halves of the Verify line's own literal wording in one test end to end through `App::tick`: `/test frobnicate` → `command_feedback == None`; `/test probe` → still a legible refusal. `cargo test -p cronus-tui --all-targets` 48/48 (46 + 2 new); full workspace `cargo test`/`clippy -D warnings`/`cargo fmt --check` green (no flake this run). Containment self-check clean. **Track A (registry projection: T-28A01/A02/A03) is now fully closed.** Computed next step (DA-6): T-28B01 (pane/panel actions register as `ClientLocal` invocables through the shared door). (Revert: `git restore crates/tui/src/{app.rs,dispatch.rs} .design/main/{TASKS,STATE}.md .design/main/tasks/phase-28.md`)

- 2026-09-07 **Decision:** `/magic.run main` — **T-28A02 Done: dispatch through the real, shared Dispatcher; App's trait-object seam deleted (2/10).** New `crates/tui/src/dispatch.rs`: `dispatch_command` resolves `group+args[0]` into a candidate `core:{group}.{verb}` id (never pre-validated against a local list — `registry.resolve` is consulted once only to find binders; an `Unknown` id still reaches the real `Dispatcher::dispatch`, which resolves it again and correctly answers `Unknown` on its own — a deliberate, accepted double-resolve over a second, potentially-drifting existence check), binds remaining raw args via `bind_args`, dispatches, renders via `render_outcome`. `bind_args` is the one legitimate origin site for `RejectionMode::Malformed` in this whole codebase — the domain tier's own `bind()` can only ever produce `Absent`/`IllShaped`, since it validates already-typed values; a raw string that fails to parse as its binder's declared type (`Integer`/`Boolean`/`Float`) short-circuits to a real `Outcome::Rejected` here, exactly where IB-4's own reasoning places it. Positional binder kinds consume raw tokens in declared order; named kinds (`Flag`/`NamedText`/`RepeatableNamedText`) are recognized by a `--{name}` token — the CLI's own three binding shapes, expressed without a clap parser; an unrecognized `--flag` is silently inert, a disclosed simplification no shipped invocable this task exercised needed stricter handling for. `render_outcome`/`render_value` reimplement the CLI's own text-rendering shape locally (INV-2 forbids depending on it). **`App` lost its trait-object seam entirely**: `Box<dyn Dispatcher>` (the crate's own local trait) plus `NoopDispatcher`/`CapabilityDispatcher` all deleted — replaced by owned `registry: InvocableRegistry`/`dispatcher: Dispatcher` fields, since there is exactly one dispatch mechanism now, not a swappable one; `App::with_dispatcher` collapsed into `App::new`, taking the pair directly (an empty, nothing-attached pair is a legitimate inert choice — every command then resolves `Unknown`, like a genuinely unbound verb). **No `.N` split needed, despite this task's own pre-flagged risk**: the real complexity, once designed, split cleanly into `dispatch.rs` (two pure, independently-tested functions, 9 tests) plus one structural wiring pass in `app.rs` — landing in one turn. Recorded that the *pre-chosen* split axis ("outcome plumbing vs feedback rendering") did not match where the complexity actually turned out to live (arg binding), so a future sizing note should re-derive the axis from the real design, not copy one forward. **Tests that exercised dispatch now register a real, test-scoped `core:test.probe` invocable** (mirroring the CLI's own `conformance_registration.rs` fixture-registry precedent) instead of injecting a fake `Capabilities` core through a trait object — the 2 tests that asserted on `CapabilityDispatcher` directly were deleted (not weakened: the property they proved is now proven more strongly, end to end, by the rewritten `App`-level tests). New Rejected-outcome test drives a real dispatch end to end and asserts both the binder name and `RejectionMode::Absent` reach `command_feedback` — the literal Verify criterion. `cargo test -p cronus-tui --all-targets` 46/46 (38 − 2 deleted + 9 new in `dispatch.rs` + 1 new in `app.rs`); full workspace `cargo test`/`clippy -D warnings`/`cargo fmt --check` green (no flake this run). Containment scan caught and fixed 4 bare task-id leaks across new/edited doc comments. Computed next step (DA-6): T-28A03 (an unresolved slash line is ordinary input) — `dispatch_command`'s own placeholder `Unknown` text is exactly what it replaces. (Revert: `git restore crates/tui/src/{app.rs,lib.rs} && rm crates/tui/src/dispatch.rs && git restore .design/main/{TASKS,STATE}.md .design/main/tasks/phase-28.md`)

- 2026-09-07 **Decision:** `/magic.run main` — **T-28A01 Done: slash catalog derived from the registry, mirror deleted, F-2 tombstoned (1/10).** `command::build_catalog(&[&Invocable])` filters `Semantic|ClientLocal` + `Shipped`, groups by `.group`, prepends `help`; `const CATALOG`/`EXPECTED_CLI_VERBS` and their 2 self-comparing parity tests deleted outright, not merely superseded. **Resolved a genuine tension between two specs before writing any code**: `l2-tui` §4.3 ("a slash verb and its shell counterpart are two renderings of one descriptor") read alone could mean per-invocable catalog entries; `l2-cli` §4.4 states the slash form directly as `/<noun> <verb> …`. Reconciled as: the *whole line* and its CLI counterpart are the two renderings, not the leading token alone — so the catalog stays noun/group-level (13 entries), matching the pre-migration granularity and keeping discovery usable, with noun→verb resolution left entirely to T-28A02's dispatch. No curated per-group description built — `Invocable.summary` is per-verb, not per-group, and inventing a second hand-maintained prose list would be the same defect class relocated, not removed; every entry reads `"{group} operations"`, the exact fallback `crates/cli/src/generated.rs` already uses. `CommandSpec.summary` changed `&'static str` → owned `String` (the catalog is built once at runtime from a real registry, not compiled in); `lookup`/`is_known`/`names`/`help_lines`/`classify` all now take `catalog: &[CommandSpec]` explicitly instead of reading a global — matching the CLI's own `generated.rs` convention of explicit slices over globals. `App` gained a `catalog: Vec<CommandSpec>` field; both constructors gained a `catalog` parameter, mechanically updated at all 11 test call sites in `app.rs` (most `Vec::new()`; three needed a `status`-naming fixture since `CapabilityDispatcher::route` — untouched, out of scope — still special-cases that literal verb for its one real dispatch path). **A real, disclosed consequence verified, not assumed**: `"status"` is `Locus::Installation` (reclassified off `Semantic` during the prior phase's own retrofit), so it no longer appears in this surface's real, registry-derived catalog at all — `route`'s hardcoded `"status"` arm is dead in production from this task on, left in place since replacing the whole dispatcher is T-28A02's job. Flipped F-2's `copies_deleted`/`deletion_pinned` to `true` in the finding inventory (both now factually true) alongside its tombstone in both ledgers; left `consumer_registered: false` since this surface has not yet registered against the corpus (T-28T01). `cargo test -p cronus-tui --all-targets` 38/38; `cargo test -p cronus-conformance --all-targets` 16/16; full workspace green twice (one `role_catalog` parallel-isolation flake confirmed pre-existing and unrelated via `--test-threads=1`); `clippy -D warnings`/`cargo fmt --check` clean workspace-wide. Containment scan caught and fixed one bare task-id leak in a new test's doc comment. Computed next step (DA-6): T-28A02 (dispatch through the shared dispatcher). (Revert: `git restore crates/tui/{Cargo.toml,src/command.rs,src/app.rs,src/lib.rs} crates/conformance/src/findings.rs .design/main/{TASKS,STATE}.md .design/main/tasks/phase-28.md`)

- 2026-09-07 **Decision:** `/magic.task main` — **Phase 28 decomposed into 10 tasks / 5 tracks; two engine-caused archival defects cleared.** Engine drift: local `.magic/.version` 2.1.80 == snapshot 2.1.80, silent. Pre-flight `ok:true` on every gate (290 specs, 290 Stable, 0 Draft, header parity clean, no orphans, role registry fully referenced), so Pre-Planning Stabilization was a genuine no-op rather than a skipped step, and Rules Parity already held (RULES v1.6.0 == TASKS Based-on-RULES). **Decomposition was grounded against `crates/tui` itself rather than the plan's prose**, which changed the phase's shape in four ways now recorded in the workbook: (1) the two dependency edges a single binary requires do not exist — `crates/tui` depends on `cronus-domain`/`crossterm`/`ratatui` with **no `cronus-core`**, and `crates/cli` has **no `cronus-tui`** — and they land in different tracks while sharing `crates/tui/Cargo.toml`, so tracks A and D are not as independent as their file lists suggest; (2) this crate has 38 in-file unit tests and **no `tests/` directory at all**, a materially weaker behaviour-preservation net than the command line's 41-test subprocess suite that caught three real pre-ship defects last phase — so T-28T01's corpus registration is named as the replacement oracle, not a formality; (3) the hand-copied mirror is already stale in *both* directions (its `CATALOG` advertises `goal`/`trigger`/`mission`/`research`/`change`, deleted from the product last phase, plus five `Installation`-locus verbs this surface must not project), so derivation visibly shrinks the slash surface — planned as a disclosed change rather than a mid-migration discovery; (4) T-28D02 deliberately reverses the `subcommand_required(true)` Phase 27 shipped, since LH-4 requires a bare invocation to bring up the default composition once one exists — recorded as a planned progression so a later reader does not undo it as a regression. Sizing stated against Phase 27's actual outcome (track D planned as one task, needed six `.N` splits): T-28A02 named as the one to watch, with its split axis pre-chosen. **Two engine-caused archival defects cleared in the same pass**: a stale duplicate `tasks/phase-5.md` shadowing its own archived copy (diffed first — the archive was canonical and strictly better; the duplicate was removed), and `tasks/phase-10b.md` (`Done`, zero open items) which `archive-phases` can **never** see because its readdir filter is `/^phase-\d+\.md$/` and the wave suffix `10b` fails it — archived by hand within the `.design/` write scope (never touching the engine) and recorded via `record-diagnostic` as `PHASE_ARCHIVER_NONNUMERIC_SUFFIX_INVISIBLE`. Computed next step (DA-6): `/magic.run main` at T-28A01. (Revert: `git restore .design/main/{TASKS,STATE,CONTEXT}.md && rm .design/main/tasks/phase-28.md`)

- 2026-09-07 **Decision:** `/magic.run main` (продолжай) — **T-27T02 Done: PHASE 27 COMPLETE, 22/22 tasks, all five tracks.** Audited (not re-ran) the corpus's own real result from T-27D03's registration test against the four named residuals, and where SP-10's own discipline (record at the owning invocable, not just in a tracker) had not yet actually been honored inline, honored it: nine format-discarding sites (`archetype_cmd::list/info/create`, `ext::scan`, `registry::show`, `activation_cmd::enable`'s two silent branches, `ext::list`/`backup_cmd::list_at`'s one branch each) each got a one-line "known residual, not fixed here" comment; audited the post-migration `commands.rs` fresh for the unescaped-JSON-output residual (never itemized by file before) and found exactly five sites interpolating genuinely free user text (`ext::activate`/`deactivate`'s extension id, `registry::create`/`disable`/`enable`'s agent name) — matching the original audit's own count — each commented; the empty-secret-list residual got its comment at the actual composition point, `invocable_bootstrap::bootstrap()`, previously undocumented despite being exercised by every surface this facade serves. Zero logic changed — comments only, behaviour preservation guaranteed by construction, confirmed by two full consecutive green workspace runs (no flaky test). **Corrected one stale Verify-line assumption explicitly rather than silently reinterpreting it**: this task's own line said `cli_smoke.rs` must stay "unmodified", written before T-27D03 existed as scoped work and deleted 13 of its tests on purpose (a disclosed removal, not a migration) — recorded that "unmodified" means "at its current, post-T-27D03, 28-test baseline," not the stale pre-removal 41. Audited every phase-created/heavily-modified file for introduced `unwrap()`/`panic!()`: found none beyond the same disclosed, precedented literal-`.expect()` pattern every `*_bootstrap.rs` file in this facade already uses. Ran a phase-wide (not just this task's own files) containment re-sweep across every crate this phase touched — clean. Closed out `phase-27.md`'s frontmatter (`status: Done`, `provides`/`key_files`/`patterns_established` filled in) and TASKS.md's status cell (`In Progress` → `Done`). Full workspace `cargo test` (×2)/`clippy -D warnings`/`cargo fmt --check` green throughout. Computed next step (DA-6, §5 Post-Task Replan): phase complete → the one user-visible next step is `/magic.task main`, never `/magic.spec`/`/magic.analyze` proactively. (Revert: `git restore crates/cli/src/commands.rs crates/core/src/invocable_bootstrap/mod.rs .design/main/{TASKS,STATE}.md .design/main/tasks/phase-27.md`)

- 2026-09-07 **Decision:** `/magic.run main` (продолжай) — **T-27T01 Done: audited the corpus's real run against the finding inventory, closed F-3, no new findings needed (21/22).** The "first run" the task names had already happened as a structural consequence of the command line's own registration (the prior task's own test drives the real harness and asserts on its result) — this task's job reduced to auditing that result against `crates/conformance/src/findings.rs`'s seed inventory rather than re-executing anything, and updating the ledger where the audit changed something real rather than rubber-stamping "expected to fail" as sufficient. The audit found zero new divergence: the two reports the command line's real projection produces are exactly F-5's already-seeded residual (`secret-bearing`/`boundary-crossing`, this dispatcher's empty secret list). It also found one finding the registration event genuinely closes: **F-3 — the command line's own deleted parity table — is now fully repaid**, its one named site's last open SP-4 condition (`consumer_registered`) satisfied by a real, running consumer. Flipped `Repayment::consumer_registered: true` for F-3 alone; rewrote `no_finding_is_recorded_as_repaid_by_accident` (a blanket "nothing is repaid" assertion, now false) into `exactly_f3_reads_as_repaid_after_the_command_lines_registration`, an explicit per-finding check — the test whose own doc comment says a repaid transition must be visible, not silent, doing exactly that. **F-1/F-2/F-5/F-8 deliberately left untouched**, each checked individually against its own `sites` field rather than assumed closed by one surface's registration: F-1 and F-5 each name three surfaces (only one registered so far); F-2 names the terminal UI's own stale mirror specifically, a site this registration cannot touch; F-8 is preemptive, about a surface that does not exist yet, and the command line already existed when it was seeded. The ledger-baseline tests (gating `tombstones()`/`accepted_debt()` specifically) were unaffected — no tombstone or debt entry changed, only a `seed_inventory()` repayment field, so no baseline edit was needed alongside it. `cargo test -p cronus-conformance --all-targets` 16/16; full workspace `cargo test`/`clippy -D warnings`/`cargo fmt --check` green. Computed next step (DA-6): T-27T02 — the phase's own final task (behaviour-preservation proof + full quality gates), the only one left. (Revert: `git restore crates/conformance/src/findings.rs .design/main/{TASKS,STATE}.md .design/main/tasks/phase-27.md`)

- 2026-09-07 **Decision:** `/magic.run main` (продолжай) — **T-27D03 Done: five unbound stub groups deleted, corpus registered — track D fully closed (20/22).** `goal`/`trigger`/`mission`/`research`/`change` and their `Command`/`*Command` enums/dispatch modules deleted outright per INV-9 (no descriptor ⇒ unrepresentable, not merely hidden). `Cli` now holds only `--format`; losing the non-`Option` subcommand field lost clap's derive-inferred `subcommand_required` too, so restored it explicitly at the one place `main.rs` builds the runtime tree (matching the project's existing preference for explicit clap config over derive inference) — verified directly that a bare invocation still exits 2 with the same usage-error shape as before. The now-unreachable final fallback arm became a defensive internal-error message rather than silently vanishing. `cli_smoke.rs`'s 13 tests for the five removed groups deleted along with them (41→28) — the one deliberate exception to this phase's unmodified-suite discipline all session, since this task's entire point is a disclosed removal, not a migration; its own Verify line never asked for the suite to stay unmodified. **This frontend became the conformance corpus's first real consumer** (§4.5's own registration step, first exercised by any surface): new `#[cfg(test)]`-only `conformance_registration.rs` builds a `SurfaceProjection` from an *isolated* registry holding only the shared corpus's 8 adversarial fixtures — deliberately not the production ~30-group catalog, since the corpus's `check_surface_set`/`check_schema` families compare against whichever `Corpus` is passed to `run`, and mixing populations would prove nothing extra about the generic tree-building/dispatch machinery under test. Real handlers attached per fixture, each producing exactly its declared expected outcome. The schema check inverts a real, generated `clap::Arg` back into a `Binder` (`Box::leak`'d name, a standard low-risk pattern for test-only `'static` data) rather than reading the registry's own stored descriptor back — the latter would only prove the registry echoes its input, exactly the tautological divergence this assertion family exists to catch; scoped honestly to what the corpus's own fixtures actually use (`Text` binders or none — distinguishing `Integer`/`Boolean`/`Float` from clap's type-erased `ValueParser` alone was out of reach and is disclosed, not silently assumed). **The corpus is expected to fail on any surface's first real run (§4.4's own callout) — made that expectation precise rather than vague**: this dispatcher is wired exactly like production's own `compose()`, which has never called `Dispatcher::set_secrets`, so the test asserts the harness reports *exactly* the two fixtures the already-disclosed empty-secret-list residual affects (`secret-bearing`/`boundary-crossing`, seeded as F-5/F-6) and fails loudly on anything else — a stronger, more honest bar than the harness's own suggested default of a blank `assert!(reports.is_empty())`. Full workspace `cargo test`/`clippy -D warnings`/`cargo fmt --check` green; unit suite 42/42, `cli_smoke.rs` 28/28. Computed next step (DA-6): T-27T01 (corpus first run — convert failures into findings before fixing anything), the only task track T has ever named as ready. (Revert: `git restore crates/cli/src/{cli.rs,commands.rs,main.rs} crates/cli/Cargo.toml crates/cli/tests/cli_smoke.rs .design/main/{TASKS,STATE}.md .design/main/tasks/phase-27.md && rm crates/cli/src/conformance_registration.rs`)

- 2026-09-07 **Decision:** `/magic.run main` (продолжай) — **T-27D02 Done: renderer formalized as a pure `Outcome`+format → `Rendered` function, testable in-process (19/22).** `render`/`render_value` had already become, incrementally across every prior D01.2/D01.2.2 sub-task, a genuine pure function of `OutcomeValue` shape plus requested format — what it lacked was provability without a subprocess, since both called `println!`/`eprintln!` directly. Introduced `Rendered { stdout: Vec<String>, stderr: Vec<String>, exit_code: i32 }`: every rendering arm now builds and returns this value; exactly one method (`Rendered::print`) performs I/O, at the one call site in `main()`. Same bytes, same order, same behavior — `cli_smoke.rs`'s real subprocess assertions confirm this directly. Wrote the Verify line's own required test against the now-pure function: a shape-exhaustive case table (11 `OutcomeValue` shapes covering every arm the renderer's `match` distinguishes — bespoke `loop run` done/other, the generic failed/aborted/stopped/paused status field, the single-id record, empty/nonempty lists, bare Text/Integer/Boolean/Empty, a nested Record-with-List) run through both formats, asserting JSON output is JSON-shaped and (for shapes with actual structure to differ) text output differs from JSON output — deliberately NOT per-invocable real dispatch, since the renderer never branches on invocable identity and several shipped groups either resolve a real un-overridable OS state directory or (`knowledge`) a live embedding backend `cli_smoke.rs` itself never exercises for the same reason; multiplying a ~30s timeout across several verbs would buy no additional proof. A second test locks in that `Rejected`/`Unavailable`/`Unknown` all render identical stderr-only diagnostic prose regardless of format — the project-wide convention, made explicit rather than left silently unexercised. A third ties shape coverage to the real registry's 13 shipped semantic group names. **Accounted for the phase's own "nine format-discarding sites" residual precisely while auditing the installation half for comparison** (not touched — out of scope, "record don't fix"): all nine still exist, itemized by file/function (`archetype list/info/create` lack a `Context` param entirely; `ext scan`/`registry show` take one but never read it; two `activation enable` branches and one branch each of `ext list`/`backup list` skip the check their sibling arm makes) — sharpens the existing accepted-debt ledger entry from a vague count into a concrete one. One real test bug caught before landing: a first draft asserted text always differs from JSON, false for a bare Integer/Boolean (`42`/`true` coincide in both syntaxes) — narrowed to only require the inequality where the shapes have actual structure to differ. Full workspace `cargo test`/`clippy -D warnings`/`cargo fmt --check` green; `cli_smoke.rs` 41/41 unmodified; unit suite 41/41 (38+3 new). Computed next step (DA-6): T-27D03 (the five unbound stub groups leave the shipped surface; corpus registration) — the only task left to open in track D. (Revert: `git restore crates/cli/src/main.rs .design/main/{TASKS,STATE}.md .design/main/tasks/phase-27.md`)

- 2026-09-07 **Decision:** `/magic.run main` (session resumed post-compaction) — **T-27D01.2.2 Done: 6 semantic groups migrated, both halves of track D's derivation now closed (18/22).** Registered `board`/`schedule`/`budget`/`loop`/`workflow`/`knowledge` as real `Outcome`-returning handlers over their actual domain calls, through the facade's public door — matching every prior group's pattern. Designed and built the two `Binder`/`ArgValue` extensions the prior task deliberately deferred: `Float` (clap `value_parser!(f64)`, a real usage-level gate) for `budget set`'s dollar limit, `RepeatableNamedText`/`ArgValue::List` (`ArgAction::Append`) for `knowledge query --collection`; dropping `Eq` (kept `PartialEq`) from `ArgValue`/`ArgValues`/`Invocation` since `f64` has no total equality and neither type was ever used as a hash key. `workflow transpile --human/--compact` mutual exclusivity moved from clap's `conflicts_with` into the handler — a disclosed usage→application exit-code shift, the same class `loop run --max-iter`/`knowledge query --top-k` already carry as hand-parsed `NamedText`. Added a generic `status`-field renderer arm (failed/aborted/stopped/paused → exit 1, collapsing `workflow run`'s pre-migration 3-way scheme into 2-way, disclosed since untested) plus a more specific, ordered-first bespoke arm reproducing `loop run`'s exact stdout text byte-for-byte, since a real smoke test parses it to extract a run id for a further round trip. `nodus::executor::Value` turned out structurally isomorphic to `OutcomeValue` — wrote a direct recursive converter (`Float` → `Text`, the same workaround `learn`/`budget` already use). **Two real, pre-ship bugs caught by the existing, unmodified `cli_smoke.rs` suite** (not the new unit tests, which caught neither): `loop run`'s `file` binder bound as positional `Text` when the grammar needed a named `--file` flag; `loop evolve`'s rewritten message dropped the literal word "unavailable" a test asserted on. Both fixed, full suite re-run green — the second time this exact discipline (trust the pre-existing suite over new tests) caught a real defect this phase. **Verified rather than left open**: `knowledge query`'s "embedding failed: Timeout" traces to `KnowledgeService::open_default` always wiring a real local Ollama-convention embedding endpoint, contacted lazily per call — pre-existing infrastructure the old `knowledge` command depended on identically; not a regression, just no local model server running in this environment. Preserved the phase's named residuals exactly (`schedule list`'s store-failure-as-empty-list) and left `board`/`schedule`/`knowledge`'s real-disk write paths untested at the facade level, matching `role`'s already-recorded precedent. Containment self-check across every touched file found and fixed one real leak: a bare task-id in a test doc comment (`dispatch.rs`), restated in plain language. Full workspace `cargo test`/`clippy -D warnings`/`cargo fmt --check` green; `cli_smoke.rs` 41/41 unmodified. Computed next step (DA-6): T-27D02 (the shared renderer, now fully unblocked). (Revert: `git restore crates/contract/src/lib.rs crates/domain/src/invocable/dispatch.rs crates/cli/src/{generated.rs,cli.rs,commands.rs,main.rs} crates/core/src/invocable_bootstrap/mod.rs .design/main/{TASKS,STATE}.md .design/main/tasks/phase-27.md && rm crates/core/src/invocable_bootstrap/{board,schedule,budget,loop_group,workflow,knowledge}.rs`)

- 2026-09-06 **Decision:** `/magic.run main` — **T-27D01.1.2.2 Done: `ext`+`skill` migrated, installation half fully closed (17/22).** Extended the installation tree builder with one extra sub-nesting level using the same dot-in-the-verb-tail signal `verb_of` already uses (`"ext.skill.import"` names sub-group `skill`, tail `import`), rather than a generic N-level scheme — `ext` is the only group that has ever needed a third level. Migrated `ext`'s 6 flat verbs + `skill`'s 3 nested ones through already-proven `crate::commands::ext::*` handlers, unchanged bodies. `installation::dispatch` now takes a group's raw `ArgMatches` and resolves up to 2 levels itself (a verb never has its own subcommand, so "does the match have a further subcommand" safely signals a sub-group) — `main.rs`'s call sites simplified, no per-group nesting-depth knowledge leaked into them. **All eleven installation groups now generate from one declaration**, confirmed against real `cronus --help` output — closes T-27D01.1's own deferred "exactly eleven" clause after 4 sub-tasks and 1 `Binder` extension. Fixed the existing set-equality test's 2-level assumption (it would have recorded `("ext","skill")` as a verb) and added a dedicated 3-level dispatch-resolution test. Full workspace `cargo test` (78 result-sets)/`clippy -D warnings`/`cargo fmt` green. Hit a genuine host memory constraint (PowerShell OOM, not a code defect) mid-verification, resolved by retry once system memory recovered. Computed next step (DA-6): T-27D01.2.2 (semantic half's parallel closure) or T-27D03 (stub removal), both valid. (Revert: `git restore crates/cli/src/{main.rs,cli.rs,commands.rs,installation.rs} .design/main/{TASKS,STATE}.md .design/main/tasks/phase-27.md`)

- 2026-09-06 **Decision:** `/magic.run main` — **T-27D01.2 Done: 6 semantic groups migrated + a real cross-group verb-collision bug fixed (16/22).** Registered `codegraph`/`agent`/`role`/`exec`/`check`/`learn` (19 verbs) as real `Outcome`-returning handlers over the actual domain calls, through the facade's public door — matching `memory`'s own T-27D01 pattern. Split `invocable_bootstrap.rs` into one submodule per group before it grew into a mixed-concern file. **Found and fixed a real, already-shipping bug**: `generated::invocation_from_matches` searched the WHOLE flat semantic descriptor list for a matched group's verb rather than scoping to the matched group, so `role list --presets` was silently dispatching `core:exec.list` (both groups declare `list`) and dropping the flag — invisible while only `memory` was migrated (no shared verb names), caught by exercising the real binary rather than any existing test. Fixed by scoping the search to the matched group; added a dedicated regression test with two same-verb-named groups. Generalized the interim CLI renderer into a real recursive `OutcomeValue` mapping instead of hand-written arms per new shape (none of the 6 new groups had bespoke prior text to preserve). Kept the ephemeral-per-call residual class uniform across `codegraph`/`exec`/`learn` (matching `memory`'s already-recorded one) rather than selectively improving some under this task's pressure. Deliberately left `role`'s write verbs untested at the facade level (would be new coverage, not a gap — pre-migration suite never covered them either). **Split the remaining 6 semantic groups into T-27D01.2.2** (`board`/`schedule`/`loop` now satisfiable, `workflow` needs a disclosed rejection-mode shift, `knowledge`/`budget` still need a new shape). Recorded a genuine host build-instability (rustc crash under full parallelism) to persistent memory. Full workspace `cargo test`/`clippy -D warnings`/`cargo fmt` green (`-j 2`); `cli_smoke.rs` 41/41 unmodified. Computed next step (DA-6): T-27D01.2.2 or T-27D01.1.2.2, both valid. (Revert: `git restore crates/cli/src/{main.rs,cli.rs,commands.rs,generated.rs} crates/core/Cargo.toml .design/main/{TASKS,STATE}.md .design/main/tasks/phase-27.md && rm -rf crates/core/src/invocable_bootstrap && git checkout HEAD -- crates/core/src/invocable_bootstrap.rs crates/core/tests/invocable_invariants.rs`)

- 2026-09-06 **Decision:** `/magic.run main` — **T-27D01.1.2 Done: 5 more installation groups + BinderKind::NamedText built (15/21).** Verifying `workspace`/`backup`/`activation`/`archetype`/`registry` against the source (as the task's own notes demanded) found 4 of 5 needed a named value flag (`--name <value>`) shape `Binder` could not express. Rather than defer a second time, designed and built `BinderKind::NamedText` this pass — modeled the same way `Flag` already conflates value-shape with binding-style — since it was blocking most of what remained on BOTH command-surface halves, not just this task's own slice. `crates/contract` gained the variant; `crates/domain/src/invocable/dispatch.rs`'s `matches_kind` accepts it (not compiler-enforced, proven by a dedicated `bind()` test); `generated.rs`'s binder-to-`Arg` mapping gained one shared arm both halves reuse. Migrated `workspace` (create --name/--path), `backup` (create --to), `activation` (enable --mode, hand-validated, exit 1 on a bad value — application refusal not a usage failure), `archetype` (create --from), and `registry` (needed no extension, migrated alongside for free). `NamedText` carries no default-value slot by design — an absent optional one reads back `None`, the handler applies its own default, matching every other optional binder's contract. **Discovered the same extension also unblocks most of T-27D01.2's own deferred semantic-side groups** (`board`/`schedule`/`loop` now satisfiable; `workflow` needs a disclosed usage→application rejection-mode shift; `knowledge`'s repeatable flag and `budget`'s float value remain genuinely unaddressed) — recorded there for whoever executes it next. **`ext`'s nested `skill` sub-group (a real 3rd tree level) split into T-27D01.1.2.2**, not improvised here. Full workspace `cargo test`/`clippy -D warnings`/`cargo fmt` green; `cli_smoke.rs` 41/41 unmodified; manually verified every new group's real behavior including both a usage-level and an application-level rejection. Fixed one pre-existing, unrelated containment leak found in `dispatch.rs` while already touching that file. Computed next step (DA-6): T-27D01.1.2.2 or T-27D01.2. (Revert: `git restore crates/cli/src/{main.rs,cli.rs,commands.rs,installation.rs,generated.rs} crates/contract/src/lib.rs crates/domain/src/invocable/dispatch.rs .design/main/{TASKS,STATE}.md .design/main/tasks/phase-27.md`)

- 2026-09-06 **Decision:** `/magic.run main` — **T-27D01.1 Done: installation-half mechanism + 5 groups migrated (14/20).** Built `crates/cli/src/installation.rs`: one declaration (`declared_invocables`) consumed two ways — a pre-composition `clap` tree (flat-vs-nested, `allow_external_subcommands(true)`) answerable with ZERO composition for a recognized installation verb (LH-1/LH-5), and the same list registered into the catalog once composition runs (SP-11). `main.rs` restructured into a real two-stage flow: stage 1 parses against installation-only grammar (`Err=>e.exit()` unconditionally correct — external-subcommand mode swallows every unrecognized top-level word, so an error there can only be a genuine usage failure inside a recognized verb's own args); stage 2 composes via a real fallible `compose()` (exit code 3 reserved, structurally honest even though nothing can fail that way yet) then builds the full tree and parses once. Migrated `init`/`status`/`doctor`/`restore` (flat) + `dev` (nested, 3 verbs) — both shapes proven; each dispatches straight to its already-proven `crate::commands` handler, bypassing the shared `Dispatcher`/`Outcome` pipeline (only the CLI ever projects `Installation` locus). **Resolved a real id collision:** `core:status` moved out of the facade's placeholder registration into this frontend's own declaration, backed by the pre-existing real workspace-status check — verified nothing else depended on the placeholder first. **Split the remaining 6 installation groups into T-27D01.1.2** by the same pre-authorized `.N` pattern T-27D01 established. Full workspace `cargo test`/`clippy -D warnings`/`cargo fmt` green; `cli_smoke.rs` 41/41 unmodified; manually verified the real binary end to end including a genuine pre-composition usage-failure exit(2). **Recorded, not fixed:** a pre-existing `role_catalog.rs` parallel test-isolation race, unrelated to this task, found by the full-workspace run (`ROLE_CATALOG_PARALLEL_TEST_RACE`). Computed next step (DA-6): T-27D01.1.2 or T-27D01.2, either valid on track D. (Revert: `git restore crates/cli/src/{main.rs,cli.rs,commands.rs} crates/core/src/invocable_bootstrap.rs crates/core/tests/invocable_invariants.rs .design/main/{TASKS,STATE}.md .design/main/tasks/phase-27.md && rm crates/cli/src/installation.rs`)

- 2026-09-06 **Decision:** `/magic.run main` — **T-27D01 Done: generated-parser mechanism + `memory` group migrated, Track D opened (13/19).** Built `crates/cli/src/generated.rs`: a real, tested registry-driven `clap` builder-API tree generator over `Semantic`+`Shipped` invocables — groups by declared `group`, one verb-subcommand per invocable, `Binder`→positional/named-flag, dispatches through the real `Dispatcher`; augments the SAME tree the derive macro produces rather than maintaining two. `main.rs` rewritten for dual-path dispatch (registry-generated group first, legacy derived enum fallback — the launcher never resolves a name an extension could define, LH-1/LH-5) plus an explicitly-interim bridge renderer (real uniform renderer is T-27D02's separate job). `memory` (store/search/forget) fully re-expressed as `Outcome`-returning handlers in the facade; old `commands.rs::mod memory` tombstoned. **Two real bugs caught by the new tree existing, not by a written test:** `core:status`/`core:version` were still `Semantic`-classified from before `Locus::Installation` existed, colliding with the legacy `status` command (clap panic, 39/41 smoke tests) — reclassified; a `Flag`-kind binder bound without `--name`, silently positional-only — fixed, regression test added. **Formally split remaining scope** (`.N` convention) rather than cut corners: 12 real semantic groups → T-27D01.2, itself pre-split into 6 groups ready now vs. 6 needing a `Binder` extension (named flags for value-args, defaults, float) designed once rather than improvised per-group. Ephemeral in-memory store behaviour preserved exactly (SP-9/SP-10) and separately flagged via `record-diagnostic --code=MEMORY_STORE_EPHEMERAL_PER_CALL`. Full workspace `cargo test`/`clippy -D warnings`/`cargo fmt` green. Computed next step (DA-6): T-27D01.1 (the installation half). (Revert: `git restore crates/cli/src/{main.rs,cli.rs,commands.rs} crates/cli/Cargo.toml crates/core/src/invocable_bootstrap.rs crates/core/tests/invocable_invariants.rs crates/domain/src/invocable/registry.rs Cargo.toml .design/main/{TASKS,STATE}.md .design/main/tasks/phase-27.md && rm crates/cli/src/generated.rs`)

- 2026-09-03 **Decision:** `/magic.run main` — **Phase 26 (Application Shell Runtime) COMPLETE, 11/11, archived.** Runtime plumbing (stores, resolver, seam, persistence) real but binds only `version`/`status` + host settings + an empty event-channel class — the honestly-partial shell. Full detail archived with `phase-26.md`; see `archives/tasks/phase-26.md`.
