# Project State

<!-- STATE.md — live project memory. Read FIRST in every workflow session. -->
<!-- Maximum 100 lines. Agent updates AFTER each completed action. -->

**Workspace:** main
**Updated:** 2026-07-18 07:24
**Phase:** 21 — Knowledge Store (In Progress, 2/8 tasks)
**Status:** Active

## Current Position

- **Task:** `/magic.run main` — **T-21B01 Done: ingestion core (2/8).** New `crates/domain/src/knowledge_ingest.rs`: `chunk_text` (sentence-boundary chunker, disclosed word-heuristic) + `FileIngester`/`RecordIngester` (KB-5) + `EmbeddingBackend` seam over `contract::InferenceBackend::embed` + `ingest_document` orchestrating the full KB-3 lifecycle (pending→indexing→ready|error). **Discovered and fixed a real correctness gap in A01** (a mid-embed failure could leave a document neither in its prior nor its new chunk state) by adding an atomic `KnowledgeStore::reindex_chunks` (contract + store-local), pre-validated before the transaction opens — the `WikiCache::apply_regeneration` precedent. 9/9 domain tests + 2 new store-local atomicity tests (19 total knowledge tests). `cargo test --workspace` green ×3; clippy + fmt clean.
- **Spec:** INDEX v1.0.123 (210 specs: **207 Stable, 3 RFC, 0 Draft**) · PLAN v2.32.0 · TASKS v1.34.0 · RULES v1.5.0 (parity). Backlog: 3 (all RFC, all concept-only L1).
- **Next Action:** Execute T-21B02 URL adapter + KB-5 source-type completeness + document status lifecycle via /magic.run main

## Progress

```
Build phases: Done 1–20 | **Phase 21 (Knowledge Store) IN PROGRESS — 2/8 tasks** (A schema DONE · B01 ingestion DONE · B02 URL adapter NEXT · C retrieval · D authorship+curation+access+CLI · T validation) | Backlog: 3 concept-only L1-RFC | Next: /magic.run main (T-21B02)
```

- 2026-07-18 **Decision:** `/magic.run main` — **T-21B01 Done: ingestion core. Phase 21 (2/8).** New `crates/domain/src/knowledge_ingest.rs`: `chunk_text`/`split_into_sentences` — a sentence-boundary chunker over `.`/`!`/`?` + trailing whitespace (Unicode-scalar scan, disclosed as a word-boundary heuristic, not full UAX#29 segmentation), `chunk_size`/`overlap` approximated by whitespace-word count matching the spec's 512/64 defaults; a single over-long sentence is kept whole rather than split (sentence-boundary invariant beats exact size). `FileIngester::extract` reads a `domain::file_store::FileStore` blob as UTF-8 (disclosed: real PDF/DOCX extraction out of scope); `RecordIngester::extract` is a documented passthrough. `EmbeddingBackend` trait + `InferenceEmbeddingBackend` adapter over `contract::InferenceBackend::embed` (facade wires the real backend; domain tests use a deterministic fake — the `wiki_regen` generator-optional precedent). `ingest_document` orchestrates the full lifecycle: write `pending→indexing`, chunk + embed **every** piece before any store write, one atomic `reindex_chunks` call, then `indexing→ready`; any embed/store failure marks `Error` with a diagnostic `error_msg` via best-effort write. **Real correctness gap found and fixed during this task itself (not user feedback):** A01's `delete_chunks`+`insert_chunk`-as-two-calls design let a mid-loop embed failure delete the OLD chunks before any NEW one was written — a document could land in neither its prior nor its new state, violating l2-knowledge-store §4.4's "collection remains queryable from its prior state." Fixed by adding `KnowledgeStore::reindex_chunks(document_id, &[(Chunk, Vec<f32>)])` (contract trait + `store-local::KnowledgeDb` — one SQL transaction, every embedding dimension pre-validated *before* the transaction opens so a malformed batch never touches the store at all) — the `WikiCache::apply_regeneration` atomicity precedent. `delete_chunks`/`insert_chunk` remain for standalone removal/GC use, just no longer paired for re-index. 2 new store-local tests (`reindex_chunks_is_all_or_nothing_on_a_malformed_batch`, `reindex_chunks_replaces_the_full_prior_set` — store-local knowledge tests 15→17) plus 9 new domain `knowledge_ingest` tests — 26 knowledge-scoped tests total across both crates. Chunk ids via a local `AtomicU64`+timestamp counter (the `contract::generate_id` idiom, kept module-local since one ingest mints many ids at once — never per-instance state). `cargo test -p cronus-domain knowledge_ingest::` 9/9; `cargo test --workspace` green across 3 consecutive runs (0 failed; the `unrecognized subcommand 'totally-unknown-cmd'` line is a pre-existing CLI test's expected assertion, not a failure) + clippy `-D warnings` (workspace-wide) + fmt clean. No `unwrap()`/`panic!()` on production paths (verified). Remaining KB-5 source type (URL adapter) is T-21B02. Next: `/magic.run main` continues with T-21B02. (Revert: git restore .design/main/{STATE,TASKS}.md .design/main/tasks/phase-21.md crates/contract/src/lib.rs crates/store-local/src/knowledge.rs crates/domain/src/lib.rs && rm crates/domain/src/knowledge_ingest.rs)
- 2026-07-18 **Decision:** `/magic.run main` — **T-21A01 Done: schema + contract types + store scaffolding. Phase 21 started (1/8).** New `crates/store-local/src/knowledge.rs` (single-file, `wiki.rs` precedent): full schema (`knowledge_collection`/`knowledge_directory`/`knowledge_document` with `origin`/`curation`/`ix_kdoc_curation`/`ix_kdoc_status`/`ix_kdoc_collection`/`knowledge_chunk`/`knowledge_chunk_fts`(FTS5 standalone)/`knowledge_chunk_vec`(real `vec0`, TEXT PK, `FLOAT[768]`)) + `KnowledgeStore` contract port (no `Send+Sync` — `rusqlite::Connection` isn't `Sync`, the `WikiCache` precedent). **KB-9** enforced via a typed `WriteOverride::HumanDirected{audit_ref}` at the single `write_document` seam (refuses rewriting an existing human-origin row without it; a brand-new human document isn't gated — the gate protects rewriting, not initial ingest). **KB-10** via `set_curation` requiring `human_auth` for `Reviewed`/`Stable` (`Draft` agent-free). **KB-8** via `soft_delete_document`+`gc`. **KB-3** via `delete_chunks`. Also delivered the retrieval primitives originally scoped for Track C (`ann_search`/`fts_search` — over-fetch from `vec0`/FTS5 then post-filter by collection/status, l2-knowledge-store §2's documented "post-ANN filtering"; `hydrate_chunks` — KB-6 attribution + KB-10 min_curation floor with human-origin always eligible) — narrowing C01's remaining scope to RRF fusion + domain composition only. **Dependency decision surfaced via AskUserQuestion** (defer-to-FTS-only vs. build real `vec0` now, the `ureq`/`windows-sys` flag-before-proceeding precedent) — **user directed "decide as the engineer, optimize for quality/scalability/flexibility"**: added `sqlite-vec = "0.1.10-alpha.4"` (workspace + store-local deps) and built the real ANN, not a deferred stub. **Real upstream bug found and fixed (not swept under the rug):** sqlite-vec 0.1.10-alpha.4 (latest published, no newer stable exists) defaults `SQLITE_VEC_ENABLE_DISKANN`/`SQLITE_VEC_ENABLE_RESCORE` to `1` in its C source, but the published crates.io tarball omits their companion `.c` files — a genuine packaging bug causing "file not found" on a bare `cargo build`, with no way to disable from the crate's own build.rs. Root-caused by reading the vendored C source directly (not guessed); fixed via **`.cargo/config.toml`** (`CFLAGS = "-DSQLITE_VEC_ENABLE_DISKANN=0 -DSQLITE_VEC_ENABLE_RESCORE=0"`, durable — applies to every `cargo` invocation, not a manual per-session env var); verified this global CFLAGS doesn't disturb any other C build in the workspace via a full `cargo check --workspace` pass. Also confirmed via the vendored source (not assumed) that this vec0 build supports `TEXT PRIMARY KEY` (`pkIsText`) matching the spec's illustrative DDL, and KNN queries return the declared PK column directly, no rowid join. **Also fixed:** `RetrievedChunk` couldn't derive `Eq` (contains `f32`, no total ordering) — dropped to `PartialEq`, caught by `cargo check` immediately. 15/15 knowledge tests pass incl. a real nearest-neighbour proof against the genuine `vec0` extension (not a stub) and both KB-9 refuse/override paths; `cargo test --workspace` green across 3 consecutive runs (0 failed) + clippy `-D warnings` (workspace-wide) + fmt clean. No `unwrap()`/`panic!()` on production paths (verified). Next: `/magic.run main` continues with T-21B01 (ingestion core — ingestion composes A01's already-built primitives, does not reimplement store I/O). (Revert: git restore .design/main/{STATE,TASKS}.md .design/main/tasks/phase-21.md Cargo.toml Cargo.lock crates/contract/src/lib.rs crates/store-local/{Cargo.toml,src/lib.rs} && rm crates/store-local/src/knowledge.rs .cargo/config.toml)
- 2026-07-18 **Decision:** `/magic.task main` — **opened Phase 21 — Knowledge Store (the `/magic.spec`→`/magic.task` handoff).** Engine drift: local `2.1.49` == snapshot `2.1.49`, silent. Pre-flight ok:true (0 orphans, header parity clean; expected warnings — 3 concept-only RFC in Backlog + the SYNC_GAP from the just-completed `/magic.spec`, resolved here). Pre-Planning Stabilization 0 promoted (0 Draft). C6 pulled the now-Stable `l2-knowledge-store` from Backlog and decomposed it into **8 tasks / 5 tracks**: A schema+types (T-21A01 `knowledge_*` DDL + FTS5 + `vec0` in `crates/store-local/src/knowledge/` + contract types with `origin`/`curation`/`WriteOverride`/`min_curation` — gates all) · B ingestion (B01 chunking + File/Record adapters + embed-via-fake-`EmbeddingBackend` seam + transactional chunk/fts/vec write + KB-3 delete-before-insert re-index → B02 URL adapter + KB-5 completeness + status lifecycle) · C retrieval (C01 ANN + FTS5 + RRF fusion, KB-1 isolation the spine → C02 KB-11 query-prep fallback-to-raw + KB-10 `min_curation` filter) · D authorship+curation+access+CLI (D01 KB-9 human-zone `WriteOverride` at the single store seam + KB-10 curation gates + KB-8 soft-delete/GC → D02 `ResourceKind::Knowledge` access gate + facade `KnowledgeStore` + `cronus knowledge` CLI) · T validation (KB-1…KB-11 sweep). **Store-subsystem shape (memory/wiki precedent), foundation-then-parallel; critical path T-21A01.** **[DR] Crate placement reconciled at plan level** — the spec §4.5 standalone `crates/knowledge-store/` maps onto the post-Phase-13 decomposition (`store-local`/`domain`/`core` facade); a mechanical `/magic.spec` §4.5 amendment is a non-blocking follow-up. (Override: /magic.spec amend l2-knowledge-store §4.5). **Principal execution risk (disclosed):** real `vec0` ANN needs the sqlite-vec extension — a genuine external-dependency decision (the `ureq`/`windows-sys` flag-before-proceeding precedent), NOT autonomous; FTS5 + RRF + ingestion + authorship/curation/access build without it, vector half degrades to a documented seam if deferred (generator-optional precedent). Embedding via `contract::InferenceBackend::embed` (Phase 17, built) tested against a deterministic fake in CI — the memory store's own vector is still a zeroed stub, so the real sqlite-vec wiring is net-new. Planner audit: sizing sound (RAG adds ANN/RRF/3-adapters over the 7-task wiki shape); hidden deps contained (C←B, D01←A; shared store-port in A01); cascade risk if A01 wrong → prove with round-trip tests first. prompt-engineer PQ-6 PASS. PLAN v2.31.0→2.32.0 (based-on INDEX v1.0.123, SYNC_GAP closed) / TASKS v1.33.0→1.34.0. RULES parity v1.5.0 unchanged. CONTEXT.md regenerated + export-wiki refreshed (2 workspaces · 225 specs). finalize --workflow=task v0.1.376→0.1.377 (CHANGELOG skipped duplicate; STATE header re-fixed by hand after finalize's update-state clobbered Task/Spec/Progress — recurring hygiene). Clean `/magic.task` completion → handoff to `/magic.run main` (Phase 21). Next: `/magic.run main`. (Revert: git restore .design/main/{PLAN,TASKS,STATE,CONTEXT}.md .design/.version && rm .design/main/tasks/phase-21.md)
- 2026-07-18 **Decision:** `/magic.spec` (blank trigger → `main`) — **`l2-knowledge-store` promoted RFC→Stable; DA-3 highest-coverage gap.** Engine drift: local `.magic/.version` `2.1.49` == snapshot `.design/INDEX.md` `2.1.49`, proceeded silently. Blank trigger → Proactive Architect: ranked the 4 Backlog RFCs by DA-3, selected `l2-knowledge-store` — the sole **L2**-RFC and the only one whose promotion opens a build phase (a whole RAG subsystem), over the three concept-only L1-RFCs (`l1-spec-driven-governance`, `l1-dynamic-harness`, `l1-dev-office` — promoting them opens no build phase). Decisive fact: the parent `l1-knowledge-base` is already **Stable v1.2.0** and fully defines KB-1…KB-11, so no L1 design blocker — the L2 had merely lagged (KB-9/KB-10 marked "Pending v1.1.0 parent", KB-11 absent entirely). **`l2-knowledge-store` 1.0.0→1.1.0 Stable**: authored the missing compliance — **KB-9** authorship zones (`knowledge_document.origin` human/agent + store-enforced read-only human zone via a typed `WriteOverride::HumanDirected` token carrying an `audit_ref`, at the single `db.rs` write seam so it survives agent drift; `origin` assigned from the ingestion source, never chosen by a later agent write, so the agent cannot mint a human row to smuggle authority), **KB-10** curation lifecycle (`curation` draft→reviewed→stable, human-gated advancement via `set_curation`, `RetrievalRequest.min_curation` trust floor with human-origin/NULL rows always eligible), **KB-11** query preparation (`QueryPreparer` seam, prepared+raw recorded, fallback-to-raw floor never an empty search, sub-query RRF merge, scope-preserving — never widens KB-4 access nor alters KB-6 attribution). Schema gained `origin`/`curation` columns + `ix_kdoc_curation`; retrieval flow gained the prep step + curation filter; added a Document History section (was missing). Delta-editing throughout (file >200 lines). Guards clear: VERSION_DRIFT none (L2 1.0.0/RFC == INDEX; parent 1.2.0/Stable == INDEX), Parent Existence + Header Parity confirmed, layer constraint satisfied. Post-Update Review real: spec-critic PASS (KB-1…KB-11 substantive, edge-cases closed — new-human-origin smuggle, NULL-curation eligibility, empty-prep fallback; all Related/Implements/Canonical links on disk; no RULES conflict — a domain-capability spec) + prompt-engineer PQ-6 PASS. No C12 cascade (upward promotion). INDEX v1.0.122→1.0.123 (207 Stable / 3 RFC / 0 Draft). export-wiki refreshed (2 workspaces · 225 specs). finalize --workflow=spec v0.1.375→0.1.376 (CHANGELOG appended "Updated specification `knowledge-store` (main)" — a bare kebab-stem consistent with the finalize script's 150+ prior entries, not a hard §6 filename/task-id/phase-designator leak; STATE.md header re-fixed by hand after finalize's update-state clobbered Next Action + Progress — the recurring hygiene pattern). `l2-knowledge-store` is now Stable-but-unbuilt → `/magic.task` opens its build phase. Next: `/magic.task main`. (Revert: git restore .design/main/INDEX.md .design/main/specifications/l2-knowledge-store.md .design/main/STATE.md CHANGELOG.md .design/.version)
- 2026-07-18 **Decision:** `/magic.run main` — **Phase 20 (Archetype Catalog) COMPLETE (6/6).** New module `crates/domain/src/archetype/{schema,catalog,selection,deviation}.rs` + facade re-export `cronus_core::archetype` + `cronus archetype list|info|set|create` CLI. Load-bearing structural properties realized: **OA-4** made unrepresentable by a closed key-set gate (`validate_definition_keys` rejects a fifth key; the zero-dep domain tier validates the key SET at the parse boundary, decoupled from JSON parsing); **OA-3** enforced by return type — `OfficeDeviations::record_hire` returns `()`, so it has no channel to refuse a hire, only classify one the gate already allowed (the pool reaches only the recorder, never a gate signature). Ships `software-engineering` (18-role pool, empty seed) + the two declared-blocked archetypes with their §4.4 missing-role lists (OA-10 honesty). `infer` (OA-7) + `set`/`clear` touch only the archetype record (OA-8) + archetype-free complete (OA-11) + `create_from_preset` derived_from (OA-6) + `Unvalidated`-until-observed (OA-9). Validation: 24 domain tests + 4 real CLI-smoke tests (incl. `archetype hire` rejected by clap — no such verb, §4.8) + a 10-test facade-level OA-1…OA-11 sweep (`crates/core/tests/archetype_invariants.rs`). One transient `role_catalog` isolation flake early (passes in isolation 17/17, QLY-8 pattern, unrelated to the pure archetype module); the full close-out gate was `cargo test --workspace` green across **3 consecutive runs (0 failed)** + clippy `-D warnings` + fmt clean. Disclosed deferrals: the ROL-9-gated `l2-role-catalog` nine-role amendment, the imported-archetype vetting path (OA-5), the office-scoped active-archetype record. Documentation closed out: PLAN v2.30.0→2.31.0 (header→Done, checklist [x]+6/6, milestone para), TASKS v1.32.0→1.33.0 (row→Done, Overview, meta), phase-20.md frontmatter Done + provides/key_files/patterns. Next: `finalize --workflow=run` (archive), then `/magic.task main` (Post-Task Replan). (Revert: git restore .design/main/{STATE,TASKS,PLAN}.md .design/main/tasks/phase-20.md crates/domain/src/lib.rs crates/core/src/lib.rs crates/cli/src/{cli,commands}.rs crates/cli/tests/cli_smoke.rs && rm -rf crates/domain/src/archetype crates/core/tests/archetype_invariants.rs)
- 2026-07-18 **Decision:** `/magic.task main` — **opened Phase 20 — Archetype Catalog (the `/magic.spec`→`/magic.task` handoff).** Engine drift: local `2.1.49` == snapshot `2.1.49`, silent. Pre-flight ok:true (0 orphans, header parity clean; expected warnings — 4 RFC in Backlog + the SYNC_GAP from the just-completed `/magic.spec`, resolved here). Pre-Planning Stabilization 0 promoted (0 Draft). C6 pulled the now-Stable `l2-archetype-catalog` from Backlog and decomposed it into **6 tasks / 5 tracks**: A schema+validation (T-20A01 closed four-key `ArchetypeDefinition` + validator: fifth key fails → OA-4 unrepresentable, pool/seed ids resolve against `roles::catalog::PRESET_CATALOG` → OA-10, seed cap 2 + justification → OA-2) · B catalog+shipped+selection (B01 `ArchetypeCatalog` + the embedded `software-engineering` archetype 18-role-pool/empty-seed + declared-blocked entries + preset/custom `derived_from` → B02 inferred selection + application, archetype-free complete OA-11) · C deviation recording (C01 three counters + **pool read by the recorder never the hire gate** OA-3 + `unvalidated` state OA-9) · D command surface (D01 `cronus archetype list|info|set|create`, no `hire` verb) · T validation sweep (OA-1…OA-11). **Domain-logic-first**, `crates/domain/src/archetype/` following the `roles` precedent; the shipped archetype is an embedded program-tier constant. Critical path = T-20A01 (the closed-key schema gates every track). Planner audit: the single most likely implementation error is inverting the pool/gate order → enforced by a test that a hire outside the pool succeeds; **two disclosed deferrals** carry forward — the ROL-9-gated `l2-role-catalog` nine-role amendment (unblocks the other two archetypes) and the imported-archetype content-vetting path (OA-5, depends on the admission-vetting gate). prompt-engineer PQ-6 PASS. PLAN v2.29.0→2.30.0 (based-on INDEX v1.0.122, SYNC_GAP closed) / TASKS v1.31.0→1.32.0. RULES parity v1.5.0 unchanged. CONTEXT.md regenerated + export-wiki refreshed (pending this turn). Clean `/magic.task` completion → handoff to `/magic.run main` (Phase 20). Next: `/magic.run main`. (Revert: git restore .design/main/{PLAN,TASKS,STATE,CONTEXT}.md && rm .design/main/tasks/phase-20.md)
- 2026-07-17 **Decision:** `/magic.spec` (blank trigger → `main`) — **`l2-archetype-catalog` promoted RFC→Stable; DA-3 highest-coverage gap.** Engine drift: local `.magic/.version` `2.1.49` == snapshot `.design/INDEX.md` `2.1.49`, proceeded silently. Blank trigger → Proactive Architect: ranked the 5 Backlog RFCs by DA-3, selected `l2-archetype-catalog` — the sole mature L2-RFC with a Stable parent (`l1-office-archetype` v1.0.0), cleanly promotable and immediately buildable, over `l2-knowledge-store` (needs KB-9/KB-10 design work first) and the three L1-RFCs (concepts with no built-bound L2 → promoting them opens no build phase). **`l2-archetype-catalog` 1.0.0→1.0.1 Stable**: office archetypes (OA-1…OA-11) — two-artifact trust-boundary split (`archetype.json` mechanically-validated references + `NORMS.md` content-vetted prose), a closed four-key schema making OA-4 unrepresentable, pool read by the deviation recorder never by the hire gate (OA-3), three deviation counters + explicit `unvalidated` state (OA-9); ships `software-engineering` (18-role pool, empty seed) with `advertising-agency`/`finance-department` declared-blocked pending a ROL-9-gated `l2-role-catalog` amendment. **Reviewed as-authored, no requirement change** — spec-critic PASS (OA-1…OA-11 substantively complete, coherent, no RULES conflict — a domain-capability spec) + prompt-engineer PQ-6 PASS; the only edit was cosmetic (§4.4 deferred-scope `<!-- TBD -->` → visible prose, so the Stable spec carries no dangling TBD; its content was already in the surrounding text). Guards clear: VERSION_DRIFT none, Parent Header Parity confirmed (`l1-office-archetype` Stable v1.0.0), layer constraint satisfied. No C12 cascade. INDEX v1.0.121→1.0.122 (206 Stable / 4 RFC / 0 Draft). **The PowerShell classifier was briefly unavailable mid-turn, so the file edits landed first and the executor steps ran once it cleared: sync-check ok:true (SYNC_GAP + SPEC_STATUS expected), export-wiki refreshed (225 specs), finalize --workflow=spec v0.1.367→0.1.368 (CHANGELOG appended "Updated specification archetype-catalog (main)" — a bare kebab-stem consistent with the finalize script's 150+ prior entries, not a hard §6 filename/task-id/phase-designator leak).** `l2-archetype-catalog` is now Stable-but-unbuilt → `/magic.task` opens its build phase. Next: `/magic.task main`. (Revert: git restore .design/main/INDEX.md .design/main/specifications/l2-archetype-catalog.md .design/main/STATE.md .design/main/CHANGELOG.md CHANGELOG.md)
- 2026-07-17 **Decision:** `/magic.run main` — **T-19T01 Done: validation sweep. PHASE 19 COMPLETE (8/8).** New `crates/core/tests/loop_invariants.rs` — 11 tests (LG-7 split refuse/promote) through the real facade export chain (`cronus_core::loop_runner::{...}`), proving the assembled stack composes: LG-1 declared class; LG-2 `guard_writes` rejects an undeclared artifact; LG-3 the six-variant exhaustive match (no `Criteria` arm compiles); LG-4 `judge` lineage-match stamping; LG-5 the second `run_execution` call receives only the compact status, not a transcript; LG-6 `check_ceiling`'s signature excludes actor/oracle entirely; LG-7 `escalate` refuses on shared lineage despite a winning metric, promotes on a real held-out gain; LG-8 two mutations across two iterations both land in the ledger in order; LG-9 `select_oracle` prefers deterministic; LG-10 the objective survives `trim_cascade(entries, 0)` and resumes with current (not stale) progress after simulated compaction. **One test bug caught during the sweep itself (not a real defect):** `lg4`'s first draft compared a Judge lineage against a literal that didn't match the `turn()` fixture's actual lineage — corrected. Closed out documentation: `phase-19.md` frontmatter `status: In Progress→Done` + `provides`(9 items)/`key_files`(10 created, 5 modified)/`patterns_established`(4 patterns) filled; `TASKS.md` v1.30.0→1.31.0 (row→Done, Overview rewritten, meta entry, stray leftover text from a prior partial edit cleaned up); `PLAN.md` v2.28.0→2.29.0 (header→**Done**, checklist `[x]`+8/8 summary, milestone paragraph). `cargo test --workspace` green across 3 consecutive full runs (0 failed every time — no flake this phase) + clippy `-D warnings` + fmt clean. Next: run `finalize --workflow=run` to close T-19T01 + archive the phase, then `/magic.task main` (Post-Task Replan — no new phase expected, every currently-Stable spec now built). (Revert: git restore .design/main/{STATE,TASKS,PLAN}.md .design/main/tasks/phase-19.md crates/core/tests/loop_invariants.rs)
- 2026-07-17 **Decision:** `/magic.task main` — **opened Phase 19 — Loop Runner (the `/magic.spec`→`/magic.task` handoff).** Engine drift check: local `.magic/.version` `2.1.49` == snapshot `.design/INDEX.md` `2.1.49`, proceeded silently. Pre-flight ok:true (0 orphans, header parity clean; the two expected warnings — 5 RFC in Backlog + the SYNC_GAP from the just-completed `/magic.spec` promotion, resolved here). Pre-Planning Stabilization 0 promoted (0 Draft remains). C6 pulled the now-Stable `l2-loop-runner` from Backlog and decomposed it into **8 tasks / 5 tracks**: A types+governor (A01 `LoopSpec`/`MutationManifest`/`MutableArtifact`-no-Criteria/`Oracle`/`Ceiling`/`ObjectiveSlot` → A02 single-point governor check_ceiling/guard_writes/judge) · B execution-runner+oracle (B01 run_execution attempt→rollback→retry → B02 deterministic/judge/human oracle wiring) · C evolution-runner+escalation (C01 run_evolution nesting execution scoring → C02 held-out escalation gate) · D LG-10+facade+CLI (D01 objective re-projection → D02 `loop_bootstrap.rs` real-subsystem wiring + `cronus loop run|evolve|log|show`) · T validation sweep (LG-1…LG-10). **Composition phase (not domain-logic-from-scratch):** the runner composes shipped subsystems (execution-workspace/budget-engine/model-router/session-checkpoint/version-control/agent-autonomy + nodus provider traits) behind one governed contract; the pure loop logic is CI-tested against fakes via the spec's own abstractions (`Oracle`/`ProviderType`/`BudgetHandle`), real subsystem wiring isolated to the `crates/core` facade + one real end-to-end CLI smoke — the Phase 18 `SystemCalls`-seam testability precedent. Critical path = T-19A01 (the type shape gates every track; LG-3-by-construction — no `Criteria` variant — is the load-bearing invariant). Planner audit: hidden dependency = the many composition targets (contained to the facade wiring D02, never spread across domain logic); cascade risk if A01 types are wrong → prove with the governor unit tests before building runners; **disclosed scope boundary** = `l2-loop-runner` §6's migration of the existing orchestration/kanban/heartbeat loops to route through the runner is a deferred behavior-preserving follow-up, NOT this phase. prompt-engineer PQ-6 PASS. `l1-loop-governance` repositioned to PLAN Phase 0 as a realized concept (no longer concept-only — it has a build-bound L2). PLAN v2.27.0→2.28.0 (based-on INDEX v1.0.121, SYNC_GAP closed) / TASKS v1.29.0→1.30.0. RULES parity v1.5.0 unchanged. CONTEXT.md regenerated; export-wiki refreshed. Clean `/magic.task` completion → handoff to `/magic.run main` (Phase 19). Next: `/magic.run main`. (Revert: git restore .design/main/{PLAN,TASKS,STATE,CONTEXT}.md && rm .design/main/tasks/phase-19.md)
- 2026-07-17 **Decision:** `/magic.spec main` — **loop-governance chain promoted RFC/Draft→Stable; DA-3 blank-trigger target.** Engine drift check: local `.magic/.version` `2.1.49` == snapshot `.design/INDEX.md` `2.1.49`, proceeded silently. Blank trigger (workspace-only) → Proactive Architect (spec.md Blank Trigger): surfaced candidate sparks, ranked by DA-3, selected the **loop-governance subsystem** as the highest-coverage gap — the mature RFC keystone `l1-loop-governance` (v0.2.0, complete, its own history naming adversarial verification as the gate to Stable) plus its Draft L2 `l2-loop-runner` held back **only** by the layer constraint (not incompleteness), and the direct conceptual downstream of the Phase 18 always-on hub (a hub exists to run autonomous loops). **Rejected `l2-value-settlement` authoring** despite the fresh `l1-value-settlement` concept: its L1 explicitly records "concept-only: no L2 realization planned yet (a durable gating contract, not pending implementation work)" — forcing an L2 would contradict recorded design intent. **`l1-loop-governance` 0.2.0→1.0.0 Stable**: reviewed as-is, no content change (LG-1…LG-10 technology-neutral, complete, coherent). **`l2-loop-runner` 0.1.0→1.0.0 Stable**: two substantive closures — (1) added the missing **LG-10 compliance row** (objective persistence across in-session reduction) + an `ObjectiveSlot` type on `LoopSpec` (the L2 predated LG-10's addition to the L1, so covered only LG-1…LG-9 — the L1's own gate requires all invariants addressed before an L2 reaches RFC/Stable); (2) reconciled crate placement to the post-Phase-13 decomposition (I/O-free runner/governor/types → `crates/domain`, adapter-touching composition `loop_bootstrap.rs` → `crates/core` facade, replacing the pre-split `crates/core/loop/`). Post-Update Review: spec-critic PASS (L1 purity, invariant completeness, L2 substantive compliance now full, coherence, links, no RULES conflict — RULES loop/oracle terms are SDD-process not domain) + prompt-engineer PQ-6 PASS (6 dimensions clean). No C12 cascade (upward promotion). INDEX v1.0.120→1.0.121 (205 Stable / 5 RFC / 0 Draft; was 203/6/1 — the sole Draft is now gone). export-wiki refreshed (2 workspaces · 225 specs). finalize v0.1.356→0.1.357 (CHANGELOG skipped duplicate; no SDD-ref leak). Sync-check ok:true with an expected SYNC_GAP (PLAN based-on v1.0.120 < INDEX v1.0.121) — the normal `/magic.spec`→`/magic.task` handoff signal, resolved by the next replan (never hand-edited in `/magic.spec`). `l2-loop-runner` is now Stable-but-unbuilt → `/magic.task` opens its build phase. Next: `/magic.task main`. (Revert: git restore .design/main/INDEX.md .design/main/specifications/l1-loop-governance.md .design/main/specifications/l2-loop-runner.md .design/main/STATE.md)
- 2026-07-17 **Decision:** `/magic.task main` — **registry-sync replan; one new `concept-only` L1 absorbed, no new phase.** Post-Phase-18 Post-Task Replan (`rules/MAGIC.md §5`). Engine drift check: local `.magic/.version` `2.1.49` == snapshot `.design/INDEX.md` `2.1.49`, proceeded silently. Pre-flight (`check-prerequisites --verify-headers --workspace main`): ok:true, 210 specs (203 Stable / 6 RFC / 1 Draft), role registry clean (14/14, 0 dangling) — but surfaced two non-HALT signals: **ORPHANED_SPEC** `l1-value-settlement.md` (in INDEX, missing from PLAN) and **SYNC_GAP** (PLAN based on INDEX v1.0.118, registry at v1.0.120). Both mechanical, resolved by this replan. `l1-value-settlement` v1.0.0 (Stable, `concept-only`) — agent-initiated **outbound payment** to an external party, the third budget axis beside inbound usage-allowance and per-request generation-budget (VS-1…VS-8: principal-set agent-read-only spend envelope on the human-write-only authority plane, bounded-authority custody un-exceedable-even-if-compromised, top-of-ladder gating with allowlisted recipients, fail-closed never-unmetered, receipted+ledgered, opt-in/local-first/rail-neutral seam) — placed into PLAN **Phase 0** (concept bucket) under a new v2.27.0 cluster sub-header. Added by an adoption pass over an external agent-harness reference whose other mechanics were already saturated by existing specs; the only uncovered mechanic was outbound payment. **No new buildable phase**: concept-only means no authored `Implements:` L2 yet — it phases only once an L2 realization reaches Stable via `/magic.spec` (C6/C28 §4). The same adoption pass refined `l1-nodus-portability → LP-17` (settlement effect seam) in the **nodus** workspace — out of scope for this `main`-scoped run, and a version bump to an already-placed Stable concept (no orphan there; not flagged by main's Pre-flight). Pre-Planning Stabilization 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent `l1-loop-governance`; RFC→Stable is a `/magic.spec` act). PLAN v2.26.0→2.27.0 (Based-on v1.0.118→v1.0.120, closing SYNC_GAP) / TASKS v1.28.0→1.29.0. Backlog unchanged at 7 (6 RFC + 1 Draft). RULES parity v1.5.0 unchanged. No cascade, no new task units — pure registry-sync. Next: `/magic.spec main` to advance a concept/RFC toward buildable (no `/magic.run` target — no open phase). (Revert: git restore .design/main/{PLAN,TASKS,STATE,CONTEXT}.md)
- 2026-07-17 **Decision:** `/magic.run main` — **T-18T01 Done: validation sweep. PHASE 18 COMPLETE (8/8).** New core acceptance suite `crates/core/tests/activation_invariants.rs` — one named test per testable BA invariant, each exercised through the **real facade export chain** (`cronus_core::activation::enable`/`disable` driving a registry implementing the real re-exported `cronus_core::ActivationRegistry`), not an isolated-crate fake: BA-1 fresh-registry-Inactive-is-default, BA-3 enabling-a-different-mode-removes-the-prior-one-first, BA-5 consent-for-one-mode-never-authorizes-the-other, BA-6 a-refused-elevation-leaves-the-host-exactly-as-it-was, BA-7 disable-removes-and-verifies-absence, BA-8 (×2: RequiresApproval-never-folded-into-Activated + an-unqueryable-facility-reports-Unknown-never-Active), BA-10 an-unsupported-mode-is-representable-and-distinct-from-supported. Module doc explicitly cites where BA-2/BA-4/BA-9/BA-10(real-detection)/BA-11 are covered elsewhere (structural inspection, `sandbox_policy`/`tool_security`, `activation-os`'s own suite, `engine_lock`'s own suite) rather than duplicating. **Real design gap found and fixed during the sweep itself (not user feedback):** `OsActivationAdapter::capabilities()` had hardcoded `ModeSupport::Supported` for both modes on every platform since D01/D02 — never actually querying host capability, violating BA-10's requirement that an unsupported host say so with a reason up front, not only discover it when `enable()` later fails. Fixed by extending `SystemCalls` with `login_capability()`/`system_capability()`, wiring `capabilities()` to delegate to them: Windows/macOS unconditionally `Supported` (core OS facilities always present); Linux performs a real check (`has_systemd_user()` OR `has_graphical_session()` for login, `has_systemd_user()` for system), matching the RFC promotion's own previously-documented-but-never-wired non-systemd-Linux resolution. 2 new activation-os tests (`both_modes_are_supported_when_the_host_reports_so`, `capabilities_reflect_a_real_per_host_check_not_a_hardcoded_yes`) — activation-os 11/11 (up from 10). Full final gate: `cargo check --workspace` clean; `cargo clippy --workspace --all-targets -- -D warnings` clean (native Windows + cross-compiled Linux/macOS); `cargo fmt --all -- --check` clean; `cargo test --workspace` green across 3–6 runs (one disclosed transient failure in a 3x-loop batch, unreproduced on immediate retry + 3 subsequent runs — environmental noise under load, matching the earlier T-18A01-era pattern, not a code defect — the new code is pure in-memory `Mutex`-based logic). **Phase 18 (Service Activation) is complete — 8/8 tasks.** Closed out documentation: `phase-18.md` frontmatter `status: Todo→Done` + `provides`/`key_files`/`patterns_established` filled; `TASKS.md` v1.27.0→1.28.0 (Active Phases row → `Done`, Overview + meta updated); `PLAN.md` v2.25.0→2.26.0 (Phase 18 header → `— **Done**`, checklist `[x]` + 8/8 summary, milestone paragraph appended). **STATE.md hygiene (5th occurrence, now escalated):** `finalize`'s `update-state` clobbered `Status: Done` mid-phase after T-18C01/T-18D01/T-18D02/T-18E01, and on this closing run — after the phase was actually complete — it wiped `Next Action` (replacing it with a proactive `/magic.spec` suggestion, itself a `rules/MAGIC.md §5` violation: `/magic.spec` must never be proactively named after `/magic.run`) and replaced the entire `Progress` block with a generic auto-computed bar a second time — corrected by hand every time. A formal `MAGIC-SPEC ENGINE BUG REPORT` was filed this session per `.agents/rules/magic.md §9`, not a self-fix of `.magic/scripts/executor.js`. Next: run `finalize --workflow=run` to close T-18T01 + archive the phase, then `/magic.task main` (Post-Task Replan — no new phase expected, every currently-Stable spec now built). (Revert: git restore .design/main/{STATE,TASKS,PLAN}.md .design/main/tasks/phase-18.md crates/activation-os/src/*.rs && rm crates/core/tests/activation_invariants.rs)
- 2026-07-17 **Decision:** `/magic.run main` — **T-18E01 Done: command surface (7/8) — only the validation sweep remains.** New facade module `crates/core/src/activation_bootstrap.rs`: `default_activation_registry()` per-platform-dispatches (`#[cfg(windows/linux/macos)]`) to the right `SystemCalls` impl, re-exporting `ActivationMode`/`ActivationRegistry`/`ActivationState` so frontends never need `cronus-contract` directly (INV-2/INV-3). CLI: `cronus activation status|enable --mode <login|system> [--acknowledge-unattended-execution]|disable`, verb-first per `l2-cli` §4, mirroring the existing `BackupCommand` pattern. **Testability discipline (mirrors D01/D02):** the consent decision is a pure `enable_gate(acknowledged, interactive, confirm)` returning `Proceed`/`RefuseNonInteractive`/`CancelledByUser`, decided BEFORE `default_activation_registry()` is ever constructed — a refusal is structurally provable to mutate nothing, not just asserted. `enable` calls the domain policy engine (`cronus_core::activation::enable`), never the raw seam directly, so BA-3/BA-7 sequencing survives the CLI path. 4 gate unit tests + 2 real end-to-end smoke tests (spawn the compiled binary; `enable` with stdin explicitly nulled — deterministically non-interactive — exits 1 and names the flag, proving the refuse-path fires for real without touching the OS). TUI/desktop wiring deferred (facade is what both would bind to; no parity violation — nothing divergent shipped). `cargo test -p cronus-cli` 30/30; `cargo test --workspace` green exit 0 ×3 (66 result-sets); clippy `-D warnings` + fmt clean. **Disclosed:** could not cross-compile-check `cronus-core`/`cronus-cli` for Linux/macOS (pre-existing: `rusqlite`'s bundled C build needs an actual cross C toolchain, unrelated to this task) — the new code has zero logic beyond the `#[cfg]` gate + a constructor referencing the already-verified `LinuxSystemCalls`/`MacosSystemCalls`, so residual risk is low and reasoned through, not silent. **STATE.md hygiene (4th occurrence, escalating):** finalize's `update-state` this time clobbered BOTH `Status: Done` (mid-phase, 7/8) AND replaced the entire Progress block with a generic auto-computed bar (`Overall: [16/17] 94%`), discarding my detailed content — corrected by hand a fourth time. This is now a confirmed recurring engine defect (not a one-off), worth a `MAGIC-SPEC ENGINE BUG REPORT` if it keeps escalating. finalize v0.1.353→next. Next: T-18T01 (validation sweep — closes the phase). (Revert: git restore .design/main/{STATE,TASKS}.md .design/main/tasks/phase-18.md crates/core/src/{lib.rs,activation_bootstrap.rs} crates/core/Cargo.toml crates/cli/src/{cli.rs,commands.rs} crates/cli/tests/cli_smoke.rs)
- 2026-07-17 **Decision:** `/magic.run main` — **T-18D02 Done: system-scoped OS adapters, all 3 platforms (6/8) — Track D complete.** Extended T-18D01's `SystemCalls` seam with system_entry_present/vetoed/write/remove + uninstall_all; renamed `LoginScopedAdapter`→`OsActivationAdapter` (both modes; `observe()` prefers System as a defensive tie-break if domain's mutual exclusion is ever bypassed). **Windows:** real S4U scheduled task via `schtasks.exe /create /sc onstart /ru <user> /rl limited` (no `/rp` — documented S4U-without-password behavior), elevated for real through `ShellExecuteExW` + `"runas"` — an actual UAC dialog, not simulated. **Linux:** system unit `User=<user>` in `/etc/systemd/system/`, elevated via `pkexec` for every mutating step; falls back to the login unit + `loginctl enable-linger` (unelevated attempt first, then `pkexec`) exactly per §4.4's prefer-then-fallback text. **macOS:** `LaunchDaemon` w/ `UserName` in `/Library/LaunchDaemons/`, elevated via `sudo` — disclosed as rougher than the native `SMAppService` dialog, same `requiresApproval`-signal gap as D01's login path. **BA-7:** `uninstall_all()` removes every artifact under this adapter's own well-known name across both modes/all sub-variants — a foreign registration is structurally never in scope (the code never inspects any other name), not merely filtered at runtime. 10 seam-logic tests (up from 6): both-modes-supported, system enable/disable round-trip, System-priority tie-break, a scripted refused elevation leaves `Inactive` never partial (BA-6), uninstall delegation. **Verification scope, disclosed (same discipline as D01):** Windows compiles + all 10 run natively; live `ShellExecuteExW`/`schtasks` elevation compiled but never invoked (real interactive UAC dialog — more invasive than the already-declined D01 registry write). Linux/macOS cross-compile-checked only, clean; clippy `-D warnings` clean on all 3 targets. `cargo test --workspace` green exit 0 ×3 (66 result-sets); fmt clean. **STATE.md hygiene (3rd occurrence):** finalize's `update-state` again set `Status: Done` mid-phase (6/8 tasks) — corrected by hand a third time; this is now a recurring pattern worth fixing in the engine itself, not just patching each time. finalize v0.1.352→next. **Track D (the platform-adapter core) is now fully complete.** Next: T-18E01 (command surface — `cronus activation status|enable|disable`). (Revert: git restore .design/main/{STATE,TASKS}.md .design/main/tasks/phase-18.md crates/activation-os/{Cargo.toml,src/*.rs})
- 2026-07-17 **Decision:** `/magic.run main` — **T-18D01 Done: login-scoped OS adapters, all 3 platforms (5/8).** New peer adapter crate `crates/activation-os` (`l2-crate-topology` §4.4(a) minting, registered in the workspace). **Dependency decision (user-directed via AskUserQuestion):** `windows-sys` raw FFI for the Windows registry — no wrapper crate, matching `model-local`'s hand-rolled-HTTP precedent. Seam: `SystemCalls` trait + pure `derive_login_state` weaker-wins function (BA-8) + `LoginScopedAdapter<C>` implementing the full `ActivationRegistry` (`System` mode `Unsupported` until D02). 6 logic tests against a scriptable `FakeSystemCalls`. **Windows** (`windows_calls.rs`): real `RegOpenKeyExW`/`RegQueryValueExW`/`RegSetValueExW`/`RegDeleteValueW` FFI for `HKCU\...\Run` + the `StartupApproved\Run` veto read (disclosed: undocumented 12-byte binary format, community-reverse-engineered `0x02`/`0x03` disabled marker, `None` on anything unexpected — never guessed). Compiles + seam-logic tests run natively; **the live registry write/delete was deliberately NOT exercised against the real `HKCU\Run` key** (would leave persistent autostart state on the host machine outside the repo — a materially different risk than a temp-file test) — disclosed as deferred pending explicit consent. **Linux** (`linux_calls.rs`) + **macOS** (`macos_calls.rs`): pure `std` (systemd unit/XDG `.desktop`/LaunchAgent plist via `std::fs` + `systemctl`/`launchctl` via `std::process::Command` — zero new deps). **Disclosed gap:** macOS's `login_entry_vetoed` always reports `None` — the real `requiresApproval` signal needs `SMAppService`/Objective-C interop (e.g. `objc2`), a separately-scoped dependency decision, unevaluable on a Windows host with no macOS SDK. **Verification scope, disclosed:** Windows native (compiled + tested); Linux/macOS cross-compile type-checked only (`rustup target add x86_64-unknown-linux-gnu aarch64-apple-darwin` + `cargo check --target ...`, clean on both, clippy `-D warnings` clean on all three) — no Linux/macOS host available to run them. `cargo test -p cronus-activation-os` 6/6; `cargo test --workspace` green exit 0 ×3 (66 result-sets, up from 64); fmt clean. **STATE.md hygiene note:** the finalize script's `update-state` has now twice set the phase header `Status: Done` mid-phase (after T-18C01 and again after this task) despite 3-5/8 tasks remaining — corrected by hand both times; Next Action itself stayed correct. finalize v0.1.351→0.1.352 (CHANGELOG skipped duplicate). **Stopping point:** T-18D02 (system-scoped, real privilege elevation — Windows S4U scheduled task creation, macOS LaunchDaemon authorization, Linux polkit/lingering) is a distinct, higher-stakes slice — deserves fresh attention rather than being rushed at the tail of this turn. Next: `/magic.run main` continues with T-18D02. (Revert: git restore .design/main/{STATE,TASKS,PLAN}.md .design/main/tasks/phase-18.md Cargo.toml Cargo.lock && rm -rf crates/activation-os)
- 2026-07-17 **Decision:** `/magic.run main` — **Tracks A/B/C complete (A01+A02+B01+C01, 4/8).** **A01** (contract seam): `ActivationMode`/`ModeSupport`/`ActivationCapabilities`/`ActivationState`/`ActivationRegistry` in `crates/contract`; `NoOpActivationRegistry` (production honest-refusal default, mirrors `NoOpBusSender`) + a private `ScriptedRegistry` test double (mirrors `ScriptedBackend`). 6 tests. **A02** (domain policy): new `crates/domain/src/activation.rs` — `enable`/`disable` free functions implementing remove-then-register-verify (BA-3/BA-7) over `&dyn ActivationRegistry`, `TransitionOutcome`/`TransitionError` keeping `RequiresApproval` distinct from `Activated` (BA-8), `ConsentLedger` (BA-5, per-mode, never inherited). Doc-only clarification on `ActivationRegistry::enable`/`disable` (adapter-level primitives; mutual exclusion is domain policy) — no signature change, A01 unaffected. 6 tests (remove-before-register ordering via call-sequence position; forced failure converges on `Inactive`; `RequiresApproval` distinct; lying-adapter caught by read-back; disable round-trip; per-mode consent). **B01** (BA-4 barriers): discovered `l2-sandbox-policy`'s `FilesystemPolicy` (`read_write`) was spec-only, never built — filled the minimal piece BA-4 needs in `sandbox_policy.rs` (deny-by-default, prefix-safe `is_read_write`); split `FILESYSTEM_REGISTRATION_LOCATIONS` (4 real paths) from `NON_FILESYSTEM_REGISTRATION_SURFACES` (Windows registry key + Task Scheduler — `FilesystemPolicy` has no vocabulary for these, documented not asserted). Added `is_activation_tool_name` guard to `tool_security.rs` (repo-wide grep confirmed zero activation-shaped tool exists today; no master tool catalogue exists to scan, so this is the standing forward guard, disclosed honestly). 5 tests. **C01** (BA-11 lock): new `crates/core/src/engine_lock.rs` — real file I/O, so facade-tier not domain (`context_compaction`/`model_bridge` precedent); `acquire()` uses std-only `OpenOptions::create_new` for atomic exclusion, `Attach`/`Failed`/reclaim-stale via an injected `is_alive` closure; `conservative_is_alive` (always-alive placeholder) is the only shipped default — a real per-OS check is deferred to Track D, disclosed per FR-6. 6 tests. **Noted, not hidden (QLY-8):** one transient `cargo test --workspace` failure (exit 101) on the very first post-A01 full run has not recurred across 20 consecutive full-workspace runs since. All gates green throughout: `cargo test --workspace` exit 0 ×3 (64 ok, 0 failed) + clippy `-D warnings` + fmt clean after each task. finalize v0.1.350→0.1.351 (STATE.md + phase-18.md; CHANGELOG skipped duplicate). **Stopping point:** Track D (T-18D01/D02) requires a NEW crate (`cronus-activation-os`) plus a genuine dependency decision (Windows registry access has no `std` API — needs a third-party crate, e.g. `winreg`, or raw FFI), which the project's own precedent (T-17B01's `ureq` addition) treats as a flag-before-proceeding moment, not an autonomous call. Next: user input on the Windows-registry dependency choice, then `/magic.run main` continues with T-18D01. (Revert: git restore .design/main/{STATE,TASKS}.md .design/main/tasks/phase-18.md crates/contract/src/lib.rs crates/domain/src/{lib.rs,sandbox_policy.rs,tool_security.rs} crates/core/src/lib.rs crates/core/tests/tool_security.rs && rm crates/domain/src/activation.rs crates/core/src/engine_lock.rs)
- 2026-07-17 **Decision:** `/magic.task main` — **opened Phase 18 — Service Activation.** The now-Stable `l2-service-activation` pulled from Backlog per C6 (DA-6 computed next step after its `/magic.spec` promotion). PLAN v2.25.0 (based-on INDEX v1.0.119) / TASKS v1.27.0. Pre-flight ok:true (0 orphans, header parity clean, no SYNC_GAP). Engine drift check: local `2.1.49` == snapshot `2.1.49`, proceeded silently. Pre-Planning Stabilization 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent `l1-loop-governance`). Decomposed into **8 tasks / 6 tracks**: A seam+policy (A01 `ActivationRegistry` contract seam + mock adapter → A02 domain transition state machine: mutual exclusion + remove-then-register-verify + consent bookkeeping) · B BA-4 structural barriers (sandbox deny entries for the registration locations + no-activation-tool assertion) · C state-root lock (BA-11 attach-never-duplicate) · D OS adapters (D01 login-scoped unelevated → D02 system-scoped elevation, Windows S4U last — serialized on the shared `activation-os` crate + a `SystemCalls` fake seam) · E command surface (`cronus activation status|enable|disable`, non-interactive refusal) · T validation sweep (BA-1…BA-11). **Real-integration phase (Phase 17 precedent), seam-first:** the load-bearing property is BA-4 (activation is a SEC-10 authority grant the agent has no structural write path to); raw OS calls sit behind a `SystemCalls` seam so adapter logic is CI-testable against a scriptable fake with no live-OS dependency. Critical path = T-18A01 (the trait shape gates every track). Planner audit: hidden dependency B→D (barrier before capability); C independent but prerequisite for any background mode; cascade risk if the seam is wrong → keep it small, prove with the mock. prompt-engineer PQ-6 PASS (Verify lines name concrete tests incl. the honest `SystemCalls`-fake criterion for un-CI-runnable OS calls). The `activation-os` peer adapter crate is registered in `l2-crate-topology` via `/magic.spec` when it lands (the `model-local` pattern), never a run-task spec edit. finalize: v0.1.348→next. Next: `/magic.run main` (Phase 18). (Revert: git restore .design/main/{PLAN,TASKS,STATE,CONTEXT}.md && rm .design/main/tasks/phase-18.md)
- 2026-07-17 **Decision:** `/magic.spec main` — **`l2-service-activation` promoted RFC→Stable (1.0.1); DA-3 topological next target realized.** Blank-trigger resolved by session DA-3 to the deterministic next target (the direct downstream of Phase 13's crate seam, Stable L1 parent `l1-background-activation`). Post-Update Review real (spec-critic + prompt-engineer PASS): the Invariant Compliance table covers BA-1…BA-11 with concrete per-OS mechanism detail. **Resolved all 3 open TBDs with design decisions, not deferrals:** (1) non-systemd Linux → system-scoped `Unsupported` (BA-10), login-scoped falls back to init-agnostic **XDG autostart** (`~/.config/autostart/*.desktop`), refusal is per-mode not whole-host; (2) **`tauri-plugin-autostart` rejected** per the std-first dependency policy (covers only login-scoped, inverts dependency direction by pulling Tauri/shell into a CLI-linked adapter) — per-OS registration implemented directly; (3) Windows **S4U** is design-settled (S4U over Service, `RunLevel=Limited`), only At-startup launch-timing remains a **build-time validation gate** with a documented `At log on` fallback — not a design blocker. **Two bundled amendments (both patch, no status revert — documentation/terminology reconciliation of shipped/authoritative reality, not new requirements):** `l2-technology-stack` 1.2.0→1.2.1 replaced "Windows service" → "Windows S4U scheduled task" in the INV-4 row + Hub tier table (resolving the RFC's self-declared divergence, now reconciled past-tense in the callout); `l2-crate-topology` 1.0.0→1.0.1 registered `cronus-model-local` as a fourth adapter crate (InferenceBackend transport, minted by §4.4(a), not a DN-2 plane) + listed the later-added `InferenceBackend`/`WikiCache`/`WikiReadSurface` contract seams — the deferred model-local housekeeping, now done. INDEX v1.0.118→1.0.119 (202 Stable, 6 RFC, 1 Draft). export-wiki refreshed. finalize v0.1.347→0.1.348 (CHANGELOG skipped duplicate — no SDD leak). `l2-service-activation` is now Stable-but-unphased → `/magic.task` opens the Service Activation build phase. Next: `/magic.task main`. (Revert: git restore .design/main/INDEX.md .design/main/specifications/{l2-service-activation,l2-technology-stack,l2-crate-topology}.md)
- 2026-07-17 **Decision:** `/magic.task main` — **post-phase milestone replan (Phase 16 complete + archived); no new phase, no open buildable phase remains.** PLAN v2.24.0 / TASKS v1.26.0. Pre-flight clean (ok:true, 0 orphans, no header/parity drift, no SYNC_GAP). Engine drift check: local `2.1.49` == snapshot `2.1.49`, proceeded silently. Pre-Planning Stabilization: 0 promoted — sole Draft `l2-loop-runner` stays Draft (RFC parent `l1-loop-governance`; RFC→Stable is a `/magic.spec` act). Confirmed by DA-3 that **with Phase 16 done every currently-Stable spec is now built** — no new phase opens; Backlog stays 8 (7 RFC + 1 Draft), each awaiting its own `/magic.spec` review. Marked Phase 16 `Done` in PLAN.md (mirrored Phase 17's treatment: header `— **Done**`, checkbox `[x]` + 7/7 delivery summary, links → archives/) and reconciled TASKS.md header/overview/meta (the archiver had already flipped the table row to `Done (Archived)`). No cascade, no new task units — pure milestone. DA-3 topological next `/magic.spec` target: `l2-service-activation` (Stable L1 parent `l1-background-activation`; its platform-adapter crate seam delivered by Phase 13). The mechanical `l2-crate-topology` amendment (register `model-local` peer infra crate row) remains a non-blocking `/magic.spec` housekeeping follow-up. CONTEXT.md regenerated; export-wiki refreshed (2 workspaces · 224 specs). RULES parity v1.5.0 unchanged. (Revert: git restore .design/main/{PLAN,TASKS,STATE,CONTEXT}.md)
- 2026-07-17 **Decision:** `/magic.run main` — **T-16T01 Done: validation sweep. PHASE 16 COMPLETE (7/7).** New core acceptance suite `crates/core/tests/wiki_invariants.rs` — one named test per invariant, each exercised **end-to-end through the real SQLite `WikiStore`** + the real domain pipeline (not in-memory fakes), so the guarantees hold against actual persistence + composition: PW-2 read-only-by-construction (`&dyn WikiReadSurface`), PW-3 rebuild-equivalence-into-fresh-store (complements the file-drop proof in `wiki_rebuild.rs`), PW-4 uncited-claim-never-persisted, PW-5 moved-source→stale (Cell-driven `MovingGround` + `check_freshness`, read back through the store), PW-6 nav-tree + FTS, PW-7 reads-follow-sharing-posture (private/owner/user/group/public via `GatedWiki`), PW-8 internal-detail-never-in-a-row, and `every_generator_dependent_step_degrades_to_a_grounded_stub_without_a_generator` (both `rebuild` and `regenerate` None paths). PW-1 (plain-language) covered via its testable proxies PW-4/PW-8 (generation is model-based, not directly assertable), noted in the file header. Fixtures: `FixedGround`/`MovingGround`/`ScriptedGen` (cited + uncited + internal-detail sections). wiki_invariants 8/8, `cargo test --workspace` green exit 0 (64 ok, 0 failed) + clippy `-D warnings` + fmt clean. **Phase 16 is the last open buildable phase — with it done, every Stable spec is built or in Backlog.** Next: `/magic.task main` (Post-Task Replan §5 — revalidate; no new phase expected). (Revert: git restore .design/main/{STATE,tasks/phase-16}.md && rm crates/core/tests/wiki_invariants.rs)
- 2026-07-17 **Decision:** `/magic.run main` — **T-16C02 Done: access gate + rebuild. Phase 16 Track C complete (6/7).** **PW-7 access gate:** added `ResourceKind::Wiki` to the uniform grant model + new domain module `wiki_access::GatedWiki` — wraps any `WikiReadSurface` and runs `has_access(Wiki, office_id, Read)` **before every read** (page/children/search/changelog), delegating only on success; a denied read never reaches the store. Owner-wins (RS-5) flows through `is_owner`; a `page(id)` naming another office's row reports absent (no cross-office leak). The gate is the single place the ports-tier read surface and the domain grant algebra meet — enforcement in one auditable spot, not sprinkled through read paths. **PW-3 rebuild:** new `WikiCache::rebuild_office` (contract + store-local) = transactional drop-of-office-rows (page+changelog+FTS) then reinsert, all-or-nothing (a failed rebuild leaves prior rows intact, never a half-cleared projection). Domain `wiki_regen::rebuild` re-derives **every** page kind via `WikiPageKind::all()` through the extracted `build_pages` core (now shared with `regenerate` — no duplication); a generation failure aborts before the store is touched. **Keystone proof (core integration `wiki_rebuild`):** build wiki into a real on-disk `wiki.db` → **physically delete the file** → rebuild from the SAME ground truth into a fresh empty store → equivalent set (same ids/kinds/parent-ord/citations/fingerprints), i.e. dropping `wiki.db` loses nothing durable; plus the gate proven denied/granted against the real `WikiStore`. Equivalence asserted as structure+sources+fingerprints (not byte-identical body — generation is model-based, non-deterministic; the deterministic no-generator stub path is used here). Tests: 5 gate (denied/granted/owner/cross-office×2) + 3 domain rebuild + 2 store-local rebuild + 2 core integration. store-local wiki 11/11, domain wiki_regen+wiki_access 18/18, `cargo test --workspace` green exit 0 (63 ok, 0 failed) + clippy `-D warnings` + fmt clean. Next: T-16T01 (validation sweep — the phase's last task). (Revert: git restore crates/contract/src/lib.rs crates/store-local/src/wiki.rs crates/domain/src/{lib.rs,resource_sharing.rs,wiki_regen.rs} crates/core/src/lib.rs && rm crates/domain/src/wiki_access.rs crates/core/tests/wiki_rebuild.rs)
- 2026-07-16 **Decision:** `/magic.run main` — **T-16C01 Done: read-only client surface. Phase 16 (5/7).** New `WikiReadSurface` trait in `crates/contract` — `page`/`children`/`search`/`changelog`, all read. **PW-2 structural read-only:** the client is handed a `&dyn WikiReadSurface`, so a mutating call (`upsert_page`, `mark_stale`, `apply_regeneration`) is a **compile error**, not a convention — there is no such method on the trait object. `WikiStore` implements it (nav-tree `children(office, parent_id: Option)` via null-safe `parent_id IS ?` — `None`→roots; `search` joins `wiki_page_fts MATCH ? ORDER BY rank`). 3 new tests (nav overview→area→detail tree; FTS finds the right page + no-match→empty; read-only-by-construction). store-local wiki 9/9, `cargo test --workspace` green exit 0 + clippy `-D warnings` + fmt clean. Next: T-16C02 (access-grants Read gate + `rebuild`, the PW-3 proof). (Revert: git restore crates/contract/src/lib.rs crates/store-local/src/wiki.rs)
- 2026-07-16 **Decision:** `/magic.run main` — **T-16B03 Done: freshness & stale-marking. Phase 16 Track B complete (4/7).** `WikiCache` seam gained `pages_for_office` / `mark_stale` / `changelog` (store-local impls: `pages_for_office` reads the office's rows; `mark_stale` flips `stale=1` in a transaction; `changelog` = `ORDER BY at DESC LIMIT ?`, newest-first). Domain `check_freshness(office, ground_truth, cache)`: for each non-stale page, recompute `fingerprint_of(current sources)` (blake3) and flip `stale=1` on mismatch — **never regenerates or mutates content**, only sets the honest "may be out of date" marker so the UI never presents a drifted page silently as current; idempotent (already-stale pages skipped). Tests: domain `a_source_moving_without_regeneration_flips_the_page_stale` (drift → stale, re-sweep marks nothing) + `a_page_whose_sources_are_unchanged_stays_fresh`; store `mark_stale_flips_the_flag_and_pages_for_office_reads_it_back` + `changelog_is_returned_newest_first` (asserts 300/200/100 order + limit). domain wiki_regen 10/10, store-local wiki 6/6, `cargo test --workspace` green exit 0 + clippy `-D warnings` + fmt clean. **Track B (regeneration) done end to end: event→pages, transactional write, PW-4/PW-8 guards, no-generator grounded-stub, fingerprint-drift freshness, newest-first changelog.** Next: T-16C01 (read-only client surface, Track C). (Revert: git restore crates/contract/src/lib.rs crates/store-local/src/wiki.rs crates/domain/src/wiki_regen.rs)
- 2026-07-16 **Decision:** `/magic.run main` — **T-16B02 Done: grounding & honesty guards. Phase 16 (3/7).** Evolved the `wiki_regen` pipeline with the PW-4/PW-8 guards. **Seam evolution within Track B (noted, not a B01 reopen):** B01's single `PageRegenerator` (which produced a finished `WikiPage`) split into `GroundTruth` (no-model source gather, always present) + optional `PageGenerator` (model-backed prose over structured `GeneratedSection { text, citations, internal_detail }`) — required so the guards operate at claim/section granularity and the no-generator path still has sources for a *grounded* stub. Guards are pure fns composed in the pipeline: `filter_internal_detail` (PW-8, drop engineering/SDD sections) → `citation_guard` (PW-4, drop uncited sections) → assemble body + union citations; if everything is dropped, fall back to a grounded stub rather than persisting an empty/uncited page. `grounded_stub` states only true grounded facts (page pending + source count) and fabricates nothing; the **no-generator** path (`generator: None`) produces exactly that stub with the gathered citations attached — degrade-without-fabrication. `source_fingerprint` = blake3 over the sources (B03-ready). 8 tests (uncited dropped; internal-detail filtered; no-generator stub; all-dropped→stub; ungrounded-kind skipped; + mapping/transactional/failure-abort from B01, adapted). `cargo test --workspace` green exit 0 + clippy `-D warnings` + fmt clean. Next: T-16B03 (freshness/stale). (Revert: git restore crates/domain/src/wiki_regen.rs)
- 2026-07-16 **Decision:** `/magic.run main` — **T-16B01 Done: wiki regeneration pipeline core. Phase 16 Track B started (2/7).** New `crates/domain/src/wiki_regen.rs`: `OfficeChange` (BoardItemDone/DecisionRecorded/DeliverableProduced/MilestoneReached), `map_event_to_kinds` (only-affected-pages; every change touches Overview+Changelog + a change-specific kind, never a full rebuild), `PageRegenerator` seam (gather ground truth + generate cited plain-language content; `Ok(None)`=skip, `Err`=abort-before-write), and `regenerate(change, office, regenerator, cache) -> RegenReport` orchestrating a transactional write. **Seam architecture (tier model):** the pipeline writes through a NEW `WikiCache` trait in `crates/contract` (`get_page` + transactional `apply_regeneration`), implemented by `store-local::WikiStore` — so `cronus-domain` never depends on `cronus-store-local`. The SQLite transaction lives in store-local (`unchecked_transaction` on `&self`, all-or-nothing). **Two-level transactional guarantee, both tested:** (1) pipeline-level — a `PageRegenerator` failure returns before any `apply_regeneration` call (`a_generation_failure_aborts_before_any_write`: cache never touched); (2) store-level — `apply_regeneration_is_atomic_and_rolls_back_on_failure` seeds a page+changelog, then a second batch whose changelog reuses an existing id fails the second INSERT mid-transaction → whole batch rolls back, prior page row intact, no partial changelog. Plus mapping + happy-path + skip tests. Changelog ids use a process-static `AtomicU64` suffix (the contract `MemoryId` id-gen idiom, not per-instance state — distinct from the BANDIT_COUNTER bug I fixed) so a batch never collides its own PKs. domain wiki_regen 4/4 ×2 + store-local wiki 4/4 + `cargo test --workspace` green exit 0 + clippy `-D warnings` + fmt clean. Next: T-16B02 (grounding/honesty guards). (Revert: git restore crates/contract/src/lib.rs crates/domain/src/lib.rs crates/core/src/lib.rs crates/store-local/src/wiki.rs && rm crates/domain/src/wiki_regen.rs)
- 2026-07-16 **Decision:** `/magic.run main` — **T-16A01 Done: project-wiki schema + contract types. Phase 16 Track A complete (1/7).** Added contract types (`WikiPage`, `WikiCitation`, `WikiPageKind` closed enum overview|area|decisions|howto|glossary|changelog, `WikiChangelogEntry`) with structure absent-by-default (`WikiPage::new` → root/`ord`0/uncited/fresh, `parent_id: None`). New `crates/store-local/src/wiki.rs`: `WikiStore` (open/open_in_memory → `setup`) with the DDL — `wiki_page` (parent/ord tree, `citations` JSON TEXT, `source_fingerprint`, `stale`), `wiki_changelog`, `wiki_page_fts` (FTS5) + `idx_wiki_parent`/`idx_wiki_stale`; `upsert_page`/`get_page`/`append_changelog` for the round-trip. **Design calls:** (1) citations serialized to JSON in store-local via `serde_json::Value` built by hand — keeps `cronus-contract` **zero-dependency** (can't derive Serialize there), added `serde_json` to store-local deps. (2) FTS5 as a **standalone** table synced manually in `upsert_page` (matching the memory store's proven pattern), not the spec's illustrative `content=wiki_page` external-content form which would couple the FTS rowid to `wiki_page`'s implicit rowid despite its TEXT PK — noted inline. (3) A corrupt row (unknown kind / bad citations JSON) surfaces as `WikiError::Corrupt`, not a panic — recovery is rebuild (PW-3), never restore. 3 `wiki::schema` tests (tables+indices created; page round-trips with citations+fingerprint; upsert-replaces-by-id + FTS re-sync) — 3/3 ×2, `cargo test --workspace` green exit 0, clippy `-D warnings` + fmt clean. Track A gates B/C. Next: T-16B01 (regeneration pipeline core, domain tier). (Revert: git restore crates/store-local/Cargo.toml crates/store-local/src/lib.rs crates/contract/src/lib.rs && rm crates/store-local/src/wiki.rs)
- 2026-07-16 **Decision:** `/magic.task main` — **post-phase replan (Phase 17 complete+archived); milestone sync, no new tasks.** PLAN v2.23.0 / TASKS v1.25.0. Pre-flight clean (ok:true, 0 orphans, no drift, no SYNC_GAP). Confirmed by DA-3 that **Phase 16 (Project Wiki Store) is the sole open buildable phase** — every currently-Stable spec is now either built or covered by an open phase, so no new phase opens; the Backlog stays 8 (7 RFC + 1 Draft), each awaiting a `/magic.spec` review. Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent `l1-loop-governance`; RFC→Stable is a `/magic.spec` act). No cascade, no new task units — pure milestone. Recorded the deferred `l2-crate-topology` amendment (register `model-local` peer-crate row) as a mechanical, non-blocking `/magic.spec` follow-up (does NOT gate Phase 16, not a Pre-flight HALT since it needs no design input). Clean `/magic.task` completion → handoff to `/magic.run main` (Phase 16). RULES parity v1.5.0 unchanged. (Revert: git restore .design/main/{PLAN,TASKS,STATE,CONTEXT}.md)
- 2026-07-16 **Decision:** Phase 17 complete (7/7). Provides: contract::InferenceBackend + model-local REST transport + facade NodusModelBridge/TransportCompactor + CI boundary guard; two cronus-core test races fixed. Model-consuming surface is live end-to-end.

- 2026-07-16 **Decision:** `/magic.run main` — **T-17D01 Done: topology registration + CI boundary guard. Track D complete (6/7).** `model-local` was already a workspace member + `workspace.dependencies` entry (B01). The existing allowlist guard (`scripts/check-domain-boundary.mjs`) **already forbade `domain → model-local`** by construction (adapters absent from the allowlist `[cronus-contract, nodus, blake3, chrono, cron]`) — T-17D01 made it *explicit and tested*, not assumed. Refactored the guard: pure `findOffenders(deps, allowlist)` core split from the cargo-metadata I/O + a `--self-test` mode (real deps clean; injected `cronus-model-local` flagged; all three adapters asserted off the allowlist so none can be silently allowlisted later); wired the self-test into `.github/workflows/deps-gate.yml` as its own CI step. **Verify: happy path exit 0; self-test exit 0; and a REAL end-to-end proof — actually adding `cronus-model-local` to `crates/domain/Cargo.toml` made the guard exit 1 (`forbidden normal dependencies: cronus-model-local`), reverted to exit 0 (manifest restored clean via `git checkout` — an intermediate PowerShell `Set-Content -Encoding utf8` had added a UTF-8 BOM, caught via `git diff` and fixed).** Code + CI only; the `l2-crate-topology` spec amendment (add `model-local` peer-crate row) deferred to `/magic.spec` per the SDD boundary (never edit a Stable spec inside run). Next: T-17T01 (the last Phase 17 task). (Revert: git restore scripts/check-domain-boundary.mjs .github/workflows/deps-gate.yml)
- 2026-07-16 **Decision:** `/magic.run main` — **Fixed both pre-existing `cronus-core` test-isolation races (user-authorized via AskUserQuestion; applied as a separate step, NOT folded into C01's diff).** These now demonstrably broke an unqualified `cargo test --workspace`, blocking T-17T01's gate. (1) **`mission_mode` env race** (`crates/core/tests/mission_mode.rs`): the four `CRONUS_MISSION_MODE`-mutating tests now serialize on a file-local poison-recovering `Mutex` held across each set→read→remove; the false "single-threaded" SAFETY comments corrected. Test-only change. (2) **`BANDIT_COUNTER`** (`crates/domain/src/router/mod.rs`): process-global `static AtomicU64` → per-`PoolInner` `bandit_counter` stepped under the routing lock (a global counter coupled unrelated pools/tests, so exploration cadence depended on every other pool's call history); switched to **increment-then-test** so the every-20th-call exploration never fires on a pool's first call (the accidental coupling the global counter masked) — this alone unbroke `prefers_higher_scoring_provider` / `offline_mode_excludes_remote_providers` which my first (count-then-increment) attempt regressed by making bandit fire on call 0; **AND fixed the real correctness bug** — the bandit branch now applies the `FitLevel::TooTight` feasibility gate the scored path already enforces, so exploration can no longer pick a context-incompatible provider. New test `bandit_exploration_still_respects_context_fit` (40 calls, asserts bandit actually fires and still never selects the too-small provider). **Verification: `cargo test --workspace` green with exit 0 across 3 consecutive runs (case-sensitively confirmed — an earlier "FAILED" was my own PowerShell pattern matching test *names* containing "failed", not a real failure); model_router 14/14; mission_mode 35/35 ×3 multi-threaded; clippy `-D warnings` + fmt clean.** No `l2-model-router` spec amendment needed — fixes bugs (global state, unfit exploration) consistent with the Stable spec's fit-gated-routing intent. Next: T-17D01. (Revert: git restore crates/domain/src/router/mod.rs crates/core/tests/mission_mode.rs crates/core/tests/model_router.rs)
- 2026-07-16 **Decision:** `/magic.run main` — **T-17C01 Done: facade wiring + nodus bridge. Track C complete (5/7).** New `crates/core/src/model_bridge.rs`: `NodusModelBridge` (holds `Arc<dyn contract::InferenceBackend>` + model name) implements `nodus::ModelProvider` — `generate` collects `Token`s into a `String` (transport error ends collection → partial text, the honest projection within nodus's error-channel-less `-> String`); `analyze` realizes over generation (prompt for a JSON verdict, extract `{`..`}`, parse, project each flag: number→Float/Int, string→Text, bool→Bool, absent/unparseable→`Null` — never a fabricated score, unlike the stub's 0.9). Facade re-exports `cronus::model` (= `cronus-model-local`) + `cronus::NodusModelBridge`. **Wiring lives in the facade, NOT the domain `Engine`** (a `Debug+Default` unit struct the tier model forbids from depending on any adapter) — realizes §4.1 "the nodus bridge adapter lives in the facade". Core gained deps `cronus-model-local` + `nodus` + `serde_json`; **nodus gains nothing (LP-1)**. 4 tests (incl. an end-to-end `run_with_provider` GEN step writing the real concatenated stream to `$out`, asserted NOT the `[STUB gen(Ada) …]` label) — 4/4 × 3 zero flakiness; `cargo test -p cronus-core --lib nodus_bridge` green; clippy `-D warnings` + fmt clean. **Gate blocker surfaced (NOT a C01 regression):** `cargo test --workspace` this run flaked on the KNOWN pre-existing `mission_mode::resolve_mode_env_overrides_config` env race (35/35 green with `--test-threads=1`; this diff touches only `crates/core/{Cargo.toml,lib.rs,model_bridge.rs}` + `Cargo.lock`, nothing near mission_mode). The two known cronus-core test-isolation races (mission_mode env + model_router `BANDIT_COUNTER`) now demonstrably break an unqualified `cargo test --workspace` intermittently → must be fixed before T-17T01's workspace-green gate. **Surfaced as a user decision** (fix-now vs. before-T01) rather than silently folding an unrelated fix into this phase's diff. Next: T-17D01 (topology + CI guard) or the race-fix. (Revert: git restore .design/main/{STATE,TASKS}.md .design/main/tasks/phase-17.md crates/core/Cargo.toml crates/core/src/lib.rs Cargo.lock && rm crates/core/src/model_bridge.rs)
- 2026-07-16 **Decision:** `/magic.run main` — **T-17B03 Done: embed/describe/pull/set_residency + failure taxonomy + egress-gated remote + `InferenceBackend` impl. Track B complete.** All non-streaming calls (`embed`/`describe`/`set_residency`) share one `http_call` helper (`FrameReader::read_to_end`, full wire taxonomy, single attempt = no retry); `pull` reuses the worker/channel pattern as NDJSON `/api/pull` progress. All capability-gated: an unset flag → `Unsupported` (MR-9, honest not emulated). **`EndpointProfile` now implements `contract::InferenceBackend`** — MR-2 realized end to end; the trait methods delegate to the inherent ones (explicit `EndpointProfile::…(self, …)` disambiguation, no recursion), proven by a `dyn InferenceBackend` streaming test. **Egress/credential seam (§4.4, SEC-8/SEC-10/INV-7):** new `EgressGrant` (endpoint-scoped, minted by the security layer, NEVER by the transport) + `CredentialResolver` (call-time secret fetch); `EndpointProfile::new_remote` requires a grant at **compile time** (type-enforced "no remote profile without an egress grant") and rejects an endpoint-mismatched grant; it stores the *resolver*, not the secret — the credential is resolved fresh per call, attached as `Authorization: Bearer …`, and never cached (test asserts the resolver is invoked exactly once per call and the header reaches the wire). `EndpointProfile` dropped `PartialEq`/`Eq` (a resolver has no equality) and got a hand-written `Debug` that redacts the resolver (secret never logged). 10 new tests (21 total). **21/21 × 3 zero flakiness; `cargo test --workspace` FULLY green this run (both previously race-prone cronus-core tests passed); clippy `-D warnings` + fmt clean.** MR-3/4/5 delegated-with-disclosure. Next: T-17C01 (facade wiring + nodus bridge). (Revert: git restore .design/main/{STATE,TASKS}.md .design/main/tasks/phase-17.md crates/model-local/src/lib.rs)
- 2026-07-16 **Decision:** `/magic.run main` — **T-17B02 Done: streaming generate + cooperative cancellation in `crates/model-local`.** `EndpointProfile::generate_stream[_with_capacity]` → `StreamReceiver` (pull-iterator with terminal-event latch; a worker that dies without a terminal event surfaces as `MalformedStream`, never a silent clean end) over `run_generate_worker`: raw `TcpStream` + hand-rolled HTTP/1.1 POST, status/header parse, SSE `data:` framing (multi-line join, non-data fields ignored), `[DONE]` sentinel + clean-EOF-treated-as-Done (some providers just close), `sync_channel` bounded backpressure (default 32, MR-8), cooperative cancel via `FrameReader` 50 ms read-timeout polling — cancel drops the socket, no async runtime. `MockSseServer` hermetic fixture established for B03/T01 reuse. 4 new tests (ordered tokens→Done; clean-EOF→Done; mid-stream cancel → exactly one `Error(Cancelled)` + server observes the socket close via a bounded read that does NOT count its own timeout as a close; bounded-channel assertion — undrained worker buffers ≤ capacity+1 of 50 events, then fully drained so no thread wedges on a small OS socket buffer). **11/11 × 3 zero flakiness; `cargo test --workspace` FULLY green this run; clippy `-D warnings` + fmt clean.** **Frugality correction:** `ureq` (added in B01) removed before ever being used — loopback needs no TLS and ureq's whole-body timeout model doesn't fit poll-and-check-cancel; `serde_json` (existing workspace dep) covers SSE payloads; the spec-sanctioned HTTP+TLS client now explicitly rides with the remote/egress-gated path (B03+), recorded in the crate doc header. `GenerateRequest.parameters` deliberately not folded into the wire body yet (typed-JSON coercion deferred, noted inline). Two test defects caught by MANUAL review during the tool-classifier outage, before first compile: a potential hang (backpressure test never drained) and a false-positive (server counting its own read-timeout as "client closed") — both fixed pre-verification and confirmed by the green run. Next: T-17B03 (embed/describe/pull + failure mapping + egress-gated remote refusal). (Revert: git restore .design/main/{STATE,TASKS}.md .design/main/tasks/phase-17.md Cargo.toml Cargo.lock crates/model-local/Cargo.toml crates/model-local/src/lib.rs)
- 2026-07-16 **Decision:** `/magic.run main` — **T-17B01 Done: `crates/model-local` scaffolded with `EndpointProfile` + reachability probe.** New crate registered (workspace members + `workspace.dependencies`), `ureq = { version = "3.3.0", default-features = false, features = ["rustls"] }` resolved cleanly (rustls-only, no native-tls — verified via `cargo add --dry-run`, network access to crates.io confirmed reachable first). `EndpointProfile::new` is loopback-only by construction (MR-1): accepts the literal `"localhost"` (the real §4.4 defaults for all six providers use it, not a numeric IP — verified in the test), `127.0.0.1`/`[::1]`; rejects `0.0.0.0`/`[::]` wildcard binds and any other host via typed `ProfileError` variants. `probe()`/`probe_with_timeout()` classifies `Reachable`/`ConnectionRefused`/`Timeout` via raw `TcpStream::connect_timeout`. 7 new tests, all green, repeated 3x (zero flakiness). **Two empirical findings during verification, both investigated to root cause rather than assumed:** (1) this sandbox's network layer silently times out on a closed local port instead of returning immediate `ConnectionRefused` — confirmed via a standalone experiment; the probe code is correct for real deployment, the test was adjusted to accept either outcome with the reasoning recorded inline, not asserting sandbox-specific behavior as universal. (2) A SECOND pre-existing, unrelated test-isolation bug found during full-workspace regression check (not fixed — out of scope): `crates/core/tests/mission_mode.rs:124-144` — three tests race on the same unsynchronized process-wide `CRONUS_MISSION_MODE` env var; the inline comment claiming single-threaded execution is wrong (cargo test defaults to multi-threaded). Combined with T-17A01's `BANDIT_COUNTER` finding, this is the second shared-global-state test-isolation defect found this session in `crates/core` — recommend a dedicated hardening task rather than folding into this phase's diff. Verified for touched crates: `cargo test -p cronus-contract -p cronus-model-local` all green, `cargo check --workspace` clean, `cargo clippy --workspace --all-targets -- -D warnings` clean, `cargo fmt --all -- --check` clean. finalize: pending. Next: T-17B02 (streaming generate + cancellation, establishes the shared mock-HTTP-server fixture reused by B03/T01). (Revert: git restore .design/main/{STATE,TASKS}.md .design/main/tasks/phase-17.md Cargo.toml Cargo.lock && rm -rf crates/model-local)
- 2026-07-16 **Decision:** `/magic.run main` — **T-17A01 Done: `InferenceBackend` trait + types landed in `crates/contract`.** Added `GenerateRequest`, `StreamEvent`, `InferenceError`, `ModelDescriptor`, `ResidencyHint`, `PullProgress`, `CancelHandle` (`Arc<AtomicBool>`, cooperative cancellation), and the `InferenceBackend` trait (`generate_stream`/`embed`/`describe`/`pull`/`set_residency`) — a new streaming call surface, deliberately separate from the untouched routing-metadata `ModelProvider`. 4 new unit tests (scripted mock backend: runs to `Done` uncancelled; mid-stream cancel yields exactly one `Error(Cancelled)` then stops; unsupported capability reported honestly not emulated; `CancelHandle` clone shares the flag) — all green. Verified for the touched crate: `cargo test -p cronus-contract` 20/20 pass, `cargo check --workspace` clean, `cargo clippy --workspace --all-targets -- -D warnings` clean, `cargo fmt --all -- --check` clean. **Found and isolated a real PRE-EXISTING bug, unrelated to this task, NOT fixed here (scope discipline):** `cargo test -p cronus-core --test model_router fit_level_routing_exclusion` fails when run in isolation — confirmed via `git stash`/`git stash pop` that it fails identically on the untouched baseline (not a regression from T-17A01). Root cause: `BANDIT_COUNTER` (`crates/domain/src/router/mod.rs:55`) is a **process-wide `static AtomicU64`**, not scoped per `RouterPool` instance — its "every 20th call" trigger fires on the very first `route()` call of an isolated test run (`0 % 20 == 0`), and the bandit branch (mod.rs:171-183) selects the first healthy provider **with no context-window fit check at all**, violating the router's own asserted invariant ("tiny context window must be excluded"). Two compounding issues: (a) shared global mutable state causes cross-test pollution / non-determinism (passes when run alongside other tests that already advanced the counter, fails in isolation), (b) the bandit exploration path has a real correctness gap — it can select a context-incompatible provider in production, not just in tests. This is a `crates/domain/src/router` (Phase 4/6-era, `l2-model-router` Stable spec) defect, orthogonal to Phase 17's `crates/contract`/`model-local` scope — flagged here per QLY-8 (quality debt surfaced, never hidden), not silently patched. Recommend a dedicated fix task (scope it via `/magic.task main "fix BANDIT_COUNTER global-state + bandit fit-check"` or as a standalone bugfix) rather than folding into this phase's diff. Next: T-17B01 (`crates/model-local` scaffold) needs a **new external HTTP+TLS dependency** fetched from crates.io — flagging before proceeding since it changes `Cargo.toml`/lockfile and may need network access this environment might not have. (Revert: git restore .design/main/{STATE,TASKS}.md .design/main/tasks/phase-17.md crates/contract/src/lib.rs)
- 2026-07-16 **Decision:** `/magic.task main` — **opened Phase 17 — Model Transport & Provider Connectivity.** The now-Stable `l2-model-runtime` pulled from Backlog per C6 (DA-6 computed next step). PLAN v2.22.0 (based-on INDEX v1.0.118 — SYNC_GAP resolved) / TASKS v1.24.0. Decomposed into 7 tasks / 5 tracks: A contract `InferenceBackend` trait (T-17A01, critical path — every track builds on the trait shape) · B `crates/model-local` transport (B01 crate+profiles+probe → B02 streaming generate + cancellation → B03 embed/describe/pull + failure-map, serialized on the shared crate + mock-HTTP fixture) · C facade wiring + nodus bridge (T-17C01) · D `model-local` peer-crate registration + CI boundary guard failure-path (T-17D01) · T end-to-end validation (T-17T01 — a previously-inert path produces a real result through the full seam + no-backend degrade). **Real-integration phase**, first non-domain-logic-first since Phase 13: spec-sanctioned blocking HTTP+TLS client, hermetic `TcpListener` mock emitting SSE/NDJSON (no live provider needed), **no async runtime** (bounded worker-thread pool + pull-iterator + atomic-flag cancel). Planner audit: critical path = T-17A01; hidden shared resource = the mock-server fixture (establish once in B02); cascade risk if the trait shape is wrong → keep it small, prove with a mock impl. The `l2-crate-topology` spec amendment (register `model-local`) rides via `/magic.spec`, NOT a run-task spec edit; T-17D01 does the CI-guard code only. Now open **alongside** Phase 16 (both Todo) — neither depends on the other; by leverage the transport wins (unblocks triage classification, context compaction, nodus `gen`). Pre-flight ok:true, 0 orphans, header parity clean; Pre-Planning Stabilization 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent); prompt-engineer PQ-6 PASS. finalize: v0.1.329→next. Next: /magic.run main (DA-3 lean Phase 17). (Revert: git restore .design/main/{PLAN,TASKS,STATE,CONTEXT}.md && rm .design/main/tasks/phase-17.md)
- 2026-07-16 **Decision:** `/magic.spec main` — **`l2-model-runtime` RFC→Stable review; promoted 1.0.1 Stable.** Blank-trigger resolved by session DA-3 to the transport keystone I had flagged. **Post-Update Review was real, not a rubber-stamp — spec-critic FAILed the first pass on a genuine code contradiction:** the RFC claimed the transport "implements `contract::ModelProvider`" with generate-stream/embed/describe, but the actual `contract::ModelProvider` (crates/contract/src/lib.rs:464) is a **routing-metadata** trait (id/health/cost/latency/tier/task_fit — no call method), and the only real generate surface is `nodus::ModelProvider` (crates/nodus/src/executor.rs:166 — sync `generate`/`analyze` → String). Fixed same-invocation: the transport now defines a NEW `InferenceBackend` trait in the contract crate (the streaming call surface neither trait had); a concrete endpoint-profile provider implements it **plus** the routing-metadata facet (score vs. call — two facets of one object); the nodus trait is satisfied by a host-side bridge that collapses the stream to a String. Two seams sharpened vs Stable neighbors: endpoint profile consumes the router's `api_base` (not a parallel address registry), and credential rotation/pool stays in router (Connection Cooldown) + error-recovery (multi-key) — the transport only attaches the selected credential. Re-verified: parent l1-model-runtime Stable ✓, all Related/Canonical links on disk ✓ (added [NODUS] ref), no RULES conflict, ok:true. prompt-engineer PQ-6 PASS. INDEX 1.0.117→1.0.118 (201 Stable, 7 RFC, 1 Draft). finalize: v0.1.328→0.1.329. l2-model-runtime is now Stable-but-unphased → /magic.task opens the transport phase. Next: /magic.task main. (Revert: git restore .design/main/INDEX.md .design/main/specifications/l2-model-runtime.md)
- 2026-07-16 **Decision:** `/magic.task main` — **registry-sync replan (audit wave absorbed); no new tasks.** PLAN v2.21.0 (based-on INDEX v1.0.117 — SYNC_GAP resolved) / TASKS v1.23.1. Absorbed 12 orphans: eleven Stable `concept-only` L1s → Phase 0 (improvement-loop, solution-frugality, completion-verification, derived-artifact-handoff, context-degradation, optimization-integrity, cost-rating, human-intervention, session-reinforcement, knowledge-horizon, generation-shaping) + `l2-model-runtime` (RFC) → Backlog per C6; `l1-model-runtime` Phase 0 entry gains its authored-L2 note (C28 §4 drops concept-only on authoring). Pre-flight ok:true, header parity clean (--verify-headers). Pre-Planning Stabilization: 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent). Planner audit: no new tasks, no cascade risk; Phase 16 untouched, still the buildable target. DA-3 next `/magic.spec` review target: `l2-model-runtime` (the transport keystone — unblocks triage classification, compaction, nodus gen). finalize: v0.1.327→0.1.328. Next: /magic.run main (Phase 16) — or /magic.spec main "review l2-model-runtime" to advance the transport wave. (Revert: git restore .design/main/{PLAN,TASKS,STATE,CONTEXT}.md)
- 2026-07-16 **Decision:** `/magic.spec main` — **concept-conformance audit wave** (user-driven: deep audit of artifacts vs the automation/client-as-client/fast-tech concepts). Audit found four spec-layer gaps and formalized them. (1) AMENDED `l1-architecture` 1.1.1→1.2.0: +INV-9 **shipped-surface honesty** — a frontend never ships a verb unbound to the core capability it names; unbound commands are hidden/marked unavailable, never "not implemented" stubs (evidence: CLI groups goal/mission/trigger/research/change answer "not yet implemented" while `cronus-domain` implements mission/research/budget/trigger_triage — an INV-3 parity break INV-6 did not cover). (2) AMENDED `l1-quality-standards` 1.0.0→1.1.0: +QLY-9 **gate-scope completeness** — gates cover every shipped deliverable unit; a unit outside the default gate runner needs an equivalent recorded lane (evidence: desktop Tauri shell is workspace-detached, invisible to `cargo test --workspace`). (3) NEW **l2-model-runtime** (RFC 1.0.0, Implements l1-model-runtime): the missing transport L2 — sync streaming REST crate `model-local` over the federated §4.4 provider catalog, pull-based token stream + cancellation, no async runtime, host-side nodus bridge (LP-1), egress-gated remote profiles; MR-3/4/5 delegated-with-disclosure; requires crate-topology amendment at implementation (noted in spec §4.1). Kept RFC per new-large-L2 precedent. (4) FR-6 disclosure patches: `l2-trigger-triage` 1.0.1 (classifier stub 0.0), `l2-context-management` 1.0.4 (no-op compactor), `l2-memory-store` 1.4.1 (zeroed HRR), `l2-agent-constitution` 1.0.10 (no-op activation) — inert seams now disclosed with upgrade triggers instead of masquerading as implemented. Audit also confirmed non-spec issues for the plan: TUI renders empty projections (no core snapshot binding), GUI IPC bridge exposes only version/status, `fallow` gate absent from PATH, near-zero logging vs Stable l1-diagnostic-log. Post-Update Review: spec-critic caught the crate-topology extension conflict — fixed same-invocation; prompt-engineer PQ-6 PASS. Next: /magic.task main (absorb SYNC_GAP + 13 orphans + Backlog placement). (Revert: git restore .design/main/{INDEX,STATE}.md .design/main/specifications/{l1-architecture,l1-quality-standards,l2-trigger-triage,l2-context-management,l2-memory-store,l2-agent-constitution}.md && rm .design/main/specifications/l2-model-runtime.md)
- 2026-07-16 **Decision:** `/magic.spec main` — **improvement-loop wave** (user-driven: self-improvement + self-healing as the flagship differentiator). NEW **l1-improvement-loop** (Stable 1.0.0, concept-only): the closed product self-improvement loop — in-work non-blocking observation from already-recorded planes (IMP-1), product-subject-only generalized capture (IMP-2), submission autonomy off/confirm(default)/automatic as a standing audited SEC-9/SEC-10 grant through the ONE reporting pipeline, no parallel egress (IMP-3), upstream AI triage at the canonical tracker — classify / spam-quarantine (recorded, never silent-delete) / dedup vs open+history+rejection ledger / evidence-score / route, queue-management authority only (IMP-4), evidence-over-eloquence weak-model robustness + report-content-as-untrusted-data CP-1 (IMP-5), append-only rejection memory with mandatory supersession criteria + auto-answer from the record, recurrence itself = corroboration (IMP-6), disposition feedback marking RP-6 outcomes (IMP-7), triage service dogfooded as a Cronus office (IMP-8), zero-server optional-by-construction (IMP-9). AMENDED: `l1-doctor` 1.2.0 (+HEAL-7 observe/heal healing-authority tiers on the SEC-10 plane; checks + crash-recovery constitutive/never disableable; suppressed repairs surface as recommendations); `l1-error-reporting` 1.1.0 (+ERR-6 off/confirm/auto — auto is a durable, scoped, revocable, audited grant satisfying ERR-1 by standing consent; improvement findings automatic-grant-only, RP-3 never gates the path; default-consent TBD resolved = confirm; + added missing Document History for RULES §5 compliance); `l1-report-prompting` 1.1.0 (RP-2 gains (f) improvement — evidence-gated system trigger replacing the "suggestions have no system trigger" stance; RP-1 intact — transmission stays the pipeline's consented act). Demarcations held: `l2-self-improvement` = agent-practice learning (changes the agent), the loop changes the product; telemetry = aggregate metrics. Post-Update Review real: spec-critic PASS (all 10 Related + 6 Canonical links verified on disk), prompt-engineer PQ-6 PASS-WITH-REWRITES (one ambiguity — RP-3 `off` vs ERR-6 auto independence — clause added same-invocation). finalize: v0.1.325→0.1.326; INDEX 1.0.115→1.0.116. Concept-only — no L2 authored, no build phase opens. Next: /magic.task main (absorb the new orphan + SYNC_GAP). (Revert: git restore .design/main/{INDEX,STATE}.md .design/main/specifications/{l1-doctor,l1-error-reporting,l1-report-prompting}.md && rm .design/main/specifications/l1-improvement-loop.md)
- 2026-07-15 **Decision:** `/magic.spec → /magic.task` chain (user-driven, from TODO line 9 "wiki-память в БД"). **Two waves.** (1) **MEM-4 amendment** (`l1-memory-model` 1.0.0→1.1.0): source-of-truth *by memory kind* — high-volume learned tier is **store-authoritative** (no `notes/*.md` dual-write; the shipped `crates/store-local` was already store-authoritative, so **spec-only, zero code delta** — verified by grep, no `.md` writes in the memory store), authored quick-memory (`MEMORY.md`/`USER.md`) stays text-authoritative, distilled skills are nodus workflows not memory records (resolved a category error: nodus is a workflow language per NL-1/NL-7, not a data format). Reconciled `l2-memory-store` 1.4.0 + `l2-memory-consolidation` 1.0.2; STO-8's "where practical" clause is the anchor (MEM-4 had over-hardened it). (2) **Wiki side:** authored NEW `l2-project-wiki` (Implements `l1-project-wiki`) — the client wiki as a per-office SQLite projection **cache** (`wiki/wiki.db`), not loose Markdown: a rebuildable cache of upstream truth (board/decisions/ledger/deliverables), `rebuild()` proves PW-3; added the `wiki/wiki.db` home to `l2-filesystem-layout` 1.2.0. Promoted RFC→Stable (1.0.1) — Post-Update Review caught + fixed a real `rebuild` **byte-equivalence overclaim** (generation is non-deterministic → corrected to informational equivalence). Opened **Phase 16 — Project Wiki Store** (7 tasks / 4 tracks: A schema · B regeneration · C read/access surface · T). Pre-flight clean throughout (ok:true, no header drift); Pre-Planning Stabilization 0 promoted. finalize: v0.1.311→0.1.316 across the chain. Next: /magic.run main. (Revert: git restore .design/main/{INDEX,PLAN,TASKS,STATE}.md .design/main/specifications/{l1-memory-model,l2-memory-store,l2-memory-consolidation,l2-filesystem-layout}.md && rm .design/main/specifications/l2-project-wiki.md .design/main/tasks/phase-16.md)
- 2026-07-13 **Decision:** Adoption pass over an external agent context-optimization reference (name kept out of `.design/` per standing rule) → INDEX v1.0.105. **Dedup was the whole story:** the reference is a local context-compression layer for AI agents (content-routed reversible compression + CCR, cache-aligned live-zone compression, cross-agent memory, failure-mining, output-token reduction). Systematic dedup showed the input side is **already saturated** — content-type-routed signal-preserving reduction + compress-cache-retrieve by `l1-context-compression` CC-1…11 + §4.5 (CC-11 was itself distilled from a prior pass over the same class of tool); cache-aligned live-zone/frozen-prefix by `l1-cache-stable-context` + `l1-inference-cache` + NL-15; cross-agent memory by the memory cluster; failure-mining→corrections by `l2-learning-loop`; durable savings ledger by `l1-operational-ledger` + CC-6/GB-8 accounting. The **one genuine delta** was the output side: `l1-generation-budget` governs output *length/truncation* but nothing governed output *character/effort*. NEW **l1-generation-shaping** (Stable 1.0.0): turn-classified modulation (GS-1), verbosity steering as request-instruction not post-trim (GS-2), floored effort/reasoning-depth modulation — down on routine resume-after-tool, full on new/error/ambiguous (GS-3), **correctness supremacy as the load-bearing floor** (GS-4, outranks all others), cache-stable steering placement composing CSC-2 (GS-5), **counterfactual measurement honesty** — output savings are structurally unobservable so estimate+CI or held-out control group, never a fabricated figure, applying FR-10 (GS-6), opt-in/reversible/live-reconfigurable (GS-7), learned-and-overridable verbosity from implicit behavioral signals via UM-2/UM-4 (GS-8), accounted (GS-9). Distinct axis from GB (length) — §4.1 makes the three output-economy concerns (budget/shaping/contract) orthogonal. Verified `l2-model-router` only passes `reasoning_effort` through (no per-turn modulation), `l1-deliberation` is multi-worker debate not single-model effort, `l1-inner-monologue` has no effort/routine concept — gap real. nodus: **no new NL invariant** — host-side over NL-15 volatile suffix + ModelProvider options + @err/!! + HO-8 (mapping in §nodus-relevance). Post-Update Review real: spec-critic PASS (all 7 Related + 4 Canonical links verified on disk; L1 purity confirmed — KV-cache/reasoning-depth is established L1 vocabulary per cache-stable-context/inference-cache), prompt-engineer PQ-6 PASS (GS-4 precedence stated explicitly, tuning-params constraint bounds "toward a floor"). Concept-only — no L2, no build phase opens. finalize: pending (classifier temporarily unavailable at run time; re-run export-wiki + finalize --workflow=spec). Next: /magic.task main. (Revert: git restore .design/main/INDEX.md .design/main/STATE.md && rm .design/main/specifications/l1-generation-shaping.md)
- 2026-07-13 **Decision:** Adoption pass over three external agent-platform prompt references (reference names kept out of `.design/` per standing rule) → INDEX v1.0.104. Saturation-driven survey: two of three references were editions of the same conversational-platform prompt (hash-verified non-identical, content-equivalent), the third a multi-vendor prompt collection read selectively until mechanics repeated. **Extracted the genuinely-missing mechanics, dropped the noise** (tone/formatting policy, wellbeing protocols, copyright quota details, product widgets, identity firewalls — all product-layer or already covered). NEW **l1-session-reinforcement** (Stable 1.0.0): typed classifier/threshold-triggered corrective advisories with the load-bearing tighten-only direction invariant (a relaxing injection is invalid by construction → forged "system" content structurally detectable; undecidable → fail-closed), host-only issuance, long-horizon re-anchoring, supervisor probes (suspend-answer-resume, no self-exit), context-not-authority, budgeted non-nagging emission; rides the dynamic-harness environment-injection seam; nodus side already covered by NL-11/NL-15/NL-19/LP-10 — deliberately no new NL invariant (RULES §6 no-duplication). NEW **l1-knowledge-horizon** (Stable 1.0.0, micro): declared horizon + injected now, retrieval-before-assertion for unrecognized/post-horizon entities, now-anchored queries, trust calibration — generation-side complement of claim-verification. AMENDED **l1-generative-surface 1.1.0** (+GS-9 durable scoped surface state via host storage seam — personal/shared with disclosure, quotas, LWW honesty, user inspect/erase; +GS-10 brokered surface inference — host-injected credentials, host-pinned economical model + budget, stateless, gate/audit parity), **l1-agent-tool-ergonomics 1.1.0** (+ATE-10 per-call intent field on mutating tools ordered before payload; +ATE-11 opaque reference passing + host-side hydration with copy-verbatim discipline), **l1-extension-marketplace 1.1.0** (+XM-10 provider choice stays with the user: suggest-not-pick even sole-candidate/urgent, no answer-withholding, no re-surfacing declined, commerce only-when-named, specific-value phrasing). Post-Update Review real: spec-critic caught 3 issues (GS-9 tech-specific wording, SR-3 undecidable-direction gap, missing KH Motivation per RULES §5) — fixed before promotion; prompt-engineer PQ-6 PASS-WITH-REWRITES applied same-invocation. finalize: v0.1.310→0.1.311. Next: /magic.task main. (Revert: git restore .design/main/INDEX.md .design/main/STATE.md .design/main/specifications/{l1-generative-surface,l1-agent-tool-ergonomics,l1-extension-marketplace}.md && rm .design/main/specifications/{l1-session-reinforcement,l1-knowledge-horizon}.md)
