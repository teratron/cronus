# Project State

<!-- STATE.md — live project memory. Read FIRST in every workflow session. -->
<!-- Maximum 100 lines. Agent updates AFTER each completed action. -->

**Workspace:** main
**Updated:** 2026-09-07 05:08
**Phase:** 27 — Invocable Registry & CLI Derivation
**Status:** Active

## Current Position

- **Task:** T-27D01.2.2 Done: `board`/`schedule`/`budget`/`loop`/`workflow`/`knowledge` migrated — **both halves of track D's command-line derivation are now fully closed, all 18 real semantic groups + all 11 installation groups.** Phase 27 now **18/22** — tracks A, B, C Done; track D advancing (D02/D03 remain).
- **What T-27D01.2.2 built:** Two new `Binder`/`ArgValue` shapes: `Float`/`ArgValue::Float(f64)` (clap `value_parser!(f64)`, a real usage-level gate) for `budget set`'s dollar limit; `RepeatableNamedText`/`ArgValue::List(Vec<String>)` (`ArgAction::Append`) for `knowledge query --collection`. Dropped `Eq` from `ArgValue`/`ArgValues`/`Invocation` (kept `PartialEq`) since `f64` has no total equality and nothing used any of the three as a hash key. `workflow transpile --human/--compact` exclusivity moved from clap's `conflicts_with` into the handler (disclosed usage→application exit-code shift, matching `loop run --max-iter`/`knowledge query --top-k`'s same class). Added a generic `status`-field renderer arm (failed/aborted/stopped/paused → exit 1, collapsing `workflow run`'s pre-migration 3-way scheme into 2-way, disclosed) plus a more specific, ordered-first bespoke arm preserving `loop run`'s exact stdout text a real smoke test parses for a run id. `nodus::executor::Value` found structurally isomorphic to `OutcomeValue` — direct recursive converter written (`Float` → `Text`, same workaround as `learn`/`budget`).
- **Two real pre-ship bugs caught by the unmodified `cli_smoke.rs` suite, not the new unit tests** — `loop run`'s `file` binder was positional `Text` when the grammar needed named `--file`; `loop evolve`'s rewritten message dropped the literal word "unavailable" a test asserted on. Both fixed, suite re-run green. Second occurrence this phase of this exact discipline catching a real defect.
- **Verified, not left open:** `knowledge query`'s "embedding failed: Timeout" traces to `KnowledgeService::open_default` always wiring a real local Ollama-convention embedding endpoint — pre-existing infrastructure the old `knowledge` command called identically; not a regression, no local model server running in this environment.
- **Gates:** `cargo test -p cronus-cli --all-targets` green (`cli_smoke.rs` 41/41 unmodified); full `cargo test --workspace --exclude cronus-desktop -j 2` green; `clippy --workspace --exclude cronus-desktop --all-targets -D warnings`/`cargo fmt --all --check` clean. Manually exercised the real binary for all six groups. Containment self-check on every touched file found and fixed one bare task-id leak in a test doc comment (`dispatch.rs`).
- **Next Action:** Execute T-27D02 One renderer over the structured outcome; uniform output-format handling via /magic.run main

## Progress

```
Phase 27: [18/22] ███████░ 82%
Overall: [25/28] ███████░ 89%
```

- 2026-09-07 **Decision:** `/magic.run main` (session resumed post-compaction) — **T-27D01.2.2 Done: 6 semantic groups migrated, both halves of track D's derivation now closed (18/22).** Registered `board`/`schedule`/`budget`/`loop`/`workflow`/`knowledge` as real `Outcome`-returning handlers over their actual domain calls, through the facade's public door — matching every prior group's pattern. Designed and built the two `Binder`/`ArgValue` extensions the prior task deliberately deferred: `Float` (clap `value_parser!(f64)`, a real usage-level gate) for `budget set`'s dollar limit, `RepeatableNamedText`/`ArgValue::List` (`ArgAction::Append`) for `knowledge query --collection`; dropping `Eq` (kept `PartialEq`) from `ArgValue`/`ArgValues`/`Invocation` since `f64` has no total equality and neither type was ever used as a hash key. `workflow transpile --human/--compact` mutual exclusivity moved from clap's `conflicts_with` into the handler — a disclosed usage→application exit-code shift, the same class `loop run --max-iter`/`knowledge query --top-k` already carry as hand-parsed `NamedText`. Added a generic `status`-field renderer arm (failed/aborted/stopped/paused → exit 1, collapsing `workflow run`'s pre-migration 3-way scheme into 2-way, disclosed since untested) plus a more specific, ordered-first bespoke arm reproducing `loop run`'s exact stdout text byte-for-byte, since a real smoke test parses it to extract a run id for a further round trip. `nodus::executor::Value` turned out structurally isomorphic to `OutcomeValue` — wrote a direct recursive converter (`Float` → `Text`, the same workaround `learn`/`budget` already use). **Two real, pre-ship bugs caught by the existing, unmodified `cli_smoke.rs` suite** (not the new unit tests, which caught neither): `loop run`'s `file` binder bound as positional `Text` when the grammar needed a named `--file` flag; `loop evolve`'s rewritten message dropped the literal word "unavailable" a test asserted on. Both fixed, full suite re-run green — the second time this exact discipline (trust the pre-existing suite over new tests) caught a real defect this phase. **Verified rather than left open**: `knowledge query`'s "embedding failed: Timeout" traces to `KnowledgeService::open_default` always wiring a real local Ollama-convention embedding endpoint, contacted lazily per call — pre-existing infrastructure the old `knowledge` command depended on identically; not a regression, just no local model server running in this environment. Preserved the phase's named residuals exactly (`schedule list`'s store-failure-as-empty-list) and left `board`/`schedule`/`knowledge`'s real-disk write paths untested at the facade level, matching `role`'s already-recorded precedent. Containment self-check across every touched file found and fixed one real leak: a bare task-id in a test doc comment (`dispatch.rs`), restated in plain language. Full workspace `cargo test`/`clippy -D warnings`/`cargo fmt --check` green; `cli_smoke.rs` 41/41 unmodified. Computed next step (DA-6): T-27D02 (the shared renderer, now fully unblocked). (Revert: `git restore crates/contract/src/lib.rs crates/domain/src/invocable/dispatch.rs crates/cli/src/{generated.rs,cli.rs,commands.rs,main.rs} crates/core/src/invocable_bootstrap/mod.rs .design/main/{TASKS,STATE}.md .design/main/tasks/phase-27.md && rm crates/core/src/invocable_bootstrap/{board,schedule,budget,loop_group,workflow,knowledge}.rs`)

- 2026-09-06 **Decision:** `/magic.run main` — **T-27D01.1.2.2 Done: `ext`+`skill` migrated, installation half fully closed (17/22).** Extended the installation tree builder with one extra sub-nesting level using the same dot-in-the-verb-tail signal `verb_of` already uses (`"ext.skill.import"` names sub-group `skill`, tail `import`), rather than a generic N-level scheme — `ext` is the only group that has ever needed a third level. Migrated `ext`'s 6 flat verbs + `skill`'s 3 nested ones through already-proven `crate::commands::ext::*` handlers, unchanged bodies. `installation::dispatch` now takes a group's raw `ArgMatches` and resolves up to 2 levels itself (a verb never has its own subcommand, so "does the match have a further subcommand" safely signals a sub-group) — `main.rs`'s call sites simplified, no per-group nesting-depth knowledge leaked into them. **All eleven installation groups now generate from one declaration**, confirmed against real `cronus --help` output — closes T-27D01.1's own deferred "exactly eleven" clause after 4 sub-tasks and 1 `Binder` extension. Fixed the existing set-equality test's 2-level assumption (it would have recorded `("ext","skill")` as a verb) and added a dedicated 3-level dispatch-resolution test. Full workspace `cargo test` (78 result-sets)/`clippy -D warnings`/`cargo fmt` green. Hit a genuine host memory constraint (PowerShell OOM, not a code defect) mid-verification, resolved by retry once system memory recovered. Computed next step (DA-6): T-27D01.2.2 (semantic half's parallel closure) or T-27D03 (stub removal), both valid. (Revert: `git restore crates/cli/src/{main.rs,cli.rs,commands.rs,installation.rs} .design/main/{TASKS,STATE}.md .design/main/tasks/phase-27.md`)

- 2026-09-06 **Decision:** `/magic.run main` — **T-27D01.2 Done: 6 semantic groups migrated + a real cross-group verb-collision bug fixed (16/22).** Registered `codegraph`/`agent`/`role`/`exec`/`check`/`learn` (19 verbs) as real `Outcome`-returning handlers over the actual domain calls, through the facade's public door — matching `memory`'s own T-27D01 pattern. Split `invocable_bootstrap.rs` into one submodule per group before it grew into a mixed-concern file. **Found and fixed a real, already-shipping bug**: `generated::invocation_from_matches` searched the WHOLE flat semantic descriptor list for a matched group's verb rather than scoping to the matched group, so `role list --presets` was silently dispatching `core:exec.list` (both groups declare `list`) and dropping the flag — invisible while only `memory` was migrated (no shared verb names), caught by exercising the real binary rather than any existing test. Fixed by scoping the search to the matched group; added a dedicated regression test with two same-verb-named groups. Generalized the interim CLI renderer into a real recursive `OutcomeValue` mapping instead of hand-written arms per new shape (none of the 6 new groups had bespoke prior text to preserve). Kept the ephemeral-per-call residual class uniform across `codegraph`/`exec`/`learn` (matching `memory`'s already-recorded one) rather than selectively improving some under this task's pressure. Deliberately left `role`'s write verbs untested at the facade level (would be new coverage, not a gap — pre-migration suite never covered them either). **Split the remaining 6 semantic groups into T-27D01.2.2** (`board`/`schedule`/`loop` now satisfiable, `workflow` needs a disclosed rejection-mode shift, `knowledge`/`budget` still need a new shape). Recorded a genuine host build-instability (rustc crash under full parallelism) to persistent memory. Full workspace `cargo test`/`clippy -D warnings`/`cargo fmt` green (`-j 2`); `cli_smoke.rs` 41/41 unmodified. Computed next step (DA-6): T-27D01.2.2 or T-27D01.1.2.2, both valid. (Revert: `git restore crates/cli/src/{main.rs,cli.rs,commands.rs,generated.rs} crates/core/Cargo.toml .design/main/{TASKS,STATE}.md .design/main/tasks/phase-27.md && rm -rf crates/core/src/invocable_bootstrap && git checkout HEAD -- crates/core/src/invocable_bootstrap.rs crates/core/tests/invocable_invariants.rs`)

- 2026-09-06 **Decision:** `/magic.run main` — **T-27D01.1.2 Done: 5 more installation groups + BinderKind::NamedText built (15/21).** Verifying `workspace`/`backup`/`activation`/`archetype`/`registry` against the source (as the task's own notes demanded) found 4 of 5 needed a named value flag (`--name <value>`) shape `Binder` could not express. Rather than defer a second time, designed and built `BinderKind::NamedText` this pass — modeled the same way `Flag` already conflates value-shape with binding-style — since it was blocking most of what remained on BOTH command-surface halves, not just this task's own slice. `crates/contract` gained the variant; `crates/domain/src/invocable/dispatch.rs`'s `matches_kind` accepts it (not compiler-enforced, proven by a dedicated `bind()` test); `generated.rs`'s binder-to-`Arg` mapping gained one shared arm both halves reuse. Migrated `workspace` (create --name/--path), `backup` (create --to), `activation` (enable --mode, hand-validated, exit 1 on a bad value — application refusal not a usage failure), `archetype` (create --from), and `registry` (needed no extension, migrated alongside for free). `NamedText` carries no default-value slot by design — an absent optional one reads back `None`, the handler applies its own default, matching every other optional binder's contract. **Discovered the same extension also unblocks most of T-27D01.2's own deferred semantic-side groups** (`board`/`schedule`/`loop` now satisfiable; `workflow` needs a disclosed usage→application rejection-mode shift; `knowledge`'s repeatable flag and `budget`'s float value remain genuinely unaddressed) — recorded there for whoever executes it next. **`ext`'s nested `skill` sub-group (a real 3rd tree level) split into T-27D01.1.2.2**, not improvised here. Full workspace `cargo test`/`clippy -D warnings`/`cargo fmt` green; `cli_smoke.rs` 41/41 unmodified; manually verified every new group's real behavior including both a usage-level and an application-level rejection. Fixed one pre-existing, unrelated containment leak found in `dispatch.rs` while already touching that file. Computed next step (DA-6): T-27D01.1.2.2 or T-27D01.2. (Revert: `git restore crates/cli/src/{main.rs,cli.rs,commands.rs,installation.rs,generated.rs} crates/contract/src/lib.rs crates/domain/src/invocable/dispatch.rs .design/main/{TASKS,STATE}.md .design/main/tasks/phase-27.md`)

- 2026-09-06 **Decision:** `/magic.run main` — **T-27D01.1 Done: installation-half mechanism + 5 groups migrated (14/20).** Built `crates/cli/src/installation.rs`: one declaration (`declared_invocables`) consumed two ways — a pre-composition `clap` tree (flat-vs-nested, `allow_external_subcommands(true)`) answerable with ZERO composition for a recognized installation verb (LH-1/LH-5), and the same list registered into the catalog once composition runs (SP-11). `main.rs` restructured into a real two-stage flow: stage 1 parses against installation-only grammar (`Err=>e.exit()` unconditionally correct — external-subcommand mode swallows every unrecognized top-level word, so an error there can only be a genuine usage failure inside a recognized verb's own args); stage 2 composes via a real fallible `compose()` (exit code 3 reserved, structurally honest even though nothing can fail that way yet) then builds the full tree and parses once. Migrated `init`/`status`/`doctor`/`restore` (flat) + `dev` (nested, 3 verbs) — both shapes proven; each dispatches straight to its already-proven `crate::commands` handler, bypassing the shared `Dispatcher`/`Outcome` pipeline (only the CLI ever projects `Installation` locus). **Resolved a real id collision:** `core:status` moved out of the facade's placeholder registration into this frontend's own declaration, backed by the pre-existing real workspace-status check — verified nothing else depended on the placeholder first. **Split the remaining 6 installation groups into T-27D01.1.2** by the same pre-authorized `.N` pattern T-27D01 established. Full workspace `cargo test`/`clippy -D warnings`/`cargo fmt` green; `cli_smoke.rs` 41/41 unmodified; manually verified the real binary end to end including a genuine pre-composition usage-failure exit(2). **Recorded, not fixed:** a pre-existing `role_catalog.rs` parallel test-isolation race, unrelated to this task, found by the full-workspace run (`ROLE_CATALOG_PARALLEL_TEST_RACE`). Computed next step (DA-6): T-27D01.1.2 or T-27D01.2, either valid on track D. (Revert: `git restore crates/cli/src/{main.rs,cli.rs,commands.rs} crates/core/src/invocable_bootstrap.rs crates/core/tests/invocable_invariants.rs .design/main/{TASKS,STATE}.md .design/main/tasks/phase-27.md && rm crates/cli/src/installation.rs`)

- 2026-09-06 **Decision:** `/magic.run main` — **T-27D01 Done: generated-parser mechanism + `memory` group migrated, Track D opened (13/19).** Built `crates/cli/src/generated.rs`: a real, tested registry-driven `clap` builder-API tree generator over `Semantic`+`Shipped` invocables — groups by declared `group`, one verb-subcommand per invocable, `Binder`→positional/named-flag, dispatches through the real `Dispatcher`; augments the SAME tree the derive macro produces rather than maintaining two. `main.rs` rewritten for dual-path dispatch (registry-generated group first, legacy derived enum fallback — the launcher never resolves a name an extension could define, LH-1/LH-5) plus an explicitly-interim bridge renderer (real uniform renderer is T-27D02's separate job). `memory` (store/search/forget) fully re-expressed as `Outcome`-returning handlers in the facade; old `commands.rs::mod memory` tombstoned. **Two real bugs caught by the new tree existing, not by a written test:** `core:status`/`core:version` were still `Semantic`-classified from before `Locus::Installation` existed, colliding with the legacy `status` command (clap panic, 39/41 smoke tests) — reclassified; a `Flag`-kind binder bound without `--name`, silently positional-only — fixed, regression test added. **Formally split remaining scope** (`.N` convention) rather than cut corners: 12 real semantic groups → T-27D01.2, itself pre-split into 6 groups ready now vs. 6 needing a `Binder` extension (named flags for value-args, defaults, float) designed once rather than improvised per-group. Ephemeral in-memory store behaviour preserved exactly (SP-9/SP-10) and separately flagged via `record-diagnostic --code=MEMORY_STORE_EPHEMERAL_PER_CALL`. Full workspace `cargo test`/`clippy -D warnings`/`cargo fmt` green. Computed next step (DA-6): T-27D01.1 (the installation half). (Revert: `git restore crates/cli/src/{main.rs,cli.rs,commands.rs} crates/cli/Cargo.toml crates/core/src/invocable_bootstrap.rs crates/core/tests/invocable_invariants.rs crates/domain/src/invocable/registry.rs Cargo.toml .design/main/{TASKS,STATE}.md .design/main/tasks/phase-27.md && rm crates/cli/src/generated.rs`)

- 2026-09-03 **Decision:** `/magic.run main` — **Phase 26 (Application Shell Runtime) COMPLETE, 11/11, archived.** Engine drift: local `.magic/.version` 2.1.80 == snapshot `.design/INDEX.md` 2.1.80, silent. Rules parity RULES v1.6.0 == TASKS Based-on-RULES. Ran the whole phase in one pass on the user's `продолжай` (offer had been "run B→C→D→T to phase close, checkpoint on a fork" — no hard fork arose; every interpretation point resolved to the smaller-change reading and was recorded as an overridable `[DR]` in the workbook). **Behaviour-neutral where it migrated existing code** — the pre-Track-B shell/overlay/conformance/store-compliance suites all pass unmodified; B02/B03/D02 each disclosed zero existing-test changes. **11 test files / 81 tests added** (`vitest` 80 → 161); public API frozen at 43 value exports throughout (every new module is internal shell/shared plumbing; only `export type` widened for `ChannelEvent`/`ListenFn`, which do not enter `Object.keys(ui)`). **Genuine findings, not glossed:** (1) `cargo test settings` cannot run on this windows-gnu host — `STATUS_ENTRYPOINT_NOT_FOUND` at test-binary load via the WebView2Loader import chain; **verified pre-existing** by reproducing it with all three tauri `src/` changes git-stashed, so environmental (same class as the `bridge.rs` comment's own note and Phase 25's `fallow audit` limitation) — `cargo fmt`/`clippy -D warnings` are clean and the 4 new `#[cfg(test)]` settings tests compile, run them on a non-gnu CI runner. (2) The committed `apps/desktop/tauri/Cargo.toml`↔`Cargo.lock` drift (memory-documented) forces a re-resolve on every cargo invocation — `git checkout HEAD -- .../Cargo.lock` after each; no dependency change belongs to this phase. (3) `i18n.ts` still carries two `// Phase 8` / `// Phase 24` comment leaks (§6) — a standing containment-cleanup item, not touched here (out of task scope); the 2 task-id + 2 phase-designator refs T-26B03/T-26T01 *introduced* were purged at the write-time gate. **The capability-admission pass (T-26D03) is the honest core of the deliverable:** the runtime plumbing (stores, resolver, seam, persistence) is real, but the seam binds only `version`/`status` + the host settings facility + the (empty) event-channel class, so every one of the 15 sidebar surfaces and every projection domain is an explicit placeholder — the honestly-partial shell. `.design/.version` bump + root `CHANGELOG.md` append + `phase-26.md` archival are `finalize`'s to do. Computed next step (DA-6): every Stable spec is now built; the Backlog is 3 RFC `concept-only` L1s (`l1-foreign-agent-invocation`, `l1-remote-liveness-verdict`, `l1-cooperative-status-projection`), none opening a phase on promotion — so `/magic.spec main` to open new scope, or dogfood the now-wired GUI. (Revert: `git restore .design/.version .design/main/{PLAN,TASKS,STATE,CHANGELOG}.md .design/main/CHANGELOG.md apps/desktop/src/main.tsx apps/desktop/tauri/src/{settings,bridge,lib}.rs packages/ui/src/{index.ts,index.test.ts} packages/ui/src/shared/{bridge.ts,bridge.test.tsx,index.ts} packages/ui/src/shell/{actions.ts,surface-router.tsx,building-shell.tsx} && rm packages/ui/src/shared/{store,store.test,projection,projection.test,session,session.test,projection-channel,projection-channel.test,keymap,keymap.test}.*packages/ui/src/shell/{view-store,view-store.test,keymap-surface,keymap-surface.test,layout-record,layout-record.test,surface-router.test,admission.test,runtime-conformance}.* && git checkout .design/main/archives/tasks/phase-26.md 2>/dev/null || git restore .design/main/tasks/phase-26.md)
