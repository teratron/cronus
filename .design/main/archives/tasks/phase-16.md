---
phase: 16
name: "Project Wiki Store (L2)"
status: Done
subsystem: "crates/contract (WikiPage/WikiCitation types + read query surface) · crates/store-local (wiki.db schema: wiki_page tree + wiki_changelog + wiki_page_fts, persistence, read-only query, rebuild) · crates/domain (office-owned regeneration pipeline: event→page mapping, ground-truth gather, PW-4 citation guard, PW-8 internal-detail filter, source_fingerprint + PW-5 stale detection over the store seam) · crates/auth-local (access-grants Read gate for PW-7): the client-facing projection cache realizing l2-project-wiki — a rebuildable cache of upstream truth, distinct from the memory learned-tier where the store IS truth"
requires: [4, 5, 11, 13]
provides: []
key_files: []
patterns_established: []
duration_minutes: ~
---

# Stage 16 Tasks — Project Wiki Store (L2)

**Phase:** 16
**Status:** Done
**Strategic Goal:** Realize `l2-project-wiki` — the client-facing living documentation materialized as a **per-office SQLite projection cache** (`<ws>/wiki/wiki.db`), not loose Markdown. Build the store (schema + read surface + rebuild), the office-owned regeneration pipeline (event-driven, incremental, transactional), and the grounding/freshness/access guards. The load-bearing property is **projection-not-source (PW-3)**: `rebuild()` reconstructs an equivalent wiki from ground truth (board / graph decisions / operational ledger / deliverables), so nothing authoritative ever lives only in `wiki.db`. This contrasts deliberately with the memory learned-tier, where the store *is* the source of truth — here the store is only a rebuildable cache of upstream truth.

> **Domain-logic-first (Phases 9–15 precedent).** Client-language page generation is model-based; this phase proves the pipeline structure, the citation/freshness/access guards, and the no-generator degrade — not prose quality. **Generator-optional is the load-bearing acceptance property:** with no generator bound, a page stores a grounded stub composed from its citations and **never fabricates** content (mirrors MI-10/MI-12). Read-only-to-client (PW-2) is enforced structurally — the client surface exposes no mutating method — not by convention. Acceptance = each of PW-2/3/4/5/6/7/8 covered by a test + `rebuild` reconstructs an equivalent wiki + dropping `wiki.db` loses nothing durable + every generator-dependent step has a tested no-generator degrade.

## Atomic Checklist

- [x] [T-16A01] Wiki schema & types: `wiki_page` (parent/ord tree, `citations` JSON, `source_fingerprint`, `stale` flag), `wiki_changelog`, `wiki_page_fts` (FTS5) + indices in `crates/store-local`; `WikiPage`/`WikiCitation` contract types, absent-by-default. **Verify:** `cargo test -p cronus-store-local wiki::schema` — tables + indices created; a page round-trips with its citations and fingerprint.
- [x] [T-16B01] Regeneration pipeline core (PW-2/PW-3): event→affected-page mapping + ground-truth gather (board / graph decisions / ledger / deliverables) + transactional per-page upsert + `wiki_changelog` append; office-owned, no client write path. **Verify:** test — a simulated office event regenerates only the affected pages transactionally; a forced mid-regen failure rolls back, leaving prior rows intact and correctly marked. → **Done:** domain `wiki_regen` 4/4 (`event_maps_to_only_the_affected_kinds`; `regenerates_only_the_affected_pages_transactionally`; `a_skipped_kind_is_omitted_not_an_error`; `a_generation_failure_aborts_before_any_write` — nothing written on failure) + store-local `apply_regeneration_is_atomic_and_rolls_back_on_failure` (a duplicate changelog-id fails the batch mid-transaction → prior page row intact, no partial changelog). `cargo test --workspace` green exit 0; clippy `-D warnings` + fmt clean. Seam design: `WikiCache` trait in `crates/contract` (domain writes through it, `WikiStore` implements it — no `domain → store-local` edge); `PageRegenerator` is the domain-tier gather+generate seam (domain-logic-first, real model-backed impl later); transaction lives in `store-local` (`unchecked_transaction`, all-or-nothing).
- [x] [T-16B02] Grounding & honesty guards (PW-4/PW-8): citation guard — every substantive claim in a stored section resolves to a cited source or is dropped, never persisted uncited; internal-detail content filter strips engineering / SDD detail; no-generator degrade stores a grounded stub from citations. **Verify:** test — an uncited claim is rejected; an internal-detail input never reaches a row; no-generator mode stores a stub and never fabricates. → **Done:** domain `wiki_regen` 8/8 (`an_uncited_section_is_dropped_and_never_persisted`; `an_internal_detail_section_never_reaches_a_row`; `no_generator_mode_stores_a_grounded_stub_and_never_fabricates`; `all_sections_dropped_falls_back_to_a_grounded_stub`; `a_kind_with_no_ground_truth_is_skipped` + mapping/transactional/failure-abort from B01). `cargo test --workspace` green exit 0; clippy `-D warnings` + fmt clean. **Seam evolution (within Track B):** B01's single `PageRegenerator` (finished-page seam) split into `GroundTruth` (no-model source gather, always present) + optional `PageGenerator` (model-backed prose over structured `GeneratedSection`s) — needed so the guards operate at claim/section granularity and the no-generator path has sources for a grounded stub. Guards are pure fns (`filter_internal_detail` PW-8 → `citation_guard` PW-4); `grounded_stub` states only true grounded facts (pending + source count), fabricating nothing; `source_fingerprint` = blake3 over the sources (B03-ready).
- [x] [T-16B03] Freshness (PW-5): `source_fingerprint` compute + drift check → set `stale=1` when a page's sources moved without regeneration; `wiki_changelog` surfaced newest-first. **Verify:** test — mutating a source without regen flips the page `stale` flag; the changelog records the change newest-first. → **Done:** domain `wiki_regen` 10/10 (`a_source_moving_without_regeneration_flips_the_page_stale` — drift detected by fingerprint mismatch → page marked stale, idempotent on re-sweep; `a_page_whose_sources_are_unchanged_stays_fresh`) + store-local `mark_stale_flips_the_flag_and_pages_for_office_reads_it_back` and `changelog_is_returned_newest_first` (ORDER BY at DESC, limit honored). `cargo test --workspace` green exit 0; clippy `-D warnings` + fmt clean. `WikiCache` seam gained `pages_for_office` / `mark_stale` / `changelog`; domain `check_freshness(office, ground_truth, cache)` recomputes each non-stale page's `fingerprint_of(sources)` (blake3) and flips `stale=1` on drift — content is never regenerated or mutated, only the honest "may be out of date" marker set. Track B (B01→B02→B03) complete.
- [x] [T-16C01] Read-only client surface (PW-2/PW-6): navigation-tree query (`parent_id`/`ord`, overview→area→detail) + `wiki_page_fts` search; no write/update endpoint exposed to the client. **Verify:** test — tree + FTS queries return the expected pages; the client-facing surface exposes no mutating method (API/compile-time check). → **Done:** store-local wiki 9/9 (`read_surface_navigates_the_parent_ord_tree` — roots/children over an overview→area→detail tree; `read_surface_full_text_search_finds_the_matching_page` — FTS MATCH+rank finds the right page, no-match→empty; `read_surface_is_read_only_by_construction`). `cargo test --workspace` green exit 0; clippy `-D warnings` + fmt clean. **Structural read-only (PW-2):** new `WikiReadSurface` trait in `crates/contract` exposes ONLY `page`/`children`/`search`/`changelog` — the client is handed a `&dyn WikiReadSurface`, so a write like `upsert_page` is not merely unavailable by convention but a compile error (no such method on the trait object). `WikiStore` implements it; `children(parent_id: Option)` uses null-safe `parent_id IS ?` (roots when `None`); `search` joins `wiki_page_fts MATCH ? ORDER BY rank`.
- [x] [T-16C02] Access gate + rebuild (PW-3/PW-7): `access-grants` `has_access(Wiki, office_id, Read)` on every client read; `rebuild(office)` drops and re-derives all rows from ground truth. **Verify:** test — a read without the Read grant is denied; `rebuild` reconstructs an equivalent wiki (same structure + citations) and dropping `wiki.db` loses nothing durable. → **Done:** **Access gate (PW-7):** new `ResourceKind::Wiki` variant + domain `wiki_access::GatedWiki` — wraps any `WikiReadSurface` and runs `has_access(Wiki, office_id, Read)` **before every read** (page/children/search/changelog), delegating only if allowed; a denied read never reaches the store, and a `page(id)` for another office reports absent (no cross-office leak). **Rebuild (PW-3):** new `WikiCache::rebuild_office` (transactional drop-of-office-rows + reinsert, all-or-nothing) in contract + store-local; domain `wiki_regen::rebuild` re-derives **every** page kind (`WikiPageKind::all()`) from ground truth via the extracted `build_pages` core (shared with `regenerate`) and hands the whole set to `rebuild_office` — a generation failure aborts before the store is cleared. 5 gate tests (denied/granted/owner-wins/cross-office-no-leak/cross-office-absent) + 3 domain rebuild tests (every-kind / replaces-prior-rows / aborts-before-write) + 2 store-local (replace+resync-FTS / rollback-on-failure) + **core integration `wiki_rebuild`**: build into a real on-disk `wiki.db` → **delete the file** → rebuild from the SAME ground truth into a fresh empty store → equivalent set (same structure + citations + fingerprints), proving drop-loses-nothing; plus the gate proven against the real `WikiStore`. store-local wiki 11/11, domain wiki_regen+wiki_access 18/18, `cargo test --workspace` green exit 0 (63 ok, 0 failed) + clippy `-D warnings` + fmt clean. Phase 16 Track C complete.
- [x] [T-16T01] Validation sweep: PW-2/3/4/5/7/8 invariant tests + drop-loses-nothing + every generator-dependent step has a tested no-generator degrade. **Verify:** `cargo test --workspace` green (new wiki tests) + clippy `-D warnings` + `cargo fmt --all --check` clean. → **Done:** new core acceptance suite `crates/core/tests/wiki_invariants.rs` — one named test per invariant, each exercised **end-to-end through the real SQLite `WikiStore`** + the real domain pipeline (not in-memory fakes): `pw2_the_client_surface_exposes_no_write_method` (read-only-by-construction), `pw3_rebuild_reconstructs_an_equivalent_wiki_into_a_fresh_store` (complements the file-drop proof in `wiki_rebuild.rs`), `pw4_an_uncited_claim_is_never_persisted`, `pw5_a_source_that_moves_without_regeneration_is_marked_stale`, `pw6_the_wiki_is_navigable_and_searchable`, `pw7_reads_follow_the_office_sharing_posture` (private/owner/user/group/public), `pw8_internal_engineering_detail_never_reaches_a_row`, `every_generator_dependent_step_degrades_to_a_grounded_stub_without_a_generator` (both `rebuild` and `regenerate` no-generator paths). PW-1 covered via its testable proxies (PW-4/PW-8), noted in the file header. Fixtures: `FixedGround`, `MovingGround` (Cell-driven drift), `ScriptedGen` (cited + uncited + internal-detail sections). **Verify:** wiki_invariants 8/8, `cargo test --workspace` green exit 0 (64 ok, 0 failed) + clippy `-D warnings` + fmt clean. Phase 16 complete (7/7).

## Tracks

- **A — Schema (foundation):** T-16A01. Gates B and C.
- **B — Regeneration (office-owned write path):** T-16B01 → T-16B02 → T-16B03 (share the regeneration entry point → serialized).
- **C — Read & access surface:** T-16C01, T-16C02 (depend on A).
- **T — Validation:** T-16T01 (last; sweeps every PW invariant).

Foundation-then-parallel: Track A gates B/C; B is serialized on the shared regeneration entry point; C can proceed once the schema lands; T closes the phase.
