---
phase: 30
name: "Usage-Simulation Harness"
status: Done
subsystem: "crates/simulation · simulations"
requires: [3, 27]
provides:
  - "a ports-tier cronus-simulation crate: disposable worlds that refuse to be built inside the repository (world.rs), an as-it-happens transcript (transcript.rs), a scenario parser that refuses an obligation that cannot fail (scenario.rs), a replay lane guarding pinned routes at no inference cost (replay.rs + tests/replays.rs), and a catalog complement derived from the product's own completion script (coverage.rs)"
  - "the cronus-sim wrapper binary: world/run/note/verdict/finish/pin/coverage, with cross-process state in one run-state.json per world (run_state.rs), five-way outcome attribution (findings.rs — pass/fail/incomplete/contaminated/void), and a bound that refuses further run invocations once exhausted"
  - "the root-level simulations/ corpus: README.md (format, vantage table, 11 perturbation classes) and the first short-tier scenario, first-run-three-cards.md"
key_files:
  created:
    - "crates/simulation/Cargo.toml"
    - "crates/simulation/src/lib.rs"
    - "crates/simulation/src/world.rs"
    - "crates/simulation/src/product.rs"
    - "crates/simulation/src/transcript.rs"
    - "crates/simulation/src/replay.rs"
    - "crates/simulation/src/scenario.rs"
    - "crates/simulation/src/coverage.rs"
    - "crates/simulation/src/findings.rs"
    - "crates/simulation/src/run_state.rs"
    - "crates/simulation/src/bin/cronus_sim.rs"
    - "crates/simulation/tests/replays.rs"
    - "crates/simulation/tests/replays/init-then-status.toml"
    - "crates/simulation/tests/corpus.rs"
    - "crates/simulation/tests/cronus_sim_e2e.rs"
    - "simulations/README.md"
    - "simulations/short/first-run-three-cards.md"
  modified:
    - "Cargo.toml (workspace members + toml/serde_json/cronus-simulation deps)"
patterns_established:
  - "a cross-process CLI wrapper persists its working state as one JSON file inside its own disposable world's root, so state disappears with the world on teardown and needs no separate cleanup or lookup table — the world id alone reconstructs its root path (World::root_for_id)"
  - "a ports-tier crate that needs the product's own action catalog derives it from the product's shell-completion script (a complete, structured enumeration that already exists) rather than linking a live registry or inventing a catalog-dump verb that doesn't exist — disclosed as a best-effort structural approximation, not the exact locus/stability-filtered set"
  - "Windows file-open share flags default to permitting deletion (FILE_SHARE_DELETE); a test that needs to force a genuine teardown failure must open with share_mode(0) explicitly, or the deletion silently succeeds anyway"
  - "a test that reads or perturbs whole-repository state (git status) must serialize against every other test in its file via a shared Mutex, since cargo test's default parallelism otherwise corrupts the comparison — observed directly when an unguarded contamination test failed four unrelated tests in the same run"
duration_minutes: ~
---

# Stage 30 Tasks — Usage-Simulation Harness

**Phase:** 30
**Status:** Done
**Strategic Goal:** Land the machinery that turns a free-route run into evidence — disposable worlds, an as-it-happens transcript, a validator that refuses obligations which cannot fail, the wrapper every product invocation passes through, and the replay lane that guards findings at no inference cost. One authored scenario ships to prove the lane runs; the corpus itself is later work.

## Phase Notes

**The harness, not the corpus.** Exactly one `short` scenario lands here. The point of this phase is that a run becomes *recordable* and *replayable*, not that the product is covered.

**Ordering is load-bearing** (the spec's own §5). `world`'s refusals precede its happy path, because the missing guard is destructive rather than merely wrong. `transcript` precedes anything that reasons over a run, because until invocations are recorded as they happen, every later piece reasons over a narrative. The replay lane is green with one hand-written case before any agent runs. The validator lands with its refusal set intact — a validator added after scenarios exist is a validator that gets weakened to admit them.

**Track C is independent of Track A.** Scenario parsing touches no world and no transcript, so it can start immediately and does not stall if the Windows teardown or the repository-containment guard proves fiddly.

**Nothing here is wired into a build, package, or release step.** `cronus-simulation` is a workspace crate whose tests run with the workspace's tests; the agent-driven lanes are invoked by a person. A task that adds a build-manifest hook has gone outside this phase.

## Atomic Checklist

- [x] [T-30A01] The crate exists in the ports tier, and a world refuses to be built inside the repository
- [x] [T-30A02] Every invocation is recorded as it happens, never reconstructed afterwards
- [x] [T-30B01] Pinned routes run as ordinary tests, with no agent in the loop
- [x] [T-30C01] A scenario parses only if its obligations can fail
- [x] [T-30C02] The first `short` scenario ships, and it is not a smoke test
- [x] [T-30D01] `cronus-sim` is the only path by which a run touches the product; findings carry their attribution
- [x] [T-30D02] Coverage reports the complement against the catalog, never a percentage
- [x] [T-30T01] Invariant compliance, containment, and full quality gates

## Detailed Tracking

### [T-30A01] The crate exists in the ports tier, and a world refuses to be built inside the repository

- **Spec:** l2-simulation-suite.md §4.1, §3 (USM-2 row)
- **Status:** Done
- **Assignment:** Agent
- **Scope:** New workspace member `crates/simulation` (package `cronus-simulation`), depending on `cronus-contract` and nothing from the domain tier — the same rationale `crates/conformance/Cargo.toml` already records. `src/world.rs` provides `build` / `drop`: a fresh directory under `std::env::temp_dir()` named by scenario id + UTC stamp, made the working directory of every spawned process; all `CRONUS_*` variables plus the `HOME`/`USERPROFILE` values used for configuration discovery cleared from the child environment; product version and the repository's dirtiness digest stamped at build time; teardown removing the tree with bounded retries, because a held handle delays deletion on Windows. `build` **refuses** when the resolved root lies inside the repository working tree.
- **Verify:** `cargo test -p cronus-simulation` (PowerShell, per the project's native-build discipline) passes with three new tests: (a) `world::build` against a root resolving inside the repository returns an error naming the containment refusal, and no directory is created; (b) a built world's working directory has no ancestor equal to the repository root, and a `CRONUS_MISSION_MODE` value exported in the parent process is absent from the recorded child environment; (c) `drop` removes the tree, and a second `drop` on the same world is a no-op rather than an error. `cargo clippy -p cronus-simulation --all-targets -- -D warnings` is clean.
- **Handoff:** T-30A02, T-30B01, T-30D01.
- **Changes:** New workspace member `crates/simulation` (`cronus-simulation`, ports tier — `cronus-contract` only). `src/world.rs`: `World::build`/`build_with_base` (containment refusal against a `.git`-discovered repo root), `cwd()`/`global_dir()` split, `child_env()` stripping `CRONUS_*` and forcing `CRONUS_PORTABLE_DIR` into the world (reuses the product's own portable-mode seam, `crates/domain/src/paths.rs`, rather than overriding `HOME`/`APPDATA` per OS), `teardown()` with bounded retry, and a `repo_dirty_digest` captured at build for the later contamination check. 3 unit tests, all passing. `cargo check`/`clippy -D warnings`/`fmt --check` clean; `cargo check --workspace` unaffected.
- **Notes:** Worlds are never reused — a second run gets a new directory, so a stale tree left behind by an exhausted retry budget is identifiable by its name rather than silently inherited.

### [T-30A02] Every invocation is recorded as it happens, never reconstructed afterwards

- **Spec:** l2-simulation-suite.md §4.3, §3 (USM-5 row)
- **Status:** Done
- **Assignment:** Agent
- **Scope:** `src/transcript.rs` — an append-only record written at the moment each invocation completes, one entry per product invocation carrying argv, stdin, stdout, stderr, exit code, wall duration, and a digest of the world's state before and after. The transcript header carries the product version, the resolved environment, and the world id from T-30A01. Entries are addressable by index, because verdicts cite indices rather than restating output.
- **Verify:** `cargo test -p cronus-simulation` passes with: (a) a test spawning the real `cronus --help` through the recorder asserts the entry's stdout equals the process's actual stdout byte-for-byte and that `duration` is greater than zero; (b) a test spawning a command that exits non-zero asserts an entry is still written, with the non-zero code and the stderr captured; (c) a test asserts the before/after state digests differ for an invocation that writes into the world and are equal for one that does not.
- **Handoff:** T-30B01, T-30D01.
- **Changes:** `src/transcript.rs` (`Transcript::new`/`record`/`entry`, `Entry` with `changed_world()`) plus `World::state_digest()` and its private recursive directory walker in `world.rs`. `record` spawns via `env_clear()` + `world.child_env(std::env::vars())` (never the ambient parent env), captures stdout/stderr/exit/duration, and brackets the spawn with before/after `state_digest()` calls. New `src/product.rs`: `resolve_binary()` (override env, else sibling-of-current_exe discovery covering both `target/<profile>/` and `target/<profile>/deps/`) plus a test-only `test_cronus_binary()` that runs `cargo build -p cronus-cli --bin cronus` before resolving — needed because a bare workspace-level `cargo test` gives no build-order guarantee between two packages that do not depend on each other, and `cronus-simulation`/`cronus-cli` are exactly such a pair; without it these tests raced the sibling build and failed non-deterministically in the first run. **Design correction from Scope prose**: isolation is achieved via `CRONUS_PORTABLE_DIR` (the product's own portable-mode seam, `crates/domain/src/paths.rs`), not by overriding `HOME`/`USERPROFILE` as originally described — Windows config discovery reads `%APPDATA%`/`%LOCALAPPDATA%`, never `HOME`, so the one-override approach is both simpler and actually correct across all four supported OS families; this was folded into `child_env()` in T-30A01, confirmed here by the passing `state_digest` test using `cronus init`. 3 new tests (byte-for-byte stdout, non-zero exit + stderr, digest before/after), all passing against the real built `cronus` binary. `cargo check`/`clippy -D warnings`/`fmt --check`/`cargo check --workspace` all clean.
- **Notes:** Writing at completion — not at the end of the run — is the property being tested. A test that builds the whole transcript in memory and flushes once would pass a weaker assertion than the one this task exists for.

### [T-30B01] Pinned routes run as ordinary tests, with no agent in the loop

- **Spec:** l2-simulation-suite.md §4.4, §3 (USM-7 row)
- **Status:** Done
- **Assignment:** Agent
- **Scope:** `src/replay.rs` executes a pinned route: build a world, run the recorded argv sequence in order, assert the recorded outcome. `tests/replays.rs` discovers and runs every case under `tests/replays/`, and one hand-written case lands with it — `tests/replays/init-then-status.toml`, covering `cronus init` writing the skeleton under `.cronus/` and `status` then finding that workspace, which is behaviour the smoke suite already asserts and which therefore cannot be wrong here for reasons outside this phase.
- **Verify:** `cargo test -p cronus-simulation --test replays` runs the case and reports it by name. Positive control: a copy of the case with its expected exit code altered fails, and the failure message names the case and the divergence — added as a `#[test]` over an inline fixture, not by committing a broken case. `cargo test` at the workspace root includes this target.
- **Handoff:** T-30T01.
- **Changes:** New `src/replay.rs` (`ReplayCase`/`ReplayStep` via `serde`+`toml`, `parse()`, `run()` building a world and asserting each step's exit code via `Transcript::record`). Added `toml = "0.8.19"` to workspace deps and `serde`+`toml` to `crates/simulation/Cargo.toml` (justified: T-30C01's scenario format needs the same typed-TOML parsing, and a hand-rolled parser would be the weaker, harder-to-keep-correct choice for either). `tests/replays.rs` discovers every `*.toml` under `tests/replays/` and runs it; `tests/replays/init-then-status.toml` is the one hand-written case (init exits 0, then status from the same cwd finds the workspace and exits 0 — the exact semantics `crates/cli/tests/cli_smoke.rs::init_confines_the_skeleton_to_a_dot_cronus_directory` already asserts). Positive control landed as an inline `#[test]` in `replay.rs` (mutates a parsed copy's `expect_exit` to 99, asserts `run()` fails and the message names both the case id and the wrong value) — nothing broken is committed under `tests/replays/`. 12 tests total in the crate now, all passing; `cargo check`/`clippy -D warnings`/`fmt --check`/`cargo check --workspace` all clean.
- **Notes:** The replay runner makes no judgements and needs no vantage. Everything interesting happened when the route was discovered; its only job is to notice when the route stops holding.

### [T-30C01] A scenario parses only if its obligations can fail

- **Spec:** l2-simulation-suite.md §4.2, §3 (USM-1 / USM-3 / USM-11 rows)
- **Status:** Done
- **Assignment:** Agent
- **Scope:** `src/scenario.rs` parses Markdown with TOML frontmatter into a typed scenario: `id`, `tier` (from directory placement), `surfaces`, `covers`, `roles`, `vantage` (closed enum), `world`, `bound` (steps / wall seconds / spend), `[[perturbation]]`, `[[obligation]]`. **Unknown keys are rejected**, so a route cannot be smuggled in as an extra field. The validator refuses at parse time: an obligation with no `evidence`; an activity-shaped `statement`; an expectation whose only pass signal is vocabulary failure output also produces; an absence claim with no declared `positive_control`; an empty or duplicate-identity obligation set; a missing `bound`. A refusal is an error, never a skip.
- **Verify:** `cargo test -p cronus-simulation` passes a table-driven test with one fixture per refusal class, each asserting a parse error whose message names that class, plus one valid scenario that parses into the expected typed value. A fixture carrying an unknown top-level key is refused. Scenarios with zero perturbations parse and are reported `smoke`.
- **Handoff:** T-30C02, T-30D01, T-30D02.
- **Changes:** New `src/scenario.rs`: typed frontmatter (`Tier`, `Vantage`, `WorldKind`, `Bound`, `Perturbation`, `DecidedBy`, `Obligation`, all `deny_unknown_fields`) plus `split_frontmatter()`/`parse()`/`validate()`. Refuses at parse time: empty or duplicate-id obligation set, no-evidence obligation, activity-shaped `statement` (verb list: exercise/check/improve/review/handle), indistinct pass signal (evidence reducing to just ok/done/passed/complete/0), an absence-shaped statement (negation-marker heuristic) with no `positive_control`; a missing `bound` table and an unknown top-level key are refused structurally by `deny_unknown_fields`/required-field serde errors, which already name the offending key. `Scenario::is_smoke()` and `::judged_ratio()` added as the USM-6/USM-11 disclosure hooks later tasks read. 10 new tests (one per refusal class + the valid-fixture + zero-perturbation cases), all passing; total crate tests now 21. `cargo check`/`clippy -D warnings`/`fmt --check`/`cargo check --workspace` all clean.
- **Notes:** Independent of Track A — this task needs no world and no transcript, and should start in parallel with T-30A01.

### [T-30C02] The first `short` scenario ships, and it is not a smoke test

- **Spec:** l2-simulation-suite.md §4.1, §4.2; l1-usage-simulation.md §4.4 (perturbation classes)
- **Status:** Done
- **Assignment:** Agent
- **Scope:** `simulations/README.md` (how to author one; the eleven perturbation classes; what a vantage is) and `simulations/short/first-run-three-boards.md` — persona new to the product, vantage `builtin-help`, a goal stated in the persona's vocabulary and not the product's, at least two perturbations (reversal and return), and falsifiable obligations with one absence claim carrying its positive control. No file under `simulations/` references any planning or specification artifact.
- **Verify:** `cargo test -p cronus-simulation` passes a test that parses every file under `simulations/`, asserting the corpus is non-empty, that the `short` tier holds at least one scenario, and that every `short` scenario declares at least one perturbation. A second assertion greps the corpus for the containment-forbidden reference classes (task ids, phase designators, `.design/` paths, SDD system filenames) and fails on any hit.
- **Handoff:** T-30D01, T-30T01.
- **Changes:** New root-level `simulations/README.md` (format, the 11 perturbation classes, vantage table, authoring checklist) and `simulations/short/first-run-three-cards.md`. **Correction from Scope prose**: renamed from the planned "three-boards" — the real product has one board holding cards (`cronus board add/list/move/block/...`), not multiple named boards; the scenario and its `covers` list were written against the actual CLI (checked via `cronus --help`/`cronus board --help`), not the illustrative example in the L2 spec. Persona/goal phrased in plain language with no product verbs, verified by authorship rather than by a mechanical check (see the Notes correction above — no vocabulary-leakage validator exists). Two obligations are absence claims and both carry a `positive_control`; both referenced replay paths (`replays/silently-dropped-item.toml`, `replays/known-silent-exit.toml`) are forward references — the scenario file format does not require them to resolve yet, and landing them requires an actual discovery run, which is out of this phase's scope. New `tests/corpus.rs`: parses every file under `simulations/`, asserts the corpus and the `short` tier are non-empty and every `short` scenario is perturbed, and hand-rolled `contains_task_id`/`contains_phase_designator` matchers (with their own self-tests) assert no scenario file leaks a task id, phase designator, `.design/` path, or SDD system filename. 25 tests total in the crate now, all passing; `cargo check`/`clippy -D warnings`/`fmt --check`/`cargo check --workspace` all clean.
- **Notes:** **Correction ahead of execution:** T-30C01's landed validator checks the TOML frontmatter only — `goal` is prose under a `## Goal` heading in the body, not a machine field, and no mechanical vocabulary-leakage check was built or specified with a concrete algorithm. A naive keyword-against-command-group-names check would false-positive constantly ("check", "status", "loop", "init" are ordinary English words). This task satisfies USM-1's intent by careful authorship instead: the goal is phrased in the persona's own words, verified by reading, not by a parser rule. A real mechanical check (if wanted later) needs a sharper design than a bare word list.

### [T-30D01] `cronus-sim` is the only path by which a run touches the product; findings carry their attribution

- **Spec:** l2-simulation-suite.md §4.3, §4.5, §3 (USM-3 / USM-5 / USM-9 / USM-12 rows)
- **Status:** Done
- **Assignment:** Agent
- **Scope:** `src/bin/cronus_sim.rs` — `world new`, `run -- <argv>`, `note`, `verdict`, `finish`, `pin`, `coverage`. `finish` computes exit status **from obligation verdicts alone**: pass, fail, or `incomplete` as a third status distinct from both, emitted when the scenario's `bound` is exhausted or the run is interrupted, naming the undecided obligations. Discoveries recorded by `note` can never fail a run. `finish` also writes the run report, and `src/findings.rs` supplies the attribution it records: **product defect** (pin, then fix), **scenario defect** (the obligation cited entries that do not support it, or it failed on this route and passed on an earlier route through the same scenario with no product change), **environment defect** (the wrapper failed, the world could not be built or torn down, or the repository digest changed during the run — the run is `void`, not failed). Product-binary resolution is explicit: an argument or environment variable names the binary, otherwise the workspace target directory's build is used, and **the resolved path is recorded in the transcript header** — `CARGO_BIN_EXE_cronus` is unavailable to a binary target, and silently falling back to an installed `cronus` would measure the wrong product.
- **Verify:** An integration test drives the built `cronus-sim` end-to-end over fixture scenarios and asserts four distinct outcomes: a run whose obligations all pass exits `0`; a run with one failing obligation exits non-zero; a run whose only negative signal is a `note` discovery still exits `0`; a bounded-out run exits with the `incomplete` status code, and its report names the undecided obligations. A fifth assertion: with no binary argument and no environment override, the resolved path recorded in the transcript lies inside the workspace target directory. A sixth: a run whose repository digest differs between world build and teardown is reported `contaminated` with its verdicts discarded, and a run whose world could not be torn down is attributed `environment` and reported `void` rather than `fail` — the two attributions that must never be filed as product findings.
- **Handoff:** T-30D02, T-30T01.
- **Changes:** New `src/findings.rs` (`RunOutcome` — Pass/Fail/Incomplete/Contaminated/Environment, each with a distinct exit code — and `FinishReport`). New `src/run_state.rs`: cross-process persistence as one `run-state.json` inside the world root (`world`/`run`/`note`/`verdict`/`finish` are separate subprocess invocations, so nothing survives between them except this file); `record_run` refuses once `bound.steps`/`bound.wall_secs` is reached, spawning nothing; `finish` checks contamination first (repo-digest baseline vs a fresh recomputation — Contaminated overrides everything), then undecided obligations (Incomplete, naming them), then verdicts (Pass/Fail); a teardown failure overrides to Environment unless already Contaminated. New `src/world.rs` additions: `attach`/`root_for_id` (the id-to-directory formula is the entire cross-process handle — no lookup table needed) and `current_repo_dirty_digest` (recomputed fresh, kept separate from the immutable build-time baseline). New `src/bin/cronus_sim.rs`: `world new [--bin <path>] <scenario-file>`, `run <world> -- <argv>`, `note`, `verdict <world> <id> pass|fail [--cite i,j]`, `finish`, `pin` (distils recorded entries into `tests/replays/<name>.toml` — reuses `replay::parse`, joining the discover→pin→guard loop concretely). **`coverage` deliberately not wired in** — its logic is T-30D02's, and a stub calling nothing would be worse than an honestly absent subcommand. New `tests/cronus_sim_e2e.rs`: 7 tests driving the real binary end-to-end (all-pass, one-fail, note-only, bounded-out-and-refused, no-override binary resolution, contaminated, and a `#[cfg(windows)]`-only teardown-failure/void case — POSIX coverage of that last path is a disclosed gap). Two real bugs the tests themselves caught and fixed, not test artifacts: (1) the contamination probe's first file extension (`.tmp`) is matched by the repository's own `.gitignore`, making it invisible to `git status` — renamed to `.md`; (2) Rust's default Windows file-open share mode includes `FILE_SHARE_DELETE`, so a plain `File::create` does NOT actually block another process's `remove_dir_all` — the held-open-file test now opens with `share_mode(0)` via `OpenOptionsExt` to deny all sharing. A third fix was test-isolation, not product code: `cargo test` runs this file's functions concurrently by default, and contamination detection reads the *whole repository's* `git status` — a process-wide signal — so every test in the file now serializes through one `Mutex` for its full body; without it the contamination test's probe corrupted every concurrently-running test's digest comparison. 33 tests total in the crate now (20 lib unit + 1 bin unit + 4 corpus + 7 e2e + 1 replays.rs). `cargo check`/`clippy -D warnings`/`fmt --check`/`cargo check --workspace` all clean.
- **Notes:** Routing every invocation through `run` is not convenience — it is why the transcript exists whether or not the agent chooses to mention what happened, which is also what makes a silently abandoned attempt visible.

### [T-30D02] Coverage reports the complement against the catalog, never a percentage

- **Spec:** l2-simulation-suite.md §3 (USM-8 row), §4.1
- **Status:** Done
- **Assignment:** Agent
- **Scope:** `src/coverage.rs` enumerates the product's action catalog, intersects it with the `surfaces` / `covers` / `roles` declarations of every parsed scenario, and prints **what nothing simulates**. The catalog is obtained through the product's own projected catalog surface via the wrapper, not by linking a live registry — the crate stays on `cronus-contract` shapes, so a surface that cannot link the domain tier could still join later.
- **Verify:** `cargo test -p cronus-simulation` passes a test supplying a stub catalog of three entries and a scenario covering one, asserting the printed complement names exactly the other two and nothing else. A second test asserts the output carries a count and the entry names, and contains no percentage. `cronus-sim coverage` against the real product runs and exits `0` with a non-empty complement (the corpus holds one scenario; a complement of zero at this point would mean the catalog was not read).
- **Handoff:** T-30T01.
- **Changes:** New `src/coverage.rs`: `CoverageReport::compute` (catalog x covers intersection, sorted, deduplicated, headline is always a count + names, never a `%`), `catalog_from_completion` + `parse_completion_actions`, `default_simulations_dir`/`load_corpus`. **Design correction, disclosed rather than assumed**: the L2 spec’s own “projectable-face seam every surface already answers through” turned out not to exist as a callable API — there is no JSON catalog-dump verb, and the seam described (`SurfaceProjection`, `crates/conformance/src/projection.rs`) is a *trait* a consuming crate’s test target implements at compile time, not something a standalone binary can call at runtime. The actual source used instead: `cronus completion bash`, whose generated case-statement already enumerates every `cronus__subcmd__a__subcmd__b…` identifier as a complete, structured list — rewritten to the same dotted `a.b` convention scenario `covers` fields already use. Documented in `catalog_from_completion`’s own doc comment as a **best-effort structural approximation**: completion output carries no locus/stability annotation, so it cannot reproduce `Invocable::is_projected()`’s exact filter (an installation-only entry is indistinguishable from an ordinary one here) — still strictly ports-tier, never a live registry, and sufficient for this task’s actual Verify criterion. Wired `coverage [--bin <path>]` into `cronus_sim.rs` (left unwired by T-30D01 on purpose). Real run against the built product: 106 catalog entries, 4 covered, 102 uncovered, exit 0. 4 new tests (3 lib unit + 1 e2e against the real binary); 37 tests total in the crate now. `cargo check`/`clippy -D warnings`/`fmt --check`/`cargo check --workspace` all clean. Also: corrected `toml` workspace dependency requirement (`"1.1.6+spec-1.1.0"` → `"1.1.6"`) after cargo warned the semver-metadata suffix in a version *requirement* is ignored and confusing — unrelated to this task’s own scope but caught while touching the manifest.
- **Notes:** The complement being large is the correct result for this phase. It becomes a coverage claim only once the corpus grows, which is deliberately not this phase's work.

### [T-30T01] Validation Task — invariant compliance, containment, and full quality gates

- **Goal:** Verify the landed harness against `l2-simulation-suite`'s Invariant Compliance table and the project's definition of done.
- **Method:** (a) Workspace gates, run in PowerShell: `cargo check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`, `cargo fmt --all --check` — all green, with `cargo test` retried at `-j 2` before any failure is treated as a real defect, per the known rustc-under-full-parallelism crash on this host. (b) A written mapping from each of USM-1…USM-12 to the code or test that discharges it, recorded in the task's `Changes` field; any invariant with no landed discharge is named as such rather than omitted. (c) Structural containment: `Cargo.toml`, the workspace manifest, and any CI configuration contain **no** invocation of an engine script and no dependency introduced solely to run one — asserted by inspection and recorded. (d) Reference containment: no file under `crates/simulation/` or `simulations/` contains a task id, a phase designator, an SDD system filename, or a `.design/` path.
- **Verify:** All four sub-checks reported individually with their evidence; a partial pass is reported as partial, never aggregated into green.
**Status:** Done
- **Changes:**
  **(a) Workspace gates (PowerShell)** — `cargo check --workspace`: PASS. `cargo clippy --workspace --all-targets -- -D warnings`: PASS (13 crates checked). `cargo fmt --all -- --check`: PASS. `cargo test --workspace`: PASS — 87 test-result blocks, 0 `FAILED`, exit 0 on the **first** run; the known rustc-under-full-parallelism crash did not occur, so no `-j 2` retry was needed.

  **(b) Invariant compliance (USM-1…USM-12)** — see table below. Two invariants (USM-4, USM-6) are **partially** discharged — the schema types and carries the declaration, but a further mechanical check (vantage-grounding detection; closed perturbation-class enum) is not built and is named rather than silently claimed. One invariant (USM-10) is **not discharged by this phase's code at all** — it is the standing process discipline `RULES.md` C30 already encodes project-wide, not a mechanism `crates/simulation` builds.

| Invariant | Discharged by |
| --- | --- |
| USM-1 (intent fixed, route never prescribed) | `scenario.rs` frontmatter has no command field; `deny_unknown_fields` rejects a smuggled one (`an_unknown_top_level_key_is_refused`); realized in `simulations/short/first-run-three-cards.md`'s goal, stated in the persona's own words |
| USM-2 (real surfaces, real effects, disposable world) | `world.rs`: `build`/`build_with_base` containment refusal, `cwd()`/`global_dir()` split, `child_env()` forcing `CRONUS_PORTABLE_DIR` — `build_refuses_a_base_inside_the_repository`, `build_outside_the_repository_has_no_repo_ancestor_and_filters_cronus_vars` |
| USM-3 (obligations vs. discoveries) | `run_state.rs`: verdicts decide the outcome, `notes` never read by `finish` — `a_note_only_discovery_does_not_affect_a_passing_outcome` |
| USM-4 (declared vantage) | **Partial.** `scenario.rs`'s closed `Vantage` enum types and carries the declaration; the shipped scenario declares `builtin-help`. The "void verdict when grounded in implementation knowledge" enforcement is a judgement discipline the agent applies when recording a `judgement`-decided verdict — no code in this phase mechanically detects that violation. Named here rather than silently claimed. |
| USM-5 (observed output is the only evidence) | `transcript.rs`'s `Entry` (byte-exact capture) — `a_help_invocation_is_recorded_byte_for_byte_with_positive_duration`; `verdict --cite` cites transcript indices rather than restating output, exercised in `a_run_whose_obligations_all_pass_exits_zero` |
| USM-6 (situation development) | `scenario.rs`'s `Perturbation` + `Scenario::is_smoke()` — `zero_perturbations_still_parses_and_is_reported_as_smoke`; two classes (reversal, return) realized in the shipped scenario. **Partial**: the eleven-class catalog is documented in `simulations/README.md`, not enforced as a closed enum in code — `class` stays a free string, disclosed in `l2-simulation-suite`'s own Drawbacks (§6). |
| USM-7 (replayable; findings pinned by a deterministic test) | `replay.rs` + `tests/replays.rs` (always-on lane, `init-then-status.toml`); `cronus_sim.rs`'s `pin`/`pin_to` distils recorded entries into a checked-in case — `pin_writes_a_replay_case_that_parses_and_matches_the_recorded_exits` |
| USM-8 (coverage against the catalog; silence is a gap) | `coverage.rs` (`CoverageReport::compute`, `catalog_from_completion`) wired as `cronus-sim coverage` — 3 unit tests + `coverage_against_the_real_product_runs_clean_with_a_non_empty_complement` (106 catalog / 4 covered / 102 uncovered against the real binary) |
| USM-9 (tiers/cadence/bound; incomplete as a third outcome) | Directory placement (`simulations/short/`) = tier; `RunState::record_run` refuses past `bound.steps`/`bound.wall_secs`; `RunOutcome::Incomplete` — `a_bounded_out_run_exits_incomplete_and_names_the_undecided_obligation` |
| USM-10 (companion artifact) | **Not discharged by code in this phase.** This is the standing project-wide discipline `RULES.md` C30 encodes; nothing in `crates/simulation` mechanically checks that new user-observable behaviour ships with a scenario. Named as a process obligation, not a built mechanism. |
| USM-11 (an obligation that cannot fail is not an obligation) | `scenario.rs`'s `validate()` — 10 dedicated tests, one per refusal class (empty/duplicate obligations, no evidence, activity-shaped statement, indistinct signal, absence claim with no `positive_control`, missing `bound`, unknown key) |
| USM-12 (reports, never repairs) | `run_state.rs`'s contamination check (`repo_dirty_digest_at_build` vs. a fresh `current_repo_dirty_digest()`) — `a_repository_dirtied_during_the_run_is_reported_contaminated`; `cronus-sim` holds no code path that writes to the product's own source (only to worlds under the OS temp root, its own `run-state.json`, and `tests/replays/*.toml`); a teardown failure is attributed `Environment`/`void`, never a product finding — `a_world_that_cannot_be_torn_down_is_attributed_environment_not_a_product_finding` (Windows-only; POSIX coverage of that exact path is a disclosed gap) |

  **(c) Structural containment** — clean. `Cargo.toml`, `crates/simulation/Cargo.toml`, and the repository's one CI workflow (`.github/workflows/deps-gate.yml`, a plain `cargo build` + domain-boundary-script gate) contain no invocation of `.magic/`/`workflows/`/`skills/`/`rules/` and no dependency (`toml`/`serde`/`serde_json`) added for any reason but real parsing. `scripts/check-domain-boundary.mjs` does not reference `cronus-simulation`.

  **(d) Reference containment** — one grep hit, reviewed and confirmed non-violating: `tests/corpus.rs` lines 90/91/94/163-165/172-173 contain the literal strings `.design/`, `PLAN.md`/`TASKS.md`/`INDEX.md`/`RULES.md`, and synthetic fixtures (`"see T-1A01"`, `"Phase 20"`) — but these are the **guard's own pattern definitions and self-tests** (`contains_task_id`/`contains_phase_designator` and their `self_tests` module), not a dead reference into anything: every string is either the literal pattern being checked *for*, or opaque synthetic test data with no connection to this project's actual `.design/` tree. A checkout with `.design/` and the engine deleted still compiles and passes this file unchanged. No other file under `crates/simulation/` or `simulations/` matched (`Cargo.toml` at both the workspace root and the crate root also checked: 0 hits).

  All four sub-checks are reported individually, per the Verify criterion; none is aggregated into a single green where it was not.
