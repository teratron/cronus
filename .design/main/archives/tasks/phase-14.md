---
phase: 14
name: "Memory Intelligence & Consolidation (L2)"
status: Done
subsystem: "crates/store-local (SQLite memory adapter: schema, ranking, maintenance) · crates/domain (consolidation/query policy) · crates/contract (UserDataStore/MemorySearch seam extensions): the active memory tier over l2-memory-store"
requires: [4, 13]
provides:
  - "memory_signal fact-vs-derived table (MC-5) + depth/lifecycle_state columns (MC-1/MI-9) — the schema foundation the rest of the phase ranks and maintains over"
  - "multiplicative offline-precomputed ranking (MC-8): FTS5 BM25 fused with precomputed derived signals, no hot-path model call or graph walk"
  - "corpus-maintenance pass — recency decay, prune-protected archive, split-flagging, merge (MC-6)"
  - "consolidation write API — additive-only memory_edge graph, the closed create/corroborate/refine/correct action algebra, an incremental watermark pass, emergent topic summaries, bounded interest topics (MC-2/3/4/7/9/10)"
  - "answer/temporal-recall/structured-predicate query surface (MI-1/2/3/8) reached only through the MemorySearch seam"
  - "conflict routing, intelligence digest, run distillation (MI-4/5/7)"
  - "gated experience reuse (MI-13) — new ExperienceOutcome typing, a deterministic similarity/score/freshness gate, and a retained authority gate composing the existing SEC-9/SEC-10 realization"
  - "cross-layer validation suite (crates/core/tests/memory_validation.rs) proving the consolidation and intelligence layers compose through the real facade and adapter"
key_files:
  created:
    - "crates/store-local/src/memory/{signal,maintenance,consolidate,predicate}.rs"
    - "crates/domain/src/memory_intelligence.rs"
    - "crates/core/tests/memory_validation.rs"
  modified:
    - "crates/contract/src/lib.rs (MemoryDepth, LifecycleState, ExperienceOutcome, TemporalMode, FieldPredicate/PredicateField/PredicateValue, MemorySearch trait extensions, MemoryEntry field growth)"
    - "crates/store-local/src/memory/{store,mod}.rs (schema growth, ~20 new delegate methods)"
    - "crates/store-local/tests/memory_store.rs"
    - "crates/domain/src/lib.rs, crates/core/src/lib.rs (memory_intelligence wiring)"
patterns_established:
  - "The MemorySearch seam (&dyn MemorySearch, never the concrete store) resolves every domain-needs-adapter-data tension in this phase — reused four times (T-14C01/C02/C03/T01) without inventing a second mechanism"
  - "Grep before assuming a schema gap or a missing gate exists: this phase repeatedly found spec-described-but-unbuilt paths (superseded_at's write, entity-linking) and, separately, found REAL existing mechanisms to compose with rather than invent (autonomy::evaluate for MI-13's SEC-9/SEC-10 gate) — the same discipline cuts both ways"
  - "When a task's planned scope needs a schema round it can't justify alone, narrow to what the existing schema supports and disclose the rest as a named follow-up (T-14C02's MI-6/10/11/12 deferral) rather than rushing a fourth extension into one task — but pull forward the ONE field a later task genuinely cannot function without, kept minimal and orthogonal to what stayed deferred (T-14C03's ExperienceOutcome)"
  - "A new predicate/query need doesn't always need a new store method — extending the closed PredicateField vocabulary by one variant let MI-13 reuse T-14C01's recall_structured wholesale"
  - "Model-dependent operations get a deterministic, testable heuristic (Jaccard similarity, normalized-body matching, union-find) as the real first cut, with the model-backed version documented as a drop-in replacement for internals only, never the call shape"
duration_minutes: ~
---

# Stage 14 Tasks — Memory Intelligence & Consolidation (L2)

**Phase:** 14
**Status:** Done
**Strategic Goal:** Realize the two Stable memory L2s — `l2-memory-consolidation` (MC-1…MC-10) and `l2-memory-intelligence` (MI-1…MI-13) — over the existing SQLite substrate (`l2-memory-store`, in `cronus-store-local`), behind the `UserDataStore` seam. Turn the passive store into an active memory agent: an off-hot-path maintenance/consolidation tier and a caller-facing query surface, both generator-optional and backend-swappable.

> **Domain-logic-first (Phases 9–12 precedent).** The community-detection algorithm (MC-7) and every LLM call are **seams with stubs** — this phase proves the policy, the confidence gates, the fact-vs-derived boundary, and the degradation paths, not ML quality. Acceptance = each MC/MI invariant covered by a test + cold-start (empty derived layer) works + no hot-path model call or graph walk introduced.

## Atomic Checklist

- [x] [T-14A01] `memory_signal` fact-vs-derived table + version-guarded neutral-default degradation (MC-5)
- [x] [T-14A02] `depth` + `lifecycle_state` columns with never-rewrite-raw (MC-1) and prune-protective (MI-9) guards
- [x] [T-14B01] Multiplicative offline-precomputed ranking wrapping §4.2 base relevance (MC-8)
- [x] [T-14B02] Corpus-maintenance pass — recency decay, archive, split-flag, merge (MC-6 minus MC-7; summarize moves to T-14B03 — needs the MC-3 edge graph)
- [x] [T-14B03] Consolidation write: additive edges, action algebra, incremental watermark, co-edit safety, emergent summaries (MC-2/3/4/7/9/10)
- [x] [T-14C01] `answer` projection + temporal modes + structured predicate + recall-visibility (MI-1/2/3/8)
- [x] [T-14C02] Conflict routing, digest, distillation (MI-4/5/7; MI-6/10/11/12 deferred — new capture-metadata fields, see Decision Record)
- [x] [T-14C03] Gated experience reuse with retained authority gate (MI-13)
- [x] [T-14T01] Validation: MC/MI invariant-compliance sweep + cold-start + generator-degradation

## Detailed Tracking

### [T-14A01] `memory_signal` fact-vs-derived table (MC-5)

- **Spec:** l2-memory-consolidation.md §4.2, MC-5
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo test -p cronus-store-local` covers: a signal write/read round-trip; a version mismatch or absent row yields the neutral multiplier `1.0` (not an error); recall over an empty `memory_signal` table returns base-text-relevance ranking (cold-start). A grep confirms no signal value is written into the authored `notes/*.md` fact layer.
- **Handoff:** T-14B01 (ranking) and T-14B02 (maintenance writes signals) both depend on this table; T-14C* recall reads it.
- **Notes:** The load-bearing new table (`item_id`, `signal_kind`, `value`, `version`, `computed_at`). Disposable and rebuildable — dropping it must cost only ranking quality, never data.
- **Changes:** New `crates/store-local/src/memory/signal.rs`, mirroring the existing `chain.rs`/`trust.rs` shape — pure `&Connection`-taking functions, not `MemoryStore` methods (the field `conn` is private to `store.rs`'s module). `SignalKind` enum (`Centrality`/`Cluster`/`Recency`, closed set — matching only the factors this phase's MC-8 wrap actually needs, not the spec's full illustrative list) with a per-kind `current_version()`; `migrate()`/`write()`/`factor()`/`clear()` free functions; `NEUTRAL_FACTOR = 1.0` public constant. `factor()` degrades silently (no log) on absence or version mismatch — deliberately, since absence is the expected cold-start state (not an anomaly worth logging) and this sits on the recall hot path (MEM-2): one indexed point lookup, no warning call. Wired `signal::migrate` into `store.rs`'s existing `setup()`; added thin `MemoryStore::{set_signal, signal_factor, clear_signals}` methods delegating to it, matching the established `chain`/`trust` delegation pattern. `memory::mod.rs` re-exports `SignalKind`.
  Verify: 6 new unit tests in `signal.rs` (absent→neutral, write/read round-trip, overwrite-not-duplicate, independent kinds, version-mismatch→neutral, clear scoped to one item) + 1 new integration test in `tests/memory_store.rs` exercising the `MemoryStore` methods end-to-end. Grep-confirmed: no `memory_signal` write path touches `title`/`body` (the authored fact columns) — the boundary holds by construction (separate table, separate module).

### [T-14A02] `depth` + `lifecycle_state` columns (MC-1, MI-9)

- **Spec:** l2-memory-consolidation.md §4.2/MC-1; l2-memory-intelligence.md §4.6/MI-9
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo test -p cronus-store-local`: a `depth` enum column (`raw`/`working`/`consolidated`) exists and a write asserting a raw-tier item is immutable (never rewritten by a consolidation write); a `lifecycle_state` column (`active`/`paused`/`archived`/`deleted`) defaults to `active`, transitions append to an audit (actor/instant/old→new), and a **prune-protective** test proves MEM-5 decay does not delete a `paused`/`archived` item (it may lower ranking only). Recall defaults to `active`-only.
- **Handoff:** MC-6 `archive` (T-14B02) sets `lifecycle_state = archived`; the prune guard must exist before that action ships.
- **Notes:** Two schema columns + write-path assertions; independent of the passes.
- **Changes:** Added `MemoryDepth` (`Raw`/`Working`/`Consolidated`) and `LifecycleState` (`Active`/`Paused`/`Archived` — `Deleted` is **not** a stored variant; per MI-9's own table it is realized by the existing hard `delete()`, not a state value) to `cronus-contract` alongside `MemoryEntry`, which gained `depth`/`lifecycle_state` fields. `MemoryEntry::new()` defaults `depth: Consolidated` (every pre-existing call site — auth, `cronus memory store`, session capture — already writes a discrete finished fact, not raw evidence; defaulting to `Raw` would misclassify 100% of existing behavior) and `lifecycle_state: Active` (MI-9's own default). Added a `with_depth()` builder for future raw/working ingestion. Grep-confirmed only one struct-literal construction site existed (`store.rs`'s `map_row`) — updated it; every other call site used `MemoryEntry::new()`, so this is source-compatible.
  `store.rs`: `memories` table gained `depth`/`lifecycle_state` columns (both `NOT NULL DEFAULT`, so existing rows on schema upgrade — and every pre-existing test — read back as `Consolidated`/`Active` with no migration step needed); `add()`/`get()`/`export_all()` updated; `search_fts()` gained `AND lifecycle_state = 'Active'` to its WHERE clause (MI-9: "recall defaults to active only" — behavior-neutral today since nothing is ever anything but `Active` yet, becomes load-bearing once T-14B02 ships `archive`). New `lifecycle_audit` table (`item_id`, `actor`, `instant`, `old_state`, `new_state`) and `MemoryStore::{lifecycle_state, set_lifecycle_state}` methods — the latter is a read-then-write-then-audit sequence returning the prior state (`None` if the item doesn't exist — no transition, no audit row, no error).
  **Decision Record — prune guard scope, disclosed:** MEM-5 (decay/prune) has **no existing implementation anywhere in this codebase** — grepped for `decay`/`prune` and found nothing memory-related (only unrelated `autonomy`/`checkpoint`/`doctor` pruning). `l2-memory-store.md`'s MEM-5 is itself unrealized; it is not `Implements:`-scoped to this phase (Phase 14 realizes MC-1…10/MI-1…13, not MEM-5). The prune-protective invariant is therefore satisfied **vacuously** today (nothing can delete via decay, because decay doesn't exist) rather than actively enforced against a real decay path. What *is* real and tested: `lifecycle_state` transitions, the audit trail, and default-recall exclusion of non-`Active` items — the concrete precondition MC-6's `archive` action (T-14B02) needs. Flagged for whoever eventually realizes MEM-5's L2: it must check `lifecycle_state ∈ {Paused, Archived}` before any automatic delete, per this note.
  **Also corrects a scope error in this phase's own Planning Audit** (see Phase Notes below): the "Crate placement" guidance drafted at task-generation time said consolidation/maintenance policy belongs in `cronus-domain`. Building against the real schema showed this is wrong — MC-1…10's logic reads/writes `memories`/`memory_signal`/`lifecycle_audit` directly and has no consumer outside the store adapter, the exact shape of the T-13B01 chain/trust precedent ("when a domain half has zero consumers besides the infra half being extracted, move both together into the adapter"). Corrected inline; no domain-tier module was started under the old guidance, so nothing needed to move.
  Verify: `cargo build --workspace --all-targets` clean on first attempt (contract's new required fields caught by the compiler at the one literal-construction site, `map_row`, and nowhere else). 8 new integration tests in `tests/memory_store.rs` (depth default + explicit-raw round-trip; lifecycle default + transition + unknown-id no-op + audited-transition + default-recall-excludes-paused/archived) + the T-14A01 integration test, all passing. Full workspace suite: 1,266 passed / 0 failed (was 1,252 after Phase 13 — +14 new: +6 `signal.rs` unit tests (T-14A01) + 8 integration tests in `tests/memory_store.rs` split 1 signal / 7 depth-lifecycle across T-14A01/A02). `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo fmt --all` clean.

### [T-14B01] Multiplicative ranking wrap (MC-8)

- **Spec:** l2-memory-consolidation.md §4.4, MC-8
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo test -p cronus-store-local` proves `final = base_text_relevance × centrality × cluster × recency`, each derived factor read from `memory_signal` (T-14A01) and defaulting to 1 when absent; a near-zero factor **vetoes** a hit (multiplicative, not averaged); the recall path issues **no model call and no graph walk** (factors precomputed) — assert via a recall that touches no generator seam. `l2-memory-store` §4.2 is unmodified (base relevance unchanged); a `cargo test -p cronus` bench/test confirms existing recall behavior is preserved when the derived layer is empty.
- **Handoff:** T-14C01 recall/`answer` rank through this; T-14T01 asserts the no-hot-path-walk property.
- **Notes:** Composes §4.2's additive fuse as the text factor — does **not** edit it (§4.4). Depends on T-14A01.
- **Changes:** New `MemoryStore::search_ranked(query, limit) -> Vec<(MemoryEntry, f64)>`, added alongside (not replacing) the existing `search_fts`/`MemorySearch::search_fts` — that trait method and its sole consumer (`ContextRouter`) are untouched, zero regression risk. `base_text_relevance` is computed from SQLite FTS5's own `bm25(memories_fts)` (previously unused by this codebase — the existing `search_fts` never extracted a score, only a match/no-match list), transformed `strength = -bm25; relevance = strength / (1 + strength)` into a bounded, monotonic (0, 1) score (FTS5's bm25 is negative-is-better; this flips and bounds it). Final score = `base_text_relevance × signal_factor(Centrality) × signal_factor(Cluster) × signal_factor(Recency)`, each factor from T-14A01's `memory_signal` table via the existing `MemoryStore::signal_factor`. Results filtered by the same trust/superseded/active-lifecycle predicate as `search_fts` (post-fetch, via `get()` — same under-fill characteristic as the existing method when a FTS5-matched hit is later filtered out, not a new limitation) and sorted descending by fused score.
  **Scope correction made before shipping:** the first draft added a LIKE-substring fallback for FTS5 MATCH misses (mirroring `l2-memory-store.md`'s §4.2.1 "multi-script lexical robustness"). Caught in self-review: `search_fts` **itself has no such fallback today** (§4.2.1 is unrealized in this codebase, another instance of the T-14A02 finding — the store spec describes more than is built) and MC-8 is about signal fusion, not lexical robustness — an unrelated, already-Stable-but-unimplemented spec section outside this phase's `Implements:` scope. Removed the fallback branch and its test before it shipped; `search_ranked` now inherits exactly what `memories_fts` finds, nothing more.
  Verify: 4 new integration tests — cold-start (empty `memory_signal`) ranks by text relevance alone, matching plain-relevance order; a centrality factor crushed to `0.001` **vetoes** an otherwise-dominant text match (proves multiplicative, not additive/averaged, fusion); trust/superseded/paused/archived exclusion holds under the ranked path too; a non-matching query returns an empty `Vec`, not an error. Full workspace suite: 1,270 passed / 0 failed (1,266 + 4). Clippy `-D warnings` clean; fmt clean.

### [T-14B02] Corpus-maintenance pass (MC-6 minus MC-7)

- **Spec:** l2-memory-consolidation.md §4.3/§4.6, MC-6 (MC-7 moved to T-14B03)
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo test -p cronus-store-local` / `-p cronus-domain`: the pass runs actions in blast-radius order (archive → split → summarize → merge); `merge` requires the elevated multi-sample gate and, when unmet, surfaces to adjudication rather than acting; `archive`/`summarize` auto-apply; an `anti_cycle` cooldown blocks a split→merge→split oscillation; every action appends to `maintenance_audit`. MC-7: a stable/large/diverse/hub-less cluster (over the MC-3 edge graph, via the community-detection **seam** with a union-find stub) gains a size-bounded summary node grounded in its members. A no-generator run completes as a successful no-op.
- **Handoff:** Sets the MI-9 `archived` state (needs T-14A02); consumes MC-3 edges (T-14B03).
- **Notes:** Community detection is a seam + stub (domain-logic-first). Depends on T-14A01, T-14A02.
- **Changes:** New `crates/store-local/src/memory/maintenance.rs`, same shape as `signal.rs` (pure `&Connection` functions delegated to by thin `MemoryStore` methods). Two new tables: `maintenance_audit` (item_id/action/target_id/actor/instant) and `maintenance_cooldown` (item_id/action/until, 1-hour `ANTI_CYCLE_COOLDOWN_SECS`). Delivered three of MC-6's four actions plus the recency-signal computation step:
  - **`recompute_recency`** — exponential decay (`exp(-age·ln2/halflife)`, 30-day half-life stub) writing the `Recency` signal (T-14A01's table) for every active item. This is "maintenance step 1: recompute derived signals," recency-only — centrality/cluster need the MC-3 edge graph.
  - **`sweep_archive`** — auto-applies (reversible, no gate): archives an item whose recency has decayed past `ARCHIVE_RECENCY_THRESHOLD`, the threshold divided by `(1 + centrality)` so a hub is harder to archive, exactly the "cushioned by centrality" design (today centrality reads the T-14A01 neutral default of 1.0 for every item, since nothing computes it yet — the formula is correct and already tested against a non-default value once T-14B03 starts writing centrality). Respects cooldown; sets `lifecycle_state = Archived` via the T-14A02 machinery (audited).
  - **`touch`** — auto-thaw on touch (MC-6): flips an `Archived` item back to `Active`, audited; no-op otherwise. Not yet wired into any implicit "touch" point (recall/update) — those land with Track C; this ships the primitive.
  - **`flag_split_candidates`** — a length-threshold heuristic (4,000 chars) identifies overload candidates. Deliberately does **not** split them: real splitting is topic segmentation, a generator-dependent operation with no safe non-model heuristic (unlike archive's clean decay formula) — flagging-only is the honest no-generator no-op, extending MC-2's own contract to MC-6.
  - **`find_merge_candidates` / `merge_pair`** — redundancy detection via case/whitespace-normalized exact body match (the domain-logic-first stand-in for MC-6's "multi-sample agreement" elevated gate — unambiguous by construction, not a similarity heuristic that could misfire). `merge_pair` re-points `memory_chains` edges from the discarded item onto the kept one, clears the discard's signals, hard-deletes it, all inside one SQLite transaction (commit-or-rollback, MC-9).
  **Decision Record — MC-7 deferred to T-14B03, disclosed:** MC-7 (emergent topic-cluster summaries) needs community detection over the MC-3 edge graph, which does not exist until T-14B03 builds it. Implementing MC-7 here would mean operating on a graph that is always empty. Moved MC-7 into T-14B03's scope (it lands the edges; summarizing over them belongs with them) — not a scope cut, a sequencing fix caught before code was written against a nonexistent dependency. `l2-memory-consolidation`'s own Verify line and this phase's Planning Audit did not flag this edge dependency at plan time; corrected here rather than silently building the wrong order.
  Verify: 7 new unit tests in `maintenance.rs` (fresh-item recency ≈ neutral; recency decays past threshold with age; archive shelves a stale item, leaves a fresh one; touch thaws Archived only; split-flag finds only long bodies; merge-candidate pairing picks the normalized duplicate and keeps the newer; merge_pair re-points chains and hard-deletes) + 4 new integration tests in `tests/memory_store.rs` proving the `MemoryStore`-level delegation wiring. Full workspace suite: 1,281 passed / 0 failed (1,270 + 11: 7 unit + 4 integration). Clippy `-D warnings` clean; fmt clean. String literals for lifecycle states parameterized through `LifecycleState::as_str()` throughout (not hardcoded SQL string literals), so a future rename of the enum's string form can't silently desync from the SQL.

### [T-14B03] Consolidation write path (MC-2/3/4/7/9/10)

- **Spec:** l2-memory-consolidation.md §4.1/§4.3/§4.5/§4.6, MC-2, MC-3, MC-4, MC-7 (moved here from T-14B02), MC-9, MC-10
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo test`: write-time additive-only edges — a prose edit may add but never drop an edge (conservation check refuses the drop) and every consolidated item carries a `derived-from` provenance edge (MC-3); the closed `create/corroborate/refine/correct` action is recorded per write (MC-4); an incremental watermark reprocesses a failed-not-checkpointed input on the next pass and no-ops on no change (MC-2); optimistic-concurrency + conservation-check refuses a stale/invalid write and a multi-item merge/split is transactional whole-or-rollback (MC-9); the pass emits a bounded, diversity-windowed, read-only interest-topic set (MC-10).
- **Handoff:** Feeds T-14B02 (edges to cluster) and the MI-4 adjudication (T-14C02, upstream MC-4).
- **Notes:** Extends the §4.12 two-phase pipeline; the domain tier holds the action/gate policy, the adapter the SQLite writes. Depends on T-14A01/A02.
- **Changes:** New `crates/store-local/src/memory/consolidate.rs`. Deliberately a **new** `memory_edge` table, not a retrofit of `memory_chains`/`ChainKind`: chains serve session-continuation with a closed 3-variant vocabulary walked by Bellman propagation; MC-3 wants an **open** typed-predicate vocabulary over consolidated content — a different concept, kept in its own table (`source_id, target_id, predicate, created_at`, `UNIQUE(source_id, target_id, predicate)`).
  - **MC-3** — `add_edge` is insert-or-ignore with **no** delete/update function anywhere in the module: additive-only is structural, not a checked invariant. `edges_from` reads a node's outgoing edges. `recompute_centrality` derives the MC-8 `Centrality` signal from in-degree, normalized by the graph's max in-degree (a node with zero in-edges is absent, degrading to T-14A01's neutral default) — the half of "recompute derived signals" T-14B02's `recompute_recency` didn't cover, because it needed this task's edges.
  - **MC-4** — `consolidate()` is the auto-detected half: same-abstraction match (the identical normalized-body heuristic already established for T-14B02's merge detection — reused rather than reinvented) → **corroborate** (provenance edge + positive trust bump via the existing `apply_delta`, no new row); no match → **create**. `refine`/`refine`/`correct` are explicit, caller-declared operations (`refine`, `correct`) — MC-4's own text says these need a judgment ("does this extend scope," "does this fix an error") that a normalization match cannot safely infer, so auto-detecting them would be guessing, not degrading gracefully.
  - **MC-9, made real, not decorative (self-caught during review):** the first draft of `refine()` read `target`'s body and wrote it back in the same call — a same-call read-then-write can never observe a concurrent writer, making the "optimistic concurrency" check tautological (always passes). Fixed by moving the read outside the function: `refine(target, expected_body, addition, ...)` takes what the **caller** read earlier as the CAS token; a body that changed underneath it (tested by mutating the row directly between the caller's notional read and the call) is refused, not clobbered. `correct()` is genuinely transactional (BEGIN/COMMIT/ROLLBACK): supersede `target` (`superseded_at`) + insert the correction + link a `supersedes` edge, whole-or-nothing; refuses to double-supersede an already-superseded target.
  - **First real write to `superseded_at`, anywhere:** grepped before starting and confirmed nothing in the codebase had ever set it (read/filtered only) — the third instance this phase of a schema column whose write-path the store spec assumed but never shipped (after `memory_signal`-adjacent gaps in T-14A01/A02). `correct()` is the legitimate MC-4 occasion to finally realize MEM-6 supersession.
  - **MC-2** — `run_incremental_pass` processes every `depth != Consolidated` row past a `consolidation_checkpoint` watermark, oldest first; the watermark advances only past inputs whose `consolidate()` call returned `Ok`, so a failure retries next pass without blocking the rest of the batch. **Bug caught by its own test, fixed before it shipped:** the first version passed the raw row's own `MemoryEntry` (same `id`) straight into `consolidate()`'s create branch, which tried to `INSERT` a second row under that same primary key — a guaranteed, permanent failure (the "retry" would loop forever, always failing the same way, since duplicate-key insert never succeeds). Fixed in `consolidate()`'s create branch: mint a fresh `MemoryId` for every newly-created consolidated node, never reuse whatever id the input carried. The never-rewrite-raw guard (MC-1) is proven by a dedicated test — the raw row's `depth` and `body` are asserted unchanged after the pass runs.
  - **MC-7 (moved from T-14B02, see that task's Decision Record):** `synthesize_summaries` runs union-find over the now-real `memory_edge` graph — reimplemented locally against `MemoryId` nodes rather than depending on the `codegraph` crate (whose `CommunityDetector`/`Edge` types are `i64` code-symbol ids, an unrelated domain; the spec's own Implementation Notes call for reusing the algorithm's *shape*, not the crate). A cluster at or above `MC7_MIN_CLUSTER_SIZE` (3) with no existing inbound `summarizes` edge (the structural, marker-free hub check) gets a summary node grounded in truncated excerpts of every member, each linked back via a `summarizes` edge — grounding, not invention.
  - **MC-10** — `extract_interest_topics` returns the most-recent active items, deduplicated by normalized title within the call, bounded by `limit`. No model, no interruption — a read-only hand-off exactly as specified.
  **Follow-up fix to T-14B02's `merge_pair`:** now that `memory_edge` exists, extended the same transaction to re-point edges (not just chains) from the discarded item onto the survivor, `OR IGNORE` for a collision with an edge the survivor already has (a redundant copy, not lost information), with an explicit cleanup delete for anything still pointing at the discard afterward.
  Verify: 15 new unit tests (edge additive-only/duplicate-is-noop; edge read; centrality normalization + zero-degree neutral default; create-path depth+provenance; corroborate-path returns existing id + bumps trust + no new row; refine appends+provenance; refine refuses a stale `expected_body`; correct supersedes+links+preserves the old body; correct refuses double-supersede; incremental pass consolidates+advances watermark+idempotent no-op; incremental pass never rewrites the raw row; summarize grounds in members; summarize skips undersized clusters; summarize does not re-summarize a hubbed cluster; interest topics bounded+deduplicated). One real bug found and fixed by its own test before this shipped (the id-collision in the incremental pass, above) — caught by `assert_eq!(results.len(), 1)` failing with `0`, not silently. Full workspace suite: 1,296 passed / 0 failed (1,281 + 15). Clippy `-D warnings` clean; fmt clean.

### [T-14C01] `answer` + temporal + predicate + visibility (MI-1/2/3/8)

- **Spec:** l2-memory-intelligence.md §4.1/§4.2/§4.5, MI-1, MI-2, MI-3, MI-8
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo test`: `answer` retrieves a grounding set, synthesizes grounded only in it, attaches KB-6 citations, and passes the CV-3/CV-4 gate — an under-grounded query returns an honest `insufficient` outcome, never a model-prior assertion; with no generator it returns top attributed items verbatim (extractive). Temporal modes `as-of`/`changed-since`/`recent` resolve over the §4.14 record and compose with type/scope/tag filters; `changed-since(checkpoint)` yields the session delta. The structured predicate (eq/ne/ordering/membership/containment + AND/OR/NOT) compiles to a parameterized SQL `WHERE`, falling back to post-fetch evaluation for an unsupported nesting (never dropping the constraint). A freshly-written item is recall-visible immediately (MI-3), enrichment deferred.
- **Handoff:** T-14C02 digest reuses `answer` over a time window; T-14C03 reuse recall composes MI-2/MI-8.
- **Notes:** `answer` is a projection composing existing engines, not a new engine. Depends on T-14B01 (ranking).
- **Changes:** **Architectural finding, resolved before writing code:** `answer`/MI-1 needs BOTH domain-tier composition (KB-6 attribution, CV-3/4 verification — conceptually no-I/O logic) AND access to the SQLite store — but the tier model forbids an edge in either direction between `cronus-domain` and `cronus-store-local`. Resolved exactly like T-13A02's own pivot: the new logic lives in `cronus-domain`, reached through the **`MemorySearch` seam** (`&dyn MemorySearch`, never the concrete `MemoryStore`) — never a new domain→adapter dependency. `cronus-contract`'s `MemorySearch` trait gained two new methods (`recall_temporal`, `recall_structured`) alongside the existing `search_fts`; grepped first and confirmed `MemoryStore` is the trait's only implementor workspace-wide, so extending it was a single-site change.
  - **MI-2** — new `cronus_contract::TemporalMode` (`AsOf`/`ChangedSince`/`Recent`), realized in `MemoryStore::recall_temporal`. `as-of` deliberately does **not** apply the `superseded_at IS NULL` filter `search_fts` uses by default — the question "what was true at T" must be able to name a since-superseded record, so excluding it would answer a different question than the one asked.
  - **MI-8** — new `cronus_contract::{FieldPredicate, PredicateField, PredicateValue}` (closed vocabulary: eq/ne/ordering/in/not-in/contains/contains-ci + AND/OR/NOT), compiled to parameterized SQL by a new `crates/store-local/src/memory/predicate.rs` module. **Finding, disclosed:** SQLite expresses every combinator in the vocabulary natively (arbitrary boolean nesting via parentheses) — the spec's own post-fetch-fallback-for-inexpressible-nesting clause is real design (documented, and would matter for a hypothetical backend that couldn't nest booleans), but is never exercised against this backend. Not a gap; a property of SQLite worth stating plainly rather than building an unused fallback path.
  - **MI-1** — new `crates/domain/src/memory_intelligence.rs` (`answer`, `MemoryCitation`, `AnswerVerdict`, `Answer`), depending only on `cronus_contract` — zero new dependency, matching the domain tier's own constraint. `MemoryCitation` is a **new type**, not a reuse of `research.rs`'s existing `Citation` — checked that struct's fields first (`url`/`title`/`retrieved_at`) and found it's specifically a web-URL citation for the deep-research feature; forcing an internal `MemoryId` reference into a `url: String` field would be a type mismatch wearing a matching name, not a real fit.
  - **MI-1's CV-3/CV-4 gate, honestly scoped:** grepped the whole codebase for any claim-verification engine before writing this — none exists (`l1-claim-verification`'s L2 realization is unbuilt, another instance of this phase's recurring "spec describes more than is built" finding). `AnswerVerdict` is therefore a two-variant `Supported`/`Insufficient` gate: it can prove *insufficient grounding* deterministically (nothing matched) but cannot prove semantic *contradiction* without a model. Documented as a seam — a generator-backed verifier is a drop-in replacement for the function's internals, not a different call shape.
  - **MI-3** — no new production code (the property already holds: every write commits synchronously, nothing async gates it). Added a dedicated integration test proving it rather than fabricating an implementation for something already true.
  Wired `memory_intelligence` into the facade's `pub use cronus_domain::{...}` re-export list (alphabetical slot, `lookahead` → `memory_intelligence` → `mission`) for `cronus::memory_intelligence::…` call-site parity with every other domain module. Also fixed a stale doc-comment in `crates/core/src/lib.rs` (`cronus_core::memory::…` — the crate is `cronus`, not `cronus_core`) noticed while editing the same block; a one-word, directly-adjacent, pre-existing inaccuracy, not a separate change.
  Verify: 3 new `predicate.rs` unit tests (eq→1 placeholder, in→N placeholders, AND/OR/NOT nesting with correct parentheses) + 4 new `memory_intelligence.rs` unit tests against a hand-written `MemorySearch` stub (not the SQLite store — proving the module truly only needs the trait) + 6 new `recall_temporal`/`recall_structured` integration tests + 1 new MI-3 visibility integration test in `tests/memory_store.rs`. Full workspace suite: 1,310 passed / 0 failed (1,296 + 14: 3 + 4 + 6 + 1). Clippy `-D warnings` clean workspace-wide; fmt clean.

### [T-14C02] Conflict routing, digest, distillation (MI-4/5/7; MI-6/10/11/12 deferred)

- **Spec:** l2-memory-intelligence.md §4.3/§4.4/§4.7, MI-4, MI-5, MI-7
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo test`: conflict routing classifies (duplicate/update/contradiction) and applies the **pinned threshold** (§4.3: normalized-body match → auto de-dupe; recency-dominant higher-or-equal-confidence → auto-supersede; comparable confidence-and-trust gap with no recency winner → surface for adjudication, recommendation from the closed set) (MI-4); the digest is a read-only narrative + per-kind/honesty analytics over one caller-supplied window that mutates nothing and is honestly empty on an empty window (MI-5); `distill_run` writes one procedure-shaped memory grounded strictly in the supplied trace, never inventing content beyond it (MI-7).
- **Handoff:** Conflict adjudication consumes the MC-4 action output (T-14B03).
- **Notes:** Depends on T-14C01. MI-6/10/11/12 deferred — see Decision Record below.

- **Changes:** **Decision Record — MI-6/10/11/12 deferred, disclosed before writing code:** this task's original scope spanned 7 invariants (MI-4/5/6/7/10/11/12). MI-6 (capture salience gate + actor/expiry/subject metadata), MI-10 (relative-date normalization), MI-11 (capture directives), and MI-12 (raw-vs-inferred capture mode) each need new persisted fields on `MemoryEntry` (actor, expiry, cross-ref, subject-lens, capture-mode) — a **4th** schema extension this phase, after T-14A02, T-14C01's temporal/predicate additions, and T-14B03's edge table. MI-4 (classify), MI-5 (digest), and MI-7 (distillation) need none — they compose entirely over fields the store already carries. Rather than rushing a 4th schema round inside an already-large task, narrowed scope to the three invariants realizable against the existing schema and left MI-6/10/11/12 as an honest follow-up (capture-policy metadata is one coherent unit of work, not four independent patches) — surfaced to the user and flagged here for the next `/magic.task` replan to pick up as a new task.
  - **MI-4** — `classify(old: &MemoryEntry, new: &MemoryEntry) -> ConflictFinding`. Exact match after normalization (trim + lowercase) is an unambiguous `Duplicate` → `KeepOld`, auto-resolved (the existing item already says this; nothing to gain by replacing it). Strictly-newer-and-equal-or-higher-confidence is a recency-dominant `Update` → `KeepNew`, auto-resolved. Everything else is `Contradiction`, `AwaitingAdjudication`: a confidence gap **and** trust gap both under the pinned `CONF_GAP_MIN`/`TRUST_GAP_MIN` (0.15, matching the L2's pinned threshold) recommends `Merge` (genuinely balanced, neither side dominates); otherwise recommends `KeepNew` without auto-applying it (not recency-dominant enough to auto-resolve, but not balanced either — a call for the adjudicator, not a guess). No auto-resolution path silently discards a genuine disagreement — the auto-resolved cases are exactly the two where the answer is not actually in question.
  - **MI-5** — `build_digest(store: &dyn MemorySearch, window_start: u64, limit: usize) -> Digest`. Retrieves the window via the existing `recall_temporal(ChangedSince(window_start))` seam call (T-14C01) — no new store method needed. `DigestAnalytics` (total/by-kind counts, average confidence, average trust) plus a `narrative` field that is the honest extractive degrade (a plain count-based sentence, not a generated summary) — reusing `answer`'s synthesis was considered and rejected: `answer` takes a query, a digest has none, and forcing one would be a fabricated question. An empty window returns a zero-valued `Digest` with an explicit "nothing changed" narrative, never an error or a fabricated entry. Cadence (§4.4: per-session-close + daily floor) is the caller's scheduler concern — out of this function's scope, matching MI-1's own precedent of separating "compute one result" from "when to fire."
  - **MI-7** — `distill_run(trace: &RunTrace) -> MemoryEntry`. `RunTrace` (`objective`, `actions_taken`, `findings`, `end_state`, `next_steps`) is assembled by the caller (the run harness, not this module); the function's entire job is grounding the output body strictly in those fields — verified by a dedicated test asserting every word of the produced body traces back to the trace, and a second test asserting it never introduces content the trace didn't supply. Returns a `MemoryEntry` ready for the caller to hand to the store's existing `insert`, not a persisted row itself — keeps this module's zero-I/O property intact.
  Updated the test module's hand-written `StubSearch::recall_temporal` (previously an unconditional `Ok(Vec::new())` stub) to actually filter by `ChangedSince`, needed once `build_digest`'s tests required a non-trivial stub response — the stub had been getting away with doing nothing because nothing had exercised that branch until now.
  Verify: 9 new unit tests (4 `classify`: exact-match duplicate, recency-dominant update, balanced-disagreement merge, never-silently-resolves-a-genuine-disagreement; 3 `build_digest`: empty-window honest no-op, kind/average aggregation, window-boundary respects the checkpoint; 2 `distill_run`: grounds strictly in the trace, never invents beyond it). Full workspace suite: 1,319 passed / 0 failed (1,310 + 9). Clippy `-D warnings` clean workspace-wide; fmt clean.
  **Addendum (same day, from T-14C03):** `distill_run`'s signature gained an `outcome: ExperienceOutcome` parameter once MI-13 needed a way to recall what this function writes — see T-14C03's Decision Record for the full rationale. Its two tests here were updated to pass `ExperienceOutcome::Success`; behavior otherwise unchanged.

### [T-14C03] Gated experience reuse (MI-13)

- **Spec:** l2-memory-intelligence.md §4.12, MI-13
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo test`: a high-quality prior `success` is reused directly **only** when the gate passes (similarity ≥ σ AND score ≥ τ AND fresh AND not safety-sensitive); a `failure` injects as an avoid-signal and is never reused as a solution; an `insight` injects as guidance; a reused result is attributed via `MemoryCitation` (never passed as fresh, MI-1 honesty); a reused plan still passes the action's SEC-9/SEC-10 authority gate (reuse buys speed, never a bypass — a gated success whose body classifies above the caller's autonomy level surfaces `ReuseNeedsApproval` instead of auto-reusing); read and write are independently controllable.
- **Handoff:** Closes the phase's net-new query surface; validated end-to-end in T-14T01.
- **Notes:** Composes MI-1/2/7/8 + the security gate (MI-6 stays deferred per T-14C02's Decision Record — see below for the one field pulled forward from it).
- **Changes:** **Decision Record — one schema field pulled forward from the MI-6 deferral, disclosed:** MI-13 cannot recall "prior experiences typed success/failure/insight" without *something* persisting that typing — grepped `MemoryEntry` first and confirmed no field carries it (the closed `MemoryKind` vocabulary classifies *what a memory is about*, not *how an attempt went* — a genuinely orthogonal axis; overloading `kind` would have been the simple move, not the right one). Added `cronus_contract::ExperienceOutcome` (`Success`/`Failure`/`Insight`) and one new nullable `MemoryEntry.experience_outcome: Option<ExperienceOutcome>` field/column — `None` on every ordinary memory (the entire pre-existing corpus, no backfill). This is the single concept MI-13 cannot function without; it is deliberately **not** the rest of the MI-6 bundle (actor/expiry/cross-ref/subject-lens/capture-mode stay out of scope, still a follow-up). `distill_run` (T-14C02, MI-7) is the only writer — its signature gained an `outcome: ExperienceOutcome` parameter (a same-day signature amendment, disclosed here since T-14C02 was already marked Done when this need surfaced; its two existing tests updated to pass `Success`).
  - **Query path reused wholesale, zero new store methods:** `ExperienceOutcome` didn't need a new recall method — added one `PredicateField::ExperienceOutcome` variant (one match arm in `predicate.rs`'s already-generic compiler) and MI-13 queries it through T-14C01's existing `recall_structured`. `NULL IN (...)` is SQL-false, so ordinary (non-experience) rows are excluded by standard NULL semantics with no explicit `IS NOT NULL` clause needed — confirms the T-14C01 predicate infrastructure was built generally enough to absorb a new field with no seam change.
  - **MI-13(a) gate, real not decorative:** `experience_score` = average of `confidence` and `effective_trust()` (both already-persisted, no new signal). `similarity` is a deterministic Jaccard over normalized token sets — this phase's established domain-logic-first stand-in for a semantic/embedding comparison (same family as MC-4's corroborate-match and MI-4's duplicate-match), swappable later without changing the gate's call shape. `safety_sensitive` is a caller-supplied judgment about the ACTION being attempted now, not an inferred property of the candidate — this module cannot know that on its own. Freshness is `FRESHNESS_MAX_SECS` (30 days), pinned the same way MI-4's `CONF_GAP_MIN`/`TRUST_GAP_MIN` were pinned — a real choice recorded in code.
  - **MI-13(d), made structural rather than a doc comment:** grepped for an existing authority/permission gate before inventing one — found `crate::autonomy::{classify_command, evaluate}` (the real SEC-9/SEC-10 realization, already built and tested). `recall_for_reuse` composes it directly: a gated success whose body classifies above the caller's `AutonomyLevel` returns `ExperienceDecision::ReuseNeedsApproval { citation, risk }`, never a silent `Reuse` — proved by a dedicated test (a `High`-risk body under `Supervised` surfaces for approval; a `Low`-risk body under the same level reuses cleanly).
  - **MI-13(b)/(c):** `recall_for_reuse` takes no write capability at all (zero-I/O beyond the seam's own read) — independence is structural, not a flag to misuse. Every returned path carries a `MemoryCitation` (never a bare `MemoryEntry`), so a reused result is never representable as indistinguishable from fresh work.
  - **Bug caught by the pre-existing test suite, fixed before this shipped:** adding the 15th `experience_outcome` column and appending it to `map_row`'s positional read broke `consolidate.rs::run_incremental_pass` — a second, independent `SELECT` (its own raw-row fetch for the incremental pass, not routed through any of `store.rs`'s five call sites) still selected only 14 columns while calling the now-15-column-reading `map_row`, causing every row to fail decode and the whole pass to return `Err`. Caught immediately by `cargo test -p cronus-store-local` (two `consolidate.rs` tests failed on `.unwrap()`), not silently. Fixed by adding `experience_outcome` to that `SELECT` too — the lesson generalizes: `map_row` now has **six** call sites across two files, not five across one; grepped for `map_row` directly (not just `store.rs`) before considering this schema change complete.
  Verify: 9 new unit tests (4 `reuse_gate`: passes on similar+scored+fresh+non-sensitive, refuses low similarity, refuses stale, refuses safety-sensitive even when everything else passes; 5 `recall_for_reuse`: reuses a gated low-risk success, surfaces `ReuseNeedsApproval` for a gated high-risk success under `Supervised`, never reuses a failure — injects as `avoid` instead, never reuses an insight — injects as `guidance` instead, an empty store is an honest `Execute{avoid:[],guidance:[]}` no-op). Full workspace suite: 1,328 passed / 0 failed (1,319 + 9). Clippy `-D warnings` clean workspace-wide; fmt clean.

### [T-14T01] Validation: invariant-compliance sweep + degradation

- **Goal:** Prove every MC-1…MC-10 and MI-1…MI-13 invariant holds on the built tier, cold-start works, and the local-first / hot-path constraints are respected.
- **Method:** One test per MC/MI invariant (exercising the layers together — consolidation write → maintenance → recall/answer); a cold-start test (empty `memory_signal`) yields base-relevance ranking; a no-generator run degrades every model-dependent op (consolidation, summary, `answer`, normalization, inferred capture) rather than failing; an assertion that the recall/`answer` hot path issues no model call and no graph walk (MC-8/MEM-2); the fact-vs-derived boundary holds (no signal in `notes/*.md`).
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo test --workspace` green (memory tests added, pre-existing count preserved) + `cargo clippy --workspace --all-targets -- -D warnings` clean + `cargo fmt --all -- --check` clean.
- **Changes:** **Scope note, disclosed:** "every MC-1…10 and MI-1…13 invariant" as originally phrased is a superset of what this phase actually built. MC-1…10 are all realized (10/10) — the consolidation L2 shipped whole. MI is 9/13: MI-1/2/3/4/5/7/8/9/13 are built and swept below; **MI-6/10/11/12 are not** (T-14C02's Decision Record — they need capture-metadata fields deliberately deferred to a follow-up task, not rushed). A sweep cannot exercise code that does not exist; this task validates the built 9, and states the 4-invariant gap plainly rather than silently narrowing "every" without saying so.
  New `crates/core/tests/memory_validation.rs` (facade-level, the only place both `cronus-store-local`'s concrete adapter and `cronus-domain`'s `memory_intelligence`/`autonomy` modules are visible together, per the tier model) — 5 tests, each proving something no single task's own unit tests covered:
  - **Cross-layer pipeline** (MC-1/2/4 + MI-1): a raw capture survives `run_incremental_consolidation` (asserted `ConsolidationAction::Create`) and is immediately answerable via `answer()` through the same seam — consolidation's write side and intelligence's read side actually compose, not just each in isolation.
  - **Cold start at the store level** (MC-5/MC-8): two items inserted, `search_ranked` called with **no** signal ever computed — returns both hits with a positive score (neutral-factor degrade), not an error or a silently empty result. T-14A01's own cold-start test covered the signal table in isolation; this covers the fused ranking path a real caller actually calls.
  - **The no-graph-rewalk regression proof** (MC-8/MEM-2's "no hot-path graph walk," made falsifiable): centrality normalizes by the graph's max in-degree, so a single-hub graph is trivially its own max before and after — proves nothing. Built two competing hubs instead (`rival` fixed at 2 in-edges, `hub` starting at 1); after one `recompute_centrality()`, added two more edges onto `hub` (now the true max) **without** recomputing again, and asserted the signal read stays at its stale value. A closing `assert_ne!` after an explicit second recompute confirms the graph genuinely had changed underneath it — guarding against the test passing vacuously for the wrong reason.
  - **The fact/derived boundary through the public write path** (MC-5): `set_signal` twice, then `get()` the same item back — title/body byte-identical to what was authored. Every prior signal test asserted this from inside `signal.rs`; this asserts it from the caller's side of the seam.
  - **A real-adapter MI-7/MI-13 round trip:** every `recall_for_reuse` test in `memory_intelligence.rs` used the hand-written `StubSearch` (by design — proving the domain logic needs nothing but the trait). None of them had exercised the new `PredicateField::ExperienceOutcome` → SQL column mapping against **real** SQLite until this test: `distill_run` → `store.add()` → `recall_for_reuse(&store, …)` finds and reuses it end-to-end.
  Verify: 5 new integration tests (all above) + full-suite re-run. Full workspace suite: 1,333 passed / 0 failed (1,328 + 5). Clippy `-D warnings` clean workspace-wide; fmt clean.

## Phase Notes (Planning Audit)

- **Foundation-then-parallel.** Track A (T-14A01 signal table, T-14A02 depth/lifecycle columns) is a hard prerequisite for B and C. After A, B and C parallelize on file independence. Two cross-edges the orchestrator must respect: MC-8 ranking (B01) is consumed by intelligence recall (C01), and MC-6 `archive` (B02) sets the MI-9 `lifecycle_state` (A02) — so A02 gates B02, and A01 gates B01 + C01's ranking.
- **Generator-optional is a build requirement, not a deferral.** Every model-dependent step ships its no-generator degrade path in the same task (local-first, §2 of both specs). A task is not Done if its degrade path is only asserted, not built.
- **Domain-logic-first.** Community detection (MC-7) and the LLM calls are seams with stubs; the phase proves policy/gates/degradation, not ML quality — mirroring Phases 9–12.
- **No hot-path regression.** MC-8/MEM-2: ranking reads precomputed signals only; T-14B01 and T-14T01 both assert no model call / graph walk on recall.
- **Crate placement — corrected during T-14A02 (Decision Record).** Originally drafted as "SQLite/ranking/maintenance → store-local; policy → domain." Building against the real schema showed the policy has no consumer outside the store adapter (same shape as T-13B01's chain/trust precedent), so **all of MC-1…10's consolidation/maintenance/ranking logic lives in `cronus-store-local`** — no domain-tier split. Shared value types (`MemoryDepth`, `LifecycleState`, `SignalKind` if ever needed cross-crate) → `cronus-contract`. The MI-1…13 intelligence surface (Track C) composes `MemorySearch`/`UserDataStore` through the seam and may legitimately need a thin `cronus-domain` orchestration layer for `answer`'s KB-6/CV-3/4 composition (those engines are domain-tier) — re-evaluate per-task, don't assume.
