---
phase: 27
name: "Invocable Registry & CLI Derivation"
status: Todo
subsystem: "crates/contract · crates/domain · crates/core · crates/cli"
requires: []
provides: []
key_files:
  created: []
  modified: []
patterns_established: []
duration_minutes: ~
---

# Stage 27 Tasks — Invocable Registry & CLI Derivation

**Phase:** 27
**Status:** Todo
**Strategic Goal:** Mint one runtime invocable registry in the core, dispatch every action through it once, and move the command line onto it as a **projection** — so command parity stops being a property that is asserted and becomes one that is structural, and a verb contributed by an extension becomes reachable at all.

## Phase Notes

**Behaviour-preservation is the acceptance property, not a nice-to-have.** Every task in tracks A–D must leave observable command output unchanged. Where a task uncovers behaviour that is wrong, it is recorded as a **residual at the owning invocable** and left working as-is; the corrections ship in Phase 29 as separate disclosed changes (SP-10). A diff that both unifies and corrects presents a regression to users as a refactor to reviewers, and neither audience can read it.

**Four residuals are already known and must be preserved through this phase:** a store failure reported as an empty listing with a success exit code; the output-format flag discarded in nine sites; structured output assembled by unescaped string formatting in five; and a redaction path constructed with an empty secret list on every surface. Preserve, record, do not fix here.

**Build environment.** Anything that triggers C compilation (`rusqlite` `bundled`) or a Tauri/`windres` step must run in **PowerShell**, not Git Bash — MSYS2 makes `cc1.exe` fail to load there, so a clean check from Bash that suddenly fails at a C step is an environment artifact, not a code defect.

**The specification moved after tracks A and B shipped — read this before opening any remaining task.** T-27A01…T-27B04 were built against `l2-invocable-registry` 1.0.0 and are **correct at that scope**; their `[x]` stands and is not reopened. The spec is now 1.1.0, and the delta is scheduled as three explicit retrofit tasks (T-27A04, T-27B05, T-27B06) rather than folded silently into C or D. What changed, concretely: an `Installation` locus exists; identity construction is fallible; a descriptor is bounds-checked and detached at registration; `register` returns the effect that reverses it; the bare-form collision rule is **shadowing, not displacement**, and is reported; an unknown invocable is a **resolution** result rather than an `Outcome`; the registry announces its own mutations; a resolved dispatch is journaled as a pair.

**Track independence.** A → B → **retrofit (A04, B05, B06)** → (C ∥ D) → T, with **one crossing edge**: T-27D03 registers the command line against the corpus and therefore needs T-27C01. So C and D are file-independent and may run in parallel once the retrofit lands, but D cannot *finish* before C01 does. Treat C01 as the earliest task in track C rather than assuming the tracks are fully independent.

**The retrofit is on the critical path and is not optional sequencing.** The corpus asserts on the resolution/outcome split, and the generated parser needs the `Installation` locus to know which half of the verb set it owns. Opening C or D against the 1.0.0 shape means writing code twice.

**The retrofit reaches backwards into files tracks A and B already delivered — this is a known crossing edge, not a surprise.** Three concrete consequences, each already identified so no executor has to rediscover it:

1. **`InvocableId`'s `From<&str>`/`From<String>` impls cannot survive.** A `From` conversion cannot fail, and T-27A04 makes identity construction fallible. They give way to a fallible constructor (`TryFrom`, or a named function returning a result). Every current call site — the facade bootstrap, the dispatcher, and both integration tests — changes.
2. **`register`'s signature changes**, so the two bootstrap call sites and the registration assertions in the facade integration test change with it.
3. **`Dispatcher::dispatch`'s return changes** when resolution splits out, which the facade integration test asserts on directly.

None of these are behaviour changes and none of them touch a *product* behaviour a user observes — but they do mean the retrofit's diff includes edits to tests written by tracks A and B. Say so in the task's `Changes` note rather than letting a reviewer read a modified test as a weakened one.

**Phase-size tripwire (planner audit) — holding at 22, T-27D01.2.2 closed without a further split.** This phase started at 14, grew to 18 for the retrofit, 19 with T-27D01.2 opening, 20 with T-27D01.1.2, 21 with T-27D01.1.2.2, and reached **22** with T-27D01.2.2 (the six semantic groups needing a `Binder`/`ArgValue` extension `NamedText` alone did not cover). T-27D01.2.2 closed all six in one pass — `knowledge`'s repeatable flag and `budget`'s float value, the two likeliest further-split candidates the prior note flagged, each turned out to need exactly one small, deliberate `Binder`/`ArgValue` addition and no sub-`.N` division of its own. Every addition so far is re-sequenced or split scope, not new scope — T-27D01.1/T-27D01.1.2/T-27D01.1.2.2/T-27D01.2/T-27D01.2.2 are all pieces of the original track D, not additions to it — so the growth stays honest, and the count now holds at 22 with only T-27D02/T-27D03 left to open in this track. The threshold from the prior note stands unchanged should a later track reopen it: **if a further `.N` split ever pushes this phase past ~24 tasks, split track D into its own phase rather than continuing to grow this one.**

**Critical path and cascade risk.** Track B is the single point every later task and both later phases depend on. If the registry or dispatch shape is wrong, tracks C and D and all of Phases 28–29 rework. Land B behind its own tests before opening C or D, and prefer discovering a shape problem in B's unit tests over discovering it in the command line's migration.

**Sizing warning (planner audit) — now exercised, not just anticipated.** Track D is the largest and most optimistically sized work here: the command line is ~4000 lines across 29 groups, and T-27D01/T-27D02 each touch all of them. T-27D01 confirmed the warning was right rather than merely cautious: it delivered the generator mechanism plus exactly one migrated group (`memory`) and split the remaining 12 real semantic groups into **T-27D01.2**, further sub-dividing that split's own group list by whether `Binder` needs an extension first (6 groups migrate immediately with the mechanism as built; 6 need a named-flag/default-value/float decision made once, deliberately, not invented under one group's pressure). Expect T-27D01.2 itself to grow further `.N` sub-task IDs once its own first few groups are done and its real size is known — the behaviour-preservation gate (`cli_smoke.rs` unmodified) is far easier to keep green in small increments, and a failed sweep is expensive to bisect. **T-27D01.1 confirmed the same pattern holds on the installation half**: it delivered the two-stage pre/post-composition mechanism plus five fully migrated groups (`init`, `status`, `doctor`, `restore`, `dev` — both the flat and nested shapes proven) and split the remaining six into **T-27D01.1.2**, which then closed five of those six (`workspace`, `backup`, `activation`, `archetype`, `registry`) — designing and building the `BinderKind::NamedText` extension along the way rather than deferring it a second time, since it was blocking most of what remained on *both* halves — and split its one remaining unmigrated group, `ext`'s 3-level nesting, into **T-27D01.1.2.2**, which then closed it — adding the dot-in-the-verb-tail signal `verb_of` already used, one level deeper. **The installation half is now fully closed**: all eleven groups (`init`, `status`, `doctor`, `restore`, `dev`, `workspace`, `backup`, `activation`, `archetype`, `registry`, `ext`) are generated from the one declaration in `crates/cli/src/installation.rs`, confirmed directly against `cronus --help`'s real output — T-27D01.1's own deferred "exactly eleven" clause is closed, four sub-tasks and one `Binder` extension later. **The semantic half is now fully closed too**: T-27D01.2.2 migrated the last six groups (`board`, `schedule`, `budget`, `loop`, `workflow`, `knowledge`), bringing all eighteen real semantic groups (`memory` + T-27D01.2's twelve + these six) onto the generated parser behind two further `Binder`/`ArgValue` extensions (`Float`, `RepeatableNamedText`). Only the stub-group removal (T-27D03) and the shared renderer (T-27D02) remain before track D's combined "exactly" property — the one both T-27D01 and T-27D01.1 originally named — is fully true.

## Atomic Checklist

- [x] [T-27A01] Invocable descriptor and catalog types in the ports tier
- [x] [T-27A02] Invocation / Outcome / Rejection envelope with four located rejection modes
- [x] [T-27A03] Optional serialization feature on the ports tier
- [x] [T-27B01] Registry: one published registration door, qualified identity, declared collision rule
- [x] [T-27B02] Dispatch: bind-before-invoke over declared binders, returning a structured outcome
- [x] [T-27B03] Contribution safety: failure policy, grant-gated reach, attribution marker
- [x] [T-27B04] Core invocables registered from the facade through the public door; redaction at the boundary
- [x] [T-27A04] Retrofit: `Installation` locus, validated identity grammar, descriptor bounds
- [x] [T-27B05] Retrofit: registration returns its own reversal; descriptor normalized and refused, never repaired
- [x] [T-27B06] Retrofit: resolution split from outcome; change announcement; paired dispatch journal
- [x] [T-27C01] Conformance fixture library and harness with three assertion families
- [x] [T-27C02] Finding inventory and the two one-way ledgers, seeded with F-1…F-8
- [x] [T-27D01] Command-line parser generated from `Semantic` descriptors (mechanism + first group)
- [x] [T-27D01.1] The installation half: one closed launcher grammar feeding both the parser and the catalog
- [x] [T-27D01.1.2] Migrate the remaining six installation groups onto the generated parser
- [x] [T-27D01.1.2.2] `ext` (with its nested `skill` sub-group): the installation half's one remaining group
- [x] [T-27D01.2] Migrate the remaining 12 real semantic groups onto the generated parser
- [x] [T-27D01.2.2] Migrate the remaining six semantic groups needing a `Binder` extension
- [ ] [T-27D02] One renderer over the structured outcome; uniform output-format handling
- [ ] [T-27D03] Shipped-surface honesty: the five unbound groups leave the default surface; corpus registration
- [ ] [T-27T01] Corpus first run — convert failures into findings before fixing anything
- [ ] [T-27T02] Behaviour-preservation proof and full quality gates

## Detailed Tracking

### [T-27A01] Invocable descriptor and catalog types in the ports tier

- **Spec:** l2-invocable-registry.md §4.1, §4.2
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo test -p cronus-contract` green; `cargo tree -p cronus-contract --depth 1` lists no external dependency in the default build.
- **Handoff:** T-27A02 (envelope types name these).
- **Notes:** Descriptor carries id, name, summary, group, `locus`, ordered binders, `stability`. The descriptor is the **whole** advertised contract — a surface must be able to render help, completion, and grouping from it alone, because any fact a surface must hold privately is the first step of a fork. `locus` is `Semantic | ClientLocal | HostOnly`; `stability` is `Shipped | Retired { superseded_by }`. Types live in the ports tier because all three surfaces and the domain must name them without depending on each other.
- **Changes:** Added `InvocableId`/`Locus`/`Stability`/`BinderKind`/`Binder`/`Invocable` to `crates/contract/src/lib.rs`, following the file's existing per-seam banner + `#[cfg(test)] mod {seam}_tests` convention (mirrors the `MemoryId`/`ActivationMode` precedent already in the file — no new module file). `Binder`'s declared shape (name/kind/optional) is included here since `Invocable.binders` needs it to compile; binder *matching* behavior stays out of scope for T-27B02. Considered and dropped a `new()` constructor on `InvocableId` as redundant with `From<&str>`/`From<String>` (minimalism). 5 new tests in `invocable_tests`, all 10 pre-existing tests in the crate unmodified and passing. `cargo test -p cronus-contract` 15/15 green; `cargo tree -p cronus-contract --depth 1` shows no dependency; `cargo clippy -p cronus-contract --all-targets -- -D warnings` clean; `cargo fmt -p cronus-contract -- --check` clean.

### [T-27A02] Invocation / Outcome / Rejection envelope with four located rejection modes

- **Spec:** l2-invocable-registry.md §4.5, §4.6
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo test -p cronus-contract` green, including a test asserting that an empty result and an unavailable source are **distinct** `Outcome` values and cannot be constructed from one another.
- **Handoff:** T-27B02 (dispatch produces these).
- **Notes:** `Outcome` is `Value | Stream | Rejected` and carries **structured data, never rendered text** — that is what lets one dispatch serve a text renderer, a structured renderer, terminal widgets, and IPC without any of them re-deriving the others' content. A `Rejection` names one of absent / unreadable / malformed / ill-shaped plus its location; collapsing the four into one "invalid input" is a defect, because each implies a different corrective action.
- **Decision (recorded, not asked — pure technical realization):** found a real tension between two already-Stable specs on where "resource unavailable" lives: one reads as a distinct `Outcome` variant, the other as "a rejection carries its mode". Resolved via the L1 parent (input-binding's own definition of rejection *location* as a position within a bound *argument*): a backend/store failure binds to no declared binder and has no such location, so forcing it into `Rejection` would either fabricate a location or make location optional and dilute the one guarantee IB-4 exists to give. Added `Outcome::Unavailable { reason }` as a fourth variant instead, keeping `Rejection` strictly binder-scoped with an always-real location. The imprecise spec wording ("a rejection carries its mode") needs a one-line patch-level correction in a future `/magic.spec` pass — flagged, not fixed here (out of this workflow's write scope).
- **Changes:** Added `Surface`/`ArgValue`/`ArgValues`/`Invocation`/`StreamHandle`/`OutcomeValue`/`RejectionMode`/`Rejection`/`Outcome` to `crates/contract/src/lib.rs`, plus a `dispatch_tests` module (5 tests) following the file's existing per-seam convention. `OutcomeValue` is a small bounded tree (Empty/Text/Integer/Boolean/List/Record) — general enough for text/structured/widget/IPC rendering per §4.5's own reasoning, deliberately no Float (no current call site needs one) and no open-ended/serde-style catch-all. `cargo test -p cronus-contract` 20/20 green (10 pre-existing unmodified); `cargo clippy -p cronus-contract --all-targets -- -D warnings` clean; `cargo fmt -p cronus-contract -- --check` clean.

### [T-27A03] Optional serialization feature on the ports tier

- **Spec:** l2-invocable-registry.md §4.1
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo check -p cronus-contract` (default, no serde in the *production* dependency graph — `cargo tree -p cronus-contract --depth 1 -e=no-dev` empty) and `cargo check -p cronus-contract --features serde` both succeed; `cargo test -p cronus-contract --features serde` exercises `Serialize` on a descriptor and every `Outcome` variant. *(Revised in-flight from a round-trip test — see Decision below.)*
- **Handoff:** Phase 29 (the desktop shell enables it; `Invocation`/`ArgValues` gain `Deserialize` there, when the generic-dispatch IPC command actually needs the JS→Rust half).
- **Notes:** The ports tier is dependency-free **by construction** and its manifest carries an empty dependency section — so serialization is a feature, off by default, never an unconditional dependency. Landing it now rather than when the desktop needs it is SP-9: extraction before the second consumer is the only cheap point on the ladder.
- **Decision (recorded, not asked — corrects my own over-specification):** the original Verify line asked for a full round-trip, which turned out to be unachievable *and* unneeded. `Invocable.name/summary/group`, `Binder.name`, `Locus::HostOnly.reason`, and `Rejection.binder` are all `&'static str` — compile-time literals that cannot be honestly reconstructed from arbitrary wire input without leaking memory per deserialized instance, so `Deserialize` fails to compile on this type shape by construction. Checked against the real IPC direction (`l2-application-shell` §4.3): `catalog() -> Promise<Invocable[]>` and the return half of `invoke() -> Promise<Outcome>` both cross **Rust → JS only** — only `Invocation` (the argument JS supplies) crosses JS → Rust, and it was already out of this task's scope. So `Invocable`/`Outcome` need `Serialize` alone; asking for `Deserialize` on them was planning-time over-specification, not a real requirement. Corrected the Verify line to match the direction that actually exists, rather than forcing a leak-based workaround to satisfy an invented symmetry.
- **Changes:** `serde` added to root `Cargo.toml` workspace.dependencies (1.0.228, `derive` feature) and to `crates/contract/Cargo.toml` as `optional = true` behind a `serde` feature (`dep:serde`); `serde_json` added there as a test-only dev-dependency (never affects the production graph or `cargo tree -e=no-dev`). `#[cfg_attr(feature = "serde", derive(serde::Serialize))]` on the 11 descriptor/outcome types from T-27A01/T-27A02. New `serde_tests` module (2 tests, gated `#[cfg(all(test, feature = "serde"))]`) verifying JSON shape and per-variant distinctness. `cargo test -p cronus-contract` 20/20 and `--features serde` 22/22, both green; `cargo clippy -p cronus-contract --all-targets [--features serde] -- -D warnings` clean both ways; `cargo fmt -p cronus-contract -- --check` clean; `cargo check --workspace` unaffected by the root manifest edit.

### [T-27B01] Registry: one published registration door, qualified identity, declared collision rule

- **Spec:** l2-invocable-registry.md §4.3, §4.4
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo test -p cronus-domain` green, including tests that (a) a contribution cannot claim the reserved core identity, (b) a bare verb colliding with a core verb resolves to the core's by the declared rule while the contribution stays reachable by qualified name, and (c) two sources claiming one identity resolve by declared source order with the later refused.
- **Handoff:** T-27B02, T-27B04.
- **Notes:** Registry logic is pure and belongs in the domain tier — no I/O. The reserved core namespace is what guarantees a contribution cannot shadow a core name **whatever the core adds later**, which a first-registration-wins rule cannot promise. Ordering must be *declared*, never a function of load order.
- **Decision (recorded, not asked):** unified "reserved core identity" and "identity/source collision" into one mechanism rather than two — the registry pre-claims `"core"` for the core's own source at construction, so an extension claiming `"core"` fails the *ordinary* collision check instead of needing a special-cased rule. Bare-form resolution is populated **only** by core registrations (a contribution never enters the bare map at all, regardless of arrival order) rather than "core displaces a later contribution" — simpler, and sufficient: the spec's own wording says a contribution is reached by its qualified name, never by the bare form, so the bare map has no reason to ever hold one. Verified order-independence directly: the test registers the contribution *before* the core entry and still finds core at the bare lookup. "Source" (who may extend an already-claimed identity) is a caller-supplied opaque token, not registry-generated — assigning real tokens is an extension-loader concern for a later task/phase, not this one.
- **Changes:** New `crates/domain/src/invocable/mod.rs` (`InvocableRegistry`, `Registrant`, `RegistrationError`, `CORE_IDENTITY`), registered in `crates/domain/src/lib.rs`. Added `InvocableId::qualifier()`/`tail()` to `crates/contract/src/lib.rs` (the registry's own need, extending the T-27A01 type rather than duplicating the split logic). 5 new tests in `invocable::tests`. `cargo test -p cronus-domain` 460/460 (455 pre-existing unmodified); `cargo test -p cronus-contract` 20/20 unaffected; `node scripts/check-domain-boundary.mjs` confirms no new dependency entered the domain tier's allowlist; `cargo clippy -p cronus-domain --all-targets -- -D warnings` and `cargo fmt -p cronus-domain -p cronus-contract -- --check` both clean.

### [T-27B02] Dispatch: bind-before-invoke over declared binders, returning a structured outcome

- **Spec:** l2-invocable-registry.md §4.5, §4.6
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo test -p cronus-domain` green, including a test that a binding failure leaves **no** side effect — the body did not run — and a test that each of the four rejection modes reports its location. *(Second half satisfied at the type level per the Decision below, not by this module's own `bind()`.)*
- **Handoff:** T-27B03, T-27D01.
- **Notes:** Every declared binder completes before the body begins, which is what makes *did not run* a structural fact rather than a self-report and makes the whole rejection class safely re-invocable. Optional means **absent, never invalid**: a value that is present and fails to bind still rejects, with mode and location intact. Mapping a malformed value onto "not provided" is the quiet failure that makes a unit do confidently wrong work.
- **Decision (recorded, not asked):** a real modeling gap surfaced against the Verify line's literal wording. `bind()` checks already-parsed, already-typed in-memory `ArgValue`s, so it can only naturally produce `Absent` (nothing supplied) and `IllShaped` (present, wrong shape) — there is no "unparsed raw candidate" to be `Malformed`, and (since this crate is I/O-free by construction, §4.3) no external source to fail as `Unreadable`. Both of those belong to whatever surface constructs `ArgValues` from raw input, upstream of dispatch — reading a source, or parsing a raw string, is surface/adapter work, not domain-tier binding. Rather than redesign the already-Done `ArgValue` shape (T-27A02) into a raw-string model just to force all four triggers into one function, left `bind()` honestly scoped to the two modes it can produce, and broadened `Outcome::Unavailable`'s doc comment (a one-line, non-breaking clarification, not a shape change) to also honestly cover "no such invocable is registered" — a third failure class this task discovered that is neither a binder rejection (no binder is at fault) nor the resource-unavailability case the variant was first written for.
- **Changes:** `crates/domain/src/invocable/mod.rs` reorganized into a thin doc+re-export shell (matching the `tool_receipts/` directory-module convention) with the T-27B01 registry moved unchanged into `registry.rs` and a new `dispatch.rs` holding `bind`/`Dispatcher`/`Handler`. Added `InvocableId`'s `Display`-based `as_str()` use in error messages (no new type). 5 new tests in `dispatch::tests`, covering: no side effect on a binding rejection, an absent-optional binder still invoking the handler, an ill-shaped value rejecting with its binder location, an unknown invocable resolving to `Unavailable` rather than panicking, and a registered-but-unattached descriptor doing the same. `cargo test -p cronus-domain` 465/465 (455 pre-existing + 10 invocable, all unmodified/passing); `cargo test -p cronus-contract` 20/20 unaffected; `node scripts/check-domain-boundary.mjs` clean; `cargo clippy -p cronus-domain --all-targets -- -D warnings` and `cargo fmt -p cronus-domain -p cronus-contract -- --check` both clean.

### [T-27B03] Contribution safety: failure policy, grant-gated reach, attribution marker

- **Spec:** l2-invocable-registry.md §4.10
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo test -p cronus-domain` green, including tests that (a) a contributed invocable that panics or exceeds its bound resolves to a rejection **attributed to the contribution** and does not unwind into the core, (b) registering an invocable without the declared manifest grant is refused, and (c) the attribution marker is produced by the projection and cannot be set or suppressed by the contribution.
- **Handoff:** T-27T02.
- **Notes:** These three are what make a contributed verb *safe to be reachable*; reachability without them is the plugin story with its failure modes unspecified. The attribution rule is security-relevant, not cosmetic: a contributed verb that can present itself as the product converts the user's trust in the product into a capability an extension holds.
- **Decision (recorded, not asked):** Rust cannot preempt a running synchronous closure without unsafe/OS-specific work, so "terminated" for an overrun contribution means the *caller* stops waiting at the bound (via a fresh, unscoped `std::thread::spawn` + `mpsc::recv_timeout`), not that the handler's thread is actually killed — it becomes orphaned and runs to completion discarding its result. `std::thread::scope` was considered and rejected: scoped threads are always joined before `scope()` returns, which would make the whole function block for the handler's real duration regardless of the timeout, defeating the purpose. `Handler` changed from `Box<dyn Fn>` to `Arc<dyn Fn>` (T-27B02 API) to get a cheap, `'static`-safe clone onto the spawned thread — the T-27B02 tests updated their `Box::new` call sites to `Arc::new`, no test *assertion* changed. Core invocables are exempted from both containment and the bound entirely (branch on `id.qualifier() == CORE_IDENTITY`): a core panic is a real defect that must propagate, and silently swallowing it would hide bugs rather than contain a contribution's fault. The bound is a `Dispatcher` field (`CONTRIBUTION_TIME_BOUND` default, `with_contribution_bound` override), not a hardcoded constant burned into `run_contribution` — otherwise proving the timeout fires would cost the test suite the production bound's real duration on every run. The grant check implements only the *general* `CONTRIBUTE_GRANT` (registering at all); the spec's further "a specific grant beyond the general one" for security-relevant invocables has no per-invocable security-relevance flag to key off yet and is deferred, not silently dropped. Attribution needed no new field or type: it is a pure function deriving the answer from `id.qualifier()`, which registration had already validated against the real registrant — so a contribution's `name`/`summary` (fields it fully controls) cannot influence it, proven directly by a test that sets a deceptive `name` and confirms the derived attribution is unaffected.
- **Changes:** `crates/domain/src/invocable/dispatch.rs`: `Handler` now `Arc`; new `run_contribution` (catch_unwind + thread + `recv_timeout`); `Dispatcher` gained a `contribution_bound` field, `with_contribution_bound`, and branches core-vs-contribution in `dispatch`. `crates/domain/src/invocable/registry.rs`: `Registrant` gained `grants: HashSet<String>` + `with_grant`; new `CONTRIBUTE_GRANT` const, `RegistrationError::MissingGrant`, the grant check in `register`, and the new `attribution` function. `mod.rs` re-exports updated. 6 new tests (2 dispatch: panic-containment, timeout; 1 dispatch: core-panic-propagates-uncontained, proving the exemption; 3 registry: missing-grant refused, core needs no grant, attribution-from-deceptive-fields) plus the 5 pre-existing B01 registry tests updated to declare `CONTRIBUTE_GRANT` (their own assertions unchanged). `cargo test -p cronus-domain` 471/471 (455 pre-existing + 16 invocable, all green, timeout test completes in milliseconds not seconds); `node scripts/check-domain-boundary.mjs` clean (std::thread/panic/sync are stdlib, not new crate dependencies); `cargo clippy -p cronus-domain --all-targets -- -D warnings` and `cargo fmt -p cronus-domain -- --check` both clean.

### [T-27B04] Core invocables registered from the facade through the public door; redaction at the boundary

- **Spec:** l2-invocable-registry.md §4.3, §4.12
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo test -p cronus-core` green *(the Verify line above named the wrong package — the facade crate is `cronus-core`, not `cronus`; corrected here, not silently)*; a test asserts every core invocable was registered through the same published function used by contributions (no private registration path exists); a test asserts dispatch output passes the shared redaction path exactly once.
- **Handoff:** T-27D01.
- **Notes:** EP-12 has one purpose — a seam exercised only by third parties is one nobody notices has become insufficient. Core-supplied contributions may differ in exactly three sanctioned ways (eager loading where ordering demands, implicit trust, surviving third-party disable switches) and no others. Redaction moves here so INV-7 is satisfied once instead of remembered three times; **the empty-secret-list half stays a recorded residual and is not fixed in this phase.**
- **Decision (recorded, not asked):** scoped "core invocables registered from the facade" to two real, already-existing capabilities (`core:status`, `core:version`, delegating to the `Capabilities` methods `Engine` already implements) rather than the full 29-group CLI surface — that migration is T-27D01's own explicitly-scoped job (its notes cite ~4000 lines needing `.N` splits), and this task's own Verify line only asks to prove the *mechanism*, not completeness. `redact_outcome` was added to the existing `cronus_domain::redact` module (extending the established single-owner precedent) rather than a new module, walking `Outcome`'s string-bearing fields (`OutcomeValue::Text`/`List`/`Record`, `Rejection.detail`, `Unavailable.reason`) — `Stream`'s channel name is left unmasked since it names a subscription, not rendered content. `Dispatcher::dispatch` was split into a private `dispatch_unmasked` plus one public wrapper applying `redact_outcome` exactly once, so every return path — including the three early-return `Unavailable`/`Rejected` cases — passes through the same single point structurally, not by remembering to call it at each site. `Dispatcher` gained a `secrets: Vec<String>` field defaulting empty (mirroring the TUI/desktop bridge's own pre-existing `secrets` field) plus `set_secrets` — proving the mechanism while leaving the empty-list-in-production defect as the recorded residual the task notes call for.
- **Changes:** `crates/domain/src/redact.rs`: `redact_outcome` + `redact_value`, 2 new tests (nested Record/List masking, Rejection/Unavailable masking). `crates/domain/src/invocable/dispatch.rs`: `Dispatcher` gained `secrets`/`set_secrets`; `dispatch` now wraps a private `dispatch_unmasked` with one `redact_outcome` call. New `crates/core/src/invocable_bootstrap.rs` (`bootstrap(Engine) -> (InvocableRegistry, Dispatcher)`, registering `core:status`/`core:version` through `Registrant::core()` + `InvocableRegistry::register`). `crates/core/src/lib.rs`: added `invocable` to the domain re-export list and `pub mod invocable_bootstrap;`. New `crates/core/tests/invocable_invariants.rs` (2 tests, matching this crate's `*_invariants.rs` convention): shared-door proof (a real contribution registers into the exact registry `bootstrap` populated) and redaction-boundary proof (a secret-bearing `Outcome::Value` comes back masked). `cargo test -p cronus-core` green across the whole crate (every `test result:` line 0 failed, unit + all ~37 integration files incl. the 2 new); `cargo test -p cronus-domain redact` 6/6; `node scripts/check-domain-boundary.mjs` clean; `cargo clippy -p cronus-core -p cronus-domain --all-targets -- -D warnings` and `cargo fmt` on both, clean; `cargo check --workspace` green.

### [T-27A04] Retrofit: `Installation` locus, validated identity grammar, descriptor bounds

- **Spec:** l2-invocable-registry.md §4.2, §4.4, §4.8
- **Status:** Done
- **Assignment:** Agent
- **Scope:** In the ports tier: add the fourth `Locus` variant; make identity construction **fallible** against a closed grammar (`<qualifier>:<tail>`, both halves non-empty, neither containing the separator) so an unvalidated identity cannot exist; declare the descriptor bound constants (name, summary, group, binder count, binder name/description) that T-27B05 enforces.
- **Why it is fallible rather than checked at registration:** a type that accepts any string parses `"a:b:c"` and `"noqualifier"` without complaint, and the ambiguity surfaces much later as a lookup resolving to the wrong entry or to none. Refusing at construction means no unvalidated identity exists to be registered.
- **Verify:** `cargo test -p cronus-contract` green, including new cases that a multi-separator, empty-half, and empty-string identity are all **refused**; `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check` clean. Existing call sites updated to the fallible constructor — the count of changed sites is reported, not glossed.
- **Handoff:** T-27B05.
- **Decision (recorded, not asked):** `From<String>`/`From<&str>` cannot survive a fallible constructor — a `From` conversion is infallible by trait contract — so they are replaced by `InvocableId::new(impl Into<String>) -> Result<Self, InvocableIdError>` plus `TryFrom<String>`/`TryFrom<&str>` delegating to it, keeping `.try_into()` available for a caller that wants the trait form. `InvocableIdError` is a closed 4-variant enum (`MissingSeparator`/`EmptyQualifier`/`EmptyTail`/`ExtraSeparator`) rather than a single opaque error, because the task's own three named test cases (multi-separator, empty-half, empty-string) need to assert *which* rule fired, not merely that construction failed. The grammar is enforced by splitting on the **first** `:` and then checking the tail itself for a second `:` — so `"a:b:c"` is refused as `ExtraSeparator` (tail `"b:c"` still contains the separator) rather than silently accepted with `tail() == "b:c"`, which is exactly the "resolves to the wrong entry, or to none" ambiguity the task names. Every one of the 31 pre-existing `InvocableId::from(...)` call sites (19 in `crates/contract` tests, 3 in `crates/domain/src/invocable/registry.rs`, 10 in `crates/domain/src/invocable/dispatch.rs`, 2 in `crates/core/src/invocable_bootstrap.rs`, 5 in `crates/core/tests/invocable_invariants.rs`) is a compile-time-known-good literal, so each becomes `InvocableId::new(...).expect(...)` rather than propagating a `Result` — the production bootstrap sites use a distinct message naming the invariant ("core-authored literal identity must be well-formed — a bug if it isn't"), consistent with the `.expect(...)` already on `register(...)` two lines below each in that same file. **Spec/code mismatch found and corrected, not implemented as written:** §4.2's EP-14 table says a contributed binder's "name and description" are bounded, but `Binder` has no `description` field — that clause predates the field and is a wording defect in the spec, not a shape this crate has ever had. Declared the bound constant for `name` only (`BINDER_NAME_MAX_LEN`) and recorded the mismatch via `record-diagnostic` rather than inventing a field no consumer asked for. Bound values themselves (`INVOCABLE_NAME_MAX_LEN = 80`, `INVOCABLE_SUMMARY_MAX_LEN = 240`, `INVOCABLE_GROUP_MAX_LEN = 40`, `INVOCABLE_MAX_BINDERS = 16`, `BINDER_NAME_MAX_LEN = 40`) are a DA-3 technical-realization choice — declared, not yet enforced; enforcement is T-27B05's scope.
- **Changes:** `crates/contract/src/lib.rs`: added `Locus::Installation` (fourth variant, no exhaustive `match Locus` existed anywhere in the workspace, so this added no compile-time ripple beyond the identity change below); added `InvocableIdError` (Debug/Clone/Copy/PartialEq/Eq + Display + std::error::Error) and `InvocableId::new`; replaced the two `impl From` with `impl TryFrom<String>`/`impl TryFrom<&str>`; updated `qualifier()`'s doc comment (construction now enforces the shape, not registration); added the five `INVOCABLE_*`/`BINDER_NAME_MAX_LEN` bound constants above `Invocable`; added 8 new tests (4 refusal-mode cases, empty-string, `TryFrom` agreement, `Installation` distinctness) to `invocable_tests`. Mechanical `InvocableId::from(X)` → `InvocableId::new(X).expect(msg)` rewrite (depth-counted paren matching, not a line-based regex, so nested calls like `"...".to_string()` and `.resolve(&InvocableId::from(...))` split correctly) applied to all 31 call sites across `crates/contract/src/lib.rs`, `crates/domain/src/invocable/{registry,dispatch}.rs`, `crates/core/src/invocable_bootstrap.rs`, `crates/core/tests/invocable_invariants.rs` — zero test *assertions* changed, only the construction call shape. `cargo test -p cronus-contract` 27/27 (29/29 with `--features serde`); `cargo test -p cronus-domain` 473/473; `cargo test -p cronus-core` clean including the 2 `invocable_invariants` integration tests; `cargo clippy -p cronus-contract -p cronus-domain -p cronus-core --all-targets -- -D warnings` clean; `cargo fmt` (same three crates) `--check` clean; `node scripts/check-domain-boundary.mjs` clean (no new dependency introduced).

### [T-27B05] Retrofit: registration returns its own reversal; descriptor normalized and refused, never repaired

- **Spec:** l2-invocable-registry.md §4.2, §4.3, §4.4
- **Status:** Done
- **Assignment:** Agent
- **Scope:** `register` returns a handle whose disposal removes that registration **and its attached handler** (EP-13); no `unregister(id)` is added. Descriptor fields are bounds-checked at registration and stored as a **normalized owned copy** the registrant cannot afterwards reach (EP-14). The bare-form collision rule is implemented as declared: the core's tail wins, the loser stays registered and reachable by qualified identity, and the shadowing is **reported** naming winner, loser, and the qualified name the loser is still reachable by.
- **Refuse, do not repair:** a descriptor failing a bound is rejected naming the field and the constraint. Truncating or defaulting it is forbidden — a repaired declaration is one whose author never learns it was invalid, and the repair silently becomes part of the contract.
- **Verify:** `cargo test -p cronus-domain` green with new cases for: disposal removing both descriptor and handler; a shadowed contribution still resolvable by qualified id **and** returning to the bare form after the shadowing entry is disposed; an over-long and an empty descriptor field each refused with the field named. `clippy -D warnings` + `fmt --check` clean; `node scripts/check-domain-boundary.mjs` clean.
- **Handoff:** T-27B06.
- **Decision (recorded, not asked):** `register()` and `attach()` stay two separate calls on two separate types (`InvocableRegistry` never references `Dispatcher`, preserving the catalog/execution split the module doc already declares) — so "registration returns a handle whose disposal removes that registration and its attached handler" is realized as **two independently disposable handles** (`RegistrationHandle` from `register`, `DispatchHandle` from `attach`) rather than one fused type, proven by a test that disposes each on its own and checks its own effect: disposing only the dispatch handle leaves the descriptor resolvable but the invocation now `Unavailable` (no handler); disposing the registration handle afterward removes the descriptor too. A fused type would need `register` to reach into `Dispatcher` state it has no reference to, coupling two tiers the existing architecture deliberately keeps apart. Neither handle is `#[must_use]`: doing so would force `let _ = ...` at all 22 pre-existing `.register(...)`/`.attach(...)` call sites across three crates for zero behavioral gain, which is scope the task did not ask for — the type signature change alone (`Result<(), _>` → `Result<Registration, _>`, `()` → `DispatchHandle`) is enough, and every existing call site compiles unmodified. **The bare-form model changed from "core-only, ever" to "whoever wins, tracked":** the old code never let ANY contribution occupy `bare` regardless of order, which is simpler but not what EP-4's declared rule (§4.4, amended in the same spec wave as this retrofit) actually asks for — a contribution must be able to win an uncontested tail and reclaim it later. `bare: HashMap<String, InvocableId>` became `HashMap<String, Vec<InvocableId>>` (every registrant for a tail, in registration order), with the winner computed on demand by `bare_winner` (core-if-present, else earliest) rather than cached — so disposal never needs a second bookkeeping step to keep a cached "current occupant" in sync, only to prune the vec. The existing `a_bare_verb_collision_resolves_to_the_core_registrant_regardless_of_order` test needed **zero changes** to pass under the new model (traced by hand: contribution-first-then-core still yields core as the bare winner). Validation runs **before** the registrant/identity checks (proven by a test using a descriptor that would ALSO fail identity-mismatch, asserting the descriptor error wins) — a malformed descriptor is invalid regardless of who is registering it. "Normalized owned copy the registrant cannot afterwards reach" needed no new copying step: `Invocable` was already moved by value into `register()`, so the property is a consequence of Rust's ownership rules, not new machinery — documented at the call site rather than silently relied upon. Bound violations are refused via the existing `RegistrationError` enum (new `InvalidDescriptor { field, problem }` variant, `problem` a closed `DescriptorFieldError` enum) rather than a `String` reason, matching `InvocableIdError`'s precedent from T-27A04 of a matchable, per-case-assertable error rather than free text.
- **Changes:** `crates/domain/src/invocable/registry.rs`: `register` returns `Result<Registration, RegistrationError>` (was `Result<(), _>`); new `Registration { handle, shadow }`, `RegistrationHandle` (+ `dispose`), `Shadow { winner, loser }`, `DescriptorFieldError`, `RegistrationError::InvalidDescriptor`; new `bare_winner`, `validate_descriptor`, `validate_text` functions; `bare` field retyped to `HashMap<String, Vec<InvocableId>>`; `resolve_bare` rewritten against the new shape; module doc comment corrected (no longer claims contributions can never occupy bare). `crates/domain/src/invocable/dispatch.rs`: `attach` returns `DispatchHandle` (was `()`); new `DispatchHandle` (+ `dispose`); 1 new test. `crates/domain/src/invocable/mod.rs`: re-exports extended (`DispatchHandle`, `Registration`, `RegistrationHandle`, `Shadow`, `DescriptorFieldError`). 7 new tests (6 registry: shadow-and-reclaim, empty field, whitespace-only field, over-long field, too-many-binders, validation-runs-first; 1 dispatch: disposal removes both descriptor and handler independently) — zero pre-existing test assertions changed. `cargo test -p cronus-domain` 480/480 (473 pre-existing + 7 new); `cargo test -p cronus-core` unaffected (facade call sites compile unchanged since neither handle is `#[must_use]`); `cargo clippy -p cronus-contract -p cronus-domain -p cronus-core --all-targets -- -D warnings` clean; `cargo fmt` (same three crates) `--check` clean; `node scripts/check-domain-boundary.mjs` clean.

### [T-27B06] Retrofit: resolution split from outcome; change announcement; paired dispatch journal

- **Spec:** l2-invocable-registry.md §4.5, §4.9, §4.13
- **Status:** Done
- **Assignment:** Agent
- **Scope:** Split resolution from dispatch so an unknown invocable yields a **resolution** result, not an `Outcome`; `Outcome::Unavailable` narrows back to *resolved, ran, could not answer*. Add the registry change announcement with **individually contained** observer failures (one failing observer neither aborts the mutation nor starves the observers after it). Journal a resolved dispatch as a paired entry/settlement record joined by an identity unique across process restarts, with a per-invocable declaration that suppresses raw-input recording.
- **Why the split is not cosmetic:** the three surfaces answer `Unknown` in three incompatible ways — the terminal UI falls through to ordinary input, the command line raises a usage error, the desktop refreshes a stale catalog. One outcome variant cannot serve all three without one of them behaving incorrectly.
- **Journal failure asymmetry (implement exactly this way):** a failure to write the **entry** record fails the dispatch loudly; a failure to write the **settlement** record on an already-failing dispatch is contained, so the handler's own failure stays the reported one.
- **Verify:** `cargo test -p cronus-domain -p cronus-core` green with new cases for: an unknown id producing a resolution miss and **no journal entry**; a panicking observer not preventing registration and not starving a later observer; a paired run/settle journal for a resolved dispatch; a suppressed-input invocable recording its name but not its arguments. `clippy -D warnings` + `fmt --check` clean.
- **Handoff:** T-27C01, T-27D01.
- **Decision (recorded, not asked):** The spec's `[REFERENCE]` block names `Resolved<'a> { Found(&'a Invocable), Unknown }` inline with `Outcome` under one "Dispatch" heading, which reads as if a single type should serve both the raw lookup and dispatch's own return. Split into **two** contract-tier types instead: `Resolved<'a>` (borrowed, for `InvocableRegistry::resolve`, usable by any caller — including a surface deciding fall-through *before* it even builds an `Invocation`) and `Dispatched` (owned, wrapping `Outcome`, for `Dispatcher::dispatch`'s return). Reason: `Outcome` carries no borrowed data, and forcing dispatch's return to carry the registry's lifetime (as a literal `Resolved<'a>`-shaped return would) would make every successful dispatch outcome unable to outlive the registry reference — a real usability cost (a caller could not store a `Dispatched::Ran(Outcome)` in a queue or send it across a channel) for zero benefit, since nothing about a *settled* outcome needs to borrow the registry. Both types get `is_found`/`is_unknown`/`found()`/`outcome()` convenience methods mirroring `Option`'s ergonomics, since `Resolved`/`Dispatched` are meant to be pattern-matched by three different surfaces, not just this crate. `InvocableRegistry::resolve_bare` was deliberately **left returning `Option<&Invocable>`, not changed to `Resolved`** — out of this task's named scope (only `resolve`/`dispatch` are named), and changing it would ripple into every bare-lookup call site for no tested behavior; noted here as a natural, not urgent, follow-up for whichever task next touches bare-form CLI/TUI lookup. The journal's `dispatch_id` is a plain `String` (`"{pid:x}-{nanos:x}-{seq}"`), not a new branded type: nothing structurally parses it the way `InvocableId` is parsed into qualifier/tail, so a newtype would add ceremony without adding a check. `DispatchJournal`/`RegistryObserver` are both **optional** (`Option<Mutex<JournalSink>>`, `Vec<Box<dyn RegistryObserver>>` starting empty) rather than mandatory constructor parameters — the exact residual shape already established by `Dispatcher::set_secrets` (T-27B04): the seam exists and is fully tested via an injectable test double, wiring a *real* durable journal or a *real* facade-side catalog-refresh observer into `cronus-core` is separate, not-yet-scheduled work, not silently claimed as done. `InvocableRegistry` **lost its `Clone` derive** (verified first: zero call sites anywhere in the workspace actually clone a registry) because `Box<dyn RegistryObserver>` cannot derive it; `Debug` is now hand-written, printing the observer list as a count rather than requiring every observer implementation to be `Debug` for a convenience nothing currently uses. Adding `Invocable::journal_raw_input: bool` (no default — Rust struct literals must state it) touched 9 pre-existing construction sites across 5 files; each states `true` except one new B06 test fixture that flips it to `false` to prove suppression.
- **Changes:** `crates/contract/src/lib.rs`: new `Resolved<'a>` enum (+ `is_found`/`is_unknown`/`found`) and `Dispatched` enum (+ `is_unknown`/`outcome`); `Invocable` gained `journal_raw_input: bool`; `Outcome::Unavailable`'s doc narrowed (no longer claims to cover "no such invocable"); 2 new contract-tier tests. `crates/domain/src/invocable/registry.rs`: `resolve` returns `Resolved<'_>` (was `Option`); new `RegistryObserver` trait, `InvocableRegistry::observe`/private `notify_observers` (panic-contained via `catch_unwind`, called at the end of `register` and inside `RegistrationHandle::dispose`); `InvocableRegistry` lost `Clone`, gained a hand-written `Debug`; 2 new tests. `crates/domain/src/invocable/dispatch.rs`: new `JournalRecord<'a>` enum, `DispatchJournal` trait, `JournalSink` type alias, private `mint_instance_token`/`next_dispatch_id`/`write_journal`; `Dispatcher` gained `journal`/`instance_token`/`dispatch_seq` fields and `set_journal`; `dispatch`'s return type changed `Outcome` → `Dispatched`, restructured into resolve-first (early `Unknown` return) → journal entry (loud failure) → `run_resolved` (new private fn, the old `dispatch_unmasked` body minus the resolve step) → redact → journal settle (contained failure); every pre-existing test updated to unwrap `Dispatched::Ran` via a new local `ran()` test helper, one test renamed and its assertion inverted (`dispatch_against_an_unknown_invocable_reports_unavailable_not_a_panic` → `..._yields_resolved_unknown_not_an_outcome`) to match the corrected behavior; 5 new tests (unknown-writes-no-entry, paired-entry-settle, suppress-raw-input, entry-failure-refuses-before-handler-runs, plus the registry's 2 counted separately above). `crates/domain/src/invocable/mod.rs` and `crates/core/src/invocable_bootstrap.rs`/`crates/core/tests/invocable_invariants.rs` updated for the new re-exports, the 2 required `journal_raw_input` fields, and the `Dispatched`-wrapped match arms. `cargo test -p cronus-contract` 29/29 (31/31 with `--features serde`); `cargo test -p cronus-domain` 486/486 (480 + 2 registry + 4 dispatch — one test renamed, not net-new, keeps the same slot); `cargo test -p cronus-core` clean including both `invocable_invariants` tests; `cargo clippy -p cronus-contract -p cronus-domain -p cronus-core --all-targets -- -D warnings` clean; `cargo fmt` (same three crates) `--check` clean; `node scripts/check-domain-boundary.mjs` clean.

### [T-27C01] Conformance fixture library and harness with three assertion families

- **Spec:** l2-surface-conformance.md §4.4
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `cargo test -p cronus-conformance` green on the harness's own self-tests; the harness is callable from another crate's test target (proven by T-27D03's use of it).
- **Handoff:** T-27D03, and Phases 28–29 registrations.
- **Notes:** New crate `cronus-conformance`, added to the engine workspace. It must be reachable **by path** from the detached desktop-shell workspace (which already depends on the facade that way), because that shell registers against the same harness in Phase 29 — a corpus crate the shell cannot link is a corpus one surface can never join. A **library plus a harness, not a suite** — the desktop shell builds in a detached workspace and cannot be linked from an engine-workspace test, and SP-7 wants registration to be something a surface does as part of becoming a surface. Three families: surface set (exposed = shipped minus **declared** exclusions), advertised schema against declared binders, and semantic outcome including rejection mode and the empty-versus-unavailable distinction. Fixtures are written **from the divergence, not the feature** — empty, single, zero-count, renamed, oversized, absent, unavailable, secret-bearing, identity-colliding — because the happy path is where implementations already agree and therefore proves nothing.
- **Decision (recorded, not asked):** The crate depends on `cronus-contract` alone, deliberately excluding `cronus-domain` — checked, not assumed: the desktop shell's own detached workspace links only `cronus-core` (the facade) and never touches a bare `InvocableRegistry`/`Dispatcher`, so a harness naming either domain type would be one the desktop could never call regardless of path-reachability. `SurfaceProjection` is therefore a 3-method trait expressed only in contract-tier shapes (`exposed() -> Vec<Invocable>`, `advertised_binders(&id) -> Option<Vec<Binder>>`, `invoke(&id, &args) -> Dispatched`) — a live registry and an IPC bridge can implement it identically. **Fixtures carry a fixed `expected: Dispatched` per invocation ("golden" values) rather than the harness comparing two live surfaces pairwise**: pairwise comparison would require every surface running in one test process simultaneously, which is impossible for the desktop (a separate Tauri process) and defeats SP-7's per-surface registration model (a new surface registers alone, against the shared expectation, without needing every prior surface present to compare against). The 10 named divergence classes split across families by what they actually test: 8 map to **outcome** fixtures with an unambiguous cross-surface-neutral expected value (empty, single-element, zero-count, oversized, absent→rejected, unavailable, secret-bearing, boundary-crossing); **renamed** (retired-with-replacement) and **identity-colliding** (shadow pair) are exported as plain descriptor pairs (`retired_pair()`, `shadow_pair()`) rather than forced into a fabricated expected-outcome, because (a) `Dispatcher::dispatch` has no retirement-redirect mechanism today — inventing an expected "message naming the replacement" would test a feature that does not exist rather than a real divergence, and (b) the shadow mechanism's winner depends on registration order the corpus does not control and is already fully proven at the domain tier (T-27B05); these two pairs exist so multiple surfaces exercise the *same* scenario in their own catalog/registration tests, not so the harness re-proves registry mechanics it cannot even import. **`secret-bearing`/`boundary-crossing` are expected to fail on every surface's first real run** — deliberately: they assert real masking against `SECRET_VALUE`, and every surface's dispatcher is currently fed an empty secret list (the still-open INV-7 residual). This converts a residual that was previously only asserted in prose into one the corpus mechanically confirms is still open, which is exactly T-27T01's "convert failures into findings before fixing" working as intended rather than a harness defect. Self-tests use an in-memory `FakeProjection` (plain `HashMap`s) rather than a real registry+dispatcher, consistent with the crate's own contract-only dependency boundary — the fake proves the *harness's* comparison logic; a real surface's own test (T-27D03 onward) proves that surface's *projection*.
- **Changes:** New crate `crates/conformance/` (workspace member; `crates/conformance` added to root `Cargo.toml` `[workspace].members` and `[workspace.dependencies]` as `cronus-conformance`), depending on `cronus-contract` only. `src/projection.rs`: `SurfaceProjection` trait. `src/report.rs`: `DeclaredExclusion`, `ConformanceReport` (3 variants: `SurfaceSet`/`Schema`/`Outcome`). `src/fixtures.rs`: `Corpus`/`OutcomeFixture`, `corpus()` (8 canonical descriptors + 8 outcome fixtures), `retired_pair()`, `shadow_pair()`, `FIXTURE_IDENTITY`/`SECRET_VALUE` constants. `src/harness.rs`: `run()` + `check_surface_set`/`check_schema`/`check_outcomes`, 8 self-tests (conforming baseline, missing/unexpected/excluded surface-set cases, mismatched/absent schema cases, divergent-outcome case, and the unavailable-collapsing-to-empty case naming the residual directly). `cargo test -p cronus-conformance` 8/8; `cargo clippy -p cronus-conformance --all-targets -- -D warnings` clean; `cargo fmt -p cronus-conformance -- --check` clean; `cargo check --workspace` clean with the new member added; `node scripts/check-domain-boundary.mjs` clean (script is scoped to `cronus-domain` alone, unaffected by a new non-domain crate).

### [T-27C02] Finding inventory and the two one-way ledgers, seeded with F-1…F-8

- **Spec:** l2-surface-conformance.md §4.1, §4.2, §4.3
- **Status:** Done
- **Assignment:** Agent
- **Verify:** The inventory file lists all eight seed findings with every field of the §4.1 record populated, including `class` for each; at least two entries are class *preemptive* (an inventory with none has given up on SP-9). Ledger direction is checked by a test or a gate that fails on a tombstone removal or a debt addition.
- **Handoff:** T-27T01.
- **Notes:** Keep tombstones boringly literal — a path or an owner-qualified symbol plus its finding. Cleverness here produces a check nobody trusts and everybody bypasses. Repayment requires **all four** SP-4 conditions; three of four is recorded as *open*, not as *mostly repaid*.
- **Decision (recorded, not asked):** The inventory lives in this same crate (`crates/conformance/src/findings.rs`) rather than a `.design/` document, because the Verify criterion demands a **mechanically checked** ledger direction, and `.design/` content is neither compiled nor tested — the finding data had to be something `cargo test` can see. All prose is written to stand on its own without naming any SDD artifact (no task ids, no phase numbers, no spec filenames), consistent with the containment rule that a product file describing rationale restates it in plain language. **Ledger-direction enforcement is a hand-duplicated baseline, not a git-history diff**: `ledger_baseline` is a second, independent literal copy of the accepted tombstones/debt, never derived by calling back into `tombstones()`/`accepted_debt()` (an earlier draft did exactly that and every comparison was vacuously true, since the baseline was then definitionally always equal to whatever the live data said — caught before landing by tracing through what a real removal would actually do to the comparison, not merely by the tests passing). A real regression requires touching two functions in one file; that requirement is the "must say so" SP-5 asks for, verified concretely both directions: a live tombstone deletion and a live debt addition were each introduced by hand and confirmed to fail their respective test before being reverted — proving the tests catch mutation, not merely that fixture data validates itself. Chose a hardcoded-literal baseline over a checked-in JSON/TOML snapshot file specifically to avoid adding a parsing dependency to a crate whose one deliberate property is depending on `cronus-contract` alone. **Repayment states reflect actual current progress, not a template default**: F-3 (the CLI's own spec-level parity table) is the only finding with real deletion+pinning today, because that table was already removed in the spec amendment that preceded this phase's code work — every other finding stays honestly `open`. F-4 (the CLI's board-path derivation) and F-6 (output-format/rendering) are marked with **no fixture landed**, each with a residual explaining why: F-4 is a specific domain fact none of the eight seed dispatch-mechanics fixtures happens to exercise, and F-6 is rendering fidelity, which the outcome family's own spec text says it does not police (semantic outcome, not bytes rendered) — recorded as gaps rather than silently claimed as covered by the corpus that exists. F-5's and the secret-bearing/boundary-crossing fixtures' residual is stated explicitly as **expected to fail** on every surface's first real run, since every dispatcher is still fed an empty secret list — this is the INV-7 residual becoming a corpus-checked fact instead of an asserted one, exactly as T-27T01 (convert failures into findings before fixing) anticipates.
- **Changes:** `crates/conformance/src/findings.rs` (new): `FindingClass`, `Repayment` (+ `is_repaid`), `Finding`, `seed_inventory()` (8 findings, F-1…F-8, every §4.1 field populated, 2 marked `Preemptive`), `Tombstone`/`tombstones()` (1 seed entry, F-3), `AcceptedDebt`/`accepted_debt()` (2 seed entries, F-5/F-6), and a `#[cfg(test)]`-only `ledger_baseline` submodule holding the independent literal comparison copy. 8 tests: anatomy-completeness, exact-id-set, preemptive-count-floor, nothing-silently-repaid, tombstone/debt-referential-integrity (2), and the two baseline-direction tests, verified by manual mutation to actually fail before being reverted. `crates/conformance/src/lib.rs` re-exports the new module's public items. `cargo test -p cronus-conformance` 16/16 total for the crate (8 pre-existing harness tests from the prior task + 8 new findings tests); `cargo clippy -p cronus-conformance --all-targets -- -D warnings` clean; `cargo fmt -p cronus-conformance -- --check` clean; `cargo check --workspace` clean; `node scripts/check-domain-boundary.mjs` unaffected (crate is not `cronus-domain`). **Containment self-check caught 2 real leaks** in the whole track's product files before this write-up: `fixtures.rs` named a phase number in a doc comment, `lib.rs` named a specification filename in its module doc — both restated in plain language (the phase reference became "until a surface's real secret store is wired into its dispatcher"; the filename reference was dropped, the bare §4.4/§4.5 section numbers already being the codebase's accepted convention for citing rationale without naming the document). Re-scanned clean across all 6 crate source files after the fix.

### [T-27D01] Command-line parser generated from `Semantic` descriptors (mechanism + first group)

- **Spec:** l2-cli.md §4.1, §4.1.1, §4.3, §4.4
- **Status:** Done (mechanism proven; remaining groups split to T-27D01.2 per the pre-authorized sizing note below)
- **Assignment:** Agent
- **Renarrowed at v2.70.0 — read this before starting.** The prior wording was *generate the parser from descriptors*, meaning all of it. That is wrong and would not work: an installation verb must be answerable when the composition it would configure is precisely what failed to come up, so it cannot be projected from a registry that does not exist yet (LH-1/LH-5). This task now covers the **`Semantic` half only**; the installation half is T-27D01.1.
- **Scope:** Build the argument parser for `Semantic` descriptors at startup, after composition. Help, completion, and grouping for that half derive from the same descriptors. The statically declared command enum is deleted **for the semantic verbs it held** and its removal recorded as a tombstone; verbs belonging to the installation half move to T-27D01.1 rather than being deleted here.
- **Verify:** `cargo test -p cronus-cli` green; `cli_smoke.rs` passes **unmodified** (behaviour preservation); `cronus --help` lists exactly the registry's shipped `Semantic` groups plus the installation verbs T-27D01.1 owns, and nothing else. `clippy -D warnings` + `fmt --check` clean.
- **Sizing — split as pre-authorized, not discovered late.** ~4000 lines / 29 groups was never one edit. This task delivers the **mechanism** (a real, tested, registry-driven `clap` tree generator — the hard, reusable part) plus **one fully migrated group** (`memory`, chosen as the cleanest real-not-stub semantic group: no default-valued flags, no `PathBuf`, no `ValueEnum`) proving the whole pipeline end to end against the real dispatch path. `cronus --help`'s "exactly" clause in Verify is therefore satisfied only for what this task actually claims (`memory`, plus whatever the installation half owns) — it does **not** yet hold for the full 13-group semantic set, which is why status reads Done-for-this-slice rather than Done-for-the-verb-list. The remaining 12 real semantic groups are split into **T-27D01.2**, named below, rather than attempted here under the same pressure the original sizing note already warned against.
- **Handoff:** T-27D01.1, T-27D01.2, T-27D02.
- **Decision (recorded, not asked):** **`core:status`/`core:version` were reclassified from `Semantic` to `Installation`** on discovery, not left as a landmine for whoever migrates the `status` group next: they were registered as `Semantic` before `Locus::Installation` existed (the only fit available at the time), and building the FIRST real generated tree surfaced the collision directly — a dynamically-added `status` subcommand fighting the still-present hand-declared top-level `Status` command, caught by clap's own debug assertion (`command name "status" is duplicated`) rather than by inspection. Diagnosing it confirmed the two are unrelated concepts anyway (the registry's `core:status` is `Engine::status()`, a placeholder string; the CLI's own `status` reads `app.json` workspace state) — reclassifying was strictly correct, not merely convenient, and no test named either locus explicitly so nothing broke. **Architecture chosen for the generated tree: `clap`'s builder API augmenting the SAME `Command` the derive macro already produces**, not a hand-rolled parser and not two independent trees merged after parsing. `Cli::command()` (via `CommandFactory`) yields the derived tree; registry-sourced subcommands are appended to it; `get_matches()` runs once; the matched top-level name decides whether to route through the registry or reconstruct the derived enum via `FromArgMatches` for the fallback path. This reuses `clap`'s mature help/error/global-arg machinery (confirmed via source read that `global(true)` args propagate through `ArgMatches::propagate_globals` to every level, so `--format` reads correctly from the root matches regardless of where the user typed it) instead of re-implementing it, and needed exactly one dependency change: enabling `clap`'s `"string"` feature (already-used crate, previously-unused feature — `Command::new`/`.about()` need `impl Into<Str>` from an owned `String`, which is feature-gated). **The interim per-verb renderer in `main.rs` is a deliberate, named bridge, not the real thing**: no shared, format-uniform `Outcome`-to-text renderer exists yet (a separate, later task's job), so `render`/`render_value` reproduce `memory`'s 3 verbs' exact prior text/JSON shapes by hand and will be deleted once that renderer lands — the alternative (inventing a "correct" uniform mapping now, under this task's own narrower scope) would have preempted that task's actual design work. **The old per-verb functions were not reused as-is**: they printed directly and returned an exit code, which is presentation logic that must not live in a handler shared across every surface (a TUI panel has no use for a `println!`) — so the pure business logic was re-expressed as `Outcome`-returning handlers in the facade, and the CLI supplies its own (interim) rendering, rather than wrapping the old i32-returning functions, which was considered and rejected for exactly this reason. **A real, previously-undocumented defect surfaced while writing a genuine round-trip test**: `memory.store` then `memory.search` then `memory.forget` in one test found nothing, because each handler opens its own fresh in-memory store per call — the *exact* pre-existing behavior of the old `open_store()` pattern, now confirmed rather than assumed, and preserved unchanged (not "fixed" mid-migration per SP-10) with the finding recorded separately. Tests were rewritten to match this real, honestly-surprising behavior instead of asserting the round trip the old code never supported either. **Registry gained one small, load-bearing method**: `InvocableRegistry::all()` — building any generated projection at all requires enumerating every registration, and no such method existed; added as a plain, policy-free enumeration (callers filter by locus/stability themselves) rather than a `shipped()`/`semantic()` convenience baked into the registry, keeping the same minimalism as `resolve`/`resolve_bare`. **A second real bug, caught before it shipped**: the first `arg_for` implementation bound `BinderKind::Flag` without `.long(name)`, meaning a flag would parse only if its own name were typed as a bare positional word — never as `--name`. Caught by reasoning through what the *next* group (`role`, with its `--presets` flag) would actually need, not by a failing test on `memory` alone (which uses no `Flag` binders and could not have caught this) — fixed and covered by a dedicated test asserting the bare word is rejected and `--presets` is accepted.
- **Changes:** New `crates/cli/src/generated.rs`: `semantic_shipped` (locus+stability filter, deterministic sort), `verb_of`, `arg_for` (positional for `Text`/`Integer`/`Boolean`, named `--flag` for `Flag`), `build_semantic_tree`, `matched_generated_group`, `invocation_from_matches` — 6 tests (grouping, positional binding, flag binding, locus/stability filtering, verb-suffix stripping). `crates/cli/src/main.rs` rewritten: composes the registry+dispatcher, augments the derived `Cli::command()` with the generated tree, routes matched output through the registry or the legacy `FromArgMatches` fallback, plus the interim `render`/`render_value` bridge. `crates/cli/src/cli.rs`: `Command::Memory` variant and `MemoryCommand` enum deleted (tombstoned below). `crates/cli/src/commands.rs`: `Command::Memory` dispatch arm and the entire `mod memory { … }` block (140 lines, including its 3 old inline tests) deleted. `crates/cli/Cargo.toml`: added `cronus-contract`. Root `Cargo.toml`: `clap` gained the `"string"` feature. `crates/core/src/invocable_bootstrap.rs`: `register_memory_store`/`register_memory_search`/`register_memory_forget` (real handlers over the real `MemoryStore`, `Outcome`-returning); `core:status`/`core:version` reclassified `Semantic` → `Installation`. `crates/core/tests/invocable_invariants.rs`: 3 new tests for the memory handlers (store returns an id; search-with-no-matches is a zero-item `List` not `Empty`; forget-of-unknown is `Unavailable` not a silent success) — replacing an initially-written round-trip test that assumed cross-call persistence the store never had. `crates/domain/src/invocable/registry.rs`: new `InvocableRegistry::all()` + 1 test. `cargo test -p cronus-cli` unit suite 43/43 (memory's 3 old inline tests removed, 6 new `generated.rs` tests added) with the separate `cli_smoke.rs` suite's all 41 tests passing **unmodified**, both counted and content-identical to before this task; `cargo test -p cronus-core` 5/5 new + all pre-existing green; `cargo test -p cronus-domain` 487/487; `cargo clippy` clean and `cargo fmt --check` clean on all four touched crates; `cargo check --workspace` clean; `node scripts/check-domain-boundary.mjs` clean. Manually exercised the real binary end to end (`cronus memory store/search/forget`, text and `--format json`, missing-args, not-found) confirming the generated `--help` output and every rendered shape. **Containment self-check caught 3 leaks** (a spec filename with `.md` implied but omitted, cited three times across `generated.rs`/`main.rs`) — restated in plain language; bare invariant codes (`LH-1`, `LH-7`) kept, matching the codebase's accepted convention.

### [T-27D01.1] The installation half: one closed launcher grammar feeding both the parser and the catalog

- **Spec:** l2-cli.md §4.1.1, §4.2; l2-invocable-registry.md §4.7.1
- **Status:** Done (mechanism proven on five groups; the remaining six split to T-27D01.1.2 for the same reason T-27D01 itself split)
- **Assignment:** Agent
- **Scope:** Declare the installation verbs **once**, in this frontend's own source: a closed grammar containing no name an extension can define. That single declaration has two consumers — the pre-composition parser is built from it, and the same declaration registers `Installation` descriptors into the catalog at composition. Add the three-way failure distinction (usage ≠ composition ≠ application) with a usage failure opening no session and journaling no run (LH-7).
- **The two-consumer shape is the point, and the shortcut is the defect.** A verb hand-declared in a parser *and* hand-listed as a descriptor is two statements of one fact — the exact fork this phase exists to close. Omitting the descriptor instead is the other failure: SP-11 forbids expressing a boundary by silence, and without the catalog entry the terminal UI cannot **declare** that it deliberately does not offer `config`, leaving it indistinguishable from unimplemented.
- **Verify:** `cargo test -p cronus-cli` green with a test asserting **set equality** between the verbs the launcher parser accepts and the `Installation` descriptors registered at composition. State the oracle honestly: this equality is what fails the moment the two are maintained separately and drift, and it is the checkable form of "one declaration" — *derived from one source* is a structural property a unit test cannot observe directly, so it is asserted through the consequence it would violate. A usage error and an application error produce different exit codes, asserted. `clippy -D warnings` + `fmt --check` clean.
- **Sizing — split for the same reason T-27D01 named its own split.** Eleven installation groups exist; this task delivers the **mechanism** (a real, tested two-stage parser: an installation-only tree answerable with no composition at all, falling through to a fully composed tree only for what that half does not own) plus **five fully migrated groups** — `init`, `status`, `doctor`, `restore` (each a **flat** single-verb group) and `dev` (a **nested**, three-verb group) — proving both shapes the mechanism must support. The remaining six (`workspace`, `registry`, `backup`, `activation`, `archetype`, `ext`) are follow-up work, named below as T-27D01.1.2, not silently dropped.
- **Handoff:** T-27D01.1.2, T-27D02.
- **Decision (recorded, not asked):** **The pre-composition parser is genuinely two-stage, not merely structured to look that way.** Stage one builds a `clap` tree from nothing but this frontend's own declared list (no registry, no `Engine`) with `allow_external_subcommands(true)`: a recognized installation verb is fully parsed and dispatched right there — answerable even if composition never runs — while anything this half does not own passes through untouched rather than being refused, since ownership of an unrecognized name is not this half's question to answer (LH-5). Because of `allow_external_subcommands`, an `Err` from that first parse can **only** ever originate from a *recognized* installation verb's own arguments (an unrecognized top-level word is always swallowed as an external subcommand, never a parse error) — so `Err(e) => e.exit()` there is unconditionally correct and needed no further branching to prove it. Only a genuinely unresolved token (or a bare/`--help`/`--version` invocation, which by definition needs the full surface to answer completely — LH-3) falls through to stage two: compose, build the complete tree (installation half again, plus whatever `Semantic` descriptors the registry now holds, plus what remains of the old derived enum), and parse once more. **Composition itself is now a real, checked `Result`**, not an `.expect()` — it cannot actually fail yet (no I/O, no extension loading exists today), but the distinction LH-7 asks for had to be representable now rather than retrofitted the day a real failure mode appears; a distinct exit code (3) is reserved for it, separate from a `clap`-level usage failure (2, `clap`'s own existing convention, unchanged) and from an application-level failure (the pre-existing 0/1 convention, also unchanged — this task did not need to invent finer-grained sub-codes within "ran and failed" to satisfy LH-7's three*-way* distinction). **An installation verb dispatches directly to its already-proven handler, never through the shared `Dispatcher`/`Outcome` pipeline** the semantic half uses: that indirection exists so one handler can serve every surface, and the registry's own surface-projection table (l2-invocable-registry.md §4.7) shows only the command line ever projects the `Installation` locus at all — paying for cross-surface uniformity here would buy nothing. **`core:status` moved out of the facade and into this declaration, resolving a real, previously-undiscovered id collision**: it had been registered in the facade's own bootstrap (T-27D01, before this task's spec section — §4.7.1 — was being actively applied) backed by a placeholder (`"no subsystems loaded yet"`); this frontend's own `status` verb (the pre-existing, real, workspace-init check) needed the identical id under the one-declaration-per-identity rule EP-4 enforces, and the spec's own words — "declared by the frontend that owns them" — settle which of the two survives at that name. The facade no longer registers it; nothing else referenced the placeholder (checked: only the terminal UI's *separate*, pre-existing `Capabilities::status()` mechanism does, and it calls the `Engine` method directly, never through the registry, so it is unaffected). `core:version` stays exactly where it was — real, non-placeholder, non-colliding, and a fact any surface might plausibly want, unlike an installation-locus verb only the command line ever projects. **A flat-vs-nested tree shape, not previously needed:** the semantic half's tree builder always nests (one subcommand level per group); the installation half's does not — a group with exactly one verb whose id-tail equals its own group name renders as a single top-level command carrying that verb's arguments directly (`cronus doctor --fix`, not `cronus doctor doctor`), reusing the exact same rule `verb_of` already computes rather than inventing a second one. **A genuine, narrow simplification, stated rather than hidden:** distinguishing "the user wants the full top-level surface" from "the user wants one installation verb's own help" is done by checking only the first non-flag argument against `{--help, -h, help, --version, -V}` (or a bare invocation) before choosing which stage to enter, rather than a fully general scan of the whole argument list — an invocation like `--format json --help` (a global flag before the help request, no verb) is the one shape this heuristic does not special-case; it still exits 0 correctly, it simply may show the narrower installation-only listing rather than the complete one. Named here for whoever next touches this area, not discovered by them.
- **Changes:** New `crates/cli/src/installation.rs`: `declared_invocables` (the one declaration — five groups, seven verbs), `build_installation_tree` (flat-vs-nested), `group_about` (per-group help text for nested groups, falling back to a generic label for groups not yet declared), `format_arg` (the shared `--format`/`-f` global, duplicated deliberately from `Cli`'s derive definition — this module's "declared once" property is about the *verb* grammar, not this one launcher-owned flag), `dispatch` (routes a resolved verb straight to its handler in `crate::commands`) — 4 tests, including the literal set-equality proof the Verify criterion names. `crates/cli/src/main.rs` restructured around the two-stage flow the Decision above describes: `pre_composition_command` (stage one), `compose` (a real `Result`-returning composition step), the routing after `get_matches()` extended to check the installation half before falling to the semantic half or the legacy enum. `crates/cli/src/generated.rs`: `arg_for`/`verb_of` widened to `pub(crate)` — the installation half's tree builder binds `Binder`s and strips group prefixes the exact same way, not a second implementation of either rule. `crates/cli/src/cli.rs`: `Init`, `Status`, `Doctor`, `Restore`, `Dev` variants and the entire `DevCommand` enum deleted (tombstoned below). `crates/cli/src/commands.rs`: the five deleted variants' arms removed from `dispatch`; the `init`/`status`/`doctor`/`backup_cmd`/`dev_office_cmd` modules widened to `pub(crate)` (their handler bodies untouched — same functions, same behavior, only reachability changed); `dev_office_cmd`'s dead `DevCommand`-shaped `dispatch` wrapper removed, its three inner functions (`status`/`admit`/`revoke`) widened to `pub(crate)` in its place. `crates/core/src/invocable_bootstrap.rs`: `register_status` deleted along with its call in `bootstrap`; `register_version`'s comment updated to explain why it stays while `core:status` moved. `crates/core/tests/invocable_invariants.rs`: the `core:status` assertion removed (the test's actual point — a core invocable and a contribution sharing one registration door — is still fully proven by the `core:version` assertion that remains). `cargo test -p cronus-cli` unit suite 47/47 (43 prior + 4 new in `installation.rs`); the separate `cli_smoke.rs` suite's all 41 tests passing **unmodified**, both counted and content-identical to before this task; `cargo test -p cronus-core` 5/5 in `invocable_invariants` (same count — one assertion removed from an existing test, not a test removed) plus all pre-existing green; `cargo clippy`/`cargo fmt --check` clean on every touched crate; `cargo check --workspace` clean. Manually exercised the real binary end to end: `init`/`doctor`/`doctor --fix`/`restore`/`dev status|admit|revoke`, `--format json` read correctly both before and after the verb, a bad flag on a recognized installation verb exiting 2 before anything ran, an unrecognized top-level word still correctly falling through to compose-then-refuse at exit 2 (matching the pre-existing `unknown_command_exits_2` test unmodified). **A pre-existing, unrelated defect surfaced by the full-workspace run, not by this task's own changes**: `crates/core/tests/role_catalog.rs`'s `list_hired_returns_all_instances`/`hire_with_custom_name` fail under parallel `cargo test --workspace` but pass 17/17 clean with `--test-threads=1` — reproduced with zero files this task touched, recorded as `ROLE_CATALOG_PARALLEL_TEST_RACE` rather than fixed here (out of scope, and the remedy — per-test isolated state directories — is the same shape as the Phase 17 test-isolation fixes, not a new pattern to invent under this task's own pressure).

### [T-27D01.1.2] Migrate the remaining six installation groups onto the generated parser

- **Spec:** l2-cli.md §4.1.1, §4.2; l2-invocable-registry.md §4.7.1
- **Status:** Done (5 of 6 groups + the `Binder` extension they needed; `ext`'s 3-level nesting split to T-27D01.1.2.2)
- **Assignment:** Agent
- **Scope:** Add `workspace`, `registry`, `backup` (its `list`/`create` subcommand pair — `restore` already migrated separately in T-27D01.1, sharing the same underlying handler module), `activation`, `archetype`, and `ext` (with its own nested `skill` sub-group — the deepest nesting this half will need) to `installation::declared_invocables`/`build_installation_tree`, following exactly the pattern T-27D01.1 proved: real existing handler functions in `crate::commands`, reached directly, no `Dispatcher`/`Outcome` indirection. Delete each group's variant from the compile-time enum once migrated, with a tombstone.
- **Verify:** `cargo test -p cronus-cli` green; `cli_smoke.rs` passes **unmodified**; the set-equality test in `installation.rs` extends to cover all eleven groups; `cronus --help` lists exactly the eleven installation groups plus whatever the semantic half currently ships (this is where the installation half's own "exactly" property finally closes, mirroring T-27D01.2's parallel closure on the semantic side). `clippy -D warnings` + `fmt --check` clean. **Satisfied for the 5 migrated groups**; the "exactly eleven" clause still waits on `ext` (T-27D01.1.2.2).
- **Handoff:** T-27D01.1.2.2, T-27D02, T-27D03.
- **Decision (recorded, not asked):** **Verifying the six groups' actual shapes against the source (as this task's own Notes demanded) found that four of the six needed something no prior task had built**: `workspace create`'s `--name`/`--path`, `backup create`'s `--to`, `activation enable`'s `--mode`, and `archetype create`'s `--from` are all **named value flags** (`--flag <value>`), a shape `Binder` could not express — every binder so far was either positional or (for `Flag`) a presence-only named bit. Rather than defer all four groups again under the same "not yet designed" reasoning T-27D01.2 already used once, **designed and built the extension this pass**, since it was blocking most of what remained on *both* halves, not just this task's own slice. Added `BinderKind::NamedText`: a text value bound as `--name <value>` rather than positionally, modeled the same way `Flag` already conflates a value's shape with its binding style (a `Flag` is always named too) — not a fourth independent axis on every kind, only the two that actually need one. Carries **no default-value slot of its own**: an absent optional `NamedText` reads back as `None` from `ArgValues`, and the handler supplies its own default exactly the way an absent positional `Text` binder's caller already does (`workspace create` with no `--name` falls back to the id, matching the pre-existing behavior byte-for-byte). `activation enable --mode`'s value is validated by hand against `"login"`/`"system"` in `installation::dispatch` rather than through clap's own `ValueEnum` machinery (which only applies to statically-derived args, not this dynamically-built tree) — an unrecognized mode is an **application**-level refusal (exit 1, the same "ran and refused" shape every other handler in this module already uses), deliberately not a clap-level usage failure (exit 2), since the flag itself parsed fine and dispatch genuinely ran before rejecting the value. **Two touch points outside `cronus-cli` were required and are both narrow**: `crates/contract/src/lib.rs` (the new enum variant) and `crates/domain/src/invocable/dispatch.rs`'s `matches_kind` (a `NamedText`-bound value is still a plain `ArgValue::Text` at runtime — only its command-line binding style differs — so binding must accept it exactly like a positional `Text`; this arm is **not compiler-enforced**, since `matches_kind` is a `matches!` over a value/kind tuple rather than an exhaustive match on `BinderKind` alone, so a dedicated test was added directly on `bind()` proving the mapping, since nothing else would catch its silent removal). `registry` needed **no extension at all** (five plain positional-`Text` verbs) and was migrated alongside the four that did, since leaving a zero-cost group undone while shipping the harder ones served no purpose. **`ext` is the one group deliberately not attempted**: its `skill` sub-group is a real third level of nesting (`ext skill import|create|status`) this module's two-level tree builder does not support, and inventing that extension under this task's own remaining pressure would repeat the exact mistake this whole discipline exists to avoid — split to **T-27D01.1.2.2** instead. **This extension also resolves most of T-27D01.2's own "needs a named-flag/default-value extension" blocker on the semantic side**, checked group by group: `board`'s `--actor` and `schedule`'s `--preset`/`--prompt` are optional `NamedText` with a handler-side default, exactly like `workspace create --name` here; `loop`'s `--max-iter` (currently `u32`) is satisfiable the same way `activation enable --mode` is — a required or optional `NamedText`, parsed by hand in the handler — needing no new `BinderKind`; `workflow`'s mutually-exclusive `--human`/`--compact` becomes two independent `Flag` binders with the exclusivity enforced by the handler rather than by clap's `ArgGroup`, a real, disclosable shift from a usage-level rejection to an application-level one if that group is migrated as-is. **`knowledge`'s repeatable `--collection` and `budget`'s `f64` limit remain genuinely unaddressed** — a repeatable named flag needs an `ArgValue`/`ArgValues` shape this extension does not add (`get_one` reads exactly one occurrence), and a float value needs its own `BinderKind` the same way `Integer` has one — noted in T-27D01.2's own record for whoever executes it next, not resolved here since neither is this task's own scope.
- **Changes:** `crates/contract/src/lib.rs`: `BinderKind` gained `NamedText`, doc comment widened to explain the kind-conflates-binding-style precedent `Flag` already set. `crates/domain/src/invocable/dispatch.rs`: `matches_kind` accepts `(ArgValue::Text(_), BinderKind::NamedText)`; new test `a_named_text_binder_accepts_a_text_value_exactly_like_a_positional_one` (also fixed one containment leak found in an adjacent pre-existing comment while touching this file, restated in plain language). `crates/cli/src/generated.rs`: `arg_for`/`invocation_from_matches` gained `NamedText` arms (bound `--name`, read back as `ArgValue::Text`) — the SAME mapping used for both halves, not reimplemented. `crates/cli/src/installation.rs`: `named_text` constructor; `declared_invocables` grew from 7 to 27 entries across five new groups (`workspace`, `backup`, `activation`, `archetype`, `registry`); `group_about` gained entries for all five; `dispatch` gained their routing, including `activation enable`'s hand-validated mode parse. 5 new tests: flat/nested rendering already covered generically by the extended set-equality test, plus two dedicated to the new binder kind (named-flag-never-positional, optional-absent-reads-as-none). `crates/cli/src/commands.rs`: `workspace`/`backup_cmd`/`activation_cmd`/`archetype_cmd`/`registry` modules and their handler functions widened to `pub(crate)`; each group's now-dead `*Command`-shaped `dispatch` wrapper deleted (handler bodies themselves untouched — same functions, same behavior, only reachability changed); `backup_cmd` gained thin `create`/`list` wrappers replacing the removed `dispatch`'s own state-root resolution. `crates/cli/src/cli.rs`: `Workspace`/`Backup`/`Activation`/`Archetype`/`Registry` variants and the `WorkspaceCommand`/`BackupCommand`/`ActivationCommand`/`ArchetypeCommand`/`RegistryCommand` enums deleted (tombstoned above); `ActivationModeArg` kept (still the value type `activation_cmd::enable` takes) with an updated comment explaining why. `cargo test -p cronus-cli` unit suite 49/49 (43 prior + 4 `NamedText`-proof tests + 2 workspace/set-equality extensions — the set-equality test itself required no new test, since it already iterates every declared invocable generically); `cli_smoke.rs` 41/41 **unmodified**; `cargo test -p cronus-domain` 488/488 (+1 for the new `matches_kind` test); `cargo test --workspace` green (one pre-existing, unrelated `role_catalog.rs` parallel-test race noted in T-27D01.1 did not even reproduce on this run — nondeterministic, already recorded, not this task's to fix); `clippy`/`fmt --check` clean on every touched crate; `cargo check --workspace` clean. Manually exercised the real binary: `workspace create/list/delete` with `--name`/`--format json`, `backup list`, `archetype list`, `activation status`, `activation enable --mode bogus` (exit 1, application refusal), `archetype create custom preset-a` (bare word instead of `--from`, exit 2, usage failure caught before any dispatch), `registry list/create/show`, `--help` listing all ten migrated groups. **Containment self-check clean** across all seven touched files.

### [T-27D01.1.2.2] `ext` (with its nested `skill` sub-group): the installation half's one remaining group

- **Spec:** l2-cli.md §4.1.1, §4.2; l2-invocable-registry.md §4.7.1
- **Status:** Done — the installation half's own "exactly eleven" property closes; the semantic half's parallel "exactly" clause still waits on T-27D01.2.2
- **Assignment:** Agent
- **Scope:** Extend `build_installation_tree` (or a sibling function, if a shared two-level builder cannot cleanly grow a third level) to support one level of sub-nesting, then migrate `ext`'s six flat verbs (`list`/`add`/`remove`/`scan`/`activate`/`deactivate`) plus its nested `skill` group (`import`/`create`/`status`) through it. Delete `Command::Ext`/`ExtCommand`/`SkillCommand` from the compile-time enum, with a tombstone.
- **Verify:** `cargo test -p cronus-cli` green; `cli_smoke.rs` passes **unmodified**; the set-equality test in `installation.rs` extends to all eleven installation groups and finally closes T-27D01.1/T-27D01.1.2's deferred "exactly eleven" clause; `clippy -D warnings` + `fmt --check` clean. **All satisfied.** The Verify text's own "nothing else" clause names the *combined* surface (installation + semantic); that fuller claim still waits on T-27D01.2.2's remaining 6 semantic groups and T-27D03's 5 stub-group removals — the eleven-groups clause this task actually owns is the one now closed, confirmed directly against `cronus --help`'s real output.
- **Handoff:** T-27D02, T-27D03.
- **Decision (recorded, not asked):** **Chose the dot-in-the-verb-tail signal over a generic N-level builder** — the same rule `verb_of` already uses to separate a group from its verbs (`"memory.store"` → group `memory`, verb `store`) extends unchanged one level deeper: a verb tail containing a second dot (`"ext.skill.import"` → group `ext`, verb-tail `skill.import`) names one sub-group (`skill`), not a fourth top-level command. This is additive to `build_installation_tree`'s existing nested-group branch (partition members into plain verbs and dotted-tail members, group the latter by their sub-group prefix, build one intermediate `Command` per distinct prefix) rather than a parallel generic recursive scheme — `ext` is still the only group that has ever needed a third level, so a fully general N-deep builder would be unexercised machinery built on spec. `installation::dispatch`'s signature changed from `(group, verb, matches)` (pre-split by the caller) to `(group, group_matches)` (the group's own raw matches): resolving the leaf verb now needs up to two `.subcommand()` calls depending on depth, and a verb never legitimately has a subcommand of its own (every verb's arguments are `Arg`s, never nested subcommands), so "does the first-level match have a further subcommand" is a safe, self-describing test for "was that a sub-group, not a leaf" — no hardcoded knowledge of which groups nest how deep leaks into `main.rs`'s two call sites, which simplified to passing the group's raw matches straight through. The composite verb key for dispatch (`"skill.import"`) matches `verb_of`'s own tail exactly, so the existing per-verb `match` arms extend by literal string rather than needing a new addressing scheme.
- **Changes:** `crates/cli/src/installation.rs`: `build_installation_tree`'s nested branch gained the plain-verb/sub-group partition (dot-in-tail signal); `dispatch` restructured into itself (now resolving up to 2 levels) plus a private `dispatch_leaf` (the original per-verb `match`, unchanged in shape) plus `application_error`; `declared_invocables` gained `ext`'s 6 flat verbs + 3 `skill.*` verbs (10 total for the group); `group_about` gained `ext`/`skill` entries; module doc corrected to state all eleven groups now live here. `crates/cli/src/main.rs`: both `installation::dispatch` call sites simplified to pass the group's raw `ArgMatches` directly (no pre-split). `crates/cli/src/cli.rs`: `Ext` variant and `ExtCommand`/`SkillCommand` enums deleted (tombstoned). `crates/cli/src/commands.rs`: `ext` module (and its nested `skill` submodule) widened to `pub(crate)`, dead `ExtCommand`/`SkillCommand`-shaped `dispatch` wrappers removed, handler bodies untouched. The existing set-equality test's `parser_verbs` computation extended to walk one extra level when a "verb" itself has subcommands (composing the same `"skill.import"`-shaped key the catalog side already produces via `verb_of`) — it would otherwise have recorded `("ext", "skill")` as if `skill` were a verb and failed against the catalog's `("ext", "skill.import")` etc. One new dedicated test proving the 3-level shape end to end: parses `ext skill create --prompt`, confirms the sub-group and leaf both resolve with the right binder, then calls `dispatch` directly and confirms it reaches the composite verb (not the sub-group's own name). `cargo test -p cronus-cli` unit suite 47/47 (46 prior + 1 new); `cli_smoke.rs` 41/41 **unmodified**. Full `cargo test --workspace` (78 result-sets) green; `clippy --workspace -D warnings` clean; `cargo fmt --all -- --check` clean. Manually verified the real binary: `ext list`, `ext --help`/`ext skill --help` showing the correct 3-level structure, `ext skill status` (no id), `ext skill create --prompt`, `ext scan` on a missing path (exit 1). `cronus --help` confirmed to list exactly the eleven installation groups (`init`, `status`, `doctor`, `restore`, `dev`, `workspace`, `backup`, `activation`, `archetype`, `registry`, `ext`) alongside the seven currently-generated semantic groups — the remaining old-enum groups (6 semantic awaiting T-27D01.2.2, 5 stub groups awaiting T-27D03) are exactly the ones still expected to be there. **A genuine host memory constraint encountered mid-task, not a code defect**: PowerShell itself crashed (`OutOfMemoryException`) and later refused to start a thread while running the full workspace test suite, tracked to genuinely low system memory (under 700 MB free) rather than any build corruption — confirmed by `cargo check --workspace` staying clean throughout and the identical `cargo test --workspace` command eventually succeeding on retry with no code change. Containment self-check clean across all four touched files.

### [T-27D01.2] Migrate the remaining 12 real semantic groups onto the generated parser

- **Spec:** l2-cli.md §4.1, §4.1.1, §4.3, §4.4
- **Status:** Done (6 of 12 groups — every one needing no `Binder` extension; the remaining 6 split to T-27D01.2.2)
- **Assignment:** Agent
- **Scope:** Register each group's invocables through the facade's own registration door (matching `memory`'s pattern exactly: real `Outcome`-returning handlers wrapping the group's existing domain calls, not the old print-and-return-code functions), delete each group's arm from the compile-time enum once migrated, add its tombstone, extend the interim per-verb renderer (or whatever `T-27D02` has landed by the time this runs — check first) for its output shapes. Split further by sub-`.N` id if a single pass across all 12 still proves too large; that decision belongs to whoever executes this, informed by how the first few groups actually go.
- **The 12, split by what they need from `Binder` today — verify this list against the source before trusting it, group implementations may have moved:**
  - **No `Binder` extension needed (migrate with the mechanism exactly as built):** `codegraph` (Index/Search, plain text), `agent` (Constitution/Status, no args), `role` (List/Hire/Show/Create/Fire — its one bool flag, `--presets`, is exactly what the `Flag` fix in T-27D01 now handles correctly), `exec` (List/Create/Finalize/Discard, plain text), `check` (Run/Show/History, one `Option<PathBuf>` — already representable as optional `Text`), `learn` (List/Approve/Reject, plain text). Migrate these first — they prove the mechanism scales to a second, third, … group with zero new design questions.
  - **Partly unblocked by T-27D01.1.2's `BinderKind::NamedText`, checked group by group before trusting it — read that task's own Decision block first:** `board` (`--actor`), `schedule` (`--preset`/`--prompt`), and `loop` (`--max-iter`) are now satisfiable with an optional `NamedText` plus a handler-side default (exactly `workspace create --name`'s own pattern) or a hand-parsed value (exactly `activation enable --mode`'s own pattern) — no further `Binder` work needed for these three. `workflow`'s mutually-exclusive `--human`/`--compact` is satisfiable as two independent `Flag` binders, but only by moving the exclusivity check from clap's own `ArgGroup` (a usage-level rejection today) into the handler (an application-level one) — a real, disclosable behavior shift if this group migrates as-is, not a free unblock. **Still genuinely unaddressed, needs its own design:** `knowledge`'s *repeatable* `--collection` (an `ArgValue`/`ArgValues` shape carrying more than one occurrence does not exist yet — `NamedText` reads exactly one) and `budget`'s **float**-carrying value (`Set`'s limit is `f64`; the current kind set has `Integer` but no float counterpart). **Do not invent the repeatable/float design under this task's own pressure** — the same reasoning T-27D01 used to defer the whole extension once applies unchanged to what remains of it; resolve it as its own small, deliberate decision before migrating `knowledge` or `budget`.
- **Verify:** `cargo test -p cronus-cli` green; `cli_smoke.rs` passes **unmodified** for every test not specifically about a migrated group's own behavior (a group's smoke test may need updating only if its *observable* CLI behavior is intentionally unchanged but its internal path changed — confirm output is byte-identical before touching any smoke assertion); once all 12 are migrated, `cronus --help` lists exactly the registry's shipped `Semantic` groups plus whatever the installation half owns, and nothing else (this is where T-27D01's own deferred "exactly" clause finally closes). `clippy -D warnings` + `fmt --check` clean. **Satisfied for the 6 migrated groups**; the "exactly" clause still waits on the other 6 (T-27D01.2.2).
- **Handoff:** T-27D01.2.2, T-27D02, T-27D03.
- **Decision (recorded, not asked):** **The facade's registration file was split into one submodule per group before adding six more registrants to it.** `invocable_bootstrap.rs` (a single flat file) held `status`+`memory` cleanly at ~250 lines; six more groups pulling in six different `cronus_domain`/adapter modules would have made it a mixed-concern dumping ground exactly one file. Converted to `invocable_bootstrap/mod.rs` (orchestrator + shared `text_arg`/`opt_text_arg`/`flag_arg`/`core_id` helpers) plus one file per group — the project's own "small, well-named modules" convention applied to the facade the same way it already applies everywhere else, not an exemption for this one file. **A real, previously-shipping bug was found and fixed, not merely avoided going forward**: `generated::invocation_from_matches` resolved a matched group's verb by searching the *entire* flat semantic descriptor list rather than the matched group alone, so once a second group declared a same-named verb (`role.list` alongside the newly-added `exec.list`/`learn.list`), the alphabetically-first group silently won — `role list --presets` was actually dispatching `core:exec.list`, dropping `--presets` on the floor and reporting an empty list instead of the 25-item preset catalog. Invisible before this task because `memory` (T-27D01) shared no verb name with anything else; caught by manually exercising the real binary (not by any existing test — the two existing `invocation_from_matches` tests each registered exactly one invocable, so neither could have observed a cross-group collision). Fixed by scoping the search to the matched group (`invocable.group == group`), which the caller now supplies; a dedicated regression test registers two groups sharing a verb name and asserts the *matched* group's own invocable resolves, deliberately ordered so the bug's own alphabetical-precedence shape would have caught it. **Ephemeral-per-call preserved uniformly, not improved selectively**: `codegraph`/`exec`/`learn`'s underlying stores are exactly as ephemeral as `memory`'s (a fresh instance per dispatch) — a `Mutex`-shared instance across dispatches was drafted for `exec` (zero CLI-observable difference, since one CLI process only ever dispatches once) and then reverted in favor of matching `memory`'s already-recorded residual exactly, so this phase's ephemeral-store class stays one consistent, disclosed shape rather than some groups quietly getting better treatment than others under this task's own pressure. **`role`'s write verbs (`hire`/`create`/`fire`) are not covered by new facade-level tests**: their handler resolves the real, un-overridable OS state directory (unchanged from the pre-migration CLI code), and every other test in `invocable_invariants.rs` — including the new ones this task adds — touches no real disk; extending that file's scope to cover `role`'s writes would be new coverage this migration does not need to add, not a gap it leaves behind (the pre-migration suite never covered `role hire` end-to-end either). The interim renderer (`crates/cli/src/main.rs`) was **generalized, not extended arm-by-arm**: the six new groups' output shapes (an arbitrary `Record` like `codegraph.index`'s `{path, symbols}` or `check.run`'s `{card, language}`, plain `Integer`/`Boolean`) have no bespoke prior CLI text to preserve — no `cli_smoke.rs` test ever asserted on their exact wording — so a genuinely recursive `render_json`/`render_text_line` mapping replaces the old per-shape special-casing's catch-all error arm, while the two arms with real, disclosed prior shape (`memory`'s single-id "Ok: …", any `List`'s JSON/empty-text handling) are kept exactly as they were.
- **Changes:** `crates/core/src/invocable_bootstrap/` (new directory, replacing the flat file): `mod.rs` (orchestrator, shared helpers), `status.rs`/`memory.rs` (moved, unchanged logic), `codegraph.rs`, `agent.rs`, `role.rs`, `exec.rs`, `check.rs`, `learn.rs` (new) — 19 new verbs registered as real `Outcome`-returning handlers over the actual domain calls (`cronus_codegraph::index::CodeIndex`, `cronus_domain::roles::RoleManager`, `cronus_domain::exec_workspace::ExecWorkspaceManager`, `cronus_domain::quality::{GateResultStore, detect_language}`, `cronus_domain::learning::LearningApprovalGate`, `cronus_domain::constitution`). `crates/core/Cargo.toml`: added `cronus-codegraph`. `crates/cli/src/cli.rs`: `Codegraph`/`Agent`/`Role`/`Exec`/`Check`/`Learn` variants and their six `*Command` enums deleted (tombstoned). `crates/cli/src/commands.rs`: their dispatch arms and module bodies deleted. `crates/cli/src/generated.rs`: `invocation_from_matches` gained a `group: &str` parameter and scopes its search to it (the collision fix); one new regression test. `crates/cli/src/main.rs`: `render_value` generalized into a real recursive `OutcomeValue` → JSON/text mapping (`render_json`/`render_text_line`/`render_text_inline`), replacing the old hard-coded `{id,title}`-only `List` handling and the "no renderer for this shape" error arm; the memory-specific single-id shape kept verbatim. `crates/core/tests/invocable_invariants.rs`: 6 new tests (one per migrated group, each proving the Outcome-shape distinction that actually matters — zero-item list vs. empty result, not-found vs. silent success, a real multi-field record). `cargo test -p cronus-cli` unit suite 46/46; `cli_smoke.rs` 41/41 **unmodified** (its own `role_list_presets_exits_0`/`exec_list_exits_0`/`check_run_exits_0`/`learn_list_exits_0` assertions — exit-code-only before this task — now also happen to exercise correct content, though that was never their contract); `cargo test -p cronus-core` 44/44 (38 pre-existing + 6 new); `cargo test -p cronus-domain` 488/488 unaffected. Full `cargo test --workspace` clean. `clippy`/`fmt --check` clean on every touched crate. Manually exercised the real binary for all six groups, `--format json` and text, including the `role list --presets` fix and the pre-existing `learn approve <unknown>` exit-1 refusal. **A genuine host-level build instability found and recorded, not a code defect**: `cargo test` with this host's default full-core parallelism intermittently crashes `rustc.exe` (`STATUS_STACK_BUFFER_OVERRUN`), producing cascading `error[E0786]`/`E0463` output that reads like build-artifact corruption but is not — confirmed by `cargo check` succeeding throughout and by the identical `cargo test` command succeeding once parallelism was capped (`-j 2`); saved to persistent memory (not `.design/`) for future sessions on this host, since it is an environment fact, not a project one. Containment self-check clean across all sixteen touched files.

### [T-27D01.2.2] Migrate the remaining six semantic groups needing a `Binder` extension

- **Spec:** l2-cli.md §4.1, §4.1.1, §4.3, §4.4
- **Status:** Done
- **Assignment:** Agent
- **Scope:** `board`, `schedule`, `loop` are now satisfiable with the existing `BinderKind::NamedText` (T-27D01.1.2's own extension) — verify each against the source, then migrate through the facade's registration door exactly like T-27D01.2's six groups. `workflow` is also satisfiable with `NamedText` + two independent `Flag` binders for `--human`/`--compact`, but only by moving `Transpile`'s mutual-exclusivity check from a clap-level usage rejection into an application-level one — a real, disclosable behavior change to record explicitly if this group migrates, not a silent one. `knowledge` (a repeatable named flag) and `budget` (a float-carrying value) each need a genuinely new `Binder`/`ArgValue` shape neither `NamedText` nor anything else in this phase has built — design that shape deliberately, as its own small decision, before migrating either.
- **Verify:** `cargo test -p cronus-cli` green; `cli_smoke.rs` passes **unmodified** for every test not specifically about a migrated group's own behavior; once all 12 semantic groups are migrated, `cronus --help` lists exactly the registry's shipped `Semantic` groups plus whatever the installation half owns, and nothing else — this is where T-27D01's own deferred "exactly" clause on the semantic side finally closes. `clippy -D warnings` + `fmt --check` clean. **Satisfied**: `cargo test -p cronus-cli --all-targets` green including `cli_smoke.rs` 41/41 **unmodified**; full `cargo test --workspace --exclude cronus-desktop` green; `clippy`/`fmt --check` clean workspace-wide.
- **Handoff:** T-27D02, T-27D03.
- **Notes:** Check `generated::invocation_from_matches`'s group-scoping fix (T-27D01.2) is still in place before assuming cross-group verb-name safety — `board`/`schedule`/`loop`/`workflow`/`knowledge`/`budget` and the already-migrated groups are likely to share more verb names (`list`, `show`, `set`, `run`) than the two that first exposed the bug. It was, and still is; no second occurrence of that bug's shape surfaced this task.
- **Decision (recorded, not asked):** **Two `Binder`/`ArgValue` shapes designed and added, exactly as scoped, no earlier.** `BinderKind::Float` + `ArgValue::Float(f64)` (bound via `clap::value_parser!(f64)`, a genuine clap-level usage gate) for `budget set`'s dollar limit; `BinderKind::RepeatableNamedText` + `ArgValue::List(Vec<String>)` (bound via `ArgAction::Append`) for `knowledge query --collection`, deliberately scoped to `Vec<String>` since no group needs a richer repeatable shape today. Adding `Float(f64)` broke the `#[derive(Eq)]` already on `ArgValue`/`ArgValues`/`Invocation` (`f64` has no total equality) — dropped `Eq` from all three, keeping `PartialEq`, after confirming by search that nothing used any of the three as a hash-map/hash-set key (no `Hash` derive existed either); a deliberate, narrow API reduction, not an oversight. **`workflow transpile`'s `--human`/`--compact` exclusivity moved from clap's `conflicts_with` into the handler** exactly as the prior task's own note anticipated — a disclosed usage→application exit-code shift (2 → 1 on conflict). The same class of shift applies to `loop run --max-iter` and `knowledge query --top-k`: both were clap-typed (`u32`/`usize`) before, so a malformed value was a usage failure; as hand-parsed `NamedText`, the same input is now an application-level `Outcome::Unavailable` — disclosed in code comments at both sites, not silently changed. **`workflow run`'s and `loop run`'s data-dependent exit codes needed a renderer mechanism the interim renderer did not yet have**: added a generic arm recognizing any `Record` carrying a `status` field of `"failed"|"aborted"|"stopped"|"paused"` as exit 1 — this deliberately collapses `workflow run`'s pre-migration three-way scheme (ok/partial → 0, failed/aborted → 1, paused → 2) into two-way, disclosed rather than fixed since no existing test locks the paused-specific exit-2 case. `loop run` needed a *more specific*, ordered-first bespoke arm instead of the generic one, because its exact stdout text (`"loop {run_id}: done ({n} iteration(s))"` / `"loop {run_id}: {status} ({reason})"`) is real, tested, shipped output — `cli_smoke.rs`'s own existing test parses that text to extract a run id and drive a further `log`/`show` round trip, so it could not be freely reshaped. **`nodus::executor::Value` (workflow execution's own value type) turned out structurally isomorphic to `OutcomeValue`** (Null/Bool/Int/Text/List/Map lining up one-for-one with Empty/Boolean/Integer/Text/List/Record) — wrote a direct recursive converter, with `Float` formatted as `Text` (the same workaround `learn`'s confidence field and `budget`'s dollar amounts already use, since `OutcomeValue` carries no float variant). **Two real, pre-ship bugs were caught by running the existing, unmodified `cli_smoke.rs` suite** (not by the new unit tests, which — written by the same hand that wrote the bug — caught neither): `loop run`'s `file` binder was declared as a positional `Text` when the pre-migration grammar bound it as a named `--file` flag, breaking a real smoke test with `unexpected argument '--file' found`; and `loop evolve`'s rewritten unavailability message dropped the literal word "unavailable", breaking a smoke test asserting `stderr.contains("unavailable")`. Both fixed, then the full `cli_smoke.rs` suite re-run and confirmed green — this is the second time in this phase this exact discipline (treat the pre-existing suite as ground truth, not the new tests) caught a real defect before ship. **Known pre-existing residuals preserved exactly, not touched**: `schedule list`'s store-open failure still renders as an empty list at exit 0 (one of the phase's own four named residuals); `board`/`schedule`/`knowledge` all resolve the real, un-overridable OS state directory with no new facade-level write-path tests added, matching `role`'s already-recorded precedent that extending test coverage to a write path a migration does not itself need is out of this task's scope. `workflow transpile`'s previously-discarded `--format` flag is now honored as a natural side effect of moving onto the shared render pipeline — one of the phase's nine known format-discarding sites closes on its own, not as a deliberate out-of-scope fix. **Verified the one open question from manual end-to-end testing before closing this task**: `knowledge query`'s "embedding failed: Timeout" result traces to `KnowledgeService::open_default` always wiring a real local embedding endpoint (Ollama convention, contacted lazily per call) — pre-existing infrastructure the old `knowledge` command called identically, unrelated to this migration, not a regression. A containment self-check across every file this task touched found one real leak (a bare task id in a test doc comment) and fixed it in place.
- **Changes:** `crates/contract/src/lib.rs`: `BinderKind::Float`/`RepeatableNamedText`; `ArgValue::Float(f64)`/`List(Vec<String>)`; `Eq` dropped from `ArgValue`/`ArgValues`/`Invocation` (kept `PartialEq`). `crates/domain/src/invocable/dispatch.rs`: `matches_kind` gained the two new arms; one new test proving both accept their shape and reject the wrong one. `crates/cli/src/generated.rs`: `arg_for`/`invocation_from_matches` gained matching arms for both new kinds. `crates/core/src/invocable_bootstrap/mod.rs`: six new group modules declared and registered (`board`, `schedule`, `budget`, `loop_group` — named for the `loop` reserved keyword, the registered `group` string stays `"loop"` — `workflow`, `knowledge`), plus shared `float_arg`/`list_arg` helpers. Six new files: `board.rs` (List/Show/Add/Move/Block/Done/Archive over `cronus_domain::kanban`), `schedule.rs` (List/Add/Delete/Run over `cronus_domain::scheduler`), `budget.rs` (Show/Set/Reset over `cronus_domain::budget`), `loop_group.rs` (Run/Evolve/Log/Show over `cronus_domain::loop_runner`), `workflow.rs` (Scaffold/Validate/Run/Transpile over `nodus`, including the `Value`→`OutcomeValue` converter), `knowledge.rs` (Collection-Create/Add/Add-Url/Query over the facade's own knowledge service). `crates/cli/src/cli.rs`: the six corresponding `Command` variants and their `*Command` enums deleted. `crates/cli/src/commands.rs`: their dispatch arms and module bodies deleted. `crates/cli/src/main.rs`: `render_value` gained the bespoke `loop run` arm (ordered first) and the generic status-field exit-code arm (ordered after it). `cargo test -p cronus-cli --all-targets` green (`cli_smoke.rs` 41/41 unmodified); `cargo test --workspace --exclude cronus-desktop -j 2` green across every crate; `cargo clippy --workspace --exclude cronus-desktop --all-targets -- -D warnings` clean; `cargo fmt --all --check` clean. Manually exercised the real binary for all six groups (list/show/add/set/run, a clap-level float usage failure, `loop run`'s stopped-exit-1 shape, `workflow --help`), confirming output matches the design intent.

### [T-27D02] One renderer over the structured outcome; uniform output-format handling

- **Spec:** l2-cli.md §4.2
- **Status:** Todo
- **Assignment:** Agent
- **Verify:** `cargo test -p cronus-cli` green; a test drives every shipped invocable through both output formats and asserts the requested format is honoured in **every** case; existing `crates/cli/tests/cli_smoke.rs` passes **unmodified** (behaviour preservation).
- **Handoff:** T-27D03.
- **Notes:** Rendering becomes one function of `Outcome` plus the requested format, replacing per-command decisions. **Record, do not fix:** the nine sites that discarded the format flag and the five that built structured output by unescaped string formatting are residuals — the renderer must reproduce today's observable output, including its defects, and Phase 29 corrects them separately. **Depends on T-27D01.2 for its own "every shipped invocable" claim to mean what it says** — T-27D01 alone only migrated `memory` onto the registry, so a renderer built before T-27D01.2 lands would honestly cover just that group's shapes (already prototyped, informally, as the interim `render`/`render_value` in `crates/cli/src/main.rs` — read that before starting; it is exactly the shape this task replaces, not a competing implementation to reconcile with).

### [T-27D03] Shipped-surface honesty: the five unbound groups leave the default surface; corpus registration

- **Spec:** l2-cli.md §3 (INV-9), l2-surface-conformance.md §4.5
- **Status:** Todo
- **Assignment:** Agent
- **Verify:** `cronus --help` and completion list **no** verb that answers "not implemented"; `cargo test -p cronus-cli` includes the conformance harness invoked against this surface's real projection.
- **Handoff:** T-27T01.
- **Notes:** `goal`, `trigger`, `mission`, `research`, `change` are unbound — they have no descriptor and therefore cannot appear. Their **behaviour is out of scope** for this phase; only their surface treatment changes. Register against the corpus here, before the second projection exists — registration is an order of magnitude cheaper before a surface has behaviour to preserve.

### [T-27T01] Corpus first run — convert failures into findings before fixing anything

- **Goal:** Run the corpus against the migrated command-line surface and treat the output as the initial inventory.
- **Method:** Execute the harness; expect it to fail immediately and unflatteringly. **Convert every failure into a finding record first**, then decide what is repaid in this phase and what is carried. Fixing on sight produces repairs that are rediscovered later as duplicates.
- **Verify:** Every corpus failure is either a closed finding with all four SP-4 conditions met, or an entry in the shrink-only debt ledger with a stated reason. No failure is silently fixed and unrecorded.
- **Status:** Todo

### [T-27T02] Behaviour-preservation proof and full quality gates

- **Goal:** Prove the migration changed structure and not behaviour, and that the phase meets the project's definition of done.
- **Method:** Assert pre-existing command-line tests pass **unmodified**; confirm the four known residuals are recorded at their owning invocables and still behave as before; run the workspace gates.
- **Verify:** `cargo fmt --all --check`, `cargo clippy --all-targets -- -D warnings` (zero), `cargo test --workspace` green across consecutive full runs; `crates/cli/tests/cli_smoke.rs` unmodified and passing; no `unwrap()`/`panic!()` introduced on production paths.
- **Status:** Todo
