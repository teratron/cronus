---
phase: 29
name: "Desktop Generic Dispatch"
status: Todo
subsystem: "apps/desktop/tauri · packages/ui"
requires: [27, 28]
provides: []
key_files:
  created: []
  modified: []
patterns_established: []
duration_minutes: ~
---

# Stage 29 Tasks — Desktop Generic Dispatch

**Phase:** 29
**Status:** Todo
**Strategic Goal:** Carry the desktop shell onto the registry the prior two phases proved — one generic `invoke`/`catalog` IPC pair replaces the per-capability handler list (finding F-1's third site), the shell's own action registry sources every `Semantic` action from the catalog by identity (AS-6), and the shared conformance corpus proves itself runnable from a genuinely **detached** Cargo workspace for the first time (F-7 repaid, F-1 fully closed).

## Phase Notes

**This phase was renarrowed at planning time, and the correction is recorded in `PLAN.md`, not only here.** Its inherited strategic draft claimed "the four residual corrections" as this phase's own closing work. Grounding against the real source before decomposition — the same discipline that renarrowed T-27D01 mid-Phase-27 — found none of the four are reachable from this phase's own subsystem: "unavailability as emptiness" owns ~15 sites in `crates/core/src/invocable_bootstrap/*.rs`, shared identically by all three frontends; the output-format flag and unescaped structured output are CLI-only (`l2-surface-conformance.md` finding F-6 names its sites as exclusively `crates/cli`); and the empty-secret-list fix needs a project-wide secret-*enumeration* primitive (`crates/domain/src/secrets.rs` currently offers only a single-key `get`) that does not exist yet for any surface to consume. All four stay recorded exactly where they already are — `l2-surface-conformance.md` §4.7 and the accepted-debt ledger — open, with their reasons stated, owned by whichever later phase actually touches their real sites. This phase's own real scope is the generic-dispatch bridge collapse, the action-registry locus split, and the corpus's first cross-workspace registration.

**A second, disclosed exception surfaced during the same grounding pass.** `core:version` and `core:status` are `Locus::Installation` (`crates/core/src/invocable_bootstrap/status.rs`; `status` itself lives only in the command line's own installation grammar). Installation is a third locus this surface — like the terminal UI — does not project; neither of `l2-application-shell.md` §4.3's two admission classes (`Semantic` via `invoke()`, `HostOnly` via its own method) covers it as currently classified. The shell's existing bespoke `capability_version`/`capability_status` bridge methods (`apps/desktop/tauri/src/bridge.rs`) stay exactly as they are, untouched by Track A — a disclosed exception this phase does not resolve, not a silent regression and not a locus reclassification this phase is positioned to decide (that is `/magic.spec` scope, if it is ever decided at all).

**The shape this phase exists to prove is structural, not behavioural.** `apps/desktop/tauri` already depends on `cronus-core` (a real path dependency, confirmed in its `Cargo.toml`) and already declares its own detached `[workspace]` table. `cronus_core::invocable_bootstrap::bootstrap()` — the identical composition door CLI and TUI each call — is therefore already reachable; Track A's own work is wiring a registry+dispatcher pair into the existing `Bridge`, not inventing a new door. The genuinely new structural fact is Track C: `cronus-conformance` has never been linked from *outside* the main `crates/` workspace before, and `l2-surface-conformance.md`'s own stated reason the corpus is shaped as a library rather than one central suite (§2, §4.4) has never actually been exercised until this phase proves it.

**Behaviour-preservation oracle carries an environmental caveat, stated up front rather than discovered mid-task.** Phase 26's own closing record found `cargo test settings` cannot *run* on this windows-gnu host (`STATUS_ENTRYPOINT_NOT_FOUND` at test-binary load via the WebView2Loader import chain) even though it compiles cleanly — confirmed pre-existing and environmental, not a code defect. `bridge.rs`'s own existing unit tests (`version_passes_through_the_core_value`, `status_masks_known_secrets_via_core_redaction`, `production_bridge_reports_the_embedded_core_status`) are plain Rust `#[test]` functions with no Tauri mock-runtime dependency, so they are expected to *run*, not merely compile — new tests this phase adds to `bridge.rs` should follow that same shape. Any test that would need Tauri's own mock IPC round-trip stays a compile-only proof on this host, exactly as `bridge.rs`'s own module doc already discloses for command *registration* itself (verified at compile time via `tauri::generate_handler!`).

**Critical path.** Track A is the foundation: B sources actions from the catalog A projects, C proves the corpus against A's real dispatch path. Land A behind its own tests before opening B or C.

## Atomic Checklist

- [x] [T-29A01] The bridge composes the shared registry and dispatcher; local redaction gives way to the boundary
- [x] [T-29A02] The TypeScript client gains `catalog()`/`invoke()`; the executable face cannot cross the seam
- [x] [T-29A03] A stale local catalog refreshes on `Unknown`, rather than rendering a failure
- [x] [T-29B01] The action registry sources `Semantic` actions from the catalog by identity
- [x] [T-29B02] The palette's semantic entries render from the catalog projection, not a hand-written list
- [x] [T-29C01] Corpus registration from the detached workspace; findings F-1 and F-7 close
- [ ] [T-29T01] Behaviour-preservation proof and full quality gates

## Detailed Tracking

### [T-29A01] The bridge composes the shared registry and dispatcher; local redaction gives way to the boundary

- **Spec:** l2-application-shell.md §4.3, §3 (AS-6, INV-7)
- **Status:** Done
- **Assignment:** Agent
- **Scope:** `apps/desktop/tauri/src/bridge.rs`'s `Bridge<C>` composes via `cronus_core::invocable_bootstrap::bootstrap()`, holding an `InvocableRegistry`/`Dispatcher` pair — the identical composition door the CLI and TUI already use, no private path. Two new Tauri commands, `capability_catalog` (→ every `Semantic`+`ClientLocal`+`Shipped` descriptor, serialized) and `capability_invoke` (→ dispatch one `Invocation`, masked `Outcome` back). `Bridge::mask`'s own local redaction call is removed in favour of the shared dispatcher boundary — the same INV-7 asymmetry TUI's own T-28A02 already closed. `capability_version`/`capability_status` are untouched (the disclosed exception the Phase Notes name) — do not fold them into `invoke()`, and do not remove them.
- **Verify:** `cargo build -p cronus-desktop` (PowerShell, per the project's own native-build discipline) succeeds. A new `cargo test -p cronus-desktop` unit test (plain `#[test]`, no Tauri mock runtime, matching `bridge.rs`'s existing shape) drives a real registered fixture invocable through `capability_invoke`'s underlying function and asserts the returned `Outcome` is masked exactly like the pre-existing `status_masks_known_secrets_via_core_redaction` test already proves for `status()`. `rg 'redact::redact' apps/desktop/tauri/src/bridge.rs` shows no local masking call remaining outside the shared dispatcher's own boundary. **Satisfied**: `cargo build -p cronus-desktop` (PowerShell) succeeds; `cargo clippy -p cronus-desktop --all-targets -- -D warnings` and `cargo fmt --all -- --check` (both crates) clean. `rg 'redact::redact' apps/desktop/tauri/src/bridge.rs` returns exactly one hit, inside `mask()` — the method `version()`/`status()` alone still call; `invoke()` calls neither `mask()` nor `redact::redact` anywhere, relying entirely on `Dispatcher::set_secrets`'s own boundary — satisfying the literal wording ("no local masking call remaining **outside** the shared dispatcher's own boundary") without contradicting the Scope's own "do not remove them" for `capability_version`/`capability_status`. `cargo test -p cronus-desktop --lib` cannot *run* on this host for the same disclosed, pre-existing reason recorded at Phase 26's own close (`STATUS_ENTRYPOINT_NOT_FOUND` at `cronus_desktop_lib`'s test-binary load, WebView2Loader import chain) — confirmed by the archived phase-26 record itself, which reproduced the identical failure with every `src` change stashed; this phase's own Notes text asserting these tests are "expected to run, not merely compile" is corrected here against that concrete prior evidence, not trusted at face value. The new test compiles clean under both `cargo build` and `cargo clippy --all-targets`.
- **Handoff:** T-29A02, T-29A03, T-29B01, T-29C01.
- **Notes:** `capability_version`/`capability_status` keep their own existing tests unchanged — this task adds to `bridge.rs`, it does not touch those two.
- **Decision (recorded, not asked):** **Two separate `Engine::new()` calls, not one shared handle.** `Engine` is currently a zero-state placeholder (`_private: ()`) that `bootstrap()` consumes by value and never returns, so `Bridge<C>`'s own `core: C` field (used only by `version()`/`status()`) and the registry/dispatcher composition each need their own instance — exactly the shape the TUI's own `run()` already establishes (`bootstrap(Engine::new())` plus a separate `CapabilitySource::new(Engine::new())`), not a new pattern invented here. **`capability_invoke`'s wire payload is `id: String` + `args: HashMap<String, ArgValue>`, never a deserialized `Invocation` as a whole.** `Invocation.caller: Surface` is asserted server-side as `Surface::Desktop` and never accepted from the wire — a script could otherwise claim to be a different surface, and `Surface`'s own doc comment already treats "a new surface joining" as "a deliberate event, not something a caller declares for itself"; extended here from registry membership to per-call identity. This also sidesteps needing `Deserialize` on `InvocableId`/`ArgValues`/`Surface`/`Invocation` at all — only `ArgValue` needed it (added in `cronus-contract`, gated behind the crate's existing optional `serde` feature, alongside `Serialize` for a real round-trip test; the contract crate's own pre-existing `serde_tests` module doc comment had explicitly named this exact argument-marshalling half as "out of this crate's current scope" — brought into scope now, with that comment corrected in the same change so it does not go stale). **`is_projected` promoted from a TUI-local free function to `Invocable::is_projected(&self)` in `cronus-contract`.** The desktop's own `capability_catalog` needed the identical `Semantic|ClientLocal` + `Shipped` predicate the TUI's `command.rs` already had; duplicating it a second time would be the same class of defect Phase 28 spent an entire finding (F-2) repaying. `crates/tui/src/command.rs::is_projected` now delegates to the inherent method instead of restating the two `matches!` checks, so both surfaces read one definition.
- **Changes:** `crates/contract/src/lib.rs`: `ArgValue` gained `#[cfg_attr(feature = "serde", derive(Serialize, Deserialize))]`; new `impl Invocable { pub fn is_projected(&self) -> bool }`; `serde_tests` module doc comment corrected (the argument half is no longer "out of scope"); new round-trip test `every_arg_value_variant_round_trips_from_json`. `crates/tui/src/command.rs`: `is_projected` now delegates to `Invocable::is_projected`; import list trimmed (`Locus`/`Stability` no longer needed at module scope — moved into the test module's own `use`, which still constructs descriptors directly). `apps/desktop/tauri/Cargo.toml`: added `cronus-contract` (direct path dependency, `features = ["serde"]`) alongside the existing `cronus-core` dependency. `apps/desktop/tauri/src/bridge.rs`: `Bridge<C>` gained `registry: InvocableRegistry`/`dispatcher: Dispatcher` fields; `Bridge::new` takes both plus applies `secrets` to the dispatcher via `set_secrets`; new `Bridge::catalog()`/`Bridge::invoke()`; two new Tauri commands `capability_catalog`/`capability_invoke`; module doc comment rewritten to describe both command families and their distinct masking paths. 4 new tests (`invoke_dispatches_a_registered_fixture_and_masks_its_outcome`, `invoke_answers_none_for_an_id_the_registry_does_not_know`, `invoke_refuses_a_malformed_id_before_reaching_the_dispatcher`, `catalog_reports_only_projectable_descriptors`); the 3 pre-existing tests updated only to thread the new constructor's extra parameters, not otherwise changed. `apps/desktop/tauri/src/lib.rs`: `capability_catalog`/`capability_invoke` added to the `tauri::generate_handler!` list. `cargo test -p cronus-contract --features serde --all-targets` 32/32 (29 + 3, the 2 pre-existing serde tests plus 1 new); `cargo test -p cronus-tui --all-targets` 67/67 unchanged; full main-workspace `cargo test --workspace -j 2`/`clippy --workspace --all-targets -- -D warnings`/`cargo fmt --all -- --check` green; `cronus-desktop`'s own `cargo build`/`clippy --all-targets -- -D warnings`/`cargo fmt --all -- --check` (PowerShell) green, its test binary compile-clean but disclosed-unable-to-run on this host. Containment self-check clean across all six touched files.

### [T-29A02] The TypeScript client gains `catalog()`/`invoke()`; the executable face cannot cross the seam

- **Spec:** l2-application-shell.md §4.3, §4.3.1 (SP-12)
- **Status:** Done
- **Assignment:** Agent
- **Scope:** `packages/ui/src/shared/bridge.ts`'s `CoreClient` gains `catalog(): Promise<Invocable[]>` and `invoke(invocation): Promise<Outcome>`, forwarding to the two new Tauri commands T-29A01 adds. `version()`/`status()`/`settings` stay exactly as they are. The descriptor type these methods carry is data-only by construction (identity, name, summary, group, locus, binders, stability) — no field through which a host handle or a closure could travel — so §4.3.1's guarantee is a property of the TypeScript type itself, not a runtime check this task adds.
- **Verify:** `pnpm -C packages/ui test` green, including a new test asserting the TypeScript `Invocable`/`Outcome` shape the client decodes carries no field capable of holding a function, a handle, or anything beyond JSON-serializable data — a type-level assertion proven by construction, matching how `l2-application-shell.md` itself frames SP-12 here. `pnpm -C packages/ui build` (`tsc --noEmit && vite build`) succeeds. **Satisfied**: `pnpm -C packages/ui test` 167/167 (163 pre-existing + 4 new in `bridge.test.tsx`, 0 net change in `admission.test.tsx`'s own count since it gained 2 map entries, not tests); `pnpm -C packages/ui build` (`tsc --noEmit && vite build`) succeeds; `biome check` clean on all three touched files after one auto-formatting pass. The JSON-round-trip test exercises every `Locus` shape (`Semantic`/`ClientLocal`/`HostOnly{reason}`) and every `Stability` shape (`Shipped`/`Retired{superseded_by}`) on `Invocable`, and every `Outcome` variant (`Value` with each `OutcomeValue` arm exercised, `Stream`, `Rejected`, `Unavailable`) — `JSON.parse(JSON.stringify(x))` deep-equals `x` for each, the runtime proxy for "nothing but plain data survived," since a function or class instance would not round-trip identically.
- **Handoff:** T-29A03, T-29B01, T-29C01.
- **Notes:** This is marshalling only (INV-2) — no business logic. If a richer typed `Outcome`/`Invocable` mirror does not already exist on the TypeScript side, add the minimal shape this task actually needs; do not import a generated-from-Rust type pipeline that does not exist yet.
- **Decision (recorded, not asked):** **`invoke()`'s TypeScript parameter is `{ id, args? }`, not a mirror of the Rust `Invocation` struct.** Matches T-29A01's own bridge design exactly: `caller` is never accepted here either — this executable face has no field through which to claim a different surface, so the TS `Invocation` interface simply has none, rather than accepting one and discarding it at the call site (which would still let a caller *write* `caller: "Cli"` and have it silently vanish — a worse API than never offering the field). **`invoke()`'s return type is `Promise<Outcome | null>`**, not the Scope's own shorthand `Promise<Outcome>` — `null` is the registry's real `Unknown` answer (SP-13), and T-29A03 (the very next task) depends on this method being able to represent that distinctly; typing it as bare `Outcome` would make T-29A03 impossible without a breaking signature change one task later. **Wire types mirror Rust's default serde external-tagging exactly, snake_case field names included where Rust has them** (`Stability::Retired.superseded_by`) — rather than adding `#[serde(rename_all = "camelCase")]` to `cronus-contract` for cosmetic JS-side ergonomics, which would reopen a Rust-crate file this track's own remaining scope (A02/A03) does not touch. The one field intentionally NOT mirrored: `Invocable.journal_raw_input` — dispatch-journal bookkeeping irrelevant to a catalog consumer, and absent from the Notes' own enumerated field list ("identity, name, summary, group, locus, binders, stability"), so its absence here is a deliberate narrowing, not an oversight. **A pre-existing capability-admission gate caught the new methods immediately**: `packages/ui/src/shell/admission.test.tsx`'s `ADMITTED` map enumerates every `CoreClient` method against its real counterpart and fails if the seam grows one without a named justification (INV-9) — `catalog`/`invoke` added with their counterpart named as the shared registry/dispatcher, exactly the kind of check this whole phase exists to prove out on a second surface.
- **Changes:** `packages/ui/src/shared/bridge.ts`: new exported types `Locus`, `Stability`, `BinderKind`, `Binder`, `Invocable`, `ArgValue`, `Invocation`, `OutcomeValue`, `RejectionMode`, `Rejection`, `StreamHandle`, `Outcome` (wire mirrors of their `cronus_contract` counterparts); `CoreClient` gained `catalog()`/`invoke()`; `createCoreClient` implements both (`invoke` defaults `args` to `{}` when the caller omits it, so the wire payload always carries a real object, never `undefined`). `packages/ui/src/shared/bridge.test.tsx`: 4 new tests in `describe("core bridge client", …)` (catalog marshalling, invoke marshalling with args, invoke defaulting empty args, invoke resolving `null` for the registry's `Unknown`) and a new `describe("core bridge — wire types carry no field beyond JSON-serializable data (SP-12)", …)` block (2 tests, the JSON round-trip proof); the pre-existing settings tests moved into their own `describe("core bridge client — settings", …)` block rather than staying implicitly nested under a block whose name no longer matched their scope. `packages/ui/src/shell/admission.test.tsx`: `ADMITTED` gained `catalog`/`invoke` entries.

### [T-29A03] A stale local catalog refreshes on `Unknown`, rather than rendering a failure

- **Spec:** l2-application-shell.md §4.3.2 (SP-13)
- **Status:** Done
- **Assignment:** Agent
- **Scope:** This surface's own catalog copy can go stale between an extension activating/deactivating in the core and the next delivery. Dispatching an id the local copy no longer recognises must resolve to the registry's real `Unknown` answer (not a fabricated failure), and this surface's correct response is to refresh its catalog cache, not render an error to the user. Land the catalog as a small cached store (matching the shape `l2-application-shell.md` §4.2's projection stores already establish) with a `refresh()` the `Unknown` path calls.
- **Verify:** `pnpm -C packages/ui test` green, including a test that dispatches an id absent from a stale cached catalog, asserts no error surface is rendered, and asserts the catalog store's `refresh()` was called exactly once — proving the ordinary-input-not-a-failure property this task names, the desktop's own equivalent of the terminal UI's T-28A03. **Satisfied**: `pnpm -C packages/ui test` 171/171 (167 pre-existing + 4 new in `catalog.test.ts`); `pnpm -C packages/ui build` (`tsc --noEmit && vite build`) succeeds; `biome check` clean. New test `dispatching an id absent from a stale cache resolves to null, renders no error, and refreshes the catalog exactly once` primes the store's cache via one real `refreshCatalog` call, then dispatches an id with `client.invoke` mocked to answer `null` (Unknown) and asserts: the result is `null` (no thrown error, no rejected promise, no `Outcome` shape a caller could mistake for a rendered failure), and `client.catalog` — the underlying call `refreshCatalog` makes — was invoked exactly once more. A sibling test proves the negative: a resolved outcome passes through untouched and `client.catalog` is never called.
- **Handoff:** T-29T01.
- **Notes:** The registry's own change-announcement mechanism (`l2-invocable-registry.md` §4.9) is the primary refresh trigger in production; `Unknown` is explicitly the backstop for the delivery window between a change and its announcement, not the only path — this task only needs to prove the backstop, not build the announcement subscription if it does not already exist on this seam.
- **Changes:** New `packages/ui/src/shared/catalog.ts`: `CatalogStore` (= `ProjectionStore<Invocable[]>`), `createCatalogStore()`, `refreshCatalog(client, store)` (request → `client.catalog()` → fulfill/fail), `dispatchThroughCatalog(client, store, invocation)` (calls `client.invoke`; on `null` calls `refreshCatalog` and returns `null`; otherwise passes the `Outcome` through untouched). New `packages/ui/src/shared/catalog.test.ts`: 4 tests (`refreshCatalog` request→pending→loaded, `refreshCatalog` failure→unavailable, a resolved outcome never touches the catalog, an `Unknown` outcome resolves to `null`/no error/exactly-one refresh). `packages/ui/src/shared/index.ts`: `export * from "./catalog"` added in alphabetical position. `pnpm -C packages/ui test` 171/171; `pnpm -C packages/ui build` clean; `biome check` clean, no reformat needed this time. `fallow audit --changed-since HEAD` (run in background, confirmed after this task's own checkpoint) reports 0 dead code / complexity / duplication across the 3 changed files. Containment self-check clean across all three touched files.
- **Decision (recorded, not asked):** **New module `packages/ui/src/shared/catalog.ts`, not an addition to `bridge.ts`.** `bridge.ts` is pure marshalling (INV-2) — no state, no caching; a catalog cache is a real, separate concern, so it gets the same treatment `projection-channel.ts` already gives the push-channel-to-projection wiring: a small, focused module of free functions over `(client, store, …)`, not a class or a method bolted onto `CoreClient`. `CatalogStore` is a plain `ProjectionStore<Invocable[]>` — reusing the existing generic `Projection<T>`/`createProjectionStore` machinery from §4.2 exactly, not a bespoke cache type invented for this one case, per the Scope's own "matching the shape... already establish" instruction. **`refresh()` lives as a free function (`refreshCatalog(client, store)`), not a method attached to the store object** — this reads slightly less literally against the Verify line's own "the catalog store's `refresh()`" phrasing, but matches this codebase's own established convention (`bindProjectionChannel` takes the store as a parameter rather than returning an augmented one) over inventing a new shape for one call site; the test proves "refresh happened exactly once" by asserting `client.catalog` — the one real call `refreshCatalog` makes — was invoked exactly once, the same observable property the literal phrasing was pointing at. **`dispatchThroughCatalog` awaits the refresh before returning**, rather than firing it and returning immediately — determinism over a marginal, unmeasured UX gain neither this task nor its Verify line asks for; both directions leave the caller with the same `null` return value regardless.

### [T-29B01] The action registry sources `Semantic` actions from the catalog by identity

- **Spec:** l2-application-shell.md §3 (AS-6), §4.4
- **Status:** Done
- **Assignment:** Agent
- **Scope:** `packages/ui/src/shell/actions.ts`'s `ActionRegistry` (or whatever composes it today) stops being populated from a hand-written list for anything that is a real core capability. `Semantic` actions are built from `catalog()`'s own descriptors (id, label, binding); `ClientLocal` actions (pane focus, dock toggles, layout moves) stay declared locally, exactly as `l2-tui.md`'s own T-28B01 precedent already established for the terminal surface. `bound` (INV-9) reflects whether the id is actually present in the current catalog snapshot, never a hardcoded assumption.
- **Verify:** `pnpm -C packages/ui test` green, including a test proving the registry is **not a second catalog**: a `Semantic` action present in a fed-in catalog fixture is `bound()`/`live()`-visible without being separately declared, and one absent from that fixture is not visible at all — the same property `command::build_catalog`'s own T-28A01 test proves on the terminal surface, expressed here for the action registry instead of a slash catalog. **Satisfied**: `pnpm -C packages/ui test` 179/179 (171 pre-existing + 8 new in `actions.test.ts`); `pnpm -C packages/ui build` (`tsc --noEmit && vite build`) succeeds; `biome check` clean after one auto-format pass. New tests prove both directions of the "not a second catalog" property directly: a `Semantic` descriptor fed into `actionsFromCatalog` is `has()`/`isBound()`/`bound()`/`live()`-visible under its own id with no separate declaration anywhere, and calling it again with that descriptor removed leaves the id entirely absent (not a `bound: false` stand-in) — the same `command::build_catalog` property from the terminal surface, expressed for the action registry. A further test proves the locus filter: `ClientLocal`/`HostOnly`/`Installation` descriptors fed through the same function produce zero actions.
- **Handoff:** T-29B02, T-29C01.
- **Notes:** Depends on T-29A02's `catalog()` existing. Where the current action list already hand-declares an id that turns out to be `Semantic` (a real core capability), removing the hand-declaration is the point, not an incidental cleanup — check every existing entry against the real registry rather than assuming the current split is already correct.
- **Decision (recorded, not asked):** **Audited both existing hand-declared actions against the real Rust registry and found neither is a `Semantic` capability, so neither moved.** `file.settings` opens a local view-state overlay (no core analog at all); `file.new-project` calls a caller-supplied `onCreateFloor` callback — the closest real registry group, `workspace create`, is `Locus::Installation` (migrated as such), not `Semantic`, so it was never going to appear in `capability_catalog()`'s own filtered output regardless. The mechanism this task installs is real even though this particular codebase's current split needed no entry moved — a different, later hand-declared action landing here in the future is exactly what this audit step exists to catch. **A real, structural type mismatch surfaced and had to be resolved before `actionsFromCatalog` could exist at all**: `ShellAction`'s label field was typed `labelKey: MessageKey` — a closed, compile-time union this frontend's own hand-authored i18n catalog defines — but a catalog-sourced action's label is `Invocable.name`, a runtime string the core registry owns and this frontend cannot look up by any `MessageKey`, since none was ever authored for it (inventing synthetic keys per runtime-discovered id would defeat the whole point of a closed catalog). Resolved by replacing the field with `label: ActionLabel`, a discriminated union (`{key: MessageKey} | {text: string}`) plus a `resolveLabel(msg, label)` helper — making "describable through exactly one channel" a property the type enforces, rather than two optional fields a caller could leave both empty. This renamed and retyped every existing `ShellAction` construction site (`building-shell.tsx`'s two hand-declared actions, 3 test fixture files) — a real, disclosed ripple, confirmed complete by `tsc --noEmit` passing clean afterward, not merely by grepping for the old name. **`BuildingShell` itself gained no new prop.** Its existing `actions?: readonly ShellAction[]` prop is already the extension point a caller uses to feed in additional actions (spread into the registry alongside the two hand-declared ones); a caller building `actionsFromCatalog(catalog, dispatch)` externally and passing the result through that same prop needs no change to this component at all — consistent with its own module doc ("Presentation only ... arrives as props ... forwards every mutation as an intent"), since `actions.ts` itself, not `BuildingShell`, is where `Invocable`-typed data may be named. **A disclosed limitation, not silently worked around**: an invocable declaring required `binders` still becomes an action whose `run()` dispatches with no arguments — no surface in this codebase collects arguments for a menu/palette click yet, and building that collection UI is not this task's own scope.
- **Changes:** `packages/ui/src/shell/actions.ts`: new `ActionLabel` type + `resolveLabel(msg, label)`; `ShellAction.labelKey: MessageKey` → `ShellAction.label: ActionLabel`; new `actionsFromCatalog(catalog, dispatch)` (filters to `locus === "Semantic"`, maps to `{id, label: {text: name}, run: () => dispatch(id)}`). `packages/ui/src/shell/building-shell.tsx`: both hand-declared actions and the palette's label-resolution line updated to the new shape. `packages/ui/src/shell/conformance.test.tsx`, `packages/ui/src/shell/shell.test.tsx`: 4 `ShellAction` literal construction sites updated. `packages/ui/src/shell/index.ts`: `ActionLabel`/`actionsFromCatalog`/`resolveLabel` added to the barrel. `packages/ui/src/index.test.ts`: the frozen public-API list gained the two new runtime exports (44 → 46), its own explanatory comment extended in the same edit. New `packages/ui/src/shell/actions.test.ts`: 8 tests (`resolveLabel`'s two channels; the not-a-second-catalog property in both directions; a live descriptor's removal on a second call; the locus filter; label content; `run()`'s dispatch). Containment self-check caught and fixed one real leak (a bare task-id in `actions.ts`'s own new doc comment, restated in plain language) before landing. `pnpm -C packages/ui test` 179/179; `pnpm -C packages/ui build` clean; `biome check` clean. `fallow audit --changed-since HEAD` (run in background, confirmed after this task's own checkpoint): gate reports **no issues** in the 8 changed files; one CRITICAL cognitive-complexity finding on `BuildingShell` (307 lines, cognitive 42, pre-existing before this task's ~13-line addition) is correctly excluded by the tool's own gate as inherited, not introduced by this diff — disclosed here rather than silently omitted, not treated as a new debt this task owes.

### [T-29B02] The palette's semantic entries render from the catalog projection, not a hand-written list

- **Spec:** l2-application-shell.md §4.6 (AS-10), l2-invocable-registry.md §4.2 (finding F-7)
- **Status:** Done
- **Assignment:** Agent
- **Scope:** The command palette's registered-actions source (`packages/ui/src/shell/command-palette.tsx` or wherever it reads from today) draws its `Semantic` rows from T-29B01's catalog-sourced registry, closing finding F-7 (the WebView's hand-written client, before it becomes the fourth divergent copy) alongside T-29C01's registration.
- **Verify:** `pnpm -C packages/ui test` green, including a test that a `Semantic` action fixture fed through the real registry appears as a palette row with the catalog's own label, and one absent from the catalog does not appear — proven through the palette's own delegate source (`SelectionDelegate`, §4.6), not by re-testing the registry itself (T-29B01 already does that). **Satisfied**: `pnpm -C packages/ui test` 181/181 (179 pre-existing + 2 new); `pnpm -C packages/ui build` clean; `biome check` clean after one auto-format pass. Two new tests in `overlays.test.tsx` (the file already establishing render-based `CommandPalette` conventions) drive the real, full pipeline (`actionsFromCatalog` → `createActionRegistry` → `.live()` → the same plain-object mapping `building-shell.tsx` performs → `<CommandPalette actions={…}>`) rather than calling `commandPaletteDelegate` in isolation: a fed-in `Semantic` descriptor renders a row bearing its own `name` and dispatches on click; an omitted one produces no row at all.
- **Handoff:** T-29C01.
- **Notes:** Small by construction if T-29B01 landed correctly — this task is the palette *consuming* the already-correct registry, not re-deriving anything.
- **Decision (recorded, not asked):** **Zero production code changed — `commandPaletteDelegate`'s own `actions` loop (`packages/ui/src/shell/command-palette.tsx`) was already generic over any plain `{id, label, binding, run}` list**, sourced today from `building-shell.tsx`'s `registry.live(CONTEXT_STACK)` — the exact pipeline T-29B01 made catalog-capable the moment a caller passes `actionsFromCatalog`'s own output through the existing `actions` prop. There was no hand-written "semantic entries" list inside the palette to remove; this task's real, concrete gap was the missing PROOF that this already-generic mechanism actually renders a catalog-sourced action correctly end to end — the gap this task's own tests close. This matches its own Notes' prediction ("small by construction if T-29B01 landed correctly") literally, not merely in spirit. **Verified F-7's premise directly rather than trusting the Scope's framing**: read `command-palette.tsx` in full before writing any test, confirmed there is genuinely no second, hand-maintained action source to delete — the finding's own text names the risk as *becoming* a fourth copy, not describing one that already exists, and this audit confirms it still has not, now with a regression test standing guard. **Tests render the full component rather than calling `commandPaletteDelegate` in isolation** — a stronger, more realistic proof of "the palette's own consumption" (this task's own Verify-line phrase) than exercising the pure delegate function alone would be, and it reuses the exact `data-testid` convention (`selection-item-${row.id}`) every other palette test in the same file already establishes. **F-7's own `Repayment` ledger entry in `crates/conformance/src/findings.rs` is deliberately left untouched here** — T-29C01's own Scope names flipping it (alongside F-1) as its own job, once the desktop's corpus registration also proves the generated client's shape; recording it here would pre-empt a decision that task's own audit is supposed to make against what is then actually true.
- **Changes:** `packages/ui/src/shell/overlays.test.tsx`: new `describe("command palette — its actions group renders from the catalog projection (AS-10, F-7)", …)` block (2 tests) plus a local `paletteActionsFrom(catalog, dispatch)` helper reusing `actionsFromCatalog`/`createActionRegistry`/`resolveLabel` — deliberately mirroring `building-shell.tsx`'s own mapping rather than inventing a second, parallel one for the test alone. Containment self-check caught and fixed one real leak (a bare task-id in this new block's own doc comment) before landing. `fallow audit --changed-since HEAD` (run in background, confirmed after this task's own checkpoint): gate reports **no issues** in the (cumulative, uncommitted) 12 changed files; the same pre-existing `BuildingShell` CRITICAL cognitive-complexity finding from T-29B01's own audit is again correctly excluded as inherited — `overlays.test.tsx` itself introduces nothing the tool flags.

### [T-29C01] Corpus registration from the detached workspace; findings F-1 and F-7 close

- **Spec:** l2-surface-conformance.md §4.4, §4.5 (F-1, F-7)
- **Status:** Done
- **Assignment:** Agent
- **Scope:** `apps/desktop/tauri/Cargo.toml` gains `cronus-conformance` as a dev-dependency (`path = "../../../crates/conformance"`) — the first time this crate is linked from outside the main `crates/` workspace, the shape `l2-surface-conformance.md` §2/§4.4 names as the reason the corpus is a library rather than one central suite. Implement `SurfaceProjection` over the desktop's **real** bridge (the same `InvocableRegistry`/`Dispatcher` pair T-29A01 built), following the CLI's and TUI's own registration shape as precedent (isolated fixture registry, real handlers, `#[cfg(test)]`-only). Drive the full corpus from this crate's own test target. Then re-audit F-1's four SP-4 conditions (all three sites now registered) and F-7's (the generated client from T-29B02 landed) against what is now true, updating `seed_inventory()` accordingly.
- **Verify:** `cargo test -p cronus-desktop --all-targets` includes a corpus run whose reports are **exactly** the already-accepted residual set (the two redaction fixtures, the same empty-secret-list reason every surface carries, per the Phase Notes' own disclosed exception) and nothing else — anything further is a genuine new finding, recorded before it is fixed, not fixed under this task's own pressure. If F-1's fourth site (all three surfaces registered) and F-7 (generated client landed) both now hold, both read repaid and the test that makes a repaid transition visible names it explicitly, as it already does for F-2 and F-3. **Satisfied**: `cargo build -p cronus-desktop`/`cargo check --all-targets`/`cargo clippy --all-targets -- -D warnings`/`cargo fmt --all -- --check` (PowerShell) all clean; the new `this_shell_registers_against_the_full_shared_conformance_corpus` test compiles clean but — confirmed directly, not assumed from the unrelated `settings` binary's own history — cannot *run* on this host for the same disclosed `STATUS_ENTRYPOINT_NOT_FOUND` reason every `cronus-desktop` test since T-29A01 carries. Verified the logic actually produces the exact accepted-residual set by reimplementing `Bridge::catalog()`/`Bridge::invoke()` and this task's own `DesktopProjection` in a throwaway, Tauri-free scratch binary (no WebView2 linkage, so it *can* run on this host) built from the same real crates (`cronus-conformance`, `cronus-core`) — it reported exactly 2 divergences, both the disclosed `secret-bearing`/`boundary-crossing` fixtures, nothing else; discarded after use, not part of the repo. Both `crates/contract`'s new `ArgValues::iter()` (needed to cross from the domain-tier `ArgValues` the trait's own signature carries to the wire's flat map `Bridge::invoke` expects) and `crates/conformance`'s own updated ledger pass their own real test suites: `cargo test -p cronus-contract --all-targets` 31/31 (29 + 2 new); `cargo test -p cronus-conformance --all-targets` 16/16; full workspace `cargo test --workspace -j 2` green. F-1 and F-7 both now read `is_repaid() == true`, alongside the already-repaid F-2/F-3 — the repayment-visibility test renamed and extended to name all four explicitly, matching the Verify line's own "names it explicitly" requirement.
- **Handoff:** T-29T01.
- **Notes:** This is the phase's own replacement oracle, the same role T-28T01 played for the terminal UI — not a formality. If `cargo test -p cronus-desktop`'s test binary fails to *load* on this host for reasons unrelated to this task's own code (the documented `STATUS_ENTRYPOINT_NOT_FOUND` class), record that as a disclosed environmental limitation rather than treating a compile-clean, run-blocked test as failure — but confirm first that this specific new test target is actually affected, rather than assuming it from the unrelated `settings` binary's own history.
- **Decision (recorded, not asked):** **`DesktopProjection` drives `SurfaceProjection` through `Bridge::catalog()`/`Bridge::invoke()` exclusively, never a raw `InvocableRegistry`/`Dispatcher` read** — the opposite of what this task's own Scope text literally names ("the same `InvocableRegistry`/`Dispatcher` pair T-29A01 built"). Read `crates/conformance/src/projection.rs`'s own module doc directly before writing any code and found it explicit and load-bearing: "the desktop shell... links no invocable registry or dispatcher at all — only a catalog and an `invoke()` call delivered across its IPC seam... every surface answers through `SurfaceProjection` — a trait a live registry **and an IPC bridge** can implement identically." Going through `Bridge`'s own public methods is what makes this registration prove the real seam, not merely the same domain-tier mechanism the CLI/TUI already proved twice; an isolated `InvocableRegistry`/`Dispatcher` pair is still built inside `DesktopProjection::new()`, but only as the **input** `Bridge::new(...)` composes over, exactly mirroring how `core_bridge()` itself is built in production — never touched again once the `Bridge` exists. Corrected the Scope's own text against the spec module's own clearer, more specific statement rather than following it literally into a design the corpus's own architecture forbids. **A genuine gap surfaced and was closed in `cronus-contract`**: `SurfaceProjection::invoke` takes `&ArgValues` (a domain-tier type with no public iterator — no in-process caller ever needed one, since `bind()` and every handler read by name), but `Bridge::invoke` takes the wire's own flat `HashMap<String, ArgValue>` shape; converting one to the other needed enumerating every bound argument, which `ArgValues` could not do. New `ArgValues::iter(&self) -> impl Iterator<Item = (&str, &ArgValue)>`, the minimal addition, with its own 2 tests. **F-1's repayment resolved as a genuine, disclosed audit finding, not a rubber stamp of the task's own optimistic framing.** The Verify line's parenthetical ("F-1's fourth site — all three surfaces registered") implies the other three SP-4 conditions were already settled; checked directly and found `copies_deleted`/`deletion_pinned` still `false` in `seed_inventory()`, with **no tombstone ever recorded for F-1's own three named sites** (its own sites — the command line's compile-time enum, the terminal UI's slash-catalog constant — are different artifacts from what F-2's and F-3's own tombstones separately cover). Verified directly, not assumed, that both artifacts are genuinely and permanently gone (`crates/cli/src/cli.rs`'s own doc comment disclaims a compile-time subcommand enum by name; the terminal UI's own closing record for its slash-catalog task states the constant "deleted outright, not merely superseded"). Rather than flip `copies_deleted`/`deletion_pinned` to `true` on the strength of the task's own framing alone, added the three tombstones this audit found missing (two backdated to real, already-permanent deletions from earlier phases' own work — recording a real historical fact, not inventing one — plus the desktop's own new one for the third site) — closing a real, disclosed gap in the ledger's own record-keeping, not merely repeating what the Scope text implied. **F-7 read as fully repaid on different grounds**, disclosed as such in its own repayment comment: as a `Preemptive` finding whose risk never materialized, there was never a hand-written copy to physically delete, so `copies_deleted`/`deletion_pinned` read `true` because the absence is now structural (proven by T-29B01's own "not a second catalog" regression tests), not because an artifact was removed — a different kind of evidence for the same two flags, named explicitly as different rather than silently treated the same as F-1's literal-deletion case.
- **Changes:** `crates/contract/src/lib.rs`: new `ArgValues::iter()` + 2 tests. `apps/desktop/tauri/Cargo.toml`: `cronus-conformance` added as a dev-dependency. `apps/desktop/tauri/src/lib.rs`: `#[cfg(test)] mod conformance_registration;` added. New `apps/desktop/tauri/src/conformance_registration.rs`: `DesktopProjection` (wraps a `CoreBridge` built over an isolated, fixture-only registry+dispatcher pair, empty secret list matching production), `attach_handlers` (byte-for-byte parity with the CLI's and terminal UI's own), 1 test (`this_shell_registers_against_the_full_shared_conformance_corpus`) — compiles and passes clippy/fmt clean but is disclosed-unable-to-run on this host, verified equivalent via an external, discarded scratch binary. `crates/conformance/src/findings.rs`: F-1 and F-7 both fully flipped to repaid (comments rewritten `[CLOSED]`, matching F-2/F-3's own precedent style); 3 new `F-1` tombstone entries added to both `tombstones()` and `ledger_baseline::tombstones()`; the repayment-visibility test renamed to `exactly_f1_f2_f3_and_f7_read_as_repaid_after_the_desktop_shells_own_registration`, naming all four explicitly. `cargo test -p cronus-contract --all-targets` 31/31; `cargo test -p cronus-conformance --all-targets` 16/16; `cargo build`/`clippy --all-targets -- -D warnings`/`cargo fmt --all -- --check` (PowerShell) clean for `cronus-desktop`; full main-workspace `cargo test --workspace -j 2`/`clippy --workspace --all-targets -- -D warnings`/`cargo fmt --all -- --check` green. Containment self-check caught and fixed two real leaks (literal spec-file-name mentions — `l2-surface-conformance.md` — in a new doc comment and a new Cargo.toml comment, an established-but-still-forbidden pattern elsewhere in this codebase not repeated here) before landing.

### [T-29T01] Behaviour-preservation proof and full quality gates

- **Goal:** Prove the migration changed structure and not behaviour, except where this phase's own tasks changed behaviour deliberately and said so — and confirm the Phase Notes' own renarrowing (no residual corrections claimed) held all the way through execution.
- **Method:** Confirm every intentional behaviour change is recorded at its own task. Confirm the two disclosed exceptions (`version`/`status` untouched; the empty secret list unfixed) still hold and remain recorded at their owning sites, not only in planning prose. Run the workspace gates for both the Rust and the TypeScript halves.
- **Verify:** `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings` (zero) for the main workspace; `cargo build -p cronus-desktop` and `cargo test -p cronus-desktop --all-targets` (PowerShell) for the detached one; `pnpm -C packages/ui test`, `pnpm -C packages/ui build`, and `fallow audit --changed-since <base>` for the frontend; no `unwrap()`/`panic!()` introduced on Rust production paths across every file this phase created or substantially modified (`#[cfg(test)]` excluded); containment self-check clean across every touched file, Rust and TypeScript alike.
- **Status:** Todo
- **Notes:** Same discipline T-28T02 already established for the terminal surface — audit residuals against `rg`, not against what the write-ups claim. This phase's own biggest risk is claiming a residual was preserved when it was actually silently touched by an adjacent edit; check `capability_version`/`capability_status` and `core_bridge()`'s secret list explicitly, by name, not by trusting that Track A's own scope line was honoured.
