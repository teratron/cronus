# Implementation Plan

**Version:** 2.32.0
**Generated:** 2026-07-18
**Based on:** .design/main/INDEX.md v1.0.123
**Status:** Active

## Overview

Implementation plan for Cronus from the project registry (209 registered specs: 201 Stable, 7 RFC, 1 Draft). Phases follow a **growth order**: the agent grows like a sprout from a seed. This revision (v2.14.0) absorbs a registry wave that opened two new subsystem fronts. Three Stable L1 concepts join Phase 0: `l1-tokenization-boundary` (`concept-only`), plus `l1-office-archetype` and `l1-background-activation`, which are **not** concept-only — each already has an authored `Implements:` L2, which drops the marker per C28 §4. Those three L2s (`l2-archetype-catalog`, `l2-service-activation`, `l2-crate-topology`) are all RFC and park in Backlog (C6). The same wave demoted `l2-source-layout` `Stable → RFC` at 1.2.0: its §4.4 crate-granularity question is now delegated to `l2-crate-topology`, so the two promote together as one **core-decomposition wave**. Phase 1's `[x]` on source-layout stands at its delivered 1.1.0 scope — only the 1.2.0 delta is parked. Phase 10's KAN-8 checkbox is corrected to `[x]` (implemented in `crates/core/src/kanban/custom_boards.rs`; the phase status line already recorded it). Prior revision (v2.13.1) absorbed the **l1-deep-research** concept into Phase 0 — a Stable L1 whose implementing L2 was already built in Phase 6, so a fully-realized concept, not concept-only. **v2.15.0 opens Phase 13 — Core Decomposition.** After the twelve build phases completed (all Stable specs implemented), the core-decomposition wave (`l2-crate-topology` + `l2-source-layout` 1.2.0) was reviewed RFC→Stable via `/magic.spec` (INDEX v1.0.97) and is now decomposed into Phase 13 — a structural, behavior-preserving refactor that repartitions `crates/core` into five crates and makes INV-8 compiler-enforced. It is unlike the growth-metaphor build phases: no new domain logic, only movement of working code plus one dependency-edge inversion. The remaining six Backlog items stay RFC/Draft, each awaiting its own `/magic.spec` review before it can be phased. **v2.15.1 (milestone sync): Phase 13 is complete and archived — all thirteen phases are Done and every currently-Stable spec is implemented, so no phase is open.** The ten Backlog items are all RFC (9) or Draft (1); none can be planned mechanically, because RFC→Stable is a `/magic.spec` review act (RULES §2), not a `/magic.task` promotion. The sole Draft (`l2-loop-runner`) stays Draft — its L1 parent `l1-loop-governance` is RFC (layer constraint). Deterministic next target by dependency topology (DA-3): `l2-service-activation` — the direct downstream of the just-completed Phase 13 (its platform-adapter crate seam, §4.4, was the precondition Phase 13 delivered), and the only Backlog L2 whose structural blocker this phase cleared, with a Stable L1 parent (`l1-background-activation`). Its open design questions (Windows S4U, non-systemd Linux, `tauri-plugin-autostart`) are resolved *during* the `/magic.spec` review, not before it. **v2.16.0 (memory wave): the memory-intelligence + memory-consolidation L1s reached Stable and moved from Backlog into Phase 0 (each now with an authored L2, so not `concept-only`); their two L2 realizations (`l2-memory-consolidation`, `l2-memory-intelligence`) were authored and parked in the Backlog as `RFC`.** No new phase opened — a memory-implementation phase opens only once those two L2s reach Stable via `/magic.spec`. Deterministic next target (DA-3) shifts to that pair (review `l2-memory-consolidation` first — the lower layer). The registry is INDEX v1.0.100 (196 specs: 186 Stable, 9 RFC, 1 Draft). **v2.17.0 opens Phase 14 — Memory Intelligence & Consolidation.** Both memory L2s reached Stable via `/magic.spec` (INDEX v1.0.101, 188 Stable) and are decomposed into an 8-task, 4-track build phase (A schema · B consolidation · C intelligence · T validation) — the first buildable phase since Phase 13, domain-logic-first over the existing SQLite substrate. Foundation-then-parallel: Track A (schema) gates B/C. **v2.18.0 records Phase 14 complete (9/9 tasks, archived) and opens Phase 15 — Memory Capture Policy & Metadata.** Phase 14 delivered `l2-memory-consolidation` whole (MC-1…MC-10) and `l2-memory-intelligence` at query-surface scope (MI-1/2/3/4/5/7/8/9/13); the four capture-path invariants (MI-6/10/11/12) were deferred at delivered scope because they each need net-new capture-metadata schema and form one coherent write-path unit. Following the source-layout 1.1.0/1.2.0 precedent — Phase 14 keeps its `[x]` at delivered scope, immutable; the remainder is a **new** Phase 15 line item, never a reopen. Phase 15 is the deterministic next target (DA-3): the same Stable spec, no `/magic.spec` needed (the Invariant Compliance table specifies each net-new item), decomposed into 5 tasks / 3 tracks (A capture-metadata schema · B capture path · T validation). Registry unchanged at INDEX v1.0.101 (196 specs: 188 Stable, 7 RFC, 1 Draft). **v2.19.0 (wiki-store wave + memory-model amendment): `l1-project-wiki` gained an authored `Implements:` L2 — `l2-project-wiki` (RFC) — so it drops the concept-only marker per C28 §4; the L2 parks in Backlog (C6). It realizes the client-facing wiki as a per-office SQLite projection cache (workspace `wiki/wiki.db`), not loose Markdown — a rebuildable cache of upstream truth (board / decisions / ledger / deliverables), distinct from the memory learned-tier where the store itself is the truth. This wave also absorbed the MEM-4 v1.1 amendment (`l1-memory-model` 1.1.0, `l2-memory-store` 1.4.0, `l2-memory-consolidation` 1.0.2 — source-of-truth by memory kind: learned tier store-authoritative with no notes/*.md dual-write, authored tier text-authoritative, distilled skills are workflows not memory records), which is spec-only with no code delta — the shipped `crates/store-local` was already store-authoritative. No new phase opened; a wiki-implementation phase opens only once `l2-project-wiki` reaches Stable via `/magic.spec`. Phase 15 (Memory Capture Policy & Metadata) remains the buildable next target (DA-3). Registry INDEX v1.0.106.** **v2.20.0 opens Phase 16 — Project Wiki Store.** `l2-project-wiki` reached Stable via `/magic.spec` (RFC→Stable at 1.0.1, one review fix — a `rebuild` byte-equivalence overclaim corrected to informational equivalence) and is decomposed into a 7-task, 4-track build phase (A schema · B regeneration · C read/access surface · T validation) — the client-facing wiki as a per-office SQLite projection cache (`wiki/wiki.db`), a rebuildable cache of upstream truth, distinct from the memory learned-tier where the store itself is the truth. Foundation-then-parallel: Track A (schema) gates B/C; B serialized on the shared regeneration entry point. It becomes the deterministic next target (DA-3) — the sole Stable-but-unbuilt L2, its ground-truth sources (board/decisions/ledger/deliverables) all delivered. Registry INDEX v1.0.107 (202 specs: 194 Stable, 7 RFC, 1 Draft). **v2.21.0 (registry-sync wave, INDEX v1.0.107 → v1.0.117): absorbs eleven Stable L1 concepts into Phase 0 (all `concept-only` — authored across the improvement-loop, session-reinforcement, generation-shaping, frugality, and grounding/honesty registry waves) and one new RFC L2 into Backlog — `l2-model-runtime` (Implements `l1-model-runtime`), the concept-conformance audit's keystone finding: model selection is implemented while no transport can reach a model. `l1-model-runtime`'s Phase 0 entry drops nothing but gains its authored-L2 note (C28 §4). The wave also carries the audit's two L1 amendments (`l1-architecture` 1.2.0 +INV-9 shipped-surface honesty; `l1-quality-standards` 1.1.0 +QLY-9 gate-scope completeness) and four FR-6 disclosure patches — all version reconciliations, no phase impact. Phase 16 (Project Wiki Store) remains the open buildable target; no new phase — `l2-model-runtime` can be phased only after its `/magic.spec` RFC→Stable review.** **v2.22.0 opens Phase 17 — Model Transport & Provider Connectivity.** `l2-model-runtime` reached Stable via `/magic.spec` (RFC→Stable at 1.0.1, one Post-Update Review fix — the transport's generate surface was wrongly attributed to the routing-metadata `contract::ModelProvider`; corrected to a NEW `InferenceBackend` trait + two-facet provider + stream-collapsing nodus bridge) and is decomposed into a 7-task, 5-track build phase (A contract inference trait · B model-local transport crate · C facade + nodus bridge · D topology + CI guard · T validation). It closes the audit keystone: model selection was fully implemented while no seam could perform the call. A **real-integration** phase (a spec-sanctioned HTTP+TLS client, hermetic mock-HTTP tests, no async runtime) — the first non-domain-logic-first phase since Phase 13. Foundation-then-parallel: Track A (the `InferenceBackend` trait) gates B/C; B serialized on the shared crate + mock-server fixture. Now open alongside Phase 16 (both `Todo`) — by leverage the transport is higher-priority (it unblocks the whole model-consuming surface: triage classification, context compaction, nodus `gen`), but neither depends on the other, so `/magic.run` picks the execution order. Registry INDEX v1.0.118 (209 specs: 201 Stable, 7 RFC, 1 Draft). **v2.23.0 (milestone sync): Phase 17 (Model Transport) is complete (7/7) and archived — the concept-conformance audit's keystone is closed, the model-consuming surface is live end-to-end (`contract::InferenceBackend` → `crates/model-local` REST transport → facade `NodusModelBridge`/`TransportCompactor`), the CI `domain → model-local` boundary guard is self-tested, and two pre-existing `cronus-core` test-isolation races surfaced by the workspace-green gate were fixed. `cargo test --workspace` green (exit 0 ×3). **Phase 16 (Project Wiki Store) is now the sole open buildable phase (DA-3 next target).** No new phase opens — every currently-Stable spec is now either built or covered by an open phase; the Backlog stays 8 (7 RFC + 1 Draft), each awaiting `/magic.spec` review. **Deferred follow-up (mechanical, non-blocking): a `/magic.spec` amendment to `l2-crate-topology` should register `model-local` as a peer infra crate row (same tier/rules as `store-local`/`auth-local`) — deferred out of run per the SDD boundary; it does not gate Phase 16.** **v2.24.0 (milestone sync): Phase 16 (Project Wiki Store) is complete (7/7) and archived** — the client-facing wiki ships end to end as a per-office SQLite projection cache: office-owned event-driven transactional regeneration with the PW-4 citation + PW-8 internal-detail guards and a no-generator grounded-stub degrade, fingerprint-drift freshness (PW-5), a structurally read-only client surface (`WikiReadSurface`, PW-2/PW-6), an `access-grants` Read gate (`GatedWiki`, PW-7), and `rebuild()` proving projection-not-source (PW-3 — drop `wiki.db`, re-derive an equivalent wiki from ground truth; the drop-loses-nothing proof deletes a real `wiki.db` file and reconstructs an equivalent set). `cargo test --workspace` green (exit 0, 64 result-sets) + clippy `-D warnings` + fmt clean. **With Phase 16 done, no buildable phase remains open — every currently-Stable spec is now built.** The Backlog stays 8 (7 RFC + 1 Draft); each advances only via a `/magic.spec` RFC→Stable review before it can be phased, so the pipeline's forward motion is now a spec-review act, not a run. DA-3 topological next target: `l2-service-activation` (Stable L1 parent `l1-background-activation`; its platform-adapter crate seam was delivered by Phase 13). The mechanical `l2-crate-topology` amendment (register `model-local` as a peer infra crate row) remains a pending, non-blocking `/magic.spec` housekeeping follow-up. Registry unchanged at INDEX v1.0.118; Pre-flight clean (ok:true, 0 orphans, no header drift); Pre-Planning Stabilization 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent `l1-loop-governance`). **v2.25.0 opens Phase 18 — Service Activation.** `l2-service-activation` reached Stable via `/magic.spec` (RFC→Stable at 1.0.1 — its three open TBDs resolved as design decisions: non-systemd Linux → login-scoped via XDG autostart + system-scoped `Unsupported` (BA-10); `tauri-plugin-autostart` rejected per the std-first dependency policy; Windows S4U design-settled with only At-startup launch-timing left as a build-time gate) and is decomposed into an 8-task, 6-track build phase (A seam+policy · B BA-4 barriers · C state-root lock · D OS adapters · E command surface · T validation). The concept-conformance keystone for INV-4: model selection and transport now ship, but nothing made a host into the always-on *hub* those presume. A **real-integration** phase (Phase 17 precedent — genuine platform I/O behind an adapter crate, `SystemCalls`-seam testability, no live-OS dependency in CI), governed by one constraint — the background engine runs as the user, never a superuser — and the load-bearing BA-4 security property (activation is a SEC-10 authority grant the agent has no structural write path to). Foundation-then-parallel: Track A (the `ActivationRegistry` seam) gates all; B (sandbox deny entries) precedes any real `enable()`; C (state-root lock) is an independent prerequisite for any background mode; D is the platform-adapter core (serialized on the shared `activation-os` crate, Windows system-scoped last). Two bundled `/magic.spec` amendments rode the promotion: `l2-technology-stack` 1.2.1 ("Windows service" → "S4U scheduled task" reconciliation) and `l2-crate-topology` 1.0.1 (registered the shipped `model-local` peer adapter crate — the deferred housekeeping). Registry INDEX v1.0.119 (209 specs: 202 Stable, 6 RFC, 1 Draft). The remaining 7 Backlog items stay RFC/Draft, each awaiting its own `/magic.spec` review. **v2.26.0 (milestone sync): Phase 18 (Service Activation) is complete (8/8) and archived** — a host becomes the always-on INV-4 hub end to end: the `ActivationRegistry` seam + domain transition state machine (BA-3 mutual exclusion via remove-then-register-verify, BA-8 observed-not-remembered), the three BA-4 structural barriers (no agent tool, no sandbox write path, no elevation the agent can supply), the state-root lock (BA-11 attach-never-duplicate), and a new peer adapter crate `cronus-activation-os` behind a `SystemCalls` seam realizing all three platforms for real (Windows S4U + `Run` via raw `windows-sys` FFI with real UAC elevation; Linux systemd/`pkexec`/lingering-user-unit + XDG-autostart; macOS `LaunchAgent`/`LaunchDaemon` via `sudo`, with the `SMAppService` veto-signal gap disclosed). The closing validation sweep caught and fixed a genuine BA-10 gap: `capabilities()` had hardcoded `Supported` since the adapters first landed; it now performs a real per-host check (non-systemd Linux correctly reports `Unsupported` with a reason). `cronus activation status|enable|disable` ships end to end, with BA-5 disclosure and BA-6 least-privilege refusal proven by a real non-interactive smoke test. `cargo test --workspace` green (exit 0, one disclosed transient flake unrelated to the new code) + clippy `-D warnings` (native Windows + cross-compiled Linux/macOS) + fmt clean. **With Phase 18 done, no buildable phase remains open — every currently-Stable spec is now built.** The Backlog stays 7 (6 RFC + 1 Draft); each advances only via its own `/magic.spec` RFC→Stable review. Registry unchanged at INDEX v1.0.119; Pre-flight clean (ok:true, 0 orphans, no header drift); Pre-Planning Stabilization 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent `l1-loop-governance`). **v2.27.0 (registry-sync, INDEX v1.0.118 → v1.0.120): absorbs ONE new Stable `concept-only` L1 into Phase 0 — `l1-value-settlement` v1.0.0 (agent-initiated outbound payment to an external party: the third budget axis beside inbound usage-allowance and per-request generation-budget, a principal-set agent-read-only spending envelope on the human-write-only authority plane, bounded-authority custody, top-of-ladder gating with allowlisted recipients, fail-closed, receipted+ledgered accountability, opt-in/local-first/rail-neutral seam — VS-1…VS-8).** Added by an adoption pass over an external agent-harness reference whose other mechanics were already saturated by existing specs; the only genuinely-uncovered mechanic was outbound payment. The same pass refined `l1-nodus-portability → LP-17` (settlement effect seam) in the **nodus** workspace — out of scope for this main-scoped run, and a version bump to an already-placed Stable concept, so no orphan there. No new buildable phase: `l1-value-settlement` is concept-only (no authored `Implements:` L2 yet), so it can be phased only once an L2 realization reaches Stable via `/magic.spec`. Also reconciled the stale `**Based on:**` header (was v1.0.118, now v1.0.120) that had lagged since Phase 18 opened. Registry INDEX v1.0.120 (210 specs: 203 Stable, 6 RFC, 1 Draft). Pre-flight ok:true (the one orphan — `l1-value-settlement` — resolved by this absorption; SYNC_GAP closed); Pre-Planning Stabilization 0 promoted (sole Draft `l2-loop-runner` stays Draft — RFC parent). No open buildable phase remains — every currently-Stable L2 is built. **v2.28.0 opens Phase 19 — Loop Runner.** The loop-governance chain reached Stable via `/magic.spec` (`l1-loop-governance` 0.2.0→1.0.0, reviewed as-is; `l2-loop-runner` 0.1.0→1.0.0, LG-10 compliance row + `ObjectiveSlot` added, crate placement reconciled to the post-decomposition topology) and `l2-loop-runner` is decomposed into an 8-task, 5-track build phase (A types+governor · B execution-runner+oracle · C evolution-runner+escalation · D LG-10+facade+CLI · T validation). It builds the single governed place every autonomous loop runs — a runner + a single governor enforcing declared class + mutation manifest + separated oracle + independent ceiling (LG-1…LG-10), with the criteria-immutability anti-drift spine (LG-3) enforced by the type system (no `Criteria` variant). The direct conceptual downstream of the Phase 18 always-on hub: a hub exists to run autonomous loops. A **composition** phase — the runner does not re-implement isolation/budget/routing/rollback but composes shipped subsystems (execution-workspace, budget-engine, model-router, session-checkpoint, version-control, agent-autonomy, nodus provider traits) behind one governed contract; the pure loop logic is CI-tested against fakes (the `Oracle`/`ProviderType`/`BudgetHandle` abstractions the spec provides), real subsystem wiring isolated to the `crates/core` facade + one real end-to-end CLI smoke. Foundation-then-parallel: Track A (the `LoopSpec`/`MutationManifest`/`Oracle` types) gates all. The §6 migration of the existing orchestration/kanban/heartbeat loops to route through the runner is a deferred, behavior-preserving follow-up, not part of this phase. Registry INDEX v1.0.121 (210 specs: 205 Stable, 5 RFC, 0 Draft — the sole Draft is gone). Pre-flight ok:true (SYNC_GAP from the `/magic.spec` promotion closed here); Pre-Planning Stabilization 0 promoted (no Draft remains). Backlog drops to 5 (all RFC). **v2.29.0 (milestone sync): Phase 19 (Loop Runner) is complete (8/8) and archived** — the single governed place every autonomous loop runs ships end to end: `domain::loop_runner` types with `MutableArtifact` lacking a `Criteria` variant (LG-3 by the type system), the single-point governor (`check_ceiling`/`guard_writes`/`judge`), the execution-loop runner (ceiling-first, illegal mutations rejected-and-continued not silently dropped, `governor::judge` — never the actor — sets `done`, LG-5 fresh-context-per-iteration), the evolution-loop runner (nesting `run_execution` per task, LG-1 composition, keep/revert/partial scoring), the escalation gate (hard-refuses on a shared-lineage oracle or absent novelty source before any metric comparison), and LG-10 objective persistence composing the already-shipped CC-9 mechanism rather than reinventing eviction. Facade + CLI: a real `FileExistsBackend`/clock/state-tier persistence, `cronus loop run|evolve|log|show` (`evolve` present-but-marked-unavailable per INV-9, never a silent stub — no harness registry exists yet). 42 domain tests + 4 core tests + 4 real CLI-smoke tests + an 11-test facade-level acceptance sweep. `cargo test --workspace` green across 3 consecutive full runs (0 failed) + clippy `-D warnings` + fmt clean. **With Phase 19 done, no buildable phase remains open — every currently-Stable spec is now built.** The Backlog stays 5 (all RFC); each advances only via its own `/magic.spec` review. Registry unchanged at INDEX v1.0.121; Pre-flight clean; Pre-Planning Stabilization 0 promoted. **v2.30.0 opens Phase 20 — Archetype Catalog.** `l2-archetype-catalog` reached Stable via `/magic.spec` (reviewed as-authored, one cosmetic TBD→prose edit) and is decomposed into a 6-task, 5-track build phase (A schema+validation · B catalog+shipped-archetype+selection · C deviation recording · D command surface · T validation): the concrete office-archetype system (OA-1…OA-11) — a prior on staffing that is never a roster, with OA-4 (no authority) made unrepresentable by a closed four-key schema and the candidate pool read by the deviation recorder never the hire gate (OA-3). Domain-logic-first (`crates/domain/src/archetype/`, resolving against `roles::catalog::PRESET_CATALOG`); ships `software-engineering` alone, with the two other archetypes declared-blocked pending a separate ROL-9-gated role-catalog amendment and the imported-archetype vetting path deferred (depends on the admission-vetting gate). Registry INDEX v1.0.122 (210 specs: 206 Stable, 4 RFC, 0 Draft). Pre-flight ok:true (SYNC_GAP from the `/magic.spec` promotion closed here); Pre-Planning Stabilization 0 promoted (no Draft). Backlog drops to 4 (all RFC). **v2.31.0 (milestone sync): Phase 20 (Archetype Catalog) is complete (6/6) and archived** — the office-archetype system ships end to end in `crates/domain/src/archetype/`: the closed four-key schema making OA-4 unrepresentable, the shipped `software-engineering` archetype + the two declared-blocked entries (OA-10 honesty), inferred selection + application that touch no staff (OA-8), the deviation recorder whose `record_hire` returns `()` so it structurally cannot refuse a hire (OA-3), and the `cronus archetype list|info|set|create` CLI (no `hire` verb). 24 domain + 4 CLI-smoke + a 10-test facade-level OA sweep; `cargo test --workspace` green across 3 consecutive full runs (0 failed) + clippy `-D warnings` + fmt clean. Disclosed deferrals carry forward: the ROL-9-gated role-catalog nine-role amendment (unblocks advertising/finance), the imported-archetype vetting path (OA-5), and the office-scoped active-archetype record. **With Phase 20 done, no buildable phase remains open — every currently-Stable spec is now built.** The Backlog stays 4 (all RFC); each advances only via its own `/magic.spec` review. Registry unchanged at INDEX v1.0.122; Pre-flight clean; Pre-Planning Stabilization 0 promoted. **v2.32.0 opens Phase 21 — Knowledge Store.** `l2-knowledge-store` reached Stable via `/magic.spec` (RFC→Stable at 1.0.0→1.1.0 — the L2 had lagged its already-Stable parent `l1-knowledge-base` v1.2.0; the promotion authored the missing KB-9 authorship-zone / KB-10 curation-lifecycle / KB-11 query-preparation compliance) and is decomposed into an 8-task, 5-track build phase (A schema+types · B ingestion · C retrieval · D authorship+curation+access+CLI · T validation). Realizes the agent-queryable **RAG** subsystem: named document collections with hybrid semantic+keyword retrieval — ingest once, retrieve cited chunks on demand. The sole mature Stable-but-unbuilt L2 (DA-3): its parent is Stable and its embedding seam (`contract::InferenceBackend`) shipped in Phase 17. Foundation-then-parallel: Track A (the `knowledge_*` schema in `crates/store-local/src/knowledge/` + contract types) gates B/C/D. **Crate placement reconciled at the plan level:** the spec §4.5's standalone `crates/knowledge-store/` layout maps onto the post-Phase-13 decomposition (store-tier in `crates/store-local/src/knowledge/`, retrieval/ingestion logic in `crates/domain`, service in the `crates/core` facade), the memory/wiki-store precedent — a mechanical `/magic.spec` §4.5 amendment is a non-blocking housekeeping follow-up, not a phase blocker. **Principal execution risk (disclosed):** the `vec0` ANN half needs the sqlite-vec extension — a genuine external-dependency decision (the `ureq`/`windows-sys` flag-before-proceeding precedent); FTS5 + RRF + ingestion + authorship/curation/access all build without it, and if deferred the vector half degrades to a documented seam (the generator-optional precedent). Registry INDEX v1.0.123 (210 specs: 207 Stable, 3 RFC, 0 Draft). Pre-flight ok:true (SYNC_GAP from the `/magic.spec` promotion closed here); Pre-Planning Stabilization 0 promoted (0 Draft). Backlog drops to 3 (all RFC, all concept-only L1).

- **Seed = the library** (`crates/core` + `crates/nodus` runtime) — Phases 1–2.
- **Stem = the CLI** — Phase 3, the first usable surface, emerging straight from the seed.
- **Internal growth** = subsystems → office engine → orchestration — Phases 4–6 (the CLI gains commands as each lands).
- **Leaf = the TUI** — Phase 7.
- **Flower = the desktop application** — Phase 8.
- **Hardening** = operational productionization — Phase 9.

Execution mode: **Parallel** (C3); tracks grouped by file independence. Critical path runs through `crates/core` and the `crates/nodus` runtime it depends on.

## Phase 0 — Requirements (Layer 1: Concept)

*Technology-agnostic contracts. All Stable — they gate the implementation phases.*

> **Concept-only semantics (RULES §7 C28):** a `[x]` on a Phase 0 L1 means "concept authored & Stable (a gating contract)", **not** "implemented". Many of these concepts have no authored L2 implementation yet — by design, this project builds the conceptual caul ahead of code. Such L1s carry the `concept-only` status: the concept-vs-code gap is named here rather than hidden, and they are exempt from the "Stable L1 without L2 child" coverage-gap advisory until an `Implements:` L2 is authored (at which point the marker auto-reverts). This replaces a hard authoring budget — L1 authoring stays cheap; the delta stays visible.

- [x] **Architecture** ([l1-architecture.md](specifications/l1-architecture.md)) [L1]
- [x] **Office Model** ([l1-office-model.md](specifications/l1-office-model.md)) [L1]
- [x] **Storage & State Model** ([l1-storage-model.md](specifications/l1-storage-model.md)) [L1]
- [x] **Memory Model** ([l1-memory-model.md](specifications/l1-memory-model.md)) [L1]
- [x] **Workspace Lifecycle** ([l1-workspace-lifecycle.md](specifications/l1-workspace-lifecycle.md)) [L1]
- [x] **Kanban Model** ([l1-kanban-model.md](specifications/l1-kanban-model.md)) [L1]
- [x] **Scheduler Model** ([l1-scheduler-model.md](specifications/l1-scheduler-model.md)) [L1]
- [x] **Quality Standards** ([l1-quality-standards.md](specifications/l1-quality-standards.md)) [L1]
- [x] **Office Visualization** ([l1-office-visualization.md](specifications/l1-office-visualization.md)) [L1]
- [x] **Orchestration & Autonomy** ([l1-orchestration.md](specifications/l1-orchestration.md)) [L1]
- [x] **Roles** ([l1-roles.md](specifications/l1-roles.md)) [L1]
- [x] **Smart Routing** ([l1-routing.md](specifications/l1-routing.md)) [L1]
- [x] **Doctor & Self-Healing** ([l1-doctor.md](specifications/l1-doctor.md)) [L1]
- [x] **Client Security** ([l1-security.md](specifications/l1-security.md)) [L1]
- [x] **Error Reporting** ([l1-error-reporting.md](specifications/l1-error-reporting.md)) [L1]
- [x] **Telemetry** ([l1-telemetry.md](specifications/l1-telemetry.md)) [L1]
- [x] **Dashboard & Statistics** ([l1-dashboard.md](specifications/l1-dashboard.md)) [L1]
- [x] **Workflow Language** ([l1-workflow-language.md](specifications/l1-workflow-language.md)) [L1]
- [x] **Extensions** ([l1-extensions.md](specifications/l1-extensions.md)) [L1]
- [x] **Automation Pipeline** ([l1-automation-pipeline.md](specifications/l1-automation-pipeline.md)) [L1] — dual-mode event-driven automation; AP-1…AP-7; 8-node taxonomy; implicit (`@ON:` blocks) + explicit (canvas) surfaces; AP-6 AuditProvider observability
- [x] **Automation Canvas** ([l1-automation-canvas.md](specifications/l1-automation-canvas.md)) [L1] — visual pipeline editor; AC-1…AC-6; three-panel layout; implicit read-only + explicit authoring; inspection via AuditProvider stream
- [x] **Harness Engineering** ([l1-harness-engineering.md](specifications/l1-harness-engineering.md)) [L1] — EVALUATE→ANALYZE→IMPROVE loop; HE-1…HE-9; six-component harness taxonomy; artifact extraction mandatory (HE-8); context freshness per iteration (HE-9)
- [x] **Navigation Model** ([l1-navigation-model.md](specifications/l1-navigation-model.md)) [L1] — canonical 12-tab sidebar (Wiki added as #11, Settings → #12); NV-1…NV-5; office tab bar with lazy loading + OfficeState icons; two-tier settings (Global/Local); IDE integration via shell-spawn
- [x] **Voice Input** ([l1-voice-input.md](specifications/l1-voice-input.md)) [L1] — on-device only speech pipeline; VI-1…VI-5; ACTIVATE→CAPTURE→VAD→TRANSCRIBE→REVIEW→INJECT; push-to-talk + toggle modes
- [x] **Deliberation** ([l1-deliberation.md](specifications/l1-deliberation.md)) [L1] — multi-worker structured debate; DL-1…DL-5; parallel independent arguments; orchestrator finality; append-only log in Channels tab
- [x] **Office Control** ([l1-office-control.md](specifications/l1-office-control.md)) [L1] — OfficeState taxonomy (Active/Idle/Paused/Hibernating/Error/Offline); OC-1…OC-5; master switch; token exhaustion hibernation with model substitution; per-subsystem granularity
- [x] **Version Control** ([l1-version-control.md](specifications/l1-version-control.md)) [L1] — virtual staging area on git worktrees; VC-1…VC-6; trunk-based vs Git Flow; role authority table; Conventional Commits with card reference footer
- [x] **Inner Monologue** ([l1-inner-monologue.md](specifications/l1-inner-monologue.md)) [L1] — heartbeat-driven background cognitive process; IM-1…IM-5; 5 intention types; Pulse log; proactivity threshold with suppression
- [x] **Lookahead Planning** ([l1-lookahead-planning.md](specifications/l1-lookahead-planning.md)) [L1] — pre-execution consequence simulation N steps ahead; LP-1…LP-6; 7-category trigger catalog (branch merge/schema migration/file deletion/dep upgrade/security policy/mass refactor/arch change); CONFIRM/MODIFY/ESCALATE/BUDGET_EXHAUSTED conclusions
- [x] **Agent Client Protocol** ([l1-acp.md](specifications/l1-acp.md)) [L1] — transport-agnostic protocol for external callers; ACP-1…ACP-7; session lifecycle, streaming event taxonomy, capability declaration, trust levels (trusted/restricted/anonymous), cross-office ACP relay
- [x] **Global Orchestration** ([l1-global-orchestration.md](specifications/l1-global-orchestration.md)) [L1] — building-level coordination above individual offices; GO-1…GO-6; phase-awareness enforcement (cross-cutting concern catalog), cross-office delegation via ACP relay, building-level escalation with HITL gate

*Concepts authored since plan v2.5.0 (registry sync INDEX v1.0.4 → v1.0.36). All Stable — they gate later implementation phases.*

- [x] **Execution Graph** ([l1-execution-graph.md](specifications/l1-execution-graph.md)) [L1] — directed graph with typed state channels, superstep atomicity, conditional routing, dynamic spawn, interrupt/resume, checkpoint durability
- [x] **Task Graph Model** ([l1-task-graph-model.md](specifications/l1-task-graph-model.md)) [L1] — decomposition algebra: requirement-to-graph, dependency-DAG integrity, deterministic next-selection, journal, drift-driven re-planning, coordinator/executor split
- [x] **Agent Framework Skeleton** ([l1-agent-framework-skeleton.md](specifications/l1-agent-framework-skeleton.md)) [L1] — paradigm-neutral primitive triad (Agent/Work-Unit/Coordinator), 5-pattern coordination catalog, 3 concrete engines, autonomous self-improvement loop
- [x] **Tool Composition** ([l1-tool-composition.md](specifications/l1-tool-composition.md)) [L1] — toolkit as named group with dependency DAG, single authorization surface, deferred tool resolution at catalog scale (TC-7)
- [x] **Code Execution** ([l1-code-execution.md](specifications/l1-code-execution.md)) [L1] — code-execution-as-tool-use: typed sandboxed program with in-language capability calls, confinement parity, stateful cells, observable replay (CE-1…CE-8)
- [x] **Output Contracts** ([l1-output-contracts.md](specifications/l1-output-contracts.md)) [L1] — inline output validation (schema + callable + LLM criteria), retry budget with verdict injection, escalation to ORC-11
- [x] **Work Liveness** ([l1-work-liveness.md](specifications/l1-work-liveness.md)) [L1] — autonomous-work liveness & ownership: atomic claim, affirmative liveness contract, wake coalescing, stranded-work reconciliation, recovery ladder (WL-1…WL-9)
- [x] **Intent Resolution** ([l1-intent-resolution.md](specifications/l1-intent-resolution.md)) [L1] — ground-before-ask, assume-and-record never silently guess, risk-proportional ask-or-assume, correction re-plans dependents (IR-1…IR-7)
- [x] **Code Intelligence** ([l1-code-intelligence.md](specifications/l1-code-intelligence.md)) [L1] — source tree as queryable semantic graph; typed node/edge taxonomy, structural analysis family, intent-aware context assembly, hybrid retrieval (CI-1…CI-17)
- [x] **Knowledge Base** ([l1-knowledge-base.md](specifications/l1-knowledge-base.md)) [L1] — access-controlled document collections, incremental indexing, hybrid RAG retrieval, authorship zones, curation lifecycle (KB-1…KB-10)
- [x] **File Management** ([l1-file-management.md](specifications/l1-file-management.md)) [L1] — content-addressed dedup (SHA-256), metadata decoupled from blobs, access-controlled download, reference-tracking GC, immutable blobs
- [x] **Notes** ([l1-notes.md](specifications/l1-notes.md)) [L1] — persistent structured artifacts, access control, per-user pinning, append-only history, CRDT concurrent merge, soft deletion
- [x] **Folders** ([l1-folders.md](specifications/l1-folders.md)) [L1] — personal hierarchical session containers, non-destructive delete, sortable, unique sibling names
- [x] **Groups** ([l1-groups.md](specifications/l1-groups.md)) [L1] — flat named user sets as a principal type in the resource-sharing model, admin-managed membership, additive access
- [x] **Resource Sharing** ([l1-resource-sharing.md](specifications/l1-resource-sharing.md)) [L1] — uniform fine-grained access model: single grant primitive, three principal types, default-private, owner-invariant, additive grants, audit trail
- [x] **Operational Ledger** ([l1-operational-ledger.md](specifications/l1-operational-ledger.md)) [L1] — id-addressable operational ground truth: atomic predicates, supersede-don't-mutate, canonical-source precedence, verbatim grounding, required-reading (OL-1…OL-10)
- [x] **User Model** ([l1-user-model.md](specifications/l1-user-model.md)) [L1] — persistent evolving model of the person, inferred/evidence-backed/non-authoritative, anti-drift accretion, privacy-first inspect/erase (UM-1…UM-8)
- [x] **Multi-Device Sync** ([l1-multi-device-sync.md](specifications/l1-multi-device-sync.md)) [L1] — personal multi-device convergence, no central coordinator, data-class reconciliation routing (CRDT / 3-way / supersede), causal metadata (SY-1…SY-9)
- [x] **Change Merge** ([l1-change-merge.md](specifications/l1-change-merge.md)) [L1] — concurrent change as typed delta, sub-unit granularity, base fingerprinting, deterministic three-way merge, no-silent-loss (CM-1…CM-9)
- [x] **Project Wiki** ([l1-project-wiki.md](specifications/l1-project-wiki.md)) [L1] — L2 authored: `l2-project-wiki` (RFC, Backlog) — client-facing office-maintained living docs as a projection, grounded/attributed anti-hallucination, freshness-honest, Wiki nav tab (PW-1…PW-8). Realized as a per-office SQLite projection cache (workspace `wiki/wiki.db`), not loose Markdown; rebuildable, read-only to client. No longer concept-only (authored L2 drops the marker per C28 §4)
- [x] **Model Runtime** ([l1-model-runtime.md](specifications/l1-model-runtime.md)) [L1] — L2 realized in **Phase 17**: `l2-model-runtime` (Stable) — local-first on-device serving, provider-abstracted backend, content-addressed model store, fit-gated hardware scheduling, multi-device placement (MR-1…MR-14). No longer concept-only (authored L2 drops the marker per C28 §4)
- [x] **Loop Governance** ([l1-loop-governance.md](specifications/l1-loop-governance.md)) [L1] — L2 building in **Phase 19**: `l2-loop-runner` (Stable) — the governance keystone over every autonomous loop (LG-1…LG-10): declared loop class (execution/evolution), mutation-rights manifest over a closed artifact taxonomy, the criteria-immutability anti-drift spine (an actor never edits the contract that judges it), separated-oracle ownership, independent termination ceiling, tier-escalation gate, auditable mutation ledger, cheapest-trustworthy-oracle preference, and objective persistence across in-session reduction. Promoted RFC→Stable via `/magic.spec` (v1.0.0). No longer concept-only (authored+Stable L2 drops the marker per C28 §4)
- [x] **Generation Budget** ([l1-generation-budget.md](specifications/l1-generation-budget.md)) [L1] — output-side token economy: minimal default reservation, truncation-detect-and-escalate, continue-from-partial, truncation-safe artifacts (GB-1…GB-8)
- [x] **Context Compression** ([l1-context-compression.md](specifications/l1-context-compression.md)) [L1] — third token-economy stage: reversible/bounded re-encoding of bulky structured content, recoverable, content-aware eligibility, runs before eviction (CC-1…CC-8)
- [x] **Retrieval Evaluation** ([l1-retrieval-evaluation.md](specifications/l1-retrieval-evaluation.md)) [L1] — IR-metric measurement of ranked-recall quality (P@K/R@K/MRR/nDCG@K) against labeled fixtures, baseline + regression gate, surface-agnostic (RE-1…RE-11)
- [x] **Application Shell** ([l1-application-shell.md](specifications/l1-application-shell.md)) [L1] — reactive desktop frontend substrate: single-authority state, push reactivity, declarative render, keymap dispatch, workbench composition (AS-1…AS-13)
- [x] **Generative Surface** ([l1-generative-surface.md](specifications/l1-generative-surface.md)) [L1] — agent-rendered interactive visual artifacts as a turn output, sandboxed, closed perception loop, user-controlled, projection-not-source (GS-1…GS-8)
- [x] **Development Workflow** ([l1-development-workflow.md](specifications/l1-development-workflow.md)) [L1] — five-stage agent-assisted pipeline (Design→Plan→Execute→Review→Deliver), task isolation, two-stage quality gate, durable ledger, human checkpoints (DW-1…DW-10)
- [x] **Practice Analytics** ([l1-practice-analytics.md](specifications/l1-practice-analytics.md)) [L1] — diagnostic/coaching engine over session traces: detector/rule separation, portable rule documents, honest data-gap accounting, severity-weighted scoring (PA-1…PA-14)
- [x] **Evaluation Suites** ([l1-evaluation-suites.md](specifications/l1-evaluation-suites.md)) [L1] — declarative version-controlled test suites for customizations, typed grader taxonomy, weighted thresholds, baseline+regression gate (ES-1…ES-16)
- [x] **Evaluations** ([l1-evaluations.md](specifications/l1-evaluations.md)) [L1] — per-message feedback: discrete sentiment + tags + free text, immutable audit, privacy-first, feeds router scoring and analytics
- [x] **Requirement Checklists** ([l1-requirement-checklists.md](specifications/l1-requirement-checklists.md)) [L1] — generated, domain-tailored, falsifiable questions validating requirements quality before code; pre-planning quality gate (RQ-1…RQ-8)
- [x] **Operational Health** ([l1-operational-health.md](specifications/l1-operational-health.md)) [L1] — on-device observability over runtime traces: explainable health score, threshold alerts, trend/anomaly detection, cost accounting, measure-don't-act (OH-1…OH-8)
- [x] **Facilitation** ([l1-facilitation.md](specifications/l1-facilitation.md)) [L1] — advisory thinking-facilitation: method catalog, elicitation loop, three ideation stances, divergence-before-convergence, advisory boundary (FC-1…FC-12)
- [x] **Agent Tool Ergonomics** ([l1-agent-tool-ergonomics.md](specifications/l1-agent-tool-ergonomics.md)) [L1] — design discipline for agent-facing tool surfaces: sufficiency-to-stop, recoverable-as-success, absence-as-signal, adapt-tool-to-agent (ATE-1…ATE-9)
- [x] **Policy Governance** ([l1-policy-governance.md](specifications/l1-policy-governance.md)) [L1] — administrative control over configuration: layered tier precedence, un-overridable managed tier, surface lockdown, integrity-verified source (PG-1…PG-8)
- [x] **Process Integrity** ([l1-process-integrity.md](specifications/l1-process-integrity.md)) [L1] — in-memory hardening of the agent's own process: no crash-dump leak, refuse tracer attach, scrub injection-vector env, early enforcement (PI-1…PI-7)
- [x] **Execution Sandbox** ([l1-execution-sandbox.md](specifications/l1-execution-sandbox.md)) [L1] — OS-level confinement across four deny-by-default axes (operations/privileges/resources/filesystem), capability drop, image pinning, fail-closed (ES-1…ES-9)
- [x] **Messaging Gateway** ([l1-messaging-gateway.md](specifications/l1-messaging-gateway.md)) [L1] — one gateway, many per-platform adapters; normalized contract, identity pairing, principal-keyed continuity, exposure safety, per-platform fault isolation (MG-1…MG-9)
- [x] **Browser Control** ([l1-browser-control.md](specifications/l1-browser-control.md)) [L1] — agent-driven web browser: persistent control daemon, accessibility-tree addressing, side-effect-classified commands, layered injection defense (BC-1…BC-12)

*Concepts authored since plan v2.6.0 (registry sync INDEX v1.0.36 → v1.0.39). All Stable — they gate later implementation phases.*

- [x] **Event Mesh** ([l1-event-mesh.md](specifications/l1-event-mesh.md)) [L1] — single in-process routing substrate: uniform event envelope, topic-based addressing, producer/consumer decoupling (producers name what happened, never a consumer) (EM-1…EM-10)
- [x] **Claim Verification** ([l1-claim-verification.md](specifications/l1-claim-verification.md)) [L1] — runtime hallucination detector over `(claims, sources)`: per-claim verdict (supported/contradicted/unverifiable) with evidence span; grounding-only, never world-truth (CV-1…CV-9)
- [x] **Perspective Model** ([l1-perspective-model.md](specifications/l1-perspective-model.md)) [L1] — theory-of-mind representation: every belief keyed by `(observer → subject)`, generalizing the single-vantage user model to perspectival self/other knowledge (PM-1…PM-8)

*Concepts authored since plan v2.8.0 (registry sync INDEX v1.0.39 → v1.0.61). All Stable, all `concept-only` (C28) — durable design contracts with no authored L2 yet; the marker auto-reverts when an `Implements:` L2 lands.*

- [x] **Work Convergence** ([l1-work-convergence.md](specifications/l1-work-convergence.md)) [L1] — all office activity through one legible surface (the board); materialize/drive/surface relations, exhaustive+exclusive, no shadow work (CONV-1…CONV-8)
- [x] **Context Provenance** ([l1-context-provenance.md](specifications/l1-context-provenance.md)) [L1] — per-fragment trust provenance, untrusted-neutralized-by-default at every composition boundary, sticky monotonic trust, structural injection defense (CP-1…CP-8)
- [x] **Attestation** ([l1-attestation.md](specifications/l1-attestation.md)) [L1] — signed, offline-verifiable artifact witnesses: content-set binding, authorship, typed claims, default-deny unattested, revocation-by-supersession (AT-1…AT-9)
- [x] **Tool Receipts** ([l1-tool-receipts.md](specifications/l1-tool-receipts.md)) [L1] — model-unforgeable per-action execution receipts; narrated-but-unreceipted actions treated as fabricated; ephemeral isolated signing secret (TR-1…TR-9)
- [x] **Issue Reporting** ([l1-issue-reporting.md](specifications/l1-issue-reporting.md)) [L1] — user-initiated report button/function: narrative + bug/feedback/idea categories, preview-before-send consent, opt-in diagnostics, shared error-reporting pipeline (ISS-1…ISS-7)
- [x] **Process Monitor** ([l1-process-monitor.md](specifications/l1-process-monitor.md)) [L1] — read-only live view of the app's own OS process tree with per-process CPU/memory, topology classified against sanctioned boundaries, CLI/TUI/GUI parity (PM-1…PM-7)
- [x] **Harness Optimization** ([l1-harness-optimization.md](specifications/l1-harness-optimization.md)) [L1] — outer-loop search over a harness-candidate space toward a frontier; archivable graded candidates, host-side search policy
- [x] **Harness Composition** ([l1-harness-composition.md](specifications/l1-harness-composition.md)) [L1] — right-sizing discipline for assembling harnesses; justification gates and pruning (HC), applied at role level by ROL-9
- [x] **Context Attachment** ([l1-context-attachment.md](specifications/l1-context-attachment.md)) [L1] — user-attached context artifacts as first-class turn inputs
- [x] **Agent Co-Evaluation** ([l1-agent-coevaluation.md](specifications/l1-agent-coevaluation.md)) [L1] — Performance = f(Model, Harness): the (model, harness) diagnostic matrix, orthogonal task labels, comparability contract, per-layer failure attribution (ACE)
- [x] **Cache-Stable Context** ([l1-cache-stable-context.md](specifications/l1-cache-stable-context.md)) [L1] — prompt-cache stability discipline: provider cache key as a cost lever, frozen prefixes, cache-warmth as a routing signal (CSC)
- [x] **Model Benchmarking** ([l1-model-benchmarking.md](specifications/l1-model-benchmarking.md)) [L1] — hardcoded three-class micro-benchmark (code/content/instruction-compliance) scoring base models on quality + time + tokens/cost into router-consumed fitness profiles (MB-1…MB-9)
- [x] **Crash Recovery** ([l1-crash-recovery.md](specifications/l1-crash-recovery.md)) [L1] — crash-safe writes, verified single-instant snapshots, unclean-shutdown detection, strict recovery ladder, work resumption + honest loss reporting (CR-1…CR-9)
- [x] **Parallel Staffing** ([l1-parallel-staffing.md](specifications/l1-parallel-staffing.md)) [L1] — same-specialty scale-out: ephemeral same-role instances under one accountable lead, parallelism only via disjoint decomposition, bounded width, first-class fan-in (PS-1…PS-9)
- [x] **Employee Availability** ([l1-employee-availability.md](specifications/l1-employee-availability.md)) [L1] — workforce state model: available/working/resting/on-leave/truant + released exit; office-state dominance, detected-never-declared truancy, resource-honest rest, honest metaphor binding (EMP-1…EMP-9)

*Concepts authored since plan v2.9.0 (registry sync INDEX v1.0.61 → v1.0.62). All Stable, all `concept-only` (C28).*

- [x] **Specialty Exemplars** ([l1-specialty-exemplars.md](specifications/l1-specialty-exemplars.md)) [L1] — per-specialty competency instrument, sibling to model-benchmarking pointed at staffing competency: one small concentrated exemplar suite per specialty
- [x] **Project Priority** ([l1-project-priority.md](specifications/l1-project-priority.md)) [L1] — cross-office resource arbitration: explicit user/board-set ordered priority per office/project governing how finite shared resources (building-level token/budget pool) are distributed under scarcity
- [x] **Project Support** ([l1-project-support.md](specifications/l1-project-support.md)) [L1] — the office's operational posture for a delivered/live project: ongoing upkeep (content updates, issue-driven fixes, product improvement) beginning where the build ends

*Concepts authored since plan v2.12.0 (registry sync INDEX v1.0.64 → v1.0.93). All Stable, concept-only until an `Implements:` L2 lands (C28).*

- [x] **Report Prompting & Diagnostic Findings** ([l1-report-prompting.md](specifications/l1-report-prompting.md)) [L1] — system-suggested, user-completed reporting bridging error-reporting and issue-reporting: invitation-never-transmission, closed trigger taxonomy, off/passive/active modes, local findings ledger for user invitations + developer triage (RP-1…RP-8)
- [x] **Work Import** ([l1-work-import.md](specifications/l1-work-import.md)) [L1] — bounded one-directional onboarding migration of an external tracker's backlog into the canonical office model
- [x] **Interception Model** ([l1-interception-model.md](specifications/l1-interception-model.md)) [L1] — interception discipline for cross-cutting behaviour: observe/decide/transform taxonomy, fixed fail-direction per class, transitive guard enforcement (INT-1…INT-8)
- [x] **Diagnostic Log Plane** ([l1-diagnostic-log.md](specifications/l1-diagnostic-log.md)) [L1] — forensic observation plane of last resort: survives native crashes, pre-init boot failures, dependency output; bounded rotating retention, consent-gated egress (DL-1…DL-8)
- [x] **Log Legibility & Economy** ([l1-log-legibility.md](specifications/l1-log-legibility.md)) [L1] — dual-audience (human + AI) contract over every observation channel: one canonical event, faithful projections, overload bounded by construction (LL-1…LL-9)
- [x] **System Readout & Refresh** ([l1-system-readout.md](specifications/l1-system-readout.md)) [L1] — one unified refresh mechanism for every live readout: closed trigger taxonomy, coalescing, honest staleness, visibility-gated economy (SR-1…SR-8)
- [x] **Search** ([l1-search.md](specifications/l1-search.md)) [L1] — application-wide capability to find anything the user can see through one query surface
- [x] **Usage Allowance** ([l1-usage-allowance.md](specifications/l1-usage-allowance.md)) [L1] — external-allowance member of the token economy: quota windows distinct from input-context and output-generation budgets
- [x] **Conversational Control** ([l1-conversational-control.md](specifications/l1-conversational-control.md)) [L1] — the natural-language chat/prompt surface as a first-class control plane over the application's own management operations
- [x] **Simulation** ([l1-simulation.md](specifications/l1-simulation.md)) [L1] — playing out a generated mechanism (workflow / pipeline / role interaction / task graph / schedule) to see how it behaves before it acts
- [x] **Tool-Call Transport** ([l1-tool-call-transport.md](specifications/l1-tool-call-transport.md)) [L1] — the wire seam by which a logical tool invocation crosses the agent↔model boundary and the reply is decoded back
- [x] **Design Identity** ([l1-design-identity.md](specifications/l1-design-identity.md)) [L1] — the office's swappable visual language + craft bar for user-facing visual output
- [x] **Competitive Execution** ([l1-competitive-execution.md](specifications/l1-competitive-execution.md)) [L1] — best-of-N quality-selection fan-out, the third coordination-family mode beside parallel-staffing and deliberation
- [x] **Component Scanning** ([l1-component-scanning.md](specifications/l1-component-scanning.md)) [L1] — admission vetting / threat scanning of third-party components (skill / tool-server / plugin) before trust
- [x] **Iterative Refinement** ([l1-iterative-refinement.md](specifications/l1-iterative-refinement.md)) [L1] — generate–evaluate–refine, the temporal member of the coordination family trading depth for quality
- [x] **Extension Marketplace** ([l1-extension-marketplace.md](specifications/l1-extension-marketplace.md)) [L1] — curated catalog / origin-and-discovery dimension for addressable extensions
- [x] **Inference Cache** ([l1-inference-cache.md](specifications/l1-inference-cache.md)) [L1] — durable tiered prefix-addressed cache of computed inference state, the storage-side companion to cache-stable authoring
- [x] **Progressive Disclosure** ([l1-progressive-disclosure.md](specifications/l1-progressive-disclosure.md)) [L1] — tiered on-demand context loading, the lazy-loading member of the context-economy family
- [x] **Agent Federation** ([l1-agent-federation.md](specifications/l1-agent-federation.md)) [L1] — interoperation with independent external agents as verifiable peers
- [x] **Deployment Neutrality** ([l1-deployment-neutrality.md](specifications/l1-deployment-neutrality.md)) [L1] — local-first server-free default as the whole product, with a pluggable door for auth + remote user-data backend
- [x] **Data Lineage** ([l1-data-lineage.md](specifications/l1-data-lineage.md)) [L1] — derivation traceability across a dataflow: what produced this datum, through which transformations
- [x] **Pattern Codification** ([l1-pattern-codification.md](specifications/l1-pattern-codification.md)) [L1] — the disciplined memory-to-governance pathway by which a stable behavioral pattern becomes an enforceable rule
- [x] **Provenance Taint** ([l1-provenance-taint.md](specifications/l1-provenance-taint.md)) [L1] — at-rest, persistent, propagating origin-trust taint on stored data
- [x] **Action Gating** ([l1-action-gating.md](specifications/l1-action-gating.md)) [L1] — authorization friction proportional to an action's consequence
- [x] **Recursive Decomposition** ([l1-recursive-decomposition.md](specifications/l1-recursive-decomposition.md)) [L1] — the third stance on an over-window input beside compression and progressive disclosure: process it in bounded parts

*Concept authored since plan v2.13.0 (registry sync INDEX v1.0.93 → v1.0.94). Stable with an already-implemented L2 (Phase 6) — a fully-realized concept, not concept-only.*

- [x] **Deep Research** ([l1-deep-research.md](specifications/l1-deep-research.md)) [L1] — autonomous investigation of an open-ended question over external, open, *discovered* sources ending in a claim-verified, source-attributed, coverage-honest report (DR-1…DR-11); the retro-authored concept parent of the already-Done `l2-deep-research` engine (Phase 6), giving that L2 a proper L1 concept; composes recursive-decomposition (sub-question tree) / claim-verification (delivery gate) / context-provenance (untrusted-fetch) / loop-governance (frozen criteria + ceiling); the disciplined iterate-evaluate-keep/discard loop it shares with the harness family is composed, not duplicated — same discipline, different subject (an external question vs. the agent's own harness)

*Concepts authored since plan v2.13.1 (registry sync INDEX v1.0.94 → v1.0.96). All Stable. Only `l1-tokenization-boundary` is `concept-only` (C28) — the other two each already have an authored `Implements:` L2 (both RFC, parked in Backlog), and authoring alone drops the marker per C28 §4. Their `[x]` still means "concept authored & Stable", not "implemented".*

- [x] **Tokenization Boundary** ([l1-tokenization-boundary.md](specifications/l1-tokenization-boundary.md)) [L1] — `concept-only` — the frontier where the system's characters become the receiving model's symbol alphabet, and the contract every size/cut/cost/trust decision across it must satisfy (TB-1…TB-10): the model's alphabet is ground truth for size, cost, cut points, and cache identity (TB-1); encoding is **not composable** — appending content retroactively re-encodes what precedes it, so any design that splits content, encodes the pieces independently, and assumes they concatenate is wrong by construction (TB-2); cut only at seams the encoder declares stable, stability asserted never inferred (TB-3); control symbols occupy a disjoint sub-alphabet structurally unreachable by encoding any content — a property of the encoder, not a sanitizer after it — which is precisely why they are the only provably stable seams (TB-4); minting a control symbol is a per-symbol authority held by trusted scaffolding, and the boundary's default for control-looking content is **refusal** (TB-5); counts under different encoders are different quantities, never substituted or aggregated (TB-6); an unidentifiable encoder fails loudly, and a declared estimate never grounds an irreversible decision (TB-7); an encoder is a versioned, digest-verified artifact whose identity covers its control sub-alphabet (TB-8); round-trip fidelity with declared — never silently default — lossy decode, and a truncated symbol sequence is not a text prefix (TB-9); symbol↔source-span correspondence is reportable (TB-10). Supplies the structural floor beneath context-provenance (CP-9), the seam rule beneath cache-stable-context (CSC-12) and inference-cache (IC-10), and the measure beneath generation-budget / context-compression / trace-derived cost accounting; nodus realizations = NL-19 + NE-14
- [x] **Office Archetype** ([l1-office-archetype.md](specifications/l1-office-archetype.md)) [L1] — L2 authored: `l2-archetype-catalog` (RFC, Backlog) — domain-scoped staffing **priors** (software engineering, advertising agency, finance department). An archetype is a prior on hiring, **never a roster**: it carries a candidate pool, an org shape adopted only if the work grows, a bounded seed, and domain norms; it changes what the manager expects, never how it decides (OA-1/OA-2), preserving OFF-4 and the §5 rejection of the fixed org chart. The pool classifies hires, never refuses them (OA-3). An archetype carries **no authority** — the schema has no vocabulary for permissions, budget, or autonomy (OA-4, SEC-10); third-party archetype prose is agent instruction and is vetted before admission (OA-5). Composes the role catalog, never forks it (OA-10); archetypes are falsifiable via recorded deviations (OA-9); the archetype-free office is complete (OA-11)
- [x] **Background Activation** ([l1-background-activation.md](specifications/l1-background-activation.md)) [L1] — L2 authored: `l2-service-activation` (RFC, Backlog) — how a host becomes the always-on hub INV-4 presumes. Three activation modes (manual default / login-scoped / system-scoped) differing in lifecycle owner, privilege, and supervision — never in behavior (BA-2). Background activation is a **SEC-10 authority grant, not a preference**: the agent has no write path to it (BA-4), so activation state lives in the OS registration and is never mirrored into agent-writable config. Consent discloses unattended autonomy and the spend ceiling (BA-5); elevation authorizes registration only, never run-time privilege (BA-6); state is observed from the OS, never remembered (BA-8); removal is verified, not assumed (BA-7); one engine per state root — frontends attach, never duplicate (BA-11); spoke hosts refuse visibly (BA-10)

*Memory-cluster concepts promoted RFC→Stable in v2.16.0 (INDEX v1.0.98). Both now carry an authored `Implements:` L2 (RFC, Backlog) — NOT concept-only; their `[x]` means "concept authored & Stable". They refine the Stable substrate `l1-memory-model` rather than replacing it.*

- [x] **Memory Consolidation** ([l1-memory-consolidation.md](specifications/l1-memory-consolidation.md)) [L1] — L2 authored: `l2-memory-consolidation` (RFC, Backlog) — the consolidation & corpus-maintenance layer between substrate and query surface (MC-1…MC-10): processing-depth tiers raw→working→consolidated, scheduled incremental idempotent consolidation, write-time additive-only relationship binding, the create/corroborate/refine/correct action algebra, hard fact-vs-derived-signal separation, the merge/split/archive/summarize maintenance set (confidence proportional to blast radius), emergent topic-cluster summary nodes, multiplicative offline-precomputed recall ranking, co-edit write safety, and advisory interest extraction
- [x] **Memory Intelligence** ([l1-memory-intelligence.md](specifications/l1-memory-intelligence.md)) [L1] — L2 authored: `l2-memory-intelligence` (RFC, Backlog) — the active query & intelligence surface over the memory substrate (MI-1…MI-13): grounded cited `answer` with honest refusal, temporal recall modes (as-of / changed-since / recent), non-silent conflict surfacing, periodic intelligence digest, salience-honest capture policy, procedural distillation, structured filter predicate, reversible lifecycle states, capture-time normalization, capture directives, raw/inferred modes, and gated experience reuse

*Concepts absorbed in v2.21.0 (registry sync INDEX v1.0.107 → v1.0.117). All Stable, all `concept-only` (C28) — none has an authored `Implements:` L2 yet; their `[x]` means "concept authored & Stable", not "implemented".*

- [x] **Improvement Loop** ([l1-improvement-loop.md](specifications/l1-improvement-loop.md)) [L1] — `concept-only` — closed product self-improvement loop (IMP-1…IMP-9): in-work non-blocking observation from already-recorded planes, product-subject-only generalized findings, user-governed submission autonomy off/confirm/automatic through the one audited reporting pipeline, autonomous upstream triage service (itself a dogfooded Cronus office), evidence-over-eloquence intake, append-only rejection memory, disposition feedback; zero-server optional-by-construction
- [x] **Solution Frugality** ([l1-solution-frugality.md](specifications/l1-solution-frugality.md)) [L1] — `concept-only` — least-code-that-is-correct discipline (FR-1…FR-10): reuse-before-build ladder, "lazy not negligent" floor, root-cause locality, tunable non-self-elevated intensity, disclosed-simplification debt ledger with upgrade triggers (FR-6 — realized this wave as disclosure patches on four shipped-seam L2s), complexity-scoped review, output economy, measured-not-asserted savings
- [x] **Completion Verification** ([l1-completion-verification.md](specifications/l1-completion-verification.md)) [L1] — `concept-only` — work-state member of the grounding/honesty family: a complete/passing/fixed claim needs fresh same-turn evidence from the authoritative check, else the actual verified state is reported
- [x] **Derived-Artifact Handoff** ([l1-derived-artifact-handoff.md](specifications/l1-derived-artifact-handoff.md)) [L1] — `concept-only` — portable handoff of deterministically-rebuildable derived artifacts (code graph, embeddings, precomputed indexes): import a snapshot, fill only the local diff, never merge derived state as truth
- [x] **Context Degradation** ([l1-context-degradation.md](specifications/l1-context-degradation.md)) [L1] — `concept-only` — the failure model the context-economy family prevents: degradation as a risk-signal-managed continuum (not a token-limit event), positional salience, long-context health discipline
- [x] **Optimization Integrity** ([l1-optimization-integrity.md](specifications/l1-optimization-integrity.md)) [L1] — `concept-only` — verification discipline for silently-degradable optimizations (compression, cache warmth, ranked recall, output shaping): a breach produces no error, only cost/quality decay — so integrity is asserted by measurement, not by absence of failure
- [x] **Cost Rating** ([l1-cost-rating.md](specifications/l1-cost-rating.md)) [L1] — `concept-only` — the owned middle step of the cost pipeline (meter → rate → charge): single-producer monetary derivation, per-class rates (input/output/cache read/creation), honesty rules in one place
- [x] **Human Intervention** ([l1-human-intervention.md](specifications/l1-human-intervention.md)) [L1] — `concept-only` — out-of-band hand-editing of work artifacts as a first-class action: baseline-comparison drift detection, reconciliation instead of overwrite, distinct from live-turn steering and the authority plane
- [x] **Session Reinforcement** ([l1-session-reinforcement.md](specifications/l1-session-reinforcement.md)) [L1] — `concept-only` — typed corrective advisories with the tighten-only direction invariant (a relaxing injection is invalid by construction), host-only issuance, long-horizon re-anchoring, supervisor probes, budgeted non-nagging emission
- [x] **Knowledge Horizon** ([l1-knowledge-horizon.md](specifications/l1-knowledge-horizon.md)) [L1] — `concept-only` — declared knowledge horizon + injected now, retrieval-before-assertion for unrecognized/post-horizon entities, now-anchored queries; generation-side complement of claim-verification
- [x] **Generation Shaping** ([l1-generation-shaping.md](specifications/l1-generation-shaping.md)) [L1] — `concept-only` — output character/effort modulation orthogonal to generation-budget length (GS-1…GS-9): turn-classified verbosity steering as request-instruction, floored effort modulation, correctness supremacy as the load-bearing floor, counterfactual measurement honesty

*Concept absorbed in v2.27.0 (registry sync INDEX v1.0.117 → v1.0.120). Stable, `concept-only` (C28) — no authored `Implements:` L2 yet; `[x]` means "concept authored & Stable", not "implemented".*

- [x] **Value Settlement** ([l1-value-settlement.md](specifications/l1-value-settlement.md)) [L1] — `concept-only` — outbound value settlement: paying an external party (paid API/service/charging peer) for value received, the third budget axis beside inbound usage-allowance and per-request generation-budget (VS-1): principal-set agent-read-only spending envelope on the human-write-only authority plane (VS-2, SEC-10), bounded-authority custody un-exceedable even if compromised with the funding secret never in context (VS-3, SEC-1), every payment gated at the top of the action-gating ladder with an allowlisted recipient a precondition not a waivable friction (VS-4), fail-closed — path down / envelope exhausted / denied blocks the paid action, never falls back to unmetered access or unbounded retry (VS-5), a bounded machine-negotiable payment-required handshake that settles once or aborts, never loops (VS-6), receipted + ledgered accountability where unreceipted = unaccounted (VS-7, TR-4), an opt-in/local-first/rail-neutral provider seam where the core performs no payment and names no rail, peer payee = federation settlement (VS-8). nodus realization = `l1-nodus-portability` LP-17 (nodus workspace, separate scope)

## Phase 1 — Seed I: Foundation

*Buildable monorepo + engine skeleton + state + security. The soil and the seed coat.*

> **Done (2026-06-30):** Rust foundation complete and verified — filesystem path model, core skeleton + contract, durable state seam, and the security baseline (secret store / redaction / default-deny egress). The JS/Tauri scaffold under source-layout + technology-stack was originally deferred here on a missing toolchain; it has since **landed via Phase 8 `T-8A01`** (pnpm + Tauri v2 provisioned, `apps/desktop` + `packages/ui` scaffolded, full toolchain green) — so the Seed's polyglot workspace is realized. The two remaining security-hardening specs (`l2-sandbox-policy`, `l2-multi-user-auth`) never gated any downstream phase and are **relocated to Phase 9 (Operational Hardening)** where they belong. Phase 1 is closed on its actual scope: the Rust seed. See `tasks/phase-1.md`.

- [x] **Source Layout** ([l2-source-layout.md](specifications/l2-source-layout.md)) [L2] — Cargo + pnpm workspaces (`crates/{core,nodus,cli,tui}`), polyglot runner (JS workspaces scaffolded via Phase 8 T-8A01)
- [x] **Technology Stack** ([l2-technology-stack.md](specifications/l2-technology-stack.md)) [L2] — toolchain (Rust; Vite/React 19 + Tauri v2 provisioned via Phase 8 T-8A01)
- [x] **Filesystem Layout** ([l2-filesystem-layout.md](specifications/l2-filesystem-layout.md)) [L2] — OS-native path resolver, state bootstrap
- [x] **Core Library** ([l2-core-library.md](specifications/l2-core-library.md)) [L2] — engine crate, public contract, durable state
- [x] **Security** ([l2-security.md](specifications/l2-security.md)) [L2] — secret store, gitignore, redaction, egress gate, sandbox, SSRF guard, internal tool loopback; config integrity shields (three-state lock, SHA-256 seal, drift detection)

## Phase 2 — Seed II: Workflow Runtime (`crates/nodus`)

*The embeddable workflow-language runtime the core depends on. Behavior-preserving Rust port, built as a vertical slice first (see runtime spec §4.5).*

> **Done (2026-06-21):** 12 atomic tasks across 7 tracks complete — lexer → parser/AST → transpiler → executor → validator/lint + library API. 126 tests (83 unit + 26 parity + 17 WFL-invariant), 0 failures. Parity verified against the reference corpus; all WFL-1..9 L1 invariants covered. See [tasks/phase-2.md](tasks/phase-2.md).

- [x] **Workflow Runtime** ([l2-workflow-runtime.md](specifications/l2-workflow-runtime.md)) [L2] — lexer → parser/AST → transpiler → executor → validator/lint; step-binding to core subsystems

## Phase 3 — Stem: CLI

*The first usable surface, growing straight from the seed. Command framework + grammar + core binding; subsystem commands attach in later phases.*

> **Done (2026-06-21):** 8 atomic tasks across 4 tracks complete — clap 4 binary scaffold + help/init/status + workflow (scaffold/validate/run/transpile). 13 unit tests + 6 integration smoke tests = 19 total, 0 failures. See [archives/tasks/phase-3.md](archives/tasks/phase-3.md).

- [x] **CLI** ([l2-cli.md](specifications/l2-cli.md)) [L2] — `cronus` binary, command grammar, core bindings, initial commands (help/init/status/workflow)

## Phase 4 — Core Subsystems

*Memory, routing, and workspace management. Each lands with its CLI commands.*

> **Done (2026-06-22):** 14 atomic tasks across 5 tracks complete — memory store + encryption + codegraph (crate), model router + error recovery + context router, agent session loop + context management + session checkpoint + inbox + agent autonomy, workspace management + agent constitution, plus CLI commands and integration tests. 453 tests, 0 failures. See `tasks/phase-4.md`.

- [x] **Memory Store** ([l2-memory-store.md](specifications/l2-memory-store.md)) [L2] — SQLite + sqlite-vec + FTS5 + tags, archivist curator; trust scoring (asymmetric feedback, TRUST_MIN_SEARCH=0.3, retrieval_count), shallow entity links (seed of deferred graph), HRR phase encoding (model-free vector fallback, SNR capacity guard); Bellman propagation (gamma=0.9, alpha=0.1, max_depth=2, 1h temporal credit window), session chaining (2h window, Continuation links), VerificationState weight ladder (Untested=0.30 → ValidatedCrossProject=1.00)
- [x] **Memory Encryption** ([l2-memory-encryption.md](specifications/l2-memory-encryption.md)) [L2] — AES-256-GCM per-chunk encryption, Argon2id KDF, OS keychain key storage, transactional rotation (depends on memory-store + security)
- [x] **Code Graph** ([l2-codegraph.md](specifications/l2-codegraph.md)) [L2] — tree-sitter extraction, SQLite + FTS5 + sqlite-vec embeddings, RRF fusion, auto-index (depends on memory-store)
- [x] **Model Router** ([l2-model-router.md](specifications/l2-model-router.md)) [L2] — local-first, difficulty/cost routing, fallback cascade, semantic cache; semantic router pool (embedding encoder + tolerance threshold, cost-optimal selection)
- [x] **Model Error Recovery** ([l2-model-error-recovery.md](specifications/l2-model-error-recovery.md)) [L2] — error taxonomy, classification pipeline, credential pool; provider health probe (ProviderHealthStatus, multi-hop subprobes, context window discovery) (depends on model-router)
- [x] **Agent Session Loop** ([l2-agent-session.md](specifications/l2-agent-session.md)) [L2] — TurnContext, IterationBudget, ContextEngine interface, tool-call loop seams, KV-cache stability, oversized-result summarizer, stop hooks, InterruptFence, post-turn hooks; text loop detection, goal re-entry cap (depends on model-router + context-router)
- [x] **Agent Autonomy** ([l2-agent-autonomy.md](specifications/l2-agent-autonomy.md)) [L2] — autonomy ladder, SecurityPolicy gate, CommandRiskLevel classifier, ActionTracker rolling cap, approval gate lifecycle; ApprovalRecord manager (create/register separation, 15s grace period) (depends on agent-session + tool-security + scheduler)
- [x] **Inbox** ([l2-inbox.md](specifications/l2-inbox.md)) [L2] — SQLite inter-actor inbox, send+drain pipeline, GC_TTL_MS=7d, MAX_DRAIN_PER_TURN=100, InboxArrived bus event, synthetic user message injection (depends on agent-session + storage)
- [x] **Session Checkpoint** ([l2-session-checkpoint.md](specifications/l2-session-checkpoint.md)) [L2] — three-file hierarchy (checkpoint/memory/notes.md), section-budgeted reads, fork-agent prefix-cache parity write, boundary invariant, system reminders, progress reconcile, auto-memory triggers (depends on agent-session + memory-store + agent-registry)
- [x] **Context Management** ([l2-context-management.md](specifications/l2-context-management.md)) [L2] — adaptive token budget, 8-step trim cascade, LLM-driven compaction, _protected messages; context engine registry (ContextEngineHostCapability, per_turn/thread_bootstrap projection, runtime modes) (depends on agent-session + model-router)
- [x] **Context Router** ([l2-context-router.md](specifications/l2-context-router.md)) [L2]
- [x] **Workspace Management** ([l2-workspace-management.md](specifications/l2-workspace-management.md)) [L2]
- [x] **Agent Constitution** ([l2-agent-constitution.md](specifications/l2-agent-constitution.md)) [L2] — per-workspace identity files (SOUL/PROFILE/MEMORY/HEARTBEAT/BOOTSTRAP), bootstrap ritual (depends on workspace-management + memory-store)

## Phase 5 — Office Work Engine

*Roles, board, scheduling, quality gates, and extensions — the machinery and capabilities that run work.*

> **Done (2026-06-22):** 11 atomic tasks across 4 tracks complete — tool security (skill scanner + runtime guard + guardrail pipeline), role catalog (25 built-in roles, hire/fire/adapter), kanban board (card CRUD + state machine + archival), scheduler (recurring + oneshot + cron + isolated sessions), budget engine (hierarchical policies + hard-stop enforcement + monthly reset), execution workspace (git worktree lifecycle + finalize write-back gate), quality pipeline (per-language gate runner + board integration), extension registry (lifecycle states + component auto-discovery + MCP transport variants), plugin hooks (9 HookEvents + rule evaluation engine + hook security), agent registry (7 built-ins + user config layer + generate-from-description seam), learning loop (post-turn review fork + skill package format + curator approval gate). 36 test suites, 0 failures. See [archives/tasks/phase-5.md](archives/tasks/phase-5.md).

- [x] **Role Catalog** ([l2-role-catalog.md](specifications/l2-role-catalog.md)) [L2]
- [x] **Kanban Board** ([l2-kanban-board.md](specifications/l2-kanban-board.md)) [L2] — implemented at 1.0.x; the 1.1.0 KAN-8 custom-boards delta is tracked as an explicit Phase 10 item
- [x] **Scheduler** ([l2-scheduler.md](specifications/l2-scheduler.md)) [L2] — recurrence + cron + webhooks + event-driven triggers; cron isolated session execution (session key, model preflight, run log, delivery dispatch, failure notification)
- [x] **Budget Engine** ([l2-budget-engine.md](specifications/l2-budget-engine.md)) [L2] — hierarchical budget policies, cost events, hard-stop enforcement, monthly reset (depends on roles + kanban)
- [x] **Execution Workspace** ([l2-execution-workspace.md](specifications/l2-execution-workspace.md)) [L2] — isolated execution environments, no-remote-git contract, finalize write-back gate; git worktree lifecycle (slug naming, boot sequence, isPristine, reset+prune, events) (depends on security + kanban)
- [x] **Quality Pipeline** ([l2-quality-pipeline.md](specifications/l2-quality-pipeline.md)) [L2]
- [x] **Extension Registry** ([l2-extension-registry.md](specifications/l2-extension-registry.md)) [L2] — skills / MCP / plugins, sandboxed; skill generation; component auto-discovery (commands/agents/skills/hooks default dirs + manifest overrides, ${PLUGIN_ROOT} portable path); SKILL.md trigger format; agent definition frontmatter (name/description/model/color/tools); command definition frontmatter (dynamic tokens: $ARGUMENTS/$1/@file/!bash!/${PLUGIN_ROOT}); MCP transport variants (stdio/sse/http/ws, tool naming convention `mcp__<plugin>_<server>__<tool>`) (depends on roles + security + workflow runtime)
- [x] **Plugin Hooks** ([l2-plugin-hooks.md](specifications/l2-plugin-hooks.md)) [L2] — actor.preStop/postStop ReAct loops, ActorMatcher filter, aggregated decision, file hooks auto-discovery, sequential external plugin loading, HookEvent observability; tool-event hook API (9 events: PreToolUse/PostToolUse/Stop/SubagentStop/SessionStart/SessionEnd/UserPromptSubmit/PreCompact/Notification, prompt+command models, parallel execution, hooks.json format, matcher syntax); rule evaluation engine (block/warn, AND conditions, 6 operators); hook security model (input validation, path safety, quoting, timeouts) (depends on extension-registry + agent-session)
- [x] **Agent Registry** ([l2-agent-registry.md](specifications/l2-agent-registry.md)) [L2] — built-in and custom agent catalog, permission layer stack, fork-agent checkpoint-writer contract, generate-from-description API, default agent resolution (depends on role-catalog + tool-security + model-router + session-checkpoint)
- [x] **Learning Loop** ([l2-learning-loop.md](specifications/l2-learning-loop.md)) [L2] — post-turn background review fork, skill package format, curator (depends on extension-registry + memory-store + agent-session)
- [x] **Tool Security** ([l2-tool-security.md](specifications/l2-tool-security.md)) [L2] — two-layer defense: static skill scanner (8 categories) + runtime tool guard (10 threat categories, approval escalation, hard-blocked patterns)

## Phase 6 — Orchestration & Autonomy

*Coordination protocol that ties subsystems into an autonomous office.*

> **Done (2026-06-22):** 5 atomic tasks across 3 tracks complete — orchestration engine (delegation, goal/judge/budget, tier hierarchy, action ranking), trigger triage (4-outcome classifier + dedup + rate limiting), mission mode (two-phase PRD + execution loop), deep research (iterative search engine + untrusted content wrapping), plus validation track. See [archives/tasks/phase-6.md](archives/tasks/phase-6.md).

- [x] **Orchestration** ([l2-orchestration.md](specifications/l2-orchestration.md)) [L2] — delegation, /goal+judge+budget, briefings, adaptive topology, agent tier hierarchy (Chat/Reasoning/Worker), MAX_SPAWN_DEPTH=3, toolkit action ranking
- [x] **Trigger Triage** ([l2-trigger-triage.md](specifications/l2-trigger-triage.md)) [L2] — TriggerEnvelope intake pipeline, 4-outcome classifier (local CPU + cloud + rule fallback), dedup cache (depends on orchestration + scheduler + agent-session)
- [x] **Mission Mode** ([l2-mission-mode.md](specifications/l2-mission-mode.md)) [L2] — two-phase autonomous goal execution: PRD generation → user checkpoint → story-verified loop with max-iterations circuit-breaker (depends on orchestration + kanban + tool-security)
- [x] **Deep Research** ([l2-deep-research.md](specifications/l2-deep-research.md)) [L2] — iterative Think→Plan→Search→Extract→Synthesize engine, date-grounding, untrusted content wrapping, max_rounds circuit breaker (depends on orchestration + tool-security + context-management); `Implements: l1-deep-research.md, l1-orchestration.md` — re-parented (2026-07-10) under the retro-authored l1-deep-research concept, DR-1…DR-11 compliance mapped (DR-4 claim-verification gate noted as the enhancement path); implementation unchanged

## Phase 7 — Leaf: TUI

*Interactive terminal surface over the now-mature core (command parity with the CLI).*

- [x] **TUI** ([l2-tui.md](specifications/l2-tui.md)) [L2]

## Phase 8 — Flower: Desktop Application

*The full graphical surface — the bloom.*

> **Done (2026-07-02):** all 10 tasks across 5 tracks complete — IPC bridge, settings persistence, shell systems (tray/shortcuts/overlay/single-instance), five-surface workbench with theming + i18n, Office View, Dashboard, provider prompt dispatch + XML env context, MCP client model, and both validation tracks (`fallow audit` clean, store-compliance proven). Tauri crate 34 tests; UI 27 tests; all gates green. See `archives/tasks/phase-8.md`.

- [x] **Application UI** ([l2-app-ui.md](specifications/l2-app-ui.md)) [L2] — Tauri v2 + React 19, theming + i18n
- [x] **Office View** ([l2-office-view.md](specifications/l2-office-view.md)) [L2] — graph + spatial projection
- [x] **Dashboard** ([l2-dashboard.md](specifications/l2-dashboard.md)) [L2] — statistics projection

## Phase 9 — Operational Hardening

*Self-healing, backup, error reporting, telemetry, and the deferred security-hardening layer.*

> **Relocated from Phase 1 (2026-06-30):** `l2-sandbox-policy` and `l2-multi-user-auth` are security-hardening specs that never gated any earlier phase; they sit here with the rest of the productionization work rather than blocking the foundation. Decomposed into tasks on entry.
>
> **Done (2026-07-03):** all 10 tasks across 5 tracks complete — sandbox policy + multi-user auth (security), doctor + config hot-reload (self-healing), backup + agent migration (data safety), GitHub issue reporting + self-improvement + telemetry (reporting/improvement), and a cross-subsystem hardening integration test proving no seeded secret reaches any egress surface. Domain-logic-first scope throughout: each subsystem's algorithm is fully implemented and tested against seeded/mock state; OS-level integration (real file watchers, GitHub API transport, SQLite persistence for dedup/brief tables) is deferred, documented per task. 40 new tests added to `crates/core` this phase (184 lib + 5 integration); workspace-wide gates green. See `archives/tasks/phase-9.md`.

- [x] **Sandbox Policy** ([l2-sandbox-policy.md](specifications/l2-sandbox-policy.md)) [L2] — deny-by-default network egress (named entries + binary allowlists), isolation tiers (restricted/balanced/open), preset catalog, PolicyContext, access failure classification (depends on security)
- [x] **Multi-User Auth** ([l2-multi-user-auth.md](specifications/l2-multi-user-auth.md)) [L2] — bcrypt passwords, session tokens, TOTP 2FA, privilege map, admin promote/demote, reserved sentinel usernames (depends on security)
- [x] **Doctor** ([l2-doctor.md](specifications/l2-doctor.md)) [L2]
- [x] **Config Hot-Reload** ([l2-config-hotreload.md](specifications/l2-config-hotreload.md)) [L2] — file-watcher with bounded backoff+polling fallback, prefix-keyed reload plan, subsystem action dispatch, skills snapshot invalidation (depends on doctor + scheduler + extension-registry)
- [x] **Backup** ([l2-backup.md](specifications/l2-backup.md)) [L2]
- [x] **GitHub Issue Reporting** ([l2-github-issue.md](specifications/l2-github-issue.md)) [L2] — consent + scrub + dedup pipeline, GitHub issue filing; error fingerprinting (BLAKE3 normalized hash, cross-episode dedup, prior-resolution surfacing)
- [x] **Self-Improvement** ([l2-self-improvement.md](specifications/l2-self-improvement.md)) [L2] — calibration buckets (overconfidence metric, verified-ratio warning), mistake log (project/category/files), should-have-asked (trigger→question→answer), ask-backs (at-most-one-pending per project via partial UNIQUE INDEX), reasoning templates (task_type+domain → JSON steps, dream cycle extracted), brief surface (5-signal join at task start, cross-project mode) (depends on memory-store + learning-loop + agent-session + github-issue)
- [x] **Telemetry** ([l1-telemetry.md](specifications/l1-telemetry.md)) [L1] — opt-in program metrics (implementation light)
- [x] **Agent Migration** ([l2-agent-migration.md](specifications/l2-agent-migration.md)) [L2] — migration manifest v1, two-layer import (archives vs memory candidates), staged apply, source adapters (depends on memory-store + extension-registry + backup)

## Phase 10 — Advanced Office Features (L2)

*L2 implementation specs for the L1 concepts added in 2026-06. These specs must be authored via `/magic.spec` before implementation tasks can be generated.*

> **Status:** Done (2026-07-04) — all 11 L2 specs implemented across `crates/core` (office_control, acp, automation, deliberation, version_control, inner_monologue, lookahead, global_orch, voice) + kanban KAN-8 custom_boards + presentation-only navigation/canvas in `packages/ui`. 33 tasks across two workbooks (phase-10.md tracks A–E, phase-10b.md tracks F–L). Domain-logic-first per the Phase-9 precedent: each subsystem's algorithm is implemented and tested against in-memory/mock state; OS/network/audio integration (real event bus, ACP transport, cpal/ONNX, cross-office relay) is deferred as documented seams. Gates green: cargo 250 lib tests + clippy `-D warnings` + fmt; vitest 39 tests (8 files) + biome. Kanban KAN-8 custom-boards delta implemented (spec Stable at 1.1.0).

*Foundational wave — authored & Stable:*

- [x] **Office Control** ([l2-office-control.md](specifications/l2-office-control.md)) [L2] — OfficeState machine, cooperative drain-and-checkpoint, token-exhaustion hibernation ladder (substitute-before-hibernate, auto-recovery wake), per-subsystem toggles; `Implements: l1-office-control.md`; deps orchestration + budget-engine + model-router (all Done)
- [x] **Agent Client Protocol** ([l2-acp.md](specifications/l2-acp.md)) [L2] — session store, monotonic event bus, capability/trust gate, pure projection adapters, cross-office relay, live-steering + interrupt over the agent-session `/acp` transport; `Implements: l1-acp.md`; deps agent-session + security + orchestration (all Done)
- [x] **Navigation** ([l2-navigation.md](specifications/l2-navigation.md)) [L2] — four-layer component tree, floor tab bar with lazy load + live OfficeState icons, frozen sidebar catalog, recursive mechanism sub-nav, two-tier settings, IDE launcher; `Implements: l1-navigation-model.md`; deps app-ui (Done) + office-control (this phase)
- [x] **Automation Engine** ([l2-automation-pipeline.md](specifications/l2-automation-pipeline.md)) [L2] — single PipelineEngine behind both modes, topological node executor, dedup window, scoped state over volatile/durable backends, control plane, lifecycle observers, composition, portable bundles; `Implements: l1-automation-pipeline.md`; deps trigger-triage + scheduler + orchestration (all Done)

*Dependent wave — authored & Stable (intra-phase deps satisfied by the foundational wave):*

- [x] **Automation Canvas UI** ([l2-automation-canvas.md](specifications/l2-automation-canvas.md)) [L2] — three-panel flow-graph projection, node rendering with live state, explicit editing + validation, read-only implicit surfaces, dev-run requests (AC-7), observer scope view (AC-8); `Implements: l1-automation-canvas.md`; deps automation-pipeline (this phase) + app-ui (Done)
- [x] **Voice Input** ([l2-voice-input.md](specifications/l2-voice-input.md)) [L2] — cpal 16 kHz capture, ONNX VAD, pluggable transcription engine, optional consent-gated transform, review overlay, model lifecycle, clipboard-safe injection; `Implements: l1-voice-input.md`; deps technology-stack + security (Done)
- [x] **Deliberation Engine** ([l2-deliberation.md](specifications/l2-deliberation.md)) [L2] — orchestrator-initiated round runner, parallel independent arguments, synthesis with attribution, immutable log over the inbox store, Channels surface; `Implements: l1-deliberation.md`; deps orchestration + inbox (Done) + navigation (this phase)
- [x] **Version Control** ([l2-version-control.md](specifications/l2-version-control.md)) [L2] — virtual staging area over execution-workspace worktrees, orchestrator-enforced role authority, quality-gated commit, Conventional Commits + card footer, trunk-based/Git-Flow; `Implements: l1-version-control.md`; deps execution-workspace + quality-pipeline + kanban-board (Done)
- [x] **Inner Monologue** ([l2-inner-monologue.md](specifications/l2-inner-monologue.md)) [L2] — heartbeat-gated cycle, read-only snapshot, token-bounded reflection, typed intentions, log-before-dispatch to Pulse log over the inbox store, proactivity threshold; `Implements: l1-inner-monologue.md`; deps scheduler + inbox + agent-session (Done) + navigation (this phase)
- [x] **Lookahead Planning** ([l2-lookahead-planning.md](specifications/l2-lookahead-planning.md)) [L2] — trigger-catalog detector, budget-bounded no-real-exec simulator, conclusion dispatcher with ORC-9 fallback, append-only decision log; `Implements: l1-lookahead-planning.md`; deps orchestration + kanban-board + execution-workspace (Done)
- [x] **Global Orchestration** ([l2-global-orchestration.md](specifications/l2-global-orchestration.md)) [L2] — event-bus aggregate view (GO-4), ACP relay routing (GO-5), phase-awareness card annotation (GO-3), building escalation (GO-6); `Implements: l1-global-orchestration.md`; deps orchestration (Done) + l2-acp + deliberation + office-control (this phase)
- [x] **Kanban Custom Boards (KAN-8 delta)** ([l2-kanban-board.md](specifications/l2-kanban-board.md)) [L2] — spec Stable at 1.1.0; the amendment delta over the Done Phase-5 board is implemented in `crates/core/src/kanban/custom_boards.rs`: custom columns with a mandatory canonical `anchor` (a card in a custom column *is* in its anchor state for every cross-cutting consumer), saved views over the single card set (no second store), re-anchor audit records; board UI surfaces follow the existing app-ui board line; depended on kanban-board (Phase 5) + app-ui (Phase 8)

## Phase 11 — Content, Sharing & Dev-Workflow Subsystems

*Ready Stable L2 subsystems authored after the Phase 1–10 narrative was drafted. Access-controlled content stores plus the bundled dev-workflow skill catalog. Decomposed into atomic tasks on entry (like Phases 8–10). Natural build order: resource-sharing (the access layer) before notes/file-store; development-workflow is independent.*

> **Status:** Done (2026-07-04) — all 4 subsystems implemented in `crates/core` domain-logic-first. resource_sharing (access foundation) → file_store + notes; development_workflow independent. 8 tasks; +26 unit tests. SQLite schemas, Yjs binary CRDT, StorageBackend, and SHA-256 are deferred seams; the resolution/dedup/convergence/stage-gate algebra is implemented and tested. Gates green: cargo 276 lib tests + clippy `-D warnings` + fmt.

- [x] **Resource Sharing** ([l2-resource-sharing.md](specifications/l2-resource-sharing.md)) [L2] — uniform access-grant model, `has_access` resolution (owner→user→group→public), write-implies-read, additive grants, audit events (RS-1…RS-8); `Implements: l1-resource-sharing.md` — the access foundation for notes/files/knowledge
- [x] **Notes** ([l2-notes.md](specifications/l2-notes.md)) [L2] — insertion-CRDT with order-independent convergence + idempotent merge, append-only version history, non-destructive soft-delete; `Implements: l1-notes.md`; depends on resource-sharing
- [x] **File Store** ([l2-file-store.md](specifications/l2-file-store.md)) [L2] — content-addressed dedup, reference-tracking GC, immutable blobs, metadata decoupled from bytes; `Implements: l1-file-management.md`; depends on resource-sharing
- [x] **Development Workflow** ([l2-development-workflow.md](specifications/l2-development-workflow.md)) [L2] — five-stage pipeline (Design→Plan→Execute→Review→Deliver), two-stage quality gate, human checkpoint, append-only progress ledger; `Implements: l1-development-workflow.md`; depends on extension-registry + agent-session

## Phase 12 — Skill System (L2)

*First post-11 implementation phase: the skill extension kind gets its concrete two-tier realization on the canonical execution stack.*

- [x] **Skill System (Two-Tier Stores & Canonical Stack)** ([l2-skill-system.md](specifications/l2-skill-system.md)) [L2] — `Implements: l1-extensions.md, l1-storage-model.md` — preset store (program tier, read-only) + mutable store (state tier) with shadowing precedence; canonical package (no interpreted scripts); built-in command surface registered into the workflow-runtime vocabulary; conversion pipeline (verify → classify → retain → transpile → degrade → report, atomic); prompt synthesis; `cronus skill` import/create/status → 8 tasks / 5 tracks in [tasks/phase-12.md](tasks/phase-12.md)

## Phase 13 — Core Decomposition (Crate Topology)

*Not a growth-metaphor build phase — a **structural refactor of the existing workspace**. The core-decomposition wave (`l2-crate-topology` + the `l2-source-layout` 1.2.0 delta), both reviewed to Stable via `/magic.spec` (INDEX v1.0.97). Unlike Phases 9–12 (new domain logic tested against mocks), this phase **moves working code and preserves behavior exactly**: it repartitions `crates/core` into `contract · domain · store-local · auth-local · facade`, realizes the DN-2 provider seams as crate boundaries, and makes INV-8's "strictly inward dependencies" a compiler-enforced fact instead of a review convention. → 8 tasks / 5 tracks (A–D, T) in [tasks/phase-13.md](tasks/phase-13.md).*

> **Execution character (Planning Audit).** This is the highest-blast-radius phase in the project — it touches all five existing crates and mints four new ones. Despite Parallel mode (C3) it is **almost entirely sequential**: the topology spec's §5 migration is a strict 7-step chain, each step defined to compile green before the next. The two adapter extractions (store-local, auth-local) could nominally parallelize but both edit `crates/core/Cargo.toml` and the facade, so the orchestrator serializes them (shared-file constraint). **Step 2 (invert the one `context_router → MemoryStore` edge) is the pivot**: it is the only task that changes a type signature, and steps 3–7 are all blocked until it lands — cascade risk is concentrated there. **The CI boundary guard (T-13D01) is non-optional**, not decoration: the spec is explicit that the current layout drifted precisely because no build step failed when it drifted. **Measurement note:** the spec's "47 pure-`std` / 53 total modules" counts predate Phase 12; the new `skills` module is pure-`std` domain (no infra dependency), so it joins the domain tier and the partition *shape* is unchanged — the axis is which modules hold infra deps, not the module count.*

- [x] **Crate Topology (Core Decomposition)** ([l2-crate-topology.md](specifications/l2-crate-topology.md)) [L2] — `Implements: l1-architecture.md` — the implementation driver: mint `cronus-contract` (zero-dep types + seam traits), invert the single domain→infra edge, extract `cronus-store-local` + `cronus-auth-local`, rename the remainder to `cronus-domain`, keep `crates/core` as the `cronus` facade with `pub use` re-exports (public contract unchanged), repoint the TUI + fix `codegraph`'s leaked `rusqlite::Connection` (resolves the §6.4 INV-2 violation), and add the boundary guard. Behavior-preserving; realizes DN-2/DN-3 and enforces INV-8
- [x] **Source Layout — 1.2.0 delta** ([l2-source-layout.md](specifications/l2-source-layout.md)) [L2] — `Implements: l1-architecture.md` — documentary companion, **realized by** the topology migration rather than a separate code unit: its §4.5 crate-granularity decision becomes true when the migration lands. Its §4.1/§4.2 tree diagram is labelled *pre-migration*; refreshing it to the post-migration crate set is a follow-up `/magic.spec amend`, not a phase gate

## Phase 14 — Memory Intelligence & Consolidation (L2)

*The first build phase since Phase 13, and a return to the domain-logic-first shape of Phases 9–12. Realizes the two now-Stable memory L2s — `l2-memory-consolidation` (MC-1…MC-10) and `l2-memory-intelligence` (MI-1…MI-13) — over the existing SQLite substrate (`l2-memory-store`, in `cronus-store-local`), behind the `UserDataStore` seam. Turns the passive store into an active memory agent: a maintenance/consolidation tier (fact-vs-derived signals, multiplicative ranking, corpus maintenance) and a caller-facing query surface (grounded `answer`, temporal modes, conflict surfacing, lifecycle states, gated experience reuse). → 8 tasks / 4 tracks (A schema · B consolidation · C intelligence · T validation) in [tasks/phase-14.md](tasks/phase-14.md).*

> **Execution character (Planning Audit).** **Foundation-then-parallel, not free parallel.** Track A (schema) is a hard prerequisite for B and C: the `memory_signal` table (MC-5), the `depth` column (MC-1), and the `lifecycle_state` column + prune guard (MI-9) must land before any pass or projection reads them. Once A lands, B and C parallelize on file independence, with two cross-edges the orchestrator must respect: consolidation's MC-8 multiplicative ranking is *consumed by* intelligence's recall, and consolidation's MC-6 `archive` action *sets* the MI-9 `lifecycle_state` — so the lifecycle column (A) gates MC-6 (B), and the signal table (A) gates both MC-8 (B) and the ranking intelligence reads (C). **Generator-optional throughout** (§2 of both specs): every model-dependent step (consolidation abstraction, cluster summary, `answer` synthesis, temporal normalization, inferred capture) has a no-generator degrade path that must be built and tested, not deferred. **Domain-logic-first**: the community-detection algorithm (MC-7) and the LLM calls are seams with stubs — the phase proves the policy, gates, and degradation, not the ML quality. Acceptance bar: MC-1…MC-10 + MI-1…MI-13 each covered by a test, cold-start (empty derived layer) works, and no hot-path model call or graph walk is introduced (MC-8/MEM-2).*

- [x] **Memory Consolidation (L2)** ([l2-memory-consolidation.md](specifications/l2-memory-consolidation.md)) [L2] — `Implements: l1-memory-consolidation.md` — **delivered whole (MC-1…MC-10)**: `memory_signal` fact-vs-derived table with version-guarded neutral-default degradation (MC-5), multiplicative offline-precomputed ranking wrapping `l2-memory-store` §4.2 base relevance (MC-8), the merge/split/archive/summarize maintenance pass with blast-radius-proportional gates + anti-cycle cooldown + audit (MC-6), emergent summary nodes (MC-7), write-time additive-only edges (MC-3), the consolidation action algebra (MC-4), the incremental watermark over the §4.12 pipeline (MC-2), co-edit write safety (MC-9), and advisory interest extraction (MC-10). Behind the `UserDataStore` seam; no new engine
- [x] **Memory Intelligence (L2)** ([l2-memory-intelligence.md](specifications/l2-memory-intelligence.md)) [L2] — `Implements: l1-memory-intelligence.md` — **delivered at query-surface scope (MI-1/2/3/4/5/7/8/9/13)**: grounded `answer` (KB-6 + CV-3/CV-4, extractive degrade) (MI-1), temporal modes over the §4.14 record (MI-2), immediate recall-visibility (MI-3), conflict routing (MI-4), the digest (MI-5), procedural distillation (MI-7), the closed structured predicate compiled to SQL (MI-8), reversible lifecycle states with the prune guard (MI-9), and gated experience reuse (MI-13). Behind the `UserDataStore` seam. **The capture-path invariants (MI-6/10/11/12) were deferred at delivered scope** — they each need net-new capture-metadata schema (actor/expiry/subject/cross-ref) and form one coherent capture-path unit; carried forward to **Phase 15** as an explicit delta (the source-layout 1.1.0/1.2.0 precedent — the Done phase keeps its `[x]` at delivered scope, the remainder is a new line item, never a reopen)

## Phase 15 — Memory Capture Policy & Metadata (MI-6/10/11/12)

*The MI-6/10/11/12 delta deferred at Phase 14's delivered scope, now its own phase — not a reopen of the immutable Phase 14. Same Stable spec (`l2-memory-intelligence`), same subsystem, the explicitly-deferred capture-path remainder: how a memory gets **written**, as opposed to how it is queried (Phase 14). One coherent unit — MI-6's net-new capture metadata (actor/expiry/subject/cross-ref), MI-10's capture-time date normalization, MI-11's caller capture directives, MI-12's raw-vs-inferred mode — all sharing the write path and the generator-optional degrade contract. Deterministic next target by dependency topology (DA-3): the direct continuation of Phase 14, no `/magic.spec` needed because the spec is already Stable and the Invariant Compliance table specifies each net-new item concretely. → 5 tasks / 3 tracks (A capture-metadata schema · B capture path · T validation) in [tasks/phase-15.md](tasks/phase-15.md).*

> **Execution character (Planning Audit).** **Foundation-then-parallel, same shape as Phase 14.** Track A (the MI-6 capture-metadata schema — the 4th such additive-column round the memory subsystem has taken; the pattern is proven) is a hard prerequisite for the capture path: the policy (B) attributes `actor`/`subject`, sets `expiry`, and writes `cross_ref` forward edges, so those columns must exist first. Once A lands, the three capture-path invariants parallelize on the write path but share `capture()`'s entry point, so the orchestrator serializes edits to it (shared-surface constraint, like Phase 13's shared-Cargo.toml serialization). **Generator-optional is the load-bearing acceptance property**, not an afterthought: MI-10 (relative→absolute date normalization) and MI-12 (`raw` mode) MUST function with no model bound — MI-10 degrades to storing verbatim (never fabricating a date), MI-12's `raw` path is the no-generator escape hatch itself. MI-11 directives must never lower the MI-6 honesty floor nor suppress a safety-relevant fact — a negative invariant the validation task asserts explicitly. **Domain-logic-first** per the Phase 9–14 precedent: the `inferred`-mode model extraction is a seam+stub; the phase proves the policy, the confidence/salience gate, the metadata degradation-to-baseline, and the no-generator paths — not extraction quality. **Crate placement** follows Phase 14's own correction: the schema is store-tier (`cronus-contract` + `cronus-store-local`), the capture policy/directives are domain-tier (`cronus-domain`, over the `MemorySearch`/`UserDataStore` seam) unless run-time grep shows a policy has zero consumers outside the adapter (then it stays store-tier, the T-13B01/T-14 precedent) — decided at execution, not pinned here. Acceptance bar: MI-6/10/11/12 each covered by a test, absent-optional-field degrades to baseline, and every generator-dependent step has a tested no-generator degrade.*

- [ ] **Memory Intelligence (L2) — capture-path delta (MI-6/10/11/12)** ([l2-memory-intelligence.md](specifications/l2-memory-intelligence.md)) [L2] — `Implements: l1-memory-intelligence.md` — the capture path deferred from Phase 14: salience-gated/confidence-honest/de-duplicated capture policy with actor/expiry/subject/cross-ref metadata (MI-6), capture-time relative→absolute temporal normalization with a verbatim no-generator degrade (MI-10), optional caller `include`/`exclude`/`custom-instruction` directives recorded as provenance that never lower the honesty floor (MI-11), and the per-write `raw`/`inferred` mode flag where `raw` must function with no model bound (MI-12). Behind the `UserDataStore` seam; the same spec Phase 14 realized at query-surface scope, now completed at capture scope

## Phase 16 — Project Wiki Store (L2) — **Done**

*The first buildable phase since Phase 15 — realizes `l2-project-wiki` (promoted RFC→Stable via `/magic.spec`, one review fix: a `rebuild` byte-equivalence overclaim corrected to informational equivalence) as a per-office SQLite projection **cache** (workspace `wiki/wiki.db`), not loose Markdown. The client-facing living documentation the office maintains: a rebuildable cache of upstream truth (board / graph decisions / operational ledger / deliverables), never a second source of truth. Deterministic next target by dependency topology (DA-3): the sole Stable-but-unbuilt L2, with a Stable L1 parent (`l1-project-wiki`) and all its ground-truth sources already delivered (Phases 5/6/11). → 7 tasks / 4 tracks (A schema · B regeneration · C read/access surface · T validation) in [archives/tasks/phase-16.md](archives/tasks/phase-16.md).*

> **Execution character (Planning Audit).** **Foundation-then-parallel, memory-phase shape.** Track A (the `wiki_page`/`wiki_changelog`/`wiki_page_fts` schema in `cronus-store-local` + `WikiPage` contract types) gates everything: the regeneration pipeline (B) upserts rows, the read surface (C) queries them. Track B is serialized on the shared regeneration entry point (B01 write path → B02 grounding/honesty guards → B03 freshness), the same shared-surface constraint Phases 13/15 handled. **Projection-not-source (PW-3) is the load-bearing acceptance property:** `rebuild(office)` MUST reconstruct an equivalent wiki from ground truth and dropping `wiki.db` MUST lose nothing durable — the operational proof the store is a cache, not a second truth. **Read-only-to-client (PW-2) is structural**, not conventional: the client surface exposes no mutating method. **Generator-optional** per the Phase 9–15 precedent: page generation is model-based, but the phase proves the pipeline + citation guard (PW-4) + internal-detail filter (PW-8) + freshness (PW-5) + access gate (PW-7), not prose quality — a no-generator run stores a grounded stub from citations and never fabricates. **Crate placement:** schema store-tier (`cronus-contract` + `cronus-store-local`), regeneration pipeline domain-tier (`cronus-domain` over the store seam), access gate via `cronus-auth-local` — decided at execution per the Phase 14/15 correction, not pinned here. Acceptance bar: PW-2/3/4/5/6/7/8 each covered by a test, `rebuild` reconstructs an equivalent wiki, drop-loses-nothing holds, and every generator-dependent step has a tested no-generator degrade.*

- [x] **Project Wiki Store (L2)** ([l2-project-wiki.md](specifications/l2-project-wiki.md)) [L2] — 7/7 tasks Done. Per-office SQLite projection cache (`wiki_page` parent/ord tree + `wiki_changelog` + `wiki_page_fts`); office-owned event-driven **transactional** regeneration from ground truth with PW-4 citation + PW-8 internal-detail guards + a no-generator grounded-stub degrade; fingerprint-drift freshness (PW-5); structurally read-only client surface (`WikiReadSurface` — no mutating method by construction, PW-2/PW-6); `access-grants` Read gate (`GatedWiki` `has_access(Wiki, office_id, Read)` per read, PW-7); `rebuild()` proving projection-not-source (PW-3 — drop `wiki.db`, re-derive an equivalent wiki from ground truth). Seams keep the tier model: domain writes through a `WikiCache` port implemented by `store-local`; the access gate composes the `resource_sharing` grant algebra over the ports-tier read surface. Acceptance sweep maps every PW invariant to a named end-to-end test through the real store. Contrast held: wiki store = cache of upstream truth, unlike the memory learned-tier where the store IS truth (MEM-4)

## Phase 17 — Model Transport & Provider Connectivity (L2) — **Done**

*Realizes `l2-model-runtime` (promoted RFC→Stable via `/magic.spec`, one Post-Update Review fix — the transport's generate surface was wrongly attributed to the routing-metadata `contract::ModelProvider`; corrected to a NEW `InferenceBackend` trait). The concept-conformance audit's keystone: model **selection** (router, credential lanes, hardware fit) is fully implemented while no seam can perform the **call** — the workspace deliberately ships no HTTP transport. This phase builds it. → 7 tasks / 5 tracks (A contract inference trait · B model-local transport crate · C facade + nodus bridge · D topology + CI guard · T validation) in [tasks/phase-17.md](tasks/phase-17.md).*

- [x] **Model Transport (L2)** ([l2-model-runtime.md](specifications/l2-model-runtime.md)) [L2] — 7/7 tasks Done. `contract::InferenceBackend` streaming call surface; `crates/model-local` REST transport (no async runtime; loopback + egress-gated remote); facade `NodusModelBridge` + `TransportCompactor` over `InferenceBackend`; CI domain→model-local boundary guard self-tested; end-to-end validated (facade → transport → HTTP mock → real result; no-backend degrades without panic). Also fixed two pre-existing cronus-core test-isolation races surfaced by the workspace-green gate. **Follow-up:** `/magic.spec` to amend `l2-crate-topology` with the `model-local` peer-crate row (deferred out of run per SDD boundary).

> **Execution character (Planning Audit).** **Real-integration phase, not domain-logic-first** — the first since Phase 13. It builds the external seam prior phases deferred: a spec-sanctioned blocking HTTP+TLS client (`l2-model-runtime` §2), tested hermetically against a `TcpListener` mock emitting scripted SSE/NDJSON so `cargo test` needs no live provider. **No async runtime is the load-bearing constraint** — concurrency is a bounded worker-thread pool, streaming a pull-iterator over a bounded channel, cancellation an atomic flag that drops the socket. **Critical path = T-17A01** (the `InferenceBackend` trait shape): every other track builds on it, so if it is wrong B/C/T rework — keep it small, prove it with a mock impl. **Hidden shared resource:** the mock-HTTP-server fixture is reused across B02/B03/T01 — establish it once in B02. **Honest seam separation** is the acceptance spine: the routing-metadata `ModelProvider` (score) and `InferenceBackend` (call) are two facets of one object; the transport owns neither selection (router) nor retry/rotation (error-recovery: no internal retry, credential rotation stays upstream) — it performs the call and maps failures onto the recovery taxonomy. **The `l2-crate-topology` spec amendment** (register `model-local` as a peer infra crate) rides via `/magic.spec`, not a run-task spec edit; the CI boundary-guard *code* (forbid `domain → model-local`, failure path tested) is T-17D01. Acceptance bar: ordered tokens → Done + mid-stream cancel → Error(Cancelled) against the mock server, a previously-inert path (triage classification / compaction) produces a real result through the full seam, and the no-backend path degrades without panic.*

- [ ] **Model Transport (L2)** ([l2-model-runtime.md](specifications/l2-model-runtime.md)) [L2] — `Implements: l1-model-runtime.md` — a NEW `InferenceBackend` trait in `crates/contract` (the streaming call surface the routing-metadata `ModelProvider` lacks) realized by a new `crates/model-local` infra crate: blocking HTTP+TLS client, endpoint profiles consuming the router `api_base`, `/v1`-compatible streaming generate with a worker-thread pull-iterator + first-class cancellation, embed/describe/pull per capability flag, wire-failure→error-recovery-taxonomy mapping with no internal retry, egress-gated remote profiles; facade wiring + a host-side nodus bridge that collapses the stream to satisfy `nodus::ModelProvider` (LP-1 preserved); `model-local` registered as a peer infra crate with the CI boundary guard forbidding `domain → model-local`. MR-3/4/5 delegated-with-disclosure to the federated server in v1

## Phase 18 — Service Activation (L2) — **Done**

*Realizes `l2-service-activation` (promoted RFC→Stable via `/magic.spec`, three TBDs resolved as design decisions + two bundled reconciliation amendments) — the per-OS mechanism by which a host becomes the always-on INV-4 hub. One governing constraint drives every choice: the background engine runs **as the user, never a superuser**, so every mode reaches the same per-user state root (this rejects the `LocalSystem` Windows Service and the root `LaunchDaemon`). The deterministic next target by dependency topology (DA-3): the sole Stable-but-unbuilt L2, Stable L1 parent `l1-background-activation`, its platform-adapter crate seam delivered by Phase 13. → 8 tasks / 6 tracks (A seam+policy · B BA-4 barriers · C state-root lock · D OS adapters · E command surface · T validation) in [archives/tasks/phase-18.md](archives/tasks/phase-18.md).*

> **Execution character (Planning Audit).** **Real-integration, seam-first.** The load-bearing security property is **BA-4**: background activation is a `l1-security` SEC-10 authority grant the agent has **no write path to** — enforced structurally (no activation tool, no sandbox filesystem write path to the registration locations, no elevation the agent can supply). Per `l2-service-activation` §5 the order is fixed: **seam + probe first** (`capabilities()`/`observe()` + a no-op adapter, so BA-8/BA-10 test before any registration exists) → **sandbox deny entries before the first `enable()`** (the barrier precedes the capability) → login-scoped (unelevated) → system-scoped (elevation-to-register-only), Windows last (the S4U launch-timing gate + `At log on` fallback). **Testability discipline (the acceptance spine):** raw OS calls sit behind a thin `SystemCalls` seam so the adapter *logic* — remove-then-register ordering, weaker-wins `observe()` derivation, state mapping — is CI-tested against a scriptable fake on any host; the actual registry/plist/`systemctl` calls are a thin layer validated on the platform where runnable (Windows partially real-testable; macOS/Linux logic via the fake + deferred real validation). **Critical path = T-18A01** (the `ActivationRegistry` trait shape — every track builds on it; keep it small, prove with the mock). **Hidden dependency:** the BA-4 sandbox barrier (B) must land before any real `enable()` (D). **State-root lock (C)** is independent but a prerequisite for shipping *any* background mode (a supervised engine + a manual one over one state root is the exact BA-11 collision). Acceptance bar: BA-1…BA-11 each covered by a test, a failed transition converges on `Inactive` (never doubly-registered), `observe()` never returns a remembered value, and the agent has no structural write path to the activation plane. The `activation-os` peer adapter crate is registered in `l2-crate-topology` via `/magic.spec` when it lands (the `model-local` pattern), never a run-task spec edit.*

- [x] **Service Activation (L2)** ([l2-service-activation.md](specifications/l2-service-activation.md)) [L2] — 8/8 tasks Done. The `ActivationRegistry` seam (`capabilities`/`observe`/`enable`/`disable`, no OS types crossing it, no persisted mirror — BA-8) + domain transition state machine (`crates/domain/src/activation.rs` — mutual exclusion + remove-then-register-verify, consent bookkeeping, failure-converges-on-`Inactive`); the three structural BA-4 barriers (`sandbox_policy::FilesystemPolicy`/`FILESYSTEM_REGISTRATION_LOCATIONS`, `tool_security::is_activation_tool_name`, no elevation the agent can supply); the state-root lock realizing attach-never-duplicate (`crates/core/src/engine_lock.rs`, BA-11); a new peer adapter crate `cronus-activation-os` behind a `SystemCalls` seam — Windows `HKCU\…\Run` + S4U scheduled task (raw `windows-sys` FFI, real UAC elevation via `ShellExecuteExW`), macOS `LaunchAgent`/`LaunchDaemon` with `UserName` via `sudo` (disclosed: no `SMAppService`/objc2 veto signal), Linux systemd user/system unit or linger + XDG-autostart fallback via `pkexec`; a genuine per-host BA-10 capability check (`login_capability`/`system_capability` — a hardcoded-`Supported` gap was found and fixed during the closing validation sweep; non-systemd Linux now reports `Unsupported` with a reason); `cronus activation status|enable|disable` wired end to end with the non-interactive `enable` refusal proven by a real smoke test. One governing constraint held throughout: the engine runs as the user, never a superuser. Acceptance sweep (`crates/core/tests/activation_invariants.rs`) maps every testable BA invariant to a named end-to-end test through the real facade export chain

## Phase 19 — Loop Runner (L2) — **Done**

*Realizes `l2-loop-runner` (promoted Draft→Stable via `/magic.spec` in the same pass that promoted its L1 parent `l1-loop-governance` — the Draft status was held only by the layer constraint, not incompleteness; the promotion added the missing LG-10 compliance row + an `ObjectiveSlot` type and reconciled crate placement to the post-decomposition topology) — the engine-side mechanic that turns the loop-governance concept into a running component: a **loop runner** driving an autonomous loop turn after turn + a single **governor** enforcing the declared class, mutation manifest, oracle, and ceiling (LG-1…LG-10). It becomes the one place "run an agent in a loop" is implemented, so every loop inherits the same governance instead of each subsystem re-deriving retry-until-done. The deterministic next target by dependency topology (DA-3): the sole Stable-but-unbuilt L2, the direct conceptual downstream of the Phase 18 always-on hub (a hub exists to run autonomous loops). → 8 tasks / 5 tracks (A types+governor · B execution-runner+oracle · C evolution-runner+escalation · D LG-10+facade+CLI · T validation) in [archives/tasks/phase-19.md](archives/tasks/phase-19.md).*

> **Execution character (Planning Audit).** **Real-integration, composition phase.** The load-bearing safety property is **LG-3 criteria immutability** — an actor never holds the right to edit the contract that judges it — enforced structurally by the type system (`MutableArtifact` has no `Criteria` variant), not by a bypassable runtime check. The runner does **not** re-implement isolation/budget/task-selection/routing/rollback; it *composes* shipped subsystems (execution-workspace worktrees, budget-engine ceiling, model-router lineage, session-checkpoint state, version-control rollback, agent-autonomy approval gate, nodus provider traits) behind one governed contract. **Testability discipline (the acceptance spine, Phase 18 `SystemCalls`-seam precedent):** the pure loop *logic* — the governor's ceiling/guard/judge decisions, the execution runner's attempt→rollback→retry control flow, the escalation gate's held-out promotion — is CI-tested against **fakes** (a scripted oracle, a fake workspace allocator, a fake ledger sink) via the abstractions the spec already provides (`Oracle` enum, `ProviderType workspace_kind`, `BudgetHandle`), so `cargo test` needs no real worktree or model call; the *real* wiring (a live workspace, a real model-router judge, a real budget handle) is isolated to the `crates/core` facade (`loop_bootstrap.rs`, D02) + one real end-to-end CLI smoke over a trivial deterministic-oracle unit. **Critical path = T-19A01** (the `LoopSpec`/`MutationManifest`/`Oracle` type shape — every track builds on it; keep it small, LG-3-by-construction the load-bearing move). **Hidden dependency:** the runner composes many existing subsystems — a composition-target API surprise is contained to the facade wiring (D02), never spread across the domain logic (which tests against the seam abstractions). **Scope boundary (disclosed):** `l2-loop-runner` §6's *migration* of the existing loops (orchestration `/goal`, kanban execution, deep-research, heartbeat) to route through the runner is a separate, incremental, behavior-preserving refactor — **not** this phase, which builds the governed runner as a first-class component + CLI + validation. Acceptance bar: LG-1…LG-10 each covered by a test, criteria structurally unreachable through the manifest, the ceiling fires independently of the actor and oracle, and a failed attempt rolls back with only the compact status carried forward.*

- [x] **Loop Runner (L2)** ([l2-loop-runner.md](specifications/l2-loop-runner.md)) [L2] — 8/8 tasks Done. The single governed place every autonomous loop runs: `LoopSpec`/`MutationManifest`/`Oracle`/`Ceiling`/`ObjectiveSlot` types with `MutableArtifact` lacking a `Criteria` variant (LG-3 by the type system, `crates/domain/src/loop_runner/spec.rs`); the single-point governor (`check_ceiling`/`guard_writes`/`judge`); the execution-loop runner (ceiling-first → turn → illegal-mutation reject-and-continue → legal mutations ledgered → `governor::judge` — never the actor — sets `done` → finalize or rollback carrying only the compact status forward); the evolution-loop runner nesting `run_execution` per task (LG-1 composition) with keep/revert/partial scoring, never adopting an out-of-manifest candidate; the escalation gate hard-refusing on a shared-lineage oracle or absent novelty source *before* any metric comparison; LG-10 objective persistence composing the already-shipped CC-9 `context_mgmt::trim_cascade`/`ContextEntry::protect()` (no new eviction machinery). Facade (`crates/core/src/loop_bootstrap.rs`): a real `FileExistsBackend` (real clock, real `Path::exists()` check), ledger/spec persisted as plain text under the real state tier. `cronus loop run|evolve|log|show`: `evolve` present but marked unavailable (INV-9 — no harness registry exists in this workspace yet), never a silent success stub. 42 domain tests + 4 core tests + 4 real CLI-smoke tests (spawning the compiled binary) + an 11-test facade-level acceptance sweep mapping every LG-1…LG-10 invariant through the real re-export chain. `cargo test --workspace` green across 3 consecutive full runs (0 failed) + clippy `-D warnings` + fmt clean

## Phase 20 — Archetype Catalog (L2) — **Done**

*Realizes `l2-archetype-catalog` (promoted RFC→Stable via `/magic.spec`, reviewed as-authored) — the concrete realization of office archetypes (OA-1…OA-11): a domain-scoped **prior on staffing** (candidate pool, an org shape adopted only if the work grows, a bounded seed, domain norms) that changes what the manager *expects*, never how it *decides*. The deterministic next target by dependency topology (DA-3): the sole mature Stable-but-unbuilt L2 with a Stable parent (`l1-office-archetype`). → 6 tasks / 5 tracks (A schema+validation · B catalog+shipped-archetype+selection · C deviation recording · D command surface · T validation) in [archives/tasks/phase-20.md](archives/tasks/phase-20.md).*

> **Execution character (Planning Audit).** **Domain-logic-first, foundation-then-parallel.** Two structural properties are load-bearing: the **closed four-key schema** makes OA-4 (an archetype carries no authority) *unrepresentable* rather than merely forbidden — no `permissions`/`budget`/`autonomy` key exists and an unrecognized key fails validation; and the candidate **pool is read by the deviation recorder, never by the hire gate** (OA-3) — inverting that order reconstructs the fixed org chart the concept rejects, and is the single most likely implementation error (enforced by a test that a hire *outside* the pool succeeds). Per §5 the order is fixed: schema + validation first (enforces OA-4/OA-10 before any definition loads) → ship `software-engineering` alone (its 18-role pool resolves against the existing `roles::catalog` today) → deviation recording (OA-9 keeps the catalog honest) → the CLI. **Crate placement:** I/O-free logic in `crates/domain/src/archetype/` (the `roles` precedent), resolving role ids against `roles::catalog::PRESET_CATALOG`; the shipped archetype is an embedded program-tier constant. **Critical path = T-20A01** (the closed-key schema + validator — every track builds on it). **Scope boundary (disclosed):** (a) the two blocked archetypes (`advertising-agency`/`finance-department`) need a **separate** ROL-9-gated `l2-role-catalog` nine-role amendment — not this phase (they ship as declared-blocked entries); (b) the imported-archetype content-vetting path (§4.7, OA-5) depends on the admission-vetting gate and is deferred (§5 note 6). Acceptance bar: OA-1…OA-11 each covered by a test, a fifth schema key fails validation (OA-4 unrepresentable), a hire outside the pool succeeds and is recorded not refused (OA-3), and the archetype-free office is a complete state (OA-11).*

- [x] **Archetype Catalog (L2)** ([l2-archetype-catalog.md](specifications/l2-archetype-catalog.md)) [L2] — 6/6 tasks Done. The concrete office-archetype system in `crates/domain/src/archetype/`: the closed four-key `ArchetypeDefinition` schema + `validate_definition_keys` (a fifth key like `permissions` fails → OA-4 unrepresentable) + `validate` resolving every pool/seed id against `roles::catalog::PRESET_CATALOG` (OA-10) with seed cap 2 + justification (OA-2); `ArchetypeCatalog::program()` shipping the embedded `software-engineering` archetype (18-role pool, empty seed) + the two declared-blocked entries with their §4.4 missing-role lists; `infer` (Confident/Inconclusive/Ambiguous, OA-7) + `ActiveArchetype::set`/`clear` that touch only the archetype record (OA-8), the archetype-free office complete (OA-11); the three deviation counters where `record_hire` returns `()` so it structurally cannot refuse a hire (OA-3) + an `Unvalidated`-until-observed status (OA-9); `create_from_preset` recording `derived_from` (OA-6); the `cronus archetype list|info|set|create` command surface (no `hire` verb — clap rejects it). 24 domain tests + 4 real CLI-smoke tests + a 10-test facade-level OA-1…OA-11 acceptance sweep; `cargo test --workspace` green across 3 consecutive full runs (0 failed) + clippy `-D warnings` + fmt clean. Disclosed deferrals: the ROL-9-gated `l2-role-catalog` nine-role amendment (unblocks the two blocked archetypes) + the imported-archetype vetting path (OA-5, depends on the admission-vetting gate) + the office-scoped (vs. single state-tier) active-archetype record

## Phase 21 — Knowledge Store (L2)

*Realizes `l2-knowledge-store` (promoted RFC→Stable via `/magic.spec`, 1.0.0→1.1.0 — the L2 had lagged its already-Stable parent `l1-knowledge-base` v1.2.0; the promotion authored the missing KB-9 authorship-zone / KB-10 curation-lifecycle / KB-11 query-preparation compliance) — the agent-queryable **RAG** subsystem: named, access-controlled document collections with hybrid semantic (sqlite-vec ANN) + keyword (FTS5) retrieval fused by RRF, an async ingestion pipeline (file/URL/record adapters), incremental re-indexing, soft-delete GC, storage-enforced authorship zones, and a curation lifecycle. Distinct from the memory model (ephemeral conversational context) and the project wiki (client-facing projection cache): this is the durable, shared, cited reference material an office queries to stay grounded. The sole mature Stable-but-unbuilt L2 (DA-3), its parent Stable and its embedding seam (`contract::InferenceBackend`) delivered by Phase 17. → 8 tasks / 5 tracks (A schema+types · B ingestion · C retrieval · D authorship+curation+access+CLI · T validation) in [tasks/phase-21.md](tasks/phase-21.md).*

> **Execution character (Planning Audit).** **Foundation-then-parallel, store-subsystem shape (the memory/wiki precedent).** Track A (the `knowledge_collection`/`knowledge_directory`/`knowledge_document` (with `origin`/`curation`) / `knowledge_chunk` schema + `knowledge_chunk_fts` (FTS5) + `knowledge_chunk_vec` (`vec0`) in `crates/store-local/src/knowledge/`, plus the `Collection`/`Document`/`Chunk`/`RetrievalRequest`/`WriteOverride` contract types) gates all: ingestion (B) writes rows, retrieval (C) queries them, the write-gates (D) guard them. **Principal execution risk = the sqlite-vec dependency:** real `vec0` ANN needs the sqlite-vec extension loaded into rusqlite — a genuine external-dependency decision, the `ureq` (T-17B01) / `windows-sys` (T-18D01) flag-before-proceeding precedent, NOT an autonomous add. Everything else — FTS5 keyword search, RRF fusion, the three ingestion adapters, authorship/curation/soft-delete/GC, the access gate — builds without it; if the extension is deferred, the vector half degrades to a documented seam (the generator-optional precedent from the wiki/memory phases) and retrieval ships FTS-first. **Embedding is a fake seam in CI:** embeddings come from `contract::InferenceBackend::embed` (Phase 17, built), but CI has no live model — the pipeline and ANN are tested against a deterministic fake `EmbeddingBackend` (the memory store's own `encode_hrr` is still a zeroed stub, so the real sqlite-vec wiring is net-new here, not a reuse). **Load-bearing security property = KB-9 authorship zones at the single store write seam:** a write to an `origin='human'` row is refused unless a typed `WriteOverride::HumanDirected { audit_ref }` is supplied — enforced in the store (`db.rs`/`store.rs`), never by caller convention, so it survives agent drift (the BA-4 / OA-3 structural-enforcement lineage). The single most likely implementation error is placing that check in the service/caller instead of the seam → proven by a test that a direct store write to a human-origin row without an override is refused. **KB-1 collection isolation** is the retrieval acceptance spine: every ANN + FTS query is scoped to explicit `collection_ids`, proven by a test that a query never returns another collection's chunk. **Crate placement:** store-tier (`cronus-contract` + `cronus-store-local`), ingestion/retrieval/query-prep domain-tier (`cronus-domain` over a store port), access gate via the `resource_sharing` grant algebra (add `ResourceKind::Knowledge`, the Phase 16 `GatedWiki` precedent), service re-exported from the `cronus-core` facade — reconciling the spec §4.5 standalone-crate layout to the post-decomposition topology (a mechanical `/magic.spec` §4.5 amendment is a non-blocking follow-up). **Critical path = T-21A01** (schema + types; keep it small, prove with round-trip tests before the pipeline). Acceptance bar: KB-1…KB-11 each covered by a named test through the real SQLite store + facade (deterministic fake embedder), a human-origin row is write-refused without override, a `draft` chunk is excluded by `min_curation` while human sources stay eligible, and an empty query-preparation degrades to the raw query rather than an empty search.*

- [ ] **Knowledge Store (L2)** ([l2-knowledge-store.md](specifications/l2-knowledge-store.md)) [L2] — `Implements: l1-knowledge-base.md` — the agent-queryable RAG subsystem in `crates/store-local/src/knowledge/` (schema + FTS5 + `vec0`) + `crates/domain` (ingestion / hybrid retrieval / RRF fusion / query preparation) + `crates/core` facade (`KnowledgeStore` service): named access-controlled collections (KB-1/KB-4), incremental re-indexing (KB-3), three source adapters file/URL/record (KB-5), per-chunk source attribution (KB-6), non-authoritative recall (KB-7), soft-delete + GC (KB-8), storage-enforced authorship zones via a typed `WriteOverride` at the single write seam (KB-9), a `draft→reviewed→stable` curation lifecycle with a `min_curation` retrieval floor (KB-10), and an optional `QueryPreparer` seam fallback-floored to the raw query (KB-11). Embedding via `contract::InferenceBackend::embed` (fake seam in CI); real `vec0` ANN gated on a disclosed sqlite-vec dependency decision; `cronus knowledge` command surface. 0/8 tasks

## Backlog

*Non-Stable specs parked until they reach `Stable` (C6). Promoted into an active phase by a later `/magic.task` run once their status and any parent dependency clear.*

- [ ] **Spec-Driven Governance** ([l1-spec-driven-governance.md](specifications/l1-spec-driven-governance.md)) [L1] — `RFC` — SDD governance meta-spec (SDG-1…SDG-15); backlog until reviewed to Stable
- [ ] **Dynamic Harness** ([l1-dynamic-harness.md](specifications/l1-dynamic-harness.md)) [L1] — `RFC` — run-time complement to harness engineering (DH-1…DH-12); backlog until reviewed to Stable
- [ ] **Dev Office** ([l1-dev-office.md](specifications/l1-dev-office.md)) [L1] — `RFC` — developer (self-hosting) office: a system workspace for maintaining Cronus itself; backlog until reviewed to Stable

> The **core-decomposition wave** (`l2-crate-topology` + `l2-source-layout` 1.2.0) left the Backlog in v2.15.0 — both reached Stable via `/magic.spec` and are now **Phase 13**.
> The **memory-implementation wave** (`l2-memory-consolidation` + `l2-memory-intelligence`) left the Backlog in v2.17.0 — both reached Stable via `/magic.spec` and are now **Phase 14**.
> The **project-wiki store** (`l2-project-wiki`) left the Backlog in v2.20.0 — reached Stable via `/magic.spec` and is now **Phase 16**.
> The **model-transport wave** (`l2-model-runtime`) left the Backlog in v2.22.0 — reached Stable via `/magic.spec` and is now **Phase 17**.
> The **service-activation front** (`l2-service-activation`) left the Backlog in v2.25.0 — reached Stable via `/magic.spec` (three TBDs resolved as design decisions) and is now **Phase 18**.
> The **loop-governance chain** (`l1-loop-governance` + `l2-loop-runner`) left the Backlog in v2.28.0 — both reached Stable via `/magic.spec` (the L2's Draft status was held only by the layer constraint; LG-10 compliance + crate reconciliation added on promotion); `l2-loop-runner` is now **Phase 19**, and `l1-loop-governance` is repositioned to **Phase 0** as a realized concept (no longer concept-only — it has a build-bound L2).
> The **archetype-catalog front** (`l2-archetype-catalog`) left the Backlog in v2.30.0 — reached Stable via `/magic.spec` (reviewed as-authored, one cosmetic TBD→prose edit) and is now **Phase 20**; its parent `l1-office-archetype` stays a realized Phase 0 concept.
> The **knowledge-store front** (`l2-knowledge-store`) left the Backlog in v2.32.0 — reached Stable via `/magic.spec` (1.0.0→1.1.0, KB-9/KB-10/KB-11 compliance authored against the already-Stable parent `l1-knowledge-base` v1.2.0) and is now **Phase 21**.

## Risks (Planning Audit)

- **Critical path = `crates/core` + `crates/nodus`**: Phases 3–8 depend on the library (Phases 1–2). Land the core contract and the nodus vertical slice early.
- **CLI-first surface (stem) is intentionally thin at Phase 3**: it ships the command framework + grammar + the commands available then; subsystem phases (4–6) attach their commands to it, and the TUI (Phase 7) mirrors the matured command set. This staging is the growth model, not scope creep.
- **nodus port size**: ~5k lines across six modules; Phase 2 builds it as a vertical slice (parse → transpile → minimal execute) before completing validator/lint and the full command set. Track parity against the reference test corpus.
- **Mobile/Tauri scaffold**: iOS/Android Tauri setup is toolchain-fragile (stack §5) — smoke-test the build/sign pipeline in Phase 1, not at Phase 8.
- **Concept-only growth (v2.9.0 sync)**: 17 more specs absorbed (15 Stable L1 concept-only into Phase 0, 2 RFC into Backlog). The concept-only count keeps rising while Phase 8 implementation is in flight — a C28 health signal worth watching: the concept caul is far ahead of code, and several new concepts (crash-recovery, employee-availability, parallel-staffing, model-benchmarking, process-monitor, issue-reporting) are natural L2 candidates for Phases 9–10 when authored.
- **Concept-caul growth continues (v2.13.0 sync)**: 27 more orphans absorbed — 25 Stable L1 concept-only into Phase 0 (the observability/diagnostics family diagnostic-log / log-legibility / system-readout / report-prompting, plus coordination, context-economy, and provenance families), 1 RFC (dev-office) to Backlog, and Phase 12 (skill-system) opened. The Stable-L1-without-L2 delta keeps widening (C28 health signal); the reporting/diagnostics cluster (issue-reporting v1.1.0 + report-prompting) is a natural candidate for the next L2 wave.
- **Registry sync debt (resolved in v2.8.0)**: 48 specs had accumulated outside the plan (INDEX raced ahead to v1.0.36). That revision absorbed them — but the new Phase 0 concept additions (e.g. execution-graph, code-intelligence, model-runtime, knowledge-base) imply future L2 implementation work not yet phased. Most have no authored L2 yet; they will surface as new phases via `/magic.spec` → `/magic.task` when promoted.
- **Amendment deltas over Done phases (v2.10.0)**: the kanban 1.1.0 amendment (KAN-8 custom boards) is the first spec change landing after its implementation phase closed. Pattern: the delta becomes an explicit item in the next open phase (here Phase 10) rather than being silently absorbed or reopening the Done phase — keeps Done phases immutable and the spec-vs-code gap visible.
- **TUI event-seam dependency (Phase 7)**: the render loop assumes a core event/subscribe seam. If the core exposes no pub/sub observer, the TUI must fall back to polling durable-state snapshots (INV-5 view-only). The Phase 7 tasks carry this fallback so the view panels are not hard-blocked on the subscription mechanism. Verify the seam first (T-7A02).
- **Core-decomposition wave sits in Backlog (v2.14.0)**: `l2-crate-topology` plus the `l2-source-layout` 1.2.0 delta are RFC, but they are not a feature — they are a structural refactor of every crate in the workspace, and the topology spec states plainly that `l1-deployment-neutrality` DN-3 is *currently unrealizable* because the provider seams do not exist. Two consequences. (1) **Sequencing vs Phase 12**: the Skill System lands a `skills` module in `crates/core`; if the decomposition lands first, Phase 12 waits, and if second, the module relocates. Phase 12 goes first — it is Stable and decomposed while the topology is RFC with open TBDs (status maturity is the deciding criterion), and `skills` is pure-`std` domain logic, which by the topology spec's own dependency-weight axis stays in the domain tier under either shape; the relocation is mechanical, not a redesign. (2) **Cost grows with delay**: every phase executed before the decomposition accretes more code inside the monolithic crate, so the refactor's size is a function of how long it waits at RFC.
- **First status regression on already-implemented code (v2.14.0)**: `l2-source-layout` went `Stable → RFC` under the RULES §2 amendment rule *after* its Phase 1 implementation shipped. This extends the KAN-8 pattern — that was an amendment to a spec over a Done phase; this one also **demoted the spec's status**, so the file no longer reads as `Stable` while the code it describes is live. The plan's response is unchanged (the Done phase keeps its `[x]` at delivered scope; the delta is tracked separately), but the failure mode is new: a later `/magic.task` could read the RFC status as "never implemented" and backlog the Phase 1 entry. The Backlog line states that boundary explicitly to prevent it.
- **Blocked archetypes are role-catalog debt, not an archetype-catalog defect (v2.14.0)**: `l2-archetype-catalog` ships one of the three archetypes it declares, because the preset role catalog is engineering-heavy and the other two name specialties it does not contain. OA-10 forbids defining them inline, so the spec declares them blocked rather than inventing specialties. The unblock path runs through `l2-role-catalog`, one ROL-9 gate per role on its own merits — a content-authoring cost the archetype work cannot absorb by itself, and one that will not surface until someone asks why only one archetype ships.
