# Nodus Workspace Specifications Registry

**Version:** 1.0.22
**Status:** Active
**Engine Version:** 2.1.46

## Overview

Nodus is a purpose-built workflow DSL and its Rust runtime — vendored in the Cronus monorepo under `crates/nodus` and intended to become a standalone library. This workspace covers nodus as a self-contained tool: language design, grammar, type system, and public API. Integration with Cronus as a host (step binding, subsystem dispatch, platform constraints) is covered in `.design/main/`.

## Domain Specifications

| File | Description | Status | Layer | Version |
| --- | --- | --- | --- | --- |
| [l1-nodus-language.md](specifications/l1-nodus-language.md) | Nodus DSL language: file types (§wf/§schema/§config), section grammar (@ON/!!rules/!PREF/@in/@out/@ctx/@err/@steps/@test/@macro), step syntax (+modifier/^validator/~flag/→pipeline), control flow (?IF/~FOR/~UNTIL MAX:n/~PARALLEL), macro invocation (RUN(@macro_name)), error taxonomy (11 NODUS:* codes), 10 core invariants (NL-1…NL-10); ~PARALLEL fail-fast error propagation; §4.6 upstream parity gap v0.4.6→v0.7 (control: ?SWITCH/~MAP/~RETRY/!HALT/!PAUSE; operators: MATCHES/?./??/WHERE/FIRST/LAST/string-interpolation; HITL dialog: ASK/CONFIRM; @needs selective schema loading; error taxonomy 11→24; @ON(priority=N); closed flag/validator/type registries) | Stable | 1 | 1.1.0 |
| [l2-nodus-runtime.md](specifications/l2-nodus-runtime.md) | Rust crate `crates/nodus`: module structure (lexer/parser/ast/vocab/validator/executor/observability/portability/transpiler/workflows), invariant compliance table (NL-1…NL-10, E013/E014 enforced), Value enum, executor boot sequence (6 steps), public API (scaffold/validate/run/transpile/test/run_with_provider/run_with_audit/run_with_provider_and_audit/run_with_schema/run_with_schema_and_audit), ModelProvider + AuditProvider + SchemaProvider extension points, vocabulary schema v0.4.6 (51 commands + RUN meta-command); §4.7 upstream parity gap v0.4.6→v0.7 (missing ASK/CONFIRM commands, control constructs ?SWITCH/~MAP/~RETRY/!HALT/!PAUSE, operators/expressions, @needs, error_code 11→24, closed flag/validator/type registries, macro execution, Status::Paused, lexer parity) | Stable | 2 | 1.1.0 |
| [l1-nodus-portability.md](specifications/l1-nodus-portability.md) | Portability and extension contract: host neutrality (LP-1), extension via abstract interfaces (LP-2), two-host generalisation rule (LP-3), vocabulary isolation (LP-4), composable extension (LP-5), semantic versioning (LP-6), feedback loop lifecycle (LP-7); extension point taxonomy (ModelProvider + AuditProvider + future StorageProvider/PolicyProvider), feedback distillation protocol, universal pattern criteria, vocabulary layering model; capability manifest with pre-run satisfiability validation (LP-8, fail-fast) as the machine-checkable LP-3 portability contract | Stable | 1 | 1.1.0 |
| [l1-nodus-observability.md](specifications/l1-nodus-observability.md) | Execution observability contract: trace-first output (HO-1), per-step attribution (HO-2), append-only immutability (HO-3), frozen evaluation boundary (HO-4), observer neutrality (HO-5), structured event taxonomy (HO-6), monotonic sequence + run-correlation ordering with streaming chunk-merge into one logical model_response (HO-7); AuditProvider role, 10-type event taxonomy (step_start/step_end/step_error/constraint_hit/branch_taken/loop_iteration/macro_enter/macro_exit/model_call/model_response), run manifest, data-safety boundary (no raw user text in traces), frozen-vs-evolvable component table; §4.6 sequence/correlation fields + streaming merge underpins the trajectory projection in l1-nodus-environment; HO-8 cost-attribution token classes (input/output/cache_read?/cache_creation?) on model_response, counts-only within data-safety, cost computable from the trace | Stable | 1 | 1.2.0 |
| [l2-nodus-observability.md](specifications/l2-nodus-observability.md) | Rust implementation of observability contract: `observability.rs` module (AuditProvider trait, ExecutionEvent 10-variant enum, NoopAuditProvider, RunManifest, FieldDescriptor), executor hook-point wiring for all 10 event types, `run_with_audit` + `run_with_provider_and_audit` public API additions; HO-1…HO-6 compliance table; 13-test plan (unit + integration including observer-neutrality gate) | Stable | 2 | 1.0.0 |
| [l2-nodus-portability.md](specifications/l2-nodus-portability.md) | Rust implementation of portability contract: LP-1…LP-8 compliance table; SchemaProvider trait (vocabulary-extension seam, `Schema::with_provider`); StorageProvider + PolicyProvider pending LP-3 interfaces; extension point registry (ModelProvider, AuditProvider, SchemaProvider implemented; StorageProvider/PolicyProvider pending); §4.7 LP-8 capability manifest (`CapabilityManifest`/`ExtensionRole`/`HostCapabilities`/`Missing`, pure `validate_manifest` resolver, `run_with_manifest`/`run_with_manifest_and_audit` fail-fast gate before executor boot, `NODUS:CAPABILITY_UNMET`, AST-derived `from_workflow`) as the machine-checkable LP-3 contract; extraction artifacts (CI, EXTRACTION.md, Cargo manifest) as LP-6 deliverables | Stable | 2 | 1.1.0 |
| [l1-nodus-testing.md](specifications/l1-nodus-testing.md) | Nodus DSL testing contract: `@test:` block structure (input:/expected:/tags:), NT-1…NT-11 invariants (block isolation, input override, assertion binding, failure semantics, provider neutrality, tag metadata, ordered reporting, schema inheritance, parse-time validation, route coverage advisory, differential parity across execution paths), execution protocol, TestReport/TestResult contract, tag-based filtering, validation diagnostics (E015/W006/W009); §4.7 differential-parity harness — interpreter↔transpiler/two-host equal observable results (output + trace shape) on a content-addressed fixture corpus, the executable form of portability LP-3 | Stable | 1 | 1.1.0 |
| [l2-nodus-testing.md](specifications/l2-nodus-testing.md) | Rust implementation of testing contract: `TestBlock` AST node (input/expected/tags/raw_lines), `RunResult.vars` variable environment exposure, `test()`/`test_with_tags()` public API, `parse_expected_value` type coercion, `evaluate_test_block` assertion evaluator, NT-1…NT-10 compliance table, validator diagnostics (E015/W006/W009), test coverage map | Stable | 2 | 1.0.0 |
| [l1-nodus-dialog.md](specifications/l1-nodus-dialog.md) | Human-in-the-loop dialog contract: `ASK` (typed question — str/bool/confirm/choice/multi_choice + options/hint/default/validate/timeout) and `CONFIRM` (approval — msg/actions/default/strict) command class; suspend/resume lifecycle (`Status::Paused`, human re-trigger); DG-1…DG-8 invariants (host neutrality, blocking progression, typed binding, suspend/resume determinism, typed failures, default-on-absence, trace data-safety, LP-8 capability declaration); dialog backend as a distinct `ExtensionRole::Dialog` (LP-2/LP-8); paused-run state = `Status::Paused` + host-persisted resume descriptor (built-in host synchronous, default-or-Paused); dialog error subset (`DIALOG_TIMEOUT`/`DIALOG_REJECTED`/`PAUSED`); elevates `l1-nodus-language` §4.6 HITL gap | Stable | 1 | 1.1.0 |
| [l2-nodus-dialog.md](specifications/l2-nodus-dialog.md) | Rust realization of the dialog contract (`l1-nodus-dialog`): `ASK`/`CONFIRM` vocabulary + modifiers; `Status::Paused` + `ResumeDescriptor` (host-persisted); `DialogProvider` trait + built-in synchronous `DefaultDialogProvider` (default-or-pause, no I/O); `ExtensionRole::Dialog` added to LP-8 taxonomy + `from_workflow` derivation (required when a dialog lacks `+default`); executor dispatch (Answer/Pause/Timeout/Rejected); `run_with_dialog`/`run_with_dialog_and_audit` combinators; emits `DIALOG_TIMEOUT`/`DIALOG_REJECTED`/`PAUSED`; DG-1…DG-8 compliance | Stable | 2 | 1.0.0 |
| [l2-nodus-control-flow.md](specifications/l2-nodus-control-flow.md) | Rust realization of the v0.7 control constructs (`l1-nodus-language` §4.6 / `l2-nodus-runtime` §4.7(b)): `?SWITCH` multi-branch dispatch (+`*` default, `SWITCH_NO_MATCH`), `~MAP` collection transform (implicit `$it`), `~RETRY:n` bounded step retry (NL-5, max 10), `!HALT` (→`Status::Failed`, requires ESCALATE), `!PAUSE` (→`Status::Paused`, reuses dialog suspension); lexer tokens + `SwitchBlock`/`MapBlock` AST + action flags + retry; parser/executor/validator/transpiler wiring; reuses existing `Status`/`Signal`/error codes; phased implementation | Stable | 2 | 1.0.0 |
| [l2-nodus-errors.md](specifications/l2-nodus-errors.md) | Rust realization of the 24-code error taxonomy (`l1-nodus-language` §4.6): `ErrorSeverity` (error/warn/info) + `ErrorCategory` (parse/runtime/validation/routing/memory/test/control/dialog) types; per-code severity×category registry; `error_meta()` lookup; `EXECUTION_FAILED` demoted to non-canonical legacy + per-site reassignment (UNDEFINED_CMD/UNDEFINED_MACRO/VALIDATION_FAILED/ESCALATION_FAILED/CONFIDENCE_LOW/KB_UNAVAILABLE/MEMORY_FAILED); validator/executor emission map; NL-1/NL-2/NL-4/NL-9 compliance; `CAPABILITY_UNMET` noted as the LP-8 portability-layer code | Stable | 2 | 1.0.0 |
| [l2-nodus-registries.md](specifications/l2-nodus-registries.md) | Rust realization of closed vocabulary registries (`l1-nodus-language` §4.6, contents per `l2-nodus-runtime` §4.7(f)): `KNOWN_FLAGS` (12 analysis extractors) + `KNOWN_VALIDATORS` (12 validator names, pre-colon match) + `PRIMITIVE_TYPES` (10 field types) as `vocab` data; `Schema::is_known_flag`/`is_known_validator`/`is_known_type` query surface; advisory (warning-severity) validator diagnostics for unknown `~flag`/`^validator`/`@in` type; strengthens NL-1/NL-7/NL-9; advisory-first (no hard break), host extension via SchemaProvider is a follow-on | Stable | 2 | 1.0.0 |
| [l1-nodus-environment.md](specifications/l1-nodus-environment.md) | Environment & Evaluation contract — the executable substrate an evaluation-driven improvement loop grades a workflow against: `EnvironmentProvider` as a 5th extension role beside Model/Audit/Schema/Dialog (NE-1); closed reset/step/evaluate lifecycle bracketed by open/release, deterministic per (task, seed, actions) (NE-2); trajectory as an ordered projection onto the existing AuditProvider stream, not a parallel channel (NE-3, uses HO-7 seq); frozen-evaluation boundary — evaluate is read-only over a completed run, reward computed after the frozen point (NE-4); reward is typed data not control, orthogonal to run Status (NE-5); addressable task catalog + profile as the interchangeability contract (NE-6); instance isolation + mandatory idempotent release (NE-7); function-scoped auxiliary model roles policy/context/summary, economical-by-default, corroborates routing RTG-9 (NE-8); host-supplied metric neutrality — nodus defines reward shape not metric (NE-9); capability-manifest-declared `ExtensionRole::Environment`, fail-fast pre-run (NE-10, LP-8); declared grading mode automated/judge/hybrid with hybrid-floor (judge may lower not rescue), judge via aux role under host judge-trust (NE-11); §4.6 division of labour vs harness-evolution (this = graded run, harness = what-to-change) + §4.7 grading-mode composition + sliceable orthogonal task labels feeding the host agent co-evaluation matrix (one graded run = one matrix cell) | Stable | 1 | 1.1.0 |

## Meta Information

- **Maintainer**: Core Team
- **Last Updated**: 2026-07-02 (adoption pass over an external model×harness agent-benchmark reference — one nodus refinement, additive, no cascade: l1-nodus-environment v1.0.0→1.1.0 (NE-11 declared grading mode — closed automated/judge/hybrid set with hybrid-floor semantics: the deterministic checker runs first and a judge may only lower not rescue a failed run; judge-mode via a function-scoped auxiliary binding under host judge-trust; refines NE-9 without weakening it — plus §4.7 sliceable orthogonal task labels on the profile, the nodus-side feed for the host agent co-evaluation methodology where one graded run = one matrix cell and labels = slice dimensions). Main-workspace deliverable of the same pass: new l1-agent-coevaluation (Performance=f(Model,Harness) diagnostic matrix) + l1-evaluation-suites ES-17 slice reporting. Prior: adoption pass over an external context-optimization reference — two nodus refinements, both additive, no cascade: l1-nodus-observability v1.1.0→1.2.0 (HO-8 cost-attribution token classes — fresh input/output plus optional cache_read/cache_creation as distinct counts-only fields on model_response, so per-run/per-step cost and cache-warmth regressions are computable from the trace not the invoice); l1-nodus-testing v1.0.1→1.1.0 (NT-11 differential parity across execution paths + §4.7 harness — interpreter↔transpiler or two conforming hosts must produce equal observable output + trace shape on a content-addressed fixture corpus, the executable form of portability LP-3, complementary to @test: correctness assertions). Main-workspace deliverable of the same pass: new l1-cache-stable-context (prompt-cache stability discipline) + refinements to l1-context-compression/l1-routing. Prior: l1-nodus-environment v1.0.0 Stable authored — Environment & Evaluation contract: EnvironmentProvider as a 5th extension role, closed reset/step/evaluate lifecycle, trajectory as observability projection, frozen-evaluation boundary, typed reward-is-data, addressable task catalog+profile, instance isolation, function-scoped auxiliary roles, host-supplied metric neutrality, capability-manifest ExtensionRole::Environment fail-fast; NE-1…NE-10; the executable substrate for evaluation-driven harness improvement. l1-nodus-observability v1.0.0→1.1.0 — added HO-7 (monotonic sequence + run-correlation ordering) + §4.6 (sequence/correlation fields, run-scoped correlation binding, streaming chunk-merge into one logical model_response); underpins the trajectory projection. Prior: l2-nodus-control-flow v1.0.0 Stable authored — Rust realization of v0.7 control constructs: ?SWITCH/~MAP/~RETRY/!HALT/!PAUSE; lexer tokens + SwitchBlock/MapBlock AST + action flags + retry; reuses Status::Paused/Signal::Pause + SWITCH_NO_MATCH/PAUSED; phased implementation recommended. Prior: l2-nodus-dialog v1.0.0 Stable authored — Rust realization of the dialog contract: ASK/CONFIRM vocabulary, Status::Paused + ResumeDescriptor, DialogProvider + DefaultDialogProvider, ExtensionRole::Dialog manifest binding, run_with_dialog combinators; DG-1…DG-8. Prior: l1-nodus-dialog v1.0.0→1.1.0 Draft → Stable — resolved both TBDs: paused-run state = Status::Paused + host-persisted resume descriptor (built-in host synchronous); dialog backend as distinct ExtensionRole::Dialog. Prior: l2-nodus-registries v1.0.0 Stable authored — closed vocabulary registries: KNOWN_FLAGS (12) + KNOWN_VALIDATORS (12) + PRIMITIVE_TYPES (10) as vocab data + Schema query surface + advisory validator diagnostics; strengthens NL-1/NL-7/NL-9; contents per l2-nodus-runtime §4.7(f). Prior: l2-nodus-errors v1.0.0 Stable authored — Rust realization of the 24-code error taxonomy from l1-nodus-language §4.6: ErrorSeverity/ErrorCategory types, per-code severity×category registry, error_meta() lookup, EXECUTION_FAILED supersede + site reassignment, validator/executor emission map; foundational dependency for control-flow/dialog/operators clusters. Prior: l1-nodus-dialog v1.0.0 Draft authored — human-in-the-loop dialog contract: ASK/CONFIRM command class, suspend/resume lifecycle (Status::Paused), DG-1…DG-8, dialog backend as LP-2 extension point + LP-8 role, dialog error subset; elevates l1-nodus-language §4.6 HITL gap; Backlog pending design review of TBDs. Prior: l2-nodus-portability v1.0.0→1.1.0 — LP-8 capability manifest implemented in `crates/nodus`: §4.7 `CapabilityManifest`/`ExtensionRole`/`HostCapabilities`/`Missing` + pure `validate_manifest` resolver + `run_with_manifest`/`run_with_manifest_and_audit` fail-fast gate before executor boot + `NODUS:CAPABILITY_UNMET` + AST-derived `from_workflow`; 13 new tests; status RFC → Stable. Prior: l1-nodus-portability v1.1.0 — LP-8 contract authored; l1-nodus-language v1.1.0 + l2-nodus-runtime v1.1.0 — upstream schema v0.4.6 → v0.7 parity gap recorded)
