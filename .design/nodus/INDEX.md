# Nodus Workspace Specifications Registry

**Version:** 1.0.47
**Status:** Active
**Engine Version:** 2.1.49

## Overview

Nodus is a purpose-built workflow DSL and its Rust runtime — vendored in the Cronus monorepo under `crates/nodus` and intended to become a standalone library. This workspace covers nodus as a self-contained tool: language design, grammar, type system, and public API. Integration with Cronus as a host (step binding, subsystem dispatch, platform constraints) is covered in `.design/main/`.

## Domain Specifications

| File | Description | Status | Layer | Version |
| --- | --- | --- | --- | --- |
| [l1-nodus-language.md](specifications/l1-nodus-language.md) | Nodus DSL language: file types (§wf/§schema/§config), section grammar (@ON/!!rules/!PREF/@in/@out/@ctx/@err/@steps/@test/@macro), step syntax (+modifier/^validator/~flag/→pipeline), control flow (?IF/~FOR/~UNTIL MAX:n/~PARALLEL), macro invocation (RUN(@macro_name)), error taxonomy (11 NODUS:* codes), 10 core invariants (NL-1…NL-10); ~PARALLEL fail-fast error propagation; §4.6 upstream parity gap v0.4.6→v0.7 (control: ?SWITCH/~MAP/~RETRY/!HALT/!PAUSE; operators: MATCHES/?./??/WHERE/FIRST/LAST/string-interpolation; HITL dialog: ASK/CONFIRM; @needs selective schema loading; error taxonomy 11→24; @ON(priority=N); closed flag/validator/type registries); NL-11 provenance-safe interpolation — a value interpolated into a model-facing prompt (GEN/REFINE) carries trusted/untrusted provenance (unknown=untrusted; model-output/ASK-answer/external-fetch/memory-read=untrusted), untrusted neutralized-as-data by default at render, raw insertion needs an explicit modifier, sticky provenance, neutralization host-supplied (LP-2); the nodus realization of the main l1-context-provenance contract; NL-12 deferred/external step execution — a step MAY suspend the run pending an external completion (backgrounded tool result, delegated/external execution, human-performed action), reusing the Status::Paused + resume-descriptor lifecycle (DG-4) but generalizing the resume trigger from a human answer to any host-supplied completion correlated to the suspended step by a token; deterministic resume, stray/consumed-token rejected, host owns background/delegation/persistence (LP-1/LP-2) + declares capability (LP-8), fail-fast never-hang; the nodus realization of the main l1-execution-graph EG-12 deferred-execution contract (a human dialog is the special case where the external actor is a person); NL-13 competitive parallel selection — a `~PARALLEL` block MAY declare a `+select` discipline (`first-success`/`best`) binding exactly one winning branch via `~PICK` instead of `~JOIN` integrate-all; non-winning branches cancelled+surfaced never merged (one whole winner, not a blend); ranking host-supplied (LP-2, `best` needs an LP-8 capability); determinism preserved (`first-success`=declaration order under sequential exec §2, `best` deterministic when the selector is, NL-6); fail-semantics (`first-success` fails only if all fail, `best` needs ≥1 success, all-fail→`@err:` typed, `!!` bypass per NL-2); a selection discipline on `~PARALLEL`, not a new command; the nodus realization of the new main l1-competitive-execution best-of-N contract (CE-2/CE-4/CE-5), `~JOIN` integrate-all remaining the parallel-staffing analog; NL-14 grader-gated iterative refinement — a `~UNTIL` loop MAY declare a `+grade` discipline whose continuation is decided by a host-supplied grader verdict over the produced artifact (latest GEN/REFINE output) instead of a bare boolean; a *continue* verdict threads structured feedback into a reserved loop variable the next REFINE reads (feedback threading) so each cycle targets the unmet criteria; the rubric/criteria are immutable for the loop's lifetime — the workflow revises the artifact but never rewrites the criteria that judge it (LP-10, no goalpost drift); bounded by MAX:n (NL-5), terminating on pass or exhaustion (exhaustion binds best-so-far and routes named unmet criteria to `@err:` typed, NODUS:MAX_REACHED/refinement code, never open-ended); grader is a function-scoped host-supplied auxiliary role (LP-2, no rubric vocabulary or metric in core) declared as an LP-8 capability when it needs an external judge and rejected fail-fast when unsatisfied; determinism preserved when the grader is (NL-6); a plain boolean `~UNTIL` is unchanged and `+grade` is opt-in (additive); a discipline layered on `~UNTIL`, not a new command; §4.4 `~UNTIL +grade` note added; the nodus realization of the new main l1-iterative-refinement generate–evaluate–refine contract (IR-1/IR-2/IR-3/IR-5/IR-6), the sequential-depth complement to NL-13's competitive width (`~UNTIL +grade` deepens one artifact, `~PARALLEL +select` selects among many); NL-15 cache-stable prompt composition — nodus renders a model-facing GEN/REFINE prompt so its reusable segments (system preamble, loaded §schema/vocabulary, macro definitions, loop-invariant +grade rubric) form a byte-stable leading prefix emitted first in deterministic stable order, with per-invocation volatile content (current @in, prior-iteration feedback, loop state, freshly interpolated values) confined to the suffix; because the prefix stays byte-identical across `~UNTIL +grade`/`~RETRY` iterations and across steps that share it, a host prefix-addressed cache (durable local or provider prompt cache) reuses the ingested prefix state instead of recomputing; nodus defines only the ordering discipline + prefix/volatile boundary, the cache entirely host-supplied (LP-2, no cache/tier/block in core) so a host with no cache is unaffected (additive); composes NL-6 (deterministic rendering makes the prefix byte-stable) + NL-11 (untrusted neutralized-as-data belongs in the volatile suffix so it never destabilizes the shared prefix), surfaced via HO-8 cache_read/cache_creation classes; the nodus realization of the new main l1-inference-cache prefix-addressed reuse contract (IC-1/IC-9) and the workflow-side realization of l1-cache-stable-context's frozen-prefix discipline — the authoring guarantee that lets recompute become lookup; NL-16 selective vocabulary disclosure — a workflow declares via `@needs` (in `§runtime:`) the specific schema/vocabulary units (named sections of an `extends:` host schema) it uses, and the runtime loads only the declared units into the validation/execution context, the rest of the host vocabulary staying deferred; a schema's `§meta`+`!!` rules always load (safety floor), every optional command group disclosed on demand; using an undeclared unit is a fail-fast validation error (declare what you disclose, LP-8 kinship: a needed vocabulary unit is a declared capability resolved before the run); the disclosed set is deterministic (same `@needs` → same vocabulary, NL-6) and cache-stable (loaded vocabulary is part of the byte-stable prefix, NL-15); nodus defines only the declare-and-load-the-slice discipline, the host schema's units + storage host-supplied (LP-4/LP-2); elevates the `@needs` selective-schema-loading feature from a §4.6 note to a first-class contract; the nodus realization of the new main l1-progressive-disclosure minimal-load contract (PD-1 mandatory `§meta`+`!!` core as always-resident descriptor / PD-2 optional command group as a deferred body expanded only when `@needs` declares it / PD-5 selective loading bounds context rot); NL-17 origin-taint provenance — a value's provenance (NL-11) MAY additionally carry a host-supplied origin taint classifying source-trust (e.g. internal-authored vs external-synced), attached when the value enters the run (memory read, external fetch, synced import); sticky + propagating exactly like NL-11 provenance (a value derived from a tainted value carries it, combining takes the most-untrusted taint), refining but NEVER relaxing the NL-11 boundary — an external/unknown-origin value stays untrusted + neutralized-as-data by default regardless of taint detail (the taint only records which external origin, for host policy/audit/trace, never licensing raw insertion), and a workflow can never upgrade its own taint untrusted→trusted (an upgrade is a host-supplied human-rooted decision, LP-10); origin taxonomy + taint store + upgrade authority entirely host-supplied (LP-2, no origin class in core), a host supplying no taint leaving NL-11 exactly as today (additive); the nodus realization of the new main l1-provenance-taint at-rest origin-trust contract (PT-4/PT-5 propagation + most-untrusted-wins at the value level, PT-6 taint-sets-runtime-provenance-on-recall), the workflow-side carrier of the origin taint the host tracks at rest, never a trust-authority; NL-18 bounded recursive decomposition — a macro/sub-workflow invocation (`RUN`) MAY recurse (invoke itself or another workflow over a piece of its input) with a mandatory depth bound `MAX_DEPTH:n` (the recursive analog of NL-5's `MAX:n` for loops): no depth bound = validation error, exceeding it at run time = fail-fast `NODUS:MAX_DEPTH_REACHED`, never an unbounded blow-up (keeping nodus bounded/non-Turing-complete §6); map-then-reduce over bounded pieces (`~MAP` slices + applies the recursive call per piece, `~JOIN` reduces, a too-large reduce itself bounded); each recursive child carries a parent/root correlation (extending HO-7) so the tree's events+cost roll up (HO-8 per-subtree) and the tree is observable, per-piece derivation riding HO-13; determinism holds (same input + same slicing → same tree, NL-6); nodus contributes only the bounded-recursion control-flow + mandatory depth bound + child correlation, whether an input warrants decomposition and how the model navigates it as an environment being host concerns (LP-1/LP-2, no window size or context metric in core), a workflow that never recurses unchanged (additive); the nodus realization of the new main l1-recursive-decomposition contract (RD-2/RD-4 bounded recursive processing over pieces / RD-3 map-reduce combination / RD-5 observable cost-rolled-up tree / RD-7 deterministic slicing) | Stable | 1 | 1.9.0 |
| [l2-nodus-runtime.md](specifications/l2-nodus-runtime.md) | Rust crate `crates/nodus`: module structure (lexer/parser/ast/vocab/validator/executor/observability/portability/transpiler/workflows), invariant compliance table (NL-1…NL-10, E013/E014 enforced), Value enum, executor boot sequence (6 steps), public API (scaffold/validate/run/transpile/test/run_with_provider/run_with_audit/run_with_provider_and_audit/run_with_schema/run_with_schema_and_audit), ModelProvider + AuditProvider + SchemaProvider extension points, vocabulary schema v0.4.6 (51 commands + RUN meta-command); §4.7 upstream parity gap v0.4.6→v0.7 (missing ASK/CONFIRM commands, control constructs ?SWITCH/~MAP/~RETRY/!HALT/!PAUSE, operators/expressions, @needs, error_code 11→24, closed flag/validator/type registries, macro execution, Status::Paused, lexer parity) | Stable | 2 | 1.2.0 |
| [l1-nodus-portability.md](specifications/l1-nodus-portability.md) | Portability and extension contract: host neutrality (LP-1), extension via abstract interfaces (LP-2), two-host generalisation rule (LP-3), vocabulary isolation (LP-4), composable extension (LP-5), semantic versioning (LP-6), feedback loop lifecycle (LP-7); extension point taxonomy (ModelProvider + AuditProvider + future StorageProvider/PolicyProvider), feedback distillation protocol, universal pattern criteria, vocabulary layering model; capability manifest with pre-run satisfiability validation (LP-8, fail-fast) as the machine-checkable LP-3 portability contract; LP-9 extraction attestation — the extraction bundle + capability manifest carry an independently-verifiable witness binding exact content-set + version, host verifies before loading an imported workflow bundle and refuses fail-closed on missing/failed witness, signing/verification mechanism host-supplied (LP-2, no crypto in core), the nodus realization of the main l1-attestation contract; LP-10 host-granted authority never self-authored — a workflow has no ambient authority, it declares capability needs (LP-8) and reads host-supplied policy (PolicyProvider) but MUST NOT grant itself a capability/relax a policy/widen its reach; the language has no vocabulary to write policy (a step may request an outcome NL-12 but never self-authorize), and because a workflow may be assembled under untrusted influence (NL-11) its declared-capability manifest is the single consent point the host resolves before the run so an injected instruction cannot escalate beyond it; granting stays host-side (LP-1/LP-2); the nodus realization of the main l1-security SEC-10 authority-self-containment contract; LP-11 per-effect authorization seam — an effectful step (model call / tool use / deferred NL-12 completion) MAY be gated by a host-supplied authorization decision evaluated before the effect, the runtime guaranteeing decide → effect → observe ordering (the AuditProvider observer runs after, on the real result); fail-closed on deny/error/timeout with a typed NODUS:*code (never silently permits, never hangs); the decision host-supplied through the now-concretized PolicyProvider role (LP-2, no policy vocabulary in core) with a step declaring only its effect class for host matching and — per LP-10 — never authoring or relaxing the policy; the dynamic per-effect complement to the static whole-run LP-8 manifest (LP-8 strips an unsatisfiable effect pre-run, LP-11 denies a satisfiable effect per attempt); purely additive — no PolicyProvider = built-in allow-all = today's behaviour (§4.1 PolicyProvider row concretized + new §4.7); the nodus realization of the new main l1-interception-model decide-class seam INT-1 / check-before-use ordering INT-2 / fail-closed guard direction INT-3 / strip-vs-deny axis INT-6; LP-12 imported-bundle admission vetting seam — before loading an imported workflow bundle or a host schema/vocabulary extension the runtime MAY submit the artifact to a host-supplied vetting classifier returning typed content findings (over embedded NL + the LP-8 manifest + LP-11 effect classes) and enforce the host's admission disposition: fail-closed refusal on an admission-blocking finding (typed NODUS:*, never a partial load) else pass-through with the verdict recorded as load-time provenance; content-safety distinct from and sequenced after LP-9 authenticity (an authentic bundle can still carry an injected instruction NL-11), the load-gate order attest → vet → run; classifier/heuristics/severity/taxonomy entirely host-supplied (LP-2, no threat pattern or metric in core) so a host with no classifier loads exactly as today (additive); verdict crosses the observability data-safety boundary as counts/descriptors (§4.4); NL-11 provenance + LP-11 effect classes are the static substrate a host reads to compute a cross-effect toxic-flow finding over the bundle graph, the flow policy staying host-supplied; new §4.8; the pre-load static complement to the per-effect runtime gate LP-11; the nodus realization of the new main l1-component-scanning admission-vetting contract (CS-2 gate / CS-3 content-≠-integrity / CS-6 cross-component flow / CS-8 advisory-vs-critical / CS-9 channel data-safety); LP-13 addressable versioned import resolution — an imported workflow bundle or host schema/vocabulary extension MAY be referenced by a stable name + version constraint that a host-supplied resolver resolves to a concrete pinned revision (from a host-supplied source/catalog) before the load gate; deterministic given a recorded lockfile-like pin, the resolved pin being the identity LP-9 attests and LP-12 vets — full load order resolve → attest → vet → run; catalog/source-addressing/transport/version-selection entirely host-supplied (LP-2, no registry protocol or URL scheme or version algebra in core) so a host with no resolver runs as today with only concrete artifacts (additive); an unresolvable/ambiguous/unpinned reference rejected fail-fast pre-run (LP-8 kinship — a missing bundle dependency is a missing capability, never mid-run); a resolved bundle's identity slug is stable (a rename is a host-supplied recorded migration, not a silent re-point) so a pinned reference cannot be swapped for a different bundle; extends the LP-8 manifest with named external bundle dependencies + their resolution; new §4.9; the nodus realization of the new main l1-extension-marketplace distribution contract (XM-1/XM-2/XM-3/XM-8), the pre-load resolution stage preceding the LP-9/LP-12 load gate; LP-14 verified-peer delegation seam — a deferred/external step (NL-12) MAY target a named external peer agent rather than an anonymous host-supplied completion; the peer's verifiable identity + offered capability are resolved by a host-supplied peer directory/identity provider before the delegation, the runtime guaranteeing resolve+authorize before the effect (the LP-11 per-effect gate applies: decide → delegate → observe), the peer's result entering as untrusted provenance (NL-11) and optionally carrying a host-supplied execution receipt (HO-9 witness) so a fabricated peer result is detectable; an unresolvable/unauthorized peer is rejected fail-fast pre-delegation (LP-8 kinship, never mid-run); peer identity/directory/transport/channel-encryption/settlement entirely host-supplied (LP-1/LP-2, no network/key/ledger/chain in core) so a workflow delegating to no peer is unaffected (additive); generalizes NL-12's external actor from an anonymous completion to a verifiable named peer (a human dialog is the special case where the actor is a person); new §4.10 reusing the NL-12 suspend/correlate/resume + LP-11 gate + NL-11 provenance + HO-9 receipt seams with no new trust primitive in the core; the nodus realization of the new main l1-agent-federation peer-interaction contract (AF-1 sovereign verifiable identity the host resolves / AF-6 default-deny peer trust / AF-7-AF-8 accountable attribution); LP-15 host-supplied durable-state seam — durable workflow state persisted across invocations (a paused-run resume descriptor NL-12/DG-4, memoized approvals DG-9, any workflow-declared outlive-one-run data) is reached through the host-supplied StorageProvider role (§4.1 TBD now resolved) so a workflow never assumes where its durable state lives; the core ships one built-in in-memory default (non-durable, discarded between invocations) and a host MAY back the seam with a local on-device store or a remote database with no workflow change (portable across both, LP-3); the core names no database/path/transport/auth (LP-1/LP-2), so where state is stored and whether persisting it leaves the device are host concerns (off-device is the host's egress decision, mirroring main DN-4), never something nodus performs or assumes; a workflow declaring durable state as an LP-8 capability is rejected fail-fast on a host with no durable StorageProvider; §4.1 StorageProvider row filled + new §4.11; the nodus realization of the new main l1-deployment-neutrality provider-seam contract (DN-2 storage plane as abstract seam / DN-3 local-or-remote by host choice / DN-7 portability across backends); LP-16 effect risk-class declaration — an effectful step (LP-11: model-call/tool-use/deferred) MAY declare consequence descriptors (reversibility, external-visibility, value-at-stake magnitude) alongside its effect class so a host graduated authorization gate can tier the effect by consequence (auto/confirm/approval) rather than gating uniformly, carried to the host PolicyProvider before the effect (decide → effect → observe preserved); tier-to-friction mapping + thresholds + gate strength entirely host-supplied (LP-2, no risk metric/tier/friction in core), and — per LP-10 — a step declares its consequence but can never set or lower its own tier; an unclassified effect defaults to the host's most-cautious handling (fail-safe-to-friction); purely additive (no descriptors + no PolicyProvider = today's behaviour); new §4.12; the how-much-friction complement to the static LP-8 manifest (may this effect exist?) and the dynamic LP-11 gate (is this use allowed?); the nodus realization of the new main l1-action-gating risk-proportional contract (AG-1/AG-2 consequence axes + proportional tiering / AG-4 fail-safe-to-friction / AG-6 & AG-8 declare-not-self-set) | Stable | 1 | 1.9.0 |
| [l1-nodus-observability.md](specifications/l1-nodus-observability.md) | Execution observability contract: trace-first output (HO-1), per-step attribution (HO-2), append-only immutability (HO-3), frozen evaluation boundary (HO-4), observer neutrality (HO-5), structured event taxonomy (HO-6), monotonic sequence + run-correlation ordering with streaming chunk-merge into one logical model_response (HO-7); AuditProvider role, 10-type event taxonomy (step_start/step_end/step_error/constraint_hit/branch_taken/loop_iteration/macro_enter/macro_exit/model_call/model_response), run manifest, data-safety boundary (no raw user text in traces), frozen-vs-evolvable component table; §4.6 sequence/correlation fields + streaming merge underpins the trajectory projection in l1-nodus-environment; HO-8 cost-attribution token classes (input/output/cache_read?/cache_creation?) on model_response, counts-only within data-safety, cost computable from the trace; HO-9 execution-authenticity receipt — a step event MAY carry a host-supplied model-unforgeable receipt binding step identity+observed result so a verifier distinguishes a genuine step result from a fabricated one, signing mechanism host-supplied (LP-2, no crypto in core, mirroring the LP-9 attestation seam), receipt an opaque secret-free token within the data-safety boundary (signing secret never in trace/prompt/context), optional+additive so HO-5 observer neutrality holds, an optional field on existing events not a new event type (HO-6); the nodus realization of the main l1-tool-receipts execution-authenticity contract; HO-10 trace-completeness honesty — a persisted trace is either complete (carries a terminal RunManifest via run_complete, status Ok/Error/ConstraintHalt/ValidationError) or explicitly truncated (events but no terminal manifest), decidable from the trace alone; a headless fragment left by a killed/panicked/OOM'd host process MUST NOT be mistaken for a completed or a still-running run; extends HO-7 dropped-event gap detection to the truncated tail so any trace classifies complete/gap-damaged/truncated; nodus does NOT capture the terminating crash — that is the host forensic diagnostic-log plane (the nodus realization of the new main l1-diagnostic-log concept, DL-2, installed below and earlier than the executor and surviving a native fault the executor cannot), HO-10 guaranteeing only that the semantic trace never lies about its own completeness and naming the witnessed-vs-only-host-forensic boundary (the observability analog of the INT-8 honest-coverage discipline); a read-side interpretation of manifest presence/absence with no new hot-path emission, HO-5 observer-neutrality preserved, purely additive (§4.7 added); HO-11 single-stream dual legibility — an ExecutionEvent MAY carry an optional host-rendered human-legible one-line message derived from its own structured fields, so one AuditProvider stream serves both a human reader and a machine/AI analyzer without a second parallel prose log that can drift; the structured fields stay the source of truth (a faithful projection that adds/contradicts no fact) within the §4.4 data-safety boundary (descriptors/counts, never raw content); renderer + localization host-supplied (LP-2, no format/locale vocabulary in core), optional + additive so a host rendering nothing emits events as today, HO-5 observer-neutrality and HO-6 closed taxonomy preserved (an optional field on existing events like HO-8/HO-9); the stream-bounding economy (payload policy, compaction, budget-bounded agent-context feed, LL-4/LL-8) stays host-side while nodus contributes the source-side pieces HO-6 taxonomy + §4.4 counts-not-content + HO-7 sequence; the nodus realization of the new main l1-log-legibility single-canonical-event / dual-audience-faithfulness contract (LL-1/LL-2); §4.8 added; HO-12 execution-mode provenance — the RunManifest declares the run's execution mode (real vs simulated, plus fidelity when simulated) so a consumer excludes simulated runs from real-run analytics (host health scoring, HO-8 cost/usage accounting, the outer evolution loop) and never mistakes a modeled play-out for production; a simulated run is one where the host substituted modeled providers (mock ModelProvider, no-effect tool/host surface) to exercise the mechanics without real effects — nodus mocks nothing (host-neutral LP-1/LP-2, the substitution host-supplied), it only records which mode the host declared, making the trace self-describing (a real run distinguishable from a simulated one from the manifest alone, as HO-10 distinguishes complete from truncated); optional + additive, a manifest without the marker is real (today's behaviour), HO-5 observer-neutrality and HO-6 closed taxonomy preserved; the nodus realization of the new main l1-simulation legible-fidelity / observable-play-out contract (SIM-3/SIM-6), same honesty family as HO-10 (completeness) and HO-11 (legibility); §4.9 added; HO-13 per-item derivation lineage — an ExecutionEvent for a collection-mapping step (a `~MAP` element, `~FOR` iteration, or pipeline transform over a List) MAY carry an optional derivation descriptor mapping each produced element to the source element(s) it derived from (reference mapping source index/id → produced index/id) so a consumer reconstructs end-to-end item lineage from the trace alone; side-band metadata on existing events like HO-8/HO-11, never a new event type (HO-6) and never part of the workflow's Value payload so NL-7's closed value-type system is untouched and lineage cannot leak into or perturb business data (matching main l1-data-lineage LN-5 metadata-not-payload); records references (indices/ids in the mapping domain) never copies of element content, within the §4.4 data-safety boundary (LN-8); records the true fan-out (split-out 1→M) / fan-in (~JOIN K→1) / filter shapes as they are (LN-4); optional + additive so a host emitting none behaves exactly as today (HO-5 observer-neutrality + HO-6 closed taxonomy preserved); §4.10 added; the nodus realization of the new main l1-data-lineage derivation-traceability contract (LN-2 per-step immediate links composing into LN-3 transitive lineage, LN-5 side-band metadata, LN-8 references-not-copies), sitting in the observability plane because lineage is observational — nodus contributes the source-side per-element mapping, the host walks it | Stable | 1 | 1.7.0 |
| [l2-nodus-observability.md](specifications/l2-nodus-observability.md) | Rust implementation of observability contract: `observability.rs` module (AuditProvider trait, ExecutionEvent 10-variant enum, NoopAuditProvider, RunManifest, FieldDescriptor), executor hook-point wiring for all 10 event types, `run_with_audit` + `run_with_provider_and_audit` public API additions; HO-1…HO-6 compliance table; 13-test plan (unit + integration including observer-neutrality gate) | Stable | 2 | 1.1.0 |
| [l2-nodus-portability.md](specifications/l2-nodus-portability.md) | Rust implementation of portability contract: LP-1…LP-8 compliance table; SchemaProvider trait (vocabulary-extension seam, `Schema::with_provider`); StorageProvider + PolicyProvider pending LP-3 interfaces; extension point registry (ModelProvider, AuditProvider, SchemaProvider implemented; StorageProvider/PolicyProvider pending); §4.7 LP-8 capability manifest (`CapabilityManifest`/`ExtensionRole`/`HostCapabilities`/`Missing`, pure `validate_manifest` resolver, `run_with_manifest`/`run_with_manifest_and_audit` fail-fast gate before executor boot, `NODUS:CAPABILITY_UNMET`, AST-derived `from_workflow`) as the machine-checkable LP-3 contract; extraction artifacts (CI, EXTRACTION.md, Cargo manifest) as LP-6 deliverables | Stable | 2 | 1.1.0 |
| [l1-nodus-testing.md](specifications/l1-nodus-testing.md) | Nodus DSL testing contract: `@test:` block structure (input:/expected:/tags:), NT-1…NT-11 invariants (block isolation, input override, assertion binding, failure semantics, provider neutrality, tag metadata, ordered reporting, schema inheritance, parse-time validation, route coverage advisory, differential parity across execution paths), execution protocol, TestReport/TestResult contract, tag-based filtering, validation diagnostics (E015/W006/W009); §4.7 differential-parity harness — interpreter↔transpiler/two-host equal observable results (output + trace shape) on a content-addressed fixture corpus, the executable form of portability LP-3 | Stable | 1 | 1.1.0 |
| [l2-nodus-testing.md](specifications/l2-nodus-testing.md) | Rust implementation of testing contract: `TestBlock` AST node (input/expected/tags/raw_lines), `RunResult.vars` variable environment exposure, `test()`/`test_with_tags()` public API, `parse_expected_value` type coercion, `evaluate_test_block` assertion evaluator, NT-1…NT-10 compliance table, validator diagnostics (E015/W006/W009), test coverage map | Stable | 2 | 1.1.0 |
| [l1-nodus-dialog.md](specifications/l1-nodus-dialog.md) | Human-in-the-loop dialog contract: `ASK` (typed question — str/bool/confirm/choice/multi_choice + options/hint/default/validate/timeout) and `CONFIRM` (approval — msg/actions/default/strict) command class; suspend/resume lifecycle (`Status::Paused`, human re-trigger); DG-1…DG-8 invariants (host neutrality, blocking progression, typed binding, suspend/resume determinism, typed failures, default-on-absence, trace data-safety, LP-8 capability declaration); dialog backend as a distinct `ExtensionRole::Dialog` (LP-2/LP-8); paused-run state = `Status::Paused` + host-persisted resume descriptor (built-in host synchronous, default-or-Paused); dialog error subset (`DIALOG_TIMEOUT`/`DIALOG_REJECTED`/`PAUSED`); elevates `l1-nodus-language` §4.6 HITL gap; DG-9 memoizable approval (§4.6) — a `+remember`-marked `ASK`/`CONFIRM` MAY be satisfied by the host `DialogProvider` from a durable prior decision keyed by a stable dialog identity, without re-prompting; store/scope/revocation are host concerns (LP-1), nodus contributes only the `+remember` marker + a remembered-vs-answered trace provenance (DG-7); never turns reject→approve, never relaxes `+strict`, never binds a value failing `+type`/`+validate`, host MAY decline → prompts normally; the host-neutral nodus realization of the main l1-security SEC-9 learnable-permission contract; DG-10 promotable remembered decision (host-ratified, never self-authored) — a `+remember`-eligible ASK/CONFIRM (DG-9) whose remembered decision recurs with a stable answer across runs MAY be surfaced, carrying its recurrence provenance (same dialog identity + same answer on distinct occasions, extending the DG-7/DG-9 remembered-vs-answered trace), as a host-promotable candidate for a standing `!PREF` preference rule, so a stably-repeated human decision graduates from remembered-per-decision to a standing workflow preference; nodus contributes only the eligibility + recurrence provenance, while the durability gate + human ratification + actual promotion to a `!PREF`/rule are entirely host-supplied (LP-2) and — per LP-10 — a workflow never self-authors the promotion (surfaces a candidate, does not mint a rule); a promoted preference enters as an ordinary `!PREF` soft rule (NL-3, `!OVERRIDE`-able), never silently hardened to a `!!` hard rule, demotion leaving DG-9 memoization untouched; purely additive (a host that promotes nothing keeps DG-9 as today); new §4.7; the nodus realization of the new main l1-pattern-codification contract (PC-1 earned-not-automatic / PC-2 & PC-8 propose-not-self-author / PC-7 auditable pathway) | Stable | 1 | 1.3.0 |
| [l2-nodus-dialog.md](specifications/l2-nodus-dialog.md) | Rust realization of the dialog contract (`l1-nodus-dialog`): `ASK`/`CONFIRM` vocabulary + modifiers; `Status::Paused` + `ResumeDescriptor` (host-persisted); `DialogProvider` trait + built-in synchronous `DefaultDialogProvider` (default-or-pause, no I/O); `ExtensionRole::Dialog` added to LP-8 taxonomy + `from_workflow` derivation (required when a dialog lacks `+default`); executor dispatch (Answer/Pause/Timeout/Rejected); `run_with_dialog`/`run_with_dialog_and_audit` combinators; emits `DIALOG_TIMEOUT`/`DIALOG_REJECTED`/`PAUSED`; DG-1…DG-8 compliance | Stable | 2 | 1.0.0 |
| [l2-nodus-control-flow.md](specifications/l2-nodus-control-flow.md) | Rust realization of the v0.7 control constructs (`l1-nodus-language` §4.6 / `l2-nodus-runtime` §4.7(b)): `?SWITCH` multi-branch dispatch (+`*` default, `SWITCH_NO_MATCH`), `~MAP` collection transform (implicit `$it`), `~RETRY:n` bounded step retry (NL-5, max 10), `!HALT` (→`Status::Failed`, requires ESCALATE), `!PAUSE` (→`Status::Paused`, reuses dialog suspension); lexer tokens + `SwitchBlock`/`MapBlock` AST + action flags + retry; parser/executor/validator/transpiler wiring; reuses existing `Status`/`Signal`/error codes; phased implementation | Stable | 2 | 1.0.0 |
| [l2-nodus-errors.md](specifications/l2-nodus-errors.md) | Rust realization of the 24-code error taxonomy (`l1-nodus-language` §4.6): `ErrorSeverity` (error/warn/info) + `ErrorCategory` (parse/runtime/validation/routing/memory/test/control/dialog) types; per-code severity×category registry; `error_meta()` lookup; `EXECUTION_FAILED` demoted to non-canonical legacy + per-site reassignment (UNDEFINED_CMD/UNDEFINED_MACRO/VALIDATION_FAILED/ESCALATION_FAILED/CONFIDENCE_LOW/KB_UNAVAILABLE/MEMORY_FAILED); validator/executor emission map; NL-1/NL-2/NL-4/NL-9 compliance; `CAPABILITY_UNMET` noted as the LP-8 portability-layer code | Stable | 2 | 1.0.0 |
| [l2-nodus-registries.md](specifications/l2-nodus-registries.md) | Rust realization of closed vocabulary registries (`l1-nodus-language` §4.6, contents per `l2-nodus-runtime` §4.7(f)): `KNOWN_FLAGS` (12 analysis extractors) + `KNOWN_VALIDATORS` (12 validator names, pre-colon match) + `PRIMITIVE_TYPES` (10 field types) as `vocab` data; `Schema::is_known_flag`/`is_known_validator`/`is_known_type` query surface; advisory (warning-severity) validator diagnostics for unknown `~flag`/`^validator`/`@in` type; strengthens NL-1/NL-7/NL-9; advisory-first (no hard break), host extension via SchemaProvider is a follow-on | Stable | 2 | 1.0.0 |
| [l1-nodus-environment.md](specifications/l1-nodus-environment.md) | Environment & Evaluation contract — the executable substrate an evaluation-driven improvement loop grades a workflow against: `EnvironmentProvider` as a 5th extension role beside Model/Audit/Schema/Dialog (NE-1); closed reset/step/evaluate lifecycle bracketed by open/release, deterministic per (task, seed, actions) (NE-2); trajectory as an ordered projection onto the existing AuditProvider stream, not a parallel channel (NE-3, uses HO-7 seq); frozen-evaluation boundary — evaluate is read-only over a completed run, reward computed after the frozen point (NE-4); reward is typed data not control, orthogonal to run Status (NE-5); addressable task catalog + profile as the interchangeability contract (NE-6); instance isolation + mandatory idempotent release (NE-7); function-scoped auxiliary model roles policy/context/summary, economical-by-default, corroborates routing RTG-9 (NE-8); host-supplied metric neutrality — nodus defines reward shape not metric (NE-9); capability-manifest-declared `ExtensionRole::Environment`, fail-fast pre-run (NE-10, LP-8); declared grading mode automated/judge/hybrid with hybrid-floor (judge may lower not rescue), judge via aux role under host judge-trust (NE-11); §4.6 division of labour vs harness-evolution (this = graded run, harness = what-to-change) + §4.7 grading-mode composition + sliceable orthogonal task labels feeding the host agent co-evaluation matrix (one graded run = one matrix cell); NE-12 archivable candidate result — a completed graded run yields a content-addressable (workflow-hash, reward, trajectory) tuple a host outer-loop optimizer can archive/compare/place-on-a-frontier, nodus supplies only the substrate (candidate space/mutation/search/frontier stay host-side, LP-1/LP-2), the nodus feed for the main l1-harness-optimization concept; NE-13 budget-normalized graded runs — an environment profile (NE-6) MAY declare a fixed resource budget (wall-clock / steps / tokens) uniformly halting every candidate run graded against that profile; hitting the budget is a normal graded outcome not an error (evaluate runs over what was achieved within the budget, NE-4), so arbitrarily heterogeneous candidates are directly comparable on outcome-at-fixed-budget and a faster candidate simply achieves more inside the budget (no separate efficiency metric); the declared budget is part of the profile's identity — an NE-12 archived tuple records the budget its reward was earned under, rewards under different budgets are not comparable, a host optimizer MUST partition its frontier by (profile, budget); enforced by the environment lifecycle (NE-2), a workflow cannot read/extend/evade its budget (LP-10 kinship); deterministic where the budget is (steps/tokens; wall-clock host-measured + recorded); a profile with no budget behaves as today (additive); the nodus realization of the main l1-harness-optimization HX-11 budget-normalized evaluation contract (fix the budget, not the workload) | Stable | 1 | 1.3.0 |
| [l2-nodus-environment.md](specifications/l2-nodus-environment.md) | Rust realization of the Environment/Evaluation contract (`l1-nodus-environment`): `EnvironmentProvider` trait + built-in deterministic `StubEnvironment`; closed `open/reset/step/evaluate/release` lifecycle; typed `Reward` carried in `EnvRunResult` alongside (never inside) the run; `Trajectory` as a read-side projection via optional `EnvInteraction` side-band descriptors on existing events — no new `ExecutionEvent` variant, HO-6 preserved (the HO-8/HO-13 additive-field discipline), FieldDescriptor-only data-safety; frozen-boundary `run_with_environment`/`run_with_environment_and_audit` combinators (evaluate strictly after the freeze, release mandatory+idempotent via drop guard); `ExtensionRole::Environment` added to the LP-8 manifest with `HostCapabilities::builtin()` providing it via the stub (deliberate contrast with `Dialog` — the stub is a complete trivial world), `from_workflow` fail-fast reusing `NODUS:CAPABILITY_UNMET`; closed `GradingMode` automated/judge/hybrid with the hybrid floor (checker-first, judge may lower not rescue, `min(checker,judge)`); `EnvironmentProfile` with orthogonal slice labels + optional `Budget` (wall_clock/steps/tokens, NE-13 budget-halt = normal `Status::Partial`, part of profile identity, workflow cannot read/extend/evade); `CandidateResult` content-addressable tuple (NE-12, std-digest zero-dep, cryptographic address host-supplied); NE-1…NE-13 compliance; adds one extension role, no new command/status/error category | Stable | 2 | 1.0.0 |

## Meta Information

- **Maintainer**: Core Team
- **Last Updated**: 2026-07-10 (authored **l2-nodus-environment v1.0.0 Stable** — the Rust realization of the `l1-nodus-environment` Environment/Evaluation contract in `crates/nodus`: `EnvironmentProvider` trait + `StubEnvironment`, `open/reset/step/evaluate/release` lifecycle, `EnvRunResult`/`Reward`, `Trajectory` as a side-band projection on existing events (no new `ExecutionEvent` variant, HO-6 preserved), `ExtensionRole::Environment` manifest binding with `builtin()` providing it via the stub, `run_with_environment` frozen-boundary combinators, closed `GradingMode` hybrid-floor, `EnvironmentProfile` budget (NE-13), `CandidateResult` content address (NE-12); NE-1…NE-13 compliance table — **reconciles the standing pending Invariant-Compliance obligation** carried by the NE-11/NE-12/NE-13 refinements. `l1-nodus-environment` is now realized (no longer concept-only). Design-only spec ahead of implementation, per the l2-nodus-dialog / l2-nodus-control-flow precedent; the implementation phase is opened by the next `/magic.task nodus`. Prior: 2026-07-09 (adoption pass over an external autonomous-experimentation-loop reference — DUPLICATION-CHECK outcome, no new spec: the reference's core (autonomous modify→run→measure→keep/discard loop, mutable-surface restriction with an immutable oracle, experiment journal) was already realized by main HX-1…HX-10 + loop-governance + NE-12; one genuine additive delta captured as paired refinements: l1-nodus-environment v1.2.0→1.3.0 (NE-13 budget-normalized graded runs — a profile MAY declare a fixed wall-clock/step/token budget uniformly halting every candidate run graded against it; budget-hit a normal graded outcome not an error, evaluate running over what was achieved within the budget NE-4, so heterogeneous candidates are directly comparable on outcome-at-fixed-budget and a faster candidate simply achieves more inside the budget; the budget is part of the profile identity — NE-12 archived tuples record it, rewards under different budgets are not comparable, a host optimizer partitions its frontier by (profile, budget); enforced by the environment lifecycle NE-2, a workflow cannot read/extend/evade its budget (LP-10 kinship); additive). Main deliverable of the same pass: l1-harness-optimization v1.0.0→1.1.0 (HX-11 budget-normalized evaluation & mutation-invariant metric — fix the resource budget not the workload so heterogeneous candidates compete directly and the optimum is platform-relative; differing budgets partition the frontier; the scoring metric must be mutation-invariant, the criteria-drift sibling of oracle-ownership). Both L1 stay Stable (C9); l2 realization carries NE-13 as a pending Invariant-Compliance obligation reconciled at magic.task (NE-11/NE-12 precedent). Prior: adoption pass over an external recursive-language-model harness reference — one additive nodus refinement paired with a new main concept, no cascade: l1-nodus-language v1.8.0→1.9.0 (NL-18 bounded recursive decomposition — a macro/sub-workflow `RUN` MAY recurse over a piece of its input with a mandatory depth bound `MAX_DEPTH:n` (the recursive analog of NL-5's `MAX:n`): no depth bound = validation error, exceeding it = fail-fast `NODUS:MAX_DEPTH_REACHED`, keeping nodus bounded/non-Turing-complete; map-then-reduce over bounded pieces (`~MAP` slices + recursive call per piece, `~JOIN` reduces); each recursive child carries a parent/root correlation (extending HO-7) so the tree's events+cost roll up (HO-8) and is observable, per-piece derivation riding HO-13; deterministic (NL-6); nodus contributes only the bounded-recursion control-flow + mandatory depth bound + child correlation, whether-to-decompose and how-to-navigate-the-environment being host concerns LP-1/LP-2, additive; the nodus realization of the new main l1-recursive-decomposition RD-2/RD-3/RD-4/RD-5/RD-7). Main deliverable of the same pass: new l1-recursive-decomposition v1.0.0 Stable (the third stance on an over-window input beside compression and progressive disclosure — process it in place by bounded recursive self/sub-calls over pieces so effective context exceeds the raw window: RD-1 input-as-environment, RD-2 recursive processing over bounded pieces, RD-3 map-then-reduce, RD-4 bounded-by-construction fail-safe, RD-5 observable cost-rolled-up tree, RD-6 third-orthogonal-stance, RD-7 deterministic slicing, RD-8 grounded reduction coverage-never-faked). L1 stays Stable (C9); l2-nodus-runtime carries NL-18 as a pending Invariant-Compliance obligation reconciled at magic.task. Prior: adoption pass over an external business-operations agent-harness reference — one additive nodus refinement paired with a new main concept, no cascade: l1-nodus-portability v1.8.0→1.9.0 (LP-16 effect risk-class declaration — an effectful step LP-11 MAY declare consequence descriptors (reversibility, external-visibility, value-at-stake magnitude) alongside its effect class so a host graduated authorization gate tiers the effect by consequence (auto/confirm/approval) rather than uniformly, carried to the host PolicyProvider before the effect; tier-to-friction mapping + thresholds + gate strength entirely host-supplied LP-2, and — per LP-10 — a step declares its consequence but can never set or lower its own tier; unclassified effect defaults to the host's most-cautious handling (fail-safe-to-friction); purely additive (no descriptors + no PolicyProvider = today's behaviour); new §4.12; the how-much-friction complement to the static LP-8 manifest and the dynamic LP-11 gate; the nodus realization of the new main l1-action-gating AG-1/AG-2/AG-4/AG-6/AG-8). Main deliverable of the same pass: new l1-action-gating v1.0.0 Stable (authorization friction proportional to an action's consequence — the how-much-friction contract distinct from the threat-guard and config-lockdown: proportional-not-uniform AG-1, explicit legible consequence axes AG-2, closed ordered auto/confirm/approval ladder AG-3, read-safe-frictionless/irreversible-always-gated/unknown-fails-to-friction AG-4, friction-fatigue a first-class failure AG-5, learned de-escalation never self-bypass AG-6, auditable gate decisions AG-7, governable-stricter-never-silently-laxer AG-8). L1 stays Stable (C9); l2-nodus-portability carries LP-16 as a pending Invariant-Compliance obligation reconciled at magic.task. Prior: adoption pass over an external memory-engine reference — one additive nodus refinement paired with a new main concept, no cascade: l1-nodus-language v1.7.0→1.8.0 (NL-17 origin-taint provenance — a value's provenance NL-11 MAY additionally carry a host-supplied origin taint classifying source-trust (internal-authored vs external-synced), sticky + propagating exactly like NL-11 provenance (derived value carries it, combining takes the most-untrusted), refining but NEVER relaxing the NL-11 boundary — an external/unknown value stays untrusted + neutralized-as-data by default regardless of taint detail, and a workflow can never upgrade its own taint untrusted→trusted (host-supplied human-rooted LP-10); origin taxonomy + taint store + upgrade authority entirely host-supplied LP-2, a host supplying no taint leaving NL-11 as today additive; the nodus realization of the new main l1-provenance-taint PT-4/PT-5/PT-6, the workflow-side carrier of the origin taint, never a trust-authority). Main deliverable of the same pass: new l1-provenance-taint v1.0.0 Stable (the at-rest persistent propagating origin-trust taint on stored data — the missing at-rest complement to render-side trust-composition, the fifth provenance-family member: captured-at-ingest-by-origin PT-1, external-untrusted-by-default PT-2, persistent + immutable-downward-never-launders + human-rooted-upgrade PT-3, propagates-through-derived-representations PT-4, most-untrusted-wins PT-5, sets-runtime-provenance-on-recall PT-6, inspectable-auditable PT-7, composes-family-distinct-from-attestation PT-8). L1 stays Stable (C9); l2-nodus-runtime carries NL-17 as a pending Invariant-Compliance obligation reconciled at magic.task. Prior: adoption pass over an external agent-memory reference — one additive nodus refinement paired with a new main concept, no cascade: l1-nodus-dialog v1.2.0→1.3.0 (DG-10 promotable remembered decision — a `+remember`-eligible ASK/CONFIRM (DG-9) whose remembered decision recurs with a stable answer across runs MAY be surfaced with its recurrence provenance as a host-promotable candidate for a standing `!PREF` preference rule, so a stably-repeated human decision graduates from remembered-per-decision to a standing preference; nodus contributes only the eligibility + recurrence provenance, the durability gate + human ratification + actual promotion entirely host-supplied LP-2 and — per LP-10 — a workflow never self-authors the promotion; a promoted preference enters as a soft `!PREF` (NL-3, `!OVERRIDE`-able) never silently a `!!` hard rule; purely additive; new §4.7; the nodus realization of the new main l1-pattern-codification PC-1/PC-2/PC-7/PC-8). Main deliverable of the same pass: new l1-pattern-codification v1.0.0 Stable (the disciplined memory-to-governance pathway by which a stable dated behavioral pattern becomes an enforceable rule: earned-not-automatic PC-1, propose→ratify→write PC-2, tiered bindingness PC-3, dated recurrence detection PC-4, re-validated + reversible PC-5, provisional quarantine PC-6, auditable pathway PC-7, feeds-governance-never-self-grants PC-8; generalizes SEC-9 to any behavioral norm). L1 stays Stable (C9); l2-nodus-dialog carries DG-10 as a pending Invariant-Compliance obligation reconciled at magic.task. Prior: adoption pass over an external workflow-automation-engine reference — one additive nodus refinement paired with a new main concept, no cascade: l1-nodus-observability v1.6.0→1.7.0 (HO-13 per-item derivation lineage — an ExecutionEvent for a collection-mapping step (a `~MAP` element, `~FOR` iteration, or pipeline transform over a List) MAY carry an optional derivation descriptor mapping each produced element to the source element(s) it derived from (reference mapping source index/id → produced index/id) so a consumer reconstructs end-to-end item lineage from the trace alone; side-band metadata on existing events like HO-8/HO-11, never a new event type HO-6 and never part of the workflow's Value payload so NL-7's closed value-type system is untouched and lineage cannot leak into or perturb business data (matching main l1-data-lineage LN-5); records references not copies within the §4.4 data-safety boundary LN-8; records true fan-out/fan-in/filter shapes LN-4; optional + additive so a host emitting none behaves as today (HO-5/HO-6 preserved); §4.10 added; the nodus realization of the new main l1-data-lineage derivation-traceability contract LN-2/LN-3/LN-5/LN-8, sitting in the observability plane because lineage is observational). Main deliverable of the same pass: new l1-data-lineage v1.0.0 Stable (derivation traceability across a dataflow — the fourth provenance-family contract answering "what produced this datum", distinct from trust/integrity/grounding: item-grained dataflow LN-1, per-item immediate source at production time LN-2, transitive end-to-end traceability LN-3, faithful links LN-4, side-band metadata never payload LN-5, composes ledger+grounding at the data grain LN-6, survives suspension/sub-flows LN-7, references-not-copies retention-bounded LN-8). L1 stays Stable (C9); l2-nodus-observability carries HO-13 as a pending Invariant-Compliance obligation reconciled at magic.task. Prior: direct design request (authentication / pluggable backend) — one additive nodus refinement paired with a new main concept, no cascade: l1-nodus-portability v1.7.0→1.8.0 (LP-15 host-supplied durable-state seam — durable workflow state persisted across invocations is reached through the host-supplied StorageProvider role (§4.1 TBD now resolved) so a workflow never assumes where its durable state lives; built-in in-memory default (non-durable), a host MAY back it with a local store or a remote database with no workflow change (portable across both LP-3); core names no database/path/transport/auth LP-1/LP-2, off-device persistence the host's egress decision mirroring DN-4; a workflow declaring durable state as an LP-8 capability rejected fail-fast on a host with no durable StorageProvider; §4.1 row filled + new §4.11; the nodus realization of the new main l1-deployment-neutrality DN-2/DN-3/DN-7). Main deliverable of the same pass: new l1-deployment-neutrality v1.0.0 Stable (local-first server-free default with a pluggable door open for auth + remote user-data backend: DN-1 zero-server default, DN-2 identity/auth/storage as provider seams, DN-3 remote backend additive/opt-in same-core, DN-4 consent-gated egress, DN-5 backend-as-infrastructure-extension, DN-6 tenancy optional overlay, DN-7 data portable across backends no lock-in, DN-8 backend never widens the agent's authority). L1 stays Stable (C9); l2-nodus-portability carries LP-15 as a pending Invariant-Compliance obligation reconciled at magic.task. Prior: adoption pass over an external agent-to-agent network reference — one additive nodus refinement paired with a new main concept, no cascade: l1-nodus-portability v1.6.0→1.7.0 (LP-14 verified-peer delegation seam — a deferred/external step NL-12 MAY target a named external peer agent whose verifiable identity + offered capability a host-supplied peer directory resolves before the delegation, the runtime guaranteeing resolve+authorize before the effect via the LP-11 gate (decide → delegate → observe), the peer result entering as untrusted provenance NL-11 and optionally carrying a host-supplied execution receipt HO-9; unresolvable/unauthorized peer rejected fail-fast pre-delegation LP-8 kinship; peer identity/directory/transport/encryption/settlement entirely host-supplied LP-1/LP-2 so a workflow delegating to no peer is unaffected additive; generalizes NL-12's external actor from an anonymous completion to a verifiable named peer; new §4.10 reusing NL-12+LP-11+NL-11+HO-9 with no new trust primitive in the core; the nodus realization of the new main l1-agent-federation AF-1/AF-6/AF-7/AF-8). Main deliverable of the same pass: new l1-agent-federation v1.0.0 Stable (interoperation with independent external agents as verifiable peers, crypto-agnostic: sovereign self-held credential = identity+auth+authz AF-1, human-rooted never self-minted AF-2, signed advertisement + open discovery AF-3, authenticated-confidential peer channels AF-4, coordinate-without-controlling relay neutrality AF-5, default-deny peer trust AF-6, accountable verifiable append-only exchange AF-7, auditable attribution to the principal AF-8). L1 stays Stable (C9); l2-nodus-portability carries LP-14 as a pending Invariant-Compliance obligation reconciled at magic.task. Prior: adoption pass over an external agent-skills reference — one additive nodus refinement paired with a new main concept, no cascade: l1-nodus-language v1.6.0→1.7.0 (NL-16 selective vocabulary disclosure — a workflow declares via `@needs` the specific schema/vocabulary units it uses and the runtime loads only those into the validation/execution context, the rest of the host vocabulary staying deferred; `§meta`+`!!` always load as the safety floor, optional command groups disclosed on demand; using an undeclared unit is a fail-fast validation error (declare what you disclose, LP-8 kinship); deterministic (NL-6) and cache-stable (NL-15); nodus defines only the declare-and-load-the-slice discipline, host schema units/storage host-supplied LP-4/LP-2; elevates `@needs` from a §4.6 note to a first-class contract; the nodus realization of the new main l1-progressive-disclosure minimal-load contract PD-1/PD-2/PD-5). Main deliverable of the same pass: new l1-progressive-disclosure v1.0.0 Stable (tiered on-demand context loading — the lazy-loading member of the context-economy family orthogonal to compression: minimal always-resident descriptor PD-1, on-demand body expansion PD-2, lazy deeper resources PD-3, descriptor-as-routing-contract PD-4, selective loading bounds context rot PD-5, lossless reversible deferral with collapse-back PD-6, addressable cache-friendly tiers with descriptor index in the frozen prefix PD-7, honest expanded-vs-deferred state PD-8). L1 stays Stable (C9); l2-nodus-runtime carries NL-16 as a pending Invariant-Compliance obligation reconciled at magic.task. Prior: adoption pass over an external local-inference-server reference — one additive nodus refinement paired with a new main concept, no cascade: l1-nodus-language v1.5.0→1.6.0 (NL-15 cache-stable prompt composition — nodus renders a model-facing GEN/REFINE prompt so its reusable segments (system preamble, loaded §schema/vocabulary, macro definitions, loop-invariant +grade rubric) form a byte-stable leading prefix emitted first in deterministic stable order, with per-invocation volatile content confined to the suffix; because the prefix stays byte-identical across `~UNTIL +grade`/`~RETRY` iterations and across steps that share it, a host prefix-addressed cache reuses the ingested prefix state instead of recomputing; nodus defines only the ordering discipline + prefix/volatile boundary, the cache entirely host-supplied LP-2 so a host with no cache is unaffected additive; composes NL-6 determinism + NL-11 provenance, surfaced via HO-8 cache_read/cache_creation; the nodus realization of the new main l1-inference-cache prefix-addressed reuse IC-1/IC-9 and the workflow-side realization of l1-cache-stable-context's frozen-prefix discipline). Main deliverable of the same pass: new l1-inference-cache v1.0.0 Stable (durable tiered prefix-addressed inference-state cache the local runtime owns — the storage-side companion to cache-stable authoring and the delta beyond l1-model-runtime: prefix-addressed reuse IC-1, hit-equals-recompute IC-2, tiered residency with write-back IC-3, warmth survives restart IC-4, copy-on-write prefix sharing IC-5, bounded admit-by-evict eviction IC-6, honest observability IC-7, on-device confinement + auxiliary-lane isolation IC-8, reuse-requires-a-stable-prefix IC-9). L1 stays Stable (C9); l2-nodus-runtime carries NL-15 as a pending Invariant-Compliance obligation reconciled at magic.task. Prior: adoption pass over an external plugin-marketplace / extension-directory reference — one additive nodus refinement paired with a new main concept, no cascade: l1-nodus-portability v1.5.0→1.6.0 (LP-13 addressable versioned import resolution — an imported workflow bundle or host schema/vocabulary extension MAY be referenced by a stable name + version constraint that a host-supplied resolver resolves to a concrete pinned revision before the load gate, deterministic given a recorded lockfile-like pin, the resolved pin being the identity LP-9 attests and LP-12 vets — full order resolve → attest → vet → run; catalog/source/transport/version-policy entirely host-supplied LP-2 so a host with no resolver runs as today additive; unresolvable/ambiguous/unpinned reference rejected fail-fast pre-run LP-8 kinship; stable identity slug so a pinned reference cannot be silently swapped for a different bundle; extends the LP-8 manifest with named external bundle dependencies; new §4.9; the pre-load resolution stage preceding the LP-9/LP-12 load gate). Main deliverable of the same pass: new l1-extension-marketplace v1.0.0 Stable (extension marketplace & distribution — catalog of addressable sourced entries XM-1, stable immutable identity + governed rename XM-2, reproducible pinning XM-3, honest trust tiers curation-is-a-signal-not-a-guarantee XM-4, publishing gate over the whole shipped payload XM-5, structured truthful discovery XM-6, bundle-or-curate-subset entry XM-7, distribution-is-a-source-not-a-trust-bypass still default-deny+vetted+attested XM-8, explicit legible sync no silent drift XM-9). L1 stays Stable (C9); l2-nodus-portability carries LP-13 as a pending Invariant-Compliance obligation reconciled at magic.task. Prior: adoption pass over an external agent-recipe collection reference — one additive nodus refinement paired with a new main concept, no cascade: l1-nodus-language v1.4.0→1.5.0 (NL-14 grader-gated iterative refinement — a `~UNTIL` loop MAY declare a `+grade` discipline whose continuation is decided by a host-supplied grader verdict over the produced artifact (latest GEN/REFINE output) instead of a bare boolean; a *continue* verdict threads structured feedback into a reserved loop variable the next REFINE reads so each cycle targets the unmet criteria; the rubric/criteria are immutable for the loop's lifetime — the workflow revises the artifact but never rewrites the criteria that judge it (LP-10, no goalpost drift); bounded by MAX:n (NL-5) terminating on pass or exhaustion (exhaustion binds best-so-far and routes named unmet criteria to `@err:` typed, never open-ended); grader a function-scoped host-supplied auxiliary role (LP-2, no rubric vocabulary or metric in core) declared as an LP-8 capability when it needs an external judge; determinism preserved when the grader is (NL-6); a plain boolean `~UNTIL` unchanged and `+grade` opt-in (additive); §4.4 `~UNTIL +grade` note; the sequential-depth complement to NL-13's competitive width). Main deliverable of the same pass: new l1-iterative-refinement v1.0.0 Stable (generate–evaluate–refine — the temporal coordination-family member trading depth for quality beside the three width fan-outs: one artifact improved in place IR-1, producer/grader independence IR-2, rubric-driven actionable feedback IR-3, grounded verification not plausibility IR-4, bounded convergence with an honest best-so-far exit IR-5, frozen criteria / no goalpost drift IR-6, observable attributed refinement trace IR-7, uncertainty-justified not reflexive IR-8). L1 stays Stable (C9); l2-nodus-runtime carries NL-14 as a pending Invariant-Compliance obligation reconciled at magic.task (NL-11/NL-12/NL-13 precedent). Prior (same day): adoption pass over an external agent-component supply-chain security-scanner reference — l1-nodus-portability v1.4.0→1.5.0 (LP-12 imported-bundle admission vetting seam — before loading an imported workflow bundle or a host schema/vocabulary extension the runtime MAY submit the artifact to a host-supplied vetting classifier returning typed content findings and enforce the host's admission disposition fail-closed on an admission-blocking finding else pass-through with the verdict recorded as load-time provenance; content-safety distinct from and sequenced after LP-9 authenticity, load-gate order attest → vet → run; classifier/heuristics/taxonomy host-supplied (LP-2) so a host with no classifier loads as today (additive); NL-11 provenance + LP-11 effect classes are the static substrate a host reads for a cross-effect toxic-flow finding; new §4.8; the pre-load static complement to the per-effect runtime gate LP-11; the nodus realization of the new main l1-component-scanning admission-vetting contract). L1 stays Stable (C9); l2-nodus-portability carries LP-12 as a pending Invariant-Compliance obligation reconciled at magic.task. Prior: 2026-07-07 (one additive nodus refinement paired with a new main capability spec, no cascade: l1-nodus-observability v1.5.0→1.6.0 (HO-12 execution-mode provenance — the RunManifest declares the run's execution mode, real vs simulated, plus its fidelity when simulated, so a downstream consumer can exclude simulated runs from real-run analytics (host health scoring, HO-8 cost/usage accounting, the outer evolution loop) and never mistake a modeled play-out for production behaviour; a simulated run is one where the host has substituted modeled providers (a mock ModelProvider, a no-effect tool/host surface) to exercise the workflow's mechanics without real effects, so nodus itself mocks nothing — host-neutral LP-1/LP-2, the substitution host-supplied — and only records which mode the host declared, making the trace self-describing exactly as HO-10 lets a consumer distinguish a complete trace from a truncated one; optional and additive, a manifest without the marker is real (today's behaviour), so observer neutrality (HO-5) and the closed taxonomy (HO-6) are preserved; §4.9 Execution-Mode Provenance added). Main-workspace deliverable of the same pass: new l1-simulation v1.0.0 Stable (simulation as a first-class capability to play out a generated mechanism — workflow, automation pipeline, role interaction, task graph, schedule — in a modeled side-effect-suppressed observable repeatable run, distinct from testing/evaluation/lookahead and unifying the project's scattered dry-run realizations: play-out not assertion (SIM-1); no production side effects, the modeled world generalizing the automation dry-run §4.9 + evaluation ES-11 quarantine (SIM-2); declared legible fidelity structural/modeled/shadow (SIM-3); deterministic & repeatable via seeding (SIM-4); mechanism-addressable, simulates what was generated (SIM-5); observable play-out trace as the product, composing l1-log-legibility (SIM-6); distinct from evaluation grades and lookahead predicts-to-gate, the four-questions taxonomy (SIM-7); bounded & safe by default (SIM-8)). L1 stays Stable (C9); l2-nodus-observability carries HO-12 as a pending Invariant-Compliance obligation reconciled at magic.task (HO-8/HO-9/HO-10/HO-11 precedent). Prior: adoption pass over a set of external agent-runtime references, logging focus — one nodus refinement, additive: l1-nodus-observability v1.4.0→1.5.0 (HO-11 single-stream dual legibility — an ExecutionEvent MAY carry an optional host-rendered human-legible one-line message derived from its own structured fields, so one AuditProvider stream serves both a human reader and a machine/AI analyzer without a second parallel prose log to keep in sync or let drift; the structured fields remain the source of truth — the human rendering introduces no fact absent from the fields and contradicts none (a faithful projection, not a new record) — and it stays within the §4.4 data-safety boundary rendering descriptors and counts never raw content; the rendering and any localization are host-supplied (LP-2 style, the nodus core naming no format or locale vocabulary) so a host that supplies no renderer emits events exactly as today, making the change optional and additive with observer neutrality (HO-5) and the closed taxonomy (HO-6, an optional field on existing events like HO-8/HO-9) preserved; the broader legibility & economy controls that bound a stream — payload policy, compaction/folding, a budget-bounded feed of the trace back into an agent's own context (LL-4/LL-8) — stay host-side (LP-1/LP-2), nodus contributing the source-side pieces they build on: the closed structured taxonomy (HO-6), counts-not-content descriptors (§4.4), and the dense correlation/sequence (HO-7); §4.8 Single-Stream Dual Legibility added). Main-workspace deliverable of the same pass: new l1-log-legibility v1.0.0 Stable (dual-audience log legibility & economy — one log stream ideal for a human AND an AI reader without overloading either: one canonical event faithfully projected to human/machine/stream/agent surfaces with no divergence (LL-1); human message + machine typed fields both first-class on one record (LL-2); consumer-shaped projections over a complete canonical event (LL-3); overload bounded by construction — severity/verbosity gating × payload policy × compaction, conservative defaults, full fidelity opt-in (LL-4); honest reduction, every truncation/fold/omission/sample flagged with magnitude so a reduced record never masquerades as complete (LL-5); stable attribution for filterability (LL-6); secret-safety re-asserted at every projection, sources denylistable (LL-7); AI-consumer context economy — compact budget-bounded feed never the raw firehose, reading own logs must not evict working context (LL-8); stable self-describing versioned namespaced schema with a migration path (LL-9); the legibility/economy contract over every observation channel composing l1-security + l1-context-provenance + l1-inner-monologue + l1-cache-stable-context). L1 stays Stable (C9); l2-nodus-observability carries HO-11 as a pending Invariant-Compliance obligation reconciled at magic.task (HO-8/HO-9/HO-10 precedent). Prior: adoption pass over an external agent-runtime reference — one nodus refinement, additive: l1-nodus-observability v1.3.0→1.4.0 (HO-10 trace-completeness honesty — a persisted trace is either complete (it carries a terminal RunManifest delivered by run_complete, status Ok/Error/ConstraintHalt/ValidationError) or explicitly truncated (it has events but no terminal manifest), and a consumer can tell which from the trace alone; if the host process is killed, panics, or is OOM-killed mid-run then run_complete is never reached and the persisted trace is a headless fragment — events up to some seq with no manifest — which MUST NOT be mistaken for a completed run nor for a still-running one by any downstream consumer (the outer evolution loop, health scoring, differential replay); this extends the honesty HO-7 already gives for dropped events inside the recorded range (a gap in the dense gap-free seq) to the truncated tail (missing records after the last recorded event), so every trace classifies as complete, gap-damaged, or truncated; crucially nodus does NOT attempt to capture the crash that truncated the trace — that is the host forensic diagnostic-log plane (the nodus realization of the new main l1-diagnostic-log concept, whose DL-2 owns native-fault and pre-init capture with a sink installed below and earlier than the executor and surviving a native fault the executor cannot) — HO-10 guarantees only that the semantic trace never lies about being complete when it was truncated, naming the boundary between what nodus witnessed and what only the host lower plane could, the observability analog of the INT-8 honest-coverage-boundary discipline; it is a read-side interpretation of manifest presence/absence that adds no new hot-path emission and leaves a normally-completing run entirely unaffected, so HO-5 observer-neutrality is preserved and the change is purely additive; §4.7 Trace Completeness & Abnormal Termination added). Main-workspace deliverable of the same pass: new l1-diagnostic-log v1.0.0 Stable (the forensic diagnostic-log plane of last resort — the raw local developer/operator record of what the process actually emitted, captured low enough to survive failures the higher observation planes structurally cannot witness: a distinct non-conflated channel separate from the semantic run-trace / execution-authenticity audit / aggregate health / live process view / outward telemetry, each with its own consumer+retention+egress and never folded together (DL-1); survives what the semantic planes cannot — a native crash/fault, output emitted before any structured-observation subsystem is initialized, and unstructured dependency/subprocess output, its sink installed at the earliest point of process life so a session dying before or without ever reaching structured-event emission still leaves a diagnosable record (DL-2, the defining invariant); self-describing session header for offline diagnosis (DL-3); own-scope fidelity with dependency noise clamped to a higher severity floor (DL-4); bounded ephemeral rotating retention, not a durable ledger (DL-5); local-first consent-gated secret-safe egress — never auto/silent, only via a user-previewed issue-reporting bundle ISS-4 or telemetry opt-in after the error-reporting scrub (DL-6); live in-product inspection at frontend parity, filter/search/copy/tail without external tools (DL-7, INV-3); resilient neutral sink that degrades gracefully and never crashes/blocks/perturbs the observed process (DL-8, OH-1/PM-1 posture)). L1 stays Stable (C9); l2-nodus-observability carries HO-10 as a pending Invariant-Compliance obligation reconciled at magic.task (HO-8/HO-9 precedent). Prior: adoption pass over an external agent-SDK reference — one nodus refinement, additive: l1-nodus-portability v1.3.0→1.4.0 (LP-11 per-effect authorization seam — an effectful step (a model call GEN/REFINE, a host tool/operation, or a deferred/external NL-12 completion) MAY be gated by a host-supplied authorization decision evaluated before the effect, the runtime guaranteeing the ordering decide → effect → observe: the authorization runs before the effect is performed and the AuditProvider observer (HO-*) runs after, against the real result; the gate is fail-closed — a deny, or an authorization that errors or times out, aborts the effect with a typed NODUS:* code and never silently permits nor hangs; the decision is host-supplied through the now-concretized PolicyProvider role (LP-2 — the nodus core names no policy vocabulary and no metric), a step declaring only its effect class (model_call / tool_use / deferred) so the host gate can match and, consistent with LP-10, never authoring nor relaxing the policy; the dynamic per-effect complement to the static whole-run LP-8 manifest — LP-8 rejects before the run an effect the host cannot satisfy (strip / unavailable), LP-11 evaluates per attempt an effect the host can satisfy but may refuse on its arguments (deny); a step with no matching policy proceeds unchanged and a host supplying no PolicyProvider runs exactly as today (additive); §4.1 PolicyProvider row concretized — the TBD resolved — and new §4.7 per-effect authorization seam; the nodus realization of the new main l1-interception-model decide-class seam INT-1, its check-before-use ordering INT-2, its fail-closed guard direction INT-3, and its strip-vs-deny axis INT-6 with LP-8 = strip / LP-11 = deny). Main-workspace deliverable of the same pass: new l1-interception-model v1.0.0 Stable (the capability-bounded interception discipline for how cross-cutting behaviour — permission decisions, provenance neutralization, execution receipts, telemetry — attaches to the agent lifecycle: a three-kind interceptor taxonomy observe/decide/transform enforced by type so the maximum authority is legible from the kind and an observer structurally cannot become a gate or a rewrite path INT-1; check-before-use TOCTOU-safe ordering decide→effect→observe so a check is never recorded as a use and an observation always reflects the completed effect INT-2; fail-direction fixed per class — decide/transform guards fail closed, observe fails forward INT-3; nested session⊃turn⊃operation interception state with one-way visibility isolated from the guarded effect's own state INT-4; transitive guard enforcement across delegation so a sub-agent cannot escape its parent's guard envelope by performing the effect itself, enforced by the mechanism not by prompt re-injection INT-5; strip-vs-deny model-visibility restriction axis INT-6; deterministic specificity×safety priority resolution with construction-time validation INT-7; honest coverage boundary naming unguarded entry paths e.g. trigger/background-initiated turns INT-8; unifies the currently-divergent fail-behaviours of the plugin-hook fail-forward and tool-security fail-open-guardrail realizations under one L1 contract, composing l1-security SEC-10 / l1-context-provenance CP-2 / l1-tool-receipts TR-8 / l1-policy-governance PG-4 / l1-orchestration ORC-12). L1 stays Stable (C9); l2-nodus-portability carries LP-11 as a pending Invariant-Compliance obligation reconciled at magic.task (LP-8/LP-9/LP-10 precedent). Prior: adoption pass over an external desktop multi-agent-orchestrator reference — one nodus refinement, additive: l1-nodus-language v1.3.0→1.4.0 (NL-13 competitive parallel selection — a `~PARALLEL` block MAY declare a `+select` discipline (`first-success` / `best`) that binds exactly one winning branch result via `~PICK` instead of `~JOIN`'s integrate-all; default `~PARALLEL`+`~JOIN` collects every branch and is all-must-succeed fail-fast, whereas a competitive block runs N branches over the same input and binds the single result chosen by a declared selector — `first-success` (first successful branch wins, rest cancelled) or `best` (a host-supplied scorer/judge ranks completed results, highest wins) — with non-winning branches cancelled and surfaced in the trace as superseded, never silently dropped and never merged into the bound target (one whole winner, not a blend); the ranking policy is host-supplied LP-2-style so the core names no metric or judge, and a `best` selector that needs an external scorer/judge declares it as a host capability LP-8 and is rejected fail-fast when unsatisfied; determinism is preserved because `~PARALLEL` is a scheduling hint a runtime MAY execute sequentially §2 — `first-success` resolves to declaration order under sequential execution and `best` is deterministic whenever the host selector is, NL-6; fail-semantics — `first-success` fails only when all branches fail, `best` requires ≥1 successful branch, an all-branches-fail competitive block routes to `@err:` with a typed `NODUS:*` code exactly like the fail-fast `~JOIN` path and `!!` violations still bypass `@err:` per NL-2; a selection discipline layered on `~PARALLEL` not a new command, §4.4 `~PICK` note added; the nodus realization of the new main l1-competitive-execution best-of-N contract CE-2/CE-4/CE-5, with `~JOIN` integrate-all remaining the parallel-staffing analog). Main-workspace deliverable of the same pass: new l1-competitive-execution v1.0.0 Stable (best-of-N quality-selection fan-out — the third coordination-family mode beside parallel-staffing (throughput / integrate-all) and deliberation (synthesis / blend-all): when a work unit's outcome quality is genuinely uncertain and budget allows, the office runs N independent complete attempts at the same whole unit, each isolated in its own context / scratch worktree CE-3 (deliberately inverting parallel-staffing PS-2's disjoint-partition rule — safe because rival attempts never co-write mainline and only one survives), an independent judge selects exactly one whole winner by a declared criterion gate-objective/scored/judged CE-4 (not a vote, not a blend), the losing attempts are discarded never merged CE-5 (salvaging a loser's idea is a separate recorded act), and the winner is integrated + gated as one whole CE-6 (a judge pick is a ranking not completion); uncertainty-justified & recorded CE-1, bounded width & honest afford / no nested tournaments CE-7, contained failure & honest all-fail CE-8, observable & attributed contest incl. the discard CE-9 — composing ORC-5/ORC-6/ORC-7/ORC-11/ORC-12 + change-merge + quality-gates + work-liveness claim/reap). L1 stays Stable (C9); l2-nodus-runtime carries NL-13 as a pending Invariant-Compliance obligation reconciled at magic.task (NL-11/NL-12 precedent). Prior: adoption pass over an external Rust agent-runtime reference — one nodus refinement, additive: l1-nodus-observability v1.2.0→1.3.0 (HO-9 execution-authenticity receipt — a step's execution event MAY carry a host-supplied, model-unforgeable receipt binding the step's identity and its observed result, so a downstream verifier can tell a genuine step result from a fabricated one and a narration cannot claim a step ran that did not; the signing/verification mechanism is host-supplied LP-2-style with no cryptographic dependency in the nodus core, mirroring the LP-9 attestation seam; the receipt is an opaque secret-free token crossing the data-safety boundary §4.4 like any counts-only descriptor — the signing secret is never placed in a trace, prompt, or the model's context; receipts are optional + additive so a host supplying no receipt provider emits events unchanged and HO-5 observer neutrality is preserved, and like HO-8 this extends existing event records with an optional field rather than adding an event type HO-6; the nodus realization of the new main l1-tool-receipts execution-authenticity contract — the workflow-side witness that a step's result is real, host-supplied exactly as the LP-9 attestation witness is). Main-workspace deliverable of the same pass: new l1-tool-receipts v1.0.0 Stable (tool-call execution authenticity via model-unforgeable per-action receipts TR-1…TR-9 — the execution-side anti-fabrication sibling of l1-claim-verification (output grounding) and l1-attestation (artifact integrity), closing the LLM-as-string-generator fabricated-action/fabricated-result gap). L1 stays Stable (C9); l2-nodus-observability carries HO-9 as a pending Invariant-Compliance obligation reconciled at magic.task. Prior: adoption pass over an external personal-assistant runtime reference — one nodus refinement, additive: l1-nodus-portability v1.2.0→1.3.0 (LP-10 host-granted authority, never self-authored — a workflow has no ambient authority: it declares the capabilities it needs (the LP-8 manifest) and reads host-supplied policy decisions (the PolicyProvider role) but MUST NOT grant itself a capability, relax a policy, widen its reach, or otherwise author the authority under which it runs — the authority plane is host-supplied and workflow-read-only; the language gives no vocabulary to write policy, a step may request an outcome (a dialog/approval, NL-12) but never self-authorize one and the request is data not a grant; because a workflow may be assembled under untrusted influence (a definition built after reading model output or external content is itself untrusted, NL-11) its declared-capability manifest is the single consent point the host resolves before the run, so an injected instruction inside a workflow cannot escalate what the run may do beyond its pre-consented manifest; granting stays entirely host-side LP-1/LP-2; the nodus realization of the new main l1-security SEC-10 authority-self-containment contract — a portable workflow, like the agent that runs it, can ask for authority but never mint it). Main-workspace deliverable of the same pass: l1-security v1.2.0→1.3.0 (SEC-10 authority self-containment — the plane governing what the agent may do and who may reach it is human-principal-write-only through a boundary the agent's execution cannot write to; agent reads its constraints but only requests changes through the SEC-9 human approval path; structurally-enforced security confinement, INV-8-sanctioned; counterpart to SEC-9 — human may grant, agent never grants). L1 stays Stable (C9); l2-nodus-runtime carries LP-10 as a pending Invariant-Compliance obligation reconciled at magic.task. Prior: adoption pass over an external production multi-agent application framework reference — one nodus refinement, additive: l1-nodus-language v1.2.0→1.3.0 (NL-12 deferred / external step execution — a step MAY suspend the run pending an external completion (a backgrounded tool result, a delegated/external execution, a human-performed action) instead of completing synchronously, reusing the existing `Status::Paused` + resume-descriptor lifecycle that `ASK`/`CONFIRM`/`!PAUSE` use (DG-4) but generalizing the resume trigger from a human answer to any host-supplied completion correlated to the suspended step by a token; the run is never busy-blocked — it yields `Status::Paused` with a correlation token, the host performs/awaits the operation and resumes by supplying the correlated completion, resume is deterministic and a stray/consumed-token completion is rejected; nodus defines only the suspension + correlation + determinism contract while whether the operation backgrounds/delegates/persists is host-supplied LP-1/LP-2 — the language names no scheduler/thread/transport — and a deferred step declares its need as an LP-8 host capability so an unsatisfiable host is rejected fail-fast, a step neither synchronous nor capability-backed failing fast with a `NODUS:*` error rather than hanging; the nodus realization of the new main l1-execution-graph EG-12 deferred-execution contract, where a human dialog (l1-nodus-dialog) is the special case in which the external actor is a person). Main-workspace deliverable of the same pass: l1-execution-graph v1.1.0→1.2.0 (EG-12 deferred asynchronous execution with correlated resumption — non-blocking per-operation deferral, launch→pending-token→typed-correlated-completion→staged-resume, unifying the EG-7 synchronous whole-graph interrupt as the human-decision special case with background/external/client-side execution). L1 stays Stable (C9); l2-nodus-runtime carries NL-12 as a pending Invariant-Compliance obligation reconciled at magic.task (NL-11 precedent). Prior: adoption pass over an external agent–human task-collaboration platform reference — one nodus refinement, additive: l1-nodus-dialog v1.1.0→1.2.0 (DG-9 memoizable approval — a `+remember`-marked `ASK`/`CONFIRM` MAY resolve from a durable prior host decision keyed by a stable dialog identity without re-prompting; the durable-decision store, its scope, and revocation are host concerns LP-1 reached through the same `DialogProvider` seam that already answers dialogs LP-2, so nodus stays host-neutral and contributes only the `+remember` eligibility marker plus a remembered-vs-answered trace provenance under the DG-7 data-safety boundary; disciplined — a non-`+remember` dialog always prompts, memoization never turns reject→approve nor relaxes `+strict` nor binds a value failing `+type`/`+validate`, and a host may decline to memoize in which case the dialog prompts normally fail-safe-to-asking; the host-neutral nodus realization of the new main l1-security SEC-9 learnable/scoped/revocable/governed permission promotion). Main-workspace deliverable of the same pass: l1-security v1.1.0→1.2.0 (SEC-9 — an explicit human "always-allow" MAY become a durable allow-rule at an explicit minimal scope keyed to a stable action identity, auditable + independently revocable, governed escape hatch, destructive/always-forbidden non-promotable fail-closed) + l2-agent-autonomy v1.0.1→1.1.0 (durable `AllowRule` store realization — "Allow always (scope)", scope ladder, revocable, governance-clamped, rules list/revoke). L1 stays Stable (C9). Prior: adoption pass over an external meta-harness outer-loop-optimizer reference — one nodus refinement, additive: l1-nodus-environment v1.1.0→1.2.0 (NE-12 archivable candidate result — a completed graded run yields a content-addressable (workflow-hash, reward, trajectory) tuple a host outer-loop optimizer can archive, compare, and place on a frontier; nodus supplies only the archivable substrate per NE-2/NE-3/NE-4, while the candidate space, mutation, search strategy, and frontier stay host-side and host-neutral LP-1/LP-2 — a host optimizes nodus workflows as candidates with no nodus-side coupling to a search policy; the nodus feed for the new main l1-harness-optimization concept). Main-workspace deliverable of the same pass: new l1-harness-optimization (outer-loop search over a harness-candidate space toward a frontier). Prior: adoption pass over an external agent-harness-generator reference — one nodus refinement, additive: l1-nodus-portability v1.1.0→1.2.0 (LP-9 extraction attestation — the extraction bundle + capability manifest carry an independently-verifiable witness binding exact content-set + version; a host verifies before loading an imported workflow bundle and refuses fail-closed on missing/failed witness; signing/verification mechanism host-supplied LP-2-style so the library core carries no crypto dependency and stays host-neutral; the nodus realization of the new main l1-attestation contract AT-1/AT-2/AT-6 — a library moving between hosts LP-3 proves integrity+authorship on arrival). Main-workspace deliverable of the same pass: new l1-attestation (verifiable signed-witness attestation of artifacts) + l1-extensions EXT-11 (verifiable import attestation). Prior: adoption pass over an external LLM-orchestration framework reference — one nodus refinement, additive: l1-nodus-language v1.1.0→1.2.0 (NL-11 provenance-safe interpolation — a value interpolated into a model-facing GEN/REFINE prompt carries trusted/untrusted provenance, unknown=untrusted, untrusted neutralized-as-data by default at render with raw insertion requiring an explicit modifier, sticky provenance, neutralization mechanism host-supplied LP-2-style so the core stays host-neutral; the nodus realization of the new main l1-context-provenance contract CP-2/CP-3/CP-4). L1 stays Stable (C9); l2-nodus-runtime carries NL-11 as a pending Invariant-Compliance obligation reconciled at magic.task (RTG-9 precedent). Main-workspace deliverable of the same pass: new l1-context-provenance (trusted composition / prompt-injection structural defense). Prior: adoption pass over an external model×harness agent-benchmark reference — one nodus refinement, additive, no cascade: l1-nodus-environment v1.0.0→1.1.0 (NE-11 declared grading mode — closed automated/judge/hybrid set with hybrid-floor semantics: the deterministic checker runs first and a judge may only lower not rescue a failed run; judge-mode via a function-scoped auxiliary binding under host judge-trust; refines NE-9 without weakening it — plus §4.7 sliceable orthogonal task labels on the profile, the nodus-side feed for the host agent co-evaluation methodology where one graded run = one matrix cell and labels = slice dimensions). Main-workspace deliverable of the same pass: new l1-agent-coevaluation (Performance=f(Model,Harness) diagnostic matrix) + l1-evaluation-suites ES-17 slice reporting. Prior: adoption pass over an external context-optimization reference — two nodus refinements, both additive, no cascade: l1-nodus-observability v1.1.0→1.2.0 (HO-8 cost-attribution token classes — fresh input/output plus optional cache_read/cache_creation as distinct counts-only fields on model_response, so per-run/per-step cost and cache-warmth regressions are computable from the trace not the invoice); l1-nodus-testing v1.0.1→1.1.0 (NT-11 differential parity across execution paths + §4.7 harness — interpreter↔transpiler or two conforming hosts must produce equal observable output + trace shape on a content-addressed fixture corpus, the executable form of portability LP-3, complementary to @test: correctness assertions). Main-workspace deliverable of the same pass: new l1-cache-stable-context (prompt-cache stability discipline) + refinements to l1-context-compression/l1-routing. Prior: l1-nodus-environment v1.0.0 Stable authored — Environment & Evaluation contract: EnvironmentProvider as a 5th extension role, closed reset/step/evaluate lifecycle, trajectory as observability projection, frozen-evaluation boundary, typed reward-is-data, addressable task catalog+profile, instance isolation, function-scoped auxiliary roles, host-supplied metric neutrality, capability-manifest ExtensionRole::Environment fail-fast; NE-1…NE-10; the executable substrate for evaluation-driven harness improvement. l1-nodus-observability v1.0.0→1.1.0 — added HO-7 (monotonic sequence + run-correlation ordering) + §4.6 (sequence/correlation fields, run-scoped correlation binding, streaming chunk-merge into one logical model_response); underpins the trajectory projection. Prior: l2-nodus-control-flow v1.0.0 Stable authored — Rust realization of v0.7 control constructs: ?SWITCH/~MAP/~RETRY/!HALT/!PAUSE; lexer tokens + SwitchBlock/MapBlock AST + action flags + retry; reuses Status::Paused/Signal::Pause + SWITCH_NO_MATCH/PAUSED; phased implementation recommended. Prior: l2-nodus-dialog v1.0.0 Stable authored — Rust realization of the dialog contract: ASK/CONFIRM vocabulary, Status::Paused + ResumeDescriptor, DialogProvider + DefaultDialogProvider, ExtensionRole::Dialog manifest binding, run_with_dialog combinators; DG-1…DG-8. Prior: l1-nodus-dialog v1.0.0→1.1.0 Draft → Stable — resolved both TBDs: paused-run state = Status::Paused + host-persisted resume descriptor (built-in host synchronous); dialog backend as distinct ExtensionRole::Dialog. Prior: l2-nodus-registries v1.0.0 Stable authored — closed vocabulary registries: KNOWN_FLAGS (12) + KNOWN_VALIDATORS (12) + PRIMITIVE_TYPES (10) as vocab data + Schema query surface + advisory validator diagnostics; strengthens NL-1/NL-7/NL-9; contents per l2-nodus-runtime §4.7(f). Prior: l2-nodus-errors v1.0.0 Stable authored — Rust realization of the 24-code error taxonomy from l1-nodus-language §4.6: ErrorSeverity/ErrorCategory types, per-code severity×category registry, error_meta() lookup, EXECUTION_FAILED supersede + site reassignment, validator/executor emission map; foundational dependency for control-flow/dialog/operators clusters. Prior: l1-nodus-dialog v1.0.0 Draft authored — human-in-the-loop dialog contract: ASK/CONFIRM command class, suspend/resume lifecycle (Status::Paused), DG-1…DG-8, dialog backend as LP-2 extension point + LP-8 role, dialog error subset; elevates l1-nodus-language §4.6 HITL gap; Backlog pending design review of TBDs. Prior: l2-nodus-portability v1.0.0→1.1.0 — LP-8 capability manifest implemented in `crates/nodus`: §4.7 `CapabilityManifest`/`ExtensionRole`/`HostCapabilities`/`Missing` + pure `validate_manifest` resolver + `run_with_manifest`/`run_with_manifest_and_audit` fail-fast gate before executor boot + `NODUS:CAPABILITY_UNMET` + AST-derived `from_workflow`; 13 new tests; status RFC → Stable. Prior: l1-nodus-portability v1.1.0 — LP-8 contract authored; l1-nodus-language v1.1.0 + l2-nodus-runtime v1.1.0 — upstream schema v0.4.6 → v0.7 parity gap recorded)
