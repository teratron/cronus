# Project State

<!-- STATE.md — live project memory. Read FIRST in every workflow session. -->
<!-- Maximum 100 lines. Agent updates AFTER each completed action. -->

**Workspace:** nodus
**Updated:** 2026-09-01 17:32
**Phase:** none open — Plan Complete
**Status:** Active

## Current Position

- **Task:** `/magic.task nodus` — **sync-only replan; no new phase.** Closed the expected `SYNC_GAP` (PLAN basis v1.0.92 → v1.0.93, from Phase 30's own T-30C01 spec reconciliation). Pre-flight showed that single warning plus the standing `DESIGN_DEBT_PENDING` (Backlog: 1 open item, unchanged since v1.41.0). Pre-Planning Stabilization: 0 promoted (nothing to promote — 20 Stable, 0 Draft). No new phase opened: the sole Backlog entry (LP-21/22/23, NL-24/25/26, restated at v1.41.0) is unaffected by Phase 30's implementation and remains blocked on the same `ResumeDescriptor` question and LP-6/carrier-fork/step-substitution gaps.
- **Spec:** INDEX v1.0.93 / PLAN v1.42.0 / TASKS v2.35.0 — all three in sync; pre-flight clean except the standing `DESIGN_DEBT_PENDING`. RULES v1.6.0 (unchanged, confirmed). 20 nodus specs Stable, 0 Draft, Phase 30 `Done (Archived)`, `tasks/` empty.
- **Next Action:** Plan complete — run /magic.task nodus to plan new scope

## Progress

```
Overall: [30/30] ████████ 100%
Build phases 1–30 Done (… → Effect Risk-Class Declaration → Uncaught-Error Handler Dispatch → Settlement Effect Seam → Memoizable & Promotable Approval → Declared Budget Measure → DG-11 Authoring Advisories); all 7 v1.0.86–v1.0.91 invariants now have an L2 record (LP-21/22/23 §3.2, NL-24/25/26 §3.1, DG-11 §3+§4.8); DG-11 implemented (W016/W017) | Baseline gates: cargo 482 tests + clippy + fmt; LP-1 zero-dep preserved
```

## Recent Decisions

<!-- Last 3-5 locked decisions. Older entries are dropped (not archived) — see PLAN.md / CHANGELOG.md for phase history. -->

- 2026-09-01 **Decision:** Phase 30 complete — DG-11 authoring advisories implemented exactly per `l2-nodus-dialog.md` §4.8's design, with two grounding corrections found at implementation time and folded back into the spec (T-30C01) rather than left for a later audit: `+reversible`/`+external` are matched with their surface `+` prefix in `CommandCall.modifiers` (the plan's assumed bare-name key came from reading the *derived* LP-11 `context` map in a test, not the raw AST field); and `W017`'s producer lookup is a dedicated single-pass walker (`w017_collect_producers`) rather than a reuse of `collect_vars_stmt`, which tracks declared/used variable-root sets only and has no producer-identity concept. A third plan-time claim — that `W016` should recurse into `?SWITCH` arms/default and `~PARALLEL` branches the same way as `Conditional`/`ForLoop` — did not survive grounding against the AST: `SwitchBlock`'s arms and `MapBlock`'s command are a single `CommandCall` with no internal `Vec<Stmt>` sequence to scope, and `ParallelBlock.branches` run concurrently, so pairing them would advise moving a dialog past a sibling that does not run "before" it at all; branches are still walked for scopes nested further inside them. `l2-nodus-dialog.md` 1.2.0 → 1.2.1 records all three corrections; `MODEL_COMMANDS`/`DIALOG_COMMANDS` widened `const` → `pub(crate) const` in `portability.rs`. 482 tests pass (was 471, +11: 7 `W016` + 4 `W017`, all asserting on `Diagnostic.code` never message text); clippy/fmt clean; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved); no `unwrap`/`panic!`/`expect` added to any production path. Ran the full `tests/fixtures/` corpus (14 files) through a scratch example (deleted after use): zero fixtures newly emit either code — checked by hand why for the two near-misses rather than assuming absence. INDEX v1.0.92 → v1.0.93; PLAN v1.41.0 held, TASKS v2.34.0 held (established run-phase convention — only INDEX moves; PLAN/TASKS resync at the next `/magic.task`). Plan Complete again — every remaining Backlog item is still design-blocked per the v1.41.0 reconciliation.
- 2026-08-12 **Decision:** `/magic.task nodus` — **sync-only replan; no new phase. Plan Complete holds.** Closed both pre-flight warnings. The `ORPHANED_SPEC` was a pure traceability gap, verified not assumed: `l1-nodus-testing` has a built L2 (`l2-nodus-testing`, Phase 6) and was named in Phase 6's *prose* but never as a checklist link, so the registry-to-plan trace ran through narrative instead of structure — the same class as `l1-dev-office` in `main`. The `SYNC_GAP` was six versions accumulated across the 27/28/29 cycle (INDEX v1.0.78 → v1.0.84), the familiar pattern where spec passes advance `INDEX.md` while `PLAN.md`'s basis pointer holds. Auto-fixed cosmetic drift: six Done phase headings (1, 5, 11, 12, 25, 26) lacked the `✓` marker, each confirmed fully-checked and `Done (Archived)` before the marker was added. **Backlog audit — three entries verified resolved in source and marked so**, because a stale catalogue is worse than none: `@err:` NL-9 (its recorded "zero call sites in `executor.rs`" against a live one now at `executor.rs:936` — the design-block it named was lifted by authoring `l2-nodus-error-dispatch`, built in Phase 26); NT-11 (its open vacuous-in-core-vs-second-host fork decided in `l2-nodus-testing` v1.3.0 §11 as **Vacuous in core**, with §8 now carrying the row explicitly); and the phase-journal gap (`CHANGELOG.md` now unbroken Phase 1 → 29). **Drafted a Phase 30 for the three remaining code-shaped items and retracted it before write-back** — the honest finding of this run. All three still matched their notes *in source* (`validator.rs:822` flat `declared` set; `parser.rs:474` single-token consume; the corpus's non-braced-only `@macro:`), which is what I checked first and what made a phase look authorable. Re-reading each entry in full showed that matching a note's **code state** is not the same as clearing its **verdict**: lexical scoping "needs a design pass deciding whether E004 should become scope-aware" and is not purely additive; the dotted `§runtime:` value was already "**confirmed design-blocked, not merely deferred**" at the v1.26.0 replan, since `l1-nodus-language` §113 is silent on whether a schema name may contain dots and so no conformance `Verify` line is writable; and the `@macro:` entry says outright "**Not a defect**", with its fixture worth adding only whenever macro body expansion itself is tackled. Each fails the same test the `@err:` entry applied to itself. Recorded the retraction in the Backlog header so a fourth replan does not rediscover the same dead end. **Net: every genuinely open nodus item is blocked on a design decision or an external integration — none on implementation capacity.** Pre-flight `ok:true` after write-back (zero warnings). Pre-Planning Stabilization: 0 promoted (20 Stable, 0 Draft). Rules Parity confirmed unchanged at RULES v1.6.0. PLAN v1.37.0 → v1.38.0, TASKS v2.30.0 → v2.31.0. Computed next step (DA-6): none — `/magic.task nodus` is now a no-op; opening scope is a `/magic.spec nodus` user decision.
- 2026-08-01 **Decision:** Phase 29 complete; Plan Complete (all three cycle phases Done, `tasks/` empty). NE-14 declared budget measure implemented in `crates/nodus` exactly per the plan-time-corrected mechanism recorded in Phase 29's own scope note (not the spec's original text). `EnvironmentProfile.token_measure`/`CandidateResult.token_measure` landed; `env_measure_rejection` (sibling to `capability_rejection`) + the check wired into the single shared `run_with_environment_impl` immediately after `env.profile()` — confirmed both public entry points delegate to that one function. **The plan-time correction proved exactly right** — implementation needed zero further adjustment beyond applying it, and the same correction was then applied to `l2-nodus-environment.md` 1.1.0 → 1.1.1 (§4.4.1: `Ok(EnvRunResult)`/`RuntimeError` not `Diagnostic`; after `env.profile()` not before `env.open`). 471 tests pass (was 467, +4); clippy/fmt clean; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved); no `unwrap`/`panic!`/`expect` added to any production path. INDEX v1.0.80 → v1.0.81. **Plan Completion triggered the first-ever Level 2 retrospective** (`RETROSPECTIVE.md` Session 1, `Full Sessions` 0 → 1): Signal 🟢 across Phases 12–29 (101 Done / 0 Blocked / 1 Cancelled, the T-23C02 supersession already on record), 0 orphans/phantoms/shadow-logic, `graph-before.json` baseline rolled forward (was 441 nodes / stale since before this whole session; now 560). Two findings recorded: the recurring `update-state` `Task`/`Spec`-field clobber (9/9 `finalize` calls this session needed manual correction — a formal engine bug report is recommended if not already filed) and the graph-baseline staleness (now fixed). **Paused before Changelog L2 / Version Bump**: root `CHANGELOG.md`'s `[Unreleased]` section is shared with `main`, whose own plan has not completed — cutting a release there is a cross-workspace, user-visible action beyond this session's nodus-scoped mandate, so it is queued for explicit user direction rather than an autonomous guess. CONTEXT.md regenerated. PLAN v1.37.0 held, TASKS v2.30.0 held (established run-phase convention).
- 2026-08-01 **Decision:** Phase 28 complete. DG-9/DG-10 memoizable & promotable approval implemented in `crates/nodus` exactly per `l2-nodus-dialog.md` §4.7's design. `DialogOutcome::Remembered(Value)` binds through `handle_dialog`'s existing `Answer` path (identical `ctx.log_step`/`ctx.set_var` call — memoization changes only where the value came from, never how it binds); `DialogProvenance{Answered,Remembered}` (`observability.rs`, beside `EventAnnotations`) + a fifth `dialog_provenance` field tag the resolved step's `StepEnd`. **Confirmed rather than assumed during implementation**: `dialog_provenance` is the first `EventAnnotations` field the crate's own dispatch logic populates directly — grepped `executor.rs` and found every sibling field (`message`/`anomaly`/`receipt`/`durability`) has zero in-crate writers, only host-side annotation was ever wired; `EventAnnotations`'s own doc comment updated to state this explicitly rather than implying universal host-supply. DG-10 confirmed to need zero code, exactly as designed — its recurrence/promotion pseudocode is entirely host-side over the DG-9 tag. **One empirical test-writing catch**: asserting no-provenance-on-rejection against `DEFERRED_WF` (which declares `@err: ESCALATE(human)`) initially expected a single `StepEnd`; `DIALOG_REJECTED` is `Signal`-free so NL-9 dispatch fires automatically, landing two `StepEnd` events (the rejected `ASK`, then the dispatched `ESCALATE`), both correctly provenance-free — the test was corrected to expect both, not the code. `l2-nodus-dialog.md` 1.1.0 → 1.1.1 reconciles §3/§4.7 to Implemented. 467 tests pass (was 462, +5: 1 unit + 4 integration); clippy/fmt clean; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved); no `unwrap`/`panic!`/`expect` added to any production path. INDEX v1.0.79 → v1.0.80; PLAN v1.37.0 held, TASKS v2.30.0 held (established run-phase convention). Phase 29 (NE-14) opens next — the last of the three phases planned this cycle.
- 2026-08-01 **Decision:** Phase 27 complete. LP-17 settlement effect seam implemented in `crates/nodus` exactly per `l2-nodus-settlement.md`'s design — no plan-time or implementation-time scope correction, the first such phase in the arc since Phase 25 (the spec was authored directly against the real `execute_command`/`handle_dialog`/`Executor` code in the same session, so every `[REFERENCE]` block's structural claim held, unlike Phase 24's first LP-11 build which found real divergences). `EffectClass::Settlement` + `SETTLEMENT_COMMANDS`/`SETTLE` reuse the LP-11 gate unchanged for the decide half; `SettlementRail`/`NoopSettlementRail` (the crate's first genuinely new extension-point trait since LP-11 itself) + ninth `ExtensionRole::Settlement` (absent from `builtin()`, `Storage`/`Policy`/`Dialog` precedent) supply the act half; `handle_settlement` mirrors `handle_dialog`'s shape, both exits (`POLICY_DENIED`, `SETTLEMENT_UNACCOUNTED`) `Signal`-free so NL-9 `@err:` dispatch covers `SETTLE` automatically with zero new dispatch code — a real synergy between Phase 26 and this one. **One empirical catch during test-writing**: the first fixture draft used a non-reserved `@out: $receipt` pipeline target and assumed it would be pre-seeded like `$out`; it is not — only reserved variables (`out`/`error`/`meta`) are pre-seeded at context construction, so an ordinary declared name starts absent from `vars` until bound — fixed by using the reserved `$out` binding throughout, matching every sibling LP-11/LP-16/NL-9 fixture's own convention. `l2-nodus-settlement.md` 1.0.0 → 1.0.1, `l2-nodus-portability.md` 1.7.0 → 1.7.1 (§3.1 LP-17 row → Implemented; three of twelve LP-9…LP-20 items now done: LP-11, LP-16, LP-17). 462 tests pass (was 452, +10: 4 unit + 6 integration); clippy/fmt clean; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved); no `unwrap`/`panic!`/`expect` added to any production path. INDEX v1.0.78 → v1.0.79; PLAN v1.37.0 held, TASKS v2.30.0 held (the established run-phase convention — only INDEX moves, PLAN/TASKS resync at the next `/magic.task`). Phase 28 (DG-9/DG-10) opens next.

## Blockers

## Blocking Constraints

## Session Continuity

**Last Session Ended:** 2026-06-24
**Handoff File:** none
**Bootstrap Mode:** false
