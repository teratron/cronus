# Project State

<!-- STATE.md — live project memory. Read FIRST in every workflow session. -->
<!-- Maximum 100 lines. Agent updates AFTER each completed action. -->

**Workspace:** nodus
**Updated:** 2026-07-31 06:05
**Phase:** 23 — Built-in Durable-State Conformance
**Status:** Active

## Current Position

- **Task:** **Phase 23 open, unstarted** — Built-in Durable-State Conformance (LP-2/LP-15), **5 live tasks** / 3 tracks (A, C, T), Sequential, `Todo`; T-23C02 **Cancelled (superseded)**. Make the shipped `StorageProvider` built-in round-trip in memory as its L1 contract requires; still **no executor wiring** (its own LP-3 record stays unsatisfied).
- **Spec:** PLAN v1.30.0 / TASKS v2.22.0 / INDEX v1.0.68; RULES v1.6.0. 18 nodus specs Stable, 0 Draft, phases 1–22 Done, 23 open. **Of the two items last cycle called task-authorable, only one survived grounding.** The LP-11 `PolicyProvider` call site is **LP-3-gated** (governance: needs a `/magic.spec` amendment documenting two independent host-usage contexts) — so LP-16/LP-17/LP-20 stay blocked behind it. The LP-15 built-in divergence survived and is Phase 23. Still design-blocked: three stale L2 tables (`l2-nodus-testing` NT-11; `l2-nodus-dialog` DG-9/DG-10; `l2-nodus-environment` NE-14) + the L1 §4.1 taxonomy reconciliation (4 roles listed vs 8 implemented) + LP-18(a)/(b) (needs an L2 decision — LP-6 major) + the LP-13→LP-9→LP-12 import triad (vacuous) + NL-11…NL-19/NL-21 + `core: schema.nodus` truncation + parity-gap clusters + lexical-scoping design pass + `@macro:` braced-body emission untested by corpus + internal-CHANGELOG.md phases-11–19 gap.
- **Next Action:** Execute T-23A01 Replace `NoopStorageProvider` with an in-memory built-in via /magic.run nodus

## Progress

```
Build phases 1–22 Done (Seed → Testing → Capability Manifest → Dialog → Control-Flow → Environment & Evaluation → Declarative Configuration Surface → Run-Manifest Identity & Reproducibility → Aggregation-Safe Event Stream → Event Annotations/Cost/Lineage/Completeness → ~MAP Conformance & Reachability → Bounded Whole-Run Self-Restart → Compensation Seam → Branch-Action Pipeline Targets & ?SWITCH Conformance → Compact-Form Round-Trip Fidelity → Whole-File NL-6 Round-Trip Closure); observability spec fully realized (all 20 HO invariants); NL-22 + NL-23 realized; NL-6 holds for the whole WorkflowFile | Baseline gates: cargo 429 tests + clippy + fmt; LP-1 zero-dep preserved
```

## Recent Decisions

- 2026-07-31 **Decision:** Sync replan — **no new phase; Phase 23 stays open and unstarted.** Resolved the expected spec→task `SYNC_GAP` (PLAN basis v1.0.68 → v1.0.69). **The substantive act was cancelling a task whose premise its own predecessor removed.** T-23C02 was planned to correct `l2-nodus-portability` §5 item 2 by *demoting* the LP-11 call site to "LP-3-gated"; the 1.3.0 pass corrected the same text from the **opposite direction** by opening the gate. Marked **Cancelled (superseded), not Done** — the outcome it described was reached, but not by this phase, and recording it Done would credit Phase 23 with another cycle's work; the phase file now says which cycle actually did it. The correction survives in sharper form: the LP-11 call site is not *ungated* but **permitted-and-undesigned** — an ordinary L2 gap, not a governance block. Queued it as the next spec target with the four things that pass must settle, one of which is a **real contradiction between the layers rather than a mere omission**: L2 §4.4 has the executor emit `ConstraintHit { halt: false }` and **skip** the denied step, while L1 §4.7 sketches a typed `NODUS:POLICY_DENIED` routed to `@err:`. Skip-and-continue and a catchable typed error are different observable behaviours, so the pass must choose rather than average — and the choice propagates to LP-16/LP-17/LP-20, which inherit the same ordering. Also confirmed Phase 23's five live tasks are genuinely unaffected rather than assuming it: `StorageProvider`'s own admission record (§4.8.2) remains **unsatisfied** (one context — `store-local` is a provider, not a second *decision shape*), so its hook points stay held exactly as the phase planned, and the built-in conformance fix was never gated in the first place. Separately recorded as RESOLVED in the Backlog: the repo-wide **SDD reference containment sweep** — a single leak found while grounding the LP-3 pass proved systemic (121 occurrences / 44 files / 8 crates), cleared across 55 files and 138 lines with zero residual references, invariant IDs and bare `§` refs deliberately kept since §6 forbids neither; gates green (`cargo check --workspace`, `fmt --all --check`, `clippy -p nodus -D warnings`, 429 nodus tests). Two facts from it are worth carrying: **16 leaks sat inside `crates/nodus`**, the crate built for standalone extraction, where §6's harm is concrete rather than theoretical; and several were **introduced by the Coder role itself** in Phases 21–22 despite §6's write-time prohibition — evidence that write-time enforcement does not hold, which together with §6 naming no remediation owner is what the engine bug report records. PLAN v1.30.0 → v1.31.0, TASKS v2.22.0 → v2.23.0, INDEX v1.0.69 held.

- 2026-07-31 **Decision:** Opened the LP-3 gate — `l1-nodus-portability` 1.13.0 → **1.14.0** (new §4.14 LP-3 Admission Evidence) and `l2-nodus-portability` 1.2.0 → **1.3.0** (new §4.8, the first admission records). **The diagnosis reframed the problem: LP-3 was not merely unsatisfied, it was *unfalsifiable*.** It named a threshold ("two independent host contexts") without ever defining what a host context is, what makes two independent, or what evidence fixes the judgment — so a seam could sit behind it indefinitely with nothing stating what would open it. That is exactly what happened: `PolicyProvider` and `StorageProvider` shipped as re-exported public API no code path consults, and LP-11's absent call site silently blocked LP-16/LP-17/LP-20 across multiple planning cycles while every participant, including me last cycle, argued about the gate's *status* rather than its *criteria*. §4.14 defines a host context by **what it decides and how it fails** rather than by ownership (two entry points of one project are two contexts when they exercise the seam differently; two separately-owned projects consuming it identically are one — the unit is the decision shape, because that is what the abstraction must span), and names **documented divergence, especially on fail direction, as the strongest independence signal**, since a seam that must span an existing disagreement is demonstrably not the single-context optimisation LP-3 exists to reject. **Applied, it yields two different dispositions — which is the gate working, not a half-finished pass.** `PolicyProvider`: **SATISFIED**, and the independence evidence is the host's own rather than my reading — `crates/domain/src/tool_security.rs` (runtime guard, every tool call evaluated pre-execution against severity-ranked signature rules) and `crates/domain/src/hooks.rs` (nine `HookEvent` kinds, parallel evaluation, deny-if-any-`Block`) are the two realizations the Stable main `l1-interception-model` was *written to unify*, its own Document History citing "the currently-divergent fail-behaviours of the plugin-hook and tool-security realizations" (INT-3). LP-3's second half was already met by construction: `evaluate(gate: &str, context: &Value) -> bool` names only `&str` and nodus's own `Value`, so no host type (`HookEvent`, `tool_security::Severity`, `contract::*`) appears and the interface needs no change. `StorageProvider`: **NOT SATISFIED** — `crates/store-local` is a provider, not a second *decision shape*, and the missing evidence is named precisely (a second integration persisting workflow state under a different durability/locality contract, the DN-3 local-vs-remote axis LP-15 already cites), so its `STORE`/`LOAD` hook points stay held while the separate LP-15 **built-in conformance** fix remains ungated, touching neither hook points nor run-parameter variants. **Corrected last cycle's §5 item 2 from the opposite direction to the one Phase 23 planned**: rather than demoting the LP-11 call site to "gated", the gate is now genuinely open — but the seam is **permitted, not yet designed**, since no L2 defines its Rust shape (effect-class notion, `gate` derivation, and a real reconciliation needed between L2 §4.4's `ConstraintHit { halt: false }` denial and L1 §4.7's typed `NODUS:POLICY_DENIED` — these differ). §5's closing sentence rewritten to separate the three states it had collapsed into one: permitted-and-designed, permitted-not-designed, not-permitted. Advisory finding I could not act on (write permission is `.design/` only): `crates/core/src/model_bridge.rs:1` carries an SDD reference leak — its module doc cites `l1-model-runtime §4.1, MR-2`, a spec filename in a product file (§6). INDEX v1.0.68 → v1.0.69.

- 2026-07-30 **Decision:** **Phase 23 opened** — Built-in Durable-State Conformance; resolved the expected spec→task `SYNC_GAP` (PLAN basis v1.0.67 → v1.0.68). **Plan-time grounding overturned one of the two items the prior spec pass promoted as task-authorable, and that correction is this cycle's substantive finding.** Last cycle's §5 item 2 named the LP-11 `PolicyProvider` call site the highest-leverage next step. It is **LP-3-gated**, confirmed four independent ways — §3's LP-3 row ("`StorageProvider` and `PolicyProvider` are held here pending that gate"), §2's Constraints ("executor integration (hook points, run-parameter variants) is deferred… once LP-3 is satisfied"), §4.4's own heading ("wiring pending LP-3"), and a standing PLAN Backlog line that **predates the spec pass entirely**. LP-3 is a *governance* gate: it requires a `/magic.spec` amendment documenting two independent host-usage contexts, which no atomic task can produce — so LP-16/LP-17/LP-20 remain blocked behind it too. **I wrote that overstatement myself last cycle by omitting a gate the same document asserts three times**; corrected in Track C rather than planned against, the Phase-18 `$restart` precedent (queue the spec fix, don't HALT and don't silently implement against wrong text). **The second item survived the gate** precisely because it touches neither hook points nor run-parameter variants — the scope §2 defers. Two Stable L1 statements mandate it: §4.1's Storage row (amended v1.8.0 by LP-15) specifies an **in-memory** built-in, and LP-2 requires every built-in be "sufficient for in-process testing without I/O"; the crate ships `NoopStorageProvider`, which discards on `store` and returns `None` on `load`, so it cannot round-trip even within one invocation. The L1's distinction is deliberate rather than sloppy — a discarding *audit* built-in is fine because audit is write-only, whereas a discarding *store* breaks the read-back that makes a store a store. Spec-ahead-of-code delta (the L1 moved in v1.8.0; the Phase-5 built-in did not), C12.1 in shape like Phases 17/20/21/22. Three guardrails written in from grounding, each against a plausible-but-wrong implementation: the existing Phase-5 test `noop_storage_and_policy_compile` **deliberately asserts `load(...).is_none()`** and so encodes the old contract — it must be updated knowingly, not hit as a mystery failure; interior mutability behind `&self` needs `std::sync::Mutex`, whose `lock()` returns a `Result` that must **not** be `unwrap()`ed on a production path (project rule) — recover the poisoned guard instead; and LP-2's "exactly one built-in per interface" forbids adding an in-memory provider *alongside* the no-op, making this a replacement and therefore a public-API rename (acceptable at `0.2.0`, since the `cargo semver-checks` gate lands at 1.0.0). Deliberately **not** in scope: the L1 §4.11 `put`/`get`/`delete` vs as-built `store`/`load` naming divergence — a second breaking change with no conformance gain. PLAN v1.29.0 → v1.30.0, TASKS v2.21.0 → v2.22.0, INDEX v1.0.68 held.

- 2026-07-30 **Decision:** Authored `l2-nodus-portability` 1.1.0 → 1.2.0 — new §3.1 reconciling the twelve deferred obligations LP-9…LP-20, each of whose L1 Document History entries names *this* spec as carrier. **Deliberately did not reuse the Phase-19 §3.1 shape unchanged.** That precedent's binary *Realized / Pending* axis would have misreported all twelve: calling LP-9 "Pending" implies the core owes code, when nodus has **no bundle-load path at all**, so the verify-before-load hook has no call site to attach to. Grounding each invariant against the crate instead produced a **four-way** axis, and the distinction that carries the pass is between a seam whose *gated construct* is absent (nothing owed) and one that **ships as re-exported public API but is never consulted**: `PolicyProvider::evaluate` and `StorageProvider::store`/`load` have **zero call sites** (verified by grep across `src/`; the two `.evaluate(` hits are `EnvironmentProvider`'s unrelated grading method). A host implementing either today observes no behaviour change — that is a defect, not deferred work, and the crate already has a name for the state ("interface-declared, wiring pending", `environment.rs:19`). Classification: **satisfied structurally** LP-10 (discharged by absence of vocabulary — no policy-writing command, `CapabilityManifest` exposes only declare/read, no grant/relax — and load-bearing in four sibling specs while having had no row here); **vacuous in core** LP-9/12/13/14/16/17/19/20; **seam declared, wiring pending** LP-11/LP-15; **partially realized** LP-18 (rule (c) discharged via NE-3/NE-7/NE-10; (a)/(b) not, since `EnvironmentProvider::open` returns `Instance` not `Result` and `ResumeDescriptor` carries no environment identity — closing them is an LP-6 **major** change, so it needs an L2 decision before it can be planned). **Two structural findings shrink the apparent backlog:** LP-11's absent call site alone blocks LP-16, LP-17 **and** LP-20, so four invariants reduce to one hook in `execute_command`; and LP-13 → LP-9 → LP-12 are ordered stages of one absent import mechanism, not three independent gaps. Named a real L1 divergence rather than papering it: L1 §4.1 specifies an *in-memory* built-in store, the crate ships `NoopStorageProvider`, which never round-trips within an invocation (also `put`/`get`/`delete` vs `store`/`load`, no `delete`). **Also corrected three stale surfaces found while grounding, all disagreeing with the code and with each other**: §4.2 registry (5 roles → 8, adding `Dialog`/`Environment`/`Config`, plus a *Wired* column, and un-marking `SchemaProvider` pending when §5 already called it delivered), §4.7's `ExtensionRole` reference enum (5 → 8) and its built-in-host prose (3 → 5 roles, with `Dialog`'s deliberate exclusion explained), and `from_workflow`'s derivation rules. Flagged for L1 reconciliation rather than fixed unilaterally: L1 §4.1 calls itself "the authoritative extension point registry" yet lists four roles against the implementation's eight. Separately discharged the correction queued at Phase-22 planning — `l2-nodus-testing` 1.2.0 → **1.2.1**, §10.4(a)'s "reproduces the body in the form the author wrote it" corrected to token-sequence-not-line-breaks, with the note that re-emitting line breaks would *break* NL-6 rather than improve it. INDEX v1.0.67 → v1.0.68.

- 2026-07-30 **Decision:** Sync-only replan — **no new phase.** Pre-flight fully clean (INDEX v1.0.67 = PLAN basis, 18/18 Stable, 0 Draft, no orphan/phantom, all 18 headers matching, **zero warnings**). The NL-6 conformance arc (Phases 17/20/21/22) is closed, and unlike every prior cycle in it, no candidate had been pre-identified — so rather than re-checking a known item, this cycle ran a **systematic invariant-traceability audit**: every L1 invariant range compared against its L2 realization spec's compliance table. **That found four stale L2 tables covering sixteen invariants, and proved the Backlog itself materially undercounted them.** `l2-nodus-portability` §3 stops at **LP-8** while `l1-nodus-portability` now defines through **LP-20** — twelve missing, where the Backlog had recorded only "LP-17/LP-18/LP-19", i.e. three of twelve, silently omitting LP-9…LP-16 and LP-20. Two of the omitted are already load-bearing elsewhere despite having no LP-side row: **LP-10** (host-granted authority, never self-authored) is cited by NL-14's criteria immutability, NL-17's no-self-upgrade, NL-20's DC-10 and NL-21's declassification authority; **LP-11** is the `decide → effect → observe` gate that `l2-nodus-compensation` §2 already records as vacuous-in-core. Also found: `l2-nodus-testing` stops at NT-10 while the L1 defines **NT-11** (differential parity, added v1.1.0 with a §4.7 harness) — absent from the table, from its Related-Specifications line, and from the crate; `l2-nodus-dialog` stops at DG-8 while the L1 defines **DG-9/DG-10**; `l2-nodus-environment` stops at NE-13 while the L1 defines **NE-14**. Only `l2-nodus-runtime` and `l2-nodus-observability` are complete — and `l2-nodus-runtime`'s completeness is itself the precedent, since the Phase-19 spec pass closed exactly this gap for it by adding §3.1. **None is plannable as a phase**: each is an L1 invariant with no L2-specified Rust shape, and eight of the twelve LP items are explicitly *seams* likely to resolve as vacuous-in-core host obligations (the Phase-15 streaming / Phase-19 NL-22 precedent) — which is a spec-time judgment, not a task-time one. Grounded rather than assumed for the candidate that looked closest to plannable: NT-11 requires two paths that **both** satisfy the same manifest and produce equal observable results, whereas the existing LP-3 two-host test compares a satisfying host against a *non-satisfying* one and asserts they **diverge** (Ok vs Failed) — the manifest gate, not a parity gate; and nodus has no transpiled execution path (the transpiler emits source text, not an alternative executor), so NT-11's first form is genuinely absent in core. Computed next step (DA-6): `/magic.spec nodus`, `l2-nodus-portability` first as the largest coherent cluster. PLAN v1.28.0 → v1.29.0, TASKS v2.20.0 → v2.21.0, INDEX v1.0.67 held.

- 2026-07-30 **Decision:** Phase 22 complete. `l2-nodus-testing` §10 / NL-6 whole-file round-trip closure implemented in `crates/nodus`: added `Transpiler::nodus_braced_raw_body` (re-quotes every `raw_lines` token except literal `{`/`}`, which must stay unquoted since `collect_braced_raw_lines`'s depth counter matches token *type*, not value — every other separator, including `:`, is matched by `parse_test_body` purely on string value, so quoting it changes nothing there); inverted `@test:` emission to check `!raw_lines.is_empty()` first; added `Validator::w015_test_pair_separator`, deliberately mirroring `parse_test_body`'s own section-tracking loop so a legitimately-consumed `key : value` triple's value token is never re-examined as a would-be key (a real bug in an early draft, caught before it shipped: treating the colon itself as a candidate key produced a false positive). Added `@macro:` and `human_mode` emission — both previously **never emitted at all**. Widened `full_corpus_ast_equal_after_compact_round_trip` from `.steps` to `ast1 == ast2`: now green across the whole 11-fixture corpus, the phase's single acceptance signal. **One planning assumption was corrected during implementation rather than left to surface as a test failure**: T-22B01 was planned assuming `@macro:`'s braced form; verifying directly against the real corpus fixture (`macro_expand.nodus`) before writing the "official" unit test showed it actually uses the non-braced `@macro: name` form, whose indented body line `parse_macro_block` never captures at all (`collect_braced_raw_lines` returns empty without an immediate `{` — macro body expansion is a separately deferred, unimplemented feature, not something this phase touches). `MacroBlock.raw_lines` is therefore empty for every macro in the corpus today; the braced-body emission path was still implemented for completeness (reusing the `@test:` renderer directly, no fork) but has no fixture exercising it — recorded in the Backlog rather than left implicit, since an implemented-but-untested path is exactly the kind of gap this whole conformance arc exists to catch. Per-task sensitivity checked empirically, not assumed: temporarily disabled `@macro:` emission and confirmed the widened harness failed with precisely the predicted diff (`macros: [MacroBlock{..}]` vs `macros: []`), then restored it. 429 tests pass (was 420; +9: 3 `@test:` round-trip + 3 `W015` validator + 3 macro/human_mode round-trip); clippy/fmt clean (1 fmt finding auto-fixed); zero new dependency (LP-1 preserved). PLAN v1.27.0 → v1.28.0, TASKS v2.19.0 → v2.20.0, INDEX v1.0.67 held (no spec files touched this phase).

- 2026-07-30 **Decision:** **Phase 22 opened** — Whole-File NL-6 Round-Trip Closure; resolved the expected spec→task `SYNC_GAP` (PLAN basis v1.0.66 → v1.0.67 from the 1.2.0 spec pass). **Plan-time grounding widened the phase materially beyond the spec pass's own subject.** The spec cycle specified the `@test:` fix and named its acceptance signal as widening Phase 21's corpus harness from `.steps` to the whole `WorkflowFile` (§10.5) — so before planning to that signal, I audited **every** `WorkflowFile` field against `to_nodus` rather than assuming `@test:` was the only thing standing between here and it. It is not: `macros` and `human_mode` are **never emitted at all** (zero references to either in `transpiler.rs`), and both hit normative corpus fixtures — `macro_expand.nodus` carries `@macro: greet`, `ticket_triage.nodus` carries a `;; HUMAN MODE` block. Eleven of fourteen fields round-trip today (nine pre-existing, `steps`+`comments` from Phase 21); this phase closes the remaining three. All three violate the same already-Stable NL-6 mandate, so none needs a new spec — `tests` additionally has its Rust shape in §10, while `macros`/`human_mode` reuse the same `raw_lines` mechanism (`parse_macro_block` even calls the identical `collect_braced_raw_lines` helper). **Including them is what the stated acceptance signal requires, not scope creep** — fixing `tests` alone would leave the phase unable to demonstrate its own goal; the Phase-21 precedent where `WorkflowFile.comments` and modifier-value quoting were outside the original enumeration but blocked every fixture. Three guardrails written in from grounding, each against a plausible-but-wrong implementation: emitting from `raw_lines` produces a **flat single-line body** and must not be "improved" to multi-line, since `collect_braced_raw_lines` drops `Newline` tokens and a re-render would change `raw_lines` on re-parse — breaking the very equality the task establishes; quoting must **skip the structural set** (`raw_lines` holds token values with no type information, so quoting `:`/`{`/`}`/`[`/`]`/`,` would break the triple-scan); and `human_mode` **must be emitted last**, because the parser routes a `HUMAN MODE` comment into `collect_comment_block()`, which greedily consumes every following Comment token and would silently absorb free-standing comments — corrupting `comments`, the field Phase 21 just fixed. Also found one inaccuracy in the spec I authored last cycle and flagged rather than acted on: §10.4(a)'s "reproduces the body in the form the author wrote it" overstates — `raw_lines` reproduces the token *sequence*, not the line breaks. The AST-equality conclusion is unaffected, so it is queued for a patch-level correction on the next `/magic.spec` touch (the Phase-18 `$restart` precedent) rather than HALTing or silently implementing against inaccurate text. PLAN v1.26.0 → v1.27.0, TASKS v2.18.0 → v2.19.0, INDEX v1.0.67 held.

- 2026-07-30 **Decision:** Authored `l2-nodus-testing` 1.1.0 → 1.2.0 (§10 Body Grammar Conformance & NL-6 Round-Trip, new `W015`, §2 correction, Canonical References added). **The design fork the prior replan enumerated collapsed under spec-time grounding — it was never three-way, and one of the replan's own supporting claims was too strong.** Reading `l1-nodus-testing` §4.1 settled the grammar question immediately: the canonical body is line-per-pair with `:` and **no braces**, so `expected: { status = SUCCESS }` is not a parser bug but a form the L1 never defined — the real defect being that non-conforming pairs are dropped *silently*, letting a declared assertion pass unchecked (now `W015`, warning-severity so existing corpus files still parse; it realizes NT-9's "not a silent assertion-miss" clause, and pairs with `W009` when a block's only assertions are all dropped). **Two further findings changed the shape of the fix.** (a) `parse_test_block` calls `collect_braced_raw_lines()` and then derives the structured fields *from that same vector* — so `raw_lines` is the token stream and the structured fields are a derived **view**, both always populated. §2's claim that "for old-style inline bodies the structured fields are empty" describes a state **no parse produces**, and the transpiler's `if has_structured { … } else if !raw_lines.is_empty() { … }` branch was written against that non-existent state, which is why its own `raw_lines` path is unreachable for every parsed block. (b) **The replan's "decisive" claim — that quoting info is destroyed at lex time and therefore no emitter-only change can restore AST-equality — was too pessimistic.** NL-6 requires equality of the *parsed AST*, not of the source text, and quoting is invisible to the AST because the lexer strips it on the way back in; so re-quoting on emit fully suffices, with no need to store quoting in `TestBlock`. What the replan got right was the conclusion (this needed `/magic.spec`, not a phase) — just not that supporting argument. Net: §10.4 states a two-part rule — (a) emit from `raw_lines` when non-empty, since it is the only representation reproducing the author's form (a branch-order correction that makes §2's long-standing round-trip claim true for the first time), and (b) emit each value so it re-lexes to exactly one token of equal value, re-quoting where the bare form would not. Also verified before asserting: `raw_lines` has two non-transpiler readers (`w006_route_test_coverage` NT-10 and the smoke-tag heuristic) so it cannot be dropped; `W010` is reserved for a deferred `extends` check so `W015` is the next free code; and the spec's token-splitting examples were softened to exactly what Phase 21's harness empirically produced (`("body","When")`, `("ticket_id","T")`) rather than asserting unverified token counts. §10.5 names the acceptance signal: widening the corpus harness from `.steps` to the whole `WorkflowFile`. INDEX v1.0.66 → v1.0.67.

- 2026-07-30 **Decision:** Sync-only replan — **no new phase; the plan is saturated**, and **the previous cycle's prediction was wrong.** Pre-flight fully clean (INDEX v1.0.66 = PLAN basis, 18/18 Stable, 0 Draft, no orphan/phantom, all 18 headers matching INDEX, **zero warnings** — Phase 21 touched no spec files so the prior `SYNC_GAP` stayed resolved). Last cycle's STATE recorded the `@test:` block gap as "a real NL-6 violation with a Stable owning spec and **no design fork**", making it "very likely the next C12.1 conformance phase". **Grounding that claim before planning it — the discipline Phase 21 vindicated twice — showed the opposite, so it is corrected here rather than acted on.** Three findings, each moving the item from code-shaped to spec-shaped: (1) **the spec's own model contradicts the parser** — `l2-nodus-testing` §2 says old-style inline bodies leave the structured fields empty with `raw_lines` holding the tokens, but `ticket_triage.nodus`'s braced body populates `input` **and** `raw_lines` while leaving `expected` empty, so neither §2's branch nor `to_nodus`'s `has_structured` branch describes a real old-style block; (2) **`expected: { status = SUCCESS }` never parses** — `parse_test_body` matches only `key : value` triples, so the `=` form is silently skipped, `expected` stays empty, and `evaluate_test_block`'s empty-expected path degrades the assertion to "pass if `Status::Ok`" — that fixture's declared assertion has never actually been evaluated, a latent correctness defect independent of round-tripping and arguably the more urgent half; (3) **decisive: quoting information is destroyed at lex time and is unrecoverable from the AST** — the lexer strips quotes before `raw_lines` is built, so `"T-001"` is stored as `T-001` and re-lexes as two tokens (`T`, `-001`), precisely the corruption Phase 21's harness observed. Finding (3) is what settles it: **no emitter-only change can restore NL-6 AST-equality**, because the information needed to reconstruct re-parseable source is in neither `raw_lines` nor the structured fields — so the resolution is a genuine design fork (retain quoting in `TestBlock`? re-quote heuristically, and is that sound given `raw_lines` must itself round-trip? or declare `@test:` bodies outside NL-6's scope?), categorically unlike Phases 17/20/21 where the Stable mandate was unambiguous and only the code diverged. Also re-checked the one other candidate and confirmed it design-blocked too: the `core: schema.nodus` truncation violates no stated mandate, since `l1-nodus-language` §113 specifies `core:` only as "loads the named schema" and says nothing about dots. Computed next step (DA-6): `/magic.spec nodus` on the `@test:` cluster — it now carries exactly what a spec pass needs as input, a concrete problem statement with the fork enumerated. PLAN v1.25.0 → v1.26.0, TASKS v2.17.0 → v2.18.0, INDEX v1.0.66 held.

- 2026-07-30 **Decision:** Phase 21 complete. `l2-nodus-control-flow` §3 / NL-6 conformance restored in `crates/nodus`: `nodus_step` now delegates to an exhaustive `nodus_stmt` dispatcher (no `_` catch-all) covering all 9 `Stmt` variants plus `Step.sub_steps`/`Step.retry`; the AST-equality harness the spec claimed lived in `workflows.rs` now really exists, in `tests/parity.rs` (the spec patched to name the real location, per the Phase-17 precedent for a claim that didn't match reality); 3 new v0.7 fixtures complete the normative corpus. **The Track B harness immediately earned its keep, surfacing 4 unplanned findings — all in `to_nodus`, all fixed here:** `Step.retry` (`~RETRY:n`) was completely unemitted, a second finding beyond the phase's original `Stmt`-variant enumeration, same function, same defect class. `Step.sub_steps` was wrongly assumed dead on the parse path at plan time (based on a `transpiler.rs` grep showing zero references) — `ticket_triage.nodus`'s own pre-existing step 2 (`?IF cond:` with an indented sub-action) already exercises it, and the harness caught the wrong assumption by failing on a real fixture before it shipped silently wrong; fixed by emitting `sub_steps` as indented lines with no terminator (they end at the next step number, not `~END`). The `$var = expr` assignment shorthand — internally a synthetic `Stmt::Command{name:"ASSIGN",..}`, reusing the Command shape rather than a new `Stmt` variant — cannot round-trip through the generic call syntax since "ASSIGN" is absent from `KNOWN_COMMANDS` and lexes as a plain `Identifier`; fixed via `nodus_command_or_assignment`, detecting the exact shape `parse_assignment_or_expr` produces and emitting the shorthand back. `WorkflowFile.comments` (free-standing top-level `;;` lines) and modifier-value quoting (a value containing whitespace needs re-quoting or the reparse silently drops everything after the first word) were both silently dropped, blocking every corpus fixture rather than one — fixed alongside the others since they're the same file, same root cause, and were blocking the harness itself from running at all. **A fifth finding was deliberately NOT fixed**: `@test:` block re-emission loses fidelity for structured `input`/`expected` values (found via the same harness on `ticket_triage.nodus` — genuinely non-AST-stable, unlike the `core`/`mode`/`@err:` oddities flagged at plan time). Pre-existing since Phase 6, owned by `l2-nodus-testing.md`, not `l2-nodus-control-flow.md` — narrowed the harness to `.steps` rather than the whole `WorkflowFile` so this out-of-scope gap doesn't block the control-flow fix, and recorded it in the Backlog with enough specificity (the brace-loss + no-requoting shape, the open question of how `expected: { status = SUCCESS }`'s `=` should be preserved) for a future dedicated pass. 420 tests pass (was 405; +15: 11 transpiler unit tests, 4 parity.rs tests); clippy/fmt clean (1 `collapsible_if` + 3 fmt findings, all fixed); zero new dependency (LP-1 preserved). PLAN v1.24.0 → v1.25.0, TASKS v2.16.0 → v2.17.0, INDEX v1.0.66 held (no spec files touched this phase).

- 2026-07-30 **Decision:** **Phase 21 opened** — third instance of the C12.1 conformance-defect shape (after Phases 17 and 20) and the largest. Pre-flight was clean but for the one mechanical `SYNC_GAP` predicted last cycle (PLAN basis v1.0.65 vs INDEX v1.0.66, from Phase 20's own Track C — verified as version-only drift on the two rows that phase corrected, not content drift), so the replan resolved it and then went looking for plannable content. The only Backlog item needing no spec was "normative fixture corpus coverage", explicitly logged as *"pure corpus tidying — no defect sits behind this"*. **Grounding that claim found a much larger defect underneath it:** `Transpiler::to_nodus` silently **drops every control-flow step** — `nodus_step` matches only `Stmt::Command`/`Stmt::Comment`, so all seven other `Stmt` variants hit a `_` catch-all returning `""` and the caller's `if !line.is_empty()` skips the step entirely; `Step.sub_steps` is never emitted at all. Three Stable specs mandate the opposite (l1-nodus-language NL-6 "AST-equal", l2-nodus-runtime §3/§4.5 "lossless", l2-nodus-control-flow §3/§4 "each new node gains compact and human emitters"). **Applied the Phase-20 Track-B lesson and proved it by execution rather than code reading**: a temporary probe transpiled the Phase-20 `?SWITCH` fixture and re-parsed it — `orig steps=2 reparsed steps=1`, with the whole `?SWITCH` step absent from the emitted `@steps:` block; probe reverted immediately after observation. Two structural facts explain the defect's lifespan: the emitter asymmetry is **total** (human side 7 emitters with full control-flow coverage, compact side 2 with none — the spec sentence is half-true, the human half was built), and **the guard the spec claims exists was never built** — l2-nodus-runtime §3 asserts an AST-equality round-trip test in `workflows.rs`, but that file contains only the `TranspileMode::Compact` dispatch; meanwhile the round-trip tests that do exist (`parity.rs`) assert only `ast2.header.name`, so a dropped step is invisible to them. That is the Phase-17 pattern ("the one construct never tested end-to-end is the one that doesn't work end-to-end") one level up. **Scope held to the actual violation rather than everything the probe surfaced:** three further oddities (`core: schema.nodus` → `schema`, injected `mode: production`, `@err:` raw re-spacing) were each checked and found **AST-stable** — they lose information on the first parse then reproduce idempotently, so `parse → to_nodus → parse` stays AST-equal, which is precisely what NL-6 mandates (AST-equality, not source identity). Excluded them explicitly and wrote that exclusion into the phase file as a guardrail, since a naive source-equality test would trip on all three for reasons unrelated to the defect; the `core` truncation is recorded as its own Backlog item. The fixture-corpus entry was promoted into the phase with its framing corrected — it now has a defect behind it, because the corpus is exactly what the round-trip harness sweeps. PLAN v1.23.0 → v1.24.0, TASKS v2.15.0 → v2.16.0, INDEX v1.0.66 held.

- 2026-07-30 **Decision:** Phase 20 complete. `l2-nodus-control-flow` §3/§4.5 conformance restored in `crates/nodus`: `try_parse_command_from_string` now splits a trailing `→ $target` off via `rsplit_once('→')` *before* the `(` split, fixing all three call sites (`?SWITCH` arms/default, `?IF` branch actions, `@err:` handlers) with one change; the executor needed no change, as predicted, since `execute_switch`/`execute_command` already honored `pipeline_target`. **Track B needed no code change — a self-correction, not a fix:** re-reading `collect_vars_stmt`'s `Stmt::Switch` arm in full showed it already walks `sw.arms`/`sw.default` via `collect_vars_cmd` (which already declares targets and marks uses); the plan-time "second, coupled defect" claim was itself a grounding error — a `sed -n '765,780p'` range that cut off one line before the arm-walking loop, making the block look shorter than it is. Corrected honestly in PLAN.md/TASKS.md rather than left standing; 2 regression tests added in its place confirming the real behavior. Track C reconciled both `l2-nodus-restart.md` and `l2-nodus-compensation.md` to their as-built implementations (both 1.0.0 → 1.0.1): the restart spec's `Signal::Restart`/`NODUS:RESTART_SCOPE` design was never built (bare validator code `E019` instead, matching the E010/E013/E014/E016/E017 validator-only precedent) and its §5/INDEX wording no longer misgroups `$restart` into `RUNTIME_OWNED_VARIABLES`; the compensation spec's planned `uncompensable`/`CompensationOutcome`/`step_identity` were never built (nothing reads any of them) and its `UNPUBLISH` example replaced with the real `NOTIFY` command — plus two findings not previously flagged in the Backlog: a never-built "explicit compensate request" arming trigger removed from the Overview/§3/§4.5, and three dangling `§7` cross-references (the document has no §7) fixed to `§6`. `INDEX.md`'s own top-level version bumped 1.0.65 → 1.0.66 to match both corrected rows (the Phase-17 Track-C precedent of bumping INDEX during a run phase's spec-reconciliation track). **Retired the Phase-18 `restart.rs` AST workaround**: `restart_count_progresses_and_context_is_fresh_each_attempt` now runs a real parsed fixture (`RESTART_ONCE_WF`, using the now-working inline `?IF $restart_count < 1 → GEN(...) → $target` syntax) through `run_with_audit` — the full parse → validate → execute path — proving the Track A fix end-to-end for the very feature that first surfaced the defect two phases ago. 405 tests pass (was 397; +8: 4 parser unit tests, 2 validator regression tests, 2 control_flow integration tests); clippy/fmt clean (2 fmt findings auto-fixed); zero new dependency (LP-1 preserved). Also noticed, not acted on: this workspace's internal `CHANGELOG.md` (Changelog L1, distinct from root `CHANGELOG.md`) has no entries for Phases 11–19 — the append step appears to have been skipped in those sessions; flagged in the file itself and in the Backlog rather than silently perpetuated or backfilled here (out of scope for this phase).

- 2026-07-30 **Decision:** **Phase 20 opened** — the plan looked saturated (registry unmoved at INDEX v1.0.65, 18/18 Stable, 0 Draft, no orphan, no phantom, all 18 file headers matching INDEX exactly, so zero *new* spec content) but was not: the replan promoted a Backlog item that is a **conformance defect against already-Stable content**, the second-ever such phase after Phase 17 and the same C12.1 shape. `l2-nodus-control-flow` §3's NL-10 row (Stable v1.0.1) mandates that `?SWITCH` arm actions bind their targets in declaration order; the code cannot. **Plan-time grounding widened the item and split it in two rather than planning the Backlog's one-line framing.** What was recorded as "a pre-existing inline-`?IF` pipeline-target defect" is (a) *structural*, not an edge case — `try_parse_command_from_string` returns `..Default::default()`, so **no** input can ever produce a `pipeline_target` — and (b) shared by **three** call sites (`@err:` handlers via `parse_error_decl`, `?IF` branch actions via `parse_branch_tail`, `?SWITCH` arms + `*` default via `parse_switch`), so the helper cannot be fixed for one caller alone; the all-three fix is the shape of the code, not scope creep. Grounding also found a **second, coupled defect the Backlog did not record**: `collect_vars_stmt`'s `Stmt::Switch` arm inserts only the scrutinee and never walks arm actions — while `extract_commands_stmt` directly below it does. This makes sequencing load-bearing and is written into the phase file as such: fixing the parser **alone** would actively break valid workflows, because a target would parse and the executor would bind it but a later step reading that variable would be rejected `E004 "used but never assigned"` — precisely the `~MAP`/`$it` failure mode Phase 17 cured, where a construct parses and executes yet is unreachable through every validated entry point. Note the error direction inverts between the two defects (missing *uses* → false negatives; missing *declarations* → false positives), which is why B cannot simply follow A at a later commit. Verified before writing Verify lines rather than assumed: the executor is **already correct** (`execute_switch` → `execute_command`, which honors `pipeline_target` at four sites), so no executor change is in scope; the arrow survives into the raw string as the lexer's `\u{2192}` token value; the no-paren path would swallow `→ $target` into the command *name* unless the arrow is split off first (written in as a guardrail); and coverage is nil — every existing `pipeline_target` assertion sits on the main-step or `~COMPENSATE` path, with `tests/restart.rs:63` already carrying a Phase-18 witness comment. Track C additionally clears the entire accumulated spec-to-as-built cluster from Phases 18/19 in one file-independent pass (Phase-17 Track-C precedent for a spec-sync track inside a run phase), so four wording-patch Backlog items retire together. Recorded a new Backlog note rather than acting on it: `collect_vars_stmt` and `extract_commands_stmt` are parallel descents that both must learn every binding construct, and three consecutive phases have now hit the same omission from the first walker (17, 19, 20) — worth a consolidating pass, narrower than the lexical-scoping design pass. PLAN v1.22.0 → v1.23.0, TASKS v2.14.0 → v2.15.0, INDEX v1.0.65 held.

- 2026-07-30 **Decision:** Phase 19 complete. `l2-nodus-compensation` (NL-22) implemented in `crates/nodus`: `TildeCompensate` token + `Step.compensation: Option<CommandCall>` (mirrors `Step.retry`) + `~COMPENSATE: CMD(args)` trailing same-line clause + transpiler round-trip; `NODUS:COMPENSATION_FAILED`; completed-effect ledger (`ExecutionContext.compensations`) populated at `run_step_with_retry`'s existing success-detection point; armed on `Status::Failed`/`Status::Aborted`, drained via plain `Vec::pop()` (LIFO falls out for free); a failed compensation is surfaced as `COMPENSATION_FAILED` without stopping the rest of the drain. **`[DR]` Dropped the spec's planned parallel `uncompensable` record and per-entry outcome enum** — nothing reads either (no accessor, no external test can see `ExecutionContext` internals), and NL-22(a)'s honesty property already holds by construction (the ledger only ever contains what a step explicitly declared); keeping unread fields would violate this project's own anti-overengineering discipline. **Found and fixed two real parser bugs, both self-contained to the new `~COMPENSATE` path (not pre-existing, unlike Phase 17/18's finds):** the spec's own canonical example command (`UNPUBLISH`) isn't a real nodus command (caught by E004/lexer, not by design); and a genuine bug — parsing the compensation's inner command via a *recursive* `parse_command_call` call means that call already consumes through its own newline, so the *outer* call's unconditional `skip_to_newline()` then found itself sitting at the start of the next line and silently swallowed the entire next step. A temporary debug print caught this immediately (a 2-step fixture parsed to only 1 step). Fixed with a `compensation_consumed_line` flag suppressing the outer skip. Also added a `nested_body_depth` counter (guarding `~FOR`/`~UNTIL`/`~PARALLEL` bodies and a step's own sub-steps) so a compensation clause can never leak out of a nested context into a later step's `pending_compensation.take()`. Proactively extended E004 (`collect_vars_step`) to see a compensation clause's own variable references, since it was squarely in this phase's own declaration surface. 397 tests pass (was 387; +10); clippy/fmt clean; zero new dependency (LP-1 preserved). **Both NL-22 and NL-23 now closed — the sync-only replan's entire scope is done.**

- 2026-07-30 **Decision:** Phase 18 complete. `l2-nodus-restart` (NL-23) implemented in `crates/nodus`: `restart_max: Option<u32>` on `RuntimeBlock` + `1..=10` bound check (E018, mirroring `e017_retry_bounded`); `$restart` reserved-but-writable + `$restart_count` reserved-and-runtime-owned (E013 rejects `→ $restart_count`); `Executor::execute_with_restart` — a bounded attempt loop wrapped **around** `execute_inner` (all 4 public callers now route through it, no signature change) — seeding `restart_count` fresh into the `@in`-overlay per attempt so LG-5 freshness falls out of `execute_inner` building a new `ExecutionContext` each call, not an explicit reset; `NODUS:RESTART_LIMIT` (Warn, pushed to `result.flags` like `MAX_REACHED`) on ceiling exhaustion. **Two deliberate `[DR]` deviations from the spec's literal §4.2/§4.6, both flagged in PLAN.md Backlog for a future spec patch:** dropped `Signal::Restart` entirely and moved the nested-request rejection into the validator as a new bare code `E019` (mirroring `find_empty_switches_stmt`'s recursive-descent shape over `~FOR` bodies/`~PARALLEL` branches/`?SWITCH` arms, unconditionally regardless of top-level-ness) — grounding showed every comparable structural rule in this crate (E010/E013/E014/E016/E017) is validator-only, with zero runtime re-checks, because `Executor::execute` can be called directly with an unvalidated AST (a real pattern already used in this crate's own tests); building `Signal`-based interception would have been the first redundant enforcement layer. `~MAP` needed no special-case code at all — `execute_map` already overwrites its inner command's `pipeline_target` before running it, so `$restart` structurally cannot be its per-element target. **Found and worked around (not fixed) a pre-existing, unrelated parser defect** while writing integration tests: inline `?IF cond → CMD(args) → $target` never attaches the trailing pipeline target (`try_parse_command_from_string` discards everything after the closing paren) — confirmed by parsing a fixture and inspecting the AST directly; predates NL-23 and would affect any workflow using that syntax. Worked around via a directly-constructed `WorkflowFile` in the one test that needed attempt-dependent behavior (Phase-16 precedent). 387 tests pass (was 373; +14); clippy/fmt clean (fixed one `redundant_closure`); zero new dependency (LP-1 preserved).

- 2026-07-30 **Decision:** Opened **two** phases in one invocation rather than one — Phase 18 (`l2-nodus-restart`) and Phase 19 (`l2-nodus-compensation`). Criterion: the No-Orphans guard flagged *both* new specs as critical blockers, and both are Stable and independently decomposable, so deferring one would have left a standing orphan for no gain. Two phases rather than one combined phase because the specs are independent (soft reference only, neither requires the other implemented), each carries its own vertical-slice order, and this matches the workspace's one-phase-per-L2-spec precedent (Phases 10–13). Phase 18 sequenced first: more tractable, and its single control-flow-changing task (the attempt loop) lands last, after the ceiling bound, error codes, reserved variables, and boundary-authority guards are all independently provable. **Plan-time grounding caught an inaccuracy in the spec I had authored the turn before, rather than propagating it into code:** `l2-nodus-restart` §5 slice 3 and its `INDEX.md` row loosely group `$restart` into `RUNTIME_OWNED_VARIABLES` alongside `$restart_count`, but §3's Invariant Compliance row (authoritative) names only the count — and runtime-owned membership makes a pipeline target an `E013` error, so a runtime-owned `$restart` would make the request unwritable and the entire feature unusable. Resolved by planning to §3 (`$restart` reserved-but-writable per the existing `$out`/`$draft` precedent; `$restart_count` runtime-owned so flow logic cannot forge the chain position it reads) and carrying the discrepancy as an explicit phase-file guardrail, with the wording flagged in the Backlog for a patch-level spec correction. Did **not** HALT for `/magic.spec`: the correct resolution is not in doubt, this is not the hard drift §5 reserves HALT for, and spec bodies are not this workflow's write domain — so the fix is queued rather than either forced through here or silently implemented wrong. Also verified before writing Verify lines rather than assuming: `RuntimeBlock` already carries `core`/`extends`/`agents`/`mode` (so `restart_max` is an added field, not a new parse shape — `parse_runtime_braces` already scans `{ key: value }` for known keys), `Signal` is a **private** enum so `Restart` changes no public API and no `Value` (NL-7 holds), `e017_retry_bounded` is the exact shape for the `1..=10` check, and `RESERVED_VARIABLES` (16) / `RUNTIME_OWNED_VARIABLES` (9) are separate lists precisely to express the writable-vs-unforgeable split this phase needs. PLAN v1.21.0 → v1.22.0, TASKS v2.13.0 → v2.14.0.

- 2026-07-30 **Decision:** Authored the NL-22/NL-23 pair as **two** L2 specs, not one — `l2-nodus-compensation` + `l2-nodus-restart`, both Stable. Criterion: they are different subsystems (effect-undo vs. run-lifecycle), independently phaseable in either order, and a combined spec would have blocked the tractable NL-23 behind NL-22's unresolved seam fork. **Resolved that fork decisively** rather than deferring it: NL-22 names three compositions whose machinery does not exist in the crate (LP-11 `decide → effect → observe` gate, NL-12 crash-resume, a declared sub-region scope), so each is recorded as *vacuous in core today* with the honest consequence stated — compensations route through the one existing `execute_command` effect path (so they inherit the LP-11 seam for free when it lands, with no second mechanism to retrofit), nodus drives at-least-once per process run with the ledger as the host's replay artifact, and run-as-scope is the honest subset. The Phase-15 streaming-chunk-merge precedent. Two design decisions the L1 left open, both resolved in-spec with the alternative weighed in §6 rather than hand-waved: a **failed compensation continues** the unwind (nodus cannot verify whether the failure invalidates earlier undos, and abandoning leaves more effects live with fewer attempts recorded), and each **restart attempt is its own event stream/manifest** (forced by HO-7 — `event_count == highest seq + 1` is a per-manifest identity a shared stream would break). Two structural choices are load-bearing: `Step.compensation: Option<CommandCall>` mirrors the existing `Step.retry` precedent, and the restart loop wraps **around** `execute_inner` so LG-5 fresh reconstruction is structural rather than depending on a clearing routine that must track every future context field. Deliberately did **not** add an attempt-chain field to `RunManifest` — that is an `l2-nodus-observability` concern, and changing a manifest field from a restart spec would smuggle a cross-spec change into a restart phase. Also closed the traceability gap the prior replan found: `l2-nodus-runtime` → 1.3.0 with §3.1 listing NL-11…NL-23 realization status, making the carrier the L1 entries name actually real (found and corrected a slip while editing it — the 1.2.0 Document History row already existed for the `~PARALLEL` concurrency change, so this pass is 1.3.0, not a duplicate 1.2.0). INDEX v1.0.64 → v1.0.65; 16 → 18 specs.

- 2026-07-30 **Decision:** Sync-only replan — no new phase (plan saturated again; Phase 17's conformance-defect opening was the exception, not a new pattern). Pre-flight surfaced one real `SYNC_GAP`: registry at INDEX v1.0.64 while PLAN was based on v1.0.62, from two `l1-nodus-language` refinements authored directly between sessions (→ 1.14.0) — **NL-22** compensation seam and **NL-23** bounded whole-run self-restart. All other guards clean: 16/16 Stable, 0 Draft, all 16 file headers match INDEX exactly, no orphan/phantom, no `Implements:` edge on the L1 (so no cycle), and both cross-workspace soft refs (`main/l1-compensation`, `main/l1-loop-governance`) resolve. Both invariants carried as pending L2 obligations rather than phased — no `Stable` L2 spec defines their Rust shape, so no atomic task can carry a concrete `Verify` line (C10), which is exactly what each invariant's own Document History anticipated ("reconciled at magic.task", NL-11…NL-21 precedent). **Verified the dependency claims rather than trusting the spec prose:** the crate contains zero traces of compensation, LP-11, LP-17/settlement, or restart — so NL-22's stated composition with the LP-11 `decide → effect → observe` gate, the LP-17 settlement sink, and NL-12 suspend/resume rests on three seams that do not exist, giving its spec pass a genuine fork (treat those compositions as vacuous-today host obligations and specify only the core declare-compensation + completed-effect ledger + LIFO unwind + honest-un-compensable contract — the Phase-15 streaming-chunk-merge precedent — or sequence the seams first). Assessed NL-23 as the lighter, more tractable slice: self-contained in the executor's existing step loop, no new value type (NL-7 untouched), and its NL-22 linkage is an honest-warning (a restart over non-idempotent, non-compensated effects double-commits), not a hard dependency. **Unplanned finding while checking the tracking mechanism the two invariants name:** `l2-nodus-runtime.md` §3 Invariant Compliance lists only NL-1…NL-10, so all thirteen of NL-11…NL-23 have no row in the very file their L1 entries designate as carrier — the obligations live only in the PLAN Backlog. Not fixable here (spec bodies are `magic.spec`'s write domain), so recorded as a Backlog item for the next spec pass to reconcile. PLAN v1.20.0 → v1.21.0, TASKS v2.12.0 → v2.13.0.

- 2026-07-25 **Decision:** Phase 17 complete. `l2-nodus-control-flow` §4.5 conformance fix implemented in `crates/nodus`: `$it` declared in the `Stmt::Map` arm of `collect_vars_stmt`, closing the gap where `E004` rejected every `~MAP` workflow. Verified deliberately narrow along the way: grepped `parity.rs`/`control_flow.rs` before writing tests rather than assuming the phase-file's suggested file split — found the normative fixture corpus actually lives in `parity.rs` (not `control_flow.rs`, which uses inline consts for its four sibling constructs), so split the new coverage accordingly: `map_transform.nodus` + validate/round-trip/execute-ok in `parity.rs`, N-element/empty/non-list behavioral assertions as inline consts in `control_flow.rs`. Retired the Phase-16 `Executor::execute` workaround in `tests/observability.rs` now that its cause is fixed. Patched `l2-nodus-control-flow` 1.0.0 → 1.0.1 (§4.5) — checked the actual implementation before writing the spec sentence: the fix uses one flat file-wide `declared` set with no scope stack (matching `~FOR`'s existing precedent), so the spec text says exactly that rather than overclaiming per-construct scoping the code doesn't provide. Verified the Phase-15 `LOG`-locks-`$out` fixture bug does not recur in the new fixture (its `LOG($out)` is the last step, no write after). 373 tests pass (was 365; +8); clippy/fmt clean; zero new dependency (LP-1 preserved). Two adjacent temptations stayed in Backlog rather than folded in: proper lexical scoping for all binding constructs (non-additive, changes existing diagnostics) and fixture-corpus backfill for `?SWITCH`/`~RETRY`/`!HALT`/`!PAUSE` (no defect behind it — already covered end-to-end in `control_flow.rs`).

- 2026-07-25 **Decision:** Post-Phase-16 replan — **new phase opened**, breaking the three-cycle "plan saturated → go author a spec" pattern. Pre-flight clean (16/16 Stable, 0 Draft, all 16 file headers match INDEX exactly, no cross-workspace collisions, RULES parity v1.6.0 held). Observability is fully closed, so the usual next-spec candidate is gone — but the replan promoted the defect Phase 16 surfaced in passing rather than treating it as a footnote: `E004` doesn't know `~MAP` binds `$it`, so **every** `~MAP` workflow is rejected before it runs and the construct has been unreachable through all validated entry points since Phase 11. This is categorically different from every other Backlog item: `l2-nodus-control-flow` §4.3/§4.4 (Stable v1.0.0) already mandate the implicit binding, so the code diverges from a Stable contract — plannable with a concrete Verify line and **no new spec** (C12.1 fix-to-regain-conformance), where the rest stay design-blocked. Grounding before planning (the discipline that paid off in Phases 15/16): localized the defect to the `Stmt::Map` arm of `collect_vars_stmt` (marks `$it` used via `collect_vars_cmd`, never declares it, unlike `collect_vars_for` which declares `~FOR`'s explicit variable); confirmed `~MAP` is the *only* construct with an undeclared implicit binding (`~UNTIL`/`~PARALLEL` bind nothing); confirmed `$it` is absent from `RESERVED_VARIABLES`; and found the coverage hole that hid it — `tests/control_flow.rs` drives `!HALT`/`!PAUSE`/`?SWITCH`/`~RETRY` end-to-end through `workflows::run` but has zero `~MAP` coverage, so the one construct never tested end-to-end is the one that doesn't work end-to-end. Also noted the spec's own blind spot: §4.5 enumerates only the three validator rules Phase 11 *added* and never records which *existing* rule the new binding invalidated — patched as T-17C01. Scope held deliberately narrow: two adjacent temptations (making `collect_vars_stmt` properly scope-aware; backfilling fixtures for the other four v0.7 constructs) were pushed to Backlog, not folded in — the first is a non-additive design change, the second has no defect behind it. PLAN v1.19.0 → v1.20.0, TASKS v2.11.0 → v2.12.0, INDEX v1.0.61 held.

- 2026-07-25 **Decision:** Phase 16 complete. `l2-nodus-observability` §4.9 implemented in `crates/nodus`: `EventAnnotations`/`Anomaly`/`Durability` carrier on all 10 `ExecutionEvent` variants (HO-9/11/16/17); 4 `Measurement` token classes on `ModelResponse` only (HO-8); `SourceRef`/`derivation` lineage on `LoopIteration` + new `LoopType::Map` (HO-13); read-side `classify_trace` with no new field (HO-10); HO-17 durable-only-`seq` rule documented on `emit`. Two findings surfaced during implementation: (1) `execute_map` emitted zero events before this phase — fixed as the planned scope expansion, now emits one `LoopIteration` per element with correct N→N derivation; (2) unplanned — driving `~MAP` through the public `run_with_audit` API hits a pre-existing validator gap (`E004` doesn't know `~MAP` binds `$it` implicitly), rejecting every `~MAP` workflow; no crate test had ever exercised `~MAP` end-to-end before. Worked around in the new test (direct `Executor::execute`, bypassing the validate-gate) rather than expanding this phase into a validator fix; flagged in PLAN.md Backlog instead. Also fixed a pre-existing doc-comment defect found while extending `emit`'s: Phase 15 had split `execute_inner`'s doc comment into two disconnected fragments. 365 tests pass (was 352; +13); clippy/fmt clean; zero new dependency (LP-1 preserved). **All 20 HO invariants closed — l1-nodus-observability.md fully realized.**

- 2026-07-24 **Decision:** Phase 16 opened — Event Annotations, Cost, Lineage & Completeness. Same trigger shape as Phases 14/15 (spec-ahead-of-code L2 update, not a new orphan — only SYNC_GAP fires; recognized cognitively). Decomposed §4.9 into 6 atomic tasks / 4 tracks (A carrier, B targeted fields, C read-side + discipline, T validation), Sequential. **Plan-time grounding found a scope item the spec assumed:** `execute_map` emits no events at all (verified — the only two `LoopIteration` sites are `execute_for`/`execute_until`), so HO-13's canonical `~MAP` N→N example is unrecordable as the code stands; folded the missing emission into the HO-13 task as a deliberate, documented scope expansion, noting it changes existing `~MAP` traces additively (HO-5 governs `RunResult`, untouched). Two guardrails written into the phase file against plausible-but-wrong implementations: do not flatten the carrier into four flat fields (quadruples all-variant churn now and for every future annotation), and do not build an `emit_transient` companion with no caller (HO-17 realizes as a documented rule + a core-emits-`Durable` guard; the path lands with a real streaming host). INDEX v1.0.60 → v1.0.61, PLAN v1.18.0 → v1.19.0, TASKS v2.10.0 → v2.11.0.

- 2026-07-24 **Decision:** Authored Pass 2 — l2-nodus-observability 1.3.0 → 1.4.0 (§4.9), closing all 20 HO invariants. Checked the L1 text before designing rather than trusting the prior turn's note: not only HO-11 but **four** invariants (HO-9 receipt, HO-11 message, HO-16 anomaly, HO-17 durability) are "an ExecutionEvent MAY carry" annotations — four separate fields would have rewritten all 10 variants four times over. Specified them instead as ONE `EventAnnotations` carrier field per variant, so the churn happens once and future annotations become struct fields rather than tenth-variant edits. Targeted realizations for the rest: HO-8 four token classes on `ModelResponse` only (born as §4.8 `Measurement`, `Unavailable`-not-`0` since `ModelProvider` has no token seam — extending that seam explicitly declared out of scope); HO-13 indices-only `SourceRef` lineage on collection events, outside the NL-7 `Value` space; HO-10 a pure read-side `classify_trace` with no field, reusing §4.8's gap identity. Caught a subtle interaction with Phase 15's work: HO-17 requires the durable stream alone to be numbered, so **a transient must not consume a `seq`** — otherwise a dropped transient reads as a gap and a severed transient tail as a truncated run; fixed the rule now (as a companion emission path, not a change to `emit`) though nodus emits no transients today. Review also caught a stale "deferred to Pass 2" sentence in the v1.3.0 Overview contradicting the new paragraph — fixed. INDEX v1.0.60 → v1.0.61.

- 2026-07-24 **Decision:** Post-Phase-15 replan — no new phase (plan saturated; third consecutive such outcome). Pre-flight clean (16/16 Stable, no orphan, no SYNC_GAP, RULES parity v1.6.0 held, PLAN basis v1.0.60 = INDEX). Phase 15 consumed the only plannable content (§4.8). Every remaining Backlog item is design-blocked — no L2 realization spec, so no verifiable atomic task (C10). Computed next step (DA-6): /magic.spec nodus for the Pass-2 event batch, whose seq/Measurement foundation Phase 15 just laid; flagged forward that HO-11's optional `message` field lands on every variant and would re-churn all 10 construction sites, so that pass should decide whether it slices separately. Mechanical fix only: removed the running "current as of vX" version stamp from the PLAN Backlog header — it re-drifted every cycle; entries are marked RESOLVED as they land instead.

- 2026-07-24 **Decision:** Phase 15 complete. `l2-nodus-observability` §4.8 implemented in `crates/nodus`: `Measurement { Taken(u64), Unavailable }` retyped onto the 5 obtainable-failure numerics; `seq`/`correlation_id` on all 10 `ExecutionEvent` variants; `Executor::emit` single choke point replacing 20 hand-paired increments; `RunManifest.event_count` gap-check identity pinned; `handle_dialog`'s fabricated `elapsed_ms: 0` → `Unavailable`. Three [DR]s resolved: (a) zero-dep correlation-id fallback via a process-local atomic counter, not `SystemTime`+hash — no wall-clock reading enters run metadata at all; (b) kept `String` over `Arc<str>` for the per-event correlation clone, having confirmed events already allocate `String`s eagerly elsewhere, so this is consistent with the existing design, not a new regression; (c) `RunManifest.run_id` now reads the resolved `correlation_id` (not the raw possibly-empty parameter), verified first that no existing test asserted an empty `run_id`. Caught and fixed a real bug in the phase's own test fixture, not the production code, during Track T authoring — `LOG` unconditionally locks `$out`, so a loop-body `LOG` before a later `GEN(...) → $out` produced a spurious `RULE_VIOLATION`. 352 tests pass (was 345; +7); clippy/fmt/doc clean; zero new dependency (LP-1 preserved).

- 2026-07-24 **Decision:** Phase 15 opened — Aggregation-Safe Event Stream. Same trigger shape as Phase 14: a spec-ahead-of-code L2 update, not a new orphan (l2-nodus-observability planned since Phase 4), so orphan-detection cannot surface it — only SYNC_GAP fired; recognized cognitively as a planning trigger. Decomposed §4.8 into 6 atomic tasks / 4 tracks (A type surface: Measurement retype then seq/correlation; B emission discipline: choke point + gap-check identity; C the fabricated-zero fix; T validation), Sequential. Decomposition inherits the spec's churn-minimizing logic: each Track-A task lands its field change *together with* every construction site it breaks, because Rust will not compile a partially-applied field change — a boundary leaving the crate broken would have no checkable Verify line. Two [DR]s deferred to implementation: (a) how to generate a zero-dep correlation id when `run_id` is "" (process-local counter vs SystemTime+counter hex), (b) `String` vs `Arc<str>` for the per-event correlation clone (noted: events already allocate Strings eagerly, so this is consistent, not a new regression). INDEX v1.0.59 → v1.0.60, PLAN v1.17.0 → v1.18.0, TASKS v2.9.0 → v2.10.0.

- 2026-07-24 **Decision:** Split the deferred 9-invariant HO event-stream batch; authored Pass 1. Assessed each invariant's real Rust weight against the code: HO-7 (`seq`+`correlation_id`) and HO-14 (two-state `Measurement`) both rewrite EVERY `ExecutionEvent` variant, while the other seven are optional fields riding on whatever exists. Batched the two cross-cutting ones as §4.8 "Aggregation-Safe Event Stream" so the all-variant churn happens once — and so Pass-2's HO-8 token classes are *born* as `Measurement` instead of added raw and retyped. Two findings verified in the crate before writing (not assumed): the 20 `record_event` calls pair 1:1 with 20 manual `event_count += 1` increments (correct today, but fragile once `seq` depends on it → spec requires a single choke point), and `handle_dialog`'s hardcoded `elapsed_ms: 0` is the crate's one fabricated zero — exactly what HO-14 forbids → becomes `Unavailable`. Also recorded honestly: HO-7's streaming chunk-merge is *vacuous in core* (`ModelProvider::generate` returns a complete `String`; no chunk exists) → host obligation, not silent omission. l2-nodus-observability 1.2.0 → 1.3.0, INDEX v1.0.59 → v1.0.60. Pass 2 (HO-8/9/10/11/13/16/17) explicitly deferred.

- 2026-07-24 **Decision:** Post-Phase-14 replan — no new phase (plan saturated; second consecutive such outcome, the pipeline's natural rhythm). Pre-flight clean (16/16 Stable, no orphan, no SYNC_GAP, RULES parity v1.6.0 held, PLAN basis v1.0.59 = INDEX). Phase 14 consumed the only plannable content (l2-nodus-observability §4.7). Every remaining Backlog item is design-blocked — no L2 realization spec, so no verifiable atomic task (C10): the deferred HO event-stream batch (9 invariants), NL-19/21, LP-17/18/19, and the parity-gap clusters (whose MATCHES/PCRE-vs-LP-1 design fork is still open). Computed next step (DA-6): /magic.spec nodus for the HO event-stream batch — the spec's own named follow-up and largest coherent cluster; flagged that HO-14's two-state Measurement type is cross-cutting over every numeric event field and may warrant splitting the batch at spec time. Mechanical fix only: corrected a stale registry-version reference in the PLAN Backlog header. No PLAN/TASKS content rewrite (already in lockstep).

- 2026-07-24 **Decision:** Phase 14 complete. `l2-nodus-observability` §4.7 implemented in `crates/nodus`: `ExecutionMode`/`SimFidelity`/`Determinism`/`ReproRecipe`/`FaultIdentity` types; `step_identity`/`fault_identity` on events; `RunManifest.{execution_mode,exposure_switches,repro}`; `Executor::execute_with_manifest_context` host-declaring entry point. Three design decisions caught during implementation, each a `[DR]`: (1) `step_identity(u32, &str)` not `step_identity(&Step)` — emission call sites carry the two definition pieces directly, not an AST reference; (2) `workflow_digest` hashes the parsed AST's `Debug` form, not raw source — `execute_inner` never sees source text (parsing happens in `workflows.rs`); (3) extended the private `execute_inner` (3 in-file callers) with the two host-declared params rather than touching `execute()`/`execute_with_params()`'s public signatures, adding one new public method for the declaring path instead. 343 tests pass (was 335; +8); clippy/fmt/doc clean (fixed 2 `derivable_impls` findings); zero new dependency (LP-1 preserved).

- 2026-07-24 **Decision:** Phase 14 opened — Run-Manifest Identity & Reproducibility. A spec-ahead-of-code L2 update (not a new orphan): l2-nodus-observability was already planned in Phase 4 (HO-1…HO-6), and its §4.7 (HO-12/15/18/19/20, v1.2.0) adds unrealized manifest-cluster content — recognized cognitively as a planning trigger (orphan-detection cannot surface it; only the SYNC_GAP warning fired). Decomposed §4.7 into 7 atomic tasks / 4 tracks (A manifest+event types/fields, B step_identity+fault_identity derivation, C repro population + host-supplied mode/switches, T validation), Sequential; each carries a concrete Verify line (C10). One [DR] deferred to implementation: the host-supplied `execution_mode`/`exposure_switches` entry-point shape (RunParams struct vs new run_with_* variant). All-additive/optional (HO-5/HO-6 preserved), zero-dep (LP-1). INDEX v1.0.58 → v1.0.59, PLAN v1.16.0 → v1.17.0, TASKS v2.8.0 → v2.9.0.

- 2026-07-24 **Decision:** Authored the observability manifest cluster. Discovered the observability backlog is bigger than flagged — the code realizes only HO-1…HO-6 + Phase-12 `env_trajectory`, so **HO-7…HO-20 are ALL pending** (14 invariants), too many for one phase. They split by data structure: HO-12/15/18/19/20 enrich `RunManifest`/`StepError` (one coherent "honest, cross-run-comparable, re-executable record" story); HO-7/8/9/11/13/16 + HO-14 + HO-10/17 enrich per-event descriptors (a separate story). Scoped this /magic.spec to the manifest cluster — updated l2-nodus-observability 1.1.0 → 1.2.0 with §4.7 (intended realization: `execution_mode`, `step_identity`, `exposure_switches`, `FaultIdentity`, `ReproRecipe`), Stable via C9 (spec-ahead-of-code, the l2-nodus-config precedent). Deferred the event-stream batch explicitly (nothing silently dropped). INDEX v1.0.58 → v1.0.59. Next: /magic.task opens the phase.

*(Phases 1–13 and their decisions: see `CHANGELOG.md` and `RETROSPECTIVE.md` — permanent record, trimmed here to keep this file under its line cap.)*

## Blockers

## Blocking Constraints

## Session Continuity

**Last Session Ended:** 2026-06-24
**Handoff File:** none
**Bootstrap Mode:** false
