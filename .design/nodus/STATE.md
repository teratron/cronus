# Project State

<!-- STATE.md — live project memory. Read FIRST in every workflow session. -->
<!-- Maximum 100 lines. Agent updates AFTER each completed action. -->

**Workspace:** nodus
**Updated:** 2026-07-31 19:48
**Phase:** 27 -- Settlement Effect Seam
**Status:** Active

## Current Position

- **Task:** Three phases planned in one pass — **Phase 27 Settlement Effect Seam** (LP-17, 5 tasks/4 tracks, Todo, sequenced first), **Phase 28 Memoizable & Promotable Approval** (DG-9/DG-10, 3 tasks/3 tracks, Todo), **Phase 29 Declared Budget Measure** (NE-14, 3 tasks/3 tracks, Todo) — mutually independent, no cross-phase dependency. NT-11 stays Vacuous in core, nothing to task.
- **Spec:** PLAN v1.37.0 / TASKS v2.30.0 / INDEX v1.0.78 (fully synced, no SYNC_GAP); RULES v1.6.0. 20 nodus specs Stable, 0 Draft, phases 1–26 Done, 27–29 Todo. Found and corrected at plan time (not acted on outside `/magic.task`'s write domain): `l2-nodus-environment.md` §4.4.1's claimed NE-14 mechanism (`Diagnostic` channel, check "before `env.open`") doesn't match `workflows.rs:777-859` — the real shape mirrors `capability_rejection`'s `Ok(EnvRunResult)`/`RuntimeError` pattern, and `env.profile()` is only reachable after `env.open`/`env.reset` already ran; Phase 29's own scope note carries the correction, spec queued for patch at next `/magic.spec` touch. Still design-blocked: L1 §4.1 taxonomy reconciliation (4 roles vs 8 implemented) + LP-18(a)/(b) (LP-6 major) + LP-13→LP-9→LP-12 import triad (vacuous) + LP-20 (own core vocabulary needed) + lexical-scoping design pass + upstream v0.4.6→v0.7 parity clusters.
- **Next Action:** Execute T-27A01 `EffectClass::Settlement` + `SETTLEMENT_COMMANDS` + `SETTLE` vocabulary + error code via /magic.run nodus

## Progress

```
Build phases 1–24 Done (Seed → Testing → Capability Manifest → Dialog → Control-Flow → Environment & Evaluation → Declarative Configuration Surface → Run-Manifest Identity & Reproducibility → Aggregation-Safe Event Stream → Event Annotations/Cost/Lineage/Completeness → ~MAP Conformance & Reachability → Bounded Whole-Run Self-Restart → Compensation Seam → Branch-Action Pipeline Targets & ?SWITCH Conformance → Compact-Form Round-Trip Fidelity → Whole-File NL-6 Round-Trip Closure → Built-in Durable-State Conformance → Per-Effect Authorization Call Site); observability spec fully realized (all 20 HO invariants); NL-22 + NL-23 realized; NL-6 holds for the whole WorkflowFile; LP-15 built-in conformant; LP-11 realized | Baseline gates: cargo 444 tests + clippy + fmt; LP-1 zero-dep preserved
```

## Recent Decisions

- 2026-07-31 **Decision:** Planned three phases in one `/magic.task nodus` invocation — Phase 27 (Settlement Effect Seam, LP-17, 5 tasks/4 tracks A-C+T), Phase 28 (Memoizable & Promotable Approval, DG-9/DG-10, 3 tasks/3 tracks A,C,T), Phase 29 (Declared Budget Measure, NE-14, 3 tasks/3 tracks A,C,T), all Sequential and mutually independent (different files, no shared call site, no cross-phase dependency edge) — matches the one-phase-per-L2-spec-delta precedent (Phases 10-13, 14-16, 18-19). Sequenced 27→28→29 by DA-3: no dependency-topology or status-maturity tiebreaker discriminates among three equally-Stable independent specs, so coverage decided the order — LP-17 is the single largest capability (new command class, new extension-point trait) and leads; DG-9/DG-10 and NE-14 are both small deltas on already-Done phases, ordered by `INDEX.md` row order. **Found and corrected a real plan-time inaccuracy in `l2-nodus-environment.md` §4.4.1, not acted on outside `/magic.task`'s write domain**: the spec claims NE-14's rejection uses "the same pre-run `Result<_, Vec<Diagnostic>>` channel LP-8's `validate_manifest` already uses" and runs "before `env.open`" — neither holds against the real code, verified directly at `workflows.rs:777-859`. The adjacent, already-shipped `ExtensionRole::Environment` manifest rejection (`workflows.rs:806-814`) actually returns `Ok(EnvRunResult{result: capability_rejection(..), ..})` — an ordinary `RunResult{status: Failed, errors: [RuntimeError{CAPABILITY_UNMET}]}`, never `Err(Vec<Diagnostic>)` (that channel is reserved for parse/validate failures earlier in the same function). And `env.profile()` — the only source of `budget`/`token_measure` — isn't called until line 828, *after* `env.open`/`env.reset` (lines 818/821) already ran for the frozen-boundary reset-observation shape. Phase 29's own scope note records the corrected insertion point (after `env.profile()`, before `execute_for_environment`) and rejection shape (mirroring `capability_rejection` exactly); `l2-nodus-environment.md` itself is queued for a patch-level fix at the next `/magic.spec` touch (the Phase-18 `$restart` precedent), with T-29T01 folding the reconciliation in once the mechanism lands. Planning Audit (`@role:planner`) and Instruction Quality (`@role:prompt-engineer`) both PASS — one minor note recorded (Phase 27 and 28 both touch `executor.rs`, but in disjoint functions and under Sequential execution mode, so no real conflict). PLAN v1.36.0 → v1.37.0, TASKS v2.29.0 → v2.30.0, INDEX v1.0.78 held; CONTEXT.md regenerated.

- 2026-07-31 **Decision:** Closed the three remaining invariant-traceability gaps (NT-11, DG-9/DG-10, NE-14) — a blank `/magic.spec nodus` invocation, resolved via Explore Mode's Blank Trigger (DA-3 highest-coverage-gap ranking: 4 invariants across 3 specs, additive-only, no breaking forks, over the heavier LP-18(a)/(b) breaking-change fork or the still-larger unscoped upstream-parity clusters). **`l2-nodus-testing` 1.2.1 → 1.3.0**: new §11 grounds NT-11 (differential parity) directly against `transpiler.rs` — confirmed `to_nodus(ast) -> String` emits source text, not an alternative executor, so there is no second execution path for the §4.7 harness to diff; correctly distinguished from NL-6 AST-equality (a different, already-realized invariant) and from `l2-nodus-portability` §4.8's LP-3 two-host record (proves divergence, the opposite of NT-11's required convergence). Classified **Vacuous in core**, the crate's established axis for a seam whose gated construct doesn't exist. **`l2-nodus-dialog` 1.0.0 → 1.1.0**: new §4.7 designs DG-9 as a new `DialogOutcome::Remembered(Value)` variant bound through `Answer`'s identical path (so the "never turns rejection into approval, never bypasses `+type`/`+validate`" discipline holds by construction, not by added checks) plus a fifth `dialog_provenance` field on the shared `EventAnnotations` carrier — the HO-8/9/10/11/13/16/17 closed-taxonomy-via-optional-field precedent, not a new event or trait method (`ask`/`confirm` already receive the raw `+remember` modifier). **DG-10 found to need no separate mechanism** — its recurrence/promotion pseudocode is entirely host-side over the host's own durable store plus the DG-9 provenance tag nodus already emits, the same "rides an existing signal" shape as this session's NL-9 dispatch and LP-17 `SETTLEMENT_UNACCOUNTED`. **`l2-nodus-environment` 1.0.0 → 1.1.0**: new §4.4.1 designs NE-14 as `EnvironmentProfile.token_measure: Option<String>` plus a pre-`env.open` fail-fast check, new code `NODUS:ENV_MEASURE_UNKNOWN` through the same `Diagnostic` channel LP-8's `validate_manifest` uses — deliberately not a new `ExtensionRole`, since an unidentified measure is a per-profile defect, not a missing host capability (a role would let the exact silent-substitution failure NE-14 forbids hide behind a satisfied capability check). `l2-nodus-errors` 1.2.0 → 1.3.0 registers the new code beside `CAPABILITY_UNMET`. `l2-nodus-portability`/`l1-nodus-portability` untouched — this pass's cross-references only. INDEX v1.0.77 → v1.0.78. Design only — nothing landed in `crates/nodus`; four items (LP-17, DG-9/DG-10, NE-14) are now task-authorable, NT-11 is not (nothing to task against a vacuous seam).

- 2026-07-31 **Decision:** Designed LP-17 settlement effect seam — new `l2-nodus-settlement.md` v1.0.0 (Stable), `Implements: l1-nodus-portability.md`, a dedicated new spec file rather than an `l2-nodus-portability.md` extension, mirroring the `l2-nodus-compensation`/`l2-nodus-restart` precedent (architecturally comparable in scope — a new provider trait — not a small `context`-extension like LP-16). **Self-corrected this session's own prior-turn `[DR]` prediction**, recorded honestly rather than silently adjusted: the sync-replan two cycles ago predicted LP-17 would follow LP-16's exact low-footprint pattern (reuse an existing DSL surface, extend `context`, add nothing structural); grounding against `main`'s `l1-value-settlement.md` (VS-1…VS-8) and the crate's own extension-point precedents found that right for the **decide** half only — `EffectClass::Settlement` + a new `SETTLE(payee, amount, purpose) → $target` command reuse the *already-shipped* LP-11 gate (`self.policy.evaluate("settlement", context)`) completely unchanged, since `context.args` already carries the three raw args positionally — but wrong for the **act** half: settling and returning a receipt needed a genuinely new `SettlementRail` trait + `NoopSettlementRail` built-in, since `PolicyProvider::evaluate` returns only `bool` with no channel for a receipt (VS-7), making `SettlementRail` the crate's first new extension-point trait since LP-11 itself. `Executor::handle_settlement` mirrors `handle_dialog`'s shape; both its exits (gate denial, unaccounted settlement) return bare `None`, so a denied/unaccounted `SETTLE` reaches NL-9's structural `@err:` dispatch (`l2-nodus-error-dispatch.md`, Phase 26) automatically — a real synergy between the two most recent phases found while designing this one. New `NODUS:SETTLEMENT_UNACCOUNTED` (`l2-nodus-errors` 1.1.2 → 1.2.0) and ninth `ExtensionRole::Settlement`, deliberately absent from `HostCapabilities::builtin()` (`Storage`/`Policy`/`Dialog` precedent). Decided **VS-6's payment-required handshake stays host/workflow-authoring territory, not a new nodus control-flow primitive** — an ordinary workflow already expresses "try the paid resource, on refusal `SETTLE`, then retry" with existing `?IF`/`~RETRY:n`. `l2-nodus-portability` 1.6.1 → 1.7.0 reconciles §3.1's LP-17 row (vacuous-in-core → **Realized by** the new spec, specified/awaiting implementation, the `l2-nodus-runtime` NL-22/NL-23 non-axis phrasing), its Leverage paragraph, and §5 (new task-authorable item 8). `l1-nodus-portability.md` needed no change — its §4.13 text already matched this realization exactly, verified by direct re-read. INDEX v1.0.76 → v1.0.77; 19 → 20 specs. Design only — nothing landed in `crates/nodus` this pass.

- 2026-07-31 **Decision:** Sync replan — **no new phase; the plan is saturated for a phase but not for a spec pass.** Resolved the expected spec→task `SYNC_GAP` (PLAN basis v1.0.75 → v1.0.76 — the only changed registry row is `l2-nodus-error-dispatch.md`'s own version bump 1.0.0 → 1.0.1, recorded during Phase 26's own spec-reconciliation task, not new spec content). Pre-flight otherwise fully clean: 19/19 Stable, 0 Draft, no orphan/phantom, all 19 file headers matching INDEX exactly. **Re-verified rather than assumed that NL-9 dispatch landing didn't newly unblock anything**: NL-9's `@err:` mechanism is entirely independent of LP-17/LP-20 (both are LP-11-gate specializations needing their own core vocabulary — a `settlement` shape and an `obligation`/`discharge` shape — neither touched by `@err:` dispatch) and independent of the three stale L2 tables / L1 taxonomy items already in the Backlog. The arc's two largest built-toward mechanisms (LP-11's gate, NL-9's dispatch) are both now realized, but neither's landing changed what's plannable elsewhere. PLAN v1.35.0 → v1.36.0, TASKS v2.28.0 → v2.29.0, INDEX v1.0.76 held. Computed next step (DA-6): `/magic.spec nodus`, **LP-17** (settlement effect seam) as the strongest candidate — L1 §4.13 already frames it as specializing the LP-11 gate the same way LP-16 did, so the identical low-footprint pattern is very likely to apply again, over LP-20 (a less-precedented shape) or the hygiene items (lower leverage).

- 2026-07-31 **Decision:** Phase 26 complete. NL-9's `@err:` handler dispatch implemented in `crates/nodus` exactly per `l2-nodus-error-dispatch.md`'s design. `execute_inner`'s main loop gained a structural eligibility check (`errors_before_this_step` captured before `run_step_with_retry`, compared after) — a `Signal`-free step that left a new `ctx.errors` entry behind populates `$error` via a new `error_to_value` helper, dispatches the declared handler through the ordinary `execute_command` path (reusing the triggering step's own `step_num`), then ends the run's step sequence. No new type, field, error code, or trait change — smallest production diff in the arc besides LP-16's. **Self-review reconciling §4.3/§4.5's pseudocode against the as-built code found two corrections**: the real parameter is `ast` not the spec's illustrative `wf`, collapsed into a let-chain (`clippy::collapsible_if`) with the unused `err_dispatched` flag dropped; and — a genuine finding while writing test coverage — `try_parse_command_from_string` returns `None` only for genuinely empty `@err:` text, never for unrecognized-but-non-empty text, so §4.5's "handler text that does not parse" framing was never really true (any non-empty text becomes `Some(CommandCall)` and dispatches harmlessly as an `UNKNOWN_COMMAND`). **Found and fixed one real regression beyond the spec's own anticipated blast radius**: `tests/control_flow.rs`'s pre-existing `retry_reruns_failing_step_up_to_bound` asserted "the step after an exhausted retry still runs" — true only because dispatch didn't exist yet; `l1-nodus-language.md`'s own `~RETRY:n` row has always said exhaustion "routes to `@err:`", so the old assertion was pinning exactly the gap this phase closes. Reviewed Phase 24's `policy_denies_model_call_effect`/`policy_denies_deferred_effect` (both use fixtures already declaring `@err:`) and confirmed their assertions still hold, not vacuously — no changes needed there. `l2-nodus-error-dispatch.md` 1.0.0 → 1.0.1, `l2-nodus-errors.md` 1.1.1 → 1.1.2, `l2-nodus-runtime.md` 1.3.1 → 1.3.2 all reconciled to Implemented. 452 tests pass (was 447; +5); clippy clean after one `collapsible_if` fix; fmt clean after auto-fix; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved); the one new production-path `.expect()` is structurally unreachable-as-`None`, proven by the guard condition immediately preceding it. INDEX v1.0.75 → v1.0.76; PLAN v1.35.0 held, TASKS v2.28.0 held (the established run-phase convention).

- 2026-07-31 **Decision:** **Phase 26 opened** — Uncaught-Error Handler Dispatch. Resolved the expected `ORPHANED_SPEC` (new `l2-nodus-error-dispatch.md`, authored Stable directly, never placed in a phase) and `SYNC_GAP` (PLAN basis v1.0.73 → v1.0.75). **Chosen over the standing default next step (LP-17/LP-20)**: those extend an already-realized gate with new capability, while `@err:` dispatch closes a real, long-standing correctness gap — `error_decl` parsed/validated/transpiled since early phases with zero call sites in `executor.rs`, and every fixture in the normative corpus already declares `@err: ESCALATE(human)` expecting it to fire. Verified rather than assumed before decomposing: read `run_step_with_retry` directly and confirmed a step succeeding via `~RETRY:n` truncates its own transient attempt-errors before returning, so retry-then-succeed structurally never reaches dispatch — no special-casing needed. Confirmed the eligibility rule is genuinely code-list-free by tracing control flow: `RULE_VIOLATION` returns `Signal::Break` (caught by an earlier match arm, never reaches the dispatch check) and `COMPENSATION_FAILED` fires only in the post-loop compensation drain (structurally unreachable from the main loop this phase attaches to) — so no hardcoded "dispatchable codes" list is needed or written anywhere. Track T sized larger than Phase 24/25's by design: beyond new coverage (dispatch fires, `$error` populated, no-handler/unparseable no-op, retry-then-succeed no-dispatch), it must review Phase 24's `policy_denies_model_call_effect`/`policy_denies_deferred_effect` (both use fixtures that already declare `@err:`) since this phase retroactively activates previously-inert behavior — named explicitly in the phase file's guardrails, not left for implementation time to discover. PLAN v1.34.0 → v1.35.0, TASKS v2.27.0 → v2.28.0, INDEX v1.0.75 held.

- 2026-07-31 **Decision:** Designed NL-9 `@err:` handler dispatch — new `l2-nodus-error-dispatch.md` v1.0.0 (Stable), `Implements: l1-nodus-language.md`. **Deliberately chosen over the previously-computed next step (LP-17/LP-20)**: those are new-capability extensions, while `@err:` dispatch is a genuine correctness gap — `WorkflowFile.error_decl` has been parsed/validated/transpiled since early phases but has zero call sites in `executor.rs`, and every single fixture in the normative corpus already declares `@err: ESCALATE(human)` expecting it to fire. **Core design insight, found by grounding rather than inventing a code list**: eligibility for dispatch falls out of the executor's existing control-flow shape — any `RuntimeError` a step returns with no `Signal` (today: `POLICY_DENIED`, `DIALOG_TIMEOUT`, `DIALOG_REJECTED`) necessarily reaches the main loop's catch-all match arm, since a `Signal`-returning error (`RULE_VIOLATION`'s `Signal::Break`) is already caught earlier; `COMPENSATION_FAILED` structurally cannot occur inside the main loop at all (it only fires in the post-loop compensation drain, itself reachable only after the run has already gone Failed/Aborted). No enumerated code list needed or maintained. `$error` — already `RESERVED`+`RUNTIME_OWNED` in `vocab.rs`, seeded to an empty `Value::Map` at context construction, never once written to by anything — is populated from the triggering error before the handler dispatches via the ordinary `execute_command` path (so LP-11's policy gate and LP-16's descriptors apply unchanged, and `@err: ESCALATE(human) → $handled`'s pipeline target — already parseable since Phase 20's arrow-splitting fix — binds correctly). The handler is the terminal action: the main step sequence ends afterward; `Status` computation is untouched (still `Partial` from `!ctx.errors.is_empty()`), so this is an additional observable side effect, not a reinterpretation of what `Partial` means. **Found and corrected two long-stale claims** in `l2-nodus-runtime.md` §3 while grounding this pass: NL-2's and NL-9's rows both named a `Status` variant that has never existed (`RuleViolation`, `Error`) — undetected until now because only §3.1 (post-v1.0.0 additions) had been repeatedly reconciled across this whole arc; §3's original rows had not. **Named explicitly, not hidden**: implementing this will retroactively activate `@err:` dispatch for nearly every existing fixture, so policy/dialog-denial tests will observe new behavior once built — the same conformance-fix pattern as Phases 17/20/21/23/24/25, not a regression. `l1-nodus-language` 1.14.0 → 1.15.0 (NL-9 amended at the concept level — it previously said nothing about dispatch mechanics), `l2-nodus-runtime` 1.3.0 → 1.3.1, `l2-nodus-errors` 1.1.0 → 1.1.1 (NL-9 row points to the new spec). INDEX v1.0.73 → v1.0.75; 18 → 19 specs.

- 2026-07-31 **Decision:** Phase 25 complete. `l1-nodus-portability` §4.12 (LP-16) implemented in `crates/nodus` exactly per the §4.10 design — the second phase in a row (after Phase 24) needing no plan- or implementation-time scope correction. `execute_command`'s existing LP-11 `context` literal gained a small loop over `["+reversible", "+external", "+value"]`, pushing each stripped-prefix pair from `cmd.modifiers` only when present — no new type, field, error code, or `PolicyProvider` signature change; the smallest production-code diff in the whole LP-11 arc. **Self-review reconciling §4.10.2's pseudocode against the as-built code found one divergence**: it named a separate `build_context` function for illustrative clarity, but the real call site builds the literal inline — corrected to match §4.9.3's own precedent of showing the real code. **No empirical surprise this cycle** — all 3 new integration tests (`risk_descriptors_reach_context_when_declared`, `undeclared_risk_descriptors_are_absent_from_context_not_defaulted`, `risk_descriptors_are_inert_without_a_policy_provider`) passed on first run, unlike Phase 24's reserved-variable-seeding catch, consistent with how thoroughly §4.10 was grounded before planning. `l2-nodus-portability` 1.6.0 → 1.6.1 reconciles §4.10/§3.1/§5 to Implemented/Done. 447 tests pass (was 444; +3); clippy clean; fmt found one line-wrap violation in the new tests (no logic change), fixed; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved); no `unwrap`/`expect`/`panic!` added to `executor.rs`'s production path. INDEX v1.0.73 → v1.0.74; PLAN v1.34.0 held, TASKS v2.27.0 held (per the established run-phase convention: only INDEX moves, PLAN/TASKS resync at the next `/magic.task`).

- 2026-07-31 **Decision:** **Phase 25 opened** — Effect Risk-Class Declaration. Resolved the expected spec→task `SYNC_GAP` (PLAN basis v1.0.72 → v1.0.73 from the LP-16 design pass). **Like Phase 24, no plan-time grounding correction was needed** — §4.10 had already settled every question a task needs: the descriptors ride the existing `+modifier=value` grammar with zero new DSL/AST/parser work, and `PolicyProvider`'s signature needs no change. Verified rather than assumed before decomposing: read the real `execute_command` code directly and confirmed `context` is an inline `Value::Map(vec![...])` literal, not the separate `build_context` function the spec's illustrative pseudocode names — the phase file's task Notes point at the real literal so implementation doesn't chase a function that doesn't exist. Checked `tests/portability.rs`'s existing fixtures (`MANIFEST_WF`, `DEFERRED_WF`, `AllowAllPolicy`/`DenyAllPolicy`) before planning Track T and confirmed none carries a `+reversible`/`+external`/`+value` modifier, so a new fixture is needed rather than assumed reusable. Track A is a single task, unlike Phase 24's three — no natural sub-seam to split along, since this phase adds no new type, no new `Executor` field, no new combinator, no new error code. PLAN v1.33.0 → v1.34.0, TASKS v2.26.0 → v2.27.0, INDEX v1.0.73 held.

- 2026-07-31 **Decision:** Designed LP-16 — `l2-nodus-portability` 1.5.0 → **1.6.0** (new §4.10). **Grounding found the realization is far smaller than L1's own pseudocode suggests**: `l1-nodus-language.md` §4.3 already gives every step `+modifier=value` decorators ("optional, repeatable", no closed name vocabulary — confirmed no `KNOWN_MODIFIERS` registry exists anywhere in `vocab.rs`), and `CommandCall.modifiers: Vec<(String,String)>` already carries them losslessly through parse→transpile→reparse since Phase 21's NL-6 quoting fix — so `reversible`/`external`/`value` need **zero new DSL grammar, lexer, parser, AST field, or transpiler work**. The only code change is a ~3-line extension to the LP-11 call site's existing `context` builder (§4.9.2), reading the three modifier keys off `cmd.modifiers` when present. **A second simplification, also found by grounding rather than assumed from L1's pseudocode**: L1 §4.12's `run_effect` shows the host computing `tier := host.tier_of(step.effect)` then a separate gate decision, but from nodus's side both collapse into the *already-shipped* `evaluate(gate, context)` — the host's own implementation decides tier-then-friction internally over the richer context, so `PolicyProvider`'s trait signature needs **no change at all**: no second method, no `Tier`/`Friction` type, no LP-6 break. Same for the failure path — `NODUS:POLICY_DENIED` is the same code (§4.9.4); LP-16 is a richer input to an existing decision, not a new one. **Found and fixed two stale surfaces dating to before LP-11 shipped**, both still describing `PolicyProvider` as if Phase 24 hadn't wired it: §2's Constraints bullet ("executor integration is deferred… once LP-3 is satisfied" — true only of `StorageProvider` now) and §4.5's module map ("interface only"). §3.1's LP-16/17/20 rows and its Leverage paragraph brought current: LP-11 landing unblocked LP-16 completely, but LP-17/LP-20 still need their own core vocabulary (settlement shape; obligation shape) before either can ride the now-open gate — landing the hook did not design for them. **Deliberately left open (§4.10.5), not pre-built**: whether `value=` should get closed-vocabulary validation (an advisory W-code) — no modifier name has a registry today, and singling out these three without evidence of real mistyping would be an unmotivated asymmetry. `l1-nodus-portability.md` needed no change — its §4.12 text already matched this realization exactly. Design only; nothing landed in `crates/nodus`. INDEX v1.0.72 → v1.0.73.

- 2026-07-31 **Decision:** Sync replan — **no new phase; the plan is saturated for a phase but not for a spec pass.** Resolved the expected spec→task `SYNC_GAP` (PLAN basis v1.0.71 → v1.0.72 — the only changed registry row is `l2-nodus-portability`'s own version bump 1.4.0 → 1.5.0, recorded during Phase 24's own spec-reconciliation task, not new spec content). Pre-flight otherwise fully clean: 18/18 Stable, 0 Draft, no orphan/phantom, all 18 file headers matching INDEX exactly. **Re-verified rather than assumed that LP-11 landing didn't newly unblock anything**: read `l1-nodus-portability.md` §4.1 directly instead of trusting the Backlog's summary of it — it still lists four roles (Model/Storage/Audit/Policy) against the crate's eight (`l2-nodus-portability` §4.2's own registry additionally carries Schema/Dialog/Environment/Config), so the taxonomy-reconciliation gap the LP-11 design pass flagged is still open and still design-blocked — an L1 amendment adding registry rows for roles already implemented, not a task. Every remaining Backlog item stays either vacuous-in-core, LP-3-adjacent-but-undesigned, or a genuine design fork; none has a Stable L2 Rust shape a task could cite in a concrete `Verify` line. PLAN v1.32.0 → v1.33.0, TASKS v2.25.0 → v2.26.0, INDEX v1.0.72 held. Computed next step (DA-6): `/magic.spec nodus`, **LP-16** (effect risk-class declaration) as the strongest candidate — §4.12 already describes it as "an optional descriptor to the LP-11 effect gate" the crate just realized, the shortest remaining design distance of any open item.

- 2026-07-31 **Decision:** Phase 24 complete. `l1-nodus-portability` §4.7 (LP-11) implemented in `crates/nodus` exactly per the §4.9 design, no plan- or implementation-time scope correction needed — the first phase in this arc where none was. `Executor::execute_command` now gates every `ModelCall`/`Deferred` effect through `self.policy.evaluate(gate, context)`, inserted after the existing `!!`-rule check and before the `ASK`/`CONFIRM` dispatch (one check covers both effect classes, since `Deferred` routes to `handle_dialog` afterward); denial emits `StepError`+`NODUS:POLICY_DENIED` (never `ConstraintHit`) and returns bare `None` — non-halting, `RunResult.status` degrades to `Partial`. `Executor` gained a fourth `policy: Box<dyn PolicyProvider>` field (default `NoopPolicyProvider`) across all six constructors, plus `with_policy`/`with_policy_and_audit` and `run_with_policy`/`run_with_policy_and_audit` mirroring `run_with_dialog`'s exact shape. **Testing caught a wrong assumption empirically, not by inspection**: the first draft asserted a denied step's pipeline target is *absent* from `RunResult.vars`; both denial tests failed immediately — reserved variables (`out`/`error`/`meta`/…) are always seeded to defaults (`Value::Null` for `out`) at context construction, so "unbound" means "still the seeded default", never "key absent". Fixing this also revealed the *permitted*-effect assertions had been vacuously true under the original `contains_key` form — a real strengthening, not just a fix. **Self-review before closing the spec-reconciliation task caught two stale doc comments in the shipped crate itself**, beyond the spec text: `portability.rs`'s module doc and `PolicyProvider`'s own trait doc still said "executor integration is deferred until LP-3 is satisfied" — both corrected. `l2-nodus-portability` 1.4.0 → 1.5.0 reconciles §4.9 (three `[REFERENCE]` blocks corrected to the exact as-built code: `as_str` → `as_gate_str`, the fuller denial `reason` string, "stays unset" → "stays at its seeded default"), §4.2/§4.4/§3.1/§5 item 2 to Implemented. 444 tests pass (was 435; +9: 4 `effect_class_of`/gate-string unit tests, 5 `run_with_policy` integration tests); clippy clean; fmt found one line-wrap violation in the new tests (no logic change), fixed; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved). **Corrected my own error mid-cycle**: initially bumped PLAN.md's and TASKS.md's own header version numbers during this run-phase completion; re-grounding against this file's own Phase-20-through-23 precedent showed no "Phase N Complete" entry ever bumps those headers — only `INDEX.md` moves during a run phase, with `PLAN.md`'s basis pointer (and TASKS.md's header) resyncing at the next `/magic.task` — reverted both to keep the convention intact. INDEX v1.0.71 → v1.0.72; PLAN v1.32.0 held, TASKS v2.25.0 held (per that same convention).

- 2026-07-31 **Decision:** **Phase 24 opened** — Per-Effect Authorization Call Site. Resolved the expected spec→task `SYNC_GAP` (PLAN basis v1.0.69 → v1.0.71 from the LP-11 design pass). **Unlike every prior phase in this workspace, plan-time grounding found nothing to correct or widen** — the preceding spec pass (`l2-nodus-portability` §4.9) had already settled every question a task needs a concrete `Verify` line for: `EffectClass{ModelCall,Deferred}` (`ToolUse` deliberately vacuous), the `gate`/`context` shape, the `execute_command` integration point, the `NODUS:POLICY_DENIED` signal via `StepError` (not `ConstraintHit`), non-halting `None`-return semantics, and the `run_with_policy`/`run_with_policy_and_audit` combinators. This is the payoff of the two-cycle arc: v1.30.0 called the call site task-authorable while still ungated → v1.31.0 opened the gate but left the design undone → v1.32.0 designed it → this cycle plans it clean. Verified rather than assumed before decomposing: confirmed `MODEL_COMMANDS`/`DIALOG_COMMANDS` already exist in `portability.rs` (no new constants beyond the classification function itself), confirmed `DialogOutcome::Timeout`/`::Rejected`'s exact push-then-`None` shape as the concrete precedent T-24A03 codes against, and confirmed `run_with_dialog`'s exact combinator signature as the template. Track A split into three atomic tasks along the design's own natural seams (type/classification/error-code; `Executor` field/constructors; gate/combinators) rather than one large task, matching Phase 23's 6-task/3-track shape — a single sprawling task would have made its own `Verify` line unwieldy. Five guardrails written into the phase file, each restating a decision the spec pass already made so implementation can't silently drift from it: no `ConstraintHit` reuse, no `Signal::Skip`/`Signal::Break` on denial, raw unresolved args in `context`, exactly two `EffectClass` variants (no `ToolUse` placeholder), and `NoopPolicyProvider` as every existing constructor's default (with an explicit regression test required, not just "existing tests still pass"). PLAN v1.31.0 → v1.32.0, TASKS v2.24.0 → v2.25.0, INDEX v1.0.71 held.

- 2026-07-31 **Decision:** Designed the LP-11 call site — `l2-nodus-portability` 1.3.1 → **1.4.0** (new §4.9), `l1-nodus-portability` 1.14.0 → **1.14.1**, `l2-nodus-errors` 1.0.0 → **1.1.0**. **`EffectClass` realized narrower than L1's own three-way sketch**: `ModelCall`/`Deferred` reuse `MODEL_COMMANDS`/`DIALOG_COMMANDS` (already load-bearing in `CapabilityManifest::from_workflow`); `ToolUse` left **vacuous** — every `tool`-shaped builtin command (`FETCH`, `WRITE`, `GIT`, `NOTIFY`, …) is, on inspection of `executor.rs`, a fixed zero-dependency stub with no host-swappable seam behind it, so gating one would authorize nothing real, the same shape as the §3.1 vacuous-in-core entries. `gate`/`context`: the DSL has no syntax for a step to declare a named gate, so `gate` is the effect-class string and `context` a raw pre-resolution `Value::Map` snapshot. **Corrected a real defect in §4.4's own doc comment**, found on inspection rather than assumed: it claimed the denial reuses `ConstraintHit { halt: false }`, but that variant's own doc comment scopes it to hard `!!`-constraints and `l2-nodus-errors.md` states the `RULE_VIOLATION` path is "unchanged" by any later spec — reusing it would have conflated NL-2 and LP-11 under one event type. Real mechanism: new `NODUS:POLICY_DENIED` code via the existing `StepError` event (no new `ExecutionEvent` variant), `ctx.errors.push`, bare `None` return — mirroring `DialogOutcome::Timeout`/`::Rejected`'s non-halting precedent, not `Signal::Skip` (absorbed as `None` at top level anyway) or `Signal::Break` (would make every denial fatal). **A genuine, previously-unflagged finding surfaced while grounding the denial signal**: `WorkflowFile.error_decl` (`@err:`) has **zero call sites in `executor.rs`** — parsed, validated, transpiled, never dispatched. L1 §4.7's `route_to(@err)` described an unbuilt mechanism; swept the whole file for `@err` rather than assuming §4.7 was the only site and found the identical stale shorthand in three more `[REFERENCE]` blocks (§4.10 LP-14, §4.12 LP-16, §4.13 LP-17) — corrected all four identically. Also corrected `l2-nodus-errors.md`'s own NL-9 compliance row, which had claimed both `@err:` dispatch and `UNHANDLED_ERROR` emission were built; neither is (`UNHANDLED_ERROR`'s constant exists only inside a validator warning string). `run_with_policy`/`run_with_policy_and_audit` specified mirroring `run_with_dialog`'s exact shape. §5 item 2 closed through its third and final state (error → permitted-not-designed → permitted-and-designed). Design only — nothing landed in `crates/nodus` yet. INDEX v1.0.69 → v1.0.71.

- 2026-07-31 **Decision:** Phase 23 complete. `l1-nodus-portability` §4.1/§4.11 (LP-2/LP-15) conformance implemented in `crates/nodus`: `InMemoryStorageProvider` (`Mutex<Vec<(String, Value)>>`, insert-or-overwrite `store`, clone-on-`load`, poisoned-lock recovery via `unwrap_or_else(|poisoned| poisoned.into_inner())`) replaces `NoopStorageProvider`, whose discarding `store`/always-`None` `load` satisfied neither the L1's in-memory built-in mandate nor LP-2's in-process-sufficiency requirement; `lib.rs` re-export and module docs updated to match. `tests/portability.rs`'s Phase-5 contract test — which had deliberately asserted the old no-op contract — split: the storage half became 4 dedicated tests (round-trip, overwrite, absent-key, instance-isolation), the policy/schema half kept as `noop_policy_and_schema_compile`; 3 equivalent unit tests added in-crate. `l2-nodus-portability` 1.3.0 → 1.3.1 reconciles Overview/§4.2/§4.3/§3.1/§5 to the as-built fix. **Caught two live references during self-review that the initial reconciliation pass missed** — §3's LP-2 compliance row and §4.5's module-additions map both still named `NoopStorageProvider` after the rest of the document had moved on; fixed in the same version bump rather than left as a second stale surface for a future cycle to find, which is exactly the failure mode the last several phases' spec-reconciliation tracks exist to close. T-23C02 confirmed correctly Cancelled at plan time — nothing left for it to do, its target already reached by the prior spec pass from the opposite direction. 435 tests pass (was 429; +6); clippy/fmt clean; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved); the one new `lock()` call recovers a poisoned guard rather than unwrapping (project rule). PLAN v1.31.0 held, TASKS v2.23.0 → v2.24.0, INDEX v1.0.69 → v1.0.70 (spec touched mid-phase, Phase-17/20 Track-C precedent).

- 2026-07-31 **Decision:** Sync replan — **no new phase; Phase 23 stays open and unstarted.** Resolved the expected spec→task `SYNC_GAP` (PLAN basis v1.0.68 → v1.0.69). **The substantive act was cancelling a task whose premise its own predecessor removed.** T-23C02 was planned to correct `l2-nodus-portability` §5 item 2 by *demoting* the LP-11 call site to "LP-3-gated"; the 1.3.0 pass corrected the same text from the **opposite direction** by opening the gate. Marked **Cancelled (superseded), not Done** — the outcome it described was reached, but not by this phase, and recording it Done would credit Phase 23 with another cycle's work; the phase file now says which cycle actually did it. The correction survives in sharper form: the LP-11 call site is not *ungated* but **permitted-and-undesigned** — an ordinary L2 gap, not a governance block. Queued it as the next spec target with the four things that pass must settle, one of which is a **real contradiction between the layers rather than a mere omission**: L2 §4.4 has the executor emit `ConstraintHit { halt: false }` and **skip** the denied step, while L1 §4.7 sketches a typed `NODUS:POLICY_DENIED` routed to `@err:`. Skip-and-continue and a catchable typed error are different observable behaviours, so the pass must choose rather than average — and the choice propagates to LP-16/LP-17/LP-20, which inherit the same ordering. Also confirmed Phase 23's five live tasks are genuinely unaffected rather than assuming it: `StorageProvider`'s own admission record (§4.8.2) remains **unsatisfied** (one context — `store-local` is a provider, not a second *decision shape*), so its hook points stay held exactly as the phase planned, and the built-in conformance fix was never gated in the first place. Separately recorded as RESOLVED in the Backlog: the repo-wide **SDD reference containment sweep** — a single leak found while grounding the LP-3 pass proved systemic (121 occurrences / 44 files / 8 crates), cleared across 55 files and 138 lines with zero residual references, invariant IDs and bare `§` refs deliberately kept since §6 forbids neither; gates green (`cargo check --workspace`, `fmt --all --check`, `clippy -p nodus -D warnings`, 429 nodus tests). Two facts from it are worth carrying: **16 leaks sat inside `crates/nodus`**, the crate built for standalone extraction, where §6's harm is concrete rather than theoretical; and several were **introduced by the Coder role itself** in Phases 21–22 despite §6's write-time prohibition — evidence that write-time enforcement does not hold, which together with §6 naming no remediation owner is what the engine bug report records. PLAN v1.30.0 → v1.31.0, TASKS v2.22.0 → v2.23.0, INDEX v1.0.69 held.

- 2026-07-31 **Decision:** Opened the LP-3 gate — `l1-nodus-portability` 1.13.0 → **1.14.0** (new §4.14 LP-3 Admission Evidence) and `l2-nodus-portability` 1.2.0 → **1.3.0** (new §4.8, the first admission records). **The diagnosis reframed the problem: LP-3 was not merely unsatisfied, it was *unfalsifiable*.** It named a threshold ("two independent host contexts") without ever defining what a host context is, what makes two independent, or what evidence fixes the judgment — so a seam could sit behind it indefinitely with nothing stating what would open it. That is exactly what happened: `PolicyProvider` and `StorageProvider` shipped as re-exported public API no code path consults, and LP-11's absent call site silently blocked LP-16/LP-17/LP-20 across multiple planning cycles while every participant, including me last cycle, argued about the gate's *status* rather than its *criteria*. §4.14 defines a host context by **what it decides and how it fails** rather than by ownership (two entry points of one project are two contexts when they exercise the seam differently; two separately-owned projects consuming it identically are one — the unit is the decision shape, because that is what the abstraction must span), and names **documented divergence, especially on fail direction, as the strongest independence signal**, since a seam that must span an existing disagreement is demonstrably not the single-context optimisation LP-3 exists to reject. **Applied, it yields two different dispositions — which is the gate working, not a half-finished pass.** `PolicyProvider`: **SATISFIED**, and the independence evidence is the host's own rather than my reading — `crates/domain/src/tool_security.rs` (runtime guard, every tool call evaluated pre-execution against severity-ranked signature rules) and `crates/domain/src/hooks.rs` (nine `HookEvent` kinds, parallel evaluation, deny-if-any-`Block`) are the two realizations the Stable main `l1-interception-model` was *written to unify*, its own Document History citing "the currently-divergent fail-behaviours of the plugin-hook and tool-security realizations" (INT-3). LP-3's second half was already met by construction: `evaluate(gate: &str, context: &Value) -> bool` names only `&str` and nodus's own `Value`, so no host type (`HookEvent`, `tool_security::Severity`, `contract::*`) appears and the interface needs no change. `StorageProvider`: **NOT SATISFIED** — `crates/store-local` is a provider, not a second *decision shape*, and the missing evidence is named precisely (a second integration persisting workflow state under a different durability/locality contract, the DN-3 local-vs-remote axis LP-15 already cites), so its `STORE`/`LOAD` hook points stay held while the separate LP-15 **built-in conformance** fix remains ungated, touching neither hook points nor run-parameter variants. **Corrected last cycle's §5 item 2 from the opposite direction to the one Phase 23 planned**: rather than demoting the LP-11 call site to "gated", the gate is now genuinely open — but the seam is **permitted, not yet designed**, since no L2 defines its Rust shape (effect-class notion, `gate` derivation, and a real reconciliation needed between L2 §4.4's `ConstraintHit { halt: false }` denial and L1 §4.7's typed `NODUS:POLICY_DENIED` — these differ). §5's closing sentence rewritten to separate the three states it had collapsed into one: permitted-and-designed, permitted-not-designed, not-permitted. Advisory finding I could not act on (write permission is `.design/` only): `crates/core/src/model_bridge.rs:1` carries an SDD reference leak — its module doc cites `l1-model-runtime §4.1, MR-2`, a spec filename in a product file (§6). INDEX v1.0.68 → v1.0.69.

- 2026-07-30 **Decision:** **Phase 23 opened** — Built-in Durable-State Conformance; resolved the expected spec→task `SYNC_GAP` (PLAN basis v1.0.67 → v1.0.68). **Plan-time grounding overturned one of the two items the prior spec pass promoted as task-authorable, and that correction is this cycle's substantive finding.** Last cycle's §5 item 2 named the LP-11 `PolicyProvider` call site the highest-leverage next step. It is **LP-3-gated**, confirmed four independent ways — §3's LP-3 row ("`StorageProvider` and `PolicyProvider` are held here pending that gate"), §2's Constraints ("executor integration (hook points, run-parameter variants) is deferred… once LP-3 is satisfied"), §4.4's own heading ("wiring pending LP-3"), and a standing PLAN Backlog line that **predates the spec pass entirely**. LP-3 is a *governance* gate: it requires a `/magic.spec` amendment documenting two independent host-usage contexts, which no atomic task can produce — so LP-16/LP-17/LP-20 remain blocked behind it too. **I wrote that overstatement myself last cycle by omitting a gate the same document asserts three times**; corrected in Track C rather than planned against, the Phase-18 `$restart` precedent (queue the spec fix, don't HALT and don't silently implement against wrong text). **The second item survived the gate** precisely because it touches neither hook points nor run-parameter variants — the scope §2 defers. Two Stable L1 statements mandate it: §4.1's Storage row (amended v1.8.0 by LP-15) specifies an **in-memory** built-in, and LP-2 requires every built-in be "sufficient for in-process testing without I/O"; the crate ships `NoopStorageProvider`, which discards on `store` and returns `None` on `load`, so it cannot round-trip even within one invocation. The L1's distinction is deliberate rather than sloppy — a discarding *audit* built-in is fine because audit is write-only, whereas a discarding *store* breaks the read-back that makes a store a store. Spec-ahead-of-code delta (the L1 moved in v1.8.0; the Phase-5 built-in did not), C12.1 in shape like Phases 17/20/21/22. Three guardrails written in from grounding, each against a plausible-but-wrong implementation: the existing Phase-5 test `noop_storage_and_policy_compile` **deliberately asserts `load(...).is_none()`** and so encodes the old contract — it must be updated knowingly, not hit as a mystery failure; interior mutability behind `&self` needs `std::sync::Mutex`, whose `lock()` returns a `Result` that must **not** be `unwrap()`ed on a production path (project rule) — recover the poisoned guard instead; and LP-2's "exactly one built-in per interface" forbids adding an in-memory provider *alongside* the no-op, making this a replacement and therefore a public-API rename (acceptable at `0.2.0`, since the `cargo semver-checks` gate lands at 1.0.0). Deliberately **not** in scope: the L1 §4.11 `put`/`get`/`delete` vs as-built `store`/`load` naming divergence — a second breaking change with no conformance gain. PLAN v1.29.0 → v1.30.0, TASKS v2.21.0 → v2.22.0, INDEX v1.0.68 held.

- 2026-07-30 **Decision:** Authored `l2-nodus-portability` 1.1.0 → 1.2.0 — new §3.1 reconciling the twelve deferred obligations LP-9…LP-20, each of whose L1 Document History entries names *this* spec as carrier. **Deliberately did not reuse the Phase-19 §3.1 shape unchanged.** That precedent's binary *Realized / Pending* axis would have misreported all twelve: calling LP-9 "Pending" implies the core owes code, when nodus has **no bundle-load path at all**, so the verify-before-load hook has no call site to attach to. Grounding each invariant against the crate instead produced a **four-way** axis, and the distinction that carries the pass is between a seam whose *gated construct* is absent (nothing owed) and one that **ships as re-exported public API but is never consulted**: `PolicyProvider::evaluate` and `StorageProvider::store`/`load` have **zero call sites** (verified by grep across `src/`; the two `.evaluate(` hits are `EnvironmentProvider`'s unrelated grading method). A host implementing either today observes no behaviour change — that is a defect, not deferred work, and the crate already has a name for the state ("interface-declared, wiring pending", `environment.rs:19`). Classification: **satisfied structurally** LP-10 (discharged by absence of vocabulary — no policy-writing command, `CapabilityManifest` exposes only declare/read, no grant/relax — and load-bearing in four sibling specs while having had no row here); **vacuous in core** LP-9/12/13/14/16/17/19/20; **seam declared, wiring pending** LP-11/LP-15; **partially realized** LP-18 (rule (c) discharged via NE-3/NE-7/NE-10; (a)/(b) not, since `EnvironmentProvider::open` returns `Instance` not `Result` and `ResumeDescriptor` carries no environment identity — closing them is an LP-6 **major** change, so it needs an L2 decision before it can be planned). **Two structural findings shrink the apparent backlog:** LP-11's absent call site alone blocks LP-16, LP-17 **and** LP-20, so four invariants reduce to one hook in `execute_command`; and LP-13 → LP-9 → LP-12 are ordered stages of one absent import mechanism, not three independent gaps. Named a real L1 divergence rather than papering it: L1 §4.1 specifies an *in-memory* built-in store, the crate ships `NoopStorageProvider`, which never round-trips within an invocation (also `put`/`get`/`delete` vs `store`/`load`, no `delete`). **Also corrected three stale surfaces found while grounding, all disagreeing with the code and with each other**: §4.2 registry (5 roles → 8, adding `Dialog`/`Environment`/`Config`, plus a *Wired* column, and un-marking `SchemaProvider` pending when §5 already called it delivered), §4.7's `ExtensionRole` reference enum (5 → 8) and its built-in-host prose (3 → 5 roles, with `Dialog`'s deliberate exclusion explained), and `from_workflow`'s derivation rules. Flagged for L1 reconciliation rather than fixed unilaterally: L1 §4.1 calls itself "the authoritative extension point registry" yet lists four roles against the implementation's eight. Separately discharged the correction queued at Phase-22 planning — `l2-nodus-testing` 1.2.0 → **1.2.1**, §10.4(a)'s "reproduces the body in the form the author wrote it" corrected to token-sequence-not-line-breaks, with the note that re-emitting line breaks would *break* NL-6 rather than improve it. INDEX v1.0.67 → v1.0.68.

- 2026-07-30 **Decision:** Sync-only replan — **no new phase.** Pre-flight fully clean (INDEX v1.0.67 = PLAN basis, 18/18 Stable, 0 Draft, no orphan/phantom, all 18 headers matching, **zero warnings**). The NL-6 conformance arc (Phases 17/20/21/22) is closed, and unlike every prior cycle in it, no candidate had been pre-identified — so rather than re-checking a known item, this cycle ran a **systematic invariant-traceability audit**: every L1 invariant range compared against its L2 realization spec's compliance table. **That found four stale L2 tables covering sixteen invariants, and proved the Backlog itself materially undercounted them.** `l2-nodus-portability` §3 stops at **LP-8** while `l1-nodus-portability` now defines through **LP-20** — twelve missing, where the Backlog had recorded only "LP-17/LP-18/LP-19", i.e. three of twelve, silently omitting LP-9…LP-16 and LP-20. Two of the omitted are already load-bearing elsewhere despite having no LP-side row: **LP-10** (host-granted authority, never self-authored) is cited by NL-14's criteria immutability, NL-17's no-self-upgrade, NL-20's DC-10 and NL-21's declassification authority; **LP-11** is the `decide → effect → observe` gate that `l2-nodus-compensation` §2 already records as vacuous-in-core. Also found: `l2-nodus-testing` stops at NT-10 while the L1 defines **NT-11** (differential parity, added v1.1.0 with a §4.7 harness) — absent from the table, from its Related-Specifications line, and from the crate; `l2-nodus-dialog` stops at DG-8 while the L1 defines **DG-9/DG-10**; `l2-nodus-environment` stops at NE-13 while the L1 defines **NE-14**. Only `l2-nodus-runtime` and `l2-nodus-observability` are complete — and `l2-nodus-runtime`'s completeness is itself the precedent, since the Phase-19 spec pass closed exactly this gap for it by adding §3.1. **None is plannable as a phase**: each is an L1 invariant with no L2-specified Rust shape, and eight of the twelve LP items are explicitly *seams* likely to resolve as vacuous-in-core host obligations (the Phase-15 streaming / Phase-19 NL-22 precedent) — which is a spec-time judgment, not a task-time one. Grounded rather than assumed for the candidate that looked closest to plannable: NT-11 requires two paths that **both** satisfy the same manifest and produce equal observable results, whereas the existing LP-3 two-host test compares a satisfying host against a *non-satisfying* one and asserts they **diverge** (Ok vs Failed) — the manifest gate, not a parity gate; and nodus has no transpiled execution path (the transpiler emits source text, not an alternative executor), so NT-11's first form is genuinely absent in core. Computed next step (DA-6): `/magic.spec nodus`, `l2-nodus-portability` first as the largest coherent cluster. PLAN v1.28.0 → v1.29.0, TASKS v2.20.0 → v2.21.0, INDEX v1.0.67 held.

- 2026-07-30 **Decision:** Phase 22 complete. `l2-nodus-testing` §10 / NL-6 whole-file round-trip closure implemented in `crates/nodus`: added `Transpiler::nodus_braced_raw_body` (re-quotes every `raw_lines` token except literal `{`/`}`, which must stay unquoted since `collect_braced_raw_lines`'s depth counter matches token *type*, not value — every other separator, including `:`, is matched by `parse_test_body` purely on string value, so quoting it changes nothing there); inverted `@test:` emission to check `!raw_lines.is_empty()` first; added `Validator::w015_test_pair_separator`, deliberately mirroring `parse_test_body`'s own section-tracking loop so a legitimately-consumed `key : value` triple's value token is never re-examined as a would-be key (a real bug in an early draft, caught before it shipped: treating the colon itself as a candidate key produced a false positive). Added `@macro:` and `human_mode` emission — both previously **never emitted at all**. Widened `full_corpus_ast_equal_after_compact_round_trip` from `.steps` to `ast1 == ast2`: now green across the whole 11-fixture corpus, the phase's single acceptance signal. **One planning assumption was corrected during implementation rather than left to surface as a test failure**: T-22B01 was planned assuming `@macro:`'s braced form; verifying directly against the real corpus fixture (`macro_expand.nodus`) before writing the "official" unit test showed it actually uses the non-braced `@macro: name` form, whose indented body line `parse_macro_block` never captures at all (`collect_braced_raw_lines` returns empty without an immediate `{` — macro body expansion is a separately deferred, unimplemented feature, not something this phase touches). `MacroBlock.raw_lines` is therefore empty for every macro in the corpus today; the braced-body emission path was still implemented for completeness (reusing the `@test:` renderer directly, no fork) but has no fixture exercising it — recorded in the Backlog rather than left implicit, since an implemented-but-untested path is exactly the kind of gap this whole conformance arc exists to catch. Per-task sensitivity checked empirically, not assumed: temporarily disabled `@macro:` emission and confirmed the widened harness failed with precisely the predicted diff (`macros: [MacroBlock{..}]` vs `macros: []`), then restored it. 429 tests pass (was 420; +9: 3 `@test:` round-trip + 3 `W015` validator + 3 macro/human_mode round-trip); clippy/fmt clean (1 fmt finding auto-fixed); zero new dependency (LP-1 preserved). PLAN v1.27.0 → v1.28.0, TASKS v2.19.0 → v2.20.0, INDEX v1.0.67 held (no spec files touched this phase).

- 2026-07-30 **Decision:** **Phase 22 opened** — Whole-File NL-6 Round-Trip Closure; resolved the expected spec→task `SYNC_GAP` (PLAN basis v1.0.66 → v1.0.67 from the 1.2.0 spec pass). **Plan-time grounding widened the phase materially beyond the spec pass's own subject.** The spec cycle specified the `@test:` fix and named its acceptance signal as widening Phase 21's corpus harness from `.steps` to the whole `WorkflowFile` (§10.5) — so before planning to that signal, I audited **every** `WorkflowFile` field against `to_nodus` rather than assuming `@test:` was the only thing standing between here and it. It is not: `macros` and `human_mode` are **never emitted at all** (zero references to either in `transpiler.rs`), and both hit normative corpus fixtures — `macro_expand.nodus` carries `@macro: greet`, `ticket_triage.nodus` carries a `;; HUMAN MODE` block. Eleven of fourteen fields round-trip today (nine pre-existing, `steps`+`comments` from Phase 21); this phase closes the remaining three. All three violate the same already-Stable NL-6 mandate, so none needs a new spec — `tests` additionally has its Rust shape in §10, while `macros`/`human_mode` reuse the same `raw_lines` mechanism (`parse_macro_block` even calls the identical `collect_braced_raw_lines` helper). **Including them is what the stated acceptance signal requires, not scope creep** — fixing `tests` alone would leave the phase unable to demonstrate its own goal; the Phase-21 precedent where `WorkflowFile.comments` and modifier-value quoting were outside the original enumeration but blocked every fixture. Three guardrails written in from grounding, each against a plausible-but-wrong implementation: emitting from `raw_lines` produces a **flat single-line body** and must not be "improved" to multi-line, since `collect_braced_raw_lines` drops `Newline` tokens and a re-render would change `raw_lines` on re-parse — breaking the very equality the task establishes; quoting must **skip the structural set** (`raw_lines` holds token values with no type information, so quoting `:`/`{`/`}`/`[`/`]`/`,` would break the triple-scan); and `human_mode` **must be emitted last**, because the parser routes a `HUMAN MODE` comment into `collect_comment_block()`, which greedily consumes every following Comment token and would silently absorb free-standing comments — corrupting `comments`, the field Phase 21 just fixed. Also found one inaccuracy in the spec I authored last cycle and flagged rather than acted on: §10.4(a)'s "reproduces the body in the form the author wrote it" overstates — `raw_lines` reproduces the token *sequence*, not the line breaks. The AST-equality conclusion is unaffected, so it is queued for a patch-level correction on the next `/magic.spec` touch (the Phase-18 `$restart` precedent) rather than HALTing or silently implementing against inaccurate text. PLAN v1.26.0 → v1.27.0, TASKS v2.18.0 → v2.19.0, INDEX v1.0.67 held.

*(Phases 1–21 and their decisions: see `CHANGELOG.md` and `RETROSPECTIVE.md` — permanent record, trimmed here to keep this file under its line cap.)*

## Blockers

## Blocking Constraints

## Session Continuity

**Last Session Ended:** 2026-06-24
**Handoff File:** none
**Bootstrap Mode:** false
