# Project State

<!-- STATE.md — live project memory. Read FIRST in every workflow session. -->
<!-- Maximum 100 lines. Agent updates AFTER each completed action. -->

**Workspace:** nodus
**Updated:** 2026-09-05 14:07
**Phase:** — plan saturated: phases 1–33 Done, no active phase
**Status:** Active

## Current Position

- **Task:** T-33A01, T-33B01, T-33T01 (config-dup, macro-dup, tests: 490 passing, was 486)
- **Spec:** INDEX v1.0.97 / PLAN v1.46.0 / TASKS v2.39.0 — all three in sync, pre-flight clean except the standing `DESIGN_DEBT_PENDING`. RULES v1.6.0 (unchanged, confirmed). 20 nodus specs Stable, 0 Draft, Phase 32 `Done (Archived)`, `tasks/` empty.
- **Next Action:** No mechanical work remains in nodus. Pre-flight's sole warning is the standing DESIGN_DEBT_PENDING (3 parked Backlog items), and re-running /magic.task nodus is a confirmed no-op: PLAN v1.49.0 = INDEX v1.0.100, 20/20 Stable, 0 Draft, phases 1-33 all Done. All three Backlog items are blocked on a design decision rather than effort — the Backlog's own rule is that they advance via /magic.spec nodus only once a real need motivates one, never on backlog-clearing grounds. The next step is a direction chosen by a person.

## Progress

```
Overall: [33/33] ████████ 100%
Build phases 1–32 Done (… → DG-11 Authoring Advisories → Pinned-Generation Digest → NE-12/HO-20 Digest Unification); workflow_digest naming collision closed end to end | Baseline gates: cargo 486 tests + clippy + fmt; LP-1 zero-dep preserved
```

## Recent Decisions

<!-- Last 3-5 locked decisions. Older entries are dropped (not archived) — see PLAN.md / CHANGELOG.md for phase history. -->

- 2026-09-05 **Decision:** Phase 33 complete. Provides: ConfigReason::DuplicateField + pre-accept-loop duplicate scan closing a real §config secret-leak path; e020_no_duplicate_macro_names modelled on e015; NL-27 verdict Pending -> Partially realized (l2-nodus-runtime 1.5.1); l2-nodus-registries 1.1.1 re-confirms Vacuous. 490 tests pass (was 486). All 4 tasks Done.
- 2026-09-02 **Decision:** Phase 32 complete — NE-12/HO-20 digest unification implemented in `crates/nodus` exactly per `l2-nodus-environment.md` §4.4 v1.2.0's design, no scope correction (second such clean phase in a row, after Phase 31). `digest_ast` (`executor.rs`) widened `fn` → `pub(crate) fn`; `EnvRunResult::candidate()` (`environment.rs`) now matches `Parser::parse(workflow_source)` — `Ok(ast)` hashes `digest_ast(&ast)`, `Err(_)` falls back to `digest_source(workflow_source)` exactly as before, signature unchanged. Grepped every `.candidate(` call at planning time (one production site, three tests, none tied to the old algorithm's specific values) predicted zero test breakage; confirmed on the first `cargo test` run. Added `candidate_digest_agrees_with_repro_recipe_digest` (integration, `tests/environment.rs`, new `ManifestCapture` `AuditProvider` mirroring Phase 31's `tests/dialog.rs` precedent exactly) — the test that makes the unification real: it proves `CandidateResult.workflow_digest` and the independently-built `ReproRecipe.workflow_digest` agree for the same run, the two construction sites never being cross-checked in production code — and `candidate_digest_falls_back_to_source_hash_when_unparseable` (unit, `environment.rs`), pinning the fallback value the pre-existing unparseable-input test never asserted on. `l2-nodus-environment.md` 1.2.0 → 1.3.0, `l2-nodus-portability.md` 1.11.0 → 1.11.1 (§3.2 cross-reference updated to the implemented version). 486 tests pass (was 484, +2); clippy/fmt clean; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved); no `unwrap`/`panic!`/`expect` added to any production path. Double-checked both touched files' `INDEX.md` cells this time (the prior spec pass missed one of three). INDEX v1.0.96 → v1.0.97; PLAN v1.45.0 held, TASKS v2.38.0 held (established run-phase convention). Plan Complete again — the `workflow_digest` naming collision is closed end to end (designed and built); Backlog's remaining items (LP-21/LP-23/NL-24/NL-25, NL-26) are unaffected.
- 2026-09-02 **Decision:** Phase 31 complete — `ResumeDescriptor.workflow_digest: String` (LP-22(c)) implemented in `crates/nodus` exactly per `l2-nodus-dialog.md` §4.3 v1.3.0's design, no scope correction (first such phase since Phase 27). One field, one construction site (`executor.rs`), populated by `digest_ast(ast)` — the identical computation `ReproRecipe.workflow_digest` already makes a few lines below it, not a new hashing scheme. Grepped every `ResumeDescriptor` reference at planning time (two integration tests read fields by name, none construct or exhaustively match it) predicted zero fallout beyond the one call site; `cargo check` confirmed it on the first pass. Added `resume_descriptor_workflow_digest_matches_digest_ast` (unit, `executor.rs`) and `resume_descriptor_digest_agrees_with_repro_recipe_digest` (integration, `tests/dialog.rs`, new `ManifestCapture` `AuditProvider` mirroring `tests/observability.rs`'s `RecordingProvider` shape) — the latter is the test that makes the pinning claim real: it proves `ResumeDescriptor.workflow_digest` and the independently-built `ReproRecipe.workflow_digest` agree for the same paused run, since the two construction sites are never cross-checked in production code. `l2-nodus-dialog.md` 1.3.0 → 1.4.0, `l2-nodus-portability.md` 1.9.0 → 1.10.0 (LP-22 (c) → Implemented in both; LP-22 overall stays Partially realized — closing (c) leaves (a)/(b) unaffected). 484 tests pass (was 482, +2); clippy/fmt clean; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved); no `unwrap`/`panic!`/`expect` added to any production path. INDEX v1.0.94 → v1.0.95; PLAN v1.43.0 held, TASKS v2.36.0 held (established run-phase convention). Plan Complete again — Backlog's remaining items (LP-21/LP-23/NL-24/NL-25, the `workflow_digest` naming-collision finding) are unaffected.
- 2026-09-02 **Decision:** `/magic.spec nodus` — LP-22(c) designed, and the v1.41.0 "LP-22(c)+NL-26 share one ResumeDescriptor decision" framing retracted as wrong, not merely restated. Ground "resume" directly against source before designing anything: every `run`/`run_with_*` signature takes only `source`/`filename`/`input` plus one provider parameter; `paused_at` (`executor.rs`) is written at suspension and never read back by any function. **No nodus function has ever consumed a `ResumeDescriptor`** — the v1.41.0 question ("does nodus need a resume entry point") was backwards; resume was always host-orchestrated re-invocation of `run` from the top, with a `DialogProvider` recognizing an already-answered prompt via DG-9 memoization (§4.7) standing in for literal continuation. That single fact corrected two standing overclaims: `l2-nodus-dialog.md` §3's DG-4 row said re-invocation "continues deterministically" and §4.3 said the host "re-supplies" the descriptor — to nothing, since no function accepts one; `l2-nodus-portability.md` §3.2's own LP-22 row said a resume "re-enters whatever that name now resolves to" — no such resolution exists in the crate. Designed LP-22(c) on the struct that owns it (`l2-nodus-dialog.md` §4.3, not `l2-nodus-portability.md`): `ResumeDescriptor.workflow_digest: String`, reusing the *exact* `digest_ast` computation `ReproRecipe.workflow_digest` (`l2-nodus-observability.md` §4.7, Phase 14) already performs — no new hashing scheme — so a host persisting/re-resolving workflow definitions by name can detect drift between pause and resume; enforcement is necessarily host-side (LP-2), since the core has no resume-time call site to check it at. `l2-nodus-dialog.md` 1.2.1 → 1.3.0, `l2-nodus-portability.md` 1.8.0 → 1.9.0 (cross-reference + retraction), `l2-nodus-runtime.md` 1.4.0 → 1.4.1 (NL-26 decoupled — independently vacuous behind NL-12 alone, since `DialogProvider::ask`/`confirm`'s synchronous call shape has no asynchronous boundary for a correlation token to attach to). Both amended specs ran a full Post-Update Review (5-lens self-critique) before promotion, catching two missing Related-Specifications cross-references, both added. **New finding recorded, not fixed**: `ReproRecipe.workflow_digest` (`digest_ast`, AST-level) and `l2-nodus-environment.md`'s `CandidateResult.workflow_digest` (`digest_source`, raw-text-level) are two different computations sharing one field name, which `l2-nodus-observability.md` §4.7's own comment treats as equivalent — the new field deliberately follows `digest_ast` rather than adding a third. INDEX v1.0.93 → v1.0.94. No code changed — LP-22(c) is spec-complete and `/magic.task`-decomposable; the Backlog's remaining four items (LP-21, LP-23, NL-24, NL-25) and the two new/independent entries (NL-26, the digest-naming collision) are unaffected.
- 2026-09-01 **Decision:** Phase 30 complete — DG-11 authoring advisories implemented exactly per `l2-nodus-dialog.md` §4.8's design, with two grounding corrections found at implementation time and folded back into the spec (T-30C01) rather than left for a later audit: `+reversible`/`+external` are matched with their surface `+` prefix in `CommandCall.modifiers` (the plan's assumed bare-name key came from reading the *derived* LP-11 `context` map in a test, not the raw AST field); and `W017`'s producer lookup is a dedicated single-pass walker (`w017_collect_producers`) rather than a reuse of `collect_vars_stmt`, which tracks declared/used variable-root sets only and has no producer-identity concept. A third plan-time claim — that `W016` should recurse into `?SWITCH` arms/default and `~PARALLEL` branches the same way as `Conditional`/`ForLoop` — did not survive grounding against the AST: `SwitchBlock`'s arms and `MapBlock`'s command are a single `CommandCall` with no internal `Vec<Stmt>` sequence to scope, and `ParallelBlock.branches` run concurrently, so pairing them would advise moving a dialog past a sibling that does not run "before" it at all; branches are still walked for scopes nested further inside them. `l2-nodus-dialog.md` 1.2.0 → 1.2.1 records all three corrections; `MODEL_COMMANDS`/`DIALOG_COMMANDS` widened `const` → `pub(crate) const` in `portability.rs`. 482 tests pass (was 471, +11: 7 `W016` + 4 `W017`, all asserting on `Diagnostic.code` never message text); clippy/fmt clean; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved); no `unwrap`/`panic!`/`expect` added to any production path. Ran the full `tests/fixtures/` corpus (14 files) through a scratch example (deleted after use): zero fixtures newly emit either code — checked by hand why for the two near-misses rather than assuming absence. INDEX v1.0.92 → v1.0.93; PLAN v1.41.0 held, TASKS v2.34.0 held (established run-phase convention — only INDEX moves; PLAN/TASKS resync at the next `/magic.task`). Plan Complete again — every remaining Backlog item is still design-blocked per the v1.41.0 reconciliation.

## Blockers

## Blocking Constraints

## Session Continuity

**Last Session Ended:** 2026-06-24
**Handoff File:** none
**Bootstrap Mode:** false
