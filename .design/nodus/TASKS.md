# Master Task Index (Registry)

**Version:** 2.32.0
**Generated:** 2026-08-12
**Based on:** .design/nodus/PLAN.md v1.39.0
**Based on RULES:** .design/RULES.md v1.6.0
**Execution Mode:** Sequential
**Status:** Active

## Overview

Tactical registry of all phases. Atomic checklists live in `tasks/phase-{N}.md`.

## Active Phases

| Phase | Description | Status |
| --- | --- | --- |
| [Phase 1](archives/tasks/phase-1.md) | Spec Completeness & Vocabulary Alignment | `Done (Archived)` |
| [Phase 2](archives/tasks/phase-2.md) | Library Hardening: fixture corpus, NL-8/NL-10 enforcement, RUN vocabulary, extraction audit | `Done (Archived)` |

## Active Phases (continued)

| Phase | Description | Status |
| --- | --- | --- |
| [Phase 3](archives/tasks/phase-3.md) | Standalone Extraction: spec sync, Cargo hardening, public API docs, CI workflow, extraction procedure | `Done (Archived)` |
| [Phase 4](archives/tasks/phase-4.md) | Observability & Extension Framework: observability.rs (AuditProvider + 10-event enum), executor hook points (Tracks A/B/C/T), l2-nodus-runtime.md delta v1.0.3 | `Done (Archived)` |

## Active Phases (continued)

| Phase | Description | Status |
| --- | --- | --- |
| [Phase 5](archives/tasks/phase-5.md) | Portability Implementation: portability.rs (SchemaProvider + StorageProvider + PolicyProvider), vocab.rs delta (Schema::with_provider), lexer/parser schema-aware parsing, workflows.rs delta (run_with_schema), l2-nodus-runtime.md v1.0.4 | `Done (Archived)` |

| [Phase 6](archives/tasks/phase-6.md) | Testing Implementation: ast.rs (structured TestBlock), parser.rs (E015 + input/expected parsing), transpiler round-trip, workflows.rs (assertion evaluator + NT isolation), validator.rs (W001/W002), test() tag filtering, tests/testing.rs (6 integration tests), l2-nodus-testing.md spec | `Done (Archived)` |

## Active Phases (continued)

| Phase | Description | Status |
| --- | --- | --- |
| [Phase 7](archives/tasks/phase-7.md) | Capability Manifest (LP-8): portability.rs (CapabilityManifest + ExtensionRole + HostCapabilities), validate_manifest() fail-fast gate wired into executor boot, NODUS:* capability-rejection diagnostic, AST-derived role manifest, run_with_manifest API, LP-3 two-host + pre-run purity tests, l2-nodus-portability.md §4.7 + RFC → Stable | `Done (Archived)` |

## Active Phases (continued)

| Phase | Description | Status |
| --- | --- | --- |
| [Phase 8](archives/tasks/phase-8.md) | Error Taxonomy (l2-nodus-errors): vocab.rs ErrorSeverity/ErrorCategory enums + 14 new error_code constants + severity×category registry + error_meta() lookup; EXECUTION_FAILED deprecated + site reassignment; validator/executor emission; lockstep test | `Done (Archived)` |

## Active Phases (continued)

| Phase | Description | Status |
| --- | --- | --- |
| [Phase 9](archives/tasks/phase-9.md) | Closed Vocabulary Registries (l2-nodus-registries): vocab.rs KNOWN_FLAGS/KNOWN_VALIDATORS/PRIMITIVE_TYPES constants + Schema query surface; advisory validator warnings for unknown ~flag/^validator/@in type; strengthens NL-1/NL-7/NL-9 | `Done (Archived)` |

## Active Phases (continued)

| Phase | Description | Status |
| --- | --- | --- |
| [Phase 10](archives/tasks/phase-10.md) | HITL Dialog (l2-nodus-dialog): ASK/CONFIRM commands, Status::Paused + ResumeDescriptor, DialogProvider + DefaultDialogProvider, ExtensionRole::Dialog + from_workflow derivation, executor dispatch, run_with_dialog combinators; DG-1…DG-8 | `Done (Archived)` |

## Active Phases (continued)

| Phase | Description | Status |
| --- | --- | --- |
| [Phase 11](archives/tasks/phase-11.md) | Control-Flow Constructs (l2-nodus-control-flow): ?SWITCH / ~MAP / ~RETRY:n / !HALT / !PAUSE — lexer tokens + SwitchBlock/MapBlock AST + action flags + retry; parser/executor/transpiler/validator; implemented as per-construct vertical slices | `Done (Archived)` |
| [Phase 12](archives/tasks/phase-12.md) | Environment & Evaluation (l2-nodus-environment): EnvironmentProvider + StubEnvironment + lifecycle, Reward/EnvInteraction trajectory side-band (no new ExecutionEvent variant), ExtensionRole::Environment manifest, run_with_environment frozen boundary + Profile/Budget/GradingMode + CandidateResult digest → 9 tasks / 5 tracks (A–D, T); all-additive, zero-dep preserved | `Done (Archived)` |
| [Phase 13](archives/tasks/phase-13.md) | Declarative Configuration Surface (l2-nodus-config): §config field-declaration AST + `parse_config` (replaces the parser stub), pure `check_config_values` shape check, `AcceptedConfig` provenance/secret value model, `ConfigProvider` + `ExtensionRole::Config` + `DefaultConfigProvider`, `run_with_config[_and_audit]`, `CONFIG_INVALID` code → 9 tasks / 4 tracks (A–C, T); all-additive, zero-dep (LP-1), reuses LP-8 manifest + error taxonomy | `Done (Archived)` |
| [Phase 14](archives/tasks/phase-14.md) | Run-Manifest Identity & Reproducibility (l2-nodus-observability §4.7): `RunManifest.{execution_mode (HO-12), exposure_switches (HO-18), repro ReproRecipe (HO-20)}` + definition-derived `step_identity` on events (HO-15) + `StepError.fault_identity` (HO-19); nodus-computed digest/version/determinism + host-supplied mode/switches entry path → 7 tasks / 4 tracks (A–C, T); all-additive/optional (HO-5/HO-6 preserved), zero-dep (LP-1); event-stream batch HO-7…HO-11/13/14/16/17 deferred | `Done (Archived)` |
| [Phase 15](archives/tasks/phase-15.md) | Aggregation-Safe Event Stream (l2-nodus-observability §4.8): `seq: u64` + `correlation_id: String` on all 10 ExecutionEvent variants + run-scoped binding (HO-7), single seq-assigning emission choke point, `RunManifest.event_count` gap-check identity, `Measurement { Taken(u64), Unavailable }` over the 5 obtainable-failure numerics (HO-14), `handle_dialog` fabricated-zero fix → 6 tasks / 4 tracks (A–C, T); no new event variant (HO-6) / no RunResult change (HO-5), zero-dep (LP-1); Pass-2 batch HO-8/9/10/11/13/16/17 deferred | `Done (Archived)` |
| [Phase 16](archives/tasks/phase-16.md) | Event Annotations, Cost, Lineage & Completeness (l2-nodus-observability §4.9): one `EventAnnotations` carrier field on all 10 variants (HO-9 receipt / HO-11 message / HO-16 anomaly / HO-17 durability), 4 `Measurement` token classes on `ModelResponse` (HO-8), `SourceRef` derivation lineage on `LoopIteration` + the missing `~MAP` emission (HO-13), read-side `classify_trace` with no field (HO-10), durable-only `seq` discipline (HO-17) → 6 tasks / 4 tracks (A–C, T); **closes all 20 HO invariants**; all-optional/defaulted (HO-5/HO-6 preserved), zero-dep (LP-1) | `Done (Archived)` |
| [Phase 17](archives/tasks/phase-17.md) | `~MAP` Conformance & End-to-End Reachability (l2-nodus-control-flow §4.3/§4.4): declare `$it` in the `Stmt::Map` arm of `collect_vars_stmt` so `E004` stops rejecting every `~MAP` workflow (the construct has been unreachable through `run`/`run_with_audit` since Phase 11); `~MAP` end-to-end tests through `workflows::run` + the fixture corpus's first v0.7 construct; retire the Phase-16 validate-gate bypass; patch-level §4.5 spec sync (1.0.0 → 1.0.1) → 5 tasks / 4 tracks (A–C, T); conformance fix against an already-Stable mandate (C12.1), no new spec, zero-dep (LP-1) | `Done (Archived)` |
| [Phase 18](archives/tasks/phase-18.md) | Bounded Whole-Run Self-Restart (l2-nodus-restart): `restart_max` in `§runtime:` + `1..=10` bound check + transpiler round-trip; `RESTART_LIMIT` (Warn) code; `$restart` reserved-but-writable + `$restart_count` reserved-and-runtime-owned (NL-8, count unforgeable); `E019` static validator rejection of a nested restart request (~FOR/~PARALLEL/?SWITCH, redesigned from a planned `Signal::Restart` — validator-only enforcement matches the E010/E013/E014/E016/E017 precedent); bounded attempt loop wrapped **around** `execute_inner` so LG-5 fresh reconstruction is structural → 6 tasks / 3 tracks (A–B, T); opt-in and additive (absent `restart_max` ⇒ today's behavior byte-identical), no new `Value` kind (NL-7), zero-dep (LP-1) | `Done (Archived)` |
| [Phase 19](archives/tasks/phase-19.md) | Compensation Seam (l2-nodus-compensation): `TildeCompensate` token + `Step.compensation: Option<CommandCall>` (mirroring `Step.retry`) + `~COMPENSATE: CMD(args)` trailing same-line clause + transpiler round-trip; `NODUS:COMPENSATION_FAILED`; completed-effect ledger (planned parallel un-compensable record + outcome enum dropped — nothing reads either); arming on `Failed`/`Aborted` + LIFO reverse-completion-order drain (`Vec::pop()`) + failure recording that leaves the original effect live and continues the unwind → 5 tasks / 3 tracks (A–B, T); three vacuous-in-core compositions (LP-11 gate, NL-12 resume, declared sub-region) explicitly not built; purely additive, no new event variant (HO-6), zero-dep (LP-1) | `Done (Archived)` |
| [Phase 20](archives/tasks/phase-20.md) | Branch-Action Pipeline Targets & `?SWITCH` Binding Conformance (l2-nodus-control-flow §3/§4.5): parse a trailing `→ $target` in `try_parse_command_from_string`, splitting the arrow off before the paren split — fixes all three branch-action sites (`@err:` handlers, `?IF` branch actions, `?SWITCH` arms + `*` default) with one change; `collect_vars_stmt`'s `Stmt::Switch` arm already walked `sw.arms`/`sw.default` correctly (the plan-time "second coupled defect" was a `sed`-range grounding artifact — regression tests added, no fix needed); reconcile `l2-nodus-restart` §4.2/§4.6/§5 + INDEX and `l2-nodus-compensation` §4.4 + example to as-built (both → 1.0.1), plus an unplanned "explicit compensate request" trigger and three dangling `§7` refs found and fixed in the latter → 6 tasks / 4 tracks (A–C, T); conformance fix against an already-Stable NL-10 mandate (C12.1), no new spec, no executor change, zero-dep (LP-1) | `Done (Archived)` |
| [Phase 21](archives/tasks/phase-21.md) | Compact-Form Round-Trip Fidelity for Control Flow (NL-6 / l2-nodus-runtime §3 / l2-nodus-control-flow §3): `Transpiler::to_nodus` silently drops every control-flow step — `nodus_step` matches only `Stmt::Command`/`Stmt::Comment`, so the seven other `Stmt` variants hit a `_` catch-all returning `""` and the caller's `if !line.is_empty()` skips the step (proved by probe: `orig steps=2 reparsed steps=1`); `Step.sub_steps` never emitted at all. Compact emitters for the branch family (`Conditional`/`Switch` incl. the Phase-20 arm `pipeline_target`), loop/collection family (`ForLoop`/`UntilLoop` with `MAX:n` / `Map` with its block-level target), and `Parallel`/`VarRef`/`sub_steps`, replacing the `_` catch-all with exhaustive arms so a future variant is a compile error not a silent drop; the AST-equality harness `l2-nodus-runtime` §3 claims exists in `workflows.rs` but which was never built; fixture-corpus backfill for `?SWITCH`/`~RETRY`/`!HALT`/`!PAUSE` → 7 tasks / 4 tracks (A–C, T); third C12.1 conformance phase and the largest, no new spec, zero-dep (LP-1) | `Done (Archived)` |
| [Phase 22](archives/tasks/phase-22.md) | Whole-File NL-6 Round-Trip Closure (l2-nodus-testing §10 / l1-nodus-language NL-6): close the last 3 of 14 `WorkflowFile` fields that don't survive a compact round-trip, so NL-6 AST-equality holds for the whole file rather than `.steps` alone (Phase 21 closed 11 and scoped its harness to `.steps`; widening that assertion to `ast1 == ast2` is this phase's single acceptance signal, §10.5). `tests` emits but not AST-equally — invert the emission branch to prefer `raw_lines` (the existing path is unreachable because the structured fields are *derived* from it), re-quote values that wouldn't re-lex to a single equal token, add `W015` for the silently-dropped non-`:` pair separator (NT-9); `macros` and `human_mode` are **never emitted at all** — both found at plan time, both hitting corpus fixtures → 6 tasks / 3 tracks (A–B, T); C12.1 conformance against an already-Stable mandate, no new spec, zero-dep (LP-1) | `Done (Archived)` |
| [Phase 23](archives/tasks/phase-23.md) | Built-in Durable-State Conformance (l1-nodus-portability §4.1/§4.11 · LP-2/LP-15): the shipped `StorageProvider` built-in did not satisfy its own L1 contract — `NoopStorageProvider` discarded on `store` and always returned `None` on `load`, so it could not round-trip even within one invocation, while §4.1's Storage row (amended v1.8.0 by LP-15) specifies an **in-memory** store and LP-2 requires every built-in be "sufficient for in-process testing without I/O". Replaced with `InMemoryStorageProvider` (`Mutex`-guarded, poisoned-lock recovery, zero-dep); updated the Phase-5 test that deliberately encoded the old no-op contract, splitting its storage half into 4 dedicated round-trip/overwrite/absent/isolation tests; reconciled `l2-nodus-portability` to as-built (1.3.0 → 1.3.1) including two live references the initial pass missed (§3's LP-2 row, §4.5's module map). T-23C02 stayed **Cancelled (superseded)** — the prior spec pass had already corrected its target from the opposite direction, opening the LP-11 gate rather than demoting the claim → 5/6 tasks Done, 1 Cancelled, 3 tracks (A, C, T); C12.1 conformance against an already-Stable mandate, **no executor wiring** (LP-3 stays unsatisfied for this seam), zero-dep (LP-1) preserved. 435 tests pass (was 429, +6); clippy/fmt clean | `Done (Archived)` |
| [Phase 24](archives/tasks/phase-24.md) | Per-Effect Authorization Call Site (l1-nodus-portability §4.7 · l2-nodus-portability §4.9 · LP-11): `PolicyProvider` converted from a re-exported, zero-call-site trait into a real pre-effect gate over `GEN`/`ANALYZE` (`EffectClass::ModelCall`) and `ASK`/`CONFIRM` (`EffectClass::Deferred`) — `EffectClass::ToolUse` stays unrealized, since every tool-shaped builtin command is a fixed, zero-dependency stub with no host-swappable seam to gate. `NODUS:POLICY_DENIED` via the existing `StepError` event (not `ConstraintHit`, scoped to hard `!!`-constraints), `ctx.errors.push`, non-halting `None` return mirroring `DialogOutcome::Timeout`/`::Rejected`; `Executor.policy` field + `with_policy`/`with_policy_and_audit` + `run_with_policy`/`run_with_policy_and_audit` combinators; `l2-nodus-portability` 1.4.0 → 1.5.0 reconciled → 6/6 tasks Done, 3 tracks (A, C, T); governance already cleared (LP-3 satisfied, §4.8.1), Rust shape already specified (§4.9), zero-dep (LP-1) preserved, no `@err:` dispatch built (out of scope — separate NL-9 gap). 444 tests pass (was 435, +9); clippy/fmt clean | `Done (Archived)` |

## Active Phases (continued)

| Phase | Description | Status |
| --- | --- | --- |
| [Phase 25](archives/tasks/phase-25.md) | Effect Risk-Class Declaration (l1-nodus-portability §4.12 · l2-nodus-portability §4.10 · LP-16): `reversible`/`external`/`value` consequence descriptors ride the existing `+modifier=value` grammar (`CommandCall.modifiers`) — zero new DSL/AST/parser/transpiler work — read into the LP-11 gate's existing `context` literal in `execute_command` when present; `PolicyProvider`'s trait signature untouched (no new method, no new error code) → 3/3 tasks Done, 3 tracks (A, C, T); smallest phase in the LP-11 arc, no plan- or implementation-time scope correction needed (second in a row after Phase 24), zero-dep (LP-1) preserved. 447 tests pass (was 444, +3); clippy/fmt clean | `Done (Archived)` |

## Active Phases (continued)

| Phase | Description | Status |
| --- | --- | --- |
| [Phase 26](archives/tasks/phase-26.md) | Uncaught-Error Handler Dispatch (l1-nodus-language §4.3/NL-9 · l2-nodus-error-dispatch §4.1–§4.5): a declared `@err:` handler is invoked exactly once when a non-fatal, `Signal`-free runtime error escapes a top-level step — eligibility structural (no enumerated code list), `$error` (already reserved, never written) populated before dispatch, handler runs via the ordinary `execute_command` path, remaining declared steps skipped afterward → 4/4 tasks Done, 3 tracks (A, C, T); closes a real correctness gap (`error_decl` parsed since early phases, zero call sites), found and fixed a real pre-existing test regression in `tests/control_flow.rs`'s retry-exhaustion test, zero-dep (LP-1) preserved. 452 tests pass (was 447, +5); clippy/fmt clean | `Done (Archived)` |

## Active Phases (continued)

| Phase | Description | Status |
| --- | --- | --- |
| [Phase 27](archives/tasks/phase-27.md) | Settlement Effect Seam (l1-nodus-portability §4.13 · l2-nodus-settlement §4.1–§4.6 · LP-17): new `EffectClass::Settlement` + `SETTLE(payee, amount, purpose) → $target` reuses the already-shipped LP-11 gate unchanged for the decide half; a new `SettlementRail` trait + `NoopSettlementRail` built-in + ninth `ExtensionRole::Settlement` (absent from `builtin()`) for the act half; `NODUS:SETTLEMENT_UNACCOUNTED` is `Signal`-free so NL-9 `@err:` dispatch covers it automatically → 5/5 tasks Done, 4 tracks (A, B, C, T); no plan/implementation scope correction (spec authored against the real code in-session); one empirical catch (non-reserved pipeline target isn't pre-seeded). 462 tests pass (was 452, +10); clippy/fmt clean | `Done (Archived)` |
| [Phase 28](archives/tasks/phase-28.md) | Memoizable & Promotable Approval (l1-nodus-dialog §3/DG-9,DG-10 · l2-nodus-dialog §4.7): new `DialogOutcome::Remembered(Value)` bound through `Answer`'s exact path + a `dialog_provenance` field on the shared `EventAnnotations` carrier; no new `DialogProvider` trait method; DG-10 needs no separate code at all (host-side over the DG-9 tag) → 3/3 tasks Done, 3 tracks (A, C, T); confirmed `dialog_provenance` is the first `EventAnnotations` field the crate itself populates. 467 tests pass (was 462, +5); clippy/fmt clean | `Done (Archived)` |
| [Phase 29](archives/tasks/phase-29.md) | Declared Budget Measure (l1-nodus-environment §3/NE-14 · l2-nodus-environment §4.4.1): `EnvironmentProfile.token_measure: Option<String>` + a fail-fast rejection when `budget.max_tokens` has no identified measure — real insertion point corrected at plan time against `workflows.rs:777-859` (after `env.profile()`, an `Ok(EnvRunResult)`/`RuntimeError` shape, not the spec's stated `Diagnostic` channel) → 3/3 tasks Done, 3 tracks (A, C, T); the plan-time correction proved exactly right, no further adjustment needed. 471 tests pass (was 467, +4); clippy/fmt clean | `Done (Archived)` |

## Meta Information

- **Last Updated**: 2026-08-12 (v2.32.0: **basis-pointer resync after the §4.1 amendment — no new phase, Plan Complete holds.** The immediately-preceding `/magic.spec nodus` reconciled `l1-nodus-portability` §4.1's extension-point registry to shipped reality (4 listed roles vs 9 shipped; 1.14.3 → 1.14.4) and advanced `INDEX.md` v1.0.84 → v1.0.85, leaving the expected `SYNC_GAP` this run closes — the standing convention that a spec pass moves only the registry and `PLAN.md`'s basis pointer resyncs at the next planning run. Pre-flight showed that single warning and nothing else: 20 specs, all Stable, 0 Draft, 0 orphans, header parity clean. **One substantive correction beyond the pointer:** `PLAN.md`'s own Phase 0 entry for the portability contract was stale in exactly the same direction the spec had been, reading "LP-1…LP-7; ModelProvider + AuditProvider + **future** StorageProvider/PolicyProvider taxonomy" — a two-shipped/two-future description thirteen invariants and five roles out of date. Corrected to LP-1…LP-20 and the nine-role registry. Worth noting as a pattern rather than a one-off: the same understatement had independently settled into the spec's §4.1 table *and* the plan's description of it, which is what a registry-shaped fact does when two documents each hold their own copy — the amendment's own §4.1 note names a table-vs-enum conformance check as the enforceable fix, and this plan line is the third copy that check would not cover. No phase opened: the amendment is documentation reconciliation of already-shipped roles, so nothing new became buildable. Pre-Planning Stabilization: 0 promoted (0 Draft). Rules Parity confirmed unchanged at RULES v1.6.0. PLAN v1.38.0 → v1.39.0, TASKS v2.31.0 → v2.32.0. Prior: 2026-08-12 (v2.31.0: **sync-only replan — registry resynced, catalogue de-staled, no new phase. Plan Complete holds.** Pre-flight found the two drift classes this run closes: an `ORPHANED_SPEC` (`l1-nodus-testing.md` registered in `INDEX.md` with no `PLAN.md` checklist line) and a `SYNC_GAP` (PLAN based on INDEX v1.0.78 against a registry at v1.0.84 — six versions accumulated across the Phase 27/28/29 cycle, the familiar pattern where `/magic.spec` waves advance `INDEX.md` while `PLAN.md`'s basis pointer stays put). **The orphan was a pure traceability gap, verified not assumed:** `l1-nodus-testing` has a built L2 (`l2-nodus-testing`, implemented in Phase 6) and was referenced in Phase 6's *prose* but never as a checklist link, so the registry-to-plan trace ran through narrative instead of structure — the same class as `l1-dev-office` in the main workspace. Now a Phase 0 entry. **Mechanical drift auto-fixed:** six Done phase headings (1, 5, 11, 12, 25, 26) lacked the `✓` marker every other Done phase carries; each was confirmed fully-checked and `Done (Archived)` in `TASKS.md` before the marker was added, so this is presentation drift, not a status claim. **Backlog audit — three entries marked RESOLVED after verification in source**, since a stale catalogue is worse than none: `@err:` handler dispatch (NL-9), whose entry recorded "zero call sites in `executor.rs`" against a live call site now at `executor.rs:936` — the design-block it named was lifted by authoring `l2-nodus-error-dispatch` and Phase 26 built it; NT-11 differential parity, whose open fork (vacuous-in-core vs. an in-crate second host) was decided in `l2-nodus-testing` v1.3.0 §11 as **Vacuous in core**, with the §8 row now stating that explicitly rather than omitting the invariant; and the internal phase-journal gap, with `.design/nodus/CHANGELOG.md` now running unbroken Phase 1 → 29. **A "Phase 30 — Validator & Parser Conformance Defects" was drafted this run and then retracted before write-back; recording that here so a fourth replan does not rediscover the same dead end.** The three remaining non-RESOLVED items (`validator.rs:822`'s flat `declared` set; `parser.rs:474`'s single-token value consume truncating a dotted `§runtime: core`; the corpus's exclusive non-braced `@macro:` form leaving `collect_braced_raw_lines` undriven) were each confirmed to still match their notes **in source** — but matching the note's *code state* is not the same as clearing the note's *verdict*, and re-reading each entry in full showed all three are blocked rather than authorable. **Lexical scoping**: "needs a design pass deciding whether E004 should become scope-aware, and would change diagnostics for existing workflows (not purely additive)" — the fix is not the scope stack, it is deciding what the diagnostic becomes. **Dotted `§runtime:` value**: already "**re-checked at the v1.26.0 replan and confirmed design-blocked, not merely deferred**" — `l1-nodus-language` §113 says only "loads the named schema" and is silent on whether the name may contain dots, so the truncation violates no stated mandate and no conformance `Verify` line can be written against it; it needs a spec decision first. **`@macro:` braced body**: the entry itself says "**Not a defect** — nothing in the corpus produces that state", with the fixture worth adding "whenever macro body expansion itself is tackled", a deferred feature. Each therefore fails the same test the `@err:` entry applied to itself: no concrete `Verify` line is writable until a design question is answered, so none is task-authorable and all three stay parked. **Two further Backlog items excluded on the same principle:** NL-19/NL-21 are marked `Pending — no L2 realization spec` in `l2-nodus-runtime`'s own compliance table, and `StorageProvider` LP-3 needs a second integration context to exist in the world. **Net: every genuinely open nodus item is blocked on a design decision (a `/magic.spec` act) or on an external integration — none on implementation capacity.** Pre-flight `ok:true` after write-back. Pre-Planning Stabilization: 0 promoted (20 specs, all Stable, 0 Draft). Rules Parity confirmed unchanged at RULES v1.6.0. PLAN v1.37.0 → v1.38.0 (Based-on INDEX v1.0.78 → v1.0.84), TASKS v2.30.0 → v2.31.0. Prior: 2026-08-01 (Phase 29 Complete — see below; all three phases planned that cycle now Done))
- **Phase 29 Complete**: 2026-08-01 (tracks A/C/T; 3/3 tasks Done; `EnvironmentProfile.token_measure`/`CandidateResult.token_measure` landed; `env_measure_rejection` (sibling to `capability_rejection`) + the NE-14 check wired into the single shared `run_with_environment_impl` immediately after `env.profile()` — the plan-time-corrected mechanism (`Ok(EnvRunResult)`/`RuntimeError`, not `Diagnostic`; after `env.profile()`, not before `env.open`) proved exactly right, requiring no further adjustment during implementation. `l2-nodus-environment.md` 1.1.0 → 1.1.1 reconciles §4.4.1/§3 to Implemented, applying the same correction the plan already recorded. 471 tests pass (was 467; +4); clippy/fmt clean; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved); no `unwrap`/`panic!`/`expect` added to any production path. **All three phases planned in this cycle (27, 28, 29) are now Done.**)
- **Phase 28 Complete**: 2026-08-01 (tracks A/C/T; 3/3 tasks Done; `DialogOutcome::Remembered(Value)` binds through `Answer`'s exact path; `DialogProvenance{Answered,Remembered}` (`observability.rs`) + `EventAnnotations.dialog_provenance` tag the resolved dialog step's `StepEnd`; no new `DialogProvider` trait method. **Confirmed rather than assumed**: `dialog_provenance` is the first `EventAnnotations` field the crate's own dispatch logic populates directly — every sibling field has no in-crate writer at all; `EventAnnotations`'s doc comment updated. DG-10 confirmed to need zero code. One test-writing finding: asserting no-provenance-on-rejection against `DEFERRED_WF` (declares `@err:`) initially expected one `StepEnd`; `DIALOG_REJECTED` is `Signal`-free so NL-9 dispatch fires automatically, landing two `StepEnd` events, both correctly provenance-free — the test was corrected. `l2-nodus-dialog.md` 1.1.0 → 1.1.1 reconciled to Implemented. 467 tests pass (was 462; +5); clippy/fmt clean; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved); no `unwrap`/`panic!`/`expect` added to any production path.)
- **Phase 27 Complete**: 2026-08-01 (tracks A/B/C/T; 5/5 tasks Done; LP-17 settlement effect seam — `EffectClass::Settlement` reuses the LP-11 gate unchanged for the decide half; `SettlementRail`/`NoopSettlementRail` (the crate's first new extension-point trait since LP-11 itself) + ninth `ExtensionRole::Settlement` supply the act half; `handle_settlement` mirrors `handle_dialog`, both exits (`POLICY_DENIED`, `SETTLEMENT_UNACCOUNTED`) `Signal`-free so NL-9 `@err:` dispatch covers `SETTLE` automatically. No plan/implementation scope correction — the spec was authored against the real code in-session. One empirical catch: a non-reserved `@out: $receipt` pipeline target isn't pre-seeded like `$out`; fixed by using the reserved binding. `l2-nodus-settlement.md` 1.0.0 → 1.0.1, `l2-nodus-portability.md` 1.7.0 → 1.7.1 (§3.1 LP-17 row → Implemented). 462 tests pass (was 452; +10); clippy/fmt clean; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved).)
- **Plan (v2.30.0)**: 2026-07-31 (opened **three** phases in one invocation — Phase 27 Settlement Effect Seam (LP-17, 5 tasks/4 tracks), Phase 28 Memoizable & Promotable Approval (DG-9/DG-10, 3 tasks/3 tracks), Phase 29 Declared Budget Measure (NE-14, 3 tasks/3 tracks), all Sequential and mutually independent — different files, no shared call site, matching the one-phase-per-L2-spec-delta precedent (Phases 10–13, 14–16, 18–19). Sequenced 27→28→29 by DA-3 (no dependency/maturity tiebreaker among three equally-Stable independent specs, so coverage decides: LP-17 is the single largest capability and leads; DG-9/DG-10 and NE-14 are small deltas on already-Done phases, ordered by INDEX.md row order). Found and corrected at plan time, not acted on outside its write domain: `l2-nodus-environment.md` §4.4.1 claims NE-14's rejection uses the LP-8 `Diagnostic` channel and runs "before `env.open`" — neither holds against `workflows.rs:777-859`, where the adjacent manifest rejection actually returns `Ok(EnvRunResult{result: capability_rejection(..), ..})` (an ordinary `RunResult`/`RuntimeError`) and `env.profile()` isn't called until after `env.open`/`env.reset` already ran; Phase 29's scope note carries the corrected mechanism, the spec itself queued for the next `/magic.spec` touch. PLAN v1.36.0 → v1.37.0, INDEX v1.0.78 held, RULES parity v1.6.0 held.)
- **Sync replan (v2.29.0)**: 2026-07-31 (**no new phase.** Resolved the expected spec→task `SYNC_GAP` — PLAN basis v1.0.75 → v1.0.76, the only changed registry row being `l2-nodus-error-dispatch.md`'s own version bump 1.0.0 → 1.0.1 from Phase 26's spec-reconciliation task, not new spec content. Pre-flight otherwise fully clean: 19/19 Stable, 0 Draft, no orphan/phantom, all headers matching INDEX. Re-verified rather than assumed that NL-9 dispatch landing didn't newly unblock anything: it is independent of LP-17/LP-20 (both need their own core vocabulary, untouched by `@err:` dispatch) and independent of the three stale L2 tables / L1 taxonomy items already in the Backlog. PLAN v1.35.0 → v1.36.0, INDEX v1.0.76 held. Computed next step (DA-6): `/magic.spec nodus`, LP-17 as the strongest candidate — L1 §4.13 already frames it as an LP-11 specialization the same shape as LP-16, so the identical low-footprint realization pattern very likely applies again.)
- **Phase 26 Complete**: 2026-07-31 (tracks A/C/T; 4/4 tasks Done; `execute_inner`'s main step loop (`executor.rs`) gained the structural dispatch check — a `Signal`-free step that left a new `ctx.errors` entry behind populates `$error` via a new `error_to_value` helper, dispatches the declared `@err:` handler through the ordinary `execute_command` path (reusing the triggering step's own `step_num`, the compensation-drain precedent), then ends the main sequence. No new type, field, error code, or `PolicyProvider`/`AuditProvider` change — the smallest production-code diff of any phase in this arc besides LP-16's. **Self-review reconciling §4.3/§4.5's pseudocode found two corrections**: the real parameter is `ast` not `wf`, collapsed to a let-chain (`clippy::collapsible_if`) with no unused `err_dispatched` flag; and `try_parse_command_from_string` returns `None` only for empty `@err:` text, never for unrecognized-but-non-empty text — the spec's "handler text unparseable" framing was corrected. **Found and fixed one real regression beyond the spec's own anticipated blast radius**: `tests/control_flow.rs`'s pre-existing `retry_reruns_failing_step_up_to_bound` asserted "the step after an exhausted retry still runs" — true only before dispatch existed; `l1-nodus-language.md`'s own `~RETRY:n` row has always said exhaustion "routes to `@err:`", so the old assertion was pinning exactly the gap this phase closes — corrected to assert the handler dispatches and the following step does not run. Reviewed Phase 24's `policy_denies_model_call_effect`/`policy_denies_deferred_effect` and confirmed their assertions still hold, not vacuously — no changes needed. `l2-nodus-error-dispatch.md` 1.0.0 → 1.0.1, `l2-nodus-errors.md` 1.1.1 → 1.1.2, `l2-nodus-runtime.md` 1.3.1 → 1.3.2 all reconciled to Implemented. 452 tests pass (was 447; +5); clippy clean after one `collapsible_if` fix; fmt clean after auto-fix; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved); the one new production-path `.expect()` is structurally unreachable-as-`None`, proven by the guard condition immediately preceding it.)
- **Phase 26 Planned (v2.28.0)**: 2026-07-31 (resolved the expected spec→task `ORPHANED_SPEC` — new `l2-nodus-error-dispatch.md`, authored Stable directly, never yet placed in a phase — and `SYNC_GAP` (PLAN basis v1.0.73 → v1.0.75) and opened **Phase 26 — Uncaught-Error Handler Dispatch**, 4 tasks / 3 tracks (A, C, T), Sequential. **Chosen over the standing default next step (LP-17/LP-20) as higher-value**: those extend an already-realized gate with new capability, while `@err:` dispatch closes a real correctness gap — `error_decl` has been parsed/validated/transpiled since early phases with zero call sites in `executor.rs`, and every fixture in the normative corpus already declares `@err: ESCALATE(human)` expecting it to fire. Verified rather than assumed before decomposing: read `run_step_with_retry` directly and confirmed a step that succeeds via `~RETRY:n` truncates its own transient attempt-errors before returning, so retry-then-succeed structurally never reaches dispatch — no special-casing needed. Confirmed the eligibility rule is genuinely code-list-free by tracing the control flow: `RULE_VIOLATION` returns `Signal::Break` (caught by an earlier match arm, never reaches the dispatch check) and `COMPENSATION_FAILED` fires only in the post-loop compensation drain (structurally unreachable from the main loop). Track T is larger than Phase 24/25's by design — beyond new coverage, it must review every existing test whose fixture already declares `@err:` and triggers a dispatchable error, since this phase retroactively activates previously-inert behavior; named explicitly in the phase file's own guardrails rather than left for implementation time to discover. INDEX v1.0.75 held, PLAN v1.34.0 → v1.35.0, RULES parity v1.6.0 held.)
- **Phase 25 Complete**: 2026-07-31 (tracks A/C/T; 3/3 tasks Done; `execute_command`'s existing LP-11 `context` literal (`executor.rs`) extended with a small loop reading `+reversible`/`+external`/`+value` off `cmd.modifiers`, pushing each stripped-prefix pair only when present — no new type, field, error code, or `PolicyProvider` signature change. **Self-review reconciling §4.10.2's pseudocode found one divergence**: it named a separate `build_context` function for illustrative clarity; the real call site builds the literal inline (matching §4.9.3's own as-built precedent) — corrected. All 3 new integration tests (`risk_descriptors_reach_context_when_declared`, `undeclared_risk_descriptors_are_absent_from_context_not_defaulted`, `risk_descriptors_are_inert_without_a_policy_provider`) passed on first run — unlike Phase 24, no empirical assumption-catch this cycle, consistent with how thoroughly §4.10 was grounded before planning. `l2-nodus-portability` 1.6.0 → 1.6.1 reconciles §4.10/§3.1/§5 to Implemented/Done. 447 tests pass (was 444, +3); clippy clean; fmt found one line-wrap violation in the new tests (no logic change), fixed; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved); no `unwrap`/`expect`/`panic!` added to `executor.rs`'s production path.)
- **Phase 25 Planned (v2.27.0)**: 2026-07-31 (resolved the expected spec→task `SYNC_GAP` — PLAN basis v1.0.72 → v1.0.73, from the LP-16 design pass — and opened **Phase 25 — Effect Risk-Class Declaration**, 1 task / 3 tracks (A, C, T), Sequential — the smallest phase in the LP-11 arc: one call site extended, no new type, no new `Executor` field, no new error code. Like Phase 24, no plan-time grounding correction was needed — the preceding spec pass (`l2-nodus-portability` §4.10) had already settled everything a task needs: the descriptors ride the existing `+modifier=value` grammar with zero new DSL/AST/parser work, and `PolicyProvider`'s signature stays untouched. Verified rather than assumed before decomposing: read the real `execute_command` code and confirmed `context` is an inline `Value::Map(vec![...])` literal, not the separate `build_context` function the spec's illustrative pseudocode names — task Notes point at the real literal. Checked `tests/portability.rs`'s existing fixtures (`MANIFEST_WF`, `AllowAllPolicy`/`DenyAllPolicy`) and confirmed none carries `+reversible`/`+external`/`+value` modifiers, so Track T needs a new fixture rather than reusing one. INDEX v1.0.73 held, PLAN v1.33.0 → v1.34.0, RULES parity v1.6.0 held.)
- **Sync replan (v2.26.0)**: 2026-07-31 (**no new phase.** Resolved the expected spec→task `SYNC_GAP` — PLAN basis v1.0.71 → v1.0.72, the only changed registry row being `l2-nodus-portability`'s own version bump 1.4.0 → 1.5.0 from Phase 24's spec-reconciliation task, not new spec content. Pre-flight otherwise fully clean: 18/18 Stable, 0 Draft, no orphan/phantom, all headers matching INDEX. Re-verified rather than assumed that LP-11 landing didn't newly unblock anything: read `l1-nodus-portability.md` §4.1 directly and confirmed it still lists four roles against the crate's eight — the taxonomy-reconciliation gap stays open and design-blocked (an L1 amendment, not a task). Every remaining Backlog item stays either vacuous-in-core, LP-3-adjacent-but-undesigned, or a genuine design fork; none has a Stable L2 Rust shape a task could cite in a `Verify` line. PLAN v1.32.0 → v1.33.0, INDEX v1.0.72 held. Computed next step (DA-6): `/magic.spec nodus`, LP-16 as the strongest candidate — the direct, already-described extension of the LP-11 gate Phase 24 just realized.)
- **Phase 24 Complete**: 2026-07-31 (tracks A/C/T; 6/6 tasks Done; `EffectClass{ModelCall,Deferred}` + `effect_class_of` (reusing `MODEL_COMMANDS`/`DIALOG_COMMANDS`) + `NODUS:POLICY_DENIED` (`vocab.rs`, beside the frozen 24-code registry, `(Error, Runtime)`); `Executor.policy: Box<dyn PolicyProvider>` fourth field (default `NoopPolicyProvider`) across all six constructors + `with_policy`/`with_policy_and_audit`; the gate itself in `execute_command` — after `check_rules`, before the `ASK`/`CONFIRM` dispatch, covering both effect classes with one check — emitting `StepError`+`NODUS:POLICY_DENIED` (never `ConstraintHit`) and returning bare `None` on denial (non-halting, mirroring `DialogOutcome::Timeout`/`::Rejected`); `run_with_policy`/`run_with_policy_and_audit` copying `run_with_dialog`'s exact shape. **Testing caught a wrong assumption empirically, not by inspection**: the first draft asserted a denied step's pipeline target is *absent* from `RunResult.vars`; both denial tests failed immediately — reserved variables (`out`/`error`/`meta`/…) are always seeded to defaults (`Value::Null` for `out`) at context construction, so "unbound" means "still the seeded default", never "key absent". Fixing this also revealed the *permitted*-effect assertions had been vacuously true under the original `contains_key` form. **Self-review before closing the spec-reconciliation task caught two stale doc comments in the shipped crate itself**, beyond the spec: `portability.rs`'s module doc and `PolicyProvider`'s own trait doc still said "executor integration is deferred until LP-3 is satisfied" — both corrected. `l2-nodus-portability` 1.4.0 → 1.5.0 reconciles §4.9 (three `[REFERENCE]` blocks corrected to the exact as-built code: `as_str` → `as_gate_str`, the fuller denial `reason` string, "stays unset" → "stays at its seeded default"), §4.2/§4.4/§3.1/§5 item 2 to Implemented. 444 tests pass (was 435; +9: 4 `effect_class_of`/gate-string unit tests, 5 `run_with_policy` integration tests); clippy clean; fmt found one line-wrap violation in the new tests (no logic change), fixed; `Cargo.toml`/`Cargo.lock` diff empty (LP-1 preserved); every `unwrap`/`expect`/`panic!` hit in the touched files falls inside `#[cfg(test)] mod tests`.)
- **Phase 24 Planned (v2.25.0)**: 2026-07-31 (resolved the expected spec→task `SYNC_GAP` — PLAN basis v1.0.69 → v1.0.71 from the LP-11 design pass — and opened **Phase 24 — Per-Effect Authorization Call Site**, 1 task / 3 tracks (A, C, T), Sequential. Unlike every prior phase in this workspace, **no plan-time grounding was needed to widen or correct the scope** — the preceding spec pass (`l2-nodus-portability` 1.3.1 → 1.4.0, §4.9) had already settled every open question a task needs a concrete `Verify` line for: `EffectClass{ModelCall,Deferred}` (reusing `MODEL_COMMANDS`/`DIALOG_COMMANDS`, `ToolUse` deliberately vacuous since every tool-shaped builtin command is a fixed zero-dependency stub with no host-swappable seam), the `gate`/`context` shape, the `execute_command` integration point, the `NODUS:POLICY_DENIED` denial signal via the existing `StepError` event (specifically **not** `ConstraintHit`, corrected in the same spec pass since that variant is scoped to hard `!!`-constraints), non-halting `None`-return semantics mirroring `DialogOutcome::Timeout`/`::Rejected`, and the `run_with_policy`/`run_with_policy_and_audit` combinators mirroring `run_with_dialog`. This is what the L1 §4.14 LP-3 falsifiability rule and the two-cycle LP-11 arc (v1.30.0 mis-scoped it as task-authorable while ungated → v1.31.0 opened the gate but left the design undone → v1.32.0 this cycle plans the now-fully-designed shape) were building toward: a phase plannable without any plan-time correction because nothing was left for grounding to discover. Verified this rather than assumed it — read §4.9's five subsections in full before decomposing, confirmed `MODEL_COMMANDS`/`DIALOG_COMMANDS` already exist in `portability.rs` (no new constants needed beyond the classification function), confirmed `DialogOutcome::Timeout`/`::Rejected`'s exact push-then-`None` shape in `executor.rs` as the concrete precedent to code against, and confirmed `run_with_dialog`'s exact combinator signature as the template for `run_with_policy`. Track A splits into three atomic tasks along the natural seam the design itself draws — the type/classification/error-code layer (§4.9.1/§4.9.4), the `Executor` field/constructors, and the `execute_command` gate plus combinators (§4.9.2/§4.9.3/§4.9.5) — matching Phase 23's exact 6-task/3-track shape. INDEX v1.0.71 held, PLAN v1.31.0 → v1.32.0, RULES parity v1.6.0 held.)
- **Phase 23 Complete**: 2026-07-31 (tracks A/C/T; 5/6 tasks Done, 1 Cancelled-superseded; `InMemoryStorageProvider` — `Mutex<Vec<(String, Value)>>`, insert-or-overwrite `store`, clone-on-`load`, poisoned-lock recovery via `unwrap_or_else(|poisoned| poisoned.into_inner())` — replaces `NoopStorageProvider` in `portability.rs`, matching L1 §4.1's in-memory built-in mandate and LP-2's in-process-sufficiency requirement that the no-op satisfied for neither; `lib.rs` re-export and module docs updated; `tests/portability.rs`'s Phase-5 contract test split (storage half → 4 dedicated tests: round-trip, overwrite, absent-key, instance-isolation) plus 3 equivalent in-crate unit tests. `l2-nodus-portability` 1.3.0 → 1.3.1 reconciles Overview/§4.2/§4.3/§3.1/§5 to the as-built fix, catching two live references the first pass missed (§3's LP-2 row, §4.5's module map) during self-review before declaring the task done. T-23C02 confirmed correctly Cancelled at plan time — nothing left to do; its outcome had already been reached by the prior spec pass from the opposite direction. 435 tests pass (was 429, +6); clippy/fmt clean; `git diff --stat -- crates/nodus/Cargo.toml crates/nodus/Cargo.lock` empty (LP-1 preserved); manual scan confirmed the sole new `lock()` call uses poisoned-guard recovery, no `unwrap()`/`panic!()` added on any production path.)
- **Sync replan (v2.23.0)**: 2026-07-31 (**no new phase; Phase 23 stays open and unstarted.** Resolved the expected spec→task `SYNC_GAP` (PLAN basis v1.0.68 → v1.0.69 from the LP-3 pass). **The cycle's substantive act was cancelling a task whose premise its own predecessor removed.** T-23C02 was planned to correct `l2-nodus-portability` §5 item 2 by *demoting* the LP-11 call site to "LP-3-gated"; the 1.3.0 spec pass corrected the same text from the **opposite direction**, opening the gate — `l1-nodus-portability` 1.14.0 §4.14 made LP-3 falsifiable (host context defined by *what it decides and how it fails*, independence evidenced by documented divergence, an admission record fixing the disposition **per seam**) and §4.8.1 recorded it **satisfied** for `PolicyProvider`, on evidence that is the host's own rather than ours: the runtime tool guard (`tool_security.rs`) and plugin-hook interception (`hooks.rs`) are the two divergent realizations the Stable main `l1-interception-model` was written to unify. Marked **Cancelled, not Done** — the outcome was reached but not by this phase, and recording it Done would credit Phase 23 with another cycle's work. The correction survives in sharper form: the LP-11 call site is not ungated but **permitted-and-undesigned**, an ordinary L2 gap now queued as the next spec target, carrying a real L1↔L2 contradiction the pass must settle rather than average — L2 §4.4 has the executor emit `ConstraintHit { halt: false }` and **skip** the step, L1 §4.7 sketches a typed `NODUS:POLICY_DENIED` routed to `@err:`; skip-and-continue and a catchable typed error are different observable behaviours. `StorageProvider`'s own record (§4.8.2) stays **unsatisfied** — one context only — so its hook points remain held and Phase 23's five live tasks are untouched. Also recorded as RESOLVED: the repo-wide SDD reference containment sweep (121 occurrences / 44 files found, cleared across 55 files / 138 lines, gates green, 429 nodus tests), including that 16 leaks sat inside `crates/nodus` — the crate built for standalone extraction, where the harm is concrete — and that several were introduced by the Coder role itself despite §6's write-time rule, which is why an engine bug report was raised. INDEX v1.0.69 held, PLAN v1.30.0 → v1.31.0, RULES parity v1.6.0 held.)
- **Phase 23 Planned (v2.22.0)**: 2026-07-30 (resolved the expected spec→task `SYNC_GAP` — PLAN basis v1.0.67 → v1.0.68 from the `l2-nodus-portability` 1.2.0 pass — and opened **Phase 23 — Built-in Durable-State Conformance**, 6 tasks / 3 tracks (A, C, T), Sequential. **Plan-time grounding overturned one of the two items the prior spec pass had promoted as task-authorable, and the correction is the substantive finding of this cycle.** That pass's §5 item 2 named the LP-11 `PolicyProvider` call site the highest-leverage next step. It is **LP-3-gated**, confirmed four independent ways — §3's LP-3 row (*"`StorageProvider` and `PolicyProvider` are held here pending that gate"*), §2's Constraints (*"executor integration (hook points, run-parameter variants) is deferred… once LP-3 is satisfied"*), §4.4's own heading (*"wiring pending LP-3"*), and a standing PLAN Backlog line that **predates the spec pass entirely**. LP-3 requires a `/magic.spec` amendment documenting two independent host-usage contexts; that is governance, not code, so no atomic task can discharge it, and with it LP-16/LP-17/LP-20 stay blocked too. The spec overstated by omitting a gate its own document asserts three times — corrected in Track C rather than planned against. **The second item survived**: LP-15's built-in divergence touches neither hook points nor run-parameter variants, the precise scope §2 defers, so it is outside the gate. Two Stable L1 statements mandate the fix — §4.1's Storage row (amended v1.8.0 by LP-15) specifies an **in-memory** built-in, and LP-2 requires every built-in be "sufficient for in-process testing without I/O" — while the crate ships `NoopStorageProvider`, which discards on `store` and returns `None` on `load`, so it cannot round-trip within a single invocation. The L1's distinction is deliberate, not an oversight: a discarding *audit* built-in is fine because audit is write-only, whereas a discarding *store* breaks the read-back that makes a store a store. Spec-ahead-of-code delta (L1 moved in v1.8.0; the Phase-5 built-in did not), C12.1 in shape like Phases 17/20/21/22. Three guardrails written in from grounding: the existing Phase-5 test `noop_storage_and_policy_compile` **deliberately asserts `load(...).is_none()`**, so it encodes the old contract and must be updated rather than left to fail mysteriously; interior mutability behind `&self` needs `std::sync::Mutex`, whose `lock()` returns a `Result` that must **not** be `unwrap()`ed on a production path (project rule) — recover the poisoned guard instead; and LP-2's "exactly one built-in per interface" forbids adding a second provider alongside the no-op, so this is a replacement. INDEX v1.0.68 held, PLAN v1.29.0 → v1.30.0, RULES parity v1.6.0 held.)
- **Sync-only replan (v2.21.0)**: 2026-07-30 (**no new phase.** Pre-flight fully clean — INDEX v1.0.67 = PLAN basis, 18/18 Stable, 0 Draft, all 18 headers matching, **zero warnings**. The NL-6 conformance arc (Phases 17/20/21/22) is closed, and for the first time in that arc no candidate was carried forward pre-identified — so this cycle ran a **systematic invariant-traceability audit** rather than re-checking a known item: every L1 invariant range compared against its L2 realization spec's compliance table. **Found four stale L2 tables covering sixteen invariants, and established the Backlog itself materially undercounted them.** `l2-nodus-portability` §3 stops at **LP-8** while the L1 defines through **LP-20** — twelve missing, where the Backlog had listed only "LP-17/LP-18/LP-19" (three of twelve), omitting LP-9…LP-16 and LP-20; notably **LP-10** (host-granted authority) is already cited by NL-14/NL-17/NL-20/NL-21 and **LP-11** is the gate `l2-nodus-compensation` already records as vacuous-in-core, both without an LP-side row. `l2-nodus-testing` stops at NT-10 while the L1 defines **NT-11** (differential parity, added v1.1.0 with a §4.7 harness) — absent from the table, the Related-Specifications line, and the crate. `l2-nodus-dialog` stops at DG-8 while the L1 defines **DG-9/DG-10**. `l2-nodus-environment` stops at NE-13 while the L1 defines **NE-14**. Only `l2-nodus-runtime` (via the §3.1 the Phase-19 spec pass added) and `l2-nodus-observability` are complete — and §3.1 is exactly the precedent the others need. **None is plannable as a phase**: each lacks an L2-specified Rust shape, and most are explicitly host-supplied *seams* likely to resolve as vacuous-in-core obligations — a spec-time judgment. Grounded rather than assumed for the closest-to-plannable candidate: NT-11 requires two paths that **both** satisfy the same manifest and produce equal results, whereas the existing LP-3 two-host test compares a satisfying host against a *non-satisfying* one and asserts divergence — the manifest gate, not a parity gate. All 22 phases remain Done; INDEX v1.0.67 held, PLAN v1.28.0 → v1.29.0, RULES parity v1.6.0 held. Computed next step (DA-6): `/magic.spec nodus`, `l2-nodus-portability` first.)
- **Phase 22 Complete**: 2026-07-30 (tracks A/B/T; 6/6 tasks; l2-nodus-testing §10 / l1-nodus-language NL-6 — added `Transpiler::nodus_braced_raw_body` (re-quotes every `raw_lines` token except literal `{`/`}`, which must stay unquoted since `collect_braced_raw_lines`'s depth counter matches token *type*); inverted `@test:` emission to prefer `raw_lines`; added `Validator::w015_test_pair_separator` mirroring `parse_test_body`'s own control flow; added `@macro:` and `human_mode` emission (both previously never emitted at all); widened `full_corpus_ast_equal_after_compact_round_trip` from `.steps` to `ast1 == ast2` — now green across the whole 11-fixture corpus, Phase 22's single acceptance signal. **One planning assumption corrected during implementation**: `@macro:` was planned assuming the braced form, but the real corpus fixture (`macro_expand.nodus`) uses the non-braced `@macro: name` form whose body isn't captured (a known, deferred limitation — macro body expansion — not introduced by this phase), so `MacroBlock.raw_lines` is empty in the corpus today and the braced-body emission path, while implemented, is untested by any fixture; recorded in the PLAN Backlog rather than left implicit. Per-task sensitivity confirmed empirically: temporarily disabling `@macro:` emission failed the widened harness with exactly the predicted diff, then restored. 429 tests pass (was 420; +9); clippy/fmt clean; LP-1 zero-dep preserved.)
- **Phase 22 Planned (v2.19.0)**: 2026-07-30 (resolved the expected spec→task `SYNC_GAP` — PLAN basis v1.0.66 → v1.0.67 from the `l2-nodus-testing` 1.2.0 pass — and opened **Phase 22 — Whole-File NL-6 Round-Trip Closure**, 6 tasks / 3 tracks (A–B, T), Sequential. **Plan-time grounding widened the phase materially beyond the spec pass's subject.** The spec cycle specified the `@test:` fix and named its acceptance signal as widening Phase 21's corpus harness from `.steps` to the whole `WorkflowFile` (§10.5); auditing every `WorkflowFile` field against `to_nodus` showed that signal is **unreachable by fixing `@test:` alone** — `macros` and `human_mode` are **never emitted at all** (zero references to either in `transpiler.rs`), and both hit normative corpus fixtures (`macro_expand.nodus` carries `@macro: greet`; `ticket_triage.nodus` carries a `;; HUMAN MODE` block). Eleven of fourteen fields round-trip today; the phase closes the remaining three, all violating the same already-Stable NL-6 mandate and so needing no new spec. Including `macros`/`human_mode` is what the stated acceptance signal requires rather than scope creep — the Phase-21 precedent for `WorkflowFile.comments` and modifier-value quoting. Three guardrails written in from grounding: emitting from `raw_lines` yields a **flat single-line body** that must not be "improved" to multi-line (newlines are dropped by `collect_braced_raw_lines`, so a re-render breaks the equality it establishes); quoting must **skip structural tokens** (`raw_lines` holds values with no type info); and `human_mode` **must be emitted last** (`collect_comment_block` greedily consumes every following Comment token and would absorb free-standing comments, corrupting the `comments` field Phase 21 just fixed). One spec inaccuracy flagged rather than acted on: §10.4(a)'s "reproduces the body in the form the author wrote it" overstates — it reproduces the token sequence, not line breaks; queued for a patch-level correction on the next `/magic.spec` touch (Phase-18 `$restart` precedent). INDEX v1.0.67 held, PLAN v1.26.0 → v1.27.0, RULES parity v1.6.0 held.)
- **Sync-only replan (v2.18.0)**: 2026-07-30 (**no new phase — the plan is saturated.** Pre-flight fully clean: INDEX v1.0.66 = PLAN basis, 18/18 Stable, 0 Draft, no orphan/phantom, all 18 file headers matching INDEX exactly, **zero warnings** (Phase 21 touched no spec files, so last cycle's mechanical `SYNC_GAP` stayed resolved). **The item predicted last cycle as "very likely the next C12.1 conformance phase" did not survive grounding — the prediction was wrong and is corrected rather than acted on.** STATE.md had recorded the `@test:` block re-emission gap as having "a Stable owning spec and no design fork"; `l2-nodus-testing.md` §2 plus the parser say otherwise. Three findings, each moving the item from code-shaped to spec-shaped: the spec's own model doesn't match the parser (§2 says old-style bodies leave structured fields empty, but a real braced body populates `input` *and* `raw_lines` while leaving `expected` empty, so neither §2's branch nor `to_nodus`'s `has_structured` branch describes reality); `expected: { status = SUCCESS }` never parses at all (`parse_test_body` matches only `key : value` triples, so the `=` form is skipped and that fixture's assertion has never been evaluated — a latent correctness defect independent of round-tripping); and decisively, **quoting information is destroyed at lex time and is unrecoverable from the AST** (`"T-001"` is stored as `T-001` and re-lexes as `T` + `-001`, exactly the observed corruption), so no emitter-only change can restore NL-6 AST-equality. That makes the resolution a genuine design fork — retain quoting in `TestBlock`? re-quote heuristically? or scope `@test:` bodies out of NL-6 explicitly? — categorically unlike Phases 17/20/21, where the Stable mandate was unambiguous and only the code diverged. The `core: schema.nodus` truncation was checked as the one other candidate and is likewise design-blocked (`l1-nodus-language` §113 specifies `core:` only as "loads the named schema", saying nothing about dots). All 21 phases remain Done; INDEX v1.0.66 held, PLAN v1.25.0 → v1.26.0, RULES parity v1.6.0 held. Computed next step (DA-6): `/magic.spec nodus`, `@test:` cluster.)
- **Phase 21 Complete**: 2026-07-30 (tracks A/B/C/T; 7/7 tasks; l2-nodus-control-flow §3/l2-nodus-runtime §3 — exhaustive `nodus_stmt` compact dispatcher (no `_` catch-all) covering all 9 `Stmt` variants plus `Step.sub_steps`/`Step.retry`; full-corpus NL-6 AST-equality harness in `tests/parity.rs` (spec patched to name the real location, not `workflows.rs`); 3 new v0.7 fixtures completing the normative corpus. **The Track B harness surfaced 4 unplanned findings, all fixed in this phase**: `Step.retry` (`~RETRY:n`) was completely unemitted; `Step.sub_steps` was wrongly assumed dead on the parse path at plan time — `ticket_triage.nodus`'s own step 2 already exercises it, caught before the wrong assumption shipped; the `$var = expr` shorthand (internally `Stmt::Command{name:"ASSIGN",..}`) couldn't round-trip via generic call syntax since "ASSIGN" isn't a lexable command name (fixed via `nodus_command_or_assignment`); `WorkflowFile.comments` and modifier-value quoting were silently dropped, blocking every fixture. A fifth finding — `@test:` block re-emission losing fidelity for structured values — was deliberately left unfixed: real, but a pre-existing (Phase 6) defect owned by `l2-nodus-testing.md`; the harness was scoped to `.steps` rather than the whole file, and the gap flagged in the Backlog. 420 tests pass (was 405; +15); clippy/fmt clean; LP-1 zero-dep preserved.)
- **Phase 21 Planned (v2.16.0)**: 2026-07-30 (see below)
- **Phase 21 Planned (v2.16.0)**: 2026-07-30 (resolved the one mechanical `SYNC_GAP` — PLAN basis v1.0.65 → v1.0.66, from Phase 20's Track C, whose only two changed registry rows were the ones that phase itself corrected — and opened **Phase 21 — Compact-Form Round-Trip Fidelity for Control Flow (NL-6)**, 7 tasks / 4 tracks (A–C, T), Sequential. **Third instance of the Phase 17 / Phase 20 C12.1 shape, and the largest.** `Transpiler::to_nodus` drops every control-flow step; three Stable specs mandate the opposite — `l1-nodus-language` NL-6 (*"compact → human → compact must produce an AST-equal result"*), `l2-nodus-runtime` §3/§4.5 (*"compact, lossless"*), `l2-nodus-control-flow` §3/§4 (*"each new node gains compact and human emitters"*). **Proved empirically at plan time rather than inferred from a code read** — the Phase-20 Track-B lesson applied: a temporary probe transpiled the Phase-20 `?SWITCH` fixture and re-parsed it, yielding `orig steps=2 reparsed steps=1` with the whole `?SWITCH` step missing from the emitted `@steps:` block; probe reverted after observation. Two structural findings explain the lifespan of the defect: the emitter asymmetry is total (7 human emitters with full control-flow coverage vs 2 compact with none — the spec's "each new node gains compact and human emitters" is half-true, the human half), and **the guard the spec claims exists was never built** (`l2-nodus-runtime` §3 asserts an AST-equality round-trip test in `workflows.rs`; that file contains only the `TranspileMode::Compact` dispatch), while the round-trip tests that do exist in `parity.rs` assert only `ast2.header.name`, making a dropped step invisible — the Phase-17 pattern one level up. Scope held to the real violation: three further probe-surfaced oddities (`core: schema.nodus` → `schema`, injected `mode: production`, `@err:` raw re-spacing) were each verified **AST-stable** and excluded, since NL-6 mandates AST-equality not source identity; the `core` truncation is recorded as its own Backlog item. Also retires the framing of the standing "normative fixture corpus coverage" Backlog entry — logged as "no defect sits behind this", it now has one, since the corpus is what the round-trip harness sweeps. INDEX v1.0.66 held, PLAN v1.23.0 → v1.24.0, RULES parity v1.6.0 held.)
- **Phase 20 Complete**: 2026-07-30 (tracks A/B/C/T; 6/6 tasks; l2-nodus-control-flow §3/§4.5 — `try_parse_command_from_string` now splits a trailing `→ $target` off before the paren split, fixing all three branch-action call sites (`?SWITCH` arms/default, `?IF` actions, `@err:` handlers) with one change; the executor needed no change, as predicted. **Track B needed no code change**: re-grounding at implementation time showed `collect_vars_stmt`'s `Stmt::Switch` arm already walked `sw.arms`/`sw.default` via `collect_vars_cmd` (which already declares `pipeline_target` and marks uses) — the plan-time "second coupled defect" was itself a grounding error, a `sed` line range that cut off mid-match-arm; corrected honestly, 2 regression tests added instead of a fix. Track C reconciled both `l2-nodus-restart.md` and `l2-nodus-compensation.md` to their as-built implementations (both → 1.0.1): the restart spec's `Signal::Restart`/`NODUS:RESTART_SCOPE` design was never built (bare validator code `E019` instead, matching the E010/E013/E014/E016/E017 validator-only precedent) and its §5/INDEX wording no longer misgroups `$restart` into `RUNTIME_OWNED_VARIABLES`; the compensation spec's planned `uncompensable`/`CompensationOutcome` were never built (nothing reads either) and its `UNPUBLISH` example replaced with the real `NOTIFY` command — plus two findings not previously flagged: a never-built "explicit compensate request" arming trigger removed, and three dangling `§7` cross-references (the document has no §7) fixed to `§6`. `INDEX.md`'s own top-level version bumped 1.0.65 → 1.0.66 to match both corrected rows (Phase-17 Track-C precedent; `PLAN.md`'s basis pointer resyncs at the next `/magic.task`). Retired the Phase-18 `restart.rs` AST workaround: `restart_count_progresses_and_context_is_fresh_each_attempt` now runs a real parsed fixture through `run_with_audit`, proving the Track A fix end-to-end for the very feature that first surfaced the defect. 405 tests pass (was 397; +8); clippy/fmt clean; LP-1 zero-dep preserved.)
- **Phase 20 Planned (v2.15.0)**: 2026-07-30 (opened **Phase 20 — Branch-Action Pipeline Targets & `?SWITCH` Binding Conformance** (l2-nodus-control-flow §3/§4.5), 6 tasks / 4 tracks (A–C, T), Sequential. Second-ever phase opened against *existing* Stable spec content rather than newly-authored content (after Phase 17), and the same C12.1 fix-to-regain-conformance shape — registry is unmoved at INDEX v1.0.65 with 18/18 Stable, 0 Draft and no orphan, so there is no new spec content, yet the plan is **not** saturated. `l2-nodus-control-flow.md` §3's NL-10 row (Stable v1.0.1) mandates that `?SWITCH` arm actions bind their targets in declaration order; the realized code cannot. **Plan-time grounding widened the promoted Backlog item and split it in two**: what was recorded as a single inline-`?IF` defect is structural rather than an edge case (`try_parse_command_from_string` returns `..Default::default()` — no code path can ever set `pipeline_target`) and shared by three call sites (`@err:` handlers, `?IF` branch actions, `?SWITCH` arms + `*` default), so the helper cannot be fixed for one caller alone. Grounding surfaced a **second, coupled defect the Backlog did not record**: `collect_vars_stmt`'s `Stmt::Switch` arm visits only the scrutinee and never walks arm actions, so nothing an arm binds is declared — making the sequencing load-bearing, since the parser fix alone would turn a bound target into an `E004` false positive on any later step reading it, exactly the `~MAP`/`$it` failure mode Phase 17 cured. Verified rather than assumed: the executor is already correct (`execute_switch` → `execute_command` honors `pipeline_target`), so no executor change is in scope, and coverage is nil — zero tests touch any of the three helper call sites. Track C clears the whole accumulated spec-to-as-built cluster from Phases 18/19 in one file-independent pass, per the Phase-17 Track-C precedent. INDEX v1.0.65 held, PLAN v1.22.0 → v1.23.0, RULES parity v1.6.0 held.)
- **Phase 19 Complete**: 2026-07-30 (tracks A/B/T; 5/5 tasks; l2-nodus-compensation — `TildeCompensate` token + `Step.compensation: Option<CommandCall>` + trailing `~COMPENSATE: CMD(args)` clause + transpiler round-trip; `NODUS:COMPENSATION_FAILED`; completed-effect ledger populated at `run_step_with_retry`'s existing success-detection point; LIFO drain via `Vec::pop()` armed on `Failed`/`Aborted`, continuing past a failed compensation; realizes NL-22 — **both NL-22 and NL-23 now closed, clearing this cycle's spec pass in full**. 397 tests pass (was 387; +10); clippy/fmt clean; LP-1 zero-dep preserved. Dropped the spec's planned parallel `uncompensable` record + outcome enum (nothing reads either). Found and fixed two parser defects self-contained to the new `~COMPENSATE` path: a recursive `parse_command_call` call swallowing the next line via a redundant `skip_to_newline()`, and the spec's own `UNPUBLISH` example not being a real command. Proactively extended E004 to see compensation-clause variables.)
- **Phase 18 Complete**: 2026-07-30 (tracks A/B/T; 6/6 tasks; l2-nodus-restart — `restart_max` in `§runtime:`, `$restart`/`$restart_count` reserved variables (writable vs. runtime-owned split), `E019` static validator rejection of nested restart requests, `Executor::execute_with_restart` bounded attempt loop wrapping `execute_inner`, `NODUS:RESTART_LIMIT`; realizes NL-23. 387 tests pass (was 373; +14); clippy/fmt clean; LP-1 zero-dep preserved. Two deliberate deviations from the spec's literal §4.2/§4.6 flagged in PLAN.md Backlog for a future spec patch: `Signal::Restart` dropped (validator-only enforcement matches the E010/E013/E014/E016/E017 precedent) and `RESTART_SCOPE` landed as bare validator code `E019` instead of a `vocab.rs` constant. Also found and worked around (not fixed) a pre-existing, unrelated parser defect: inline `?IF cond → CMD() → $target` never attaches its trailing pipeline target.)
- **Phase 18/19 Planned (v2.14.0)**: 2026-07-30 (opened **two** phases, clearing both orphans the spec pass created — **Phase 18 — Bounded Whole-Run Self-Restart** (`l2-nodus-restart`, 6 tasks / 3 tracks A–B, T) and **Phase 19 — Compensation Seam** (`l2-nodus-compensation`, 5 tasks / 3 tracks A–B, T), both Sequential. Two phases rather than one: the specs are independent (soft reference only), each carries its own vertical-slice order, and this matches the one-phase-per-L2-spec precedent of Phases 10–13. Phase 18 executes first — more tractable, and its single control-flow-changing task lands last, after every guard it relies on is provable. Plan-time grounding caught a self-inflicted spec inaccuracy rather than propagating it: `l2-nodus-restart` §5/INDEX loosely group `$restart` into `RUNTIME_OWNED_VARIABLES`, which would make the request variable an `E013` error to assign; the phase file plans to §3 (authoritative — only `$restart_count` is runtime-owned) and carries the discrepancy as a guardrail, with the wording flagged for a patch-level spec correction. INDEX v1.0.65, PLAN v1.21.0 → v1.22.0, RULES parity v1.6.0 held.)
- **Prior (v2.13.0)**: 2026-07-30 (**sync-only replan — no new phase.** Registry advanced INDEX v1.0.62 → v1.0.64 via two additive `l1-nodus-language` refinements authored between sessions (→ 1.14.0): **NL-22** compensation seam and **NL-23** bounded whole-run self-restart. Both carried as pending L2 Invariant-Compliance obligations — neither has a `Stable` L2 spec defining its Rust shape, so no atomic task with a concrete `Verify` line is authorable (C10), exactly as each invariant's own Document History anticipated. Grounded, not assumed: the crate has zero traces of compensation/LP-11/LP-17/restart, and NL-22 composes three seams absent from the crate (LP-11 gate, LP-17 settlement, NL-12 resume-for-effects) — a real spec-time design fork, recorded in the Backlog alongside the assessment that NL-23 is the lighter and more tractable of the two. Also flagged: `l2-nodus-runtime.md` §3 covers only NL-1…NL-10, so NL-11…NL-23 have no row in the file each L1 entry names as their carrier. All 17 phases remain Done; PLAN v1.20.0 → v1.21.0; RULES parity v1.6.0 held.)
- **Phase 17 Complete**: 2026-07-25 (tracks A/B/C/T; 5/5 tasks; l2-nodus-control-flow §4.5 — `$it` declared in the `Stmt::Map` arm of `collect_vars_stmt`; `~MAP` end-to-end tests through `workflows::run` + the fixture corpus's first v0.7 construct (`map_transform.nodus`); Phase-16 `Executor::execute` validate-gate bypass retired; §4.5 patched 1.0.0 → 1.0.1 recording the `E004`↔`$it` interaction. `~MAP` is now reachable through every validated public entry point for the first time since Phase 11. 373 tests pass (was 365; +8); clippy/fmt clean; LP-1 zero-dep preserved. INDEX v1.0.61 → v1.0.62.)
- **Phase 17 Planned (v2.12.0)**: 2026-07-25 (opened **Phase 17 — `~MAP` Conformance & End-to-End Reachability** (l2-nodus-control-flow §4.3/§4.4), 5 tasks / 4 tracks (A–C, T), Sequential. First phase opened against *existing* Stable spec content rather than newly-authored content: §4.3/§4.4 mandate `$it` as `~MAP`'s implicit binding, but the realized `E004` rule rejects every `~MAP` workflow, making the construct unreachable through all validated entry points since Phase 11. Plan-time grounding localized it to one `collect_vars_stmt` arm and confirmed `~MAP` is the only construct with an undeclared implicit binding. Needs no new spec (C12.1 fix-to-regain-conformance). INDEX v1.0.61 held (spec version bumps during run), PLAN v1.19.0 → v1.20.0, RULES parity v1.6.0 held.)
- **Phase 16 Complete**: 2026-07-25 (tracks A/B/C/T; 6/6 tasks; l2-nodus-observability §4.9 — `EventAnnotations`/`Anomaly`/`Durability` carrier on all 10 `ExecutionEvent` variants, 4 `Measurement` token classes on `ModelResponse`, `SourceRef` derivation lineage on `LoopIteration` + the previously-missing `~MAP` emission, read-side `classify_trace`, durable-only `seq` discipline documented on `emit`; realizes HO-8/9/10/11/13/16/17 — **all 20 HO invariants now closed**; 365 tests pass (was 352; +13); clippy/fmt clean; LP-1 zero-dep preserved. Found and worked around a pre-existing validator gap: `E004` doesn't recognize `~MAP`'s implicit `$it` binding, rejecting every `~MAP` workflow through `run_with_audit` — flagged in PLAN.md Backlog for a future fix, not expanded into this phase.)
- **Phase 16 Planned (v2.11.0)**: 2026-07-24 (opened **Phase 16 — Event Annotations, Cost, Lineage & Completeness** (l2-nodus-observability §4.9), 6 tasks / 4 tracks (A–C, T), Sequential — realizes the final seven HO invariants and **closes all 20**. One `EventAnnotations` carrier keeps the all-variant churn to one field, not four. Plan-time finding folded in: `execute_map` emits no events, so HO-13's `~MAP` example needs an emission added there. INDEX v1.0.60 → v1.0.61, PLAN v1.18.0 → v1.19.0, RULES parity v1.6.0 held.)
- **Phase 15 Complete**: 2026-07-24 (tracks A/B/C/T; 6/6 tasks; l2-nodus-observability §4.8 — `Measurement` type + retype of 5 obtainable-failure numerics, `seq`/`correlation_id` on all 10 ExecutionEvent variants, `Executor::emit` single choke point, `RunManifest.event_count` gap-check identity, `handle_dialog` fabricated-zero fix; realizes HO-7 + HO-14; 352 tests pass (was 345; +7); clippy/fmt clean; LP-1 zero-dep preserved)
- **Phase 15 Planned (v2.10.0)**: 2026-07-24 (opened **Phase 15 — Aggregation-Safe Event Stream** (l2-nodus-observability §4.8), 6 tasks / 4 tracks (A–C, T), Sequential — realizes HO-7 + HO-14, the two cross-cutting members of the event batch, batched so the all-variant churn happens once. INDEX v1.0.59 → v1.0.60, PLAN v1.17.0 → v1.18.0, RULES parity v1.6.0 held. Pass-2 batch (HO-8/9/10/11/13/16/17) deferred.)
- **Phase 14 Complete**: 2026-07-24 (tracks A/B/C/T; 7/7 tasks; l2-nodus-observability §4.7 — ExecutionMode/SimFidelity/Determinism/ReproRecipe/FaultIdentity types, step_identity definition-derived cross-run identity, StepError.fault_identity message-independent grouping, nodus-computed repro (digest/version/determinism), Executor::execute_with_manifest_context host-declaring entry point; realizes HO-12/15/18/19/20; 343 tests pass (was 335; +8); clippy/fmt clean; LP-1 zero-dep preserved)
- **Phase 14 Planned**: 2026-07-24 (v2.9.0: opened **Phase 14 — Run-Manifest Identity & Reproducibility** (l2-nodus-observability §4.7), 7 tasks / 4 tracks (A–C, T), Sequential — realizes HO-12/15/18/19/20 (the manifest/identity cluster; a spec-ahead-of-code L2 update, not a new orphan). INDEX v1.0.58 → v1.0.59, PLAN v1.16.0 → v1.17.0, RULES parity v1.6.0 held. Event-stream batch HO-7…HO-11/13/14/16/17 deferred.)
- **Phase 13 Complete**: 2026-07-24 (tracks A/B/C/T; 9/9 tasks; l2-nodus-config — ConfigDecl/ConfigField/FieldConstraint AST + `parse_config` + transpiler round-trip, `CONFIG_INVALID` code + `check_config_values` shape check + `AcceptedConfig` value model, `ConfigProvider`/`ExtensionRole::Config` + `run_with_config[_and_audit]` public API; realizes NL-20; 335 tests pass (was 292; +43); clippy/fmt clean; LP-1 zero-dep preserved)
- **Phase 13 Planned**: 2026-07-24 (v2.8.0: opened **Phase 13 — Declarative Configuration Surface** (l2-nodus-config), 9 tasks / 4 tracks (A–C, T), Sequential — realizes NL-20 (§config), the net-new-weight obligation flagged by the prior sync; INDEX v1.0.57 → v1.0.58, PLAN v1.15.0 → v1.16.0, RULES parity v1.6.0 held. Prior v2.7.0 Sync: registry INDEX v1.0.47 → v1.0.57; RULES parity re-synced v1.5.0 → v1.6.0. New additive invariants NL-19/20/21 · LP-17/18/19 · HO-14…HO-20 carried as pending L2 Invariant-Compliance obligations — no new phase opened (no Stable L2 spec to decompose into verifiable tasks); NL-20 §config flagged for a dedicated l2-nodus-config spec. All 12 phases remain Done (Archived). Prior 2026-07-10 (v2.6.0): Phase 12 — Environment & Evaluation complete: all 9 tasks / 5 tracks (A–D, T) landed in `crates/nodus` — EnvironmentProvider + StubEnvironment, Reward/GradingMode/Budget/CandidateResult, ExtensionRole::Environment, run_with_environment public API, EnvInteraction trajectory side-band (no new ExecutionEvent variant). All-additive; zero new dependency (LP-1 preserved). Gates green: 292 tests (was 265) + clippy + fmt + doc; downstream cronus-cli unaffected. Prior v2.6.0: phase opened. Prior v2.5.1: l1-nodus-environment absorbed into Phase 0, Phase 11 reconciled → Done+Archived, RULES parity v1.4.0 → v1.5.0.)
- **Phase 3 Planned**: 2026-06-24 (9 tasks, tracks A/B/C/D/T)
- **Phase 4 Complete**: 2026-06-24 (9 tasks, tracks A/B/C/T; all archived)
- **Phase 5 Complete**: 2026-06-24 (7 tasks, tracks A/B/C/T; all archived)
- **Phase 6 Complete**: 2026-06-24 (9 tasks, tracks A/B/C/T; all archived; 204 tests pass)
- **Phase 7 Complete**: 2026-06-27 (9 tasks, tracks A/B/C/T/D; all archived; l2-nodus-portability v1.1.0 Stable via LP-8; 217 tests pass)
- **Phase 8 Complete**: 2026-06-27 (7 tasks, tracks A/B/T; all archived; l2-nodus-errors — 24-code taxonomy; 222 tests pass)
- **Phase 9 Complete**: 2026-06-27 (tracks A/B/T; all archived; l2-nodus-registries — closed flag/validator/type registries + advisory W011/W012/W013; 228 tests pass)
- **Phase 10 Complete**: 2026-06-27 (tracks A–E/T; all archived; l2-nodus-dialog — ASK/CONFIRM + Status::Paused + DialogProvider + ExtensionRole::Dialog; 237 tests pass)
- **Phase 11 Complete**: 2026-06-28 (tracks A/B/C/D/T; all archived; l2-nodus-control-flow — !HALT/!PAUSE + ?SWITCH + ~MAP + ~RETRY:n; lexer/AST/parser/executor/validator; 265 tests pass)
- **Phase 12 Complete**: 2026-07-10 (tracks A/B/C/D/T; all archived; l2-nodus-environment — EnvironmentProvider + StubEnvironment, Reward/GradingMode/Budget/CandidateResult, ExtensionRole::Environment, run_with_environment public API; 292 tests pass)
- **Backlog**: operators [MATCHES/PCRE design fork], @needs (blocked), @ON priority, macro execution; Storage/Policy executor integration (LP-3)
- **Maintainer**: Core Team
